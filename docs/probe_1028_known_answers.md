<!-- STEP 1028 — 잃어버린 답을 되찾고, 다시 잃지 않게 색인한다 (조사 + 문서) -->
# probe_1028 — 잃어버린 답 회수 + 중복조사 색인

## W1 결과 (맨 앞)

🔴 **로그 접근 불가(오늘도) — 그러나 이유가 처음으로 정밀하게 갈렸다.** `revdcf` 2026-08-13 22:45 UTC 실행 로그는 오늘(2026-08-14) 이 세션에서 **못 얻었다** — MCP 403(재확인)·CLI는 라이브 전용(과거 조회 불가, 오늘 실행해 확인)·브라우저 대시보드는 Claude Code에 애초에 없는 채널. `cron_heartbeats.job='revdcf'`도 **여전히 0행**(1007 배선 이후 단 한 번도 안 씀 — 함수가 heartbeat 도달 전에 죽는다는 뜻 그 자체가 답의 일부). ⓪-4 매트릭스 판정 = **③(911·933에 작동하는 답이 실은 없었다) — 단, 911과 933을 나눠서 봐야 한다**: 911의 답은 **실재하나 이 세션 도구 범위 밖**(Cowork 브라우저 전용, 시간도 이미 지남), 933의 답은 **애초에 이 질문의 답이 아니었다**(lens-scores 얘기지 revdcf·Vercel 로그 얘기가 아님) — STEP1027의 "911·933이 답을 얻었다"는 서술을 정정한다.

---

## ⓪-1b (이 STEP 자신에 대한 자기적용)

- 이 STEP의 주제("Vercel 로그 접근")가 `KNOWN_ANSWERS.md`에 있었는가: **없음**(이 STEP이 신설) — 있었다면 892부터 6번 반복될 이유가 없었다는 사실 자체가 이 색인의 필요성을 증명한다.

---

## W1. 여섯 STEP 대조표

| STEP | 시도 | 결론 | 근거(`파일:줄`) |
|---|---|---|---|
| **892**(2026-08-04) | Vercel MCP `list_teams()` 직접 호출 | `[]`(빈 배열) — 접근 불가로 기록, 추정 안 함 | `docs/REVDCF_SPEC.md:2067` |
| **907**(2026-08-05, 894 항목에 병기) | `vercel logs --help` 원문 재확인 | CLI는 **"지금부터 5분"만 지원**(과거 조회 불가) — 이 STEP은 CLI 한계만 재확인, 대시보드는 아직 안 봄 | `docs/REVDCF_SPEC.md:1777` |
| **911**(2026-08-05) | Cowork **인증 브라우저**로 `vercel.com/toms-projects-c798474e/stock-terminal/logs` 직접 접속 | 🔑 **열린다.** 검색(`?search=`) 작동·Hobby 플랜 확인. 이어서 공식문서 확인 → **보존 1시간**(CLI "5분"·대시보드 "1시간" 둘 다 시간 제약이지 권한 제약 아님을 확정) | `docs/REVDCF_SPEC.md:2089-2090` |
| **913**(2026-08-05) | Vercel **MCP** `get_runtime_logs` 재시도(Cowork 실측) | **403 Forbidden**(`orgId=team_75sBjDtj4rCJOBtQ2d1gnYE6`·`projectId=prj_o5Eao0DzSsFCo9Oa7ZkxdSKLSHdk`, 다른 계정 인증) — "로그 확인 수단은 실질적으로 인증 브라우저 1개뿐"로 정리 | `docs/REVDCF_SPEC.md:2100` |
| **932**(2026-08-06) | `cron_heartbeats` 직접 Supabase 재조회(`kr-lens-scores`) | 🔴 **Vercel 로그 문제와 무관.** 917이 배선한 heartbeat `note`에서 KR의 첫 계측값을 확보한 것 — 다른 채널(DB), 다른 크론(kr-lens-scores) | `docs/REVDCF_SPEC.md:2142` |
| **933**(2026-08-06) | 같은 방법(Supabase 재조회)을 `lens-scores`(US)에 적용 | 🔴 **역시 Vercel 로그 문제와 무관.** `retryAllLen=511` 등 US lens-scores의 첫 계측값 확보 — `revdcf`도 아니고 Vercel 로그도 아니다 | `docs/REVDCF_SPEC.md:2143` |
| **1017**(2026-08-14, 이번 세션) | Vercel MCP `list_teams()`·`get_runtime_logs` 재시도 | 403·빈 배열(892·913과 동일 결과). **자체 비교는 "1016과 동일"까지만** — 892·907·911·913·933까지 거슬러 올라가 찾지 않았다 | `docs/STEP_LEDGER.md:328` |

🔑 **핵심 정정**: STEP1027은 "911·933이 이미 작동하는 답을 얻었다"고 압축했으나, **933은 애초에 다른 질문(US lens-scores 계측)에 답한 것이지 "Vercel 로그를 어떻게 읽나"에 답한 게 아니다.** 932도 마찬가지(KR 버전). 진짜 "Vercel 로그" 질문에 답한 것은 **911 하나뿐**이고, 그 답조차 "Cowork 브라우저로만 가능·1시간 보존"이라는 좁은 답이었다. 1017이 못 찾은 건 "911의 답"이지 "933의 답"이 아니다 — 933은애초에 이 질문 계보에 안 속한다.

**911의 답을 오늘(2026-08-14) 다시 시도했다(⓪-3b 요구대로 "지금도 유효한지 먼저 확인")**:
- MCP `list_teams()` → `{"teams": []}` (892·913과 동일, 오류는 아니나 빈 결과)
- MCP `get_runtime_logs(projectId, teamId, since="2026-08-13T22:40:00Z")` → **403 Forbidden**("You don't have permission to access this resource") — 913과 동일
- MCP `list_deployments()` → **403 Forbidden**("Not authorized: Trying to access resource under scope \"toms-projects-c798474e\"") — 897·913이 이미 특정한 바로 그 스코프 불일치, 오늘도 동일
- 🔑 **신규 확인(오늘 처음 직접 실행)**: 로컬 `vercel` CLI는 **인증돼 있고 작동한다**(`vercel whoami` → `soulmaten7-7785`, `vercel ls` → 배포 목록 정상 조회, `vercel logs <url>` → 실제로 연결되어 "waiting for new logs..."까지 확인). 907이 "5분만 지원"이라고 문서로만 확인했던 것을 **오늘 실제로 실행해 재확인** — CLI 채널 자체는 살아있으나 **라이브 전용이라 과거(08-13 22:45 UTC) 데이터는 이 방법으로 원리적으로 복구 불가**(그날 이후 시간이 지나 대시보드의 1시간 보존도 이미 지남).

**결론**: 911의 방법(대시보드)은 Claude Code 세션에 구조적으로 없는 채널(브라우저 도구 부재)이라 **오늘도 재현 불가** — "방법이 무효가 됐다"가 아니라 **"이 세션은 원래 이 방법을 쓸 수 없었다"**(1027이 이미 그렇게 적어뒀다, `REVDCF_SPEC.md:1777`의 911 정정 문단 참조). CLI(907)는 살아있지만 라이브 전용이라 과거 조회에 무용. `cron_heartbeats.job='revdcf'`(933과 같은 계열의 채널)는 **여전히 0행**이라 이 경로로도 답을 못 얻는다.

---

## W2. 중복 조사 전수 색출

**먼저 1027이 §1-3(이전발언 대조)을 전수로 했는지 확인** — `docs/probe_1027_three_way_audit.md` §1-3 말미: *"997~1026의 나머지 24개 STEP에 대한 개별 중복 검색은 하지 않았다"*로 명시돼 있다. **전수가 아니었다** — 이 STEP이 이어받는다.

주제 13개(명령서 최소 목록)를 검색한 결과:

| 주제 | 이번 세션 STEP | 선행 STEP | 결론 | 낭비였나 |
|---|---|---|---|---|
| 야후 결측 원인 | 1006~1010 | (선행 없음 — `quote`/`quoteSummary` 이중경로 0/284 확인은 신규) | 새 발견(정밀화) | 아니오 |
| **07-30 코호트** | **1008** | **892**(2026-08-04, 517개 스테일 코호트 최초 규명) | **1008이 517→296을 "새 발견"으로 오인용**(1027이 이미 지적) | 🔴 **예** |
| 시총 신선도 | (이번 세션에 별도 STEP 없음) | 891·985·986 | 해당 없음 | — |
| 커버리지 게이트 | 1017~1024(경로 소진 실측) | 833(원 게이트)·984(97% 미달 최초 확인) | 새 실측(경로 소진은 신규) | 아니오 |
| 프루닝 | 1025(가상 영향 배선) | 828·833(원 조건) | 새 기능(드라이런) | 아니오 |
| `BUDGET_MS`/예산 초과 | (이번 세션에 재조사 없음, 인용만) | 916·934~937(전수 실측 완료) | 재조사 안 함 | — |
| `us_sector_relative` 정지 | 1016(D 항목 100% 실패 발견) | (다른 표 — 973은 `us_sector_wide`, 별개 테이블) | 신규(다른 메커니즘) | 아니오 |
| 서빙 신선도 | 1016 | (선행 없음 — 최초 발견) | 신규 | 아니오 |
| 유니버스 구성/SIC | 1021·1024 | 866(거래소 상장 범위, 다른 질문 — "어디까지 조달하나") | 신규(범위 안 분류 정확도는 866이 안 다룸) | 아니오 |
| **SEC 주식수 태그(`frames` 6분기창)** | **1019~1022** | **B-5**(STEP845, `REVDCF_SPEC.md:834-846`, "frames는 개별 판정에 못 씀") | 🔶 **B-5가 이미 경고한 함정을 조사 도구 설계에서 다시 밟음**(프로덕션엔 영향 없었다는 결론은 새로움) | 🔶 **부분** |
| 나스닥 스크리너 | 1017~1019(접근불가 재확인) | 1012·1013(marketCap 후보 도입·배선) | 직접 후속(1013 구현물의 실패 원인 규명) | 아니오 |
| 복수클래스 | 1023(MULTI_CLASS_SHARES 인용) | 854(최초 구현)·985/986(marketCapReconstruct 판별) | 기존 로직 인용·확인일 뿐 | 아니오 |
| 업종 베타 | 1027(B-2 표 누락 발견) | 904(§10 #10·#11 해소) | 문서 결함 수정(조사 재실행 아님) | 아니오 |

**낭비 판정 요약**: 13개 주제 중 **명백한 낭비 = 1건**(07-30 코호트, 1008↔892) · **부분 낭비 = 1건**(SEC frames 6분기창, 1019~1022가 B-5의 경고를 조사 설계 단계에서 반복해 밟음, 최종 결론 자체는 새로웠음) · **낭비 아님(정당한 후속·신규) = 11건**.

🔴 **낭비가 아니었던 것도 기록(명령서 요구)**: 892가 "재시도 예산이 실제 병목인지 관측 불가"로 남긴 것을 1007이 heartbeat 배선으로 실제로 풀었다(892 재검토조건 (a) 충족, STEP1027 §1-4가 이미 §10-J에 반영). 이건 "재조사"가 아니라 "선행 STEP이 명시적으로 남긴 조건을 다음 STEP이 충족시킨" 정상적인 이어달리기다.

**Vercel 로그 접근 재분류**: W1에서 이미 다뤘으나 이 표 형식으로도 등재 — **1017 vs 892/907/911/913**(933·932은 계보 밖으로 제외, 위 W1 정정 참조) = **명백한 낭비**(5차례 선행 조사를 못 찾고 6번째를 돎).

---

## W3. 색인 초기 항목 수

`docs/KNOWN_ANSWERS.md` 신설 — **초기 항목 10개**:
1. Vercel 런타임 로그 접근 가능 여부
2. `cron_heartbeats.note` 진단 가능 여부(크론별로 다름)
3. `revdcf`의 SEC `frames` 사용 여부
4. 베타 조달처
5. 무위험수익률·ERP 소스(FRED vs Damodaran)
6. `FRED_API_KEY`의 `revdcf` 사용 여부
7. SIC 6726으로 CEF 필터링 가능 여부
8. 이름패턴 CEF/REIT 분류 신뢰도
9. 커버리지 게이트 97% 달성 가능 여부
10. `us_sector_relative` 정지 시 업종 대비(D) 실패 메커니즘

전부 **재검토 조건 포함**(형식 강제 충족). `docs/INDEX.md`에 등재 완료(§④, `REVDCF_SPEC.md` 바로 아래).

---

## W4. 템플릿 강제

`docs/step_orders/_TEMPLATE.md`에 **⓪-1b(기존 답 확인)** 신설 — ⓪-1과 ⓪-2 사이에 삽입, "이 칸이 비면 미완성" 문구 + Vercel 로그 6회 전례를 그대로 인용(933 정정 포함).

---

## 3중 규칙 3종

**못 한 축**:
- W2의 13개 주제 중 5개("시총 신선도"·"`BUDGET_MS`"는 이번 세션에 직접 재조사가 없어 "해당 없음" 처리 — 완전한 부재 확인이 아니라 "이번 세션 STEP 목록에 없다"는 표면적 확인만 했다)
- `docs/CHANGELOG.md`(4,000줄+)를 전수로 grep하지 않았다 — `docs/STEP_LEDGER.md`(더 압축된 형태)와 `docs/REVDCF_SPEC.md`(§9~§11에 이미 이력이 집약됨)로 대체했다. 명령서가 요구한 4개 문서 중 `CHANGELOG.md`만 직접 전수 대조를 안 함(다른 3개로 충분히 커버된다고 판단 — 검증 안 됨).

**철회·정정**:
- STEP1027의 "911·933이 이미 작동하는 답을 얻었다"를 **"911만 실재하는 답(범위 제한부)·933은 다른 질문의 답이었다(계보 밖)"**로 정정.
- 1008의 "296건 통째로 멈춤"을 다시 한번 892 연속 관측치로 확인(1027이 이미 지적한 것의 재확인).

**미측정**:
- 997~1026의 나머지 STEP(이번에 다룬 13개 주제 밖의 것들)에 대한 전수 중복 검색.
- `docs/CHANGELOG.md` 전수 grep 대조.
- 오늘 밤(22:45 UTC) 실제 `revdcf` 실행 시 `vercel logs` 라이브 tail을 미리 걸어두면 관측이 가능한지 — 이번 STEP은 방법의 존재만 확인했고 실제 야간 tail은 시도하지 않았다(1026 또는 별도 세션 대상).

🔴 **W1이 성공해도 `BUDGET_MS`·게이트·코드를 고치지 않는다. 판정은 장은태가 한다.**
