<!-- 2026-07-02 -->
# STEP 516 — F-Score 카드 문구 최종화 (넓은 검증 반영) · F-Score 사이클 종료

## ▶ 실행 (Sonnet)
```bash
cd ~/stock-terminal && claude --dangerously-skip-permissions --model sonnet
```
```
@docs/STEP_516_COMMAND.md 파일 내용대로 실행해줘
```

## 🎯 목표
STEP 515 넓은 유니버스 재검증 결론(146종목·256관측, high−low −36%, 저점수 낙폭과대 반등)을 카드에 정직 반영. 기존의 "소형·가치주에서 유효(정설)"은 **우리가 재현 못 했으므로 삭제**.
- **Cowork이 `app/stock/[symbol]/page.tsx` F-Score 카드 문구 이미 수정** — 빌드·커밋만.

## 1) 확인 (반영된 문구)
```
9개 재무 규칙 통과 개수 = 재무 건전성 지표. 자체 검증(미국 넓은 표본 2014~24)에선 점수와 이후 수익률의 유효한 관계가 없었어요(저점수 낙폭과대주 반등이 오히려 큼). 수익 예측이 아니라 재무 건전성 해석으로만 보세요.
```

## 2) 빌드 + 커밋
```bash
npm run build
```
```bash
git add "app/stock/[symbol]/page.tsx" && git commit -m "docs(lens): F-Score 카드 넓은 검증 반영(수익 예측력 없음·건전성 해석만) (STEP 516)" && git push
```
- [ ] `/stock/NVDA` F-Score 카드에 위 정직 문구. "유효(정설)" 등 미검증 주장 없음.

## ✅ F-Score 최종 (2사이클 검증 완주)
- 정의→데이터(EDGAR 深)→엣지→**검증(대형주 + 넓은 표본 2회)**→표현. 결론: **재무 건전성 렌즈, 수익 예측 아님**(정직).
- 방법론·데이터 토대·플레이북(#1~11)이 이걸로 단단해짐.

## ▶ 다음 — 모멘텀·저변동 넓은 재확인
- STEP 517: `scripts/backtest_momentum.ts`·`backtest_lowvol.ts` 유니버스도 us_symbols 넓은 표본으로 바꿔 재확인(가격만이라 가벼움·빠름).
  - 모멘텀: 대형주에선 +4%p였는데 넓은 못에선? (더 강할 수도)
  - 저변동: 넓은 못에선 이례현상 나타나나?
- 결과마다 플레이북 로그(그때그때·조건부). 그 후 3기법 검증을 "제대로 된 유니버스"로 마무리 → 표시 정리 → (그 다음에야 수익화).
