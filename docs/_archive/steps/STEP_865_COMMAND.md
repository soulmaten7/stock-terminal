# STEP 865 — 🔴 **실측 전용**: 적자 78사에 엔진을 돌리면 숫자가 나오나

**실행**: `cd ~/stock-terminal && claude --dangerously-skip-permissions --model opus`

**전제 상태**: STEP 864 커밋 `895ddf2`(코드 HEAD `33607fe`) 이후 HEAD · 트리 클린

**착수 전 필독**: 🔴 `CLAUDE.md` **🚫 창작 금지**·⓪-3 · `lib/revdcf/engine.ts` · `data/sources/expectations-investing/T8.xlsx`

---

## 0. 성격 — 🔴 재보기만. 바꾸지 않는다

🔴 **화면·DB·크론·엔진 변경 0.** 읽기 전용 스크립트 + 문서.
🔴 **채택 제안 금지.** 숫자만 낸다. 결정은 장은태.
🔴 플래그 OFF 유지.
🔴 **⓪-3 준수.**

**배경 (STEP 864 검색 · 3중 규칙)**

New Constructs 공개 방법론:
> 그들의 역DCF는 각 예측 지평에서 **성장 0 터미널(`NOPAT/WACC`)** 을 쓴다. 이 방식이 **NOPAT이 음수인 기업에 대해서도** 주가에서 거꾸로 풀어 기대치를 정량화할 수 있게 한다.

- 그들 제외 규칙은 **셋뿐**: 매출 0 · OTC · **주식구조 복잡(주식수 신뢰 불가)**.
  🔑 세 번째는 우리 `MULTI_CLASS_SHARES` 5사와 **동일 사유**.
- 적자 기업은 **커버하되 "경제적 이익 음수·감소" 로 표시**하고 Unattractive 등급.
- 커버리지 사다리: S&P500→400→600→R3000, **거래량 순** 우선.

🔴 **우리는 856에서 적자 78사를 "적용 밖"으로 뺐다.** 업계 표준과 다르다. **그런데 우리 터미널은 인플레 영구연금(T8)** 이라 그들 논거가 그대로 적용되지 않는다. **재봐야 안다.**

---

## §1 — 적자 78사 전수 실행

대상: `as_of` 최신 · `operating_margin ≤ 0` 인 발행사 (861 실측 78사 · verdict 분포 `value_destroying` 63 · `over_cap` 11 · `years` 4)

**두 가지 터미널로 각각 실행**하고 나란히 보고:

| 안 | 터미널 |
|---|---|
| **A. 현행(원전 T8)** | `NOPAT(N)×(1+i)/(WACC−i)` · i = `expected_inflation` |
| **B. NC 방식** | `NOPAT(N)/WACC` (성장 0) |

**각 안에서 보고할 것**
1. verdict 분포 (`years` / `25+` / `<1` / `value_destroying` / `invalid`)
2. `years`가 나온 종목의 **GAP 값과 종목명** (전수)
3. 🔴 **그 숫자가 말이 되는가** — 상·하위 10종목을 눈으로 보고 판단. 적자인데 GAP이 짧게 나오면 이유를 규명.
4. `invalid`/`NaN`/발산이 나오면 **원인**(NOPAT 음수 × 터미널 부호 등)

🔴 **가드 넣지 말 것.** 음수·발산 그대로 보고.

---

## §2 — 수학적 성질 규명

🔴 **NOPAT < 0 일 때 우리 수식이 어떻게 되는지 명시적으로 적을 것.**

- 터미널 = `NOPAT×(1+i)/(WACC−i)` 에서 NOPAT<0 이면 **터미널이 음수** → 기업가치가 음수로 갈 수 있다.
- 그러면 주당가치가 주가보다 항상 작아 **`"25+"`로 몰릴 것**인가? 아니면 다른 양상인가.
- **실측으로 확인**하고, 78사가 어디로 몰리는지 수치로.

---

## §3 — 원전은 이 경우를 어떻게 두었나

🔴 **T8 원본을 직접 열어** 확인:
- `C31`의 `IF` 분기가 **음수 주당가치**를 어떻게 처리하는가 (`주가 < D27` → `"<1"`).
- 원전 스프레드시트에 **적자 입력에 대한 경고·주석이 있는지**.
- 🔴 **없으면 "원전은 이 경우를 상정하지 않았다"고 기록**한다. 추측 금지.

---

## §4 — 비교표 (결론 아님 · 재료)

| 축 | 현행(적용 밖) | A. T8 터미널로 계산 | B. NC 터미널로 계산 |
|---|---|---|---|
| 커버 종목 | 0 | ? | ? |
| `years` 산출 | 0 | ? | ? |
| 숫자가 읽히나 | — | ? | ? |
| 🔴 **약점** | 78사 정보 0 | ? | ? |

🔴 **약점 행을 반드시 채울 것.** (힌트: 적자 기업의 "몇 년"이 투자자에게 오해를 줄 수 있음 · 터미널이 음수면 해석 불가 · NC는 자체조정 NOPAT이라 우리와 전제가 다름)

---

## 검증

1. `npx tsc --noEmit` 0 · `npm run test`
2. 🔴 **DB·화면·크론 무변화 증거** (`revdcf_results` 행수 전후 동일)
3. §1 A·B 각각 verdict 분포 + `years` 종목 전수
4. §2 수학적 성질 + 실측 일치
5. §3 원전 원본 확인 결과
6. §4 비교표 — **약점 행 채워짐**
7. 🔴 `[3중 점검]` ⓪ 줄 명시
8. `docs/CHANGELOG.md`·`docs/STATE.md` 오늘 날짜 (🔴 **"실측만 · 미채택"** 명시)
9. 커밋(main):
   ```bash
   git add scripts/ docs/
   git commit -m "STEP 865: probe engine behavior on loss-making firms under both terminal conventions (measurement only)"
   git push && git push origin main:revdcf-preview
   ```

## 완료 보고 → Cowork에게

- §1 A·B **verdict 분포** + `years` 산출 종목
- §2 NOPAT<0 일 때의 수학적 귀결
- §3 **원전이 이 경우를 다루는가**
- §4 비교표 + **약점**
- 🔴 못 한 것
- **여기서 멈춘다. 채택 제안 금지.**
