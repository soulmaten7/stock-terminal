<!-- 2026-08-08 · Cowork 작성 · Claude Code 실행용 -->

# STEP 946 — Q0 마감: **요약 화면에 섹터 라벨 ＋ ⑥ 완성 기준 전수 대조**

> **범위**: ① 요약 화면(`/explore` 첫 화면) 목록에도 섹터 라벨을 붙인다 ② **Q0을 완성 기준(9항목 ＋ §10 깊이 4축)으로 전수 대조**해 표를 낸다.
> 🔴 **판정은 하지 않는다.** 「완료」 선언은 **장은태 확인 후**다.

---

## 🔴 확정된 판정 (2026-08-08 장은태)

**① 요약 화면에도 섹터를 붙인다.** — *"요약화면에 붙였다가 나중에 UI가 깨지면 그때 수정하도록 하자."*
**② 「커버리지 100%」의 기준을 문서에 명시** — Cowork이 이미 `USER_QUESTIONS §Q Q0`에 반영했다(이 STEP에서 재수정 금지).

## 🔑 Cowork 육안 확인 결과 (2026-08-08 · 브라우저 직접 확인)

`/explore?list=amount`에서 **필터 칩·행별 라벨·필터 동작·출처 안내 전부 정상.** 어휘도 확정 대응표 그대로.
**출처 우선순위가 화면에서 작동하는 증거 2건**: `UBER`→**산업재**(야후 Technology를 SPDR이 이김) · `APP`→**커뮤니케이션**(941에서 Damodaran이 IT로 틀렸던 종목을 SPDR이 고침).
🔴 **관찰**: 거래대금 상위 50 중 **2건**(`SK hynix`·`The Trade Desk`)이 섹터 없음 — `us-list` 모집단이 `lens_scores` 1,021보다 넓기 때문(`TTD` 부재를 DB 조회로 확인). **빈칸 동작은 원칙대로 맞다.**

---

## ⓪-4 4×3 기록

### ③ 자체 데이터 확인 (Cowork)

| # | 확인 | 결과 |
|:--:|---|---|
| 1 | 브라우저 직접 확인(`onetrillion.app`) | 위 표 |
| 2 | `us_sector_resolved` 직접 조회 | `TTD` 없음 · `UBER`=Industrials(spdr) · `APP`=Communication Services(spdr) · `ASML`=Information Technology(yahoo) |
| 3 | `docs/LENS_COMPLETION_STANDARD.md:16-27` | 완성 9항목 원문 · `:194-206` §10 깊이 4축 원문 |
| 4 | 같은 문서 현황표 | **Q0 행이 🟡🟡🟡❌🟡❌❌❌❌ = 정식 통과 0건**으로 등재돼 있음(944 이전 상태) |

### ① 3번 검색 ＋ ⓪-5-B

```
필요한 데이터 : 없음 — 화면 한 줄 추가와 자체 대조만 하는 STEP
link_hub 후보 : 해당 없음
실제 조회     : 미실시
직접 웹검색   : 미실시
```
🔴 **못 한 축으로 명시.**

### ② 3번 검증

1. **요약 화면 회귀** — 섹터를 붙여도 **기존 정보가 하나도 사라지지 않아야** 한다(가격·등락률·도트·관심 별·거래대금)
2. **레이아웃** — 🔴 좁은 화면(모바일 폭)에서 **줄바꿈으로 깨지지 않는지** 확인. 깨지면 **말줄임 처리**하고 그 사실을 보고
3. **KR 불변** — KR 요약 목록은 **손대지 않는다**

### ④ 3번 검수 (자기 공격)

1. 🔴 **⑥ 대조에서 「통과」를 후하게 주고 싶어질 것이다.** 각 항목마다 **근거(파일·행·수치)를 적고**, 근거가 없으면 **❌ 또는 🟡**로 둔다. **판정은 장은태가 한다.**
2. 🔴 **§10 깊이 4축은 「원전 없는 항목」이라 그대로 적용되지 않는다.** 규칙 5-1대로 **「정의 공개표」로 대체**되는지, 4축 각각이 Q0에 성립하는지 **성립/미성립을 사실로 적는다**(억지로 채우지 말 것).
3. 🔴 **UI 변경이 또 라이브에 나간다.** 완료 선언 금지 · **「배포됨 · 육안 확인 대기」**로 보고.

---

## 실행 지시

### 1. 요약 화면에 섹터 라벨 추가

```
대상: /explore 첫 화면의 US 「오늘 거래가 많았던 종목」 5줄
방법: 전체 목록(list=amount)과 동일한 표기 — "거래대금 $X · {섹터}"
🔴 전체 목록과 같은 컴포넌트·같은 라벨 함수를 쓸 것 (규칙 5-2 ① — 복붙 금지)
🔴 섹터가 없으면 기존처럼 비운다
🔴 요약 화면에는 필터를 넣지 않는다 (공간 · 전체 목록에 이미 있음)
🔴 출처 안내 한 줄은 요약 화면에도 필요한가 — 🔑 넣는다. 섹터가 보이는 화면에는 출처가 보여야 한다(규칙 5-2 ④).
   이미 만든 messages 키(sectorSourceNote)를 재사용 · 새 키 추가 0
🔴 KR 요약 목록은 손대지 않는다
```

### 2. 🔑 ⑥ 완성 기준 전수 대조 (`docs/probe_946_q0_dod.json` ＋ 리포트 출력)

**완성 9항목** — 각 항목에 **판정(✅/🟡/❌) ＋ 근거(파일·행·수치) ＋ 못 한 것**을 적는다.

| # | 항목 | Q0에서 무엇을 봐야 하나 (참고) |
|:--:|---|---|
| 1 | 원전 대조표 | 🔴 **원전 없음** → 규칙 5-1 「정의 공개표」로 대체. `USER_QUESTIONS §Q Q0`의 표가 그것인지 · **화면 공개는 됐는지**(출처 안내 한 줄) |
| 2 | 입력 검증 | 출처 4개의 결측률·건수·기준일이 수치로 실측됐는지 |
| 3 | 값 검증 | SPDR 정답지 대비 채점(1순위 99.6% 등) · 🔴 **야후 구간 미검증**을 그대로 적을 것 |
| 4 | 컷·분포 | `sector_cuts` 71 적용/7 제외 · 부트스트랩 · 임계 근거 |
| 5 | 경계 처리 | 미분류·제외조합·`us-list` 모집단 초과분(2건) 처리 |
| 6 | 주장 정합 | 화면 문구(출처 안내)와 문서(정의 공개표)가 무모순인지 |
| 7 | 화면 일관성 | 🔑 **어휘 통일 후** ETF 화면·요약·전체 목록에서 같은 라벨인지 |
| 8 | 테스트 | 관련 테스트 건수와 무엇을 덮는지 |
| 9 | 라이브 실측 | 🔴 **DoD9 원문은 "KR·US 각 2종목"** — Q0는 **US 전용**이라 원리적 충돌. **사실만 적고 판정하지 말 것**(역DCF에서 같은 충돌이 929에 기록돼 있음) |

**§10 깊이 4축** — 각 축이 Q0에 **성립하는지/안 하는지를 사실로** 적는다.

```
① 구성요소 분해 — 섹터는 「어느 출처에서 왔는가」가 분해에 해당하는가?
② 시계열 추이   — 섹터에 시계열이 의미가 있는가? (as_of는 있으나 값이 거의 안 변함)
③ 분포 내 위치  — 섹터별 종목 수 분포는 있음
④ 판정 이력     — 섹터 변경 이력을 담고 있는가?
🔴 성립 안 하면 "성립 안 함 + 이유"로 적는다. 억지로 채우지 말 것.
```

### 3. 테스트

1. 요약 화면에 섹터 라벨이 뜬다(US) · 없으면 빈다
2. 🔴 **요약 화면 KR 회귀** — KR 목록 렌더가 변하지 않는다
3. 🔴 **라벨 함수 단일** — 요약·전체 목록이 같은 함수를 쓴다(복붙 아님)
4. 🔴 **회귀**: 기존 테스트 전부 통과 · `messages.test.ts` 통과 · 새 messages 키 0

### 4. 문서

- `docs/CHANGELOG.md` **(108) STEP 946** — 요약 화면 변경 ＋ **⑥ 대조표 전문**(9항목 판정과 근거)
- `docs/LENS_COMPLETION_STANDARD.md` — 🔑 **Q0 행 갱신**(현재 🟡🟡🟡❌🟡❌❌❌❌) · 항목별 실태표도 이번 대조 결과로 교체. 🔴 **판정을 후하게 주지 말 것**
- `docs/STATE.md` ▶다음 0번 — **⑥단계 상태 ＋ 🔴 「Q0 완료 판정 = 장은태 확인 대기」** 명시
- `docs/STEP_LEDGER.md` 등재

🔴 **`CLAUDE.md` · `docs/USER_QUESTIONS_2026-08-08.md`는 고치지 않는다.**

---

## 🔴 금지 (전부 불변)

- 🔴 **「완료」 선언 금지** — 「배포됨 · 대조표 산출 · 장은태 판정 대기」로 보고
- 🔴 **새 messages 키 추가 금지** (`sectorSourceNote` 재사용)
- 🔴 **KR 경로 불변** · **기존 API 수정 금지** · **`lib/sector.ts`·`sectorCuts.ts` 수정 금지**
- 🔴 **기존 렌즈 판정 로직·`lens_cuts` 수정 금지**
- `revdcf_results` · `us_market_cap` · `lens_scores` — **쓰기 금지**
- `REVDCF_ENABLED` · `data/us_symbols.json` · `.github/workflows/**` · `vercel.json` — 불변
- 크론 등록·수동 실행 — **금지**
- `probe_*` 기존 파일 — 불변
- 🔴 **API 키·비밀번호를 어떤 필드에도 입력하지 않는다**

## 성공 기준

1. 요약 화면 US 목록에 섹터 라벨 ＋ 출처 안내 · **기존 정보 삭제 0** · KR 불변
2. 🔑 **⑥ 대조표 산출** — 9항목 ＋ §10 4축, **각 항목에 근거와 못 한 것**
3. `LENS_COMPLETION_STANDARD` Q0 행 갱신
4. `npm test` · `npx tsc --noEmit` 통과 · 렌즈·역DCF 경로 diff 0

## 🔴 막히면

**추측해서 진행하지 말고 멈추고 보고할 것.** 특히 ① 요약 화면 레이아웃이 좁은 폭에서 깨짐 ② KR 렌더 변화 ③ 9항목 중 근거를 못 찾는 항목 — 이 셋은 **멈추고 보고**한다(③은 실패가 아니라 **그대로 ❌로 적으면 되는 것**이니, 막히면 그렇게 적고 진행).
