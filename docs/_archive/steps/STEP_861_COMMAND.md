# STEP 861 — DoD **항목 2(입력 검증)** 하나만

**실행**: `cd ~/stock-terminal && claude --dangerously-skip-permissions --model opus`

**전제 상태**: STEP 860 커밋 `10b3093`(코드 HEAD `feee3da`) 이후 HEAD · 트리 클린 · DB `as_of=2026-08-02`

**착수 전 필독**: 🔴 `CLAUDE.md` **🚫 창작 금지**·⓪-3 · `docs/LENS_COMPLETION_STANDARD.md`(항목 2 정의 + **모멘텀·저변동성의 `2) 입력 검증` 형식**)

---

## 0. 성격

🔴 **항목 2만.** 다른 항목 금지. 끝나면 멈추고 보고. **다음 항목 제안 금지.**
🔴 **코드·화면·DB 변경 0.** 읽기 전용 + 문서.
🔴 플래그 OFF 유지.
🔴 **⓪-3 준수** — 원본·DB 직접 열고 ⓪ 줄에 명시.

**기준 원문**: *"2. **입력 검증** — 필드 출처·단위·결측률·이상치를 **수치로** 실측."*

---

## §0 — 🔴 먼저: 번호 정리

역DCF 섹션의 `### 2) 화면 §1 블록과의 일치 확인`이 **DoD 항목 2 자리를 차지**하고 있다.
→ 그 내용은 **항목 6(주장 정합)** 소속이므로 **`### 6) 주장 정합 (초안)`** 등으로 옮기고, **`### 2) 입력 검증`** 을 신설한다.
🔴 내용을 지우지 말 것. 이동만.

---

## §1 — 필드별 입력 검증표

역DCF가 쓰는 **모든 입력 필드**를 한 표로. 흩어진 실측(838~860)을 **재조회해 갱신**한다. 옛 수치 복사 금지.

| 필드 | 출처(태그/파일·셀) | 단위 | 결측률(604 기준) | 이상치 |
|---|---|---|---|---|

**포함할 필드(최소)**
- SEC: 매출(항등식 선택·`revenueTag`/`revenueBasis` 분포) · 영업이익(+Pretax+Interest 폴백 비율) · `AssetsCurrent` · `LiabilitiesCurrent` · 현금(union) · `PP&E`(union) · 부채(리스 포함 여부) · 주식수(희석) · capex·D&A(marginal용)
- 다모다란(B분류): 한계세율 · 업종 실효세율 · ERP · 무위험 · 신용스프레드 · 무차입베타(현금조정) · 업종매핑 — **각각 `as_of` 명시**
- 시장: 주가·시가총액

🔴 **결측률은 "5년 연속 확보" 기준으로 통일**하고, 그 정의를 표 위에 1줄로 명시.

---

## §2 — 이상치 실측 (가드 금지)

각 필드에서 **수치로**:
- 음수·0 비율 (영업이익·마진·운전자본율·자본집약도·잔여가치)
- 극단값 상·하위 5종목 (심볼·값)
- 🔴 **자본집약도 141.4%(APD)류가 몇 사인가** — 100% 초과 종목 수와 업종
- 🔴 **가드를 넣지 말 것.** 세는 것만.

---

## §3 — 단위·부호 정합 검산

- SEC 값이 **전부 USD**인지(단위 혼입 0 확인)
- capex·D&A의 **부호**(현금흐름표는 음수로 오는 경우 있음) — 우리 처리와 일치하는지 3종목 손검산
- 다모다란 비율이 **소수 vs 퍼센트** 혼입 없는지(예: 0.2563 vs 25.63)

---

## §4 — 판정

`docs/LENS_COMPLETION_STANDARD.md` 역DCF **`### 2) 입력 검증`** 에 기록하고, 완성 현황표의 항목 2를 **✅/🔶/❌ 중 하나로 판정 + 사유**.
🔴 **낙관 금지.** 결측률이 높거나 이상치 원인이 미규명이면 🔶.

---

## 검증

1. `npx tsc --noEmit` 0 · `npm run test`
2. 🔴 코드·DB·화면 무변화 증거
3. §1 표 — **빈칸 0**
4. §2 이상치 수치 + 100% 초과 자본집약도 종목수·업종
5. §3 단위·부호 검산 3종목
6. §4 **판정 + 사유**
7. 🔴 `[3중 점검]` ⓪ 줄 명시
8. `docs/CHANGELOG.md`·`docs/STATE.md` 오늘 날짜
9. 커밋(main):
   ```bash
   git add docs/ scripts/
   git commit -m "STEP 861: DoD item 2 — input validation (source, unit, missing rate, outliers) for reverse-DCF"
   git push && git push origin main:revdcf-preview
   ```

## 완료 보고 → Cowork에게

- §1 표 행 수 + **최대 결측 필드**
- §2 이상치 요약
- §3 단위·부호 이상 유무
- 🔴 §4 **판정과 사유**
- 🔴 못 한 것
- **여기서 멈춘다.**
