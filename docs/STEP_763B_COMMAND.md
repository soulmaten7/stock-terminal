# STEP 763b — 모바일 Phase A 폴리시 2차 (사용자 폰 검수 피드백 4건 · 데스크톱 불변)

**실행**: `cd ~/stock-terminal && claude --dangerously-skip-permissions --model sonnet` (Sonnet)

**전제 상태**: 코드 HEAD `2458303`(STEP 763) · 트리 클린

**배경(07-19 · 사용자 폰 실물 검수)**: 763 라이브 확인 결과 4건 보완 — ① 범례 위치 ② 정렬줄 컨트롤 크기 미달 ③ 종목명 정렬 불필요(모바일) ④ 종목 상세 렌즈 텍스트 과소. **전부 모바일 뷰포트 한정 · 데스크톱 완전 불변**(763과 동일 원칙).

---

## 수정 (모바일만)

### 1) 범례 이동 — 보드 6개 공통

- 정렬줄의 상시 범례(●강점 ●주의 ●보통) **제거** → **힌트 블록 둘째 줄("사고팔 신호 아님")의 우측 정렬**로 이동:
  ```
  [손 아이콘] 종목을 누르면 TR-AI 렌즈가 보입니다          [X]
  사고팔 신호 아님                    ●강점 ●주의 ●보통
  ```
- 힌트 블록과 함께 X로 닫히고 재방문 시 안 보임(의도 — 학습 도구·상세 페이지엔 텍스트 라벨 존재). `lens_hint_seen` 로직 불변.

### 2) 정렬줄 컨트롤 44px — 보드 6개 공통

- [전체 ▾](KR·CN)·[1일전 ▾]·[방향 ↑↓]·현재가 정렬 버튼을 **주식 서브탭 칩과 동일한 높이(≥44px)·라벨 13~14px**로. 범례가 빠져 공간 확보됨 — 390px에서 한 줄 유지 확인.

### 3) 종목명 정렬 제거 — 모바일만·보드 6개 공통

- 모바일 정렬줄에서 "종목명 ↑↓" 제거 → [전체 ▾] · [현재가 ↑↓] · [1일전 ▾] (+방향 토글 기존 유지). 이유 = 이름 찾기는 검색의 일·토스/로빈후드 모바일도 이름순 없음. **데스크톱 테이블 헤더의 종목명 정렬은 유지.** 정렬 상태 로직은 불변(진입점만 숨김 — 데스크톱에서 이름순 저장된 상태로 모바일 진입 시 깨지지 않게 폴백 확인).

### 4) 종목 상세 렌즈 텍스트 확대 — `StockLensClient` + `EtfLensClient` + `LensPreview`(모바일)

- 모바일에서 최소 12px 하한 + 스케일 상향: 렌즈명·판정 라벨 **15~16px** · 근거 상세 본문·detail 수치 **14px** · 배지(검증/약한 신호 등) **12px** · 브리핑 본문 **15px**(line-height 여유). `text-[10px]`/`text-[11px]` 전수 제거(모바일 경로).
- 압축 요약 헤더(④A)·도트 크기는 유지. 데스크톱 표기는 전부 불변(`sm:` 분기).

## 검증

1. `npx tsc --noEmit` 0 · `npm run test` · `npm run build`
2. 코드 레벨: 모바일 경로 10/11px grep 잔존 0 · 정렬줄 버튼 min-h-11 클래스 확인 · 6개 보드 일관.
3. 데스크톱 불변: 1280px 기준 정렬 헤더(종목명 포함)·범례 없음(데스크톱은 원래 미리보기 카드에 범례)·렌즈 카드 크기 기존 그대로.
4. 커밋·푸시:
   ```bash
   git add components/ docs/STEP_763B_COMMAND.md
   git commit -m "STEP 763b: mobile polish r2 - legend into hint block, 44px sort controls, drop name-sort on mobile, larger lens detail type"
   git push
   ```
5. 최종 판정 = 사용자 폰(정렬줄 한 줄 유지·컨트롤 누르기 쉬움·상세 가독성).

## 완료 보고 → Cowork에게
- tsc/vitest/build · 데스크톱 불변 확인 · 커밋 해시.
