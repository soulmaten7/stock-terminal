# STEP 1027 — 답지와 맞춘다: 스펙 ↔ 카탈로그 ↔ 코드 3자 대조 (읽기 전용 + 문서 갱신)

> 🔴 **오늘 밤 22:45 UTC W1 첫 시험은 STEP 1026이 읽는다.** 이 STEP은 문서·코드 읽기라 무관하다. `app/api/cron/revdcf/route.ts` **diff 0**.
> 🔴 **`CLAUDE.md` 3중 규칙을 이 STEP에 적용한다.** 아래 §0-B가 그 적용이다.

---

## 0-A. 🔴 왜 이 STEP인가 — 답지를 안 읽고 20개 STEP을 돌았다

`docs/REVDCF_SPEC.md`가 **답지**다. §5에 Rappaport 7 value driver, B-1에 SEC XBRL 태그 12개, B-2에 SEC 밖 재료 4개. §499에 *"원전(Rappaport 1986)과 실무(New Constructs 공개 문서)의 입력 목록이 **완전히 일치**한다"*까지 적혀 있다.

🔴 **이번 세션(1006~1026)에서 이 문서를 한 번도 열지 않았다.** 그 결과:

| 스펙에 이미 있던 것 | 우리가 한 일 |
|---|---|
| A-12(892, **2026-08-04**): *"Vercel 런타임 로그 접근 = **불가**(`list_teams()` → `[]`)"* | 🔴 **STEP 1017이 8월 14일에 같은 걸 다시 부딪혀 "확정 불가"로 종료** |
| A-12(892): *"**07-30 스테일 517개** 심볼의 배열 인덱스 평균 비율 0.477 … 배열 뒤쪽에 몰려 있지 않다"* | 🔴 **STEP 1008이 "296건이 07-30에 통째로 멈췄다, 단일 이벤트"를 새 발견으로 보고** — 실은 517→296으로 **줄어든 상태**를 본 것 |
| A-12(892) 처방 판정 + **재검토 조건 (a)**: *"`retryBudgetHit`를 로그·Sentry에 연결해 재시도 예산이 실제 병목인지 관측 가능해지면 A를 재평가한다"* | ✅ **1007이 그 조건을 충족시켰다**(heartbeat 배선) — 🔴 **그런데 스펙에 되돌려 적지 않았다** |

🔴 **그리고 답지 자체가 낡았다.** 스펙은 **STEP 1005까지** 유지되다 멈췄고, 1006~1026 결과가 하나도 안 들어가 있다. **조달 지도(카탈로그)만 1024까지 최신이고 답지가 뒤처진 상태.**

---

## 0-B. 🔴 3중 규칙 적용 (`CLAUDE.md` §3중 규칙)

> *"결론을 말하기 전에 ①②③을 전부 거친다. 못 거친 항목은 「못 했다」고 명시한다."*
> *"축 A 원문 / 축 B 실무 / 축 C 반대 증거 · 검증 3(원문·우리데이터·제3자) · 검수 3(반박·수치출처·이전발언 대조)"*

**① 검색 3축 — 이 STEP에서 무엇에 해당하나**

| 축 | 이 STEP에서의 대상 | 🔴 지시 |
|---|---|---|
| **A 원문** | `docs/REVDCF_SPEC.md` · `docs/VALUATION_SPEC.md` · 원전(Expectations Investing) 인용부 | 🔴 **전문을 연다.** 요약·기억으로 대체 금지 |
| **B 실무** | 스펙 §501의 New Constructs 대조표 · §1-B 동종서비스 3곳 | 🔴 **이미 조사돼 있다. 재조사하지 말고 인용**한다 |
| **C 반대 증거** | 🔴 **스펙이 틀렸을 가능성** — 스펙은 1005에서 멈췄고 1006~1026이 뒤집은 사실이 있다 | 🔴 **스펙을 무조건 옳다고 두지 않는다.** 어느 쪽이 최신 사실인지 날짜로 판단 |

🔴 **A만 보고 결론 내면 조사가 아니라 추론이다**(`CLAUDE.md`). B·C를 못 하면 "못 했다"고 적는다.

**② 검증 3**: 원문(스펙·원전) · 우리 데이터(DB 실제 행) · 제3자(New Constructs 등 기존 조사분)

**③ 검수 3 — 🔴 이 STEP의 핵심**
- **반박**: 각 불일치에 대해 "반대 해석이 가능한가"를 적는다
- **수치출처**: 모든 수치에 출처·날짜(`CLAUDE.md` 작성 규칙)
- 🔴 **이전발언 대조** — **이게 이번에 우리가 빠뜨린 축이다.** 🔴 결론을 적기 전에 `REVDCF_SPEC.md` 전문 · `docs/STEP_LEDGER.md` · `docs/CHANGELOG.md`에서 **같은 주제를 이미 다룬 STEP이 있는지 검색**한다. 있으면 그 STEP 번호와 결론을 인용한다

---

## ⓪ 전제 점검

### ⓪-1. 직전 STEP 산출물 — 원문 인용 (검산 포함)

| 출처 | 인용 | 검산 |
|---|---|---|
| STEP 1025 | 관측 필드 5개 추가 · 기존 4개 계산 불변 · **833 값 잠금 테스트 11/11 무수정 통과** · W4 컷 교체 시 **117/1,036(11.3%)**만 판정 변화 | ✅ |
| STEP 1024 | 분모 축 닫힘 — SIC가 지지하는 것만 제외 시 96.95% → **96.91%로 악화** · REIT 오분류 **54건** | — |
| STEP 1026(예정) | 오늘 밤 네 값(`newCutGateOk`·`wouldPruneRows`·`stage`·`us_sector_relative` as_of) | 🔴 이 STEP과 무관 |
| 🔴 Cowork 실측(2026-08-14) | `REVDCF_SPEC.md` B-2: *"무위험수익률 \| **FRED** 미 국채 \| ✅ `FRED_API_KEY` **보유**"* | 🔴 **키를 이미 갖고 있다** |
| 🔴 Cowork 실측 | B-2의 SEC 밖 재료 4개에 **베타가 없다.** 그런데 §5 #7 WACC는 "자기자본비용(CAPM)" — CAPM에 베타는 필수 | 🔴 스펙 누락 또는 구현 이탈 |
| STEP 1011 | 슬롯 20개를 **카탈로그 ↔ 코드 2자만** 대조(5줄 불일치 정정) — 🔴 **스펙이 대조 대상에서 빠졌다** | — |

### ⓪-2. 이 STEP이 건드리는 슬롯
**전 슬롯(20개, 실질 19개).** 🔴 **값을 바꾸지 않는다. 세 문서가 같은 말을 하는지만 본다.**

### ⓪-3. 걸리는 금지사항 (해당되는 것만)
- [x] 🔴 **프로덕션 코드 diff 0** — `app/**` · `lib/**` · `scripts/**` 읽기만. 산출은 **문서뿐**
- [x] 🔴 **DB 쓰기 0** · **크론 수동 실행 금지**(특히 `email-brief` = 실제 메일 발송)
- [x] 🔴 **`app/api/cron/revdcf/route.ts` diff 0** — 오늘 밤 W1 첫 시험 보호
- [x] 🔴 **게이트 임계·`BUDGET_MS`·`maxDuration`·취득 경로 불변**
- [x] 🔴 **불일치를 발견해도 코드를 고치지 않는다.** 어느 쪽이 옳은지도 **판정하지 않는다** — 근거만 정리
- [x] 🔴 **API 키 발급·유료 가입 금지** — `FRED_API_KEY`는 **이미 보유**로 스펙에 기재돼 있다. 🔴 **존재 여부만 확인하고 VALUE를 타이핑·출력하지 않는다**
- [x] 🔴 **7렌즈 라이브 화면 수정 금지** · `data/us_symbols.json` · `.github/workflows/**` · `vercel.json` 수정 금지
- [x] `REVDCF_ENABLED` Production OFF 유지 · robots.txt 우회 금지 · **KR 전면 동결**

### ⓪-3b. 🔴 결함의 소재 확정
- 이 STEP이 찾는 불일치는 **문서 사이의 불일치**다. 코드 결함이 아닐 수 있다
- 🔴 각 불일치마다 **"스펙이 낡은 것 / 카탈로그가 틀린 것 / 코드가 이탈한 것"** 중 무엇인지 **날짜로** 가른다. 🔴 확정 못 하면 `미확정`

### ⓪-4. 🔴 반증 조건

| 관측 | 결론 | 다음 축 |
|---|---|---|
| 3자가 대체로 일치(불일치 ≤3) | 답지·조달지도·구현이 정합. 이번 세션 드리프트는 **읽지 않은 문제**였지 문서 문제가 아니다 | 스펙 갱신만 |
| **불일치가 많다(≥8)** | 🔑 세 문서가 따로 논다 — 정본 체계 자체가 깨졌다 | 정본 관계 재설계 |
| 🔴 **스펙이 코드보다 옳은 사례가 나온다** | 구현이 스펙을 이탈했다 — **값이 틀렸을 수 있다** | 해당 슬롯 값 영향 산정 |
| 🔴 **스펙이 낡아 코드가 옳은 사례가 나온다** | 답지 갱신이 밀린 것 | 갱신 절차 신설 |

🔴 **네 결과 모두에 다음 행동이 정해져 있다.**
🔴 세 번째가 나오면 **크게 보고**한다 — 답지대로 안 만든 값이 라이브에 있다는 뜻이다.

### ⓪-5. 3번 생각
1. **범위** — 🔴 곁가지가 아니다. **이번 세션 드리프트의 근본 원인**이고, 안 고치면 다음 세션도 같은 일이 난다.
2. **표본** — 🔴 **슬롯 20개 전수.** 🔴 **전수 비용: 문서 읽기 + 코드 grep이라 분 단위.** 상한 불필요.
3. **시점** — 읽기 전용이라 오늘 밤 두 크론과 무관. 🔴 1026이 오늘 밤 값을 읽으므로 **이 STEP과 1026은 순서 무관**하다.

---

## 1. 할 일

### 1-1. 🔴 답지 전문 읽기 — 먼저
`docs/REVDCF_SPEC.md`(및 `VALUATION_SPEC.md`) **전문**을 읽는다.
- 🔴 **요약·목차·기억으로 대체 금지.** 3중 규칙 축 A다
- 읽은 뒤 **스펙의 마지막 갱신 STEP 번호**를 확인한다(현재 1005로 추정 — 확인할 것)
- 🔴 스펙이 "미설계 / 남은 것"으로 표시한 항목을 **전부 목록화**한다

### 1-2. 3자 대조표 — 슬롯 20개 전수

| 슬롯# | ① 스펙이 뭐라 하나(`파일:줄`) | ② 카탈로그가 뭐라 하나(`파일:줄`) | ③ 코드가 뭘 읽나(`파일:줄`) | 일치 여부 | 불일치면 어느 쪽이 최신인가(날짜) |
|---|---|---|---|---|---|

🔴 **세 칸 모두 `파일:줄번호`.** 근거 없으면 `확인불가`.
🔴 **"대체로 같다"를 판정으로 쓰지 않는다.**

**특별히 확인할 것(Cowork가 이미 발견)**
- 🔴 **#16 베타** — B-2의 SEC 밖 재료 4개에 없는데 코드는 `damodaran_beta`를 쓴다. **스펙 어딘가에 베타 조달처가 있는지 전문에서 찾는다.** 없으면 "스펙 누락"으로 확정
- 🔴 **#14 무위험수익률** — B-2는 **FRED**, 카탈로그·코드는 **Damodaran `ERPbymonth.xlsx`**. 🔴 스펙 §10-E(999·1000)·§10-F(1001)·§10-I(1005)에 이 전환이 기록돼 있는지 확인하고, **있으면 B-2 표가 낡은 것**이다
- 🔴 **#17 신용스프레드** — 카탈로그가 *"FRED ICE BofA OAS, 매일 — 🔑 최대 발견"*으로 적어뒀다. 🔴 **`FRED_API_KEY` 보유가 사실인지 환경변수 존재 여부만 확인**(VALUE 출력 금지)

### 1-3. 🔴 검수 3 — 이전발언 대조
결론을 적기 전에 **같은 주제를 이미 다룬 STEP을 검색**한다.
- `REVDCF_SPEC.md` 전문 · `docs/STEP_LEDGER.md` · `docs/CHANGELOG.md`
- 🔴 **이번 세션(1006~1026)이 다룬 주제 각각에 대해** 그 이전에 같은 걸 다룬 STEP이 있는지 찾는다
- 산출: **중복 조사 목록** — 「우리 STEP / 이미 있던 STEP / 결론이 같았나 달랐나」
- 🔴 Cowork가 이미 두 건을 찾았다(1017↔892 Vercel 로그 · 1008↔892 07-30 코호트). **더 있는지 전수로 본다**

### 1-4. 🔴 답지 갱신 — 이번 세션 결과를 스펙에 되돌려 넣는다
`docs/REVDCF_SPEC.md`에 1006~1026 결과를 **기존 서술 방식 그대로**(STEP 번호 · 날짜 · 실측 근거 · 🔴 표시) 추가한다.

반드시 들어가야 할 것:
- 🔑 **892 재검토 조건 (a) 충족** — 1007이 heartbeat를 배선해 `retryBudgetHit`·`noResponse`·`failedChunks`가 관측됐고, **재시도 예산이 병목이 아님이 확정**됐다. 892가 "관측 불가"로 남긴 자리다
- **야후 프로덕션 프로파일 필드 결측**(1006~1010): `quote`·`quoteSummary` 둘 다 0/284 · `currency`/`region`/`symbol` 284/284 일치 → "다른 시장 레코드" 가설 닫힘
- **커버리지 경로 소진**(1017~1024): 야후 93.9% · SEC 조립 96.95%(엄격 95.96%) · 나스닥 접근 불가 · 조회창 확장 실질 +1건 · 유니버스 정리 96.91%로 악화
- **게이트 재정의 판정**(1025, 장은태): 급락 탐지(절대 하한 85% + 낙폭 3%p) · 드라이런 배선 · 🔴 **상수 미확정**
- **서빙 신선도 게이트 부재**(1016): 임계가 `health` 크론 전용, 화면 경로는 미참조
- 🔴 **1021~1023이 프로덕션에 없는 "6분기 조회창"을 쫓았다는 사실**(1023 확정) — 실패도 기록한다

🔴 **스펙의 기존 서술을 지우지 않는다.** 취소선·정정 표기로 보존(카탈로그와 같은 방식).

### 1-5. 정본 관계 명문화
🔴 지금 세 문서의 역할이 암묵적이다. **명시한다**(어느 문서에 쓸지는 §1-2 결과를 보고 정하되, 🔴 **`REVDCF_SPEC.md` 맨 앞이 유력**):
- **`REVDCF_SPEC.md`** = 답지. **무엇을 넣는가**(원전 정의 · driver별 확정 · 원전 대조표)
- **`DATA_SOURCE_CATALOG.md`** = 조달 지도. **어디서 가져오는가**
- **`ANSWERABILITY_MAP.md`** = 출력 지도. **무엇을 답할 수 있는가**
- **코드** = 실제로 하는 것
- 🔴 **셋이 어긋나면**: 날짜가 최신인 것이 사실이고, **스펙을 최신으로 되돌리는 것이 STEP의 의무**다(게이트 9와 같은 순서 규칙)

### 1-6. 산출물
`docs/probe_1027_three_way_audit.md`
- 맨 앞에 **⓪-4 네 갈래 중 어느 것인지** + `일치 n / 불일치 n / 확인불가 n`(슬롯 20개 기준)
- 1-2 3자 대조표 전수
- 1-3 중복 조사 목록
- 1-4 스펙에 추가한 항목 목록
- 🔴 3중 규칙 **못 한 축**(B·C 중 못 한 것) 명시

🔴 카탈로그에 **스펙과의 불일치**가 있으면 취소선 정정(md+xlsx 동시).

## 2. 커밋 전 게이트
- 게이트 7: git 미추적 파일 참조 검사 — 🔴 런타임 `fs.readFileSync('/tmp/...')`도 잡을 것
- 게이트 8: push 후 **Vercel 배포 성공 + verify 초록을 눈으로 확인**하기 전까지 미완료
- 게이트 9: 🔴 **정본(`REVDCF_SPEC.md`) 먼저, 사본 나중**
- `docs/CHANGELOG.md` 1027 항목 · `docs/STEP_LEDGER.md` 1027 등재

## 3. DoD
- [ ] 🔴 **§1-1에서 스펙 전문을 실제로 읽었다**(요약 대체 아님) · 스펙의 마지막 갱신 STEP 번호 명시
- [ ] ⓪-4 네 갈래 중 어느 것인지 맨 앞에 + `일치/불일치/확인불가`
- [ ] 1-2 **슬롯 20개 전수**, 세 칸 모두 `파일:줄번호`(없으면 `확인불가`)
- [ ] #16 베타 · #14 rf · #17 신용스프레드 3건 각각 결론
- [ ] 🔴 `FRED_API_KEY` **존재 여부만** 확인(VALUE 미출력)
- [ ] 1-3 **이전발언 대조** — 중복 조사 목록 전수(1017↔892 · 1008↔892 포함)
- [ ] 1-4 스펙에 1006~1026 결과 반영, 🔴 **892 재검토 조건 (a) 충족 기록 포함**, 기존 서술 취소선 보존
- [ ] 1-5 정본 관계 명문화
- [ ] 🔴 **3중 규칙 3종 명시**: ① 못 한 축 ② 철회·정정 ③ 미측정 목록
- [ ] 🔴 **DB 쓰기 0 · 프로덕션 코드 diff 0** · `app/api/cron/revdcf/route.ts` diff 0
- [ ] tsc 0 · test 전량 통과
- [ ] Vercel 배포 성공 + verify 초록 **눈으로** 확인

🔴 **어느 쪽이 옳은지의 판정은 장은태가 한다. 이 STEP은 셋이 어긋난 자리를 전부 찾아 답지를 최신으로 되돌리는 것까지다.**
