# STEP 1058 — 2026-08-16 밤 US 파이프라인 연쇄 실패 원인 규명 (🔵 읽기 전용 · 코드 diff 0 · DB 쓰기 0 · 크론 수동 실행 0)

> 🔴 **이 STEP은 고치지 않는다.** 원인을 실측으로 확정하는 데까지다. **재실행·수정 여부 판정 = 장은태.**
> 🔴 **렌즈 감사 ④(퀄리티)보다 이것이 먼저다.** 감사 ④~⑦이 전부 이 DB 값을 재료로 쓴다. 오염 의심 상태의 재료 위에 감사를 쌓으면 감사 결과가 무효가 된다.
> 🔵 읽기 전용 — `lib/**`·`app/**`·`components/**` diff 0 · DB 쓰기 0 · 화면 변경 0 · 플래그 무접촉 · **크론 수동 실행 0**.

---

## ⓪-0. 🔴 먼저 할 일 — `git pull`

로컬 체크아웃이 `08699ce`(STEP1054)에서 멈춰 있다. 2026-08-17 13:2x 확인 실측:
- `docs/step_orders/STEP1055.md`·`STEP1056.md`·`STEP1057.md` → **untracked**
- `docs/LENS_AUDIT_03_LOWVOL.md` → **로컬에 없음**(`ls docs/` 전수 확인)

🔴 **`git pull` 없이 시작하면 직전 산출물을 못 읽는다.** 먼저 pull하고, 충돌이 나면 조사를 시작하지 말고 그 사실부터 보고할 것.

---

## ⓪ 전제 점검

### ⓪-1. 직전 산출물 — 원문 인용

| 출처 | 인용(그대로) |
|---|---|
| `docs/LENS_AUDIT_03_LOWVOL.md` 신규 결함 ③ | 🔴 **인용 못 함.** 이 명령서를 쓴 시점에 로컬에 파일이 없어 **원문을 열지 못했다.** pull 직후 **가장 먼저** 이 문서의 결함 ③을 열어 아래 §1의 실측과 대조할 것. (작성자 기억으로는 *"08-16 US 렌즈 배치 취득 실패로 US 컷 2일째 정지 · successRate 24.2%"* 취지이나 **이건 인용이 아니라 내 추측이다.** 원문과 다르면 원문이 맞다.) |
| `docs/probe_1017_cron_failure_causes.md:5` | *"**A(`revdcf` heartbeat 없음)**: 🟡 **확정 불가 — Vercel 로그 접근 자체가 막혀 있다(403 Forbidden).**"* |
| 같은 문서 `:7` | *"**B(나스닥 timeout)**: … 로컬에서 정확히 같은 URL·헤더·20초 timeout으로 1회 재현했더니 **로컬도 20,008ms에서 동일하게 timeout** … 🔴 **1회 관측이다. 단정하지 말 것.**"* |
| 같은 문서 `:22~30` | `app/api/cron/revdcf/route.ts:374-399`의 이중 `finally` 구조 — **이미 읽힌 구간이다. 다시 읽되 처음부터 읽는 척하지 말 것.** |

🔴 **직전 STEP(1057)이 「못 한 것」으로 남긴 것 중 이 STEP이 건드리는 것**: pull 후 확인. 겹치면 이 STEP 범위를 그만큼 줄인다.

### ⓪-1a. 🔴 로드맵 원문 대조 (`docs/ROADMAP_V2.md` — 여섯 층 전부)

| 층 | 걸리는 절 | 인용(원문 그대로) |
|---|---|---|
| **WHY** | 조건 1 (`:65`) | *"**정확해야 한다** — 틀린 것을 보는 법으로 가르치면 **안 가르치느니만 못하다.** 🔴 계산이 먼저다 — 협상 대상 아님"* → 🔴 **지금 화면에 나가는 배수가 27% 결측이다. 이 조건이 지금 깨져 있다.** |
| **HOW** | H-7 (`:206`) | *"**결측을 채우고 실측인 척** 🔴 **절대 금지**"* → 🔴 **반대 방향도 본다** — 결측을 결측이라 **표시하고 있는가**. 552종목이 화면에서 어떻게 보이는지 확인 대상 |
| **WHAT** | W-3 | 지금 내놓고 있는 것 = 배수 포함. **오염이면 지금 내놓고 있는 것이 틀린 것이다** |
| **만드는 절차** | ⬜ 확정 대기 | 무관 |
| **관문·순위** | F-1 ② (`:334`) | *"**정확한가** — 계산이 맞는가, **검산할 수 있는가** … 없으면 **만들면 안 된다**"* → 이 STEP은 관문 ②의 사후 검산이다 |
| **완성의 정의** | C-1 #5 (`:495`) | *"**경계 처리** — 계산 불가 조건·최소 표본·**결측 표기**"* → 🔴 **결측이 표기되고 있는지가 이번 조사의 두 번째 축** |

### ⓪-1b. 기존 답 확인 — 🔴 `ls docs/` 전수 (이름 패턴 검색으로 끝내지 않는다)

2026-08-17 실측으로 발견된 관련 문서:
- `docs/probe_1017_cron_failure_causes.md` (179줄) — **같은 종류의 조사가 이미 있다**
- `docs/CRON_OBSERVABILITY.md` (174줄) — 관측 필드 정의
- `docs/AUDIT_895_SKIP_REASONS.md` — 스킵 사유 분기

🔴 **그러므로 산출물은 새 파일이 아니다**(§2-6).

### ⓪-2. 건드리는 슬롯
**해당 없음** — 읽기 전용. 조회만.

### ⓪-3. 걸리는 금지사항
- [ ] 🔴 **크론 수동 실행 금지** — 재현으로 확인하지 않는다
- [ ] **코드 diff 0 · DB 쓰기 0 · 화면 변경 0** — 7렌즈 라이브 화면은 매번 별도 승인(899·901 위반 전례)
- [ ] **KR 전면 동결** — `kr-lens-scores`는 **대조군 조회로만**
- [ ] `data/us_symbols.json`·`.github/workflows/**`·`vercel.json` **수정 금지**(읽기만)
- [ ] `REVDCF_ENABLED` Production OFF 유지 · `Q1_ENABLED` 무접촉
- [ ] **키 VALUE 타이핑 금지 · 로그인 자격증명 입력 금지**
- [ ] 🇺🇸 US 단독

### ⓪-3b. 🔴 결함의 소재 확정
문제가 관측될 지점 = `app/api/cron/us-perf/route.ts` · `app/api/cron/lens-scores/route.ts` · `app/api/cron/revdcf/route.ts` · `us_valuation`·`us_sector_relative`·`us_sector_wide`·`lens_scores`·`lens_cuts`(DB) · `vercel.json` 스케줄. **전부 프로덕션이다.**

### ⓪-4. 🔴 반증 조건

**전제 ①** — 세 실패(`us-perf` 나스닥 0행 · `lens-scores` 68.8% · `revdcf` 예산소진)와 배수 27% 급락이 **하나의 원인**이다.
- 틀렸다면 → ㉠상류가 서로 다른 곳으로 밝혀진다 ㉡552종목이 「나스닥 상장」·「revdcf 미처리」·「lens-scores noResponse」 **어느 집합과도 안 겹친다** ㉢규모가 끝내 안 맞는다(아래)
- 그때 → 🔴 **각각 독립 조사로 쪼갠다.** 하나의 서사로 억지로 묶지 않는다. *"연쇄가 아니라 동시 발생"*이라 적는다.

**전제 ②** — 배수 급락이 **08-16 크론 실패 때문**이다.
- 틀렸다면 → 배수 계산 코드가 08-15~16에 배포됐다
- 🔴 **이걸 제일 먼저 5분 안에 확인하라**: `git log --since=2026-08-14 --until=2026-08-17 -- lib/ app/api/cron/revdcf/`. **코드 변경이 원인이면 나머지 조사가 전부 헛수고다.**

**규모 불일치 — 미리 놓아두는 반증** 🔴
`revdcf`가 5,820 중 **1,561만 처리**한 것이 원인이라면 결측 증가가 **4,259 규모**여야 한다. 실제 PER 감소는 **552건**이다. **자릿수가 안 맞는다.** 이 어긋남을 설명하지 못하면 *"revdcf 예산소진은 단독 원인이 아니다 — 인과 미확정"*이라고 **그대로 적는다.**

### ⓪-5. 🔴 3번 생각
1. **범위** — 곁가지 아님. 렌즈 감사 ④~⑦이 전부 이 DB를 재료로 쓴다.
2. **표본** — 🔴 **전수.** `us_valuation` 08-15·08-16 각 5,820행 전량 대조. 상한 없음. **외부 신규 호출 0**(DB 조회만).
   🔴 **전수의 조건** — 최소 둘로 교차: ㉠정방향(08-16 null 나열) ㉡역방향(08-15 값 있음 → 08-16 null) ㉢전체 나열 후 필터.
3. **시점** — 🔴 조사 중에도 크론이 돈다. **조회한 시각(UTC)과 `as_of`를 반드시 함께 적는다.**

---

## 1. 배경 — 실측 (Cowork Supabase MCP 조회 · 2026-08-17 13:2x UTC)

### 1-1. `cron_heartbeats` 전량 (총 6행 — job당 마지막 1행만 보유)

| job | last_run_at (UTC) | ok | 핵심 note |
|---|---|:--:|---|
| `kr-lens-scores` | 2026-08-17 10:50:26 | ✅ | coverage **1.0** · computed 978/1000 — 🟢 **KR 정상(대조군)** |
| `email-brief` | 2026-08-16 23:12:31 | ✅ | note null |
| `revdcf` | 2026-08-16 23:10:59 | ✅ | processed **1561** · finished **false** · budgetExhausted **true** · routeMs 297,472 · valuationSaved **5820** · sectorRelativeSaved 5820 · sectorWideAdded **0** |
| `lens-scores` (US) | 2026-08-16 22:25:55 | 🔴 **false** | freshCoverage **0.688** · compRatio 0.665 · cutGateOk false · computed **242**/1000 · batchOk 3952 · **noResponse 1800** · fallbackUsed 1519 · failedChunks **18/60** · retryAllLen 2026 · countHit true · timeHit false · retryBudgetHit **true** |
| `us-perf` | 2026-08-16 22:25:20 | ✅ | nasdaqRows **0** · nasdaqMs 20,005 · nasdaqError **"rate_limited_or_timeout: The operation was aborted due to timeout"** |
| `jp-disclosures` | 2026-07-27 16:01:57 | ✅ | — |

🔴 **여기서 바로 보이는 것 둘**
1. `us-perf`(22:25:20)와 `lens-scores`(22:25:55)는 **35초 차**다. 둘 다 상류 취득에서 무너졌다.
2. 🔴 **08-17에 US 크론이 하나도 실행 기록을 남기지 않았다.** KR만 08-17 10:50에 돌았다. 단 `cron_heartbeats`는 job당 마지막 1행만 보유하므로 *"안 돌았다"*와 *"돌았는데 하트비트를 못 남겼다"*를 **이 표만으로는 구분 못 한다** — probe_1017의 A 케이스가 정확히 그 구분 문제였다.

### 1-2. `us_valuation` 일자별 (조회 실측)

| as_of | rows | per | pbr | psr | ev_ebitda |
|---|--:|--:|--:|--:|--:|
| **2026-08-16** | 5,820 | **1,516** | **2,381** | **2,592** | **1,046** |
| 2026-08-15 | 5,820 | 2,068 | 3,283 | 3,558 | 1,452 |
| 2026-08-14 | 5,820 | 2,067 | 3,279 | 3,554 | 1,451 |
| 2026-08-13 | 5,820 | 2,061 | 3,268 | 3,542 | 1,445 |
| 2026-08-12 | 5,780 | 2,056 | 3,261 | 3,535 | 1,440 |
| 2026-08-11 | 5,780 | 2,052 | 3,254 | 3,527 | 1,435 |

- 🔴 **08-17 행 없음.**
- 08-11~15는 **완만 증가**. 08-16만 **−26.7% / −27.5% / −27.2% / −28.0%**.
- 🔴 **행 수(5,820)는 그대로다** — 행이 빠진 게 아니라 **값이 null이 됐다.**

**확정 안 됨** 🔴
- 세 실패와 배수 급락의 **선후·인과**
- 08-17 US 크론이 **실행됐는지**
- 552종목의 **정체와 공통점**
- 결측이 **화면에 표기되고 있는지**

---

## 2. 할 일

### 2-1. 🔴 코드 배포 배제 (제일 먼저 · 5분)
`git log --since=2026-08-14 --until=2026-08-17 -- lib/ app/api/cron/` 및 Vercel 배포 목록. 08-15~16에 배수 계산 경로가 바뀌었나. **바뀌었으면 여기서 방향을 튼다**(⓪-4 전제 ②).

### 2-2. 08-17 US 크론 실행 여부 확정
- `vercel.json` **읽기만** — US 크론 스케줄(UTC) 확인.
- DB 간접 판별: `us_prices`·`us_market_cap`·`lens_scores`·`us_valuation`·`us_sector_relative`에 **08-17 `as_of` 행이 있는가**.
- Vercel MCP `get_runtime_logs`·`list_deployments` 시도 — 🔴 **probe_1017에서 403 Forbidden이었다. 또 403이면 3회 안에 포기하고 「로그 미확인」으로 적는다.** 시간 태우지 않는다. **로그인 요청·자격증명 입력 금지.**

### 2-3. `us-perf` 나스닥 0행이 무엇에 전파됐나
- `app/api/cron/us-perf/route.ts` 완독 — `nasdaqRows`가 **어느 테이블에 쓰이는지**, 0행일 때 **기존 행을 덮는지 남기는지**(🔴 이게 갈림길이다).
- 시가총액 테이블 08-15 vs 08-16 행수·결측 대조.
- 🔴 **probe_1017 B(08-13)와 같은 에러 문자열인가.** 같으면 **3일 이상 반복되는 상시 결함**이지 1회 사고가 아니다. 그 구분을 명시.

### 2-4. `lens-scores` US — 분모가 안 맞는다
- `computed 242 / universe 1000`인데 `batchOk 3952` · `noResponse 1800`. 🔴 **1000짜리 유니버스에서 왜 3,952+1,800이 나오나.** 청크 단위인가 종목 단위인가 재시도 누적인가 — **코드로 확정.**
- `failedChunks 18/60`(30%) · `retryAllLen 2026` · `retryBudgetHit true`(`countHit` true·`timeHit` false → **건수 상한에 걸림**). 예산이 어디서 정의되고 무엇에 걸렸나.
- 🔴 **이 상류가 `us-perf`의 나스닥과 같은 곳인가 다른 곳인가.** 같으면 단일 원인, 다르면 우연의 일치. **⓪-4 전제 ①의 갈림길이 여기다.**

### 2-5. 🔴 `revdcf` 예산소진 ↔ 배수 결측 (이 STEP의 중심)
- `processed 1561`인데 `valuationSaved 5820` — **처리 1,561 / 저장 5,820.** 🔴 **이 둘의 관계를 코드로 확정한다**(`app/api/cron/revdcf/route.ts:374~399` 이중 `finally`, probe_1017이 이미 읽은 구간).
- 08-16 결측 종목 명단 추출 — 🔴 **셋 다 null**과 **하나만 null**을 **나눠서** 센다.
- 그 종목들을 **셋 다** 대조: ㉠나스닥 상장인가(2-3 연결) ㉡`processed 1561`에 안 든 4,259에 있나 ㉢`lens-scores` noResponse와 겹치나.
- 🔴 **⓪-4의 규모 불일치**(4,259 vs 552)를 설명하지 못하면 **「단독 원인 아님 · 인과 미확정」이라 적는다.**

### 2-6. 08-15 ↔ 08-16 종목 단위 전수 대조
- 08-15 값 있음 → 08-16 null인 종목을 **전수 나열**(⓪-5②의 역방향).
- 공통점을 **분포로**: 섹터 · 시가총액 구간 · 거래소 · 상장연차.
- 🔴 **섹터 공백**(Financials 856중 14 · Real Estate 195중 1 · 섹터없음 1,135)과 **겹치는가.** 겹치면 하나의 원인, 안 겹치면 둘이다.

### 2-7. 🔴 결측이 화면에 표기되는가 (HOW·H-7 / C-1 #5)
552종목이 화면·이메일·브리핑에서 **어떻게 보이는지** 코드로 확인. *"—"*인가, 이전 값이 남는가, 종목이 목록에서 사라지는가. 🔴 **화면은 고치지 않는다. 확인만.**

### 2-8. 산출물 — 🔴 새 파일을 만들지 않는다
- `docs/probe_1017_cron_failure_causes.md`에 **`## 2026-08-16 US 연쇄 실패` 절을 추가**한다.
  근거: 같은 종류(크론 종료 사유)의 조사가 이미 그 문서에 있다. 별도 파일을 만들면 다음에 같은 일이 났을 때 **어디를 봐야 하는지 갈린다** — 2026-08-17 장은태 규칙: *"업데이트를 해야지 새로운 파일로 만들어버리면 계속 중복 파일이 남는다."*
  🔴 **기존 절은 지우지 않는다.** 08-13 조사는 그대로 두고 아래에 덧붙인다.
- `docs/CRON_OBSERVABILITY.md`에 **「관측하고 싶었는데 남아 있지 않았던 필드」 목록**을 추가한다. 🔴 **필드를 실제로 추가하지는 않는다 — 목록만.**
- `docs/CHANGELOG.md` · `docs/STEP_LEDGER.md` 등재.

---

## 3. 🔴 하지 말 것
- 🔴 **크론 수동 실행 금지.** 실패를 다시 일으키면 DB가 한 번 더 오염된다.
- 🔴 **고치지 않는다.** 원인을 찾아도 코드·설정·스케줄을 건드리지 않는다. **재실행 여부 = 장은태 판정.**
- 🔴 **야후/나스닥에 직접 호출을 넣어 재현하지 않는다.** probe_1017이 로컬 1회 재현을 했고 *"1회 관측이라 단정 못 한다"*로 끝났다. 같은 걸 반복하지 않는다. 굳이 한다면 **왜 이번엔 다른가**를 먼저 적고 하라.
- 🔴 **Vercel 로그는 3회 안에 포기.** 403 전례 있음. 로그인 요청·자격증명 입력 금지.
- 🔴 **원인을 하나로 몰지 않는다** — `LENS_COMPLETION_STANDARD:137` STEP889 원칙: *"계산 불가 사유는 뭉뚱그리지 않고 실제 원인별로 정확히 분기한다."*
- 🔴 **KR 무접촉** — 대조군 조회로만. 판단·집계에 넣지 않는다.
- 🔴 **렌즈 감사 ④~⑦을 겸하지 않는다.** 하나를 하나씩.
- 🔴 **`us_symbols.json`·`.github/workflows/**`·`vercel.json` 수정 금지**(읽기만).

## 4. 커밋 전 게이트
- 게이트 7: git 미추적 파일 참조 검사
- 게이트 8: push 후 **GitHub Actions verify 초록 + Vercel 배포 성공을 눈으로 확인**
- 게이트 9: 새 테이블·크론·관측 필드 없음 → 해당 없음
- `docs/CHANGELOG.md` 항목 · `docs/STEP_LEDGER.md` 등재
- 🔴 **브랜치 규약(STEP1056 §2-7)** — `main`에 직접 push. **브랜치·PR 만들지 않는다. 예외 없음.**

## 5. DoD
- [ ] ⓪-0 `git pull` 완료 · `LENS_AUDIT_03_LOWVOL.md` 결함 ③ **원문 인용**으로 §1 대조
- [ ] ⓪-1a 로드맵 **여섯 층** 대조 · ⓪-1b `ls docs/` 전수 목록
- [ ] 🔴 2-1 **코드 배포 배제** 먼저 (⓪-4 전제 ②)
- [ ] 2-2 08-17 US 크론 실행 여부 — **확정 또는 「확정 불가」 중 하나로 명시**
- [ ] 2-3 나스닥 0행 전파 경로 + probe_1017 B와 **같은 결함인지** 판정
- [ ] 2-4 `lens-scores` 분모 확정(3,952 / 1,800의 단위) + **상류가 같은 곳인가** 판정 — 🔴 ⓪-4 갈림길
- [ ] 2-5 `processed 1561` vs `valuationSaved 5820` 코드 확정 + **규모 불일치(4,259 vs 552) 설명 또는 「미확정」 명시**
- [ ] 2-6 08-15↔08-16 **전수** 종목 대조(정방향+역방향 둘 다) + 공통점 분포 + 섹터 공백과의 관계
- [ ] 2-7 결측의 **화면 표기 여부**(H-7 / C-1 #5)
- [ ] 2-8 **probe_1017에 절 추가**(새 파일 0) + `CRON_OBSERVABILITY.md` 관측 공백 목록
- [ ] 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · **크론 수동 실행 0** · tsc 0 · test 전량 통과
- [ ] 조회 시각(UTC)과 `as_of` 병기
- [ ] 마지막에: **못 한 축 / 철회·정정한 것 / 미측정으로 남은 것**

🔴 **판정은 장은태가 한다. 이 STEP은 원인을 놓는 것까지다.**
