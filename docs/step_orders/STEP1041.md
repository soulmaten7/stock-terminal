# STEP 1041 — 모델 선정: 63개 우주에 새 기준을 걸어 순위 도출 (🔵 조사·도출 · 코드 diff 0)

> 🔴 **장은태 지시(2026-08-15)**: *"어떤 모델이 진짜 필요한 건지를 정해야 순서를 정하지."*
> 🔴 **63개를 다시 찾는 조사가 아니다. 재료는 그대로 두고 「기준」을 갈아끼우는 작업이다.**
> 🔴 **선택지를 나열하지 마라.** 하나의 순위를 도출하고 근거를 보인다. 최종 승인만 장은태.
> 🔵 코드·DB 쓰기 0. **DB 읽기는 한다**(재료 실측에 필요).

---

## 0-A. 왜 이 STEP인가

**우리는 「정보 목록」을 정해놓고 「모델」을 정했다고 착각해 왔다.**

`ROADMAP_V2` F-4 표의 열은 **「정보」**다 — "재무건전성"은 정보고, 그걸 Piotroski로 낼지 Novy-Marx GP/A로 낼지 Ohlson으로 낼지는 그 표 어디에도 없다. 배수도 마찬가지다.

🔑 **그런데 모델 층의 조사는 이미 되어 있다.** `MODEL_UNIVERSE_63`(63개 우주 × 재현 비용) · `MODEL_ROSTER`(원전 확보 4단계) · `MODEL_BUILD_ORDER`(관문 필터) · `MARKET_MODEL_USAGE_TOP20`(애널리스트 2,263건·CFA 1,980명·펀드매니저 692명).

🔴 **판정만 안 났다.** `MODEL_ROSTER.md` 머리가 스스로 적었다 — *"08-07의 63개 모델 로스터·관문 표는 그 뒤로 **한 번도 다시 열리지 않은 채** 남아 있었다."* 원인도 같은 줄에 있다: 2026-08-08에 `CLAUDE.md`가 「모델」 기준에서 「질문」(Q0~Q5) 기준으로 바뀌었는데, 그건 08-07 조사의 질문에 대한 답이 아니라 **다른 질문으로 옮겨간 것**이었다.

🔴 **그래서 08-07 순위표의 기준 셋 중 둘이 지금 무효다.** 기준이 *"이용가치 × 재현 가능성 × **우리 정의**"*였는데 「우리 정의」는 WHY·HOW·WHAT이 전면 재정의됐고(`ROADMAP_V2`), 걸러낸 **관문 7개(G1~G7)는 폐기**됐다(`ROADMAP_V2` F-2). 표 안에 KR도 섞여 있다(4위 *"KR·US 유일 즉시"* · 6위 *"KRX 공표"*).

🔑 **살아있는 것은 재료다** — 63개 우주 · SEC 태그 실측 기반 재현 비용 · 이용가치. 사실이라 기준이 바뀌어도 안 변한다.

---

## ⓪ 전제 점검

### ⓪-1. 직전 산출물 — 원문 인용
| 출처 | 인용 |
|---|---|
| `ROADMAP_V2` F-1 | 관문 3개 — ① 원하는 **정보**인가 → ② 정확한가 → ③ 정직한가 |
| `ROADMAP_V2` F-1-1 | *"차이는 계산이 아니라 문장이다"* — 모델을 자르는 게 아니라 문장을 자른다 |
| `ROADMAP_V2` F-2 | 🔴 **구 관문 7개(G1~G7) 폐기** |
| `ROADMAP_V2` F-4 | 정보 7행 × 관문 통과 × 재료 상태. 🔴 *"판정이 아니다"* |
| `ROADMAP_V2` F-4-1 ② | *"새 모델 1개를 골라야 한다는 전제 자체가 성립하지 않았을 수 있다"* — 미판정 |
| `probe_1040` (정본) | 🔑 **0층(업종·이익상태) → 1층(배수·재무건전성·시장내재기대·배당·부도위험) → 시간축(최근변화)** |
| `MODEL_UNIVERSE_63` §2 | 구 순위 1위 EV/EBITDA · 2위 P/E · 3위 P/B · 4위 이동평균 · 5위 P/S류 · 6위 배당수익률 · 7위 DuPont · 8위 Piotroski |
| `MODEL_UNIVERSE_63` §3-1 | *"선행 작업이 하나뿐이고, 그게 전부를 푼다 — 재무 원자료 DB 적재. `financials` 0행"* |
| `MODEL_UNIVERSE_63` §3-3 | *"역DCF는 낭비가 아니라 인프라였다"* — 1~5위 중 넷이 그 파이프라인 위에서만 성립 |
| Cowork DB 실측(2026-08-15) | `financials` **0행**(08-07과 동일) · `dividends` **60행** · `us_valuation` 26,741 · `revdcf_results` 8,456 · `us_sector_relative` 7,541 |

### ⓪-1b. 🔴 기존 답 확인 — 재조사 금지
`MODEL_UNIVERSE_63_2026-08-07` · `MODEL_ROSTER` · `MODEL_BUILD_ORDER_2026-08-07` · `MODEL_DEMAND_SURVEY_2026-08-07` · `MARKET_MODEL_USAGE_TOP20_2026-08-07` · `probe_1037` · `probe_1038` · `probe_1040` · `DRAFT_MODEL_SELECTION_RECOVERY.md`
🔴 **63개 모델을 새로 검색하지 않는다.** 웹 재조회는 **기존 문서에 공백이 명시된 칸에만** 허용하고, 했으면 어디를 왜 열었는지 적는다.
🔴 `KNOWN_ANSWERS.md` 확인. 🔴 이 칸이 비면 미완성.

### ⓪-2. 슬롯 — 없다(도출 전용)

### ⓪-3. 걸리는 금지사항
- [x] 🔵 **코드 diff 0 · DB 쓰기 0 · 크론 호출 0.** 🔴 **DB 읽기는 허용**
- [x] 🔴 **선택지 나열 금지** — *"A안/B안 중 고르세요"를 쓰지 않는다*
- [x] 🔴 **창작 금지** — 재현 비용·이용가치를 추정으로 채우지 않는다. 없으면 「미측정」
- [x] 🔴 **구 관문 7개를 쓰지 않는다** — 폐기됐다(F-2). 관문 **3개**로 다시 건다
- [x] 🔴 **KR 전면 동결** — 구 순위표의 KR 근거(4위·6위)는 US 기준으로 다시 판정한다
- [x] `data/us_symbols.json`·`.github/workflows/**`·`vercel.json` 수정 금지 · `REVDCF_ENABLED` OFF 유지
- [x] 🔴 **유료 가입·API 키 발급 금지** · robots.txt 우회 금지 · `api.nasdaq.com` 접근 금지

### ⓪-3b. 🔴 결함의 소재 확정
- 🔴 **「정보」와 「모델」을 섞지 않는다.** 정보(F-4·probe_1040)는 **무엇을 보여줄지**, 모델은 **그 정보를 어떤 계산으로 낼지**. 각각 다른 열
- 🔴 **「모델」과 「인프라」를 섞지 않는다.** 재무 원자료 적재는 모델이 아니다. 순위표의 행이 아니라 **선행 층**으로 따로 적는다
- 🔴 **08-07 수치를 그대로 베끼지 않는다.** 재료 상태는 **오늘 DB로 다시 잰다**(8일 지났다)

### ⓪-4. 🔴 반증 조건

| 관측 | 결론 |
|---|---|
| 새 기준으로도 **EV/EBITDA 1위** | ✅ 구 순위 유효 — **기준만 갱신**됐다고 적는다 |
| 🔴 **순위가 크게 바뀐다** | 🔑 **재정의의 실질** — 무엇이 바꿨는지(관문 3개인지 0층인지) 축을 지목한다 |
| 🔴 **상위 모델이 0층(업종·이익상태)을 만족 못 시킨다** | 🔴 0층이 모델보다 선행인지 보고. 순위표를 고치지 말고 **선행 층으로 분리** |
| 🔴 **상위 모델 대부분이 `financials`에 의존** | 🔴 **인프라가 실질 1순위** — §3-1이 8일 지나서도 유효하다고 적는다 |
| 1위가 **이미 있는 것** | 🔑 F-4-1 ②(*"새 모델 찾기가 아니라 1위를 제대로 만들기"*)가 **자동으로 답이 나온다**. 별도 판정 불필요해짐 |
| 63개 중 새 기준을 적용할 수 없는 것이 다수 | 🔴 방법 한계 — 몇 개를 왜 못 걸었는지 수를 적는다 |

### ⓪-5. 3번 생각
1. **범위** — **모델 선정과 순위**까지다. 🔴 화면 배치·문구·구현 방법은 이 STEP이 아니다
2. 🔴 **표본 — 63개 전수.** 기존 표를 읽는 작업이라 비용이 낮다. 🔴 **상한을 걸 이유가 없다.** 못 거는 것은 이유와 함께 적는다
3. 🔴 **「전수의 조건」 적용** — 순위를 두 방법으로 검증: ① **새 기준 적용 결과** ② **구 순위(08-07)와의 차이 설명**. 🔴 **차이를 설명 못 하면 둘 중 하나가 틀린 것이므로 그렇게 적는다**

---

## 1. 할 일 — 🔴 이 순서를 지킨다

### 1-1. 정보 ↔ 모델 매핑 (재조사 없음)
`probe_1040`의 **0층·1층·시간축** 각 칸에 `MODEL_UNIVERSE_63`의 63개 중 **어느 모델이 후보인가**를 붙인다.
- 🔴 한 정보에 여러 모델이 붙는다(예: 재무건전성 ← Piotroski / Novy-Marx GP/A / Ohlson / DuPont)
- 🔴 **어느 칸에도 안 붙는 모델**은 따로 모은다 — 우리 구조 밖이거나 우리 구조에 빠진 칸이 있다는 뜻
- 🔴 **후보가 0개인 정보 칸**도 표시한다(부도위험 등)

### 1-2. 🔑 새 기준 적용 (이 STEP의 핵심)

각 후보 모델에 **네 축**을 건다. 🔴 **08-07 수치를 베끼지 말고 오늘 상태로 판정한다.**

| 축 | 무엇 | 출처 |
|---|---|---|
| **① 관문 3개** | 원하는 정보인가 / 정확한가(우리가 값을 매기지 않는가) / 정직한가(검산 가능한가) | `ROADMAP_V2` F-1·F-1-1 |
| **② 0층 적용성** 🔑 | **업종에 따라 뜻이 달라지는가** · **적자면 성립하는가** — 🔴 **08-07 순위에 없던 새 축이다** | `probe_1040` 1-2 |
| **③ 재료** | 🔴 **오늘 DB로 실측.** 지금 계산 가능 / 태그 추가 필요 / `financials` 대기 / 불가 | Supabase 조회 |
| **④ 이용가치** | 애널리스트·CFA·펀드매니저 실사용률 | `MARKET_MODEL_USAGE_TOP20` 인용 |

🔴 **④에 대한 주의**: 전문가 사용률은 **개인 수요의 대리값이 아니라 「무엇이 그 판단에 실제로 드는가」의 증거**로 쓴다. 🔴 `ROADMAP_V2` W-2-2가 이걸 *"타깃이 어긋난다"*고 깎아놨는데 **그 판정은 2-1에서 정정 대상**이다.

**출력**: 후보 표 — `모델 · 정보 칸 · 관문 3 · 0층 적용성 · 재료(오늘) · 이용가치 · 종합`

### 1-3. 순위 도출 — 하나의 답
1-2의 표에서 **하나의 순위**를 낸다. 🔴 **선택지가 아니라 도출 결과다.**
- 🔴 **정렬 규칙을 먼저 명시하고 그다음 정렬한다**(사후에 순위에 맞춰 규칙을 쓰지 않는다)
- 각 순위에 **어느 축에서 갈렸는지** 좌표를 단다
- 🔴 **동점은 동점으로 둔다** — 억지로 줄 세우지 않는다(`probe_1040` 1층과 같은 원칙)

### 1-4. 🔴 선행 층 분리
모델이 아닌 것을 순위표에서 빼서 따로 적는다.
- **재무 원자료 적재**(`financials` 0행) — §3-1이 *"이 하나를 하면 1~8위가 전부 풀린다"*고 했다. 🔴 **오늘도 0행임을 확인했다**. 8일 지나서도 유효한지 판정
- **0층(업종·이익상태)** — 모델이 아니라 전제. 🔴 이게 없으면 상위 모델이 몇 %에서 틀리는지 `probe_1040` 실측(32.5%·43.4%)을 인용해 적는다
- 🔴 **선행 층과 모델 순위 중 무엇이 먼저인지 판정하고 근거를 댄다**

### 1-5. 구 순위와의 대조 — 🔴 도출 이후에만
`MODEL_UNIVERSE_63` §2의 구 순위와 나란히 놓는다.
- 유지된 것 / 순위가 바뀐 것 / 탈락한 것 / 새로 들어온 것
- 🔴 **각 변동에 원인 축을 지목**한다(관문 3개 때문인가, 0층 때문인가, US 단독 때문인가, 재료 변화 때문인가)
- 🔴 KR 근거로 순위에 올랐던 4위·6위를 US 기준으로 다시 판정

### 1-6. F-4-1 ②에 답한다
1-3의 1위가 **이미 있는 것**인지 확인한다.
- 있으면 → *"새 모델 찾기가 아니라 1위를 제대로 만들기"*가 **사실로 확인**된 것. 🔴 그러면 **「제대로」의 정의**(무엇이 미완인지)를 구체적으로 적는다
- 없으면 → 새로 만드는 것이 맞다. 재현 비용과 선행 조건을 적는다

### 1-7. 산출물
`docs/probe_1041_model_selection.md`
- 맨 앞 **도출된 순위 한 장** + ⓪-4 매트릭스 결과
- 1-1 매핑 · 🔑 **1-2 네 축 표** · 1-3 순위 · 1-4 선행 층 · 1-5 구 순위 대조 · 1-6 F-4-1 ② 답
- 🔴 3중 규칙 4종: 못 한 축(시도 기록) / 아직 안 함 / 철회·정정 / 미측정

🔴 `ROADMAP_V2.md` **「무엇을 먼저 만드는가」 층(281행)을 이 결과로 갱신**하고 상태를 바꾼다. `roadmap_v2.html` 동기화.

---

## 2. 🔴 함께 고치는 것 — 이번에 확인된 정정 3건

### 2-1. `ROADMAP_V2` W-2-2
현재 *"전문가 사용률을 개인 수요의 대리값으로 쓰면 타깃이 어긋난다"*로 적혀 있다.
🔴 **장은태 정정(2026-08-15)**: 원하는 것·사용하는 것·필요한 것은 어긋나는 게 아니라 **하나의 사슬**이다 — 동기는 하나(*"오를지 내릴지 알고 싶다"*)이고, 그 판단에 필요한 게 정보이며, 그 정보가 일반인에게 어려우니 우리가 모델로 정확히 계산해 쉽게 푼다.
→ 전문가 자료는 **수요 증거가 아니라 「무엇이 그 판단에 드는가」의 증거**다. **타깃 어긋남이 아니라 애초에 다른 질문에 답하는 자료**로 고쳐 쓴다.
🔴 F-1-1이 이미 같은 말을 적어놨다 — *"필요한 것을 주면 원하는 것에 스스로 도달한다"*. **W-2-2가 F-1-1과 모순돼 있었다.**

### 2-2. `probe_1040` 1-7 ①
*"43.4%가 32.5%보다 크고"* — 🔴 **서로 다른 양이다.** 32.5%는 「업종을 무시하면 판정이 바뀌는 비율」(302/928), 43.4%는 「적자라서 PER이 아예 성립 안 하는 비율」(1,584/3,651)이고 모집단도 다르다. 같은 문서 1-3이 「성립불가」를 「강/중/약」과 **별개 범주**로 정의해놓고 결론에서 한 자에 올렸다.
→ 🔴 **결론(이익상태 = 0층 전제)은 유지**한다. 성립 자체가 안 되는 것은 판정이 바뀌는 것보다 강한 의존이기 때문이다. **근거 문장만 고친다** — 크기 비교가 아니라 **범주 차이**로 서술.

### 2-3. `ROADMAP_V2` W-2-3
*"서로 순서 없음(**측정된 상호 의존 0건**)"* → 정본 `probe_1040` 1-3은 *"측정하지 않았다 — 측정 방법 자체를 못 찾았다"*이다. 현재 표현은 *"재봤더니 없더라"*로 읽힌다.
🔴 같은 문서 W-2가 인용한 `LENS_DISPOSITION` §2 — *"「없다」는 조사 부족의 결과일 수 있다"* — 에 정확히 걸린다.
→ **"미측정 — 측정 방법을 못 찾았다"**로 고친다.

---

## 3. 커밋 전 게이트
게이트 7 · 게이트 9 · `CHANGELOG` 1041 · `STEP_LEDGER` 등재. 🔵 배포 불필요, 커밋·푸시는 한다.

## 4. DoD
- [ ] ⓪-1b 기존 답 확인 — 🔴 **63개를 재검색하지 않았다**
- [ ] 1-1 정보 ↔ 모델 매핑 · 🔴 **어느 칸에도 안 붙는 모델 / 후보 0개인 칸을 표시**
- [ ] 🔑 **1-2 네 축 전부 · 🔴 재료는 오늘 DB로 실측(08-07 인용 아님)**
- [ ] 🔴 **② 0층 적용성 축을 63개 후보 전부에 걸었다**(08-07에 없던 축)
- [ ] 1-3 **하나의 순위** · 🔴 정렬 규칙을 먼저 명시 · 🔴 동점은 동점으로
- [ ] 1-4 선행 층 분리 · 🔴 **선행 층 vs 모델 순위 중 무엇이 먼저인지 판정·근거**
- [ ] 🔴 **1-5 대조는 도출 이후에 했다** · 변동마다 원인 축 지목 · KR 근거 4위·6위 재판정
- [ ] 1-6 F-4-1 ②에 답했다
- [ ] **2-1·2-2·2-3 정정 3건 반영**
- [ ] `ROADMAP_V2` 281행 층 갱신 + html 동기화
- [ ] 코드 diff 0 · DB 쓰기 0
- [ ] 마지막에: 못 한 축 / 아직 안 함 / 철회·정정 / 미측정

🔴 **「정보」와 「모델」과 「인프라」는 각각 다른 층이다 — 한 표에 섞지 않는다.**
🔴 **1위가 정해지면 다음은 그것 하나를 완성하는 일이다.**
