# STEP 1083 — 🔴 **「최근변화」 칸 완성 착수** (모멘텀+기술) — 결함 수정 + 9항목 검증 (🔴 **코드 diff 발생** · DB 쓰기 0 · 🔴 **라이브 화면 값 변동** · 크론 수동 실행 0)

> 🔴 **이 STEP은 STEP1055 이후 처음으로 코드를 고친다.** 지금까지 28개가 전부 문서 전용이었다.
> 🔴 **라이브 화면 수정 승인** — 7렌즈 라이브 화면 수정은 매번 별도 승인 대상이고, **장은태가 이 명령어를 실행한 것이 그 승인**이다(2026-08-19). 단 **아래 범위 밖은 승인되지 않았다.**
>
> **장은태 판정(2026-08-19) — 이 STEP이 집행하는 것:**
> | 판정 | 채택 | 좌표 |
> |---|---|---|
> | `LENS_DISPOSITION §7-4` **#2** | **①** `avg(r1,r3)` 폐기 + ±5%→분포컷 전환 + `adjUsed` 노출 + note 정정(**4건 전부**) | §7-4 #2 |
> | 같은 문서 **#5** | **①** `pos52`에 `sma`·`rsi`와 같은 최소표본 가드 추가 | §7-4 #5 |
> | 같은 문서 **#3** | **②** 현행 유지 + 공개 문구(🔴 **코드 무변경** — API 소비처 4곳 수정은 **승인 대상 아님**) | §7-4 #3 |
>
> **왜 지금인가 — 3번 규칙:**
> **축 A(원문)** — 판정 B 확정(*"확정된 모델을 1개씩 완성한다"*, `BUILD_SEQUENCE` §6-B) · `CLAUDE.md:82` 규칙4 *"모델 완성이 UI보다 먼저"* · §7-4 #2·#3·#5 전부 *"재료가 더 필요한가 = **불필요**"*.
> **축 B(실무)** — 확정된 칸 넷 중 **새 판정 없이 착수 가능한 유일한 칸**. 재무건전성=9/9 충돌 판정 2건 · 배수+역DCF=`Q1_ENABLED` 승인 선행.
> **축 C(반대 증거)** — ㉠🔴 **`CLAUDE.md` 규칙1 위반이 라이브 노출 중**: *"원전에 없는 산출물·지표를 추가하지 않는다"* ↔ `avg(r1,r3)`(`lenses.ts:176`), 감사가 **㉢기만 깨짐**으로 분류 ㉡9항목 ⑨「라이브 실측」을 밟을 수 있는 **유일한 칸**(화면에 이미 있음) ㉢🔴 **반대로, 산식을 바꾸면 화면 값이 움직인다** — §3의 before/after 실측이 필수인 이유.

---

## ⓪ 전제 점검 — 🔴 고치기 전에 전부 한다

### ⓪-1. 원전부터 (🔴 `BUILD_SEQUENCE §2` ②단계 — 저장소를 먼저 뒤진다)
`avg(r1,r3)`를 **폐기한 뒤 무엇을 쓰는가**가 이 STEP의 핵심이다. **창작 금지.**
1. `docs/LENS_AUDIT_02_MOMENTUM_2026-08-07.md` **§제안(5개 항목) 전문**을 연다 — §7-4 #2가 이것을 「확보된 재료」로 지목했다.
2. 모멘텀 **원전**(Jegadeesh–Titman 등)이 저장소에 있는지 먼저 뒤진다(`docs/`·`data/sources/`). 🔴 **없으면 외부 조사 전에 「저장소에 없음」을 먼저 기록**한다.
3. 🔴 **원전이 정한 산식이 아니면 쓰지 않는다.** 원전을 못 찾으면 **규칙 5-1(정의 공개표 5단계)**로 대체하고, 그 사실을 화면에 밝힌다.
4. 🔴 **대체 산식을 Cowork/실행 측이 지어내면 그건 §7-4 #2 ①의 범위 밖이다** — 그 경우 **고치지 말고 멈추고 보고**한다.

### ⓪-2. 현재 코드 확인 (🔴 줄 번호가 감사 시점과 다를 수 있다)
```
lib/lenses.ts:156        # ±5% 하드코딩 (#8)
lib/lenses.ts:176        # avg(r1,r3) 창작+이중계산 (#7)
lib/lenses.ts:201~204    # pos52 가드 없음 (#15)
lib/lensCompute.ts:129~130  # adjUsed 미노출 (#9)
lib/lensCopy.ts          # momentum note — FF 오귀속 (#10)
lib/lensTones.ts         # STATE_SPEC (#16 — 🔴 코드 무변경, 문구만)
lib/lensCuts.ts          # 분포컷 기존 패턴 — 🔴 새로 만들지 말고 재사용
```
🔴 **줄 번호가 다르면 다른 대로 기록**하고, `git log`로 그 사이 변경이 있었는지 확인한다.

### ⓪-3. 🔴 롤백 경로 확보
- 착수 전 커밋 해시를 **기록**한다(`git rev-parse HEAD`).
- 🔴 **한 커밋으로 몰지 않는다** — #7·#8(산식·컷 변경, 값 이동) / #9·#10(표기) / #15(가드) 를 **분리 커밋**한다. 값이 이상하면 산식 커밋만 되돌린다.

---

## §1. 수정 — §7-4 #2 ① (모멘텀 4건)

| # | 결함 | 할 일 | 🔴 주의 |
|:--:|---|---|---|
| **#7** | `avg(r1,r3)` 창작+이중계산 (`lenses.ts:176`) | ⓪-1 결과에 따라 **폐기**. 대체가 원전에 있으면 원전대로, 없으면 **단기축 제거**(§8-3이 *"재설계 **또는** 단기축 제거"*라 적음) | 🔴 **지어낸 산식으로 대체 금지.** 애매하면 **제거** 쪽이 창작 금지에 맞다 |
| **#8** | ±5% 하드코딩 (`lenses.ts:156`) | **분포컷으로 전환** — `lensCuts.ts`의 기존 컷 산출 패턴 재사용 | 🔴 **컷이 움직이면 판정이 바뀐다.** §3에서 반드시 잰다 |
| **#9** | `adjUsed` 미노출 (`lensCompute.ts:129~130`) | 플래그를 **화면까지 노출**. 어떤 종목이 raw인지 사용자가 알 수 있게 | 혼재 자체는 남는다 — **남는다는 사실도 밝힌다** |
| **#10** | FF 오귀속 (`lensCopy.ts` momentum note) | **문구만 정정** — 계산 불변 | 🔴 정확한 출처를 못 대면 **출처 주장 자체를 뺀다**(과장보다 무주장이 정직) |

## §2. 수정 — §7-4 #5 ① (기술 1건) + #3 ② (문구만)

- **#15** `pos52` 최소표본 가드 (`lenses.ts:201~204`) — 🔴 **`sma`·`rsi`가 쓰는 가드 형태를 그대로 재사용**한다. 새 임계값을 만들지 않는다. 기존 것과 다르게 할 이유가 있으면 **하지 말고 보고**.
- **#16** 집계 레이어 — 🔴 **`lensTones.ts` 코드 무변경.** *"참고용 렌즈도 동일 가중"*임을 **공개 문구로만** 밝힌다(§7-4 #3 ②의 원문 그대로).

---

## §3. 🔴 before/after 실측 (이 STEP에서 가장 중요)

수정 **전후로** 같은 조회를 돌려 비교한다:

| 항목 | 재는 법 | 🔴 기준 |
|---|---|---|
| **모멘텀 판정 분포** | `lens_scores`에서 모멘텀 verdict(pos/warn/neutral 등) 개수 | 분모 명시 · UTC 시각 병기 |
| **판정이 바뀐 종목 수** | before/after 종목 단위 대조 | 🔴 **몇 종목이 어느 방향으로 바뀌었는지** |
| **기술 `pos52` na 전환** | 가드 추가로 na가 된 종목 수 | 신규 상장주가 주 대상일 것 — 확인 |
| **극단치** | 바뀐 종목 중 상·하위 10개를 **손으로 본다** | 🔴 값이 말이 되는지 육안 |

🔴 **판정이 바뀐 종목이 예상보다 크면(예: 30% 이상) 커밋하지 말고 멈추고 보고한다.** 컷 전환이 의도보다 크게 흔든 것일 수 있다.
🔴 **크론을 수동 실행하지 않는다** — 이미 쌓인 값으로 재고, 재계산이 필요하면 **로컬에서만** 돌린다(DB 쓰기 0).

---

## §4. 9항목 검증 (`LENS_COMPLETION_STANDARD`)

수정 후 **최근변화 칸(모멘텀+기술)**에 9항목을 대본다:

| # | 항목 | 🔴 이 칸의 기지 상태 |
|:--:|---|---|
| 1 | 원전 대조표 | ⓪-1 결과에 따름 — 원전 없으면 **정의 공개표**(규칙 5-1) |
| 2 | 입력 검증 | |
| 3 | 값 검증 | 🔴 **기술은 손계산 구조적 불가**(원시 종가 DB 미저장 + 외부 재취득 금지) — `LENS_DISPOSITION §8-1`. **못 하면 "구조적 불가"라 쓴다** |
| 4 | 컷·분포 | #8 전환 결과 |
| 5 | 경계 처리 | #15 가드 |
| 6 | 주장 정합 | #10 문구 · #16 공개 문구 |
| 7 | 화면 일관성 | #9 노출 |
| 8 | 테스트 | |
| 9 | 라이브 실측 | §3 결과 |

🔴 **9/9가 안 되면 「몇/9」인지 정확히 쓰고, 안 된 항목마다 「왜」를 한 줄씩.** 🔴 **억지로 ✅ 붙이지 않는다** — `BUILD_SEQUENCE:106`이 *"현재 9/9 통과한 모델은 0개다"*라 적어둔 것을 뒤집는 일이라 특히 엄격하게 본다.

---

## §5. 문서 반영

- `docs/LENS_DISPOSITION_2026-08-08.md` §7-4 **#2·#3·#5**에 🟢 **판정 채택 기록**(2026-08-19 장은태 · 채택 선택지 · STEP1083). 🔴 **원 선택지 지우지 않음.**
- `docs/BUILD_SEQUENCE.md` §6-B-1 **최근변화 행** — 「확정의 범위」를 9항목 결과로 갱신 · §6-C 대장의 해당 행(#2·#3·#5 대응)을 🟢**판정됨**으로.
- `docs/model_list_30_9.html` 「확정된 칸 넷」 최근변화 행 갱신.
- `docs/CHANGELOG.md`.
- 🔴 표시본은 **머리 요약과 본문 둘 다**(`BUILD_SEQUENCE.md:304`). 🔴 **`~/Downloads/` 무접촉.**

---

## §6. 검증 3 / §7. 검수 3

**검증 3** — ① **원전 대조**: 새 산식(또는 제거)이 ⓪-1의 원전/§제안과 일치하는지, 지어낸 것이 **0개**인지 ② **before/after**: §3을 정방향(분포)·역방향(종목 단위 전환) 둘로 재고 수가 맞는지 ③ **역방향 문서**: 이 수정이 사실이라면 어느 문서가 더 바뀌어야 하나(최소 `LENS_AUDIT_02`·`LENS_AUDIT_05`·§8-2 결함표) — 안 바꾼 곳은 사유 한 줄.

**검수 3** — ① **창작 안 했는가**: 지어낸 산식·임계값 **0개** · 애매하면 제거를 택했는지 ② **범위 밖 안 건드렸는가**: `lensTones.ts` 코드 **무변경** · API 소비처 4곳 **무변경** · `REVDCF_ENABLED`·`Q1_ENABLED` **무접촉** · `data/us_symbols.json`·`.github/workflows/**`·`vercel.json`·`supabase/migrations/**` **무접촉** · DB 쓰기 0 · 크론 수동 실행 0 ③ **롤백 가능한가**: 분리 커밋 3덩어리 · 착수 전 해시 기록 · `git status` 신규 = `docs/step_orders/STEP1083.md` 하나.

---

## §8. 커밋 + 보고

🔴 **`main` 직접 push.** 브랜치·PR·worktree 금지. 🔴 **CI·Vercel 배포 확인까지 한다**(라이브 화면이 바뀌므로).
커밋 메시지(3덩어리): `fix(lenses): STEP1083 모멘텀 산식 — avg(r1,r3) 폐기 + 분포컷 전환` / `fix(lenses): STEP1083 표기 — adjUsed 노출 + note 정정` / `fix(lenses): STEP1083 pos52 최소표본 가드`

```
[STEP1083 결과]
⓪ 원전: LENS_AUDIT_02 §제안 확인 / 저장소 원전 있음·없음 / #7 처리 = 원전대체 or 단기축제거 (근거)
⓪ 롤백: 착수 전 해시 = xxxx · 분리 커밋 3개?
① 모멘텀 4건(#7·#8·#9·#10): 각각 성공/실패 · 지어낸 산식·임계값 개수 = 0?
② 기술(#15) + #16 문구: 성공/실패 · lensTones.ts 코드 무변경 확인
③ before/after: 판정 바뀐 종목 N/M (X%) · 방향 분포 · pos52 na 전환 N종목 · 극단치 육안 결과
   🔴 30% 이상이면 멈췄나?
④ 9항목: N/9 — 안 된 항목마다 사유 (특히 ③값검증 = 구조적 불가?)
⑤ 문서 반영: §7-4 #2·#3·#5 채택 기록 / §6-B-1 / §6-C / WHAT / CHANGELOG / 표시본
⑥ 검증 3 · ⑦ 검수 3
🔴 못 한 것: (전부 나열)
🔴 조회 시각(UTC):
커밋: 3개 해시 · CI · Vercel 배포
```
