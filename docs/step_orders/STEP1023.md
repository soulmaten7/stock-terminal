# STEP 1023 — 322건의 낡은 주식수가 실제로 어디에 닿는가 (읽기 전용 · DB 쓰기 0)

> 🔴 **오늘 밤 22:45 UTC가 W1(4단계 heartbeat)의 진짜 첫 시험이다.** `app/api/cron/revdcf/route.ts` **diff 0**.
> 🔴 이 STEP은 **커버리지가 아니라 정확도**를 다룬다. 게이트와 무관하다.

---

## 0-A. 🔴 축이 바뀌었다 — 분자 경로가 전부 소진됐다

1022로 시가총액 **커버리지**를 올릴 분자 쪽 경로가 전부 닫혔다.

| 경로 | 결과 | 근거 |
|---|---|---|
| 야후 `quote` | 93.9% | 16일째 게이트 미달 |
| 야후 `quoteSummary` | 0/284 | STEP 1010 |
| 나스닥 스크리너 | 접근 불가(robots.txt · 60초도 timeout) | STEP 1017·1018 |
| SEC 자체 조립 | 96.95%(엄격 95.96%) | STEP 1019·1020 |
| **SEC 조회창 확장** | 🔴 **+18건(9.9%), 그중 17건은 중앙값 5.3년(최대 16.6년) 낡아 실질 +1건** | **STEP 1022** |

🔴 **남은 커버리지 경로는 분모(유니버스 재정의)뿐이고, 그건 분류 정확도 검증이 선결이다**(1021이 "CEF/신탁" 320건 중 7건이 전부 REIT였음을 발견).

**그런데 1022가 커버리지보다 큰 걸 찾았다.**
🔑 **이미 SEC 주식수가 채워진 4,557건 중 322건(7.07%)이, 더 최신 값이 있는데도 며칠~몇 달 낡은 값을 쓰고 있었다**(시점 중앙값 49일).
🔴 **조회창 결함이 결측분에만 있는 게 아니라 "정상"으로 표시된 데이터에도 번져 있었다.**

**이 STEP은 그 322건이 실제로 어디에 닿는지 잰다.** 커버리지는 이 STEP의 관심사가 아니다.

---

## 0-B. 🔴 여섯 번째 소표본 오판 — 내 책임

STEP 1021이 10종목 표본으로 **"최대 약 420건 회복 가능"**이라고 추정했고, 1022 전수가 **18건**으로 뒤집었다.
🔴 **그 10종목 상한은 내(Cowork)가 STEP 1021 §1-4에 걸었다.** 이유로 "SEC 부담 최소화"를 적었는데, 전수 비용은 10 req/s 기준 **약 18초**였다.
🔴 이미 `_TEMPLATE.md` ⓪-5에 규칙으로 반영했다(**표본 상한을 걸려면 전수 비용을 숫자로 적고, 분 단위 이내면 전수로 한다**). 이 STEP부터 그 칸이 비면 미완성이다.

---

## ⓪ 전제 점검

### ⓪-1. 직전 STEP 산출물 — 원문 인용 (검산 포함)

| 출처 | 인용 | 검산 |
|---|---|---|
| STEP 1022 판정 | **복합 — 두 번째·세 번째·네 번째 갈래가 동시에 사실.** 182건 전수(companyfacts, 창 없이 전체 이력) 재조회 → **회복 18건(9.9%)**, 그중 **17건은 시점 중앙값 5.3년(최대 16.6년)** | ✅ 18/182 = 9.89% |
| STEP 1022 🔑 | **이미 채워진 4,557건 전수(100%) 재조회 → 322건(7.07%)이 더 최신 값을 두고 며칠~몇 달 낡은 값을 쓰고 있었다**(중앙값 **49일**) | ✅ 322/4,557 = 7.066% |
| STEP 1022 | 🔴 조회창 결함이 **결측분에만 있는 게 아니라 "정상"으로 표시된 데이터에도 번져 있다** | — |
| STEP 1022 | 🔴 STEP1021의 "최대 약 420건 회복 가능"을 전수로 뒤집음 — **여섯 번째 소표본 오판** | — |
| STEP 1022 | 유니버스 분모 표기 오류 **5,973 → 5,976** 직접 재확인해 정정(검산 없이 STEP 전체를 반복했던 것) | 🔴 **이후 모든 비율은 5,976 기준** |
| STEP 1022 미측정 | 조회창 수정 여부 · **322건 반영 시 다운스트림 영향** · 유니버스 재정의 — 전부 장은태 판정 |
| STEP 1021 | CEF/신탁 제외 시 커버리지 98.39% — 🔴 **채택 안 함**. 분류 정확도 미검증(320건 중 7건이 전부 REIT) |
| 현재 | US `lens_cuts` 07-30 **17일째** · `us_sector_relative` 08-10 **5일째** · `us_market_cap_nasdaq` 0행 · **W1 오늘 밤 첫 시험** |

### ⓪-2. 이 STEP이 건드리는 슬롯

| 슬롯# | 검증 상태 | 이 STEP의 취급 |
|---|---|---|
| **#2 발행주식수** | 🔴 **322건이 낡은 값** · `source_tags`에 `shares` 키 없음 | 🔑 **이 STEP의 대상** |
| **#1 시가총액** | 야후 93.9% · 조립 경로 소진 | 🔴 **다루지 않는다** |
| #4~#10 재무제표 | ✅ 1011·1014 | 322건이 이들과 같은 행에 있는지 확인 |

### ⓪-3. 걸리는 금지사항 (해당되는 것만)
- [x] 🔴 **DB 쓰기 0** — 322건을 **고치지 않는다.** `us_fundamentals`·`revdcf_results`·`us_valuation` 어디에도 쓰지 않는다
- [x] 🔴 **조회창 상수 수정 금지** — 프로브에서만 다른 값으로 조회. 프로덕션 `lib/**` diff 0
- [x] 🔴 **`app/api/cron/revdcf/route.ts` diff 0** — 오늘 밤 W1 진짜 첫 시험 보호
- [x] 🔴 **게이트 임계(97%/95%) 불변** · `BUDGET_MS`·`maxDuration` 불변 · `lib/lensPrecompute.ts` 수정 금지
- [x] 🔴 **재계산은 프로브 안에서만.** 🔴 결과를 DB에 쓰거나 화면에 반영하지 않는다
- [x] 🔴 **`data/us_symbols.json` 수정 금지** · **유니버스 재정의 논의 금지**(이 STEP 범위 밖)
- [x] 🔴 **SEC 레이트리밋 준수** — 10 req/s · UA 필수 · `nextAt` 동기예약(994). 🔴 **1022가 받아둔 결과를 재사용**할 수 있으면 재조회하지 않는다
- [x] 🔴 **크론 수동 실행 금지**(특히 `email-brief` = 실제 메일 발송) · **7렌즈 라이브 화면 수정 금지** · D 조회 키 버그도 고치지 않는다
- [x] `REVDCF_ENABLED` Production OFF 유지 · `.github/workflows/**` · `vercel.json` 수정 금지 · robots.txt 우회 금지
- [x] API 키 발급·유료 가입 금지 · **KR 전면 동결**

### ⓪-4. 🔴 반증 조건

| 관측 | 결론 | 다음 축 |
|---|---|---|
| 322건이 **역DCF 604 유니버스에 거의 안 걸린다** | 프로덕션 영향이 작다 — 조회창 수정의 우선순위가 내려간다 | 유니버스 축으로 복귀 |
| **상당수가 걸리고 verdict가 바뀐다** | 🔑 **역DCF 판정 일부가 낡은 주식수로 나왔다** | 조회창 수정 판정(장은태) |
| 걸리지만 **verdict는 안 바뀐다** | 값은 틀렸으나 판정은 견고하다 | 우선순위 판정 |
| 🔴 **322건이 `us_fundamentals.shares`가 아니라 1019~1022의 조립용 임시 데이터였다** | 프로덕션 영향 **0** — 아직 배선 안 된 경로의 문제 | 🔴 **그러면 이 STEP은 여기서 끝난다** |

🔴 **네 번째를 §1-1에서 가장 먼저 확인한다.** 이걸 확인하지 않고 영향 산정부터 하면 헛일이 될 수 있다.

### ⓪-5. 3번 생각
1. **범위** — 곁가지가 아니다. **정상으로 표시된 데이터의 7.07%가 낡았다**는 것은 커버리지보다 신뢰도 문제에 가깝고, `ANSWERABILITY_MAP.md`가 정의한 "이 값을 믿어도 되는가"의 핵심이다.
2. **표본** — 🔴 **322건 전수.** 🔴 **전수 비용을 먼저 적는다**: 1022가 이미 5,959건을 조회해뒀으므로 **재조회 0회**로 가능한지 먼저 확인한다. 필요하다면 322건 = 10 req/s 기준 **약 33초** — 상한을 걸 이유가 없다.
3. **시점** — 읽기 전용이라 오늘 밤 W1을 오염시키지 않는다. 🔴 22:45 UTC를 넘기면 §1-5로 값을 읽어 붙인다.

---

## 1. 할 일

### 1-1. 🔴 먼저 확정 — 322건은 어디에 있는가
**이걸 먼저 하지 않으면 나머지가 헛일이다.**
- 1022가 "이미 채워진 4,557건"이라 한 것이 **`us_fundamentals.shares`인지**, 아니면 1019~1022가 만든 **조립용 임시 데이터**인지 확정한다
- 🔴 근거를 `파일:줄번호`로. 프로브 스크립트가 어느 테이블을 읽었는지 직접 확인
- **`us_fundamentals`가 아니면 → ⓪-4 네 번째 갈래. 그 사실만 적고 §1-2~1-4를 건너뛴다**(🔴 억지로 진행하지 말 것)
- 맞으면 계속

### 1-2. 낡음 분포 — 전수
322건 각각의 **현재 값 시점**과 **더 최신 값의 시점** 간격.
- 간격 분포: 중앙값(기지 49일) · **p90 · p99 · 최대**
- 🔴 **주식수 변화율도 함께** — `(최신 주식수 − 현재 주식수) / 현재 주식수`. 절대값 중앙값 · p90 · 최대 · **5% 초과 건수**
- 🔴 간격이 길어도 주식수가 안 변했으면 실질 영향이 없다. **두 축을 교차표로** 낸다

### 1-3. 다운스트림 도달 — 전수
322건이 실제로 쓰이는 경로를 **전수로** 찾는다(1015가 서빙 경로를 훑은 방식 재사용).
- **`revdcf_results` 604 유니버스에 속한 건수** ← 🔴 가장 중요
- `us_valuation`(PBR·PSR 분모 경로)에 닿는 건수
- 화면(`q1/[symbol]` 등)에 표시되는 건수
- 🔴 각 경로에 대해 `파일:줄번호`

### 1-4. 🔴 판정 영향 — 프로브 재계산, DB 쓰기 0
604 유니버스에 걸린 건에 대해 **최신 주식수로 역DCF를 다시 계산**해 본다.
- 🔴 **프로브 스크립트 안에서만.** 결과를 DB에 쓰지 않는다
- 산출: **verdict가 바뀌는 건수** · 바뀐 종목의 before → after 전수 나열 · `gap_years` 변화 분포
- 🔴 **"바뀔 수 있다"가 아니라 실제 재계산 결과로 낸다.** 추정 금지
- 🔴 재계산이 원리적으로 불가능하면(입력이 더 필요하면) **"불가능"이라고 적고 그 이유를 쓴다.** 억지로 근사하지 말 것

### 1-5. 오늘 밤 관측 — 22:45 UTC가 지났으면 읽기만
🔑 **W1의 진짜 첫 시험.**
- 🔴 **먼저 배포 시각이 22:45 UTC보다 앞섰는지 확인**(1020이 겪은 함정)
- `cron_heartbeats.job='revdcf'`의 **`stage`**(`loop_done`/`valuation_done`/`sector_relative_done`/`complete`) ← 🔴 **이게 답이다**
- `elapsedMsAtStage` · `finallyMs` 6구간 · `finallyTotalMs` · `loopMs` · `budgetExhausted` · `sectorRelativeError`(스택) · `sectorRelativeSaved`
- `us_sector_relative` 최신 `as_of`가 08-14로 생겼는지
- 🔴 heartbeat 행 자체가 없으면 **크게 보고**
- `us-perf`의 `nasdaqError`도 함께(🔴 **성공했으면 예상 밖이므로 보고**)
- 🔴 **크론 호출 금지** · **`BUDGET_MS` 판정 금지** — 값만 옮긴다

### 1-6. 산출물
`docs/probe_1023_stale_shares_impact.md`
- 맨 앞에 **⓪-4 네 갈래 중 어느 것인지** + **604 유니버스 도달 건수** + **verdict 변화 건수**
- 1-1 322건의 소재 확정(`파일:줄번호`)
- 1-2 낡음 × 변화율 교차표
- 1-3 다운스트림 경로별 도달 건수
- 1-4 재계산 결과(verdict 전이 전수) 또는 "불가능" 사유
- 1-5 W1 첫 시험 결과 또는 미도래·구코드 사유

🔴 `docs/ANSWERABILITY_MAP.md` **§F(재무 원문 수치)의 한계 고지**에 주식수 낡음(7.07%)을 반영한다. **§3은 건드리지 말 것.**
🔴 카탈로그 슬롯 #2 서술에 **조회창 결함의 전수 결과**를 반영(md·xlsx 동시, 취소선 보존).
🔴 카탈로그에 **0-A 표(분자 경로 소진)**를 절로 추가한다 — 다음에 같은 길을 다시 파지 않도록.

## 2. 커밋 전 게이트
- 게이트 7: git 미추적 파일 참조 검사 — 🔴 런타임 `fs.readFileSync('/tmp/...')`도 잡을 것 · 벌크·중간 산출물 커밋 금지
- 게이트 8: push 후 **Vercel 배포 성공 + verify 초록을 눈으로 확인**하기 전까지 미완료
- `docs/CHANGELOG.md` 1023 항목 · `docs/STEP_LEDGER.md` 1023 등재

## 3. DoD
- [ ] 🔴 **§1-1을 가장 먼저** 했고, `us_fundamentals`가 아니면 그 자리에서 멈췄다
- [ ] ⓪-4 네 갈래 중 어느 것인지 맨 앞에 + 604 도달 건수 + verdict 변화 건수
- [ ] 1-2 낡음 **× 주식수 변화율 교차표**(간격만 보지 않았다)
- [ ] 1-3 경로별 도달 건수, 각각 `파일:줄번호`
- [ ] 1-4 **실제 재계산** 결과(추정 아님) 또는 불가능 사유
- [ ] 🔴 **전수 비용을 명시**했다(재조회 0회 가능 여부 · 필요 시 322건 ≈ 33초)
- [ ] `ANSWERABILITY_MAP.md` §F · 카탈로그 슬롯 #2 · **0-A 분자 경로 소진 절** 반영, md·xlsx 일치, **§3 미변경**
- [ ] 1-5 W1 `stage` 값 또는 미도래/구코드 사유 · 🔴 배포 시각을 크론 시각과 대조했다
- [ ] 🔴 **DB 행수 변화 0 · 프로덕션 코드 diff 0** · `app/api/cron/revdcf/route.ts` diff 0 · `data/us_symbols.json` diff 0
- [ ] tsc 0 · test 전량 통과
- [ ] Vercel 배포 성공 + verify 초록 **눈으로** 확인
- [ ] 마지막에: 못 한 축 / 철회·정정한 것 / 미측정으로 남은 것

🔴 **조회창 수정 · 322건 반영 · 유니버스 재정의는 전부 장은태 판정이다. 이 STEP은 영향의 크기를 재는 것까지다.**
