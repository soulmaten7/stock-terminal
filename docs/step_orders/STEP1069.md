# STEP 1069 — Downloads 표시본을 저장소로 들여놓고 **디테일하게** 합치기 (🟡 문서 전용 · 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 수동 실행 0)

> 🔴 **판정하지 않는다.** 합치되 **새 결론을 만들지 않는다.** 충돌은 양쪽 다 적고 미판정으로 남긴다.
> 🔴 **문제**: 표시본이 **두 곳에 있고 이미 어긋났다.** `docs/roadmap_v2.html`(91,937) ↔ `~/Downloads/roadmap_v2_2.html`(65,755) — **26KB 차이**. `docs/build_sequence.html`(25,482) ↔ `~/Downloads/build_sequence.html`(24,609).
> 그리고 🔴 **`model_list_30_9.html`(WHAT 작업 파일)은 저장소에 한 번도 커밋된 적이 없다** — `git log --all --diff-filter=A -- '*model_list*'` **0건**(2026-08-18 확인).
> 🔴 **`~/Downloads/tombstone.html`이 이 문제를 이미 경고해뒀다** — *"**같은 내용을 두 곳에 두면 한쪽이 반드시 뒤처지고, 그때 어느 쪽이 정본인지 알 수 없게 됩니다.**"* STEP1065가 그 실물을 잡았다(`build_sequence.html`이 신설 5시간 뒤 폐기된 관문 7개를 그대로 달고 **커밋 0건**).
> 🔴 **3번 규칙 전면 적용** — 축 A(원문) · 축 B(실무) · 축 C(반대 증거) · 검증 3 · 검수 3. **못 거친 축은 「못 했다」고 명시한다.**

---

## ⓪ 전제 점검

### ⓪-0. 🔴 3번 규칙을 이 STEP에서 무엇으로 보는가 (축 정의 — 먼저 고정한다)

| 축 | 이 STEP에서의 실체 |
|---|---|
| **축 A — 원문** | **정본 md**(`docs/ROADMAP_V2.md` · `docs/BUILD_SEQUENCE.md` · `docs/DATA_SOURCE_CATALOG.md`) + **`git log` 이력** + **각 절이 인용하는 STEP 번호**. 🔴 **HTML 둘이 다투면 md 정본이 심판이다.** |
| **축 B — 실무** | 🔴 **이 저장소가 이미 쓴 합치기 관행.** STEP1056(정본↔표시본 대조 후 정본에 반영) · `tombstone.html`(은퇴 처리 형식) · STEP1060·1064·1065(취소선 + 정정 + 좌표 병기). **새 방법을 만들지 않는다.** |
| **축 C — 반대 증거** | *"저장소 쪽이 최신"*을 무너뜨릴 것 — ㉠**Downloads에만 있는 절** ㉡**Downloads가 더 큰 STEP 번호를 인용하는 절** ㉢**Downloads 숫자가 DB 실측과 더 맞는 곳**. **셋 다 찾아본다.** |

### ⓪-1. 직전 산출물 — 원문 인용

| 출처 | 인용(그대로) |
|---|---|
| `~/Downloads/tombstone.html`(2026-08-16) | *"**같은 내용을 두 곳에 두면 한쪽이 반드시 뒤처지고, 그때 어느 쪽이 정본인지 알 수 없게 됩니다.**"* |
| `docs/BUILD_SEQUENCE.md` 부록-4(STEP1065) | *"`BUILD_SEQUENCE.md`는 신설 이후 **커밋이 한 번도 없다** … 이 파급을 반영한 적이 없다"* |
| 같은 문서 §7 문서 위계 | 정본 목록 — 🔴 **「표시본」 행이 없다.** 이 공백이 이번 어긋남의 구조적 원인(§2-6) |
| 같은 문서 §8 #4 | *"**기존 조사를 안 찾고 재조사**"* — 🔴 이 STEP은 **STEP1056의 방법을 재사용**한다(축 B) |
| `docs/LENS_DISPOSITION_2026-08-08.md` §7·§8·§9·§10 | STEP1064·1066·1067·1068 산출 — WHAT 작업 파일에 반영할 재료의 **원문 출처**(§2-4) |
| `docs/probe_1017_cron_failure_causes.md` §1~§4 | 배수 27% 급락 **원인 확정**(STEP1058) — WHAT 파일은 아직 *"원인 미확인 · 조사 필요"* |

🔴 **직전 STEP(1068)이 「못 한 것」으로 남긴 것 중 이 STEP이 건드리는 것**: 없음.

### ⓪-1a. 🔴 로드맵 원문 대조 (여섯 층 전부)

| 층 | 이 STEP과의 관계 |
|---|---|
| **WHY·HOW·WHAT** | 🔴 **내용을 바꾸지 않는다.** 표시본을 정본에 맞추는 것이지 반대가 아니다 |
| **만드는 절차** | `BUILD_SEQUENCE.md` §7에 **표시본 행이 없다** → §2-6(사실 기록) |
| **관문·순위** | 무관 |
| **완성의 정의** | C-1 #7 *"**화면 일관성** — 같은 이름·판정·단위"* → 🔴 **문서에도 같은 원칙** |

### ⓪-1b. 기존 답 확인 — 🔴 `ls` 전수
🔴 `ls docs/*.html` **전수**와 `ls ~/Downloads/*.html` **전수**를 둘 다 나열해 산출물에 싣는다.
🔴 **STEP1056 전문을 먼저 읽는다**(축 B) — 그때 **표시본이 스스로 「이 절이 정본이다」라 선언한 자기지시적 문장**을 찾아낸 전례가 있다. **같은 것이 다른 파일에도 있는지 본다.**

### ⓪-2. 건드리는 슬롯 — **해당 없음**(문서 전용)

### ⓪-3. 걸리는 금지사항
- [ ] **코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 수동 실행 0 · 외부 취득 0**
- [ ] 🔴 **`~/Downloads/`의 원본을 지우거나 옮기지 않는다.** 읽기만 — **사용자 파일이다**
- [ ] 🔴 **내용을 잃지 않는다.** 한쪽에만 있는 것은 **반드시 살린다**
- [ ] 🔴 **판정을 만들지 않는다.** 충돌은 양쪽 병기 + 미판정
- [ ] 🔴 **파일 이름을 바꾸지 않는다** — `model_list_30_9.html` 그대로(*"30_9"의 뜻을 모르므로 바꾸면 뜻을 우리가 정하는 것*)
- [ ] **KR 전면 동결** · 🇺🇸 US 단독 · `REVDCF_ENABLED` OFF · `Q1_ENABLED` 무접촉

### ⓪-3b. 🔴 이 STEP의 함정
🔴 **「합친다」가 「한쪽을 덮어쓴다」가 되면 내용이 사라진다.** §2-2의 4분류를 **먼저 끝내고** 합친다.
🔴 **크기가 크다고 최신이 아니다.** 축 A(인용 STEP 번호·md 정본)로 판별한다.
🔴 **절 단위로 끝내지 않는다** — 같은 절 안에서 **숫자·날짜·STEP 번호가 다른 곳**이 진짜 위험이다.
🔴 **좌표 없는 문장 금지.**

### ⓪-4. 🔴 반증 조건

**전제 ①** — 저장소 `docs/` 쪽이 최신이다.
- 🔴 **크기는 근거가 아니다.** 축 C ㉠㉡㉢을 **셋 다** 돌린 뒤에만 말할 수 있다.
- 틀렸다면 → **Downloads에만 있는 절**이나 **Downloads가 더 큰 STEP을 인용하는 절**이 나온다.
- **다음 행동**: 그 절은 **Downloads 쪽을 살린다.** 절마다 방향이 다를 수 있다 — **파일 단위로 한쪽을 고르지 않는다.**

**전제 ②** — 겹치는 파일은 **둘**(roadmap · build_sequence)이다.
- 틀렸다면 → `data_source_catalog.html`·`tombstone.html`도 저장소에 대응물이 있을 수 있다.
- **다음 행동**: 전수 매핑표(§2-1). **양방향**(저장소에만 있는 표시본도).

**전제 ③** — `model_list_30_9.html`은 저장소에 없다.
- ✅ git 이력 0건으로 확인. 단 🔴 **다른 이름으로 같은 내용이 들어와 있을 수 있다**(`docs/probe_1040_what_structure.md` 등). **내용으로 확인한다.**

**전제 ④** — 합치면 내용이 안 사라진다.
- 🔴 **보장 없다.** §2-2 4분류와 §2-3 문장 단위 diff를 **산출물에 그대로 남겨** 사후 검증 가능하게 한다.
- **다음 행동**: §2-8 검수①에서 **합치기 전후 절 수·핵심 문장 수를 대조**한다.

### ⓪-5. 🔴 3번 생각
1. **범위** — 곁가지 아님. `tombstone.html`이 경고한 것이 실제로 일어났고, **핵심 작업 파일이 git 밖에 있다.**
2. **표본** — 🔴 **전수.** `~/Downloads/*.html` 전량 · `docs/**` 전량 · 겹치는 파일은 **절 전수 + 차이 나는 절은 문장 전수.**
   🔴 **전수의 조건** — ㉠정방향(Downloads→저장소) ㉡**역방향**(저장소→Downloads) 둘 다.
3. **시점** — 🔴 **파일 mtime과 내용 안 날짜가 다를 수 있다.** 둘 다 적고 **판별은 축 A로.**

---

## 1. 배경 (2026-08-18 실측)

| Downloads | bytes | 저장소 대응 | bytes |
|---|--:|---|--:|
| `roadmap_v2_2.html` | 65,755 | `docs/roadmap_v2.html` | **91,937** |
| `build_sequence.html` | 24,609 | `docs/build_sequence.html` | **25,482** |
| `data_source_catalog.html` | 108,889 | 🔴 없음(`DATA_SOURCE_CATALOG.md`만) | — |
| `model_list_30_9.html` | 57,628 | 🔴 **없음 · 커밋 이력 0건** | — |
| `tombstone.html` | 2,409 | 🔴 없음 | — |

🔴 **저장소에 없는 셋은 전부 `docs/`로 들여놓는다.** 이는 새 판정이 아니라 **기존 관행을 따르는 것**이다 — `docs/roadmap_v2.html`·`docs/build_sequence.html`이 이미 그렇게 저장소 안에 있다. **표시본의 자리는 `docs/`다.**

**확정 안 됨** 🔴 — 겹치는 둘에서 **어느 쪽에만 있는 내용이 무엇인가** · WHAT 파일에 **반영 안 된 재료** · 🔴 **`model_list_30_9_1.html`의 존재 여부**(§2-1㉠)

---

## 2. 할 일

### 2-1. 전수 매핑 (⓪-4②) — 🔴 **번호 붙은 사본을 반드시 잡는다**
`~/Downloads/*.html` 전량 ↔ `docs/**` 전량. **양방향.** 각 파일의 **mtime**·**내용 안 날짜 표기**·**인용된 최대 STEP 번호**를 함께 적는다.

🔴 **㉠ 번호 붙은 사본(`*_1.html`·`*_2.html`·`* (1).html`·`*_복사본*` 등)을 전수로 잡아라.**
2026-08-18 장은태가 **`model_list_30_9_1.html`**을 언급했으나 **Cowork 쪽 마운트에서는 보이지 않았다**(`~/Downloads` 목록에 `model_list_30_9.html` 하나뿐). 🔴 **Claude Code는 사용자 컴퓨터에서 도니 보일 수 있다.**
- **있으면** → 🔴 **그 파일이 최신 작업본일 가능성이 높다**(브라우저가 같은 이름을 다시 저장할 때 붙이는 접미어). §2-2 4분류를 **`_1` 포함 3자 대조**로 돌리고, **어느 것이 최신인지 축 A로 판별**한다. 🔴 **`_1`을 무시하고 진행하면 최신 작업이 통째로 빠진다.**
- **없으면** → *"`~/Downloads`에 `model_list_30_9_1.html`은 없다(전수 확인)"*를 산출물에 **명시**한다. 🔴 **조용히 넘어가지 않는다.**
- 🔴 **`~/Desktop`·`~/Documents`·iCloud Drive 등 다른 폴더는 뒤지지 않는다**(사용자 파일 범위를 임의로 넓히지 않는다). Downloads에 없으면 **「Downloads에는 없음」까지만** 적고 멈춘다.

🔴 **㉡ 같은 문서의 사본이 셋 이상이면** 전부 나열하고 **각각의 mtime·인용 최대 STEP·고유 절 수**를 표로 낸 뒤 판별한다. **하나만 골라 나머지를 버리지 않는다** — 고유 절은 전부 살린다(§2-2 ③).

### 2-2. 🔴 절 단위 4분류 — 합치기 전에 반드시 끝낸다
겹치는 파일마다 태그 제거 후 **heading(`h1`~`h4`) 전수 추출** → 절 매핑표. 각 절을 **넷 중 하나로**:

| 분류 | 처리 |
|---|---|
| **① 양쪽 동일** | 그대로 |
| **② 저장소에만 있음** | 그대로 유지 |
| **③ Downloads에만 있음** | 🔴 **반드시 저장소로 가져온다.** 어디에 넣을지(어느 절 뒤) 명시 |
| **④ 양쪽 있으나 내용 다름** | 🔴 **§2-3으로 넘긴다** |

🔴 **④의 개수와 목록을 먼저 산출물에 적는다.** 그다음에 합친다.

### 2-3. 🔴 ④(내용 다름)을 **문장 단위**로 가른다 — 이 STEP의 본체
각 ④ 절에 대해 **문장 단위 diff**를 내고, 차이를 **네 종류로 분류**:

| 차이 종류 | 판별 방법(축 A) |
|---|---|
| **㉠ 숫자가 다르다**(커버리지 %·건수·행수) | 🔴 **DB로 실측해 어느 쪽이 맞는지 확정.** 실측 못 하면 **양쪽 병기 + 「미확인」** |
| **㉡ 날짜·STEP 번호가 다르다** | **더 큰 STEP 번호를 인용한 쪽이 최신.** 단 🔴 **번호가 크다고 내용이 맞는 건 아니다** — 그 STEP 문서를 열어 확인 |
| **㉢ 판정 상태가 다르다**(한쪽은 확정, 한쪽은 미판정) | 🔴 **md 정본에서 확인.** 정본에 판정 기록이 없으면 **미판정 쪽이 맞다** |
| **㉣ 서술만 다르다**(같은 사실, 다른 표현) | md 정본 표현을 따른다 |

🔴 **각 차이에 판별 근거를 한 줄씩 단다.** 근거를 못 달면 **양쪽 병기 + 미판정.**
🔴 **26KB 차이가 무엇인지 반드시 설명한다** — *"저장소가 더 크다"*로 끝내지 않는다. **어느 절이 통째로 없는지, 아니면 절마다 조금씩 짧은지**를 가른다.
🔴 **자기지시적 주장**(표시본이 스스로 *"이 절이 정본이다"*·*"장은태 판정"*이라 선언한 것) 유무를 확인한다 — STEP1056 전례.

### 2-4. 합치기 — 🔴 절마다 방향을 정한다
§2-2·§2-3 결과대로 `docs/roadmap_v2.html`·`docs/build_sequence.html`을 갱신한다.
🔴 **파일 단위로 한쪽을 고르지 않는다.** 절마다 다를 수 있다(⓪-4①).
🔴 **취소선 보존**(축 B) — 지우지 말고 취소선 + 사유 + 좌표.
🔴 **충돌은 양쪽 병기 + 「어느 쪽이 맞는지 미판정」.** 고르지 않는다.

### 2-5. 🔴 `model_list_30_9.html`을 `docs/`로 들여놓고 재료 갱신
`~/Downloads/model_list_30_9.html` → **`docs/model_list_30_9.html`**(이름 불변). 그다음 **아래 다섯을 반영**한다.
🔴 **요약해서 옮기지 않는다 — 원문 좌표에서 문장을 가져온다.**

| # | 고칠 자리 | 반영할 것 | 원문 좌표 |
|:--:|---|---|---|
| 1 | 「정정 ㉢」의 *"🔴 원인 미확인 · 조사 필요"* + *"08-16은 STEP1048·1051이 돌았던 날"* | 🔴 **그 추정은 틀렸다**(취소선). 진짜 원인 = `computeAndSaveValuation`의 `us_market_cap` **단일-`as_of` 정확일치 조인** · 결측 증가 **2,826건 전부(100%·예외 0건)** 설명 · 상류는 **`lens-scores`(US) 야후 시총 배치 실패** · 🔴 **`us-perf`(나스닥)·`revdcf`는 무관**(코드 확정) · **08-17 밤 자연 복구**(PER 1,516→**2,133**) · 🔴 **조인 구조 그대로라 재발 가능 · 수리 미판정** | `docs/probe_1017_cron_failure_causes.md` §1~§4 |
| 2 | 「나. Piotroski 32.3%」 | 🔴 **전제가 바뀌었다.** 라이브 **718/999 = 71.9%** · 32.3%는 **SEC 단독 파이프라인** 숫자 · 🔴 **우리 SEC 캐시로는 단 한 종목도 3년 요건을 못 채운다** · 🟢 **부분 합산 코드에 없음**(H-7 우려 배제) · 🔴 기존 선택지 셋은 **SEC 전제**라 다시 놓아야 함 · 🔴 `probe_1054`가 무엇을 쟀는지 **원문 재확인 전엔 취소선 금지** | `LENS_DISPOSITION` §8-1 · `LENS_AUDIT_06_FSCORE.md` §1 |
| 3 | 「라. 자산성장 처분」 | ⑦감사 완료 — 신규 결함 **0건** · 원전 두 층·소형주 편중·생존편향 **이미 공개** · 🔴 **원문(CGS 2008) 미확보** · 손계산 **구조적 불가** · 🔴 **「못 쓴다」가 아니라 「어느 질문에도 안 붙는다」** | `LENS_AUDIT_07_ASSETGROWTH.md` · `LENS_DISPOSITION` §8-4 |
| 4 | 「실무와 어긋나는 둘 — Past performance」 | 🔴 *"수준을 안 묻는다"*는 **부정확**(9신호 **수준 3 : 변화 5 : 사건형 1**, 단 전부 **이진 문턱**) · SWS는 **성장 3 : 수익성 3**(6/6 아님) · Stock Analysis **n=3 레벨 전용**(업종마다 필드 다름) · AAII **0건** · 🔴 **실무 지표는 ROE·ROA, GP/A는 0곳** · 🔴 **ROE·ROA·ROIC·ROCE가 63모델 우주·5계층 로스터·STEP1041 랭킹 어디에도 이름 0회** · 🔴 **판정은 여전히 없음** | `LENS_DISPOSITION` §9-1·§9-2·§10-1·§10-5 |
| 5 | **새 절 신설** — 7렌즈 재사용 판정 | `CLAUDE.md` 규칙 3 인용 · 감사 ①~⑦ 완결 · 결함 **16건**(18→16 정정 근거 포함) · A13/B3/C2 · H-7로 가르면 표기해소 8·고쳐야 6·인프라 3 · 🔴 **사용 차단 0건** · 🔴 **고쳐진 것 0건** · 렌즈별 「쓰려면 무엇이 필요한가」 7행 · 🔴 **WHAT에 실제로 들어가는 건 셋**(F스코어·모멘텀·기술) · 밸류는 **Q1이 별개 경로** · 🔴 **퀄리티·저변동·자산성장은 관문 ①에서 걸린다**(GP/A 쓰는 실무 0곳·변동성 축 0곳) · 공통 패턴 셋 | `LENS_DISPOSITION` §7-1·§7-2·§7-5·§8-2·§8-3·§8-4 · `LENS_AUDIT_04_QUALITY.md` §6 |

🔴 **판정된 것과 미판정을 반드시 구분 표기.** 「나」·「라」·「Past performance」·7렌즈 재사용은 **전부 미판정**이다.
🔴 **판정 A(제품 정의 우선순위)는 반영하지 않는다** — *"폐기"*가 원문에 없는 낱말이었음이 확인돼 재확인 중.

### 2-5b. 🔴 나머지 둘도 `docs/`로 들여놓는다
`~/Downloads/data_source_catalog.html` → **`docs/data_source_catalog.html`** · `~/Downloads/tombstone.html` → **`docs/tombstone.html`**(둘 다 이름 불변).
🔴 **내용을 고치지 않는다. 자리만 옮긴다**(복사 — 원본 무접촉).
🔴 **단 `data_source_catalog.html`은 정본 `docs/DATA_SOURCE_CATALOG.md`(120,743 bytes)와 대조해 어긋난 곳을 목록으로 적는다**(§2-7과 같은 방식, **고치지 않음**). 🔴 STEP1068이 *"카탈로그 20슬롯은 역DCF 전용이라 총자산 슬롯 자체가 없다"*는 스코프 불일치를 발견했으므로, **그 사실이 표시본에 반영돼 있는지**도 함께 본다.
🔴 `tombstone.html`은 **은퇴 문서**다 — 내용을 갱신하지 않는다. 자리만 옮기고 **그것이 은퇴 문서임을 §2-6 위계 표에 표시**한다.

### 2-6. 문서 위계에 표시본 행 추가
`docs/BUILD_SEQUENCE.md` §7에 **「표시본」 행**과 **`~/Downloads/` 사본의 지위** 한 줄(사실 기록). 🔴 **판정 아님.**

### 2-7. 정본(md)과 정합 확인
`docs/model_list_30_9.html` ↔ `docs/ROADMAP_V2.md` W-2 어긋남 **목록만**. 🔴 **고치지 않는다**(STEP1056 방식).

### 2-8. 🔴 검증 3 · 검수 3 (문서 말미에 명시)
**검증 3**
1. **축 A** — md 정본·`git log`·인용 STEP 번호로 판별한 항목 수
2. **축 B** — STEP1056 방법 재사용 사실 + 취소선/좌표 표기 준수
3. **축 C** — ㉠Downloads에만 있는 절 ㉡Downloads가 더 큰 STEP 인용 ㉢Downloads 숫자가 더 정확 — **셋 각각 몇 건인가**(0건이면 0건이라 적는다)

**검수 3**
1. 🔴 **내용 손실 없나** — 합치기 **전후 절 수**와 **③에서 가져온 항목 수**를 숫자로 대조
2. 🔴 **판정을 만들지 않았나** — 미판정으로 남긴 충돌 건수
3. 🔴 **숫자 판별이 추측 아닌가** — ㉠(숫자 차이) 중 **DB 실측으로 확정한 건수 vs 미확인으로 남긴 건수**

🔴 **못 거친 축은 「못 했다」고 명시한다.**

### 2-9. 산출물
- `docs/model_list_30_9.html` **신규 등재**(이름 불변) + 재료 5건
- `docs/roadmap_v2.html`·`docs/build_sequence.html` 합치기 결과
- `docs/BUILD_SEQUENCE.md` §7 한 줄 + **부록-7**에 §2-1 매핑표 · §2-2 4분류 · §2-3 문장 단위 차이 목록 · §2-8 검증3·검수3 **전부 기록**
- `docs/CHANGELOG.md` · `docs/STEP_LEDGER.md` 등재
- 🔴 **새 md 정본을 만들지 않는다** — WHAT 작업 파일의 정본은 `ROADMAP_V2.md` W-2다

---

## 3. 🔴 하지 말 것
- 🔴 **판정하지 않는다.** 충돌은 양쪽 병기 + 미판정.
- 🔴 **내용을 잃지 않는다.** ③(Downloads에만)은 **전부** 살린다. 4분류·문장 diff를 산출물에 남긴다.
- 🔴 **파일 단위로 한쪽을 고르지 않는다.** 절마다 방향을 정한다.
- 🔴 **`~/Downloads/` 원본을 지우거나 옮기지 않는다.**
- 🔴 **파일 이름을 바꾸지 않는다.**
- 🔴 **판정 A를 반영하지 않는다.**
- 🔴 **「크기가 크니 최신」으로 판별하지 않는다.**
- 🔴 **요약해서 옮기지 않는다** — §2-5는 **원문 좌표에서 문장을 가져온다.**
- 🔴 **숫자 차이를 추측으로 메우지 않는다** — DB 실측 아니면 「미확인」.
- 🔴 **새 md 정본을 만들지 않는다.**
- 🔴 **코드·화면·DB 무접촉 · KR 무접촉 · 외부 취득 0.**

## 4. 커밋 전 게이트
- 게이트 7: git 미추적 파일 참조 검사
- 게이트 8: push 후 **GitHub Actions verify 초록 + Vercel 배포 성공을 눈으로 확인**
- 게이트 9: 새 테이블·크론·관측 필드 없음 → 해당 없음
- `docs/CHANGELOG.md` 항목 · `docs/STEP_LEDGER.md` 등재
- 🔴 **브랜치 규약(STEP1056 §2-7)** — `main`에 직접 push. **브랜치·PR 만들지 않는다. 예외 없음.**

## 5. DoD
- [ ] ⓪-0 **축 A·B·C 고정** 후 시작 · ⓪-1b `ls` 전수 양쪽 + **STEP1056 전문 선독**
- [ ] 🔴 2-1 전수 매핑 — 양방향 · mtime·내용날짜·**인용 최대 STEP 번호** 셋 다 · 🔴 **`model_list_30_9_1.html` 존재 여부를 산출물에 명시**(있으면 3자 대조, 없으면 「없음」이라 적기)
- [ ] 🔴 2-2 **절 단위 4분류** — ①②③④ 개수와 목록을 **합치기 전에** 기록
- [ ] 🔴 2-3 **④를 문장 단위로** — ㉠숫자 ㉡날짜/STEP ㉢판정상태 ㉣서술 로 분류 + **각 차이에 판별 근거 한 줄** · 🔴 **26KB 차이 설명** · 자기지시적 주장 유무
- [ ] 2-4 합치기 — **절마다 방향** · 취소선 보존 · 충돌은 양쪽 병기+미판정
- [ ] 🔴 2-5 `docs/model_list_30_9.html` 등재 + **재료 5건**(원문에서 가져옴) · **미판정 표기 유지** · 판정 A 미반영
- [ ] 🔴 2-5b `data_source_catalog.html`·`tombstone.html`도 `docs/`로 등재(이름·내용 불변) + 카탈로그 md 정합 **목록만**
- [ ] 2-6 `BUILD_SEQUENCE.md` §7 표시본 행 + Downloads 지위 + **은퇴 문서 표시**
- [ ] 2-7 `ROADMAP_V2.md` W-2 어긋남 **목록만**
- [ ] 🔴 2-8 **검증 3 · 검수 3 명시** — 축 C 세 갈래 각각 건수 · 손실 대조 숫자 · DB 확정/미확인 건수 · **못 거친 축은 「못 했다」**
- [ ] 🔴 `~/Downloads/` 원본 **무접촉 확인**
- [ ] 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 수동 실행 0 · 외부 취득 0 · tsc 0 · test 전량 통과
- [ ] 마지막에: **못 한 축 / 철회·정정한 것 / 미측정으로 남은 것**

🔴 **판정은 장은태가 한다. 이 STEP은 자리를 바로잡고 합치는 것까지다.**
