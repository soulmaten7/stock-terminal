# STEP 1049 — DB 테이블 69개 인벤토리 + 범위를 「US 종목 분석」으로 명시 (🔵 조사·기록 · 코드 diff 0 · DB 쓰기 0)

> 🔴 **장은태 판정(2026-08-15)**: *"이 모델을 만드는 과정을 US 종목에 대해 분석할 때로 정의를 내려야겠다. 다른 국가 탭을 만들 때는 우리의 이 로드맵을 분석해서 그에 맞춰 데이터를 넣어 사용하면 된다."*
> 🔴 **이번 STEP은 판정·기록만 한다. 삭제·이관·코드 수정은 하지 않는다** — STEP1047이 조사와 실행을 한 STEP에 넣었다가 NO-GO로 멈췄다. **분리한다.**
> 🔵 코드 diff 0 · DB 쓰기 0 · 화면 변경 0.

---

## 0-A. 왜 이 STEP인가

Cowork DB 실측(2026-08-15): 테이블 **69개** 중 `us_` 접두어 **16개** · `kr_` **2개** · **접두어 없음 51개**.

🔴 **그 51개가 오늘 두 번 사고가 난 표면이다.**
- `financials` 0행 → *"재무 원자료 적재가 선행 작업 하나"*(`MODEL_UNIVERSE_63` §3-1) — **KR 테이블이었다**
- `dividends` 60행 → F-4 배당 순위 — **KR 27종목이었다**

🔑 **`DATA_SOURCE_CATALOG.md`는 US 전용이라 혼동이 없다**(기관 5개 전부 US·DART/KRX 언급 0건). **문제는 카탈로그가 「밖에서 무엇을 받아오나」의 지도이고, 「우리 DB에 무슨 테이블이 있고 누가 쓰나」는 어디에도 없다는 것이다.** 그래서 *"카탈로그에 없다 = 우리가 안 쓴다"*로 읽혔어야 할 것이 *"아직 안 넣었다"*로 읽혔다.

🔴 **없는 층을 이번에 만든다.**

---

## ⓪ 전제 점검

### ⓪-1. 직전 산출물 — 원문 인용
| 출처 | 인용 |
|---|---|
| Cowork DB 실측(오늘) | 테이블 **69** · `us_` 16 · `kr_` 2 · 접두어 없음 **51** |
| `CLAUDE.md` 배너(2026-07-18) | *"새 국가탭 = … ② **TR-AI 엔진 보편·어댑터만 나라별** …"*(상세 `COUNTRY_TAB_PLAYBOOK` §0-2) |
| `CLAUDE.md:31` | *"무조건 언어권도 US 탭도 US만 만드는 거야"* |
| `probe_1048` | 🔴 `components/toolbox/` **26개 파일이 어떤 라우트에서도 도달 불가**로 보임 — `app/[locale]/toolbox/page.tsx`는 `redirect({href:"/"})`뿐 |
| `probe_1043` | 칸별 최종 모델 — Piotroski(고 B/M 조건)·Altman(US 제조업 표본)·Novy-Marx(금융업 제외) |
| `probe_1039` | ⓒ 7건 판정 대상 — 🔴 그중 `COUNTRY_TAB_PLAYBOOK.md`는 **이번 판정으로 존치 확정** |
| `ROADMAP_V2` C-3 ㉡ | *"확인했다고 생각했지만 실제로는 안 봤다"* |

### ⓪-1a. 🔴 로드맵 원문 대조 (`docs/ROADMAP_V2.md`)
| 층 | 걸리는 절 | 인용 |
|---|---|---|
| **WHY** | 조건 1(정확) | 재료 귀속이 틀리면 순위·판정이 전부 틀린다 |
| **HOW** | **H-2(카탈로그가 먼저다)** · H-2-1(3층 구조) | 🔴 **3층에 「우리 DB 테이블」 층이 없다 — 이번에 더한다** |
| **WHAT** | W-1~W-2-4 | 🔴 **범위에 「US 종목 분석」 명시** |
| **관문·순위** | F-4-3 · F-5 | 모델별 보편/US특정 칸 추가 |
| **완성의 정의** | C-1 항목 1(원전 대조표)·항목 2(입력 검증) | 원전 표본 조건이 국가 어댑터의 일부다 |
| **수익 모델** | **없음** | 무관 |

### ⓪-1b. 🔴 기존 답 확인 — 재조사 금지
`DATA_SOURCE_CATALOG.md` · `SYSTEM_MAP.md` · `COUNTRY_TAB_PLAYBOOK.md` · `probe_1039`~`1048` · `STATE.md` · `MODEL_ROSTER` · `KNOWN_ANSWERS.md`
🔴 **모델·소스를 새로 조사하지 않는다.** 이 STEP은 **우리 것을 세는 일**이다.
🔴 이 칸이 비면 미완성.

### ⓪-2. 슬롯 — 없다(기록 전용)

### ⓪-3. 걸리는 금지사항
- [x] 🔵 **코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 호출 0.** 읽기만
- [x] 🔴 **삭제·이관·정리를 하지 않는다** — 이번은 **인벤토리와 판정 기록**까지다. 실행은 다음 STEP
- [x] 🔴 **`Q1_ENABLED`·`REVDCF_ENABLED` 무접촉** · 환경변수 VALUE 타이핑 금지
- [x] 🔴 **`kr_stock_snapshot` 계열 무접촉** — 동결이지 삭제가 아니다
- [x] 🔴 **창작 금지** · **선택지 나열 금지** · `data/us_symbols.json`·`.github/workflows/**`·`vercel.json` 수정 금지

### ⓪-3b. 🔴 결함의 소재 확정
- 🔴 **귀속을 이름으로 판정하지 않는다.** `financials`·`dividends`가 정확히 그 실패다. **참조하는 데이터 소스와 쓰는 코드로** 판정한다
- 🔴 **도달 가능성은 위에서 아래로 본다.** 라우트 → 컴포넌트 → API → 테이블. 🔴 Cowork이 어제 아래에서 위로만 보고 `ToolboxClient`를 *"라이브"*라 단정한 오류의 반대 방향
- 🔴 **「미사용」은 두 방법으로만 판정한다** — ① 이름 검색 ② 역방향(코드가 부르는 이름 수집 → DB 대조). ①만으로 「미사용」이라 쓰지 않는다
- 🔴 **「보편」과 「US 특정」을 감으로 나누지 않는다** — 원전의 표본 조건·국가 파라미터·데이터 소스, **세 축으로 각각 판정**한다

### ⓪-4. 🔴 반증 조건

| 관측 | 결론 |
|---|---|
| 69개가 US/KR/공용/미사용으로 깔끔히 갈린다 | ✅ 인벤토리 완성 — 처분은 다음 STEP |
| 🔴 **귀속을 못 정하는 테이블이 있다** | 🔴 「불명」으로 두고 **무엇을 더 봐야 정해지는지** 적는다. 추측으로 채우지 않는다 |
| 🔴 **미사용 테이블이 10개를 넘는다** | 🔑 KR 파일럿에 이은 패턴 — 규모를 적고 처분은 다음 STEP |
| 🔴 **`us_` 접두어인데 실제로는 안 쓰이는 것이 있다** | 🔴 접두어가 보증이 아니라는 증거 — 별도 표시 |
| 🔴 **접두어 없는 51개 중 KR이 더 나온다** | 🔴 STEP1048에서 다 걷어내지 못했다는 뜻 — 목록화 |
| 모델이 전부 「보편」으로 판정된다 | 🔴 **의심한다.** Piotroski·Altman·Novy-Marx는 원전에 표본 조건이 명시돼 있다 — 「보편」이면 근거를 대야 한다 |
| `components/toolbox/` 26개가 정말 고아로 확인 | 목록에 기록(처분은 범위 밖) |

### ⓪-5. 3번 생각
1. **범위** — **세고 판정하고 기록**까지다. 🔴 지우거나 옮기지 않는다
2. 🔴 **표본 — 테이블 69개 전수 · 확정 모델 전수.** SQL과 grep이라 비용이 낮다. **상한 없음**
3. 🔴 **「전수의 조건」 적용** — 사용 여부를 **두 방법**으로(⓪-3b 셋째). 🔴 **차집합을 반드시 보고**

---

## 1. 할 일

### 1-1. 🔑 테이블 69개 인벤토리 — 전수
| 테이블 | 행수 | 시장 귀속 | 근거(🔴 이름 아님) | 채우는 것 | 최신 갱신 | 쓰는 코드 | 화면 도달 | 판정 |
|---|---:|---|---|---|---|---|---|---|

- **시장 귀속**: `US` / `KR` / `공용` / `미사용` / `불명`. 🔴 **근거는 참조하는 데이터 소스·조인 대상·쓰는 코드**
- **채우는 것**: 크론명 / 스크립트 / 1회 적재 / 없음. 🔴 **`vercel.json` 크론 9개와 대조**
- **최신 갱신**: `as_of` 또는 `created_at`/`updated_at` 최대값. 🔴 **멈춘 것을 드러낸다**
- **쓰는 코드**: 파일:줄. 없으면 「참조 0」
- **화면 도달**: 🔴 **라우트에서 위→아래로 추적.** 도달 / 플래그 뒤 / 도달 불가
- 🔴 **뷰·함수/RPC도 같은 표에 포함**(테이블만 세면 `stock_snapshot_v` 같은 것이 빠진다)

### 1-2. 카탈로그에 없던 층 추가
`DATA_SOURCE_CATALOG.md` H-2-1의 3층(**기관 엔드포인트 → 카테고리 25 → 슬롯 20**) 아래에 🔑 **4층 「우리 DB 테이블」**을 더한다.
- 슬롯 20개가 **어느 테이블에 떨어지는지** 연결한다(슬롯 → 테이블 역인덱스)
- 🔴 **어느 슬롯에도 안 붙는 테이블**을 표시한다 — 그것이 카탈로그 밖에서 살고 있던 것들이다
- 🔴 **카탈로그는 US 전용임을 문서 머리에 명시**한다(지금은 암묵이다)

### 1-3. 범위 명시 — 「US 종목 분석」
- 🔴 `ROADMAP_V2.md` 머리에 **적용 범위 = US 상장 종목 분석**을 명시하고, **지금까지의 모든 판정이 US 기준임**을 못박는다
- 🔴 **다른 국가 탭 절차를 여기서 잇는다** — `CLAUDE.md` 배너 2026-07-18 룰 ②(*"엔진 보편·어댑터만 나라별"*)와 `COUNTRY_TAB_PLAYBOOK` §0-2를 인용
- 🔴 `probe_1039` ⓒ 7건 중 **`COUNTRY_TAB_PLAYBOOK.md` 존치 확정**을 기록(*"US 단독이면 불필요"*가 아니라 **이 결정의 실행 장치**)

### 1-4. 🔑 모델별 「보편 / US 특정」 칸
`probe_1043`의 칸별 확정 모델 전부에 칸을 하나 더한다. 🔴 **세 축으로 각각 판정**한다.

| 모델 | ① 원전 표본 조건 | ② 국가 파라미터 | ③ 데이터 소스 | 종합 |
|---|---|---|---|---|

- **① 원전 표본 조건** — 원전이 어느 표본에서 검증됐나. 🔴 Piotroski(고 B/M)·Altman(US 제조업 1968)·Novy-Marx(금융업 제외)는 **명시돼 있다**
- **② 국가 파라미터** — rf·ERP·세율처럼 나라마다 값이 달라지는 것(역DCF)
- **③ 데이터 소스** — SEC 같은 US 전용 소스에 묶여 있나
- 🔴 **「보편」으로 판정하려면 세 축 모두 근거를 대야 한다.** 근거 없으면 「불명」
- 🔑 **이 표가 나중에 국가 탭을 만들 때 그대로 체크리스트가 된다** — 그렇게 쓰라고 표에 적는다

### 1-5. 산출물
`docs/probe_1049_table_inventory.md`
- 맨 앞 **요약 한 장**(귀속별 개수 · 미사용 수 · 멈춘 것 수) + ⓪-4 매트릭스 결과
- 🔑 **1-1 69개 전수표** · 1-2 4층 연결 · 1-3 범위 명시 · 🔑 **1-4 보편/US특정 표**
- 🔴 3중 규칙 4종: 못 한 축(시도 기록) / 아직 안 함 / 철회·정정 / 미측정

🔴 `DATA_SOURCE_CATALOG.md`(4층 + US 전용 명시) · `ROADMAP_V2.md`(범위 · F-4-3에 보편/US특정) · `SYSTEM_MAP.md` · `STATE.md` 반영 · `roadmap_v2.html` 동기화.

---

## 2. 커밋 전 게이트
게이트 7 · 게이트 9 · `CHANGELOG` 1049 · `STEP_LEDGER` 등재. 🔵 배포 불필요, 커밋·푸시는 한다.

## 3. DoD
- [ ] ⓪-1a 로드맵 여섯 층 대조표
- [ ] 🔑 **1-1 69개 전수**(뷰·RPC 포함) · 🔴 **귀속 근거가 이름이 아님** · 🔴 **화면 도달은 위→아래 추적** · 🔴 **미사용 판정은 두 방법·차집합**
- [ ] 1-2 카탈로그 4층 추가 · 슬롯↔테이블 연결 · 🔴 **어느 슬롯에도 안 붙는 테이블 표시** · US 전용 명시
- [ ] 1-3 `ROADMAP_V2` 범위 = **US 종목 분석** 명시 · 국가탭 절차 연결 · `COUNTRY_TAB_PLAYBOOK` 존치 기록
- [ ] 🔑 **1-4 모델 전수 · 세 축 각각 · 🔴 「보편」은 근거 필수 · 체크리스트 용도 명기**
- [ ] 🔴 **삭제·이관·코드 수정 0** · 플래그 무접촉 · DB 쓰기 0
- [ ] 마지막에: 못 한 축 / 아직 안 함 / 철회·정정 / 미측정

🔴 **귀속은 이름이 아니라 소스와 코드로 판정한다 — `financials`·`dividends`가 그 증거다.**
🔴 **이번엔 세기만 한다. 처분은 다음이다.**
