# STEP 1026 — 오늘 밤 네 관측을 읽고 판정 재료를 확정한다 (읽기 전용 · 🔴 내일 아침에 실행)

> 🔴 **실행 시점: 2026-08-14 22:45 UTC 이후**(한국시간 08-15 오전). 그 전에 돌리면 값이 없다.
> 🔴 이 STEP은 **읽고 정리만** 한다. 게이트 전환·프루닝·`BUDGET_MS`·D 버그 수정 어느 것도 하지 않는다.

---

## 0-A. 🔴 오늘 밤에 네 개가 한꺼번에 갈린다

| # | 값 | 출처 | 무엇을 가르나 |
|---|---|---|---|
| ① | `newCutGateOk` | `cron_heartbeats.job='lens-scores'` (21:30 UTC) | 새 게이트 산식이 통과 판정을 내는가 |
| ② | `wouldPruneRows` | 같은 곳 | 게이트를 열면 프루닝이 몇 행을 지우는가 |
| ③ | `stage` | `cron_heartbeats.job='revdcf'` (22:45 UTC) | `revdcf`가 어디서 죽는가 |
| ④ | `us_sector_relative` 최신 `as_of` | DB | 6일째 정지가 풀렸는가 |

🔴 **①②는 `lens-scores`, ③④는 `revdcf` — 서로 다른 크론이다.** 보고에서 반드시 구분한다.

---

## ⓪ 전제 점검

### ⓪-1. 직전 STEP 산출물 — 원문 인용 (검산 포함)

| 출처 | 인용 | 검산 |
|---|---|---|
| STEP 1025 | `capGateDecision`에 관측 필드 5개 추가(`newCoverageOk`·`newCutGateOk`·`priorCoverage`·`priorSource`·`coverageDrop`) — 🔑 **기존 4개 필드 계산 불변, 833 값 잠금 테스트 11/11 무수정 통과** | ✅ |
| STEP 1025 | `us_coverage_history` 신설(마이그레이션 적용) · `pruneImpact()` · `recordCoverageHistory()` — US·KR 양쪽에 try/catch 격리로 배선 | — |
| STEP 1025 🔑 | **W4 컷 교체 시뮬레이션**: 07-30 컷을 오늘 것으로 바꾸면 **117/1,036 종목(11.3%)만 판정이 바뀐다** — 18일 백로그가 시사한 것보다 훨씬 작다. 읽기 전용, 라이브 `lens_scores.*_value` 재사용(야후 재조회 0) | ✅ 117/1036 = 11.29% |
| STEP 1025 | 1024 자기신고 SIC `"0000"` 버그 **17종목**으로 전수 확정(우연히 찾은 11이 아님) — 🔴 1024의 "분모 축도 소진" 결론을 **뒤집지 않음** | — |
| STEP 1025 | 값 불변 전항목 통과: `lens_cuts` US **07-30 유지** · `revdcf_results` md5 동일 · 보호 파일(`route.ts`·`us_symbols.json`·`vercel.json`) diff 0 | — |
| STEP 1025 | 배포 **2026-08-14T07:37:23Z** — 오늘 밤 `lens-scores`(21:30)·`revdcf`(22:45) **둘 다 새 코드로 돈다** | ✅ 크론보다 앞섬 |
| 어제 실측 | `freshCoverage` 93.89%(전일 93.76%) · `compRatio` 96% · `coverageDrop` 예상 +0.13%p | — |
| 상수 | `ABS_FLOOR` 0.85 · `DROP_LIMIT` 0.03 — 🔴 **제안값이며 확정은 장은태 판정** | — |
| 현재 | US `lens_cuts` 07-30 **19일째** · `us_sector_relative` 08-10 **6일째** · `us_market_cap_nasdaq` 0행 | — |

### ⓪-2. 이 STEP이 건드리는 슬롯
🔴 **없다.** 읽기만 한다.

### ⓪-3. 걸리는 금지사항 (해당되는 것만)
- [x] 🔴 **코드 수정 0 · DB 쓰기 0** — 산출은 문서뿐
- [x] 🔴 **게이트를 전환하지 말 것** · **프루닝을 켜지 말 것** · **상수(0.85/0.03)를 바꾸지 말 것**
- [x] 🔴 **`BUDGET_MS`·`maxDuration` 불변** · **D 조회 키 버그 수정 금지**
- [x] 🔴 **크론 수동 실행 금지** — 값이 없으면 "미도래"라고 적는다. **불러서 만들지 말 것**(특히 `email-brief` = 실제 메일 발송)
- [x] 🔴 **7렌즈 라이브 화면 수정 금지**
- [x] `REVDCF_ENABLED` Production OFF 유지 · `data/us_symbols.json` · `.github/workflows/**` · `vercel.json` 수정 금지 · **KR 전면 동결**(KR 값은 대조용으로만 읽고 US 판정에 쓰지 않는다)

### ⓪-3b. 🔴 결함의 소재 확정
- 이 STEP은 **프로덕션 크론이 남긴 값을 읽기만** 한다. 새 조사 도구를 만들지 않는다
- 🔴 값이 이상하면 **그게 프로덕션 동작인지 1025 계측의 버그인지**를 먼저 가른다(`파일:줄번호`)

### ⓪-4. 🔴 반증 조건 — 판정 매트릭스

**A. 게이트 축 (① × ②)**

| `newCutGateOk` | `wouldPruneRows` | 결론 | 다음 축 |
|---|---|---|---|
| **통과** | **≤100행** | 게이트 전환과 프루닝을 같이 켜도 위험이 작다 | 전환 판정(장은태) |
| **통과** | **>100행** | 🔴 **게이트와 프루닝을 분리해야 한다** | 분리 설계 판정 |
| **차단** | — | 낙폭 기준이 빡빡하거나 전일값을 못 읽었다 | `priorSource` 확인 → 기준·부트스트랩 재검토 |
| 필드 자체가 **없음** | — | 🔴 1025 배선이 실행 경로를 안 탔다 | 배선 재확인 |

🔴 **100행 기준의 근거**: 코드에 이미 `churnDecision` 임계 10%가 있고(유니버스 1,000 → 100종목), 어젯밤 실제 churn은 6.3%였다. 🔴 **이 기준은 판단 보조일 뿐 판정이 아니다.**

**B. `revdcf` 축 (③ × ④)**

| `stage` | `us_sector_relative` 08-14 | 결론 | 다음 축 |
|---|---|---|---|
| `complete` | **생김** | 🔑 **6일 정지 해소.** 남은 건 D 조회 키 버그뿐 | D 수정 판정 |
| `complete` | 안 생김 | 함수는 끝났는데 저장이 안 됐다 | upsert 경로 조사 |
| `sector_relative_done` | 생김 | 정지 해소 + 마지막 구간에서 죽음 | 잔여 구간 조사 |
| `valuation_done` | 안 생김 | 🔑 **`computeAndSaveSectorRelative` 도중 함수가 죽었다 = 예산 초과 확정** | `BUDGET_MS` 판정 |
| `loop_done` | 안 생김 | `computeAndSaveValuation`부터 못 갔다 — 더 앞이다 | 루프 구간 조사 |
| **행 없음** | — | 🔴 **계측이 도달조차 못 한다.** 1018·1025 배선이 둘 다 무력 | **크게 보고** · 계측 위치 재설계 |

🔴 **모든 칸에 다음 행동이 정해져 있다.** 🔴 **표에 없는 조합이 나오면 그것 자체를 크게 보고**한다.

### ⓪-5. 3번 생각
1. **범위** — 오늘 밤 관측이 **네 개 STEP의 판정을 한꺼번에 푼다**(게이트 전환 · 프루닝 분리 · `BUDGET_MS` · D 버그 근본). 곁가지 아님.
2. **표본** — 🔴 **1일 관측이다.** 🔴 **①이 통과로 나와도 "하루치"라는 걸 명시**한다. 특히 `coverageDrop`은 전일 대비라 **하루만 봐선 안정성을 모른다**. 🔴 **전수 비용: DB 읽기만이라 초 단위** — 상한 불필요.
3. **시점** — 🔴 **이 STEP 자체가 오늘 밤 이후에만 의미가 있다.** 22:45 UTC 전에 실행하면 값이 없다. 🔴 그리고 **다음 밤 크론 전에 아무것도 바꾸지 않는다** — 바꾸면 이 관측의 재현성이 사라진다.

---

## 1. 할 일

### 1-1. 배포 시각 대조 — 먼저
🔴 1020이 겪은 함정: 배포가 크론보다 늦으면 구 코드로 돈다.
- 1025 배포 시각(**2026-08-14T07:37:23Z**)이 두 크론 시각보다 앞섰는지 재확인
- 🔴 늦었으면 **"이번에도 구 코드"**라고 적고 나머지를 그대로 진행(값이 구 코드 기준임을 명시)

### 1-2. ①② — `lens-scores` (21:30 UTC)
`cron_heartbeats.job='lens-scores'` note에서:
- `newCoverageOk` · **`newCutGateOk`** · `priorCoverage` · **`priorSource`**(history/heartbeat/none) · `coverageDrop`
- **`wouldPrune`** · **`wouldPruneRows`** · `wouldPruneSample`(심볼 ≤10) · `successRate` · `universeOk` · `pass2Ok` · `pruned`
- 대조군: `freshCoverage` · `coverageOk`(현행) · `compositionOk` · `compRatio` · `cutGateOk`(현행)
- 🔴 **`cutGateOk`(현행)가 여전히 false인지 확인** — true면 드라이런이 아니라 실제로 열린 것이다. **크게 보고**
- `us_coverage_history` 행이 생겼는지 · `market`별로 US/KR 각 1행인지

### 1-3. ③④ — `revdcf` (22:45 UTC)
`cron_heartbeats.job='revdcf'` note에서:
- **`stage`** · `elapsedMsAtStage` · `finallyMs`(6구간) · `finallyTotalMs` · `loopMs` · `budgetExhausted`
- `sectorRelativeError`(스택 포함) · `sectorRelativeSaved` · `sectorWideAdded` · `sectorWideError`
- `us_sector_relative` 최신 `as_of` + 그 날짜의 행수 + **`sector IS NULL` 비율**
- 🔴 08-14 행이 생겼는데 **전부 `sector=null`이면** 그건 08-10과 같은 상태다 — **정지 해소가 아니다.** 그렇게 적을 것

### 1-4. 값 불변 확인
🔴 이 STEP은 아무것도 안 바꾸지만, **크론이 밤새 무엇을 바꿨는지**는 봐야 한다:
- `lens_cuts` US `as_of` — 🔴 **07-30 그대로여야 한다**(드라이런이므로). 바뀌었으면 **크게 보고**
- `lens_scores` US/KR 행수 · `us_market_cap` 행수와 07-30 코호트 수(직전 287)
- `revdcf_results` 최신 `as_of`와 행수 · `us_valuation` 행수
- `us_market_cap_nasdaq` 행수(🔴 0이 정상 — 1018에서 교체 안 함) · `us-perf` note의 `nasdaqError`

### 1-5. 산출물
`docs/probe_1026_tonight_readout.md`
- 맨 앞에 **⓪-4 A·B 매트릭스에서 어느 칸인지** 각각 한 줄
- 1-2 `lens-scores` 값 전부(현행 대조군 포함)
- 1-3 `revdcf` 값 전부 + `sector=null` 비율
- 1-4 값 불변 확인표
- 🔴 말미에 **장은태 판정 요청 목록**: 게이트 전환 여부 · 프루닝 분리 여부 · 상수(0.85/0.03) 확정 · `BUDGET_MS` 조정 · D 조회 키 수정 — 🔴 **각각에 이번 관측이 준 근거만 붙이고, 고르지 말 것**

🔴 `docs/ANSWERABILITY_MAP.md` §E(7렌즈) 현재 상태를 오늘 값으로 갱신. **§3은 건드리지 말 것.**
🔴 `docs/CRON_OBSERVABILITY.md`(정본)에 고칠 게 생기면 **이 STEP에서 정본을 먼저 고친다.** Cowork 예약 프롬프트(사본)는 그 뒤에 맞춘다(게이트 9 순서 규칙).

## 2. 커밋 전 게이트
- 게이트 7: git 미추적 파일 참조 검사 — 🔴 런타임 `fs.readFileSync('/tmp/...')`도 잡을 것
- 게이트 8: push 후 **Vercel 배포 성공 + verify 초록을 눈으로 확인**하기 전까지 미완료
- 게이트 9: 정본 먼저 · 사본 나중
- `docs/CHANGELOG.md` 1026 항목 · `docs/STEP_LEDGER.md` 1026 등재

## 3. DoD
- [ ] 1-1 배포 시각을 **두 크론 시각과 각각 대조**했다
- [ ] ⓪-4 **A·B 매트릭스에서 어느 칸인지** 맨 앞에 각각 한 줄 · 🔴 표에 없는 조합이면 크게 보고
- [ ] 1-2 `newCutGateOk`·`wouldPruneRows`·`priorSource` 포함 전 필드 · 🔴 **`cutGateOk`(현행)가 false인지 확인**
- [ ] `us_coverage_history`에 US/KR 각 1행 생성 확인
- [ ] 1-3 `stage` + 6구간 + `sectorRelativeError` 스택 · 🔴 **`sector=null` 비율**까지
- [ ] 1-4 🔴 **`lens_cuts` US가 07-30 그대로**임을 확인했다
- [ ] 🔴 **①이 통과여도 "1일 관측"임을 명시**했다
- [ ] 판정 요청 목록 5건 — 🔴 **근거만 붙이고 고르지 않았다**
- [ ] 🔴 **DB 쓰기 0 · 코드 diff 0**(문서만) · tsc 0 · test 전량 통과
- [ ] Vercel 배포 성공 + verify 초록 **눈으로** 확인
- [ ] 마지막에: 못 한 축 / 철회·정정한 것 / 미측정으로 남은 것

🔴 **판정은 전부 장은태가 한다. 이 STEP은 네 값을 매트릭스에 놓는 것까지다.**
🔴 **다음 밤 크론 전까지 아무것도 바꾸지 않는다** — 바꾸면 이 관측의 재현성이 사라진다.
