# STEP 1089 — 🔴 **지침 문서 전수 갱신** — 「빠진 서술」을 역방향으로 잡는다 (🟡 문서 전용 · 코드 diff 0 · DB 쓰기 0 · 스키마 변경 0 · 화면 변경 0 · 크론 수동 실행 0)

> 🔴 **장은태 지적(2026-08-19)**: *"지침용 파일에 있는 **모든 세부파일까지** 기록과 내용 싹다 업데이트 했어?"* → **실측 결과 안 됐다.**
> 🔴 **STEP1086·1088의 방식 결함**: 둘 다 **씨앗 검색어 grep(정방향)** 이었다. **grep은 「틀린 서술」은 찾아도 「빠진 서술」은 못 찾는다.** `SYSTEM_MAP.md`에 새 컬럼 얘기가 **아예 없으니** 검색에 안 걸렸다.
> 🔴 **이 STEP은 역방향으로 돈다** — 문서에서 출발해 *"이 문서가 담당하는 것 중 이번 세션에 바뀐 게 무엇인가"*를 묻는다.
> 🔴 **판정하지 않는다.** 사실만 반영하고 판정은 등재한다.
>
> **왜 지금인가 — 3번 규칙(2026-08-19):**
> **축 A(원문)** — `CLAUDE.md` 자신의 규칙: `:657` *"코드/아키텍처 바뀌면 **`docs/SYSTEM_MAP.md`** 해당 부분 갱신"* · `:644` *"AI 렌즈 관련 STEP 결과를 받을 때마다 문제·원인·해결·교훈이 생겼으면 **즉시** `docs/LENS_DEV_PLAYBOOK.md` 문제해결 로그에 한 행 — 🔴 **몰아서 하지 말 것**"* · `:666` *"`docs/INDEX.md` = 전체 문서 마스터 인덱스"*.
> **축 B(실무·실측)** — mtime 전수(2026-08-19): `CLAUDE.md` **08-18** · `SYSTEM_MAP.md` **08-16** · `LENS_DEV_PLAYBOOK.md` **08-18** · `INDEX.md` **08-15** · `DATA_SOURCE_CATALOG.md` **08-16**. 🔴 **오늘 스키마를 바꾸고(STEP1087) 렌즈 코드를 고쳤는데(STEP1083) 이 다섯이 안 움직였다.**
> **축 C(반대 증거)** — ㉠**mtime이 안 바뀐 게 「고칠 게 없었다」는 뜻일 수 있다** → 🔴 **문서를 열어 확인한다.** 정말 없으면 *"검사함·해당 없음"* ㉡**CLAUDE.md 배너는 매 세션 강제가 아니다**(`:632` *"방향·규칙 바뀔 때만"*) → 🔴 **판정 A·B 확정이 「방향 변화」인지 판별**한다 ㉢**과거 세션에도 같은 누락이 쌓였을 수 있다** → 🔴 이번 세션분만 한다. 과거분은 **발견하면 등재만**.

---

## ⓪ 전제 점검

### ⓪-1. 🔴 이번 세션 변경 목록 (역방향의 출발점)
`CHANGELOG.md`의 2026-08-19 블록 전체(STEP1073~1088)를 읽어 **바뀐 것을 유형별로** 뽑는다:
- **DB/스키마**: `lens_scores` 7컬럼 추가(STEP1087) · 마이그레이션 파일 1개 신설
- **코드**: `lenses.ts`·`lensCompute.ts`·`lensCopy.ts`·`lensPrecompute.ts`(STEP1083·1087)
- **새 자산**: `data/sources/academic/jegadeesh_titman_1993_momentum.pdf`(STEP1084) · `data/sources/README.md`
- **새 문서 절**: `BUILD_SEQUENCE §6-B`·`§6-B-1`·`§6-C`(판정 대장 76행) · `LENS_DISPOSITION §7-4 #7-부록` · `STATE.md ⓪`
- **판정 확정**: A·B · 검산 인프라 ①
- **교훈/실패**: STEP1073 §8 재발(#1·#4) · STEP1085 설계 낙관(`total_assets_prior`) · **STEP1086·1088 grep 방식 결함(이 STEP이 발견)**

🔴 **위 목록에 빠진 게 있는지 `CHANGELOG` 원문으로 재확인**하고 개수를 보고한다.

### ⓪-2. 🔴 대상 지침 문서 (전부 연다 — 안 연 것은 "안 열었다")
```
CLAUDE.md                       # 최상위 지침 — :632 배너 규칙 · :655~657 마감 체크리스트
docs/SYSTEM_MAP.md              # 아키텍처·파이프라인
docs/LENS_DEV_PLAYBOOK.md       # 렌즈 문제해결 로그
docs/INDEX.md                   # 전체 문서 마스터 인덱스
docs/DATA_SOURCE_CATALOG.md     # 자료 출처 카탈로그
docs/COMMIT_GATES.md            # 커밋 게이트
docs/CROSS_REFERENCE.md         # 저장소 간 참조
docs/LENS_ARCHITECTURE.md       # 렌즈 구조
docs/REVDCF_SPEC.md             # 역DCF 정본
docs/VALUATION_SPEC.md          # 밸류에이션 정본
docs/MODEL_ROSTER.md            # 모델 명부
docs/LENS_STRENGTH_MAP.md · LENS_COPY.md · LENS_DISPLAY_CHARTER.md · ANSWERABILITY_MAP.md
```
🔴 **`CLAUDE.md`·`docs/INDEX.md`가 위 목록 밖의 지침 문서를 더 지목하면 추가**한다. 목록을 짐작으로 닫지 않는다.

---

## §1. 🔴 역방향 대조 — 문서마다 「담당 범위 × 이번 변경」

각 대상 문서에 대해 **이 질문에 답한다**: *"이 문서가 담당하는 것 중, ⓪-1 목록에서 바뀐 게 무엇인가?"*

| 문서 | 담당 범위(그 문서가 스스로 밝힌) | 이번에 바뀐 것 | 처리 |
|---|---|---|---|
| `SYSTEM_MAP.md` | 아키텍처·파이프라인 | `lens_scores` **19→26컬럼** · `lensPrecompute` 쓰기 경로 · 마이그레이션 | 🔴 **갱신** |
| `LENS_DEV_PLAYBOOK.md` | 렌즈 문제해결 로그 | STEP1083(창작 산식 제거·죽은 필드 발견) · STEP1084(원전 확보·OCR 우회) · STEP1085(설계 낙관) · STEP1087(배선 불가) | 🔴 **행 추가** — `:644`가 *"몰아서 하지 말 것"*이라 했으나 **이미 몰렸다**. 그 사실도 함께 적는다 |
| `INDEX.md` | 전체 문서 마스터 인덱스 | 새 절(§6-C 대장·#7-부록·STATE ⓪) · 새 자산(`data/sources/academic/`) · 새 마이그레이션 | 🔴 **갱신** |
| `DATA_SOURCE_CATALOG.md` | 자료 출처 92소스 | **Jegadeesh–Titman(1993) 원전 신규 확보**(무료 취득 경로 포함) | 🔴 **등재** |
| `CLAUDE.md` | 최상위 지침 | 판정 A·B 확정 = **방향 변화인가?** | 🔴 **§2에서 판별** |
| 나머지 | (각자 밝힌 범위) | | 🔴 **관련 없으면 "검사함·해당 없음"이라 명시** |

🔴 **표를 이대로 베끼지 말고, 문서를 열어 「그 문서가 스스로 밝힌 담당 범위」를 확인한 뒤 채운다.**
🔴 **"해당 없음"도 결과다** — 안 고친 문서마다 **왜 안 고쳐도 되는지 한 줄**.

---

## §2. 🔴 `CLAUDE.md` — 고칠지 말지부터 판별

`CLAUDE.md:632`: *"`CLAUDE.md` 배너는 **방향·규칙 바뀔 때만** 갱신 — 매 세션 강제 아님(배너 비대화 방지)"*

판별 대상:
| 후보 | 방향·규칙 변화인가 |
|---|---|
| **판정 A 확정**(정의1 폐기·정의3 최상위) | 🔴 `CLAUDE.md`가 제품 정의를 서술하는 절이 있으면 **방향 변화**다 |
| **판정 B 확정**(확정된 모델 1개씩 완성) | 🔴 제작 방식 규칙이므로 **규칙 변화** 가능성 |
| **판정 대장 §6-C 신설** | 🔴 *"판정은 어디에 모이나"*가 새로 생겼다 — 문서 위계(`:666~`)에 추가 대상인지 판별 |
| **검산 인프라 7컬럼** | 스키마 변화 → `SYSTEM_MAP` 쪽. `CLAUDE.md`는 아닐 수 있음 |
| **STEP1086·1088 grep 방식 결함** | 🔴 **재발 방지 규칙 후보** — *"전수 대조는 정방향(grep)만으로 부족하다. 문서에서 출발하는 역방향을 함께 돈다"* |

🔴 **넣기로 한 것만 넣는다. 배너를 비대하게 만들지 않는다.** 🔴 **판별 결과(넣음/안 넣음 + 사유)를 전부 보고**한다.
🔴 **`CLAUDE.md`의 절대규칙·금지사항을 삭제하거나 약화시키지 않는다.**

---

## §3. 🔴 재발 방지 — 방식 결함을 규칙으로

`BUILD_SEQUENCE.md` §8(반복된 실수)에 **신규 행 추가**:
> 🔴 **전수 대조를 정방향(씨앗→grep)으로만 돌았다**(2026-08-19 STEP1086·1088). **grep은 「틀린 서술」은 찾아도 「빠진 서술」은 못 찾는다** — `SYSTEM_MAP.md`에 새 컬럼 서술이 **아예 없어** 검색에 안 걸렸고, `LENS_DEV_PLAYBOOK`·`INDEX`·`DATA_SOURCE_CATALOG`도 같은 이유로 누락됐다. **장은태가 지적해서 발견**(STEP1089). → **이후 전수 대조는 정방향 + 역방향(문서→"내 담당 중 뭐가 바뀌었나")을 함께 돈다.**

그리고 `docs/COMMIT_GATES.md`에 **게이트 신설 여부를 판별**한다 — 🔴 *"세션 마감 시 `CLAUDE.md` 마감 체크리스트(`:655~657`)의 문서가 전부 갱신됐는지 mtime으로 확인"*. 🔴 **신설이 과하면 안 만든다**(게이트 비대화도 문제). 판별 결과를 보고.

---

## §4. 과거 누락 (🔴 이번 세션분만 고치고, 과거분은 등재만)
역방향을 돌다 **이번 세션 이전의 누락**이 보이면(예: `LENS_ARCHITECTURE.md` 07-12, `CROSS_REFERENCE.md` 04-23) — 🔴 **고치지 않는다.** `BUILD_SEQUENCE §6-C` 대장에 **판정 대기 1행**으로 등재한다(*"과거 세션 누락분을 소급 갱신할 것인가"*). 🔴 **10개를 각각 등재하지 말고 한 행.**

## §5. 표시본 + 마감 문서 재갱신
- `docs/*.html` — 이번 갱신이 표시본에 걸리면 **머리 요약과 본문 둘 다**.
- 🔴 **`STATE.md ⓪` 인수인계 절을 다시 손본다** — 이 STEP의 결과가 반영돼야 한다. 🔴 **부수 정정**: 현재 *"총 76행(🟢45)"*과 *"🟢43행 중 선택"*이 **어긋나 있다**(45가 맞다, 45+19+12=76). **세어보고 확정**해 고친다.
- `CHANGELOG.md` · `STEP_LEDGER.md`. 🔴 **`~/Downloads/` 무접촉.**

## §6. 검증 3
① **역방향 전수** — 대상 문서 **전부**에 대해 「담당 범위 × 이번 변경」을 물었는지. 🔴 안 물은 문서 0개. "해당 없음"에도 사유가 붙었는지.
② **빠진 서술 검사** — 🔴 각 문서에서 *"있어야 하는데 없는 것"*을 찾았는지(grep으로는 안 되는 것). 찾은 개수 보고.
③ **역방향의 역방향** — 갱신 후, ⓪-1 변경 목록의 **각 항목이 최소 한 문서에는 기록됐는지** 확인. 🔴 어느 문서에도 안 남은 변경이 있으면 **그것도 누락**이다.

## §7. 검수 3
① **판정 안 했는가** — `CLAUDE.md` 절대규칙 약화 0 · 과거 누락 소급 여부 **안 정함** · 게이트 신설을 과하게 안 했는지 · `Q1_ENABLED`·`REVDCF_ENABLED` 무접촉.
② **삭제·창작 안 했는가** — 취소선 보존 · 새 파일 0(신규는 이 명령서 하나) · 지어낸 내용 0.
③ **정직한가** — 🔴 "해당 없음" 판정이 **실제로 문서를 열고 내린 것**인지 · 못 한 것 전부 나열.

## §8. 커밋 + 보고
- 🔴 **`main` 직접 push.** 브랜치·PR·worktree 금지. **`docs/` + `CLAUDE.md` 외 무접촉.**
- 커밋 메시지: `docs: STEP1089 지침 문서 전수 갱신 — 역방향 대조로 빠진 서술 보완`

```
[STEP1089 결과]
⓪ 이번 세션 변경 목록: 유형별 N건 (CHANGELOG 재확인으로 추가된 것 M건)
① 대상 지침 문서: N개 (CLAUDE.md/INDEX.md가 더 지목한 것 M개 추가?)
② 역방향 대조 결과 — 문서별 한 줄:
   - SYSTEM_MAP.md: 갱신 내용 / 해당없음
   - LENS_DEV_PLAYBOOK.md: 추가한 로그 행 N개 (몰아서 한 사실 기록했나)
   - INDEX.md: 갱신 내용
   - DATA_SOURCE_CATALOG.md: Jegadeesh-Titman 등재?
   - (나머지 전부 — 해당없음도 사유 포함)
③ CLAUDE.md 판별: 후보 5개 각각 넣음/안넣음 + 사유 / 절대규칙 약화 0 확인
④ §3 재발방지: §8 신규 행 / COMMIT_GATES 게이트 신설 여부 + 사유
⑤ 과거 누락: 발견 N건 → 대장 1행 등재(통번호)
⑥ STATE.md ⓪ 재갱신 + 🔴 45↔43 불일치 정정(어느 쪽이 맞았나)
⑦ 검증 3 — 1)역방향 전수(안 물은 문서 0?) 2)빠진 서술 발견 N건 3)역방향의역방향(어느 문서에도 안 남은 변경 N건)
⑧ 검수 3 — 1)판정안함 2)삭제·창작안함 3)정직
🔴 못 한 것: (전부 나열)
🔴 열지 않은 파일: (⓪-2 목록 중)
커밋: <해시>
```
