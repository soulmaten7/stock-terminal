# STEP 1064 — 7렌즈 전수감사 **종합** + 판정 재료 한 장 (🔵 읽기 전용 · 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 수동 실행 0)

> 🔴 **판정은 장은태가 한다. 이 STEP은 판정을 「가능하게」 만드는 데까지다.** 결론을 내지 않는다.
> 🔑 **⑦이 명시적으로 넘긴 일이다** — `LENS_AUDIT_07_ASSETGROWTH.md` 머리: *"**7렌즈 종합·정리는 이 STEP의 몫이 아니다**(하나를 하나씩)."* 이제 그 하나를 한다.
> 🔴 **왜 지금인가**: 감사 결과가 **7개 문서에 흩어져** 있다. 판정 대기가 쌓여 있고(아래 §2-5), 판정하려면 **한 장**이 필요하다. 새 조사를 하는 STEP이 아니라 **이미 확보된 재료를 판정 가능한 형태로 놓는** STEP이다.
> 🔵 읽기 전용 — **외부 신규 취득 0.** 새 DB 조사도 최소(§2-2에 명시된 것만).

---

## ⓪ 전제 점검

### ⓪-1. 직전 산출물 — 원문 인용

| 출처 | 인용(그대로) |
|---|---|
| `docs/LENS_AUDIT_07_ASSETGROWTH.md` 요약 | *"🔴 ⓪-4① 집계 레이어 — **확정 결함 — ⑤ 결함①과 같은 뿌리(묶어서 기록).** 코드 주석 *「라벨=확장 강도 … verdict 아님」*인데 `lenses.ts:396`의 `verdict.tone`은 `conservative→pos/aggressive→warn`"* |
| 같은 문서 §4 | *"손계산 — **구조적으로 불가능 — 네 번째 사례 확정**(④grossProfit·⑤rsi14/pos52w·⑥reason에 이어)"* |
| 같은 문서 §8 | 7렌즈 전수 완결 확인 — `momentum·technical·valuation·lowVol·quality·assetGrowth`(`lenses.ts` `export const` 6개) + `computeFScore`(`fscore.ts`, 별도 모듈) = **7개** |
| 같은 문서 「못 한 것」 | *"Cooper-Gulen-Schill(2008) 원문 물리적 재확보 — **무료 공개본을 찾지 못함**(원문 미확보로 종결)"* |
| `docs/LENS_AUDIT_06_FSCORE.md` 요약 | *"앞선 결함 18건 생존 → **18/18 그대로**"* — 🔴 **①밸류 이후 지금까지 고쳐진 결함은 0건이다.** |
| `docs/LENS_AUDIT_05_TECHNICAL.md` 결함① | *"`toneForKey`·`tonesFromStates`·`tonesFor`·`firstPosLens`는 **등급을 전혀 보지 않는다**"* |
| `docs/LENS_DISPOSITION_2026-08-08.md` §0 | v1 오류 4건, 원인 = *"자료를 쓰기 전에 ③(자체 데이터 확인)을 안 돌렸다"* — 🔴 **이 STEP은 그 반대다. ③이 일곱 번 끝난 뒤에 쓴다.** |

🔴 **직전 STEP(1063)이 「못 한 것」으로 남긴 것 중 이 STEP이 건드리는 것**: 없음. ⑦의 미완(손계산·역방향·CGS 원문)은 **전부 측정 한계**이고, 이 STEP은 새 측정을 하지 않는다. **그 「못 함」들을 종합표에 그대로 옮겨 적는 것**까지가 이 STEP의 일이다.

### ⓪-1a. 🔴 로드맵 원문 대조 (`docs/ROADMAP_V2.md` — 여섯 층 전부)

| 층 | 걸리는 절 | 인용(원문 그대로) |
|---|---|---|
| **WHY** | 조건 1·3 (`:65·67`) | *"**정확해야 한다** … 🔴 계산이 먼저다 — 협상 대상 아님"* · *"**왜 그런지 알 수 있어야 한다**"* → 🔴 **결함을 「화면에 영향 있음/없음」으로 가르는 기준이 이 둘이다**(§2-2). |
| **HOW** | H-4 (b) 보류(Parked) (`:166~181`) | 🔴 **처분이 「보류」로 갈 경우 5칸 형식**(①왜 보류 ②깔아둔 배선 ③활성화 체크리스트 ④비용·노력 ⑤재사용 범위)이 이미 정해져 있다. **선택지를 놓을 때 이 형식을 쓴다.** |
| **WHAT** | W-2-4 구조 잔여 3건 | *"저변동성·자산성장 … **처분 판정 자체는 여전히 미정**"* · *"Beneish M-Score(… **칸 신설 여부**)"* → 🔴 **판정 대기 목록의 원천.** |
| **만드는 절차** | ⬜ 확정 대기 | 🔴 **이 층이 아직 비어 있다.** 종합표가 그 재료가 될 수 있으나 **이 STEP이 절차를 정하지 않는다.** |
| **관문·순위** | F-1 ①②③ (`:333~335`) | 🔴 **각 렌즈를 관문 셋으로 재채점하는 것이 §2-3이다** — 감사가 끝났으므로 이제 ②(정확)·③(정직)에 실측 근거가 붙는다. |
| **완성의 정의** | C-1 9항목 (`:487~499`) | 🔴 **§2-4의 축.** 7렌즈 × 9항목 격자를 채운다. *"정본 = `docs/LENS_COMPLETION_STANDARD.md`. 아래는 그 요약이며 **정본이 우선**한다."* → **정본을 먼저 읽는다.** |

### ⓪-1b. 기존 답 확인 — 🔴 `ls docs/` 전수
2026-08-18 확인분(원격 `main` = `a0979a4`): 감사 일곱 완비. **종합 문서는 없음.**
🔴 **`ls docs/` 전수로 「종합·처분·완성기준」 성격의 기존 문서를 먼저 찾아라** — 최소 `LENS_DISPOSITION_2026-08-08.md` · `LENS_COMPLETION_STANDARD.md` · `LENS_STRENGTH_MAP.md` · `LENS_DEV_PLAYBOOK.md` · `LENS_ROADMAP*`를 열어볼 것. **셋 다 이미 「렌즈를 어떻게 볼 것인가」를 다룬다.**

### ⓪-2. 건드리는 슬롯
**해당 없음** — 읽기 전용.

### ⓪-3. 걸리는 금지사항
- [ ] **코드 diff 0 · DB 쓰기 0 · 화면 변경 0** — 🔴 7렌즈 라이브 화면 수정은 매번 별도 승인(899·901 위반 전례)
- [ ] 🔴 **크론 수동 실행 금지 · 외부 신규 취득 0**(논문 확보도 이 STEP에선 안 한다 — 새 조사가 아니다)
- [ ] **KR 전면 동결**
- [ ] `data/us_symbols.json`·`.github/workflows/**`·`vercel.json` 수정 금지
- [ ] `REVDCF_ENABLED` OFF 유지 · `Q1_ENABLED` 무접촉
- [ ] 🇺🇸 US 단독

### ⓪-3b. 🔴 이 STEP의 함정 — **새 결론을 만들지 않는다**
소재는 **일곱 감사 문서 + 로드맵 + 완성기준 정본**이다. 🔴 **종합하다 보면 「그러니까 이렇게 하면 된다」가 튀어나온다. 그게 판정이다. 하지 말 것.**
🔴 **각 칸은 반드시 좌표(문서:절)를 달고**, 좌표를 못 다는 문장은 **적지 않는다.** 못 달겠으면 *"이건 내 추측"*이라 명시하고 **판정 재료가 아니라 별도 칸**에 둔다.

### ⓪-4. 🔴 반증 조건

**전제 ①** — 감사 일곱이 찾은 결함 **18건**이 정확한 개수다.
- 틀렸다면 → ㉠같은 결함이 다른 이름으로 두 번 세어져 있다(⑤ 배당 미조정 ↔ ③ 신규①이 이미 묶였다 — **또 있는가**) ㉡한 문서에서 결함으로 적힌 것이 다른 문서에서 「이미 공개된 한계」로 판정돼 있다.
- 🔴 **묶기·빼기를 하면 개수가 바뀐다. 바뀐 개수와 그 근거를 명시한다.** 18을 지키려 하지 않는다.

**전제 ②** — 결함 대부분이 **지금 화면에 영향을 준다.**
- 🔴 **반대 방향 증거가 이미 여럿이다** — ④가 *"`Q1_ENABLED`가 프로덕션에 아예 없다(=OFF)"*를 확인했고, ⑤ 결함③(`pos52` 가드)은 *"몇 % 종목을 오도하는지 미측정"*이고, ⑥은 신규 결함 0건이다.
- 틀렸다면 → **화면에 실제로 영향을 주는 결함은 소수**다. 그러면 **우선순위가 완전히 달라진다.**
- 🔴 **이 갈림이 「제품을 언제 내놓을 수 있나」에 직결된다. 반드시 결함별로 갈라라.**

**전제 ③** — 판정 대기 항목이 **11건**이다.
- 🔴 **근거는 내(Cowork) 대화 중 집계이고 문서 하나에 모여 있지 않다.** 틀렸을 수 있다.
- 🔴 **`ROADMAP_V2.md`·`STATE.md`·`STEP_LEDGER.md`·감사 일곱을 전수로 훑어 「미판정·판정 대기·처분 미정」 표현을 찾아 다시 센다.** 개수가 다르면 **문서 쪽이 맞다.**
- **전수의 조건** — 문자열 검색만으로 세지 않는다. **역방향**: 각 감사 문서의 「못 한 것 / 미측정」이 판정 대기로 올라가야 하는지도 본다.

### ⓪-5. 🔴 3번 생각
1. **범위** — 곁가지 아님. **판정이 병목**이고, 판정하려면 한 장이 필요하다. 새 조사가 아니라 **정리**다.
2. **표본** — 🔴 **전수.** 감사 일곱 문서 **전문**을 다시 읽는다(요약만 보고 종합하지 않는다 — 각 문서의 「못 한 것」이 종합표의 절반이다). DB 재조사는 §2-2에 명시된 것만.
3. **시점** — 인용은 **원격 `main` = `a0979a4` 기준**임을 머리에 명시한다. 이후 커밋이 있으면 그것도 적는다.

---

## 1. 배경

**확정**
- 7렌즈 감사 완결 — ①밸류 ②모멘텀 ③저변동 ④퀄리티 ⑤기술 ⑥F스코어 ⑦자산성장
- 결함 누적 **18건**(①6 ②5 ③4 ⑤3) · **⑥⑦ 신규 0건**(⑦의 집계 레이어는 ⑤와 묶임)
- 🔴 **고쳐진 결함 0건** — 전부 읽기 전용 감사였다
- 🔴 **공통 패턴이 드러났다**(종합에서 확정할 것):
  ㉠ **집계 레이어가 등급을 안 본다** — 기술(`ref`)·자산성장(`partial`)이 검증 렌즈와 동일 가중(⑤·⑦)
  ㉡ **가격 계열 배당 미조정** — 저변동·기술(모멘텀만 `adjCloses`)(③·⑤)
  ㉢ **검산 인프라 공백** — 중간 판정 근거가 DB에 저장 안 됨, **네 번 연속**(④⑤⑥⑦)
- 원전 확보 상황: Novy-Marx(④) ✅ · George-Hwang(⑤) ✅ · Piotroski(⑥) ✅ · BBW(③) ✅ · **Cooper-Gulen-Schill(⑦) ❌ 미확보**

**확정 안 됨** 🔴
- 결함 18건 중 **지금 화면에 실제로 영향을 주는 것이 몇 개인가**
- 각 렌즈가 **질문 여섯 중 어디에 붙는가**(⑤⑥은 배정 확인, ⑦은 미배정, ①~④는 흩어져 있음)
- **판정 대기의 정확한 목록과 개수**
- C-1 9항목 기준으로 **완성에 도달한 렌즈가 있는가**

---

## 2. 할 일

### 2-1. 감사 일곱 전문 재독 + 결함 대장(臺帳)
일곱 문서를 **전문으로** 읽는다(요약만 보지 않는다). 결함을 한 표로 모은다.

| # | 렌즈 | 결함 | 좌표(파일:행) | 출처 감사 | 현 상태 | **화면 영향** | 공통 패턴 |
|---|---|---|---|---|---|---|---|

🔴 **묶기·빼기를 먼저 한다**(⓪-4①) — 같은 뿌리는 하나로, 「이미 공개된 한계」로 판정된 것은 뺀다. **최종 개수와 그 근거를 적는다.**

### 2-2. 🔴 결함별 「화면 영향」 판정 — 이 STEP의 중심 (⓪-4②)
각 결함에 셋 중 하나:
- **A. 지금 사용자에게 보인다** — 라이브 화면·이메일·브리핑에 실제로 나간다
- **B. 코드에 있으나 지금은 안 보인다** — 플래그 OFF(`Q1_ENABLED`·`REVDCF_ENABLED`)이거나 경로가 안 쓰인다
- **C. 영향 범위 미측정** — 감사가 못 잰 것

🔴 **A는 근거를 대야 한다** — 어느 화면 어느 컴포넌트인지 좌표. 못 대면 **C**다. *"보일 것이다"로 A에 넣지 않는다.
🔴 **허용된 DB 조사는 이것뿐**: A 판정에 필요한 최소 확인(예: 해당 값이 `lens_scores`에 실제로 채워져 있는가). **새 지표를 만들지 않는다.**

### 2-3. 관문 셋 재채점 (F-1)
7렌즈 각각을 **① 원하는 정보인가 / ② 정확한가(검산 가능한가) / ③ 정직한가(밝힐 수 있는가)** 로 재채점.
🔴 **이제 ②③에 실측 근거가 붙는다** — 감사가 끝났으므로. 각 칸에 **좌표**를 단다.
🔴 **탈락시키지 않는다.** 채점표를 놓는 데까지.

### 2-4. C-1 9항목 격자
🔴 **정본 `docs/LENS_COMPLETION_STANDARD.md`를 먼저 읽고**(로드맵은 요약이며 정본이 우선), 7렌즈 × 9항목 격자를 **✅ / ❌ / 🟡부분 / — 해당없음**으로 채운다. 각 칸에 좌표.
🔴 **가장 중요한 것은 ❌가 어디에 몰려 있는가**다 — #3(값 검증: 손계산+외부 대조)이 ④⑤⑥⑦에서 연속 막혔다. **그게 격자에서 세로줄로 보여야 한다.**

### 2-5. 🔴 판정 대기 한 장 (⓪-4③)
문서 전수로 「미판정·판정 대기·처분 미정」을 찾아 다시 센다. **각 항목마다 다섯 칸:**

| 칸 | 내용 |
|---|---|
| **무엇을 정해야 하나** | 한 문장. 🔴 **낱말의 뜻이 갈리면 그것부터 물음으로 적는다** |
| **선택지** | 2~4개. 🔴 **H-4 (b) 보류가 선택지면 5칸 형식**(①왜 보류 ②깔아둔 배선 ③활성화 체크리스트 ④비용·노력 ⑤재사용 범위) |
| **각 선택의 결과** | 무엇이 열리고 무엇이 닫히나 |
| **확보된 재료** | 좌표. 🔴 **없으면 「없음」** |
| **재료가 더 필요한가** | 필요하면 **무엇을 재면 되는지** 한 줄 |

🔴 **추천하지 않는다. 순위를 매기지 않는다.** 선택지를 나란히 놓는 데까지.
🔴 **단 「재료 부족」은 명시한다** — 재료 없이 판정하라고 내밀지 않는다.

### 2-6. 렌즈↔질문 귀속 종합
7렌즈 각각을 **여섯 중 하나 / 새 질문 후보 / 못 씀** 셋으로. 🔴 **⑤⑥⑦이 각자 낸 결론을 인용**하고, ①~④는 문서에서 찾아 채운다. 🔴 **억지로 맞추지 않는다**(W-2-1). 🔴 **비는 칸은 「미분류」로 남긴다.**

### 2-7. 산출물 — 🔴 **새 파일을 만들지 않는 쪽을 먼저 시도한다**
1순위: **`docs/LENS_DISPOSITION_2026-08-08.md`에 종합 절을 추가**한다. 근거 — 그 문서가 정확히 *"렌즈를 어떻게 처분할 것인가"* 문서이고, ③ 감사가 이미 그 문서의 §3 오류를 정정했다. **기존 절은 지우지 않는다**(⑤·⑥·1058이 한 방식).
🔴 **`ls docs/` 전수 결과 더 맞는 곳이 있으면 그쪽.** 정말 없을 때만 새 파일을 만들고 **왜 기존 문서에 못 넣었는지 근거를 적는다.**
`docs/CHANGELOG.md` · `docs/STEP_LEDGER.md` 등재.

---

## 3. 🔴 하지 말 것
- 🔴 **판정하지 않는다.** 고칠지 말지, 뺄지 남길지, 무엇을 먼저 할지 정하지 않는다. **추천·순위도 금지.**
- 🔴 **새 결론을 만들지 않는다**(⓪-3b). 좌표를 못 다는 문장은 적지 않는다.
- 🔴 **새 조사를 하지 않는다.** DB는 §2-2의 A 판정에 필요한 최소만. **외부 취득 0**(논문 포함).
- 🔴 **결함 개수를 18로 지키려 하지 않는다.** 묶기·빼기의 결과가 진짜 개수다.
- 🔴 **「보일 것이다」로 화면 영향 A에 넣지 않는다.** 좌표 없으면 C.
- 🔴 **코드·화면·DB 무접촉.**
- 🔴 **KR 무접촉.**
- 🔴 **「만드는 절차」 층을 채우지 않는다** — 로드맵에서 ⬜ 확정 대기이고, 그건 장은태 판정이다.
- 🔴 **일곱 문서를 요약만 보고 종합하지 않는다.** 각 문서의 「못 한 것」이 종합표의 절반이다.

## 4. 커밋 전 게이트
- 게이트 7: git 미추적 파일 참조 검사
- 게이트 8: push 후 **GitHub Actions verify 초록 + Vercel 배포 성공을 눈으로 확인**
- 게이트 9: 새 테이블·크론·관측 필드 없음 → 해당 없음
- `docs/CHANGELOG.md` 항목 · `docs/STEP_LEDGER.md` 등재
- 🔴 **브랜치 규약(STEP1056 §2-7)** — `main`에 직접 push. **브랜치·PR 만들지 않는다. 예외 없음.**

## 5. DoD
- [ ] ⓪-1a 로드맵 **여섯 층** 대조 · ⓪-1b `ls docs/` 전수(종합·처분 성격 기존 문서 선확인)
- [ ] 2-1 감사 일곱 **전문 재독** + 결함 대장 — 🔴 **묶기·빼기 후 최종 개수와 근거**
- [ ] 🔴 2-2 결함별 **화면 영향 A/B/C** — A는 전부 좌표 있음
- [ ] 2-3 7렌즈 × 관문 셋 재채점(좌표 포함) — 🔴 탈락시키지 않았다
- [ ] 2-4 7렌즈 × **C-1 9항목 격자**(정본 먼저 읽음) — 🔴 **❌가 몰린 세로줄**을 명시
- [ ] 🔴 2-5 **판정 대기 한 장** — 항목마다 다섯 칸. 🔴 **추천·순위 없음** · 재료 부족은 명시
- [ ] 2-6 렌즈↔질문 귀속 — 🔴 비는 칸은 「미분류」
- [ ] 2-7 🔴 **기존 문서 갱신 우선**(1순위 `LENS_DISPOSITION`), 새 파일이면 근거 명시
- [ ] 인용 기준 커밋(`a0979a4`) 명시 · 이후 커밋 있으면 반영
- [ ] 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 수동 실행 0 · 외부 취득 0 · tsc 0 · test 전량 통과
- [ ] 마지막에: **못 한 축 / 철회·정정한 것 / 미측정으로 남은 것**

🔴 **판정은 장은태가 한다. 이 STEP은 판정할 수 있게 놓는 것까지다.**
