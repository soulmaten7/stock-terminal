# STEP 1088 — 🔴 **세션 마감 전체 문서 업데이트 + 인수인계** (🟡 문서 전용 · 코드 diff 0 · DB 쓰기 0 · 스키마 변경 0 · 화면 변경 0 · 크론 수동 실행 0)

> 🔴 **장은태 지시(2026-08-19)**: *"전체 모든 문서 업데이트하면 **세션 마감**하고 대화할 거야"*
> 🔴 **판정하지 않는다.** 사실만 반영하고, 판정이 필요한 것은 **판정 대기로 등재**한다. **다음 세션이 이어받을 수 있게 만드는 것**이 이 STEP의 목적이다.
> 🔴 **새 작업을 시작하지 않는다.** 남은 것은 「남았다」고 적는다.
>
> **왜 지금인가 — 3번 규칙(2026-08-19):**
> **축 A(원문)** — 위 지시 + `BUILD_SEQUENCE.md:346`(정본 먼저·새 파일 금지) · `:304`(표시본은 머리+본문 둘 다) · `/preferences.md` *"문서 정리가 끝나면 모든 기록해야 하는 문서를 전수 업데이트한다"*.
> **축 B(실무·실측)** — STEP1086이 08-19에 한 번 돌았으나 **씨앗 #18(STEP1085)이 미실행이라 확인 불가**로 남았다. 그 뒤 **STEP1085·1086·1087**이 더 쌓였다. **그 세 건이 아직 안 퍼졌다.**
> **축 C(반대 증거)** — ㉠**STEP1086이 이미 했다** → 🔴 **범위가 다르다**(1073~1084까지였고, 1085~1087이 빠졌다) ㉡**각 STEP이 자기 문서를 고쳤다** → 🔴 STEP1086 실측으로 **실제 정정 2건이 남아 있었다**. 자기 문서만 고치는 패턴은 실재한다 ㉢**마감이라고 서둘러 「다 됐다」고 적는 위험** → 🔴 **이 STEP의 최대 위험.** 남은 것을 반드시 나열한다.

---

## ⓪ 전제 점검

### ⓪-1. 🔴 실행 상태부터 확인 (짐작 금지)
`git log --oneline` 으로 **STEP1085·1086·1087이 실제로 커밋됐는지** 각각 확인한다.
- **STEP1087이 미실행이면** — 🔴 **그 사실을 명시**하고, 씨앗에서 빼고, §5 인수인계에 **"STEP1087 미실행 — 다음 세션 첫 후보"**로 적는다. 🔴 **실행하지 않는다**(이 STEP은 문서 전용).
- `docs/step_orders/` 안에 **untracked 명령서가 더 있는지** 확인하고 전부 나열한다(STEP1086이 STEP1085에 대해 발견한 것과 같은 검사).

### ⓪-2. 🔴 씨앗 — 이 세션(STEP1073~1087) 확정 사실 전수
STEP1086 §⓪-1의 **씨앗 17건**(1073~1084분)을 **그대로 인용**하고, 아래를 **추가**한다:

| # | 씨앗 | STEP | 좌표 |
|:--:|---|:--:|---|
| 18 | **검산 인프라 ①채택 + 설계 확정** — 제안 컬럼 **7개**, 상상 필드 1건 삭제(`costOfRevenue`) | 1085 | `LENS_DISPOSITION §7-4 #7-부록` §2 |
| 19 | 🔴 **①로도 최근변화는 9/9 불가 — 최대 7/9** (원시 종가 252봉·F스코어 3개년은 행에 안 들어감) | 1085 | 같은 부록 §4 |
| 20 | 🟢 **자산성장의 「손계산 구조적 불가」는 ①로 지워진다** · 퀄리티 ⑦도 풀림 | 1085 | 같음 |
| 21 | **읽기 소비처 9곳 전부 명시 `select`, `select("*")` 0곳** → 컬럼 추가해도 화면 페이로드 불변 | 1085 | 같은 부록 §3 |
| 22 | **백필 불가** — 원자료가 저장된 적 없어 기존 1,976행은 못 채움. 다음 정규크론부터 자연 채움 | 1085 | 같음 |
| 23 | 🟢 **최근변화는 5/9가 아니라 6/9였다**(머리글↔표 불일치 정정) | 1085 | `LENS_COMPLETION_STANDARD` |
| 24 | **문서 1,226개 분류** — 살아있는 기록 **67** · 시점 이력 1,119 · 죽은 문서 30(전부 자기표기 근거) · **판별 불가 10** | 1086 | `BUILD_SEQUENCE` 부록 |
| 25 | **`STATE.md` `sectorSource` 침묵버그 서술 정정 2건** — STEP1050 해소 + STEP1081 재측정(11일 격차) | 1086 | `STATE.md` |
| 26 | **검산 인프라 ① 집행 완료** — `lens_scores` 7컬럼 추가(전부 nullable) · 마이그레이션 `20260819_lens_scores_raw_verification_fields.sql` · 착수 전 재측정이 §1 기록값과 완전 일치(드리프트 0) | 1087 | `CHANGELOG` STEP1087 |
| 27 | 🔴 **씨앗 #20이 뒤집혔다 — `total_assets_prior` 배선 불가.** `assetGrowth.compute()`가 그 값을 `LensRead` 어떤 필드로도 반환하지 않는다(접근 경로 자체가 없음). **6/7컬럼만 배선**, `total_assets_prior`는 null로 남김 | 1087 | 같음 |
| 28 | 🔴 **따라서 「자산성장의 손계산 구조적 불가가 ①로 지워진다」(씨앗 #20)는 지금 상태로는 성립하지 않는다** — `total_assets`만으로는 성장률 재계산 대조 불가 | 1087 | §7-4 #7-부록 §4 |

🔴 **씨앗 밖에 더 있는지 `git log` STEP1085~1087 구간으로 역확인**하고 개수를 보고한다.

### ⓪-3. 🔴 씨앗 #20 정정 — **축 C ㉢(정정 근거가 뒤집혔다)의 실제 사례**
STEP1085 `#7-부록` §4가 *"자산성장 ③값검증 = **예**"*라 적었으나, STEP1087이 `total_assets_prior` **배선 불가**를 확인했다.
→ 🔴 **`#7-부록` §4의 자산성장 행을 취소선 + 정정**한다: *"~~예~~ → 🔴 **아니오(현재 상태)** — `total_assets_prior` 접근 경로 없음(STEP1087). 배선하려면 `lib/lenses.ts` 수정이 필요하며 이는 별도 판정."*
→ 🔴 **`LENS_DISPOSITION §8-1` 자산성장 행의 「손계산 구조적 불가」를 지우지 않는다.** 아직 유효하다.
→ 🔴 **판정 대기 신규 등재**: *"`total_assets_prior`를 얻기 위해 `lib/lenses.ts`(판정 인접 파일)를 고칠 것인가"* — `BUILD_SEQUENCE §6-C` 대장에 1행. **고르지 않는다.**

---

## §1. `docs/STATE.md` 전면 갱신 — 🔴 **이것이 인수인계 문서다**

`STATE.md`가 다음 세션이 **가장 먼저 읽는** 문서다. 이 세션에서 바뀐 큰 것들이 반영돼야 한다:

| 반영할 것 | 내용 |
|---|---|
| **판정 A·B 해소** | §6(다음에 할 일)에 A·B가 *"미판정"*으로 남아 있으면 🟢 **확정**으로. 좌표 병기 |
| **판정 대장 신설** | `BUILD_SEQUENCE §6-C` **74행**(🟢43·🟡19·🔴12)이 생겼다는 것 — 🔴 **다음 세션은 여기서 시작한다** |
| **「확정된 칸 넷」** | + **완성 승인 0** |
| **최근변화 칸 착수** | **6/9** · 코드 수정 3건 완료 · 원전 확보 · 🔴 **7/9가 상한**(인프라 ①로도) |
| **검산 인프라** | ①채택·설계 완료 · 집행 여부(STEP1087 실행/미실행) |
| **08-16 조인** | 재발 없음(배제율 0.47%) · 🔴 **원인 코드는 그대로** |

🔴 **옛 서술은 취소선으로 보존.** 🔴 **`STATE.md`가 「지금」을 말하게 만든다** — 이 세션 전에 쓰인 「다음에 할 일」이 이미 끝났으면 ✅로.

---

## §2. 정본 전수 반영 — 대상은 STEP1086이 가른 **㉠ 67개**

STEP1086이 만든 분류를 **다시 만들지 않고 그대로 쓴다**(재조사 금지, §8 #4).
씨앗 18~26을 검색어로 만들어 **㉠ 67개 문서에 grep** → 히트를 셋으로 가른다:
- **반영(사실 정정)** — 취소선 + 정정 + 좌표
- **등재(판정 필요)** — `BUILD_SEQUENCE §6-C` 대장에 행 추가(통번호 이어서). 중복이면 좌표 병기만
- **해당 없음** — 사유 한 줄

🔴 **쓴 검색어를 전부 기록**한다(재현 가능하게).
🔴 **역방향도 돈다** — ㉠ 67개에서 출발해 *"이 문서가 지금 사실과 다른 말을 하나"*. 씨앗에 없던 게 나오면 처리하고 개수를 보고.

---

## §3. STEP1086이 남긴 것 처리

| 남은 것 | 처리 |
|---|---|
| **㉣ 판별 불가 10건**(`SITE_MAP_V7`·`MARKET_RESEARCH_V1`·`DASHBOARD_SPEC_V1`·`HOME_REDESIGN_V2_SPEC`·`SITE_REVIEW_FIXES`·`SUPABASE_MIGRATION`·`SUPABASE_MIGRATION_HANDOFF`·`V3_RELEASE_NOTES`·`LOGO_PROMPT`·`MAKE_AUTOMATION`) | 🔴 **생사를 판정하지 않는다.** `§6-C` 대장에 **판정 대기 1행으로 등재**(*"이 10개 문서가 살아 있는지 죽었는지"*). 🔴 10개를 각각 등재하지 말고 **한 행**으로 |
| **씨앗 #18 확인 불가**(STEP1086 시점) | 🟢 이제 확인됨 — STEP1086 보고의 해당 줄에 **해소 표기** |
| **html 4개 미접촉** | 주제 무관이었는지 **재확인**하고, 무관이면 *"검사함·해당 없음"*이라 명시 |

---

## §4. 표시본 6개
`docs/*.html` 5개 + `CLAUDE.md`(있으면) — 🔴 **머리 요약과 본문 둘 다**. 🔴 **`~/Downloads/` 무접촉**(사용자 파일).

---

## §5. 🔴 세션 마감 기록 — **다음 세션 인수인계 한 장**

`docs/STATE.md` 맨 앞(또는 §0)에 **「직전 세션(2026-08-19) 요약」**을 신설한다. 🔴 **새 파일 금지, `STATE.md` 안에.**

| 칸 | 채울 것 |
|---|---|
| **무엇이 확정됐나** | 판정 A·B + 확정된 칸 넷 + 검산 인프라 ① — **각 좌표** |
| **무엇을 만들었나** | 최근변화 칸 6/9(코드 수정 3건·원전 확보) + (STEP1087 실행 시 컬럼 7개) |
| **지금 어디인가** | 🔴 **판정 대장 `BUILD_SEQUENCE §6-C` — 🟢N행이 지금 정할 수 있는 상태** |
| **다음 세션이 먼저 할 것** | 🔴 **Cowork이 정하지 않는다.** 후보만 나열: ①STEP1087(미실행 시) ②최근변화 ⑨라이브실측 after(크론 후) ③🟢43행 중 선택 ④9/9 위한 시계열 테이블 판정 — **순서·추천 없이** |
| **건드리면 안 되는 것** | `REVDCF_ENABLED` OFF 유지 · `Q1_ENABLED` 무접촉 · 크론 수동 실행 금지 · `~/Downloads/` 사용자 파일 · 수정 금지 경로 4개 |
| **미해결 위험** | 08-16 조인 원인 코드 그대로(재발 가능) · 섹터 11일 격차 · `lens_scores` 41종목 못 찾음 |

🔴 **「다 됐다」고 적지 않는다.** 남은 것을 반드시 나열한다(축 C ㉢).

---

## §6. 검증 3
① **재현 가능** — 검색어 전부 기록 · STEP1086 분류를 **다시 만들지 않고 인용**했는지(재조사 0).
② **반대 증거 셋** — 히트마다 ㉠판정인가 ㉡이미반영인가 ㉢근거가 뒤집혔는가. 각 몇 건인지.
③ **인수인계 검사** — 🔴 **`STATE.md`만 읽고 다음 세션이 이어받을 수 있는가.** 실행 측이 스스로 *"이 문서만 보고 내일 시작할 수 있나"*를 자문하고, **부족한 칸이 있으면 채운다.**

## §7. 검수 3
① **판정 안 했는가** — ㉣ 10건 생사 · 완성 순서 · 다음 세션 우선순위 **전부 안 정했는지** · `Q1_ENABLED`·`REVDCF_ENABLED` 무접촉.
② **삭제·창작 안 했는가** — 취소선 보존 · 새 파일 0(신규는 이 명령서 하나) · 지어낸 정정 0.
③ **정직한가** — 🔴 **남은 것을 전부 나열했는지** · *"다 됐다"*류 문장이 없는지 · 못 한 것에 사유가 붙었는지.

## §8. 커밋 + 보고
- 🔴 **`main` 직접 push.** 브랜치·PR·worktree 금지. **`docs/` 밖 무접촉.**
- 커밋 메시지: `docs: STEP1088 세션 마감 전체 문서 업데이트 + 인수인계`

```
[STEP1088 결과]
⓪ 실행 상태: STEP1085 ✅/❌ · 1086 ✅/❌ · 1087 ✅/❌ · untracked 명령서 N건(나열)
① 씨앗: 17 + 신규 9(18~26) + git log 추가분 N = 총 M건
② STATE.md 갱신: 항목 6개 각각 성공/실패
③ 정본 ㉠67개 대조: 검색어 전수 / 히트 N건 → 반영 a · 등재 b(대장 통번호) · 해당없음 c
④ 역방향 신규 발견: N건
⑤ STEP1086 잔여: ㉣10건 한 행 등재 / 씨앗#18 해소표기 / html 4개 재확인 결과
⑤-b 🔴 씨앗#20 정정(⓪-3): #7-부록 §4 자산성장 행 취소선+정정 / §8-1 「구조적 불가」 유지 확인 / total_assets_prior 판정 신규 등재(통번호)
⑥ 표시본 6개: 머리+본문 둘 다
⑦ 인수인계(§5): 6칸 전부 채움? 「다음 세션이 먼저 할 것」에 순서·추천 없나?
⑧ 대장 최종: 🟢/🟡/🔴 · 총 N행
⑨ 검증 3 — 1)재현가능(재조사 0?) 2)반대증거 셋 각 N건 3)인수인계 검사(STATE.md만으로 이어받기 가능?)
⑩ 검수 3 — 1)판정안함 2)삭제·창작안함 3)정직
🔴 못 한 것 / 남은 것: (전부 나열 — 이 STEP에서 가장 중요)
🔴 조회 시각(UTC):
커밋: <해시>
```
