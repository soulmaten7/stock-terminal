# STEP 1032 — 프루닝을 켠다 (🔴 되돌릴 수 없는 삭제 · 장은태 승인 완료) + 기록·정정 3건

> 🔴 **장은태 승인(2026-08-15): 프루닝을 지금 켠다.**
> 🔴 **이 STEP은 행을 삭제한다.** 1031이 "되돌릴 수 없는 변경은 한 번에 하나씩"이라고 미뤄둔 바로 그 축이다.
> 🔴 **그냥 켜지 않는다 — 삭제 상한(§1-2b)을 같이 넣는다.** 이유는 ⓪-5-1.

---

## 0-A. 🔴 1031은 성공했다 — 실측으로 확정

어젯밤 21:30 UTC 크론이 새 코드로 돌았다. `cron_heartbeats.job='lens-scores'`, `last_run_at=2026-08-14 21:39:32 UTC`, `ok=true`:

| 관측 | 값 | 뜻 |
|---|---|---|
| `freshCoverage` | **94.16%** | 🔴 구 산식(97%)이면 **FAIL** — 전환 없었으면 20일째 정지였다 |
| `priorCoverage` / `coverageDrop` | 93.89% / **−0.27%p** | 낙폭이 아니라 개선. 3%p 상한 여유 충분 |
| `coverageOk` · `compositionOk`(96%) · `cutGateOk` | 전부 **true** | 새 산식 통과 |
| `newCoverageOk` · `newCutGateOk` | 전부 **true** | 🔑 1025 관측 필드와 **일치** = 자체 검증 통과 |
| `pruned` / `pruneBlockedByFlag` | **false / true** | 🔑 §1-3 차단 **작동 확인** |
| `lens_cuts` US `as_of` | **2026-08-14** | 🔑 07-30에서 **19일 만에 갱신** |

**컷 이동(07-30 → 08-14)** — 19일치를 한 번에 반영:

| lens | lo (07-30→08-14) | hi (07-30→08-14) | n |
|---|---|---|---|
| momentum | −2.707 → **−1.194** | 34.272 → 36.891 | 974→944 |
| assetgrowth | 2.48 → 2.6 | 12.125 → 12.768 | 976→947 |
| lowvol | 27.184 → 27.512 | 40.896 → 42.725 | 978→950 |
| valuation | 18.24 → 18.02 | 35.1 → 35.7 | 901→875 |
| quality | 13.909 → **13.9** | 30.329 → 30.438 | 852→819 |

---

## 0-B. 🔴 Cowork이 정정하는 것 — 이 STEP이 기록해야 한다

### 정정 ①: 프루닝 규모 63행 → **76행**
1031 §0-A에 Cowork이 "프루닝 63행(6.08%)"이라고 적었다. **실측 `wouldPruneRows` = 76**(1,039행 중 **7.3%**). 1026이 세운 100행 기준 아래인 것은 맞지만 **숫자가 틀렸다.**

### 정정 ②: 🔴 W4 예측 11.3% → 실측 **6.2%** (예측의 절반)
STEP1025 W4 시뮬레이션은 컷 교체 시 판정 변화 **117/1,036 = 11.3%**를 예측했다. 실측 `churn` = **0.062**.

**Cowork 검산**(`lens_state_changes`, market='US'):

| change_date | 상태 | 축별 건수 | 합 |
|---|---|---|---|
| 08-13 | 컷 **정지**(07-30 컷) | lowvol 7 · momentum 45 · technical 46 · valuation 8 | **106** |
| 08-14 | 컷 **재유도**(08-14 컷) | assetgrowth 22 · lowvol 39 · momentum 55 · quality 5 · technical 33 · valuation 15 | **169** |

차분 = 169 − 106 = **63건 ÷ 1,039 = 6.1%** → heartbeat `churn` 0.062와 부합.

🔴 **원인은 규명하지 않는다(장은태 판정: 기록만).** Cowork의 **미검증 가설**만 남긴다: W4는 *"데이터 고정 · 컷만 교체"*를 시뮬레이션했으나 실제 08-14는 *"데이터와 컷이 함께 이동"*했다. 컷이 전반적으로 위로 갔고(시장 상승) 종목 값도 같이 올랐다면 상대 위치가 덜 바뀌어 상쇄된다. 🔴 **검증 안 함 — 가설로만 기록한다.**

### 정정 ③: 🔴 `STATE.md:62` "EV/EBITDA·PSR 미보유"가 낡았다
현재 기술: *"Q1 재료 현황: PER·PBR = 기보유 · **EV/EBITDA·PSR = 미보유(SEC 태그 조립 필요)**"*

**Cowork 실측**(`us_valuation`, `as_of=2026-08-14`, 5,820행):

| 축 | non-null 건수 |
|---|---|
| per | 2,067 |
| pbr | 3,279 |
| psr | **3,554** |
| ev_ebitda | **1,451** |

🔑 **넷 다 보유하고 있다.** STEP996 대량 적재(1,247→5,740행)의 부산물로 이미 채워졌는데 `STATE.md`가 안 따라왔다. 🔴 **`STATE.md:210` "Q1 착수 준비" 목록의 「EV/EBITDA·PSR SEC 태그 조립」은 이미 끝난 항목이므로 제거한다.**

---

## ⓪ 전제 점검

### ⓪-1. 직전 STEP 산출물 — 원문 인용 (검산 포함)

| 출처 | 인용 | 검산 |
|---|---|---|
| STEP 1031 | `capGateDecision` 고정 97% → 절대하한 85% + 전일 낙폭 상한 3%p. `compositionOk` 불변 | ✅ heartbeat 확인 |
| STEP 1031 | 프루닝 차단 = **모듈 상수 아닌 `pruneEnabled` 파라미터**, US 호출부에만 `false` 전달 — KR 무영향 | ✅ `lensPrecompute.ts:493·682` 확인 |
| STEP 1031 | 배포 Ready **11:25 UTC** · 크론 21:30 UTC → 여유 10시간 5분 | ✅ 21:30 − 11:25 = 10h05m |
| STEP 1031 | 833 테스트 11개 보존(기대값 1건만 변경 + 사유 주석) · 신규 6건 · **378/378 통과** | — |
| Cowork 실측 | `wouldPruneRows` **76** · 샘플 사유 = 「유니버스이탈」 다수 + 「계산실패(유니버스잔류)」 · **전부 `updatedAt=2026-07-30`** | ✅ 19일 묵은 행 |
| Cowork 실측 | `lens_scores` US **1,039행**(1031 백업 시점 1,036에서 +3) · 최종 갱신 08-14 21:37:06 UTC | ✅ |
| Cowork 실측 | `lens_cuts` KR `as_of=2026-08-13` **정상** — US 전환이 KR을 안 건드렸다 | ✅ |

### ⓪-1b. 🔴 기존 답 확인 (KNOWN_ANSWERS)
- 🔴 `docs/KNOWN_ANSWERS.md`에 「프루닝」·「삭제 상한」·「좀비 행」 항목이 있는지 먼저 확인
- 🔴 **이전발언 대조**: 프루닝을 켜거나 끈 전례를 `CHANGELOG.md`·`STEP_LEDGER.md`에서 검색. 🔴 **STEP833이 `canPrune`을 신설할 때 왜 4중 게이트를 걸었는지 원 근거를 읽는다** — 이번에 5번째 조건(상한)을 더하는 것이므로 기존 4개의 취지와 어긋나지 않아야 한다
- 🔴 **STEP1026의 「100행 이하」 기준이 어떤 근거로 세워졌는지** 원문 확인 — §1-2b 상수의 근거가 된다
- 🔴 이 칸이 비면 미완성이다

### ⓪-2. 이 STEP이 건드리는 슬롯
🔴 **슬롯 값은 안 바꾼다.** 바뀌는 건 `lens_scores`의 **행 수**다.

### ⓪-3. 걸리는 금지사항 (해당되는 것만)
- [x] 🔴 **삭제 전 백업이 선행이다** — §1-1. 백업 없이 삭제하면 되돌릴 수 없다
- [x] 🔴 **KR 무영향 확인** — `pruneEnabled` 기본값 `true`라 **KR은 원래부터 프루닝하고 있었다.** 이 STEP은 US 호출부 한 곳만 건드린다. 🔴 **KR 경로 diff 0을 명시적으로 확인**한다
- [x] 🔴 **`compositionOk`·커버리지 산식 불변** — 1031이 세운 것을 건드리지 않는다
- [x] 🔴 **`app/api/cron/revdcf/route.ts` diff 0** · `BUDGET_MS`·`maxDuration` 불변
- [x] 🔴 **크론 수동 실행 금지** — 오늘 밤 21:30 UTC가 첫 실행이다. 🔴 **`lens-scores`를 부르지 않는다**
- [x] 🔴 **833 테스트 + 1031 신규 테스트를 지우지 말 것** — 추가만 한다
- [x] `REVDCF_ENABLED` Production OFF 유지 · `data/us_symbols.json` · `.github/workflows/**` · `vercel.json` 수정 금지 · **KR 전면 동결**

### ⓪-3b. 🔴 결함의 소재 확정
- 이 STEP은 **프로덕션 코드를 바꾸고 데이터를 지운다.** 프로브가 아니다
- 🔴 지워진 행이 예상과 다르면 **산식 문제인지 유니버스 문제인지**를 가른다 — §1-1 백업이 그 대조군이다

### ⓪-4. 🔴 반증 조건 (오늘 밤 21:30 UTC 이후 확인)

| 관측 | 결론 | 다음 축 |
|---|---|---|
| `pruned=true` · `pruneBlockedByFlag=false` · `lens_scores` US **1,039 → 960~970행** | 🔑 **정상.** 좀비 정리 완료 | Q1으로 |
| 🔴 **삭제 행이 100을 넘어 상한에 걸림**(`pruneAborted=true`) | 🔑 **상한이 제 일을 했다.** 사고를 막은 것 | 왜 늘었는지 규명 |
| 🔴 **100을 넘었는데 그대로 지워졌다** | 🔴 **상한 배선 실패.** 즉시 크게 보고 | 복구 + 재설계 |
| `pruned=false`인데 이유 불명 | 배선이 실행 경로를 안 탐 | 배선 재확인 |
| 🔴 **컷이 다시 정지(`cutGateOk=false`)** | 1031과 별개 축 — 커버리지가 그날 나빴다는 뜻 | 게이트 재검토 |

🔴 **세 번째가 나오면 최우선으로 보고**한다.

### ⓪-5. 3번 생각
1. 🔴 **상한이 왜 필요한가** — 1031은 커버리지 게이트에 **낙폭 상한(3%p)**을 넣었다. *"서서히 나빠지는 것"과 "갑자기 무너지는 것"을 따로 막는다*는 논리였다. 프루닝에는 그 대칭짝이 **없다.** 4중 게이트는 전부 「계산이 잘 됐는가」만 보고 「몇 행을 지우는가」는 안 본다. 🔴 **유니버스 파일이 잘못 바뀌면 4중 게이트를 전부 통과한 채 수백 행이 지워질 수 있다.** 이건 832형 사고의 프루닝 판이다. **켜는 김에 같이 넣는다.**
2. 🔴 **표본** — 프루닝 규모 근거는 **2일 관측뿐**(63행 → 76행). 이미 하루 만에 **+20%** 움직였다. 🔴 **76이 상한이라는 근거가 없다.** 상한 100은 1026 기준을 그대로 쓰되, **그 근거가 2일 관측임을 문서에 명시**한다
3. **시점** — 🔴 `lens-scores`는 **21:30 UTC**다. 그 전에 배포가 끝나야 오늘 밤에 반영된다(1020 함정 · 1031은 10시간 여유로 성공)

---

## 1. 할 일

### 1-1. 🔴 삭제될 행 전수 백업 — 먼저, 예외 없이
지워질 76행을 **문서에 원문 그대로 기록**한다: `symbol`·`name`·`price`·7축 `*_state`·`updated_at`.
🔴 **삭제 후에는 복원할 원본이 없다.** DB 조회로 미리 뽑아 문서에 박아둔다.
🔴 각 행이 **왜** 지워지는지도 함께 적는다(「유니버스이탈」 / 「계산실패(유니버스잔류)」 — heartbeat의 `wouldPruneSample`이 쓰는 분류 그대로).
🔴 삭제 대상을 고르는 조건은 **프로덕션 코드가 실제로 쓰는 조건과 동일해야 한다** — 백업 쿼리를 따로 지어내면 대조군이 아니다.

### 1-2. 프루닝 활성화
`lib/lensPrecompute.ts:682` — US 호출부의 `pruneEnabled: false`를 켠다.
- 🔴 **파라미터 구조 자체는 남긴다.** 1031이 만든 opt-in 형태(기본 `true`, 호출부에서 명시)는 유효한 설계다 — 되돌릴 때 다시 `false`만 넣으면 된다
- 🔴 `pruneBlockedByFlag` 관측 필드는 **그대로 둔다**(이제 `false`가 정상값)
- 🔴 1031이 남긴 차단 주석은 **지우지 말고 "1032에서 해제됨"으로 갱신**한다 — 왜 한 번 막았었는지가 기록으로 남아야 한다

### 1-2b. 🔴 삭제 상한 신설 — 이 STEP의 핵심
`canPrune`이 참이어도 **지울 행 수가 상한을 넘으면 중단**한다.
```
PRUNE_MAX_ROWS = 100  // 1026 기준. 실측 63(08-13)·76(08-14) — 🔴 2일 관측이 근거다
```
- 🔴 `canPrune`의 **기존 4개 조건을 지우지 않는다.** 5번째로 더한다
- 🔴 상한에 걸리면 **삭제를 전혀 하지 않는다**(일부만 지우지 않는다 — 어느 76행을 남길지 고를 근거가 없다)
- 🔴 heartbeat에 `pruneAborted`(상한 초과로 중단) · `pruneRowsAttempted`(지우려던 수)를 실어 **관측 가능**하게 한다
- 🔴 상한에 걸리면 **Sentry error**를 낸다 — 833이 게이트 실패에 그랬던 것과 같은 급의 사건이다

### 1-3. 테스트 추가 (보존 + 확장)
- 🔴 **833의 11개 · 1031의 6개를 전부 보존**한다. 추가만 한다
- 🔴 새 케이스: 상한 **경계**(정확히 100 → 지운다 / 101 → 중단) · `pruneEnabled=false`일 때 상한과 무관하게 차단 · 4중 게이트 실패 시 상한과 무관하게 차단
- 🔴 **테스트를 고쳐서 통과시키지 말 것**

### 1-4. 🔴 정정·기록 3건 — §0-B 그대로
- **`docs/STATE.md:62`** — Q1 재료 현황을 실측으로 교체(per 2,067 · pbr 3,279 · psr 3,554 · ev_ebitda 1,451 / `as_of=2026-08-14` · 5,820행). 🔴 **`STATE.md:210` 「Q1 착수 준비」에서 「EV/EBITDA·PSR SEC 태그 조립」 항목 제거**(이미 완료)
- **`docs/probe_1031_gate_activation.md`** — 프루닝 63행 → **76행** 정정 + 1031 전환이 실측으로 성공했음을 §0-A 표 그대로 추가
- **W4 괴리 기록** — 예측 11.3% vs 실측 6.2%, 검산 표(08-13 106건 / 08-14 169건 / 차분 63건), 🔴 **상쇄 가설은 「미검증」으로 명시.** 🔴 **장은태 판정 = 「기록만, 규명 안 함」**을 함께 적는다. 기록 위치는 `REVDCF_SPEC.md` §10-J
- 🔴 **`STATE.md` Q1 행 갱신** — `lens_cuts` 19일 정지가 **해소**됐음을 반영(982·47번 항목이 「정지」를 전제로 쓰여 있다)

### 1-5. 값 불변 확인 (배포만으로는 아무것도 안 지워진다)
배포 직후:
- `lens_scores` US **1,039행 그대로** · `lens_cuts` US **08-14 그대로**
- `revdcf_results`·`us_valuation`·`us_sector_relative` 08-14 그대로
- 🔴 **KR 무영향**: `lens_cuts` KR `as_of=2026-08-13` 그대로 · KR 프루닝 동작 변화 없음
- 보호 파일 diff 0: `app/api/cron/revdcf/route.ts` · `data/us_symbols.json` · `vercel.json`
🔴 **배포만으로 행이 줄었으면 그 자체가 이상이다. 즉시 보고.**

### 1-6. 🔴 배포 시각 대조
🔴 배포 Ready 시각을 **21:30 UTC**와 대조해 문서에 적는다. 늦었으면 "오늘 밤은 구 코드, 내일 밤이 첫 실행"이라고 명시한다.

### 1-7. 산출물
`docs/probe_1032_prune_activation.md`
- 맨 앞에 **전환 내용 한 줄** + 배포 시각 vs 21:30 UTC
- 🔴 **1-1 백업 76행 전수**(요약 아님 — 전부)
- 1-2·1-2b 코드 diff · 상한 상수 근거(🔴 **2일 관측임을 명시**)
- 1-3 테스트 변경 내역(보존한 것 / 추가한 것)
- 1-4 정정 3건
- 🔴 **⓪-4 매트릭스를 그대로 싣는다**
- 🔴 3중 규칙 3종: 못 한 축 / 철회·정정 / 미측정

🔴 `docs/CRON_OBSERVABILITY.md`(정본) §5에 **`pruneAborted`·`pruneRowsAttempted`** 관측 항목 추가(게이트 9 · 정본 먼저).
🔴 `docs/KNOWN_ANSWERS.md`에 **「프루닝은 언제 몇 행을 지우는가」** 항목 추가(3줄 · 재검토 조건).

## 2. 커밋 전 게이트
- 게이트 7: git 미추적 파일 참조 검사
- 게이트 8: push 후 **Vercel 배포 성공 + verify 초록을 눈으로 확인**하기 전까지 미완료
- 게이트 9: 정본 먼저, 사본 나중
- `docs/CHANGELOG.md` 1032 항목 · `docs/STEP_LEDGER.md` 1032 등재 — 🔴 **"프루닝 활성화 승인(장은태, 2026-08-15)"** 명시

## 3. DoD
- [ ] ⓪-1b 기존 답 확인 — 🔴 **833의 `canPrune` 원 근거 + 1026의 100행 기준 근거를 읽었다**
- [ ] 🔴 **1-1 백업 완료 — 76행 전수**(삭제 전에, 예외 없이)
- [ ] 1-2 프루닝 활성화 · 파라미터 구조 보존 · 1031 주석 갱신
- [ ] 🔴 **1-2b 삭제 상한 신설** — 기존 4조건 보존 · 전량 중단 · `pruneAborted` 관측 · Sentry error
- [ ] 1-3 🔴 **833·1031 테스트 전부 보존** + 경계 케이스 추가
- [ ] 1-4 정정 3건(`STATE.md` 재료현황·착수준비목록·Q1행 · probe_1031 63→76 · W4 괴리 기록)
- [ ] 1-5 배포 직후 값 불변 + 🔴 **KR 무영향** 확인
- [ ] 🔴 **1-6 배포 시각 vs 21:30 UTC 대조**
- [ ] `CRON_OBSERVABILITY.md`·`KNOWN_ANSWERS.md`·`REVDCF_SPEC.md` §10-J 갱신(정본 먼저)
- [ ] 🔴 **상한 근거가 2일 관측임을 문서에 명시**
- [ ] tsc 0 · test 전량 통과
- [ ] Vercel 배포 성공 + verify 초록 **눈으로** 확인
- [ ] 마지막에: 못 한 축 / 철회·정정한 것 / 미측정으로 남은 것

🔴 **W4 괴리 원인 규명은 하지 않는다(장은태 판정: 기록만).**
🔴 **다음은 Q1이다** — 「기존 7렌즈를 수리할지 Q1~Q4 카드를 신설할지」 판정 자료(장은태 판정, 2026-08-15). 🔴 **곁가지 STEP을 새로 만들지 않는다.**
🔴 **오늘 밤 21:30 UTC가 첫 실행이다. 그 전에 배포가 끝나야 한다.**
