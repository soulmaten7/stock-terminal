# STEP 1086 — 🔴 **모든 기록 문서 전수 업데이트** (STEP1073~1085 산출 반영) (🟡 문서 전용 · 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 수동 실행 0)

> 🔴 **장은태 지시(2026-08-18)**: *"문서 정리 다 완료되면 한번 **모든 문서 업데이트** 진행하자 물론이것도 **3번규칙**으로"* · 재확인(2026-08-19): *"모든 **기록해야하는** 문서 업데이트"*
> 🔴 **문서 정리는 STEP1080(판정 대장 마감)에서 끝났다.** Cowork이 그 시점에 이 지시로 가지 않고 모델 제작(STEP1083~1085)으로 넘어간 것이 순서 오류다. 이 STEP이 그 밀린 것을 한다.
> 🔴 **판정하지 않는다. 사실 정정만 반영하고, 판정이 필요한 것은 판정 대기로 등재**한다(STEP1072가 쓴 방식 그대로 — 새 방식 창작 금지).
>
> **왜 지금인가 — 3번 규칙(2026-08-19):**
> **축 A(원문)** — 위 장은태 지시. `BUILD_SEQUENCE.md:346` *"정본 먼저, 표시본 나중 · 기존 파일 덮어쓰기, 새 파일 금지"* · `:304`(STEP1075) *"표시본은 머리 요약과 본문 둘 다"*.
> **축 B(실무·실측)** — STEP1072가 **08-19 오전**에 「미반영 정정 전수 수집 후 반영」을 이미 한 번 했다(커밋 `722778f`). 🔴 **그건 STEP1073 이전이다.** 그 뒤 **13개 STEP(1073~1085)** 의 산출이 전 문서에 안 퍼졌다.
> **축 C(반대 증거)** — ㉠**각 STEP이 이미 반영했다** — 🔴 **아니다. 각 STEP은 「자기가 건드린 문서」만 고쳤다**(STEP1076이 WHAT 파일을, 1084가 `LENS_COMPLETION_STANDARD`를). **옆 문서는 안 봤다** ㉡**1,130개 전수는 불가능하다** — 🔴 그래서 **§1에서 대상을 실측으로 가른다**(내 짐작으로 자르지 않는다) ㉢**고치면 이력이 사라진다** — 🔴 **취소선 보존**으로 막는다.

---

## ⓪ 전제 점검

### ⓪-0. 축 정의
| 축 | 실체 |
|---|---|
| **축 A — 원문** | 씨앗 = **STEP1073~1085의 실제 산출물 본문**(`CHANGELOG.md` 요약이 아니라 그 STEP이 고친 절 원문) + `git log`/`git show` |
| **축 B — 실무** | 🔴 **STEP1072가 쓴 정정 방식 그대로** — **취소선 + 정정 + 좌표 병기** · 정본 먼저·표시본 나중 · 기존 파일 덮어쓰기 |
| **축 C — 반대 증거** | ㉠**실은 판정이다**(어느 쪽이 맞는지 정해야 함 → 반영 말고 **등재**) ㉡**이미 반영돼 있다**(중복) ㉢**정정 근거가 뒤집혔다**(더 나중 STEP이 다시 뒤집음). **셋 다 확인** |

### ⓪-1. 🔴 씨앗 — STEP1073~1085가 확정한 것 전수 (이것이 전 문서에 퍼져야 한다)

| # | 씨앗(확정 사실) | 나온 STEP | 정본 좌표 |
|:--:|---|:--:|---|
| 1 | **판정 A 확정** — 정의3 최상위·정의2 신뢰층·정의1 **폐기** | 1073 | `BUILD_SEQUENCE` §6 표 A행·§1 |
| 2 | **판정 B 확정** — 「확정된 모델을 1개씩 완성한다」, **「1위」 미채택**(근거 3건) | 1073 | 같은 문서 §6-B |
| 3 | **C~F 재분류** — 특히 **E는 「7렌즈 거취」가 아니라 `CLAUDE.md:81` 규칙3 재사용 심사**였음 | 1073·1074 | 부록-3 「새 상태」 열 |
| 4 | **확정된 칸 넷** + **완성 승인 0** | 1073·1074 | §6-B-1 |
| 5 | **가-1(배수+역DCF)은 정본 W-2에 2026-08-16 이미 확정** / 가-2=정합 · 가-3=방향일치 · 다=진짜 미반영 | 1074 | §6-B-1 · `ROADMAP_V2` F-5 |
| 6 | **7렌즈 재사용 결과** — WHAT에 들어가는 건 **셋**(F스코어=재무건전성 · 모멘텀+기술=최근변화), 나머지 셋은 관문①에서 걸림 | 1073·1076 | §6-B-1 · WHAT 「마」 |
| 7 | **판정 대장 §6-C 74행** 신설·마감(🟢43·🟡19·🔴12) + **§6-C-미확인 0건** | 1079·1080·1081·1082 | `BUILD_SEQUENCE` §6-C |
| 8 | **`AUDIT_904_OPEN_ITEMS` #17·#37·#43 = STEP919로 이미 승인됨** | 1079 | 해당 문서 |
| 9 | **08-16 조인 결함 재발 없음** — 배제율 **30.5% → 0.47%**, 단 **원인 코드 그대로** | 1078 | `probe_1017` §6 |
| 10 | **`lens_scores` 41종목 감소 = 못 찾음**(과거 스냅샷 미저장) | 1078 | 같음 |
| 11 | **섹터 11일 격차** — `us_sector_wide` `max(as_of)=08-08` vs `us_valuation` `08-18` | 1081 | §6-C #65 |
| 12 | **`avg(r1,r3)`(창작 산식) 완전 제거** · `short`/`long` = 죽은 필드였음 · **판정 변화 0건** | 1083 | `CHANGELOG` STEP1083 |
| 13 | **`pos52` 최소표본 가드**(`win.length>=252`) · **FF/Carhart 문구 정정** · **집계 레이어 공개 문구** | 1083 | 같음 |
| 14 | **Jegadeesh–Titman(1993) 원전 확보** + 차이 3건(decile vs percentile · skip 방식 · 16조합 vs 단일 12-1) | 1084 | `data/sources/academic/` · `LENS_COMPLETION_STANDARD` |
| 15 | 🔴 **`LENS_COMPLETION_STANDARD` 상단표가 모멘텀·기술을 ALL-✅(9/9)로 오표기하던 것 정정** | 1084 | 해당 문서 |
| 16 | **최근변화 칸 = 6/9**(「완료」 아님) · ③⑦은 **검산 인프라 미비가 뿌리** · ⑨는 크론 대기 | 1083·1084 | 같음 |
| 17 | **`adjUsed` 저장 컬럼 없음 — 못 쟀다** | 1084 | 같음 |
| 18 | (STEP1085 결과 — 실행 시점에 확인해 **씨앗에 추가**한다) | 1085 | `§7-4 #7-부록` |

🔴 **씨앗 18건 밖에 더 있는지 `git log --oneline` STEP1073~1085 구간으로 역확인**한다. 있으면 **추가**하고 개수를 보고한다.

---

## §1. 🔴 대상 문서 가르기 (범위를 짐작으로 자르지 않는다)

`docs/` 전수(md 1,130 + html 5) + `CLAUDE.md`를 **다음 기준으로 분류**한다:

| 갈래 | 뜻 | 처리 |
|:--:|---|---|
| **㉠ 살아 있는 기록 문서** | **현재 상태**를 서술한다(정본·현황·명세·카탈로그·로드맵·표시본) | 🔴 **대상** |
| **㉡ 시점 이력** | 그때 무엇을 했는지의 기록(`STEP_*_COMMAND.md` 893 · `step_orders/` · `probe_*` 62) | 🔴 **대상 아님 — 고치지 않는다**(이력을 고치면 이력이 아니다) |
| **㉢ 죽은 문서** | KR 동결·타국 파킹 등으로 **적용 대상이 아님** | 🔴 **고치지 않되, 죽었다는 표기가 있는지 확인**. 없으면 **판정 대기로 등재**(죽음 선언은 판정이다) |
| **㉣ 판별 불가** | 위 셋으로 못 가르겠다 | 🔴 **"판별 불가"로 나열**. 짐작으로 ㉡·㉢에 넣지 않는다 |

🔴 **㉡·㉢으로 보낸 것은 개수와 사유를 보고**한다. 🔴 **"낡아 보인다"만으로 ㉢에 넣지 않는다**(STEP1080이 세운 규칙 그대로 — 근거 좌표 필요).
🔴 **㉠의 최종 개수를 보고**한다. 그것이 이 STEP의 실제 작업 대상이다.

---

## §2. 씨앗 × 대상 문서 전수 대조

각 씨앗마다 `grep -rn` 으로 **어긋난 서술이 남아 있는 문서**를 찾는다. 검색어 예(씨앗별로 실행 측이 확장):
```
"1위를 제대로" | "미판정" | "장은태 몫" | "판정 대기"        # 씨앗1·2·7
"7렌즈 거취" | "7렌즈 처분"                                  # 씨앗3
"avg(r1,r3)" | "단기신호" | "shortState" | "r1"              # 씨앗12
"9/9" | "ALL-✅" | "3중 검증 완결"                           # 씨앗15·16
"32.3%" | "71.9%"                                            # (STEP1077분, 함께)
"us_sector_wide" | "섹터 신선도"                             # 씨앗11
```
🔴 **검색어를 이 목록으로 한정하지 않는다** — 씨앗 18건 각각에 맞는 말을 실행 측이 만든다. **만든 검색어를 전부 보고**한다(재현 가능하게).

각 히트를 **셋으로 가른다**:
| 갈래 | 처리 |
|:--:|---|
| **반영(사실 정정)** | 🔴 취소선 + 정정 + 좌표. **어느 쪽이 맞는지 이미 정해진 것만** |
| **등재(판정 필요)** | 🔴 고치지 말고 `BUILD_SEQUENCE` §6-C 대장에 **행 추가**(통번호 78~). **중복이면 좌표 병기만** |
| **해당 없음** | 이미 반영됨·문맥이 다름 — 사유 한 줄 |

---

## §3. 표시본 (🔴 정본 먼저 끝난 뒤)
`docs/*.html` 5개 — 🔴 **머리 요약과 본문 둘 다**(`BUILD_SEQUENCE.md:304`).
🔴 **`~/Downloads/` 무접촉**(사용자 파일).

## §4. 🔴 상한과 남기기
한 STEP에 다 못 해도 된다. **한 것만 반영하고, 남은 것은 전부 나열**한다 — 씨앗별·문서별로. 🔴 **다 한 척하지 않는다.**

---

## §5. 검증 3
① **재현 가능** — §2에서 쓴 **검색어를 전부 기록**했는지. 다른 사람이 같은 검색으로 같은 히트를 얻을 수 있는지.
② **반대 증거 셋** — 히트마다 ㉠판정인가 ㉡이미반영인가 ㉢근거가 뒤집혔는가를 **실제로 물었는지**. 각 몇 건인지 보고.
③ **역방향** — 씨앗에서 출발(정방향) + **대상 문서에서 출발해 "이 문서가 지금 사실과 다른 말을 하나"를 훑기**(역방향). 🔴 **역방향에서만 나온 신규 발견이 있으면 그것도 처리**하고 개수를 보고.

## §6. 검수 3
① **판정 안 했는가** — 「등재」로 보낸 것을 고치지 않았는지 · ㉢(죽은 문서) 선언을 **판정 없이** 하지 않았는지 · 완성 순서·`Q1_ENABLED`·`REVDCF_ENABLED` 무접촉.
② **삭제·창작 안 했는가** — 취소선 보존 · 새 파일 0 · 새 방식 0 · 지어낸 정정 0.
③ **범위 지켰는가** — `docs/` 밖 무접촉 · `~/Downloads/` 무접촉 · `git status` 신규 = `docs/step_orders/STEP1086.md` 하나.

## §7. 커밋 + 보고
- 🔴 **`main` 직접 push.** 브랜치·PR·worktree 금지.
- 커밋 메시지: `docs: STEP1086 모든 기록 문서 전수 업데이트 — STEP1073~1085 산출 반영`

```
[STEP1086 결과]
⓪ 씨앗: 18건 + 추가 N건 (git log 역확인) = 총 M건
① 문서 분류: ㉠살아있는기록 a / ㉡시점이력 b / ㉢죽은문서 c / ㉣판별불가 d  (합 = 1,135)
   - ㉢으로 보낸 것: 각 근거 좌표 (없으면 판정 대기 등재)
   - ㉣판별불가: 전부 나열
② 씨앗×문서 대조: 검색어 전수 나열 / 히트 N건
   - 반영(사실정정) a건 / 등재(판정) b건(대장 통번호 78~) / 해당없음 c건
③ 역방향 신규 발견: N건 (내용)
④ 표시본 5개: 머리+본문 둘 다
⑤ 대장 최종: 🟢/🟡/🔴 · 총 N행 (STEP1082 74행 → 변화)
⑥ 검증 3 — 1)재현가능(검색어 기록) 2)반대증거 셋 각 N건 3)역방향
⑦ 검수 3 — 1)판정안함 2)삭제·창작안함 3)범위지킴
🔴 못 한 것: (씨앗별·문서별로 전부 나열. 없으면 "없음")
🔴 열지 않은 것: (있으면 나열)
커밋: <해시>
```
