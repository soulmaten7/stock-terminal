# STEP 1071 — 초안 26개를 **현재본에 업데이트**하고, 반영 끝난 중복을 **삭제**하고, **제자리를 고정**한다 (🟠 문서 변경 + 파일 삭제 · 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 수동 실행 0)

> 🔴 **이 STEP은 파일을 삭제한다. 되돌릴 수 없다. §2-8의 안전 조건 넷을 전부 통과한 뒤에만 지운다.**
> **목적 셋** — ㉠`_to_delete` 26개에서 **현재본에 없는 내용을 찾아 현재본에 업데이트** ㉡반영이 끝난 **중복 사본을 삭제** ㉢**제자리를 문서에 고정**해 다시 흩어지지 않게.
> 🔴 **방향이 바뀌었다(2026-08-18 장은태).** 앞선 초안은 *"지워도 되는지 판정 재료"*였는데 그것부터 규칙 위반이었다.
> `/preferences.md`(2026-08-16 장은태): *"문서를 고칠 때는 **기존 파일을 업데이트**한다 — **새 파일·새 버전을 만들지 않는다.** **중복 파일이 쌓이는 것이 문제.**"*
> 🔴 **그 뜻은 「지우자」가 아니라 「새 파일을 안 만들면 지울 것도 안 생긴다」였고, `_to_delete/` 폴더 자체가 그것을 청소로 오독한 산물이다.** 그러니 물음은 *"지워도 되나"*가 아니라 ***"살릴 것을 어떻게 업데이트해 넣나, 그다음 중복을 어떻게 없애나"***다.
> 🔴 **이전 버전이 필요하면 git에서 꺼낸다** — `git show <해시>:<경로>`. 2026-08-18 시연 확인: `docs/roadmap_v2.html`의 덮어쓰기 전 버전(91,937 bytes)이 그대로 나온다. **사본을 남길 이유가 없다.**
> 🔴 **3번 규칙 전면 적용** — 축 A(원문) · 축 B(실무) · 축 C(반대 증거) · 검증 3 · 검수 3.

---

## ⓪ 전제 점검

### ⓪-0. 🔴 3번 규칙 축 정의 (먼저 고정)

| 축 | 실체 |
|---|---|
| **축 A — 원문** | **초안 26개의 텍스트 그 자체**(요약·기억 아님) + **현재본 집합**(§2-0) + `git log`/`git show`로 본 변천 |
| **축 B — 실무** | 🔴 **이 저장소가 이미 쓴 방법.** STEP1056(정본↔표시본 절 단위 대조 · *"기존 파일 덮어쓰기, 새 파일 금지"*, `BUILD_SEQUENCE.md:346`) · STEP1069(4분류 + 문장 단위 diff + 양방향 나열) · **취소선+정정+좌표**(STEP1060·1064·1069). **새 방법을 만들지 않는다** |
| **축 C — 반대 증거** | *"초안엔 살릴 게 없다"*를 무너뜨릴 것 — ㉠**인접 버전에서 사라진 문단** ㉡**현재본 어디에도 없는 고유 문장** ㉢**제목이 다른 파일**. **셋 다 찾는다** |

### ⓪-1. 직전 산출물 — 원문 인용

| 출처 | 인용(그대로) |
|---|---|
| `/preferences.md`(2026-08-16 장은태) | *"문서를 고칠 때는 **기존 파일을 업데이트**한다 — 새 파일·새 버전을 만들지 않는다. **중복 파일이 쌓이는 것이 문제.** 특히 로드맵(`roadmap_v2_2.html`)과 층별정리가 그 대상"* |
| `docs/BUILD_SEQUENCE.md:346` | *"STEP1056의 표시본 재생성 원칙(**「정본 먼저, 표시본 나중」 · 「기존 파일 덮어쓰기, 새 파일 금지」**) 준수"* |
| `docs/CHANGELOG.md:286`(STEP1056 §2-6) | *"`Downloads/roadmap_v2_2.html`을 **기존 파일째 덮어써** 정본에 맞춤(**새 파일 생성 금지**)"* |
| `CLAUDE.md:631` | *"⛔ **현재상태 기록용 새 파일 신설 금지.** 새 세션은 `STATE → SYSTEM_MAP → 작업별 PLAYBOOK` 순으로 읽는다"* |
| STEP1070 커밋 `d414037` | 🔴 *"메인 폴더 로컬에만 있던 gitignore 잔재 파일(**`scripts/_probe_B_flows.ts`**, 7/28일자) 충돌은 이 STEP과 무관, **손대지 않음**"* — **미보존 작업을 담고 있는지 미측정.** §2-10에서 처리 |
| `~/Downloads/tombstone.html` | *"**같은 내용을 두 곳에 두면 한쪽이 반드시 뒤처지고, 그때 어느 쪽이 정본인지 알 수 없게 됩니다.**"* |
| `docs/BUILD_SEQUENCE.md` §8 #1 | *"**한 계층만 보고 「후보 소진」 선언**"* — 🔴 **「마지막 버전만 보면 된다」가 이 STEP의 같은 함정** |

### ⓪-1a. 🔴 로드맵 원문 대조 (여섯 층 전부)

| 층 | 관계 |
|---|---|
| **WHY** | 조건 1 *"정확해야 한다"* — 🔴 **사라진 판정·근거가 있다면 지금 문서가 그만큼 부정확하다** |
| **HOW** | H-7 *"**결측을 채우고 실측인 척** 🔴 절대 금지"* → 🔴 **반대 방향도 같은 종류** — 있던 것을 잃고 없던 척하는 것 |
| **WHAT** | 초안 전부가 WHAT/로드맵 계열 — **잃은 것을 되살리되 새 결론을 만들지 않는다** |
| **만드는 절차** | `BUILD_SEQUENCE.md` §7-부록(STEP1069·1070 기록 자리) → §2-12 산출 위치 |
| **관문·순위** | 무관 |
| **완성의 정의** | C-1 #6 *"**주장 정합** — 공개 문서와 무모순"* |

### ⓪-1b. 기존 답 확인 — 🔴 `ls docs/` 전수
🔴 **STEP1056·1069·1070 전문을 먼저 읽는다**(축 B). `docs/probe_1044_roadmap_audit.md`가 「사라진 절」을 다룬 적이 있는지도 확인.

### ⓪-2. 건드리는 슬롯 — **해당 없음**

### ⓪-3. 걸리는 금지사항
- [ ] **코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 수동 실행 0 · 외부 취득 0**
- [ ] 🔴 **새 md/html 문서를 만들지 않는다**(`/preferences.md`). 원자료(json/txt)만 예외
- [ ] 🔴 **`docs/` 안의 파일을 삭제하지 않는다** — 저장소 파일은 git이 관리한다. 삭제 대상은 **`~/Downloads/` 안의 중복 사본만**
- [ ] 🔴 **`~/Downloads/`의 「현재본 5개」는 이번에 지우지 않는다** — 판정 대기(§2-8 ㉣)
- [ ] **KR 전면 동결** · `REVDCF_ENABLED` OFF · `Q1_ENABLED` 무접촉
- [ ] 🔴 **브랜치·PR·worktree 만들지 않는다**(STEP1070 신설 조항)

### ⓪-3b. 🔴 이 STEP의 함정
🔴 **「마지막 버전만 보면 된다」로 시작하면 안 된다**(§8 #1). §2-2가 0건일 때만 줄인다.
🔴 **「반영」이 「새 절 추가」로 흘러 문서가 부풀면 안 된다** — 살릴 것은 **원문 그대로**, 들어갈 자리는 **기존 절 안**.
🔴 **삭제를 서두르면 안 된다.** §2-8 조건 넷 전부 통과 후.
🔴 **거른 건수를 숨기면 안 된다** — 원시/잔여 둘 다 적는다.
🔴 **좌표 없는 문장 금지.**

### ⓪-4. 🔴 반증 조건

**전제 ①** — 버전이 올라가며 **추가만 됐다**(삭제 없음).
- 🔴 **미확인.** 크기 증가는 삭제가 없었다는 증거가 아니다.
- **다음 행동**: §2-2 인접 diff. **0건이면** §2-3 대상을 각 계보 마지막 + C·D로 줄인다(근거 기록). **1건 이상이면 26개 전부.**

**전제 ②** — 「현재본」은 `docs/`의 HTML들이다.
- 🔴 **틀렸다.** **md 정본을 반드시 포함**한다(`ROADMAP_V2.md`·`BUILD_SEQUENCE.md`·`LENS_DISPOSITION_2026-08-08.md` 등). HTML에서 빠졌어도 md에 있으면 **잃은 것이 아니다.**
- 🔴 **여기서 빠뜨리면 「사라졌다」가 과다 집계되고, 그만큼 §2-6에서 문서를 헛되이 부풀린다.**

**전제 ③** — `dup_12.html`은 현재본과 같으니 볼 필요 없다.
- ✅ md5 일치(`ff2ce5d5d0fe8016566ed4e18c9e1741` = `~/Downloads/model_list_30_9.html`).
- 🔴 단 `docs/model_list_30_9.html`(62,199)은 STEP1069에서 재료 5건이 더해져 **다르다.** 제외하되 **근거를 적는다.**

**전제 ④** — 26개가 전부 같은 계보다.
- 🔴 **틀렸다.** `model_list_30_4.html` = *"공통 모델 22 — 채택·탈락 판정"* · `_30_5.html` = *"쓰는 모델 19 · 탈락 11"*. **별개 문서일 수 있다**(§2-5).

**전제 ⑤** 🔴 **삭제 안전** — 반영이 끝나면 지워도 이력이 남는다.
- 근거: 저장소 파일은 git이 전 버전을 갖는다(2026-08-18 `git show 5668061:docs/roadmap_v2.html` = 91,937 bytes 시연).
- 🔴 **틀렸다면**(= 반영 누락이 있으면) → **그 내용은 영원히 사라진다.** 그래서 §2-7 반영 검증이 §2-8보다 먼저다.

### ⓪-5. 🔴 3번 생각
1. **범위** — 곁가지 아님. **판정·근거가 초안에만 남아 있다면 지금 문서가 그만큼 부정확하다.**
2. **표본** — 🔴 **26개 전수.** 줄이는 것은 전제① 확인 후에만.
   🔴 **전수의 조건** — ㉠정방향(초안→현재본) ㉡**역방향**(인접 diff: 사라진 것) 둘 다.
3. **시점** — mtime·내용 안 날짜·`_to_delete` 폴더 mtime 전부 기록. 🔴 **삭제 전후 파일 목록을 둘 다 남긴다.**

---

## 1. 배경 — 26개 실측 (2026-08-18 · `~/Downloads/_to_delete/2026-08-16_roadmap_drafts/`)

**A. 로드맵 계보 9개**(`<title>` = *"Trillion 로드맵 V2 — 모델을 만드는 과정"*)
`roadmap_v2` 15,394 → `roadmap_v2_1` 27,536 → `roadmap_v2_2_1` 53,113 → `roadmap_v2_2_1__2` 54,749 → `dup_1` 55,750 → `dup_2` 58,693 → `dup_3` 62,229 → `dup_4` 64,152 → **`dup_11` 64,421**
현재본: `~/Downloads/roadmap_v2_2.html` 65,755 · **`docs/roadmap_v2.html` 130,663**

**B. model_list 계보 14개**(`<title>` 비어 있음)
`model_list_30` 15,855 · `_30_1` 15,917 · `_30_2` 15,285 · `_30_3` 17,147 · `_30_6` 18,233 · `_30_7` 21,900 · `_30_8` 25,343 · `_30_9` **26,851** · `dup_5` 31,460 · `dup_6` 36,318 · `dup_7` 46,505 · `dup_8` 48,195 · `dup_9` 50,631 · **`dup_10` 54,003**
🔴 **`_to_delete` 안의 `model_list_30_9.html`(26,851)과 밖의 동명 파일(57,628)은 다른 파일이다**

**C. 제목이 다른 2개** 🔴 `model_list_30_4` 18,527(*"공통 모델 22 — 채택·탈락 판정"*) · `_30_5` 18,160(*"쓰는 모델 19 · 탈락 11"*)

**D. 현재본 사본 1개** `dup_12` 57,628 — md5 일치

**확정 안 됨** 🔴 — 사라진 내용 유무 · 고유 문장 유무 · 제목 다른 둘의 정체 · `_probe_B_flows.ts`의 내용

---

## 2. 할 일

### 2-0. 「현재본 집합」 확정 (⓪-4②)
최소: `docs/model_list_30_9.html` · `docs/roadmap_v2.html` · `docs/build_sequence.html` · `docs/data_source_catalog.html` · `docs/tombstone.html` · **`docs/ROADMAP_V2.md`** · **`docs/BUILD_SEQUENCE.md`** · **`docs/LENS_DISPOSITION_2026-08-08.md`** · `~/Downloads/model_list_30_9.html` · `~/Downloads/roadmap_v2_2.html`
🔴 **md 정본 반드시 포함.** 더 넣을 것이 있으면 넣고 근거를 적는다. 목록을 산출물에 싣는다.

### 2-1. 26개 전량 텍스트 추출 + 계보·순서 확정
문자 수 · `<title>` · 내용 안 날짜 · mtime 표로. 🔴 **크기 순 = 시간 순으로 가정하지 않는다.** 확정 못 하면 「순서 미확정」 명시 후 크기 순으로 돌리고 한계를 적는다.

### 2-2. 🔴 인접 diff — 「사라진 문단」 (축 C㉠ · ⓪-4①)
계보별 인접 쌍(A 8쌍 · B 13쌍) diff. **앞 버전에 있다가 뒤에서 사라진 문단**을 전부 뽑고 **건수·목록을 산출물에 싣는다.**
**0건 → §2-3 축소 가능(근거 기록). 1건 이상 → 26개 전부.**

### 2-3. 고유 문장 추출 (축 C㉡)
§2-0 현재본 집합 **어디에도 없는 문장**을 뽑는다. 문장 분할 → 공백·기호 정규화 → 포함 검사. 🔴 `dup_12` 제외(근거: md5).

### 2-4. 🔴 거르는 기준 — **먼저 고정하고 돌린다**
길이 **30자 이상** · 숫자·고유명사·좌표(`파일:행`)·판정어(*확정·판정·탈락·보류*) 중 **하나 이상** · 현재본에 **유사 문장이 있으면 제외**(판단 근거 한 줄씩).
🔴 **원시 건수와 잔여 건수를 둘 다 적는다.**

### 2-5. 제목 다른 둘 (축 C㉢ · ⓪-4④)
㉠계보의 한 버전인가 별개 문서인가 ㉡「22 → 19 + 탈락 11」 숫자 변천이 현재 어디에 있나 ㉢🔴 **현재 WHAT은 「탈락 3」인데 여기는 「탈락 11」** — **그 8개의 행방**을 현재본에서 찾는다(`model_list_30_9.html`의 *"겹침이 부족해 앞서 제외한 8개"* 절이 대응하면 「추적됨」으로 적는다).

### 2-6. 🔴 **반영** — 현재본에 업데이트 (이 STEP의 본체)
§2-4 잔여 항목을 **현재본에 넣는다.**
🔴 **원칙 넷**:
1. **새 파일·새 절 신설 최소화** — 들어갈 자리는 **기존 절 안**. 새 절이 꼭 필요하면 **근거를 적는다**
2. **원문 그대로** 옮긴다. 요약·재서술 금지
3. **어느 초안 어느 자리에서 왔는지 좌표를 단다**(예: `_to_delete/…/dup_7.html §X`)
4. 🔴 **현재본과 충돌하면 양쪽 병기 + 「어느 쪽이 맞는지 미판정」.** 고르지 않는다 — **판정을 만들지 않는다**
🔴 **md 정본에 넣을지 HTML 표시본에 넣을지**는 그 내용의 성격을 따른다. *"정본 먼저, 표시본 나중"*(축 B).

### 2-7. 🔴 **반영 검증** — §2-8보다 먼저
- **찾은 항목 수 = 반영한 항목 수 + 명시적으로 「반영 안 함」으로 남긴 수**. 🔴 **셋을 숫자로 대조해 적는다**
- 「반영 안 함」이 있으면 **각각 사유 한 줄**(예: 이미 md에 있음·중복·판정 대기)
- 🔴 **미반영이 「사유 없이」 하나라도 남으면 §2-8로 넘어가지 않는다**

### 2-8. 🔴 **삭제** — 안전 조건 넷을 전부 통과한 뒤에만
**조건**
㉠ §2-7 검증 통과(사유 없는 미반영 0건)
㉡ 🔴 **반영분이 `main`에 push되고 CI 그린 확인된 뒤**(게이트 8) — git이 이력을 가진 뒤에만 지운다
㉢ 삭제 대상 **전체 목록(경로·크기·md5)을 산출물에 먼저 기록**
㉣ 🔴 **대상은 `~/Downloads/_to_delete/` 폴더 전체(26개)뿐이다.** `docs/` 안 파일과 `~/Downloads/`의 **현재본 5개**(`model_list_30_9.html`·`roadmap_v2_2.html`·`build_sequence.html`·`data_source_catalog.html`·`tombstone.html`)는 **지우지 않는다** — 후자는 열람용으로 쓰이는지 미확인이라 **판정 대기**로 남긴다(§2-9에 물음으로 기록)

**실행**: 조건 넷 통과 시 `~/Downloads/_to_delete/` 삭제. 🔴 **한 번에 하나씩 확인하며 지우지 말고, 목록을 낸 뒤 폴더째 지운다**(부분 삭제가 더 위험하다).
🔴 **삭제 전후 `ls -la ~/Downloads/` 출력을 둘 다 산출물에 남긴다.**

### 2-9. 🔴 **제자리 고정** — 다시 흩어지지 않게
`docs/STATE.md`의 「어디를 보면 되는가」(STEP1070 신설)에 **이어서** 적는다. 🔴 **새 문서 만들지 않는다.**
- **문서를 고칠 때**: 저장소 `docs/`의 **그 파일을 덮어쓴다.** 사본을 만들지 않는다
- **이전 버전이 필요하면**: `git log --oneline -- <경로>` → `git show <해시>:<경로>` 🔴 **명령 두 줄을 그대로 적는다**
- **표시본(html)**: `docs/`에 둔다. `~/Downloads/`는 **열람용 사본**이며 어긋나면 저장소가 맞다
- 🔴 **판정 대기로 남길 물음 하나**: *"`~/Downloads/`의 현재본 5개를 계속 둘 것인가"* — 저장소에 있으므로 중복이지만 브라우저 열람에 쓰일 수 있다. **정하지 않는다**

### 2-10. `scripts/_probe_B_flows.ts` 처리 (STEP1070 이월)
🔴 **먼저 내용을 확인한다** — 미보존 작업(다른 곳에 없는 코드·결과)을 담고 있는가.
- **담고 있으면** → 🔴 **지우지 말고** 그 사실과 내용 요지를 산출물에 기록하고 **판정 대기**로 남긴다
- **안 담고 있으면**(이미 커밋된 것의 사본이거나 일회성 조사 잔재) → **삭제**하고 근거를 적는다
- 🔴 **판단 못 하면 지우지 않는다.**

### 2-11. 검증 3 · 검수 3
**검증 3** — ① 축 A: 26개 **원문 텍스트** 대조 ② 축 B: STEP1056·1069 방법 재사용 + *"기존 파일 덮어쓰기"* 준수 ③ 축 C: ㉠사라진 문단 ㉡고유 문장 ㉢제목 다른 둘 — **각각 건수**(0건이면 0건이라 적는다)
**검수 3** — ① 🔴 **§2-4 원시/잔여 건수**와 기준 ② 🔴 **§2-7 삼자 대조 숫자**(찾은 = 반영 + 사유 있는 미반영) ③ 🔴 **삭제가 §2-8 조건 넷을 전부 통과한 뒤였나** + 삭제 전후 목록
🔴 **못 거친 축은 「못 했다」고 명시.**

### 2-12. 산출물 — 🔴 새 md/html 문서 0
- **반영분** — 현재본 파일들(§2-6). 취소선·좌표 병기
- `docs/BUILD_SEQUENCE.md` **§7-부록에 이어** 기록 — §2-0 현재본 목록 · §2-2 사라진 문단 · §2-4 원시/잔여 · §2-5 · §2-7 삼자 대조 · §2-8 삭제 목록과 전후 상태 · §2-10 · §2-11
- `docs/STATE.md` 「어디를 보면 되는가」 확장(§2-9)
- 원자료가 크면 `docs/probe_1071_to_delete_diff.json` — **원자료만**
- `docs/CHANGELOG.md` · `docs/STEP_LEDGER.md` 등재

---

## 3. 🔴 하지 말 것
- 🔴 **§2-7 전에 지우지 않는다.** 조건 넷 미통과면 삭제 없이 보고하고 끝낸다.
- 🔴 **`docs/` 안 파일을 지우지 않는다.** `~/Downloads/`의 **현재본 5개도 지우지 않는다**(판정 대기).
- 🔴 **판정을 만들지 않는다.** 충돌은 양쪽 병기 + 미판정.
- 🔴 **새 md/html 문서를 만들지 않는다.** 새 절도 근거 없이 만들지 않는다.
- 🔴 **요약·재서술로 옮기지 않는다.** 원문 그대로 + 출처 좌표.
- 🔴 **「마지막 버전만」으로 시작하지 않는다** — §2-2가 0건일 때만 줄인다.
- 🔴 **md 정본을 현재본 집합에서 빼지 않는다** — 빼면 과다 집계되고 문서가 헛되이 부푼다.
- 🔴 **거른 건수를 숨기지 않는다.**
- 🔴 **`_probe_B_flows.ts`를 판단 없이 지우지 않는다.**
- 🔴 **브랜치·PR·worktree 만들지 않는다** · **코드·화면·DB 무접촉** · **KR 무접촉** · **외부 취득 0**.

## 4. 커밋 전 게이트
- 게이트 7: git 미추적 파일 참조 검사
- 게이트 8: push 후 **GitHub Actions verify 초록 + Vercel 배포 성공을 눈으로 확인** — 🔴 **§2-8 삭제는 이 게이트 통과 후**
- 게이트 9: 새 테이블·크론·관측 필드 없음 → 해당 없음
- `docs/CHANGELOG.md` 항목 · `docs/STEP_LEDGER.md` 등재
- 🔴 **브랜치 규약 + worktree 금지(STEP1070)** — `main` 직접 push
- 🔴 **STEP1070이 `.claude/settings.json`으로 worktree 자동 격리를 껐다. 이 STEP이 그 첫 검증이다** — worktree가 생기면 보고한다

## 5. DoD
- [ ] ⓪-0 축 고정 · ⓪-1b `ls docs/` 전수 + **STEP1056·1069·1070 전문 선독**
- [ ] 🔴 2-0 현재본 집합 — **md 정본 포함** · 목록 기록
- [ ] 2-1 26개 추출 + **실제 순서 확정**(못 하면 명시)
- [ ] 🔴 2-2 인접 diff — **사라진 문단 건수·목록** · 축소했다면 근거
- [ ] 2-3 고유 문장 — `dup_12` 제외 근거(md5)
- [ ] 🔴 2-4 **기준 먼저 고정** + **원시/잔여 건수 둘 다**
- [ ] 🔴 2-5 제목 다른 둘 + **「탈락 11 → 3」의 8개 행방**
- [ ] 🔴 **2-6 반영** — 원문 그대로 · 출처 좌표 · 충돌은 양쪽 병기 · **새 절 신설 시 근거**
- [ ] 🔴 **2-7 반영 검증 삼자 대조**(찾은 = 반영 + 사유 있는 미반영) — 사유 없는 미반영 **0건**
- [ ] 🔴 **2-8 삭제** — 조건 ㉠㉡㉢㉣ 전부 통과 후 · **목록 먼저 기록** · **전후 `ls` 출력** · 🔴 **현재본 5개·`docs/` 무접촉**
- [ ] 🔴 2-9 `STATE.md` 「어디를 보면 되는가」 확장 — **git 두 줄 명령 포함** · 현재본 5개 물음을 판정 대기로 기록
- [ ] 2-10 `_probe_B_flows.ts` — 내용 확인 후 처리(판단 못 하면 **지우지 않음**)
- [ ] 🔴 2-11 **검증 3 · 검수 3** 명시
- [ ] 🔴 **worktree 미생성 확인**
- [ ] 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 수동 실행 0 · 외부 취득 0 · tsc 0 · test 전량 통과
- [ ] 마지막에: **못 한 축 / 철회·정정한 것 / 미측정으로 남은 것**

🔴 **삭제는 이 STEP이 한다 — 단 §2-8 조건 넷을 전부 통과한 뒤에만. 조건 미통과면 지우지 않고 보고한다. 판정(현재본 5개 처분)은 장은태.**
