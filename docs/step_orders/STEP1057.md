# STEP 1057 — 렌즈 전수감사 ③ 저변동성 + 앞선 감사 결함 11건 생존 확인 (🔵 읽기 전용 · 코드 diff 0 · DB 쓰기 0 · 화면 변경 0)

> 🔴 **감사 순서는 이미 정해져 있다.** `LENS_AUDIT_02_MOMENTUM_2026-08-07.md` 머리 — *"등급이 셋만 `strong`이다(모멘텀·저변동·퀄리티). **가장 센 주장부터 본다** — 틀렸을 때 피해가 가장 크므로."* ①밸류·②모멘텀이 2026-08-07에 끝났고 **③부터 열흘간 멈춰 있다.**
> 🔴 **이 STEP은 판정하지 않는다.** 결함을 실측으로 뜯어 적는 데까지다. **판정 = 장은태.**
> 🔵 읽기 전용 — `lib/**`·`app/**`·`components/**` diff 0 · DB 쓰기 0 · 화면 변경 0 · 플래그 무접촉.

---

## ⓪ 전제 점검

### ⓪-1. 직전 산출물 — 원문 인용

| 출처 | 인용(그대로) |
|---|---|
| `docs/LENS_DISPOSITION_2026-08-08.md` 머리 | *"`CLAUDE.md` 규칙 3 = ① 그 렌즈가 확정된 질문에 실제로 답하는지 확인하고 ② 결함을 실측으로 뜯은 뒤에만 재사용한다. **②는 끝났고 ①이 남았다.**"* → 🔴 **이 서술이 틀렸다**(아래 §1) |
| `docs/LENS_STRENGTH_MAP.md` 저변동 행 | *"저 leg 위험=고 leg의 **18%**(방어), **CAPM 알파 t≈3.1·FF3 t≈2.6 유의**(βMkt음=방어적), 회전율 모멘텀의 1/4 **[검증·위험대비]** · **raw 수익차는 약함**(raw 롱숏 t≈1.6)·수준 편향 과대"* |
| 같은 문서 3중 교차검증 | *"저변동(raw) \| **[−,−,+]** \| ⚠️ raw 수익 취약 → 등급 근거는 방어(BAB 위험대비)로 별도"* |
| `docs/LENS_DEV_PLAYBOOK.md` #19 | *"저변동 신뢰도: raw 롱숏 t=1.56(유의 미달)인데 CAPM 알파 t=3.05(유의) — 모순? 저변동 롱숏은 시장베타 음(−0.81)이라…"* |
| 같은 문서 #13 | *"저변동 넓은 유니버스(250종목) **고변동군 vol=99%** — 비교 왜곡. 소형·초소형·페니스탁 포함 시…"* |
| 같은 문서 #54·#89 | *"저변동 주장 정합(824) — 🔴 `calmHigh` **40% 임계와 임의성이 화면에 없음**"* |

🔴 **직전 STEP(1056)이 「못 한 것」으로 남긴 것 중 이 STEP이 건드리는 것**: 없음(1056은 문서 정합 STEP이고 렌즈 감사와 별개).

### ⓪-1a. 🔴 로드맵 원문 대조 (`docs/ROADMAP_V2.md`)

| 층 | 걸리는 절 | 인용(원문 그대로) |
|---|---|---|
| **WHY** | 조건 1 | *"**정확해야 한다** — 틀린 것을 보는 법으로 가르치면 안 가르치느니만 못하다"* → 감사의 목적이 이것 |
| **HOW** | H-7 · 5-1 | *"**결측을 채우고 실측인 척** 🔴 절대 금지"* · 규칙 5-1(원전 없는 항목은 정의 고정+공개 5단계) — 🔴 **저변동은 「원전 없음」으로 등재돼 있다**(`LENS_DISPOSITION` §3) |
| **WHAT** | W-2-1 | *"**원하는 정보 목록이 먼저 있고**, 그것을 사람이 묻는 형태로 묶은 것이 질문 모델이다"* → 🔴 **질문 여섯에 맞추려 하지 않는다**(2026-08-17 장은태 지시) |
| **관문·순위** | F-1 ②③ | ② *"정확한가 — 계산이 맞는가, **검산할 수 있는가**"* · ③ *"정직한가 — **계산 방법을 밝힐 수 있는가**"* → 감사의 두 기준 |
| **완성의 정의** | C-1 · DoD5 | *"**경계 처리** — 계산 불가 조건·최소 표본·결측 표기"*(`LENS_COMPLETION_STANDARD:22`) |
| **수익 모델** | 없음 | 무관 |

### ⓪-1b. 기존 답 확인
- `docs/KNOWN_ANSWERS.md`에 저변동 렌즈 항목: **없음**
- 🔴 **이름 패턴으로 목록을 만들지 않는다** — `ls docs/`로 전수 나열해 `*AUDIT*`·`*DEFECT*`를 눈으로 확인할 것. 2026-08-17 확인분: `VALUE_LENS_DEFECT_AUDIT_2026-08-07.md`(①밸류) · `LENS_AUDIT_02_MOMENTUM_2026-08-07.md`(②모멘텀) — **③~⑦은 없음**

### ⓪-2. 건드리는 슬롯
**해당 없음** — 읽기 전용. 단 §2-3에서 슬롯 #3(주가)·#16(베타)을 **조회만** 한다.

### ⓪-3. 걸리는 금지사항
- [ ] **코드 diff 0 · DB 쓰기 0 · 화면 변경 0** — 🔴 **7렌즈 라이브 화면 수정은 매번 별도 승인**(899·901 위반 전례)
- [ ] **크론 수동 실행 금지**
- [ ] **KR 전면 동결** — 🔴 저변동 렌즈는 KR에도 살아있다(`lens_scores` KR 970행). **US만 감사하고 KR은 조회조차 비교용으로만**
- [ ] `data/us_symbols.json`·`.github/workflows/**`·`vercel.json` 수정 금지
- [ ] `REVDCF_ENABLED`·`Q1_ENABLED` 무접촉

### ⓪-3b. 🔴 결함의 소재 확정
- 문제가 관측될 지점 = `lib/lenses.ts:282~310`(저변동 렌즈) · `lib/lowvol.ts`(`realizedVol`) · `lib/lensCompute.ts` · `lib/lensCopy.ts` · `lens_cuts`·`lens_scores`(DB). **전부 프로덕션이다** — 조사 스크립트 결함이 아니다.
- 🔴 **단 「결함」과 「정직하게 표기된 한계」를 구분한다.** `LENS_STRENGTH_MAP`이 *"raw 수익차는 약함"*을 이미 적어놨다면 그건 **결함이 아니라 공개된 한계**다. 감사가 찾을 것은 **문서에 적힌 것과 코드가 다른 지점**이다.

### ⓪-4. 🔴 반증 조건
- **전제 ①**: `LENS_DISPOSITION`의 *"②는 끝났다"*가 틀렸다(감사가 둘만 됐다).
  - 틀렸다면 → `ls docs/`에서 ③~⑦ 감사 문서가 발견된다.
  - 그때 → 그 문서를 읽고 이 STEP의 범위를 그만큼 줄인다. **중복 감사하지 않는다.**
- **전제 ②**: 저변동 렌즈는 「원전 없음」이다(`LENS_DISPOSITION` §3).
  - 틀렸다면 → 코드·문서에서 BAB(Frazzini-Pedersen 2014) 등 원전 인용이 발견된다.
  - 그때 → **원전 대조표**로 감사 축을 바꾼다(정의 공개표가 아니라).
  - 🔴 **이게 중요하다** — BAB의 원전 측정치는 **베타**인데 우리는 **실현변동성**(`realizedVol(closes, 252)`)을 쓴다. 같은 것이 아니다. **원전이 있다면 이건 원전 이탈이고, 없다면 정의 공개 대상이다.** 어느 쪽인지가 감사의 갈림길.
- 🔴 **양쪽 모두 다음 행동이 정해져 있다.**

### ⓪-5. 🔴 3번 생각
1. **범위** — 곁가지 아님. `CLAUDE.md` 규칙 3이 *"결함을 실측으로 뜯은 뒤에만 재사용"*을 요구하고, 저변동은 **지금 라이브로 US 1,026행·KR 970행에 서빙 중**이다.
2. **표본** — 🔴 **전수다.** `lens_scores` US 전량(1,039행) · `lens_state_changes` 전량. SEC/야후 신규 호출 0(DB 조회만). **표본 상한 걸지 않는다.**
   🔴 **전수의 조건** — 코드 grep만으로 「전수」라 부르지 않는다. **②역방향**을 함께 돈다: ㉠코드→문서(코드가 하는 일이 문서에 적혀 있나) ㉡문서→코드(문서가 적은 것이 코드에 있나). #54가 *"문서엔 명시인데 화면엔 없음"*을 잡은 것이 ㉡ 방향이다.
3. **시점** — 읽기 전용이라 크론과 무관. 🔴 단 `lens_cuts` US가 정지 이력이 있으므로(모멘텀 감사 ⑤) **조회 시점의 as_of를 반드시 함께 적는다.**

---

## 1. 배경

**확정**
- 감사가 **둘만 끝났다** — ①밸류(결함 6건, 🔴 5건) · ②모멘텀(결함 5건). `LENS_DISPOSITION`의 *"②는 끝났다"*는 **7렌즈 기준으로 틀린 서술**이다.
- 저변동 등급 = `verifiedDefensive`/`strong`. 근거는 **방어**(CAPM α t≈3.1)이고 **raw 수익은 t≈1.6으로 약함**. 3구간 [−,−,+].
- 저변동은 `LENS_DISPOSITION` §3에서 **「갈 곳 없음」**으로 남았다(*"Q4는 재무 위험, 이건 주가 변동성"*).
- 🔴 2026-08-17 장은태 지시: **질문 여섯에 맞추려 하지 않는다.** 정확히 답하는 것이면 새 질문 후보로 올린다(W-2-1 순서).

**확정 안 됨** 🔴
- 저변동 렌즈의 **계산이 정확한지** — `realizedVol(closes, 252)`의 정의·결측 처리·연율화 방식 미검증
- **원전이 있는지 없는지** — BAB인가 「원전 없음」인가(⓪-4 ②)
- `calmHigh` **40% 임계의 근거** — #54가 *"화면에 없음"*을 잡았으나 **임계 자체의 근거**는 미확인
- 밸류·모멘텀 결함 **11건이 지금도 살아있는지**

## 2. 할 일

### 2-1. 코드 완독 (감사 3축 ①)
`lib/lenses.ts:282~310` · `lib/lowvol.ts` · `lib/lensCompute.ts`의 저변동 경로 · `lib/lensCopy.ts`의 `lowvol` 문구 4곳(`:31·124·222·275`) · `lensCuts.ts`.
확인할 것 — ㉠`realizedVol`의 산식(로그수익률인가 단순수익률인가 · 252 영업일 연율화 · 표본표준편차 n−1인가 n인가) ㉡가격 계열 출처(배당조정 여부 — 🔴 모멘텀 감사 ③이 *"배당조정 폴백이 잣대를 섞음"*을 잡았다, 같은 문제가 있나) ㉢결측·상장 252일 미만 처리 ㉣컷 산출 방식(절대 임계 vs 분포 백분위)

### 2-2. 원전 대조 or 정의 공개 심사 (감사 3축 ②)
🔴 **⓪-4 ②의 갈림길을 먼저 확정한다.**
- 코드·문서에 **BAB(Frazzini-Pedersen) 인용이 있는가**를 전수로 확인.
- **있으면** → 원전 대조표: 원전은 **베타**, 우리는 **실현변동성**. 이 치환이 원전 이탈인지, 원전이 허용하는 변형인지 원문으로 판정.
- **없으면** → 규칙 5-1의 **정의 공개 5단계** 충족 여부: ①정의 하나로 고정 ②화면 공개 ③원자료 직접 계산 ④손계산 검산 ⑤성립 안 하는 경우 명시.
  🔴 **①밸류 감사가 ③에서 걸렸다**(야후 계산값 받아씀). 저변동도 같은지 반드시 본다.

### 2-3. 자체 DB 실측 (감사 3축 ③)
- `lens_scores` US 전량 — `lowvol_value` 분포(최소·p10·중앙·p90·최대) · `lowvol_state` 분포 · 결측 건수
- `lens_cuts` — `lowvol` 컷의 현재 값과 **as_of**(🔴 정지 여부 확인)
- `lens_state_changes` — 저변동 변화 건수·마지막 변화일(`LENS_DISPOSITION` §1은 **256건·148종목·08-07**)
- 🔴 **#13 재확인** — 고변동군 실현변동성이 지금도 극단인가(당시 250종목 유니버스에서 99%). 현재 유니버스에서 재측정
- 🔴 손계산 검산 — 표본 3종목을 골라 `realizedVol`을 손으로 재계산해 DB 값과 대조

### 2-4. 🔴 앞선 감사 결함 11건 생존 확인
`VALUE_LENS_DEFECT_AUDIT`의 6건과 `LENS_AUDIT_02_MOMENTUM`의 5건이 **2026-08-17 현재도 살아있는지** 코드·DB로 하나씩 확인한다. 특히 —
- 밸류 결함 2(야후 계산값 받아씀) — `lensCompute.ts:145·241` 지금도 그런가
- 모멘텀 결함 ⑤(US 판정 컷 8일째 정지) — 🔴 그 뒤 게이트 재정의(STEP1031)·프루닝 변경(STEP1032)이 있었다. **상태가 바뀌었을 수 있다**
- 각 건에 **고쳐짐 / 그대로 / 상태 변경**을 표시. 🔴 **확인 못 한 건은 「확인 못 함」이라 적는다.**

### 2-5. 산출물
`docs/LENS_AUDIT_03_LOWVOL.md` — 앞선 둘과 같은 형식(요약표 → 🟢 잘 된 것 → 결함별 상세 → 못 한 것).
🔴 그리고 `docs/LENS_DISPOSITION_2026-08-08.md`의 *"②는 끝났고 ①이 남았다"*를 **정정**한다(취소선 보존 + 사유 + 좌표). **문서 자체를 다시 쓰지 말고 그 문장만 고친다.**

## 3. 🔴 하지 말 것
- 🔴 **판정하지 않는다.** 저변동을 남길지 뺄지, 어느 질문에 붙일지 정하지 않는다.
- 🔴 **질문 여섯에 맞추려 하지 않는다**(W-2-1 · 2026-08-17 지시). *"어느 질문에도 안 붙는다"*를 결함처럼 쓰지 않는다.
- 🔴 **코드·화면·DB 무접촉.** 결함을 찾아도 고치지 않는다. 7렌즈 라이브 화면은 **별도 승인** 사안.
- 🔴 **KR 무접촉** — 비교 목적 조회만, 판단·집계에 넣지 않는다.
- 🔴 **다른 렌즈(퀄리티·자산성장·F스코어·기술)를 함께 감사하지 않는다.** 하나를 하나씩.
- 🔴 **문서에 이미 적힌 한계를 결함으로 이중 계상하지 않는다**(⓪-3b).

## 4. 커밋 전 게이트
- 게이트 7: git 미추적 파일 참조 검사
- 게이트 8: push 후 **GitHub Actions verify 초록 + Vercel 배포 성공을 눈으로 확인**
- 게이트 9: 새 테이블·크론·관측 필드 없음 → 해당 없음
- `docs/CHANGELOG.md` 항목 · `docs/STEP_LEDGER.md` 등재
- 🔴 **브랜치 규약(STEP1056 §2-7)** — `main`에 직접 push. 브랜치·PR 만들지 않는다

## 5. DoD
- [ ] ⓪-1a 로드맵 여섯 층 대조 · ⓪-1b `ls` 전수 목록
- [ ] 🔴 ⓪-4 ② **갈림길 확정** — 원전(BAB) 있음 / 없음 중 어느 쪽인지 근거와 함께
- [ ] 2-1 코드 완독 — `realizedVol` 산식·가격 출처·결측 처리·컷 방식 넷 다
- [ ] 2-2 원전 대조표 **또는** 정의 공개 5단계 충족 심사(어느 쪽인지 §2-2 결과에 따라)
- [ ] 2-3 DB 전수 실측 + 🔴 **표본 3종목 손계산 검산** + as_of 명기
- [ ] 🔴 2-4 앞선 결함 **11건 전부** 생존 여부(고쳐짐/그대로/상태변경/확인못함)
- [ ] 2-5 `LENS_AUDIT_03_LOWVOL.md` 생성 + `LENS_DISPOSITION` 문장 정정
- [ ] 🔴 **양방향 대조**(⓪-5②) — 코드→문서 · 문서→코드 둘 다 돌았다
- [ ] 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · tsc 0 · test 전량 통과
- [ ] 마지막에: **못 한 축 / 철회·정정한 것 / 미측정으로 남은 것**

🔴 **판정은 장은태가 한다. 이 STEP은 결함을 놓는 것까지다.**
