# STEP 1051 — 미사용 8개 처분 · `products` 귀속 · 「동결」의 정확한 정의 (🟠 DB 변경 가능 · 2단계)

> 🔴 **STEP1049가 인벤토리에서 멈췄다**(설계대로). 남은 처분을 끝낸다.
> 🔴 **Cowork 실측(2026-08-15)**: `kr_stock_snapshot`을 읽는 코드가 **10곳**이고 그중 `lib/todayChanges.ts`(홈 다이제스트)·`app/api/search/route.ts`·`app/sitemap.ts`·`app/api/watchlist/quotes/route.ts`가 있다. `/explore`도 `market==='KR'`에서 `/api/krx/ranking`을 부른다. **KR은 동결인데 홈·검색·탐색·관심목록까지 살아 있다.** → 🔴 **KR 크론 3개는 끄면 안 된다. 이건 판정거리가 아니라 이미 답이 있는 것이다.**
> 🔵 그 대신 **「동결」이 무엇을 뜻하는지 문서에 못박는다** — 오늘 이 혼선이 반복됐다.

---

## ⓪ 전제 점검

### ⓪-1. 직전 산출물 — 원문 인용
| 출처 | 인용 |
|---|---|
| `probe_1049` | 69개 귀속 — US 27 · KR 3 · JP/CN/VN/GB(파킹) 9 · 공용 21 · **불명 1(`products`)** · **미사용 8건** |
| Cowork 실측(오늘) | `kr_stock_snapshot` 참조 10곳 — `sitemap.ts`·`explore/lens-top`·`search`·`krx/ranking`·`krx/kr-performance`·`watchlist/quotes`·`cron/health`·`cron/kr-lens-scores`·`lensPrecompute.ts`·`todayChanges.ts` |
| `components/explore/ExploreClient.tsx:87-88` | `if (market === 'KR') { … fetchJson('/api/krx/ranking?…') }` |
| `PARKED_FIELD_SURFACES.md:40` | *"그 표면에만 데이터를 대던 크론들은 아무 화면에도 안 뜨는 테이블을 매일 갱신하는 **순수 낭비**였음"* — 🔴 **KR은 이 경우가 아니다** |
| `PARKED_FIELD_SURFACES.md:45` | *"`kr-etp` 체크는 유지(크론 유지)"* — 선례 |
| `CLAUDE.md:31` | *"한국시장 기준 모든 내용은 싹 다 **미뤄버려**"* |
| 오늘의 두 사고 | `AdvisorDirectory`(제거됨이라 적혔으나 라이브) · `dividends`(0.46%인 줄 알았으나 KR) — 🔴 **「미사용」 판정은 재확인 없이 믿지 않는다** |

### ⓪-1a. 🔴 로드맵 원문 대조 (`docs/ROADMAP_V2.md`)
| 층 | 걸리는 절 | 인용 |
|---|---|---|
| **WHY** | 조건 1(정확) | 쓰는 것과 안 쓰는 것이 뒤섞이면 판단이 계속 틀린다 |
| **HOW** | H-2(카탈로그가 먼저) | 🔴 4층(테이블)이 이번에 생겼다 — 처분 결과를 거기 반영 |
| **WHAT** | 범위(US 상장 종목 분석) | 🔴 **KR 화면이 라이브인 것과 범위의 관계를 명시**한다 |
| **관문·순위** | F-5 판정 대기 | 처분되는 항목을 F-5에서 내린다 |
| **완성의 정의** | C-1 항목 2(입력 검증) | 미사용 테이블은 입력 검증 대상이 아님을 구분 |
| **수익 모델** | **없음** | 무관 |

### ⓪-1b. 🔴 기존 답 확인 — 🔴 이름 패턴 금지, `ls`로 전수
`docs/`의 `PARKED_*` 전부(🔴 `ls`로 확인) · `probe_1047`~`1050` · `DATA_SOURCE_CATALOG.md`(4층) · `SYSTEM_MAP.md` · `STATE.md` · `KNOWN_ANSWERS.md`
🔴 이 칸이 비면 미완성.

### ⓪-2. 슬롯 — 없다

### ⓪-3. 걸리는 금지사항
- [x] 🔴 **`vercel.json`·`.github/workflows/**`·`data/us_symbols.json` 수정 금지** — 크론 구성은 건드리지 않는다
- [x] 🔴 **KR 크론 3개(`kr-perf`·`kr-etp`·`kr-lens-scores`)를 끄지 않는다** — 위 실측대로 라이브 화면이 의존한다
- [x] 🔴 **`kr_stock_snapshot` 계열 무접촉**
- [x] 🔴 **`Q1_ENABLED`·`REVDCF_ENABLED` 무접촉** · 환경변수 VALUE 타이핑 금지
- [x] 🔴 **화면 변경 0** — 나오면 중단하고 보고
- [x] 🔴 **참조 0을 증명하기 전 어떤 DROP도 하지 않는다**(STEP1047 게이트 그대로)
- [x] 🔴 **창작 금지** · **선택지 나열 금지**

### ⓪-3b. 🔴 결함의 소재 확정
- 🔴 **`probe_1049`의 「미사용 8건」을 그대로 믿지 않는다.** 오늘 두 번 틀렸다(`AdvisorDirectory`·`dividends`). **두 방법으로 재확인**한 뒤에만 처분한다
- 🔴 **「동결」과 「제거」와 「파킹」은 서로 다르다.** KR은 **동결**(새 작업 안 함 · 화면은 그대로) · 타국 9개는 **파킹**(스위치 OFF) · 미사용은 **처분 대상**. 세 낱말을 섞지 않는다
- 🔴 **처분 = 삭제가 아니다.** 데이터가 있으면 보존 후 제거, 0행이면 기록 후 제거, 애매하면 **등재만**

### ⓪-4. 🔴 반증 조건

| 관측 | 결론 |
|---|---|
| 미사용 8건 재확인에서 **참조 0** | ✅ 처분 진행 |
| 🔴 **8건 중 실제로 쓰이는 것이 나온다** | 🔑 **오늘 세 번째 사례** — 🔴 그 건은 처분에서 빼고, `probe_1049`의 판정 방법에 무엇이 빠졌는지 적는다 |
| 🔴 **`products`가 어느 쪽으로도 안 갈린다** | 🔴 「불명」 유지 + **무엇을 더 봐야 정해지는지**. 추측으로 채우지 않는다 |
| 🔴 **KR 크론을 끌 수 있다는 근거가 나온다** | 🔴 **끄지 말고 보고** — `vercel.json`은 승인 사항 |
| 미사용 중 **0행이 아닌 것**이 있다 | 🔴 보존 덤프 필수 |
| 「동결」 정의를 적으니 **기존 문서와 어긋나는 서술**이 나온다 | 🔴 목록화하고 정정 |

### ⓪-5. 3번 생각
1. **범위** — **미사용 8 처분 · `products` 귀속 · 「동결」 정의**까지. 🔴 크론·KR 화면·Q1은 대상 아님
2. 🔴 **표본 — 8건 전수 + `products`.** 비용 낮음. **상한 없음**
3. 🔴 **「전수의 조건」 적용** — 미사용 재확인을 **두 방법**으로: ① **이름 검색**(따옴표 종류·문자열 조합 포함) ② **역방향**(코드가 부르는 테이블명 수집 → 대조). 🔴 **뷰·RPC·마이그레이션·RLS도 본다**(`advisor_directory` 선례)

---

## Phase A — 재확인 (🔵 읽기만)

### A-1. 미사용 8건 재확인
- `probe_1049`가 「미사용」으로 판정한 8개를 **두 방법으로 다시 확인**(⓪-5-3)
- 각각 **행수 · 최신 갱신 · 참조 0 근거(두 방법 각각)**를 적는다
- 🔴 **하나라도 참조가 나오면 그 건은 처분에서 제외**하고 `probe_1049`의 판정 방법에 무엇이 빠졌는지 기록

### A-2. `products` 귀속
- 스키마·행수·참조 코드·데이터 내용을 보고 **US / KR / 공용 / 미사용 / 불명** 중 하나로 판정
- 🔴 **이름이 아니라 내용과 참조로** 판정. 못 정하면 「불명」 유지 + 필요한 추가 확인 항목

### A-3. 🔴 판정: 진행 가능한가
참조 0이 증명된 것만 Phase B로. 🔴 **임의로 코드를 고쳐 진행하지 않는다.**

---

## Phase B — 처분 (A-3 통과분만)

### B-1. 보존
- **0행이 아닌 것**: `spinoff/kr-pilot-2026-06-25/`와 같은 형식으로 덤프 + DDL + 사유. 🔴 위치는 성격에 맞게(KR이면 그쪽, 아니면 새 폴더) — **어디에 뒀는지 README에 남긴다**
- **0행인 것**: 덤프 대신 **DDL과 존재 기록**만(있었다는 사실이 남아야 나중에 "왜 없지"가 안 나온다)

### B-2. 제거
- 🔴 **의존 역순** · 🔴 **`supabase/migrations/`에 마이그레이션 파일로 기록**
- 🔴 DROP 전 행수 재확인(덤프와 일치)

### B-3. 검증 — 양방향 + 눈으로
- ① 코드 → DB 고아 참조 0 · ② DB → 코드 고아 테이블 목록
- 🔴 **tsc clean · 전체 테스트 통과** · 🔴 `npm run build`는 **dev 서버 충돌 위험이 있으면 사유를 적고 생략**(STEP1050 선례)
- 🔴 **`us_*`·`kr_stock_snapshot` 행수 before/after 불변** 수치로
- 🔴 **로컬에서 홈·`/explore`(KR 포함)·검색이 정상인지 확인하고 기록**(C-3 ㉡)

---

## 2. 🔑 「동결」의 정확한 정의 — 문서화 (이 STEP의 두 번째 목적)

🔴 오늘 이 혼선이 반복됐다. `ROADMAP_V2` 범위 절과 `CLAUDE.md` 인접 자리에 **세 낱말을 구분해 못박는다.**

| 낱말 | 뜻 | 지금 해당하는 것 | 화면 |
|---|---|---|---|
| **동결**(KR) | 🔴 **새 작업을 안 한다. 기존 화면·크론·데이터는 그대로 돈다** | `kr_stock_snapshot` 계열 · KR 크론 3개 · KR 보드 | **라이브** |
| **파킹**(타국·기능) | 배선 보존 · 스위치 OFF · 활성화 체크리스트 | JP/CN/VN/GB 9개 · Q1 · 역DCF · KR 배당 탭 | **안 나감** |
| **제거** | 데이터 보존 후 DB·코드에서 없앰 | KR 파일럿 11개(1048) · 리딩방(1035) | 없음 |

- 🔴 **근거를 함께 적는다** — KR이 동결인데 라이브인 이유: `lib/todayChanges.ts`(홈)·`search`·`sitemap`·`watchlist/quotes`·`/explore`가 `kr_stock_snapshot`에 의존(오늘 실측, 참조 10곳)
- 🔴 **그러므로 KR 크론 3개는 유지가 답이다**를 근거와 함께 기록 — 다음에 또 묻지 않도록
- 🔴 **범위(「US 상장 종목 분석」)와의 관계를 한 문단으로** — 범위는 **우리가 새로 만드는 모델의 적용 대상**이지, **화면에서 KR을 내린다는 뜻이 아니다**

---

## 3. 산출물
`docs/probe_1051_unused_disposal.md` — A-1 두 방법 재확인표 · A-2 `products` · B 처분 기록 · before/after · 로컬 육안 기록 · 🔴 3중 규칙 4종

🔴 `DATA_SOURCE_CATALOG.md` 4층 갱신 · `ROADMAP_V2`(범위 절에 §2 표 반영 · F-5에서 처분 완료분 내림) · `SYSTEM_MAP.md` · `STATE.md` · `roadmap_v2.html` 동기화.

## 4. 커밋 전 게이트
게이트 7 · 게이트 9 · `CHANGELOG` 1051 · `STEP_LEDGER` 등재. 커밋·푸시.

## 5. DoD
- [ ] ⓪-1a 로드맵 여섯 층 대조 · ⓪-1b 🔴 **`ls` 전수 목록**
- [ ] 🔑 **A-1 8건 두 방법 재확인** · 🔴 참조 나오면 제외하고 판정 방법 결함 기록
- [ ] A-2 `products` 귀속 — 🔴 **이름 아닌 내용·참조로** · 못 정하면 「불명」 유지
- [ ] 🔴 **A-3 참조 0 증명분만 Phase B**
- [ ] B-1 보존(0행도 DDL·기록) · B-2 의존 역순 · 마이그레이션 기록 · B-3 양방향 + 🔴 **로컬 육안(홈·/explore KR·검색)**
- [ ] 🔴 **§2 「동결/파킹/제거」 3낱말 표 + KR 크론 유지 근거 + 범위와의 관계**
- [ ] 🔴 **`vercel.json` 무변경 · KR 크론 무접촉 · 화면 변경 0 · 플래그 불변**
- [ ] 마지막에: 못 한 축 / 아직 안 함 / 철회·정정 / 미측정

🔴 **「미사용」은 오늘 두 번 틀렸다. 두 방법으로 다시 확인한 것만 지운다.**
🔴 **KR은 동결이지 제거가 아니다 — 홈·검색·탐색이 지금도 그 데이터를 쓴다.**
