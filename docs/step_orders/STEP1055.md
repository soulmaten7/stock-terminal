# STEP 1055 — 재료 다섯 배선 + 죽은 태그 정리 + 실패 기록 (🟠 코드·DB 변경 · 화면 변경 0 · 플래그 무접촉)

> 🔴 **판정은 장은태가 한다.** 이 명령서는 STEP1054가 **실측으로 근거를 만든 것만** 배선한다. 새 판단을 만들지 않는다.
> 🔵 화면 변경 0 · `REVDCF_ENABLED`·`Q1_ENABLED` 무접촉 · 크론 수동 실행 없음.

---

## ⓪ 전제 점검

### ⓪-1. 직전 STEP 산출물 — 원문 인용

| 출처 | 인용(그대로) |
|---|---|
| `docs/probe_1054_tag_supply.md` §2-4 | `Assets` **73.8%** · `NetCashProvidedByUsedInOperatingActivities`∪`…ContinuingOperations` **72.3%** · `RetainedEarningsAccumulatedDeficit` **72.2%** · `Liabilities` **65.9%**(재구성 경로 `LiabilitiesAndStockholdersEquity` **73.6%**) · 배당 지급기준 **34.2%** |
| 같은 문서 §2-2 ② | *"죽은 태그 4개(`CostOfSales`·`CostOfOperatingRevenues`·`CostOfRevenues`·`DepreciationAmortizationAndDepletion`)"* |
| 같은 문서 「아직 안 함」 | *"5개 태그를 `drivers.ts`에 실제로 추가할지 · 죽은 태그 4개를 배열에서 뺄지"* — **이 STEP이 그 둘을 집행한다** |
| 같은 문서 §2-4 머리 | 🔴 *"기준연도 없음 2,136건 → 자체 최신연도로 대체"* 때문에 ㉡㉢가 **다소 낙관적** |

🔴 **STEP1054가 「못 한 것」으로 남긴 것 중 이 STEP이 건드리지 않는 것**: SEC 404 78종목 원인 분류 · Altman 결측 1,099건 항목별 분해 · 배당 무배당/데이터없음 분리 · Altman SIC 교체. **전부 손대지 않는다.**

### ⓪-1a. 🔴 로드맵 원문 대조 (`docs/ROADMAP_V2.md`)

| 층 | 걸리는 절 | 인용(원문 그대로) |
|---|---|---|
| **WHY** | 조건 1 (`:65`) | *"**정확해야 한다** — 틀린 것을 보는 법으로 가르치면 안 가르치느니만 못하다. 🔴 계산이 먼저다"* → 재료 배선은 조건 1의 일 |
| **HOW** | H-7 | *"**결측을 채우고 실측인 척** 🔴 절대 금지"* → 새 필드는 없으면 `null`, 0으로 채우지 않는다 |
| **WHAT** | W-3 | *"**US 상장 종목**에 대해"* |
| **관문·순위** | F-5 ⑬-b | *"부도위험은 애초부터 0%(`Assets`·`Liabilities` 컬럼 자체가 없음)"* → 🔴 **이 STEP이 그 서술을 무효화한다**(실측 73.8%/65.9%). F-5 ⑪(자산성장)도 실측 99.7%로 근거가 뒤집혔다 |
| **완성의 정의** | C-1 | 화면을 안 만들므로 9항목 심사 대상 아님 |
| **수익 모델** | 없음 | 무관 |

🔴 **로드맵과 어긋난 것 = 이 STEP의 산출물**: F-5 ⑬-b·⑪의 「재료 없음」 서술이 실측과 어긋난다. §2-5에서 로드맵 본문을 고친다.

### ⓪-1b. 기존 답 확인 (`docs/KNOWN_ANSWERS.md`)

- 있음/없음: **없음**(`Assets`·`태그`·`배당` 어느 검색어로도 항목 없음, STEP1054 ⓪-1b 재확인).
- 🔴 **이름 패턴으로 목록을 만들지 않는다** — `ls docs/`로 전수 나열해 `probe_105*`를 눈으로 확인할 것.

### ⓪-2. 건드리는 슬롯

| 슬롯 | 카탈로그가 말하는 소스 | 검증 상태 | 실제 도달률 |
|---|---|---|---|
| SEC companyfacts (us-gaap) | XBRL 태그 | ✅ **2026-08-16 STEP1054 전수 실측**(5,742/5,820, 98.66%) | 위 ⓪-1 표 |

### ⓪-3. 걸리는 금지사항

- [ ] **크론 수동 실행 금지** — 특히 `revdcf`·`email-brief`
- [ ] **화면 변경 0** · `REVDCF_ENABLED`·`Q1_ENABLED` 무접촉
- [ ] **수정 금지 파일** — `data/us_symbols.json` · `.github/workflows/**` · `vercel.json`
- [ ] **KR 전면 동결**
- [ ] 취득 경로 변경 금지 — 🔴 **기존 REV·COST·PPE 등 driver 계산 경로에 손대지 않는다**(§3 참조)
- [ ] API 키 발급·유료 가입·키 VALUE 타이핑 금지

### ⓪-3b. 결함의 소재 확정

- STEP1054가 잡은 **단위 버그**(`shares`/`USD/shares`)는 🔴 **조사 스크립트 `scripts/probe_1054_analysis.ts:105·137`에만 있었다.** 프로덕션 `lib/revdcf/drivers.ts:367·371·372`는 이미 `"shares"`를 정확히 넘기고 있다 — **프로덕션 영향 0.** 이 STEP은 그 버그를 고치는 STEP이 아니다.
- 🔴 새로 추가하는 필드도 **단위를 명시 전달**한다(금액=`USD`, 주당=`USD/shares`, 주식수=`shares`). 기본값 `USD`에 기대지 않는다.

### ⓪-4. 🔴 반증 조건

- **전제 ①**: 죽은 태그 넷은 출현 0건이므로 배열에서 빼도 **기존 604건 값이 하나도 안 바뀐다.**
  - 틀렸다면 → 불변 검증(§2-4)에서 값이 바뀐 종목이 1건이라도 나온다.
  - 그때 다음 축 → **즉시 되돌린다.** 출현 0건 실측이 틀렸다는 뜻이므로 STEP1054 §2-2를 재조사한다. 🔴 **되돌림이 기본값이다** — "몇 건뿐이니 괜찮다"로 넘기지 않는다.
- **전제 ②**: 새 필드는 기존 driver 계산과 무관하므로 `revdcf_results`의 판정·GAP이 안 바뀐다.
  - 틀렸다면 → 같은 불변 검증에서 `verdict`·`gap_years`가 바뀐다.
  - 그때 다음 축 → 새 필드가 기존 경로를 건드린 지점을 찾아 분리한다.
- 🔴 **양쪽 모두 다음 행동이 정해져 있다.**

### ⓪-5. 🔴 3번 생각

1. **범위** — 곁가지 아님. F-5 ⑬-b·⑪의 근거를 실측이 뒤집었고, 이 STEP이 그 결과를 코드·DB·로드맵에 반영한다.
2. **표본** — 🔴 **불변 검증은 전수다.** `revdcf_results` 최신 `as_of` **604건 전량**을 변경 전후로 대조한다. SEC 신규 호출 0(로컬 캐시 재사용). 표본 상한 걸지 않는다.
3. **시점** — 🔴 **오늘 밤 크론이 이 변경의 첫 반영이다.** 이 STEP 외의 다른 변경을 같은 날 얹지 않는다 — 얹으면 원인 분리가 불가능해진다(`STATE.md` STEP990 전례).

---

## 1. 배경 — 확정된 것 / 확정 안 된 것

**확정**
- 태그 다섯의 공급률이 전수 실측됐다(⓪-1 표). 추정이 아니다.
- `RetainedEarnings`(bare)는 5,742종목 전수에서 **0건** — 표준 태그는 `RetainedEarningsAccumulatedDeficit`.
- 죽은 태그 넷은 출현 0건.
- 배당 세 축(지급기준 34.2% · 선언기준 24.8% · 지급기준 주당 15.4%)이 **서로 다른 종목을 담는다** — 섞으면 안 된다.

**확정 안 됨** 🔴
- Piotroski 병목 둘(⑤Δ레버리지 64.2% · ⑧Δ마진 51.6%)의 배열을 확장할지 — **이 STEP 범위 밖**(다음 STEP에서 판정)
- Altman 섹터 근사를 SIC로 교체할지 — **범위 밖**
- 총부채를 직접 태그로 갈지 재구성 경로(`Assets − StockholdersEquity`)로 갈지 — 🔴 **이 STEP은 둘 다 저장만 하고 어느 쪽을 쓸지 정하지 않는다**
- 새 재료로 Altman·Piotroski·배당 모델을 **계산·표시**하는 것 — 전부 후속

## 2. 할 일

### 2-1. 🔴 실패 기록 — `CLAUDE.md` ⓪-5-B 「왜」 절에 추가

`CLAUDE.md:191`의 *"2026-08-09 실제 사례 — 빗나간 추론 4건"* 블록 **바로 아래**에 다음을 추가한다. 기존 4건 블록은 **손대지 않는다**.

```
**왜 (2026-08-16 실제 사례 — 같은 날 5건 · 08-09와 원인 축이 다르다)**
🔑 **08-09 4건은 「실측을 안 해서」 틀렸고, 08-16 5건은 「문서를 안 열어서」 틀렸다.** 앞은 바깥을 안 봤고 뒤는 안을 안 봤다.
- WHY 조건 2 「단순」을 원문(`ROADMAP_V2.md:65` *"어렵게 설명하면 원인을 재생산한다"*)을 열지 않고 「문장의 단순 / 선택의 단순」 두 층으로 임의 정의한 뒤 *"문서가 한 층만 다룬다"*고 비판했다 — **그 두 층은 문서에 없다.** 없는 문제 위에 「모델엔 표본 잣대를 대면서 H-1엔 안 댄다」는 일관성 비판까지 쌓았다.
- 역DCF 표시 판정 셋(3점 민감도·판정 넷 문구·색)을 *"화면 앞에서 30초에 정해진다"*고 했으나 `components/RevDcfSection.tsx:49·56~58·60`에 **이미 구현돼 있었다**(STEP889가 색까지 정리).
- *"플래그를 켜서 보자"*고 제안했으나 `docs/STATE.md:60`에 로컬 dev가 이미 `REVDCF_ENABLED=true`로 기동 중이라 적혀 있었다.
- *"자산성장은 `Assets`가 없어 못 만든다"*고 단정했으나 실측 **99.7%**였다(STEP1054).
- STEP1054 명령서의 SEC 비용 산식을 0.1초/건으로 썼으나 실측 **0.45초/건**(4.5배) — 레이트리밋 간격만 세고 다운로드 시간(평균 1.35MB/파일)을 안 셌다.
🔴 **직접 원인 — `CLAUDE.md:255`(답변마다 「이번에 연 파일·시트·셀」을 `[3중 점검] ⓪` 줄에 적는다)를 그날 대화 내내 0번 실행했다.** 안 적으니 안 열고도 넘어갔다. **규칙이 없어서가 아니라 규칙을 실행하지 않아서다.**
🔴 **금지된 것은 「추론」이 아니라 「좌표 없는 단정」이다**(`/preferences.md` 근거 좌표 의무화). 같은 날 STEP1054 ⓪-4의 `RetainedEarnings` 부재 예측도 추론이었으나 **반증 조건으로 박아 실측이 갈랐고 적중했다.** 차이는 시험 가능하게 내놓았는지다.
🔴 **행동 선택지는 묻고 말의 뜻은 안 묻는 패턴** — 「단순」의 뜻을 되묻지 않고 스스로 정의했다. **뜻이 갈릴 수 있으면 분석 전에 되묻는다.**
```

### 2-2. `DriverFundamentals`에 새 필드 다섯 — 🔴 기존 배열에 끼우지 않는다

`lib/revdcf/drivers.ts`에 **새 상수 배열**을 만들고 `DriverFundamentals`에 **새 필드**로 병기한다(STEP963이 `commonEquity`를 `equity` 옆에 병기한 방식 그대로).

| 새 필드 | 태그(coalesce 순서) | 단위 |
|---|---|---|
| `totalAssets` | `Assets` | `USD` |
| `totalLiabilities` | `Liabilities` | `USD` |
| `liabilitiesAndEquity` | `LiabilitiesAndStockholdersEquity` | `USD` |
| `retainedEarnings` | `RetainedEarningsAccumulatedDeficit` | `USD` |
| `cashFromOps` | `NetCashProvidedByUsedInOperatingActivities` → `NetCashProvidedByUsedInOperatingActivitiesContinuingOperations` | `USD` |
| `dividendsPaid` | `PaymentsOfDividends` → `PaymentsOfDividendsCommonStock` | `USD` |
| `dividendsDeclaredPerShare` | `CommonStockDividendsPerShareDeclared` | 🔴 `USD/shares` |

🔴 **`liabilitiesAndEquity`를 따로 두는 이유** — 재구성 경로(73.6%)가 직접 태그(65.9%)보다 7.7%p 넓다. **어느 쪽을 쓸지는 이 STEP이 정하지 않는다.** 둘 다 저장하고 소비처가 고른다.
🔴 **배당 두 축을 한 필드에 합치지 않는다**(실측 근거: 서로 다른 종목을 담음).
🔴 값이 없으면 **`null`**. 0으로 채우지 않는다(§2-4 947 규약).
🔴 `sourceTags`에 각 필드가 실제 채택한 태그명을 기록한다(기존 규약).
🔴 수집 위치는 **기존 `fundamentals` 선수집 블록 안**(5년 창 게이트보다 앞) — skip 경로에도 실리게. 🔴 **게이트 조건식·순서·`skipReason` 문자열은 불변.**

### 2-3. 죽은 태그 넷 제거

`COST` 배열에서 `CostOfSales`·`CostOfOperatingRevenues`·`CostOfRevenues`를, 감가상각 배열에서 `DepreciationAmortizationAndDepletion`을 뺀다.
🔴 **뺀 뒤 §2-4 불변 검증을 통과해야만 유지한다.** 실패하면 되돌린다(⓪-4).

### 2-4. 🔴 불변 검증 — 전수 604건

변경 전후로 `revdcf_results` 최신 `as_of` **604건 전량**을 재계산해 대조한다(로컬 캐시 재사용·SEC 신규 호출 0·DB 쓰기 0).
대조 대상: `verdict` · `gap_years` · `gap_wacc_minus1` · `gap_wacc_plus1` · `sales_growth` · `operating_margin` · `fixed_capital_rate_marginal` · `wacc`.
**기대값 = 차이 0건.** 차이가 나오면 종목·항목·이전값·이후값을 전부 적고 §2-3을 되돌린다.

### 2-5. DB 컬럼 + 로드맵 반영

- `us_fundamentals`에 위 7개 컬럼을 추가한다(`apply_migration`). 전부 **nullable**.
- 크론이 새 필드를 그 컬럼에 쓰도록 배선한다. 🔴 **기존 컬럼 쓰기 경로는 안 건드린다.**
- `docs/ROADMAP_V2.md` F-5 ⑬-b·⑪의 「재료 없음/0%」 서술을 실측치로 정정한다(취소선으로 원문 보존 + 정정 사유 + `probe_1054` 좌표).

## 3. 🔴 하지 말 것

- 🔴 **기존 driver 계산 경로 무접촉** — `REV`·`PPE`·`CASH_OP`·`DEBT_*`·`SHARES_*` 배열과 `computeDrivers`의 계산식·게이트에 손대지 않는다. 이 STEP이 만지는 것은 **새 필드 추가**와 **죽은 태그 제거** 둘뿐이다.
- 🔴 **Piotroski·Altman·배당 모델을 계산하지 않는다.** 재료만 싣는다. 계산·표시는 후속 STEP.
- 🔴 **Piotroski 병목 배열(`DEBT_LT`·COGS 계열)을 확장하지 않는다** — 별도 판정 대상.
- 🔴 **결측을 채우지 않는다**(H-7).
- 🔴 **크론 수동 실행 금지** · 화면 변경 0 · 플래그 무접촉 · KR 무접촉.
- 🔴 **총부채를 어느 경로로 쓸지 정하지 않는다.** 저장까지만.

## 4. 커밋 전 게이트

- 게이트 7: git 미추적 파일 참조 검사(`git ls-files` 기준 — 경로 모양 아님)
- 게이트 8: push 후 **GitHub Actions verify 초록 + Vercel 배포 성공을 눈으로 확인**하기 전까지 미완료
- 게이트 9: 새 컬럼 7개가 생기므로 🔴 **`docs/CRON_OBSERVABILITY.md` §5 신선도 목록에 추가**(정본 먼저 — Cowork 예약 프롬프트는 이 STEP이 끝난 뒤에 맞춘다)
- `docs/CHANGELOG.md` 항목 · `docs/STEP_LEDGER.md` 등재

## 5. DoD

- [ ] ⓪-1a 로드맵 여섯 층 대조 · ⓪-1b `ls` 전수 목록
- [ ] 2-1 `CLAUDE.md` 실패 기록 추가 — 🔴 **기존 08-09 4건 블록 불변**
- [ ] 2-2 새 필드 7개 · 🔴 **단위 명시 전달** · `null` 유지 · `sourceTags` 기록 · 게이트 문자열 불변
- [ ] 2-3 죽은 태그 4개 제거
- [ ] 🔴 2-4 **불변 검증 전수 604건 · 차이 0건** — 차이 나면 되돌리고 그 사실을 적는다
- [ ] 2-5 컬럼 7개 마이그레이션 · 크론 배선 · 로드맵 F-5 ⑬-b·⑪ 정정
- [ ] tsc 0 · test 전량 통과 · 화면 변경 0 · 플래그 무접촉
- [ ] 게이트 7·8·9 · CHANGELOG · STEP_LEDGER
- [ ] 마지막에: **못 한 축 / 철회·정정한 것 / 미측정으로 남은 것**

🔴 **판정은 장은태가 한다. 명령서는 숫자를 놓는 것까지다.**
