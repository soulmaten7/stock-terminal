# STEP 1048 — KR 파일럿: 데이터 보존 이관 + 코드 파킹 + DB 제거 (🟠 코드·DB 변경 · 화면 일부 변경)

> 🔴 **장은태 판정(2026-08-15)**: *"KR은 추후에 사용을 해야 할 수도 있으니 데이터인 거면, 이건 KR 데이터를 모아둔 곳으로 옮겨두고 우린 US를 계속 진행하는 게 맞아."*
> → **데이터는 버리지 않고 이관 · 코드는 지우지 않고 파킹(배선 보존·스위치 OFF) · DB만 정리 · US는 무영향.**
> 🔴 **STEP1047 Phase A가 NO-GO였다**(`dividends`가 KR 배당 탭에 라이브 서빙 중). 이번엔 그 참조를 **파킹으로 끊고** 진행한다.

---

## 0-A. STEP1047이 남긴 사실

- 파일럿 **11개** 테이블(`financials` 중복 계산 정정) + 미사용 뷰 `stock_snapshot_v`
- 🔴 **`dividends`(60행)만 라이브 참조**: `ToolboxClient.tsx:130` → `OfferingsFeed.tsx:33` → `DividendFeed.tsx` → `app/api/dividend/feed/route.ts` → `dividends ⋈ stocks`
- 나머지 **9개 0행 테이블 + 뷰는 참조 0**(A-2·A-3 확인 완료)
- 🔴 **US는 별개 계보** — `UsOfferingsFeed` → `UsDividendFeed`. 이 테이블을 전혀 안 쓴다
- 데이터 실상: `created_at` 단일 타임스탬프(2026-06-25 12:50:42) · **52일 갱신 0** · `payout_ratio` 전 행 NULL

---

## ⓪ 전제 점검

### ⓪-1. 직전 산출물 — 원문 인용
| 출처 | 인용 |
|---|---|
| `probe_1047` | Phase A **NO-GO** — 참조 체인 4단계 확인, 선제거 계획 3안 제안(판정은 남김) |
| `components/toolbox/ToolboxClient.tsx:120-130` | `case 'ipo': return country === 'US' ? <UsOfferingsFeed /> : … : <OfferingsFeed />` |
| `components/toolbox/OfferingsFeed.tsx:33` | 🔴 **`{view === 'ipo' ? <IpoFeed /> : <DividendFeed />}`** — **한 컴포넌트가 IPO와 배당을 함께 담는다** |
| `LOCALE_SOURCE_PLAYBOOK` §11 | Parked Feature Protocol — *가짜로 채우지 않고 배선 보존 · 스위치만 OFF · 활성화 체크리스트 · 재사용 범위* |
| STEP1035~1039 | `spinoff/advisor-directory/` 선례 — 코드+스키마+README 분리 보관 후 DB DROP |
| `CLAUDE.md:31` | *"한국시장 기준 모든 내용은 싹 다 **미뤄버려**"* — 🔴 **미룸**이 이번 처리의 근거 |

### ⓪-1a. 🔴 로드맵 원문 대조 (`docs/ROADMAP_V2.md`)
| 층 | 걸리는 절 | 인용 |
|---|---|---|
| **WHY** | 조건 1(정확) | 52일 묵은 데이터를 라이브로 내보내는 상태를 끝낸다 |
| **HOW** | **H-7(창작의 경계 — 기만 금지)** · H-4(막혔을 때) | 🔴 낡은 값을 현재값처럼 보여주는 것이 H-7에 걸린다 |
| **WHAT** | W-2-3 배당 칸 | US 배당 재료는 **0건** — 이 테이블과 무관했다 |
| **관문·순위** | F-4 · F-4-3 · F-5 | 🔴 배당 재료 표기·순위·판정 대기 정정 |
| **완성의 정의** | C-1 항목 2(입력 검증) · 항목 6(주장 정합) | 지켜졌으면 잡혔을 사안 |
| **수익 모델** | **없음** | 무관 |

### ⓪-1b. 🔴 기존 답 확인 — 재조사 금지
`probe_1047`(참조 전수 — **그대로 사용, 다시 안 찾는다**) · `LOCALE_SOURCE_PLAYBOOK` §11 · `spinoff/advisor-directory/README.md`(선례 형식) · `PARKED_*_ACTIVATION.md` 4종(형식) · `KNOWN_ANSWERS.md`
🔴 이 칸이 비면 미완성.

### ⓪-2. 슬롯 — 없다

### ⓪-3. 걸리는 금지사항
- [x] 🔴 **`kr_stock_snapshot` 및 계열 무접촉** — 실제 KR 보드다
- [x] 🔴 **`us_*` 테이블·US 컴포넌트(`UsOfferingsFeed`·`UsDividendFeed`·`UsIpoFeed`) 무접촉** — 🔴 diff 0으로 증명
- [x] 🔴 **KR IPO 피드를 죽이지 않는다** — ⓪-3b 참조. 🔴 **이번 STEP의 최대 사고 위험**
- [x] 🔴 **코드를 삭제하지 않는다** — 파킹(스위치 OFF)이다. `spinoff` 이관도 **데이터·DDL만**
- [x] 🔴 **덤프 검증 전 DROP 금지** · 크론 수동 실행 금지 · `.github/workflows/**`·`vercel.json`·`data/us_symbols.json` 수정 금지
- [x] 🔴 **`Q1_ENABLED`·`REVDCF_ENABLED` 무접촉** · 환경변수 VALUE 타이핑 금지
- [x] 🔴 **약관·개인정보처리방침 수정 보류**(`PARKED_TERMS_PRIVACY_ACTIVATION.md` 그대로)

### ⓪-3b. 🔴 결함의 소재 확정 — 최대 위험
🔴 **`OfferingsFeed`는 IPO와 배당을 함께 담는 컴포넌트다**(`:33` — `view === 'ipo' ? <IpoFeed /> : <DividendFeed />`).
**컴포넌트 전체를 끄면 KR IPO 피드까지 죽는다.** 🔴 **배당 토글만 파킹하고 IPO는 그대로 살린다.**
- 🔴 `IpoFeed`가 `dividends`·`stocks`를 **안 쓰는지 코드로 먼저 확인**한다(안 쓸 것으로 보이나 확인 없이 진행 금지)
- 🔴 파킹 후 **KR IPO 탭이 정상 렌더되는지 로컬에서 눈으로 확인**한다(C-3 ㉡ — *"확인했다고 생각했지만 실제로는 안 봤다"*)

그 밖에:
- 🔴 **「이관」과 「삭제」를 구분한다.** 데이터는 옮기는 것이고 DB 테이블만 없애는 것이다 — README에 그 구분을 명시
- 🔴 **「0행이니 안전」 가정 금지** — 이미 `dividends`에서 틀렸다

### ⓪-4. 🔴 반증 조건

| 관측 | 결론 |
|---|---|
| 덤프·파킹·DROP 전부 통과 | ✅ 완료 — before/after 수치로 증명 |
| 🔴 **`IpoFeed`가 대상 테이블을 쓴다** | 🔴 **중단하고 보고** — 파킹 범위를 다시 그어야 한다 |
| 🔴 **파킹 후 KR IPO 탭이 깨진다** | 🔴 **되돌리고 보고**(커밋 전에 발견해야 한다) |
| 🔴 **`us_*`·`kr_stock_snapshot`에 변화가 생긴다** | 🔴 **되돌리고 보고** — 경계를 잘못 그은 것 |
| DROP 중 FK·뷰 의존이 걸린다 | 의존 역순으로 재시도, 순서를 기록 |
| 🔴 **파일럿 계보가 11개보다 많다** | 목록에 추가(`created_at` 2026-06-25 단서로 전수) |

### ⓪-5. 3번 생각
1. **범위** — **데이터 이관 · 코드 파킹 · DB 제거 · 문서 정정**까지. 🔴 KR 재개발·US 신기능은 대상 아님
2. 🔴 **표본 — 전수.** 11개 테이블 + 뷰 + 참조 코드 전부. **상한 없음**
3. 🔴 **「전수의 조건」 적용** — 완료 증명을 **두 방법**으로: ① **코드 → DB**(고아 참조 0) ② **DB → 코드**(고아 테이블 0). 🔴 리딩방 제거 때 쓴 양방향 검증 그대로

---

## 1. 할 일 — 🔴 이 순서를 지킨다

### 1-1. 데이터 이관 (🔴 가장 먼저)
`spinoff/kr-pilot-2026-06-25/`에:
- **11개 테이블 전량 덤프**(JSON 또는 CSV — `stocks` 27행·`dividends` 60행·나머지 0행도 빈 파일로 남겨 존재를 기록)
- **`schema.sql`** — 11개 + 뷰 `stock_snapshot_v`의 DDL 원형
- **`README.md`** — 🔴 ① 무엇인가(2026-06-25 KR 파일럿) ② 왜 만들어졌나 ③ 왜 옮겼나(US 단독 전환 + 52일 갱신 0) ④ **복원 방법**(DDL 적용 + 덤프 로드 + 파킹 해제) ⑤ 🔴 **데이터 한계**(`payout_ratio` 전 행 NULL · `fiscal_year` 2019~2024 · 27종목뿐) ⑥ 재사용 시 확인할 것
- 🔴 **덤프 행수를 DB 실측과 대조해 검증한 뒤에만** 다음 단계

### 1-2. 🔴 코드 파킹 — 배당만, IPO는 살린다
`LOCALE_SOURCE_PLAYBOOK` §11 그대로.
- 🔴 **먼저 `IpoFeed`가 대상 테이블을 안 쓰는지 코드로 확인**(⓪-3b)
- `OfferingsFeed.tsx:33`의 **배당 분기만** 파킹 — 🔴 **`DividendFeed.tsx`·`app/api/dividend/feed/route.ts` 파일을 지우지 않는다.** 배선을 남기고 **호출만 끊는다**
- 🔴 KR 배당 탭이 사라진 자리를 **가짜로 채우지 않는다** — 토글 자체를 안 보이게 하거나, 이유를 밝히는 문구. 🔴 *"데이터 없음"*을 현재 상태처럼 위장하지 않는다
- `docs/PARKED_KR_DIVIDEND_ACTIVATION.md` 신설 — 기존 `PARKED_*_ACTIVATION.md` 4종과 같은 형식 · **활성화 체크리스트** 포함
- 🔴 **US 컴포넌트 diff 0** 확인

### 1-3. DB 제거
- 🔴 **의존 역순**(참조하는 것 → 참조받는 것). 뷰 `stock_snapshot_v` 먼저, `stocks`는 마지막
- 🔴 **`supabase/migrations/`에 마이그레이션 파일로 기록** — 임의 SQL 실행이 아니라 추적 가능한 변경으로
- 🔴 DROP 전 **각 테이블 행수를 한 번 더 기록**(덤프와 일치 확인)

### 1-4. 검증 — 🔴 양방향 + 눈으로
- ① 코드 → DB: **고아 참조 0**
- ② DB → 코드: **고아 테이블 0**(남은 테이블 중 코드가 안 쓰는 것 목록도 참고로)
- 🔴 **tsc clean · 전체 테스트 통과 · build 성공**
- 🔴 **`git diff --stat`으로 US 코드·`kr_stock_snapshot` 관련 코드 무변경 증명**
- 🔴 **로컬에서 실제로 열어본다** — KR Toolbox의 **IPO 탭 정상 렌더** · 배당 토글 처리 상태 · US Toolbox 무변화. 텍스트로 기록(C-3 ㉡)
- 🔴 `us_*`·`kr_stock_snapshot` **행수 before/after 불변** 수치로

### 1-5. 문서 정정 — 🔴 전수
- 🔴 **`MODEL_UNIVERSE_63` §3-1 전면 철회** — *"선행 작업이 하나뿐이고 그게 전부를 푼다(`financials` 0행)"*는 **KR 테이블 기준이었고 US에 해당사항 없음**. STEP1041의 "8~11위로 축소"(부분 철회)도 **전면 철회로 갱신**
- 🔴 `ROADMAP_V2` **F-4** — 배당 *"`dividends` 60/1,977행"* → **「US 재료 0건」**
- 🔴 `ROADMAP_V2` **F-4-3** — 순위 재산출(배당이 부도위험과 같은 0% 그룹). 정렬 규칙 그대로
- 🔴 `ROADMAP_V2` **F-5** — *"재료부족 3건의 처리 경로"*를 **둘로 분리**: 성장(7.7%, 있는데 부족) vs 배당·부도위험(아예 없음). H-4 경로가 다르다
- 🔴 **`probe_1039`에 정정 추가** — KR 전수 색출이 **데이터 스키마 축을 안 봤다**(이번 누락의 발생 경로). 🔴 *"이름에 KR이 없으면 패턴 검색에 안 걸린다"*를 「전수의 조건」 사례로 `_TEMPLATE` ⓪-5 §2에 한 줄 추가
- `SYSTEM_MAP.md`·`docs/INDEX.md`·`STATE.md` 반영 · `roadmap_v2.html` 동기화

### 1-6. 산출물
`docs/probe_1048_kr_pilot_parked.md` — 이관·파킹·제거·검증 기록 + before/after 수치 + 로컬 육안 기록 + 🔴 3중 규칙 4종

---

## 2. 커밋 전 게이트
🔴 **tsc clean · 전체 테스트 통과 · build 성공 · US 코드 diff 0 증명.** 게이트 7 · 게이트 9 · `CHANGELOG` 1048 · `STEP_LEDGER` 등재. 커밋·푸시.

## 3. DoD
- [ ] ⓪-1a 로드맵 여섯 층 대조표
- [ ] 1-1 덤프 11개 + DDL + README 6항목 · 🔴 **행수 검증 후 진행**
- [ ] 🔴 **1-2 `IpoFeed` 무관 확인 먼저** · 배당만 파킹 · **파일 삭제 0** · `PARKED_KR_DIVIDEND_ACTIVATION.md` 신설 · 가짜 채움 없음
- [ ] 1-3 의존 역순 DROP · 🔴 **마이그레이션 파일로 기록**
- [ ] 🔴 **1-4 양방향 검증 + 로컬 육안**(KR IPO 탭 정상 · US 무변화) · tsc·test·build · 행수 before/after
- [ ] 🔴 **1-5 문서 정정 전수** — §3-1 전면 철회 · F-4 · F-4-3 재산출 · F-5 분리 · `probe_1039` 정정 · `_TEMPLATE` 한 줄
- [ ] 🔴 **`kr_stock_snapshot`·`us_*` 무접촉 증명**
- [ ] 마지막에: 못 한 축 / 아직 안 함 / 철회·정정 / 미측정

🔴 **데이터는 옮기는 것이지 버리는 것이 아니다. 코드는 끄는 것이지 지우는 것이 아니다.**
🔴 **`OfferingsFeed` 하나에 IPO와 배당이 같이 있다 — 배당만 끈다. IPO를 죽이면 이 STEP은 실패다.**
