# STEP 1073 — 판정 **A 확정 반영** + 판정 **B 낱말 교체 후 확정** + 「확정된 모델」 목록 **정본화** (🟡 문서 전용 · 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 수동 실행 0)

> 🔴 **이 STEP은 「장은태가 이미 내린 판정을 문서에 옮기는 일」이다.** 새 판정을 만들지 않는다.
> 🔴 **단 하나 예외** — §4는 *"확정된 모델이 무엇인가"*를 **수집·대조**한다. 충돌이 나면 **판정하지 않고 판정 대기로 등재**한다.
>
> **왜 지금인가 — 3번 규칙(2026-08-19):**
> **축 A(원문)** — `BUILD_SEQUENCE.md:139` *"🔴 **A와 B가 먼저다.** C~F는 전부 그 아래에 달려 있다."* · `:134` B에 걸린 것 = *"다음에 착수할 대상 전부"*. 그 둘이 이 STEP에서 답을 받는다.
> **축 B(실무)** — STEP1055~1072 **18개 중 「만드는」 STEP은 1055 하나**. 나머지 17개는 감사·정리. B가 안 풀려서 만들 수 없었다.
> **축 C(반대 증거)** — ①*"재료가 더 필요한가"* → `BUILD_SEQUENCE.md:226`·`:240` 둘 다 **"불필요"** ②STEP1072가 문서 어긋남을 이미 정리했다(`722778f`) ③🔴 **결정적** — 판정을 받고도 반영을 안 하면 다음 STEP이 또 미판정 문서를 읽고 또 헛돈다(§8 #3의 병).

---

## ⓪ 전제 점검

### ⓪-0. 🔴 축 정의 (먼저 고정)

| 축 | 실체 |
|---|---|
| **축 A — 원문** | **장은태 발언 기록**(이 명령서 ⓪-1) + 그 발언이 가리키는 **저장소 원문 좌표**. Cowork 요약·해설은 축 A가 아니다 |
| **축 B — 실무** | 이 저장소의 반영 방식 — **취소선 + 정정 + 좌표 병기** · *"정본 먼저, 표시본 나중"* · *"기존 파일 덮어쓰기, 새 파일 금지"*(`BUILD_SEQUENCE.md:346`) |
| **축 C — 반대 증거** | ㉠**이미 반영돼 있다**(중복) ㉡**반영 대상 좌표가 더 있다**(누락) ㉢**판정 문구가 원문과 다르다**(오기). 셋 다 확인 |

### ⓪-1. 🔴 장은태 판정 — 원문 그대로 (2026-08-19, AskUserQuestion + 대화)

| # | 판정 | 장은태 원문 |
|:--:|---|---|
| **A** | 제품 정의 우선순위 | **선택지 ① 채택** — *"①정의3(질문)이 최상위, 정의2(정직함)는 신뢰층, 정의1(기법나열)은 폐기"* (`BUILD_SEQUENCE.md:227` 원문 그대로). 답변 라벨: *"그대로 확정 반영"* |
| **B** | 다음에 착수할 대상 | 🔴 **선택지 문구 자체를 장은태가 고쳤다** — *"**1위를 제대로가 아니라 확정된 모델을 1개씩 완성을 하자**가 맞는말 아니야?"* |

🔴 **B는 "선택지 ①을 골랐다"가 아니라 "선택지 문구를 바꿨다"이다.** 반영도 그렇게 한다 — **선택지를 고르는 게 아니라, 물음과 답을 새 문구로 다시 적는다.**

### ⓪-2. 🔴 「1위」라는 낱말을 빼는 근거 3건 — 전부 저장소 원문

| # | 좌표 | 원문 | 뜻 |
|:--:|---|---|---|
| 1 | `docs/ROADMAP_V2.md:304` | *"배수 \| 🔴 **동점 4개(순위 아님)**"* | B 선택지 `:235`의 *"이미 있는 것(**배수 1~4위**)"*은 **낡은 서술**. 순위가 아니라 역할이 갈린 것(STEP1052) |
| 2 | `docs/ROADMAP_V2.md:429` | *"🔴 **적정주가/내재가치** \| ✅ **수요 1위** \| 🔴 **탈락** — 우리가 값을 매김 \| … \| 🔴 **만들지 않는다**"* | `MODEL_BUILD_ORDER:74`의 *"사용률 1위"*가 가리키던 그 1위는 **관문 ②에서 이미 죽었다** |
| 3 | `docs/BUILD_SEQUENCE.md:526` (§8 반복실수 #2) | *"관문 없이 「원전 대조 가능성」만으로 모델 선정 … **사용률 1위**가 「원전이라 부를 저작이 없다」는 이유로 밀려났다"* | **「1위」로 대상을 지목하는 방식 자체가 이미 반복 실수로 등재돼 있다** |

### ⓪-3. 🔴 「1개씩 완성」이 새 말이 아니라는 근거

`docs/BUILD_SEQUENCE.md:33` (§2 머리, 장은태 규칙 2026-08-15):
> *"한번에 몰아서 만든 게 7렌즈이고, 그렇게 하니 제대로 완성된 게 아니어서, 다시 검색한 후 **1개씩 만들고 완성 후 다음 거**를 만든다."*

🔴 **장은태의 08-19 발언은 08-15 자기 규칙과 같은 말이다.** 즉 B의 답은 **§2를 §6-B에 끌어와 못 박는 것**이지, 새 규칙 신설이 아니다.

### ⓪-4. 🔴 이 STEP이 열어야 할 파일 (전부 열고, 안 연 것은 "안 열었다"고 보고)

```
docs/BUILD_SEQUENCE.md        # 정본 — §1 LOCK · §6 표 · §6-부록 부록-1·2·5
docs/STATE.md                 # 최신 현황 정본 — ⑥ 다음에 할 일
docs/ROADMAP_V2.md            # F-4-1② · F-4-3(:468~470) · F-5(:503~511) · :304 · :429
docs/MODEL_BUILD_ORDER_2026-08-07.md   # :74 §3-2 원문 (B의 출처)
docs/LENS_DISPOSITION_2026-08-08.md    # §4 · §7-4 · §8-3 (E 종속)
docs/model_list_30_9.html     # 표시본(WHAT 작업 파일) — "완성까지 남은 것 — 가·다 확정 · 남은 것 셋"
docs/build_sequence.html      # 표시본
docs/roadmap_v2.html          # 표시본
```

---

## §1. 판정 A 확정 반영 — 정본 4곳

🔴 **문구는 `BUILD_SEQUENCE.md:227` 선택지 ①을 글자 그대로 쓴다. 바꿔 쓰지 않는다.**

확정 문장(이 문장을 그대로 복사해 쓸 것):
> 🟢 **판정 A 확정(2026-08-19 장은태)** — **정의3(질문구조, `USER_QUESTIONS_2026-08-08.md`)이 최상위 · 정의2(정직함, `MODEL_BUILD_ORDER_2026-08-07.md`)는 그 위에 얹히는 신뢰층 · 정의1(기법나열, `LENS_ROADMAP.md` 2026-07-03)은 폐기.**

### §1-1. `docs/BUILD_SEQUENCE.md` §6 표 (`:132` 부근 A행)
- A행의 *"셋 다 살아 있고 순위가 없다"* → **취소선** 후 위 확정 문장 + 좌표 `(STEP1073, 2026-08-19)` 병기.
- `#` 칸은 **A** 그대로 두고 머리에 `🟢`를 붙인다(번호 재배열 금지 — C~F가 A·B를 이름으로 참조한다).

### §1-2. `docs/BUILD_SEQUENCE.md` §6-부록 부록-1 (`:150~158`)
- 부록-1 결론 *"**결론: 부분 해소도 아니고 살아 있음.**"* → **취소선** 후 *"🟢 **2026-08-19 STEP1073 — 판정됨.** 장은태가 선택지 ①을 채택. 부록-1의 「살아 있음」 진단(승인 기록이 없다) 자체는 **그 시점 사실로 정확했다** — 이제 그 승인 기록이 생겼다."*
- 🔴 부록-1 본문 ㉠㉡㉢은 **지우지 않는다**. 그것이 A를 판정하게 만든 재료다.

### §1-3. `docs/BUILD_SEQUENCE.md` §1 (제품 정의 셋 + LOCK)
- §1 표의 세 정의 행에 **층 라벨**을 추가한다: 정의3 = `🟢 최상위(2026-08-19)` · 정의2 = `🟢 신뢰층(2026-08-19)` · 정의1 = `🔴 폐기(2026-08-19)`.
- 🔴 **정의1 행을 삭제하지 않는다.** 폐기 표시만 한다(이력 보존 — `git`이 아니라 문서가 스스로 말해야 한다).
- 🔴 LOCK 4항목은 **손대지 않는다**(창작 금지 조항 포함).

### §1-4. `docs/STATE.md` ⑥
- `STATE.md`에서 판정 A·B를 *"미판정"*으로 적은 줄(§6 마지막 불릿, `BUILD_SEQUENCE` 신설 항목 안의 **A**·**B** 서술)을 찾아 **취소선 + 확정 문장**으로 갱신.
- 🔴 `STATE.md:219` 부근 *"A를 미판정 칸으로 나열"*이 부록-1의 근거였다. **그 줄을 반드시 잡아야 부록-1과 STATE가 어긋나지 않는다.**

---

## §2. 판정 B — 낱말 교체 후 확정

### §2-1. 🔴 물음 자체를 다시 적는다 (`docs/BUILD_SEQUENCE.md:134` §6 표 B행)

**옛 문구(취소선 처리, 지우지 않음):**
> ~~「새 모델 1개를 고른다」 vs 「이미 있는 것 중 1위를 제대로 만든다」~~

**새 문구:**
> 🟢 **판정 B 확정(2026-08-19 장은태)** — **「확정된 모델을 1개씩 완성한다」.**
> 새 모델을 고르지 않는다. **이미 확정된 것 중 1개**를 골라 **완성**하고, 완성한 뒤에 다음 것으로 간다.
> 🔴 **「1위」라는 낱말은 뺀다** — 근거 3건: ①`ROADMAP_V2.md:304` 배수는 *"동점 4개(순위 아님)"* ②`ROADMAP_V2.md:429` 사용률 1위(적정주가)는 관문 ②에서 **탈락·만들지 않는다** ③`BUILD_SEQUENCE.md:526` §8 #2가 *"사용률 1위가 밀려났다"*를 **반복 실수로 등재**.
> 🔑 **이 답은 새 규칙이 아니다** — `BUILD_SEQUENCE.md:33`(§2 장은태 규칙 2026-08-15) *"1개씩 만들고 완성 후 다음 거를 만든다"*를 §6-B에 못 박은 것이다.

### §2-2. `docs/BUILD_SEQUENCE.md` §6-부록 부록-2 (`:168~174`)
- *"**결론: 살아 있음.**"* → **취소선** 후 *"🟢 **2026-08-19 STEP1073 — 판정됨.**"*
- 🔴 부록-2의 *"「확인」과 「판정」을 구분"* 문단은 **지우지 않는다.** 그 구분이 옳았음이 이번에 증명됐다 — F-4-3이 *"사실로 확인됐다"*고 쓴 문장을 판정으로 삼았다면 **「1위」라는 틀린 낱말째로 확정될 뻔했다.** 이 사실을 부록-2 끝에 한 줄로 남긴다.

### §2-3. `docs/BUILD_SEQUENCE.md` 부록-5 판정 B 다섯 칸 (`:232~240`)
- *"무엇을 정해야 하나"* 칸: *"새 모델 1개를 고를지, 이미 있는 것(**배수 1~4위**) 중 1위를 제대로 만들지"* → 취소선 + `🟢 판정됨(§6-B)`.
- *"선택지"* 칸 ①의 *"「1위를 제대로」"* → 취소선 + *"→ 🟢 「**확정된 모델을 1개씩 완성**」으로 교체 확정"*.
- 🔴 다섯 칸 구조 자체는 **유지**한다(재료 이력).

### §2-4. `docs/ROADMAP_V2.md` F-4-1② · F-4-3 (`:438`·`:468~470`)
- `:438`의 *"🔴 **판정 = 장은태.**"* → *"🟢 **2026-08-19 판정 완료(STEP1073)** — 「확정된 모델을 1개씩 완성한다」. 단 **「1위」라는 낱말은 채택되지 않았다**(근거 3건 = `BUILD_SEQUENCE.md` §6-B)."*
- `:470`의 *"F-4-1②(「새 모델 찾기가 아니라 1위를 제대로 만들기」)가 사실로 확인됐다"* → **취소선 금지**(실측 사실은 그대로 맞다). 대신 **바로 뒤에 한 줄 추가**: *"🔴 2026-08-19 정정 — 이 문장의 **전반부(「새 모델 찾기가 아니다」)는 판정으로 채택**됐고, **후반부의 「1위」는 채택되지 않았다.** 좌표=`BUILD_SEQUENCE.md` §6-B."*

### §2-5. `docs/MODEL_BUILD_ORDER_2026-08-07.md:74`
- *"🔴 이 판단은 장은태 몫이다."* 뒤에 **한 줄 추가**: *"🟢 **2026-08-19 판정 완료** — 「확정된 모델을 1개씩 완성한다」. 「이미 있는 것 중 **사용률 1위**」라는 이 줄의 표현은 채택되지 않았다(§5가 스스로 기록한 반복 실수와 같은 뿌리). 좌표=`BUILD_SEQUENCE.md` §6-B."*

---

## §3. 종속 해제 — C~F 재분류 (🔴 판정하지 않는다, **상태만 바꾼다**)

`BUILD_SEQUENCE.md` 부록-3 종속표를 그대로 쓰되, **A·B가 풀렸으므로 「종속」 딱지를 뗀다.** 각 행에 결과 한 줄:

| 항목 | A·B 확정이 준 것 | 새 상태 |
|---|---|:--:|
| **C** 역DCF 위치 | B가 *"새 모델을 고르지 않는다"*로 답함 → 역DCF는 **이미 확정된 모델(가-1 병합)의 일부**이지 새 모델이 아니다 | 🟡 **착수 가능** — 단 `REVDCF_ENABLED` Production OFF는 **유지**(별도 승인 필요) |
| **D** `ROADMAP.md` 재작성 내용 | A가 최상위 정의를 줌 → 재작성 기준선 확보 | 🟡 **착수 가능** |
| **E** 7렌즈 | 🔴 **E의 「거취」 프레임이 틀렸다** — 감사 일곱의 목적은 처분이 아니라 `CLAUDE.md:81` 규칙 3(**WHAT에 쓸 수 있는지 재사용 자격 심사**)이었고, `STEP1066.md` 머리가 스스로 *"규칙 3의 종착"*이라 적었다. **답은 이미 나와 있다** → §4-0으로 옮긴다 | 🟢 **답 있음** — §4-0. 남는 것은 `LENS_DISPOSITION §4`의 **배치**(어느 칸에 두나)뿐 |
| **F** Q1 착수 전 4건 | B가 *"확정된 모델 1개 완성"*으로 답함 → **Q1이 그 1개인지는 §4가 정한다** | 🔴 **§4 결과 대기** |

🔴 **금지** — 이 STEP에서 C·D·E·F를 **실제로 착수하지 않는다.** 상태 라벨만 바꾼다.
🔴 **`Q1_ENABLED`·`REVDCF_ENABLED` 무접촉.** 환경변수 조회도 하지 않는다.

---

## §4. 🔴 「확정된 모델」 목록 정본화 — B의 답이 즉시 요구하는 것

B가 *"**확정된** 모델을 1개씩"*이므로 **「무엇이 확정됐는가」의 목록이 정본에 있어야 한다.** 지금은 **표시본에만 있다.**

### §4-0. 🔴 7렌즈 감사 결과 = 「확정된 모델」의 **첫 재료** (2026-08-19 장은태 지적으로 바로잡음)

🔴 **바로잡은 것**: 7렌즈 감사(STEP1057·1059·1061·1062·1063·1064·1066)는 **판정 E(7렌즈 거취/처분)** 아래에서 돈 것이 아니다. **`CLAUDE.md:81` 규칙 3** — *"기존 7렌즈를 검증 없이 가져가지 않는다 … ①확정된 질문에 답하는지 ②결함을 실측으로 뜯은 뒤에만 **재사용**"* — 즉 **WHAT에 쓸 재료가 있는지 점검**한 것이다. `STEP1066.md` 머리가 스스로 *"규칙 3의 종착 … 감사 일곱이 무엇을 위한 것이었는지의 종착점"*이라 적었다.

**그러므로 §4의 「확정된 모델」 목록은 이 결과부터 넣고 시작한다.** 아래는 Cowork이 `LENS_DISPOSITION_2026-08-08.md` §8-1·§8-2·§8-3·§8-4 + `docs/model_list_30_9.html` 「마」 절에서 **그대로 옮긴 것**(새 판단 0):

| 렌즈 | WHAT 칸 배정 | 넣기 전 고칠 것 | 좌표 |
|---|---|---|---|
| **F스코어** | ✅ **재무건전성** 단독 모델 | **없음(당장)** — 신규 결함 0건 | §8-1·§8-3 · `ROADMAP_V2.md` W-2-4 |
| **모멘텀** | ✅ **최근변화** | 고쳐야 2 — #7 `avg(r1,r3)` 창작+이중계산(`lenses.ts:176`) · #9 `adjUsed` 미노출(`lensCompute.ts:129~130`) | §8-3 |
| **기술** | ✅ **최근변화**(모멘텀과 같은 칸) | 고쳐야 1 — #15 `pos52` 최소표본 가드 없음(`lenses.ts:201~204`) | §8-3 |
| 밸류 | 🔵 **추가 아님** — Q1 별개 경로, 가-1(배수↔역DCF 병합, 2026-08-17 확정)에 이미 포함 | (Q1 트랙에서 처리) | `model_list_30_9.html` 가-1 |
| 퀄리티 | ❌ **관문①에서 걸림** — 새 질문 후보였다가 기각(SWS `Past Performance` 원문대조 불일치, STEP1056) | — | §8-1·§8-4 |
| 저변동 | ❌ **관문①에서 걸림** — 3개 외부소스 전부 변동성 축 대응 0 | — | §8-4 |
| 자산성장 | ❌ **관문①에서 걸림** — "커지는가"와 "무리하게 커지는가"는 다른 질문 | — | §8-4 |

🔑 **결함 16건 중 「사용 차단」급 0건**(§8-2 마지막 줄) — *"쓸 수 있느냐"*는 **끝난 물음**이다.

🔴 **단, 셋 다 「배정 확정」이지 「완성 승인」이 아니다.** 반대 증거 셋을 §4-3 표에 **반드시 함께 적는다**:
- **F스코어** — 커버리지 전제가 2026-08-18에 뒤집혔다(라이브 **71.9%**(718/999) vs SEC 단독 **32.3%**). `model_list_30_9.html` 「나」가 *"위 선택지 셋은 SEC 전제라 다시 놓아야 한다"*로 **판정 대기**.
- **기술** — 손계산 **구조적 불가**(원시 종가 DB 미저장 + 외부 재취득 금지).
- **F스코어** — 손계산 **구조적 불가**(SEC 캐시가 종목당 1개년, 3년 요건 미충족).
- 공통 — `BUILD_SEQUENCE.md:106` *"현재 **9/9 통과한 모델은 0개**다."*

🔴 **이 STEP은 위 셋 중 무엇을 먼저 완성할지 정하지 않는다.** 목록에 올리는 데까지다.

### §4-0b. 🔴 §8-3과 `LENS_COMPLETION_STANDARD`의 충돌 — 판정 대기로 등재
`LENS_COMPLETION_STANDARD`가 *"7렌즈 9항목 ✅"*라 적은 것과, 감사가 낸 *"3개 렌즈는 손계산 **구조적 불가**"*가 **정면 충돌**한다. 🔴 **이 STEP이 정하지 않는다** — `LENS_DISPOSITION_2026-08-08.md` §7-4 C에 신규 항목으로 등재만 한다(이미 등재돼 있으면 좌표만 확인하고 중복 등재 금지).

### §4-1. 실측된 어긋남 (Cowork이 2026-08-19에 직접 확인)

| 좌표 | 내용 |
|---|---|
| `docs/model_list_30_9.html` (표시본, WHAT 작업 파일) | *"**완성까지 남은 것 — 가·다 확정 · 남은 것 셋**"* · 가-1 *"✅ **병합 확정** 2026-08-17 장은태"*(배수↔역DCF) · 가-3 *"**DDM 병합 확정(2026-08-17)**"* |
| `docs/ROADMAP_V2.md:511` (정본, STEP1072가 남긴 주석) | *"`docs/model_list_30_9.html`이 이 넷을 **「2026-08-17 장은태 확정」**으로 서술하고 있으나, 그 확정이 위 F-5 행에는 **반영된 적 없다** … **어느 쪽이 맞는지는 이 STEP이 정하지 않는다**"* |

### §4-2. 할 일 — 전수 수집

1. `docs/model_list_30_9.html`에서 **「확정」이 붙은 항목을 전수 추출**한다(문자열 `확정` 전건 · 앞뒤 문맥 포함 · 개수 명시).
2. 각 항목마다 **git 이력으로 출처를 추적**한다:
   - `git log --oneline --all -- docs/model_list_30_9.html`
   - 편입 이전 이력이 없으면(STEP1069가 `~/Downloads`에서 편입) **"저장소 이력 없음"**이라고 명시한다. 🔴 **추측 금지.**
3. 각 항목을 **정본 좌표와 대조**한다 — `ROADMAP_V2.md` F-5 / W-2 / `LENS_DISPOSITION` / `MODEL_ROSTER.md`.
4. 결과를 3분류한다:
   - **㉠ 정본에 이미 있음** → 아무것도 안 함
   - **㉡ 표시본에만 있고 정본에 없음 · 출처 추적됨** → 🟢 **정본에 등재**(F-5 해당 행 또는 W-2)
   - **㉢ 표시본에만 있고 출처 추적 불가** → 🔴 **판정 대기로 등재**(`LENS_DISPOSITION_2026-08-08.md` §7-4 C에 신규 항목). **정본에 올리지 않는다.**

### §4-3. 산출 — 「확정된 모델」 한 장 (정본에 신설, 새 파일 아님)

`docs/BUILD_SEQUENCE.md` **§6-B 바로 아래**에 표 하나를 추가한다:

| 칸 | 채울 것 |
|---|---|
| 모델/칸 이름 | 예: 「지금 사면 비싸게 사는 걸까」(배수+역DCF 병합) |
| 확정 근거 | 날짜 + 좌표 (`git` 해시 또는 문서:줄) |
| 확정의 범위 | **구조 확정**인가 **완성 승인**인가 — 🔴 이 둘을 반드시 가른다 |
| 9항목 검증 | 통과 개수 (`BUILD_SEQUENCE.md:106` *"현재 9/9 통과한 모델은 0개다"* 기준) |
| 다음에 필요한 것 | 한 줄 |

🔴 **§4-0의 셋(F스코어·모멘텀·기술)은 이 표의 첫 세 행으로 먼저 올린다.** 그다음에 §4-2의 추출 결과를 붙인다.
🔴 **이 표는 "무엇을 먼저 만들지"를 정하지 않는다.** 목록만 만든다. 🔴 **순서·우선순위 판정 = 장은태(다음 STEP).**

---

## §5. 표시본 동기화 (🔴 정본 먼저, 표시본 나중)

§1~§4가 **전부 끝난 뒤에만** 손댄다.

| 표시본 | 반영할 것 |
|---|---|
| `docs/build_sequence.html` | §1·§2·§3·§4의 정본 변경 전부 |
| `docs/roadmap_v2.html` | §2-4의 F-4-1②·F-4-3·F-5 변경 |
| `docs/model_list_30_9.html` | §4-2 분류 결과 — ㉡은 *"🟢 정본 등재됨(좌표)"*, ㉢은 *"🔴 판정 대기(좌표)"* 라벨 부착. 🔴 **내용을 지우지 않는다** |

🔴 **`~/Downloads/`의 파일은 건드리지 않는다**(읽기 전용, 사용자 파일).

---

## §6. 검증 3 (🔴 셋 다 하고, 못 한 것은 "못 했다"고 쓴다)

| # | 검증 | 방법 | 통과 기준 |
|:--:|---|---|---|
| **1** | **문구 일치** | §1·§2에서 쓴 확정 문장을 `BUILD_SEQUENCE.md:227`·장은태 원문(⓪-1)과 **글자 단위 대조** | 낱말 하나도 다르지 않음. 🔴 특히 **「폐기」·「확정된」·「1개씩」·「완성」**은 원문 낱말 |
| **2** | **누락 없음** | `grep -rn "미판정\|판정 대기\|장은태 몫\|판정 = 장은태" docs/*.md`로 **A·B를 아직 미판정으로 적은 줄이 남아 있는지** 전수 확인 | A·B 관련 잔존 0건. 다른 판정(C~F·§7-4)의 미판정 표기는 **남아 있는 게 정상** |
| **2-b** | **E 프레임 정정** | `BUILD_SEQUENCE.md` §6-E · 부록-3 · `LENS_DISPOSITION §4`에서 7렌즈를 **「거취/처분」**으로만 적은 서술을 찾아, *"감사의 목적은 `CLAUDE.md` 규칙 3(재사용 자격)이었다"*는 좌표를 **병기**한다 | 🔴 **기존 서술을 지우지 않는다.** 병기만 |
| **3** | **역방향** | 새로 쓴 확정 문장에서 출발해 *"이게 맞다면 어디가 더 바뀌었어야 하나"*를 역추적 — 최소 `STATE.md`·`ROADMAP_V2.md`·`MODEL_BUILD_ORDER`·`LENS_DISPOSITION` 넷 | 넷 전부 확인. 안 바뀐 곳은 **"왜 안 바꿔도 되는지"** 한 줄 |

---

## §7. 검수 3

| # | 검수 | 무엇을 본다 |
|:--:|---|---|
| **1** | **창작 안 했는가** | 이 STEP이 쓴 문장 중 **장은태 원문·저장소 원문에 없는 판단**이 섞였는지. 섞였으면 **삭제하거나 "Cowork 추정"이라 명시** |
| **2** | **새 파일 안 만들었는가** | `git status`로 신규 파일 확인. 허용되는 신규는 **이 명령서 하나**(`docs/step_orders/STEP1073.md`). 그 외 신규 파일이 있으면 **기존 파일로 합친다** |
| **3** | **판정 안 했는가** | §4-2 ㉢(출처 추적 불가)을 **정본에 올리지 않았는지** · §3에서 C~F를 **실제로 착수하지 않았는지** · `Q1_ENABLED`·`REVDCF_ENABLED`를 **건드리지 않았는지** |

---

## §8. 커밋 + 보고

### §8-1. 커밋
- 🔴 **`main`에 직접 커밋·push.** 브랜치·PR 만들지 않는다(STEP1056 §2-7, 예외 없음).
- 🔴 **`git worktree` 만들지 않는다**(STEP1070 신설 조항). 작업 폴더는 `/Users/maegbug/stock-terminal` 하나다.
- 커밋 메시지: `docs: STEP1073 판정 A·B 확정 반영 + 확정된 모델 목록 정본화`
- 🔴 **수정 금지 경로 재확인** — `data/us_symbols.json` · `.github/workflows/**` · `vercel.json` · `supabase/migrations/**`. 이 STEP은 **`docs/` 밖을 건드리지 않는다.**

### §8-2. 보고 (이 형식 그대로)

```
[STEP1073 결과]
① 판정 A 반영: 4곳 (BUILD_SEQUENCE §6표 / 부록-1 / §1 / STATE.md) — 성공/실패
② 판정 B 반영: 5곳 (§6표 / 부록-2 / 부록-5 / ROADMAP F-4-1②·F-4-3 / MODEL_BUILD_ORDER:74) — 성공/실패
③ C~F 상태 라벨 변경: 4건
④ 「확정」 전수 추출: N건 → ㉠정본에있음 a건 · ㉡정본등재 b건 · ㉢판정대기등재 c건
⑤ 「확정된 모델」 표: 행 수 N — 각 행의 「구조 확정 / 완성 승인」 구분 결과
⑤-b 7렌즈 반영(§4-0): 재무건전성=F스코어 / 최근변화=모멘텀+기술 — 표에 올렸나 · 「고칠 것 3건」 명시했나 · 반대증거 넷 병기했나
⑥ 표시본 동기화: 3개 파일
⑦ 검증 3: 1)문구일치 2)누락없음 3)역방향 — 각각 통과/실패/못함
⑧ 검수 3: 1)창작 2)새파일 3)판정 — 각각 통과/실패
🔴 못 한 것: (전부 나열. 없으면 "없음")
🔴 새로 생긴 판정 대기: (항목 + 좌표. 없으면 "없음")
커밋: <해시>
```

🔴 **⑤에서 「완성 승인」이 하나도 없으면 그렇게 보고한다.** `BUILD_SEQUENCE.md:106`이 *"현재 9/9 통과한 모델은 0개다"*라 적어뒀으므로 **그 결과가 나오는 것이 정상일 수 있다** — 억지로 채우지 않는다.
