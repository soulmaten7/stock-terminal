# STEP 1046 — 재료 실상 정정 · 파이프라인 지도 · 판정 대기 한 장 (🔵 조사·대조 · 코드 diff 0 · 화면 무변경)

> 🔴 **Cowork 실측(2026-08-15)**: `probe_1045`가 성장 재료를 *"이미 완성"*이라 적었으나 **520종목 / 오늘 유니버스 5,820 = 8.9%**다. 같은 표가 배당을 *"🔴 재료 갭(60/1,977 = 3%)"*으로 적고 있다 — **같은 종류를 다르게 표기했고, 정렬 규칙 ③(재료)이 순위를 직접 움직인다.**
> 🔴 **이 STEP의 진짜 목적은 마지막 §1-4다** — 지금 병목은 명령어가 아니라 **판정**이다. 흩어진 판정 대기를 한 장으로 모은다.
> 🔵 코드·DB 쓰기 0. DB 읽기·코드 읽기는 한다.

---

## ⓪ 전제 점검

### ⓪-1. 직전 산출물 — 원문 인용
| 출처 | 인용 |
|---|---|
| `probe_1045` F-4-3 갱신 | *"성장 행 추가 — **재료는 이미 완성**(`revdcf_results.sales_growth`)"* 🔴 실측 8.9% |
| Cowork DB 실측(오늘) | `revdcf_results.sales_growth` distinct symbol **520** · `us_valuation` 최신 as_of distinct symbol **5,820** · 상관 재현 **r = −0.093**(518종목, `probe_1045`의 −0.10과 정합) |
| `ROADMAP_V2` F-4-3 | 배당 = *"🔴 **재료 갭**(`dividends` 60/1,977행)"* |
| `REVDCF_SPEC.md:2048` | 🔑 *"driver1 교체 시… **부호반전(과거CAGR↔전망 부호 상이) 55/515(10.7%)**"* |
| `app/api/cron/revdcf/route.ts:333` | `sales_growth: dr.drivers.salesGrowth` — 🔴 **역DCF 크론이 성장 재료도 함께 만든다** |
| `STATE.md:60` | `REVDCF_ENABLED` Production **OFF**(불변) · `Q1_ENABLED` 기본 **OFF** |
| STEP1044 발견 | `us_sector_wide` — 한 파이프라인이 여러 칸을 조용히 좌우한 첫 사례 |

### ⓪-1a. 🔴 로드맵 원문 대조 (`docs/ROADMAP_V2.md`)
| 층 | 걸리는 절 | 인용 |
|---|---|---|
| **WHY** | 조건 1(정확) | 재료 상태를 실제보다 좋게 적으면 순위가 틀린다 |
| **HOW** | H-4(막혔을 때 4경로) | 🔴 성장·배당·부도위험이 전부 **재료에서 막힌** 상태 — H-4 대상인지 판정 |
| **WHAT** | W-2-4(성장 복원) | 재료 서술 정정 |
| **관문·순위** | F-4-3 순위표 | 🔴 **정렬 규칙 ③(재료)이 바뀌면 순위가 바뀐다** |
| **완성의 정의** | **C-1 항목 6(주장 정합)** | 🔑 *"검증 범위·실패 모드·적용 조건이 공개 문서와 무모순"* — 부호 상충 10.7%가 여기 걸린다 |
| **수익 모델** | M-2(커버리지가 여기로 온 이유) | 커버리지가 수익 계층 조건이므로 8.9%가 걸릴 수 있다 — 확인만, 판정 안 함 |

### ⓪-1b. 🔴 기존 답 확인 — 재조사 금지
`probe_1040`~`1045` · `ROADMAP_V2` · `STATE.md` · `REVDCF_SPEC` · `SECTOR_AXIS_APPLICABILITY` · `Q1_*` · `KNOWN_ANSWERS.md`
🔴 **새 조사를 하지 않는다.** 이 STEP은 **정정·대조·수집**이다. DB 실측은 재료 상태 확인에만.
🔴 이 칸이 비면 미완성.

### ⓪-2. 슬롯 — 없다

### ⓪-3. 걸리는 금지사항
- [x] 🔵 **코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 호출 0**
- [x] 🔴 **`Q1_ENABLED`·`REVDCF_ENABLED` 무접촉** · 환경변수 VALUE 타이핑 금지
- [x] 🔴 **판정 금지** — §1-4는 **모으는 것**이지 정하는 것이 아니다. 🔴 하나라도 판정하면 이 STEP 실패
- [x] 🔴 **표시 설계 금지** · **창작 금지** · **선택지 나열 금지** · **KR 전면 동결**
- [x] `data/us_symbols.json`·`.github/workflows/**`·`vercel.json` 수정 금지

### ⓪-3b. 🔴 결함의 소재 확정
- 🔴 **「재료가 있다」와 「재료가 충분하다」를 섞지 않는다.** 8.9%는 **있는데 부족한** 상태다. F-4-3이 칸마다 **같은 단위(종목수/유니버스 %)**로 적히게 통일한다
- 🔴 **「칸」과 「파이프라인」은 다른 지도다.** 정보 구조상 독립인 두 칸이 재료는 한 크론에 묶일 수 있다 — 이번에 지도를 그린다
- 🔴 **§1-4는 판정을 「대신」하는 것이 아니라 「모으는」 것이다.** 각 항목에 *"무엇을 정하는 것인지"*만 한 문장으로 쓴다

### ⓪-4. 🔴 반증 조건

| 관측 | 결론 |
|---|---|
| 재료를 같은 단위로 통일하니 **순위가 바뀐다** | ✅ 서술 오류가 순위를 왜곡하고 있었다 — 바뀐 순위와 원인을 적는다 |
| 순위가 안 바뀐다 | 표기만 정정하고 순위는 유지 — 🔴 **"안 바뀌었다"를 명시** |
| 🔴 **한 파이프라인에 묶인 칸이 2개 이상 더 나온다** | 🔑 `us_sector_wide`·`revdcf`에 이은 **패턴** — 전수 지도가 이 STEP의 수확 |
| 🔴 **플래그 OFF가 재료 갱신까지 멈추는 칸이 있다** | 🔴 **의도된 것인지 부작용인지 코드로 확인**하고 구분해 적는다 |
| 부호 상충 10.7%가 **오늘 재료에서도 재현된다** | 🔴 C-1 항목 6 위반 후보로 등재(판정은 안 함) |
| 🔴 **판정 대기가 10건을 넘는다** | 🔴 그 자체가 보고 대상 — **의존 순서**를 매겨 무엇부터 정해야 하는지 적는다 |

### ⓪-5. 3번 생각
1. **범위** — **정정·지도·수집**까지다. 🔴 판정·구현·표시는 이 STEP이 아니다
2. 🔴 **표본 — 칸 전수·판정 대기 전수.** 문서·DB 읽기라 비용이 낮다. **상한 없음**
3. 🔴 **「전수의 조건」 적용** — 판정 대기 수집을 **두 방법**으로: ① **문서 검색**(「판정 대기」·「장은태」·「미결」·「보류」 표기 전수) ② **구조 역산**(각 칸·각 층에서 *"이게 정해져야 다음이 되는데 안 정해진 것"*). 🔴 **차집합을 보고**

---

## 1. 할 일

### 1-1. 재료 상태 통일 — 🔴 같은 단위로
`ROADMAP_V2` F-4-3의 **모든 행**에 재료를 **`보유 종목수 / 오늘 유니버스 = %`** 형식으로 통일한다.
- 🔴 **오늘 DB로 전부 다시 잰다**(문서 인용 금지). 유니버스 기준은 `us_valuation` 최신 `as_of`의 distinct symbol
- 성장 = 🔴 **8.9%(520/5,820)** — *"이미 완성"* 표기를 정정
- 정렬 규칙 ③(재료)을 통일된 값으로 다시 적용해 **순위를 재산출**한다. 🔴 바뀌면 바뀐 대로, 안 바뀌면 안 바뀌었다고 적는다

### 1-2. 🔑 재료 파이프라인 지도 (신규)
**각 칸 ← 어느 테이블 ← 어느 크론/스크립트 ← 어느 플래그**를 전수로 그린다.

| 칸 | 테이블 | 채우는 것(크론/스크립트/1회적재) | 걸린 플래그 | 최신 `as_of` | 🔴 다른 칸과 공유하나 |
|---|---|---|---|---|---|

- 🔴 **오늘 각 테이블의 최신 `as_of`를 DB로 확인**해 함께 적는다(멈춘 것이 있는지)
- 🔴 **이미 확인된 두 사례를 지도에 넣는다**: `us_sector_wide`(1회 적재, as_of 고정 — STEP973/974 설계) · `revdcf` 크론이 **역DCF와 성장 재료를 함께** 생산(`route.ts:333`)
- 🔴 **플래그 OFF가 재료 갱신을 멈추는가**를 코드로 확인한다 — 화면만 끄는지, 생산까지 끄는지. **의도/부작용 구분**

### 1-3. 주장 정합 점검 — C-1 항목 6
- `REVDCF_SPEC.md:2048`의 **부호 상충 10.7%(55/515)**가 오늘 재료에서도 나타나는지 확인 — 🔴 **DB로 재현 시도**(못 하면 「미측정」)
- 🔴 성장 카드(과거 CAGR)와 역DCF 카드(시장 내재 기대)가 **같은 화면에서 반대 방향을 가리키는 종목 수**를 센다
- 🔴 **처방을 만들지 않는다.** 사실과 규모만 적고 C-1 항목 6 위반 후보로 등재
- 그 밖에 **공개 문서의 주장과 실제가 어긋나는 것**을 이 기회에 같이 적는다(예: `sectorSource` 침묵 — 출처를 밝힌다는 주장 대비 실제)

### 1-4. 🔑 판정 대기 전수 — 한 장 (이 STEP의 목적)
🔴 **두 방법으로 모으고(⓪-5-3), 하나도 판정하지 않는다.**

| # | 판정 질문(한 문장) | 어디서 나왔나(좌표) | 재료 상태 | 이것이 막고 있는 것 | 선행 판정 |
|---|---|---|---|---|---|

- 🔴 **판정 질문 형태로 쓴다** — 배경 설명이 아니라 *"무엇을 정하는가"*
- 🔴 **의존 순서를 매긴다** — 어느 판정이 다른 판정의 전제인가(`probe_1040` 1-3 방식). **무엇부터 정해야 하는지가 이 표의 값이다**
- 🔴 **재료가 없어 지금 판정할 수 없는 것**은 그렇게 표시한다(판정 대기가 아니라 재료 대기)
- 최소 아래는 포함: 배수 4축 동점 · 조건부 4칸(405건) 표시 · 저변동성·자산성장 처분 · Beneish 칸 신설 · Q1 켜기 승인 · `REVDCF_ENABLED` · Piotroski 조건부 적용 범위 · 성장/배당/부도위험의 H-4 경로

### 1-5. 산출물
`docs/probe_1046_material_and_decisions.md`
- 맨 앞 🔑 **판정 대기 한 장**(의존 순서 포함) + ⓪-4 매트릭스 결과
- 1-1 통일된 재료표·재산출 순위 · 🔑 **1-2 파이프라인 지도** · 1-3 주장 정합 · 1-4 판정 대기 두 방법·차집합
- 🔴 3중 규칙 4종: 못 한 축(시도 기록) / 아직 안 함 / 철회·정정 / 미측정

🔴 `ROADMAP_V2.md` **F-4-3 재료·순위 정정** + `roadmap_v2.html` 동기화. 🔴 **판정 대기 한 장을 `ROADMAP_V2`에 F-5로 신설**한다(흩어진 것을 한 자리에).

---

## 2. 커밋 전 게이트
게이트 7 · 게이트 9 · `CHANGELOG` 1046 · `STEP_LEDGER` 등재. 🔵 배포 불필요, 커밋·푸시는 한다.

## 3. DoD
- [ ] ⓪-1a 로드맵 여섯 층 대조표 채움
- [ ] 1-1 F-4-3 **전 행**을 같은 단위로 · 🔴 **오늘 DB로 재측정**(문서 인용 아님) · 순위 재산출 결과 명시
- [ ] 🔑 **1-2 파이프라인 지도 전수** · 각 테이블 최신 `as_of` · 🔴 **플래그가 재료 생산까지 멈추는지 코드 확인** · 의도/부작용 구분
- [ ] 1-3 부호 상충 재현 시도(못 하면 「미측정」) · 🔴 **처방 만들지 않음**
- [ ] 🔑 **1-4 판정 대기 — 두 방법 · 차집합 · 🔴 의존 순서 · 🔴 하나도 판정하지 않음**
- [ ] `ROADMAP_V2` F-4-3 정정 + **F-5 신설** · html 동기화
- [ ] 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 플래그 무접촉
- [ ] 마지막에: 못 한 축 / 아직 안 함 / 철회·정정 / 미측정

🔴 **「재료가 있다」와 「충분하다」는 다르다. 같은 단위로 적어야 순위가 정직해진다.**
🔴 **이 STEP은 판정하지 않는다 — 판정할 것을 한 자리에 모으는 것이 전부다.**
