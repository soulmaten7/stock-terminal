# STEP 1047 — KR 파일럿 스키마 제거 (🟠 코드·DB 변경 있음 · 2단계 · 화면 무변경 목표)

> 🔴 **장은태 판정(2026-08-15)**: *"KR은 지금 빼는 게 맞아."*
> 🔴 **대상은 「KR 전체」가 아니라 「2026-06-25 파일럿 스키마」다.** `kr_stock_snapshot` 계열(2,776행, 실제 KR 보드)은 **동결이지 삭제가 아니다** — 🔴 **절대 건드리지 않는다.**
> 🔴 **2단계로 나눈다.** Phase A(증명)에서 **참조 0을 증명하지 못하면 Phase B(실행)를 중단**한다.

---

## 0-A. 왜 이 STEP인가

Cowork DB 실측(2026-08-15): `stocks` 테이블이 **전체 27행 · 전부 country='KR' · market='KOSPI'**이고, `created_at`이 **2026-06-25 12:50:42.076746+00 단 하나**다. 이를 `stock_id`로 참조하는 테이블이 **10개**이며 그중 **8개가 0행**이다.

| 테이블 | 행수 |
|---|---:|
| `stocks` | 27 (전부 KR) |
| `dividends` | 60 (KR 27종목) |
| `financials` | **0** |
| `ai_analysis`·`disclosures`·`insider_trades`·`news`·`quant_factors`·`short_credit`·`stock_prices`·`supply_demand` | **전부 0** |

🔴 **문제는 잔재의 존재가 아니라 US 판단에 인용된 것이다.**
- `MODEL_UNIVERSE_63` §3-1 *"선행 작업이 하나뿐이고 그게 전부를 푼다 — `financials` 0행"* → **KR 테이블이었다.** US 재무 원자료는 `us_fundamentals`가 이미 담당
- `ROADMAP_V2` F-4 *"`dividends` 60/1,977행"* → **분자도 분모도 US가 아니다.** US 배당 재료는 **0건**

🔑 **왜 안 잡혔나**: US 테이블은 전부 `us_` 접두어가 있는데(`us_valuation`·`us_fundamentals`·`us_sector_wide`) 이 둘은 없다. **이름이 시장 중립이라 공용처럼 읽혔다.** STEP1039의 KR 색출이 못 잡은 것도 같은 이유 — 이름에 KR이 없어 패턴 검색에 안 걸렸다(「전수의 조건」 사례).

---

## ⓪ 전제 점검

### ⓪-1. 직전 산출물 — 원문 인용
| 출처 | 인용 |
|---|---|
| Cowork DB 실측(오늘) | `stocks` 27행 전부 KR/KOSPI · `created_at` 단일 타임스탬프 · 참조 테이블 10개 중 8개 0행 · `dividends` `payout_ratio` **전 행 NULL** |
| `CLAUDE.md:31` | *"한국시장이라는 기준으로 된 모든 내용은 싹 다 **미뤄버려** … 이제부터 US만 만들 거야"* — 🔴 **「미룸」이지 「삭제」가 아님. 이번 삭제는 「파일럿 잔재」에 한정** |
| STEP1035~1039(리딩방) | 🔴 **선례**: `account/delete/route.ts`가 DROP 대상 9개 테이블을 참조하고 있었다 — **선제거 안 했으면 회원탈퇴 전체가 깨졌을 것** |
| `_TEMPLATE` ⓪-5 §2 | 「전수의 조건」 — 이름·패턴 검색은 "찾은 것"이지 "전부"가 아니다 |
| `probe_1046` | 판정 대기 14건(F-5) · 재료 단위 통일로 순위가 실제로 바뀌었음 |

### ⓪-1a. 🔴 로드맵 원문 대조 (`docs/ROADMAP_V2.md`)
| 층 | 걸리는 절 | 인용 |
|---|---|---|
| **WHY** | 조건 1(정확) | 틀린 재료 상태 위에서 순위를 매기고 있었다 |
| **HOW** | H-2(카탈로그가 먼저) · H-7(창작의 경계) | 🔴 **카탈로그가 DB 스키마를 안 담고 있었다** — 이번에 드러난 공백 |
| **WHAT** | W-2-3·W-2-4 | 배당 칸 재료가 0이면 칸 상태가 바뀐다 |
| **관문·순위** | F-4·F-4-3·F-5 | 🔴 **배당 재료 표기·순위·판정 대기 전부 정정 대상** |
| **완성의 정의** | C-1 항목 2(입력 검증 — 출처·결측률을 수치로) | 🔴 이 항목이 지켜졌으면 잡혔을 사안 |
| **수익 모델** | **없음** | 계층·가격 무관 |

### ⓪-1b. 🔴 기존 답 확인
`STEP1035`~`1039`(리딩방 제거 절차 — **그대로 재사용**) · `probe_1039`(KR 전수 색출, ⓒ 7건) · `SYSTEM_MAP.md` · `docs/INDEX.md` · `KNOWN_ANSWERS.md`
🔴 **절차를 새로 설계하지 않는다.** 리딩방 제거가 검증된 절차다.
🔴 이 칸이 비면 미완성.

### ⓪-2. 슬롯 — 없다

### ⓪-3. 걸리는 금지사항
- [x] 🔴 **`kr_stock_snapshot` 및 그 계열 무접촉** — 실제 KR 보드다. **동결이지 삭제가 아니다**
- [x] 🔴 **`us_*` 테이블 전부 무접촉**
- [x] 🔴 **Phase A에서 참조 0을 증명하기 전에는 어떤 DROP도 하지 않는다**
- [x] 🔴 **크론 수동 실행 금지** · `.github/workflows/**`·`vercel.json`·`data/us_symbols.json` 수정 금지
- [x] 🔴 **`Q1_ENABLED`·`REVDCF_ENABLED` 무접촉** · 환경변수 VALUE 타이핑 금지
- [x] 🔴 **화면 변경 0이 목표다** — 화면에 영향이 나오면 **중단하고 보고**(7렌즈 라이브 화면은 별도 승인)
- [x] 🔴 **법적 명시 문서(약관·개인정보처리방침) 수정 보류** — `PARKED_TERMS_PRIVACY_ACTIVATION.md` 그대로

### ⓪-3b. 🔴 결함의 소재 확정
- 🔴 **「KR 동결」과 「파일럿 잔재 제거」는 다른 일이다.** 이번은 **후자만**. `kr_stock_snapshot`·`kr-perf` 크론·KR 화면 코드는 대상이 아니다
- 🔴 **「0행이니 안전하다」고 가정하지 않는다.** 행이 0이어도 **코드가 참조하면 런타임에 깨진다**(리딩방 선례가 정확히 이것)
- 🔴 **삭제 전에 데이터를 보존한다.** 27+60행은 작다 — 덤프 비용이 사실상 0인데 되돌릴 수 없는 삭제다

### ⓪-4. 🔴 반증 조건

| 관측 | 결론 |
|---|---|
| 코드 참조 0 · 뷰/FK 0 | ✅ Phase B 진행 |
| 🔴 **참조가 하나라도 있다** | 🔴 **Phase B 중단.** 참조 목록과 선제거 계획만 내고 보고 — 🔴 **임의로 코드를 고쳐 진행하지 않는다** |
| 🔴 **12개 밖에 파일럿 잔재가 더 있다** | 목록에 추가 — `created_at` 2026-06-25 단서로 전수 |
| 🔴 **`kr_stock_snapshot` 계열과 FK로 얽혀 있다** | 🔴 **중단하고 보고** — 경계를 잘못 그은 것 |
| 화면·API에 영향이 나온다 | 🔴 **중단하고 보고** |
| 제거 후 배당 재료가 **0**으로 확정 | 🔴 F-4·F-4-3·F-5 정정 — 배당은 「재료 갭」이 아니라 **「재료 없음」** |

### ⓪-5. 3번 생각
1. **범위** — **파일럿 12개 제거 + 그로 인한 문서·순위 정정**까지다. 🔴 KR 서비스 코드·화면은 대상이 아니다
2. 🔴 **표본 — 전수.** 테이블 12개 · 코드 전체 · DB 오브젝트 전체. **상한 없음**
3. 🔴 **「전수의 조건」 적용** — 참조 확인을 **두 방법**으로: ① **이름 검색**(테이블명 문자열 grep — 🔴 따옴표 종류·백틱·문자열 조합 전부) ② **역방향**(코드가 부르는 테이블명을 모아 DB 실제 목록과 대조 — 고아 참조·고아 테이블 양방향). 🔴 **①만으로 「참조 0」이라 하지 않는다**

---

## Phase A — 증명 (🔵 읽기만 · 여기서 멈출 수 있다)

### A-1. 대상 확정
- `stocks` + `stock_id` 참조 10개 + `financials` = **12개**를 확정하고, 🔴 **`created_at`·마이그레이션 파일 날짜로 2026-06-25 계보인지 각각 확인**
- 🔴 **12개 밖에 같은 계보가 더 있는지** 전수 확인(⓪-4 셋째)
- 🔴 **`kr_stock_snapshot` 계열과의 경계를 명시적으로 긋는다** — FK·뷰로 얽혀 있으면 즉시 중단

### A-2. 🔑 코드 참조 전수 — 두 방법
- **방법 ①**: 12개 테이블명을 코드 전체에서 검색. 🔴 `.ts`·`.tsx`·`.sql`·`.json`·`.md` 전부. 🔴 **따옴표 종류(작은·큰·백틱)와 문자열 조합(`from("fin"+"ancials")` 류)까지** — STEP1039의 따옴표 누락(49 vs 57) 선례
- **방법 ②**: 코드가 `.from(...)`·`.rpc(...)`로 부르는 **테이블명을 전부 수집**해 DB 실제 목록과 대조 — **고아 참조 / 고아 테이블 양방향**
- 🔴 **회원탈퇴·관리자·크론·RLS 정책을 명시적으로 확인**(리딩방 선례)
- 🔴 **차집합을 보고**한다

### A-3. DB 오브젝트 전수
뷰 · FK · 인덱스 · 트리거 · RLS 정책 · 함수/RPC · 마이그레이션 파일에서 12개를 참조하는 것을 **전부** 찾는다. 🔴 `advisor_directory` 뷰 선례 — **뷰가 로그아웃 방문자에게 서빙되고 있었다**

### A-4. 🔴 판정: 진행 가능한가
- 참조 0 · 오브젝트 0 → **Phase B 진행**
- 하나라도 있으면 → 🔴 **여기서 멈춘다.** 목록·선제거 계획을 내고 보고. **코드를 임의로 고쳐 진행하지 않는다**

---

## Phase B — 실행 (🟠 A-4 통과 시에만)

### B-1. 보존 먼저
`docs/_archive/kr_pilot_2026-06-25/`에 **12개 테이블 전량 덤프**(JSON 또는 CSV) + `schema.sql`(DDL) + `README.md`(왜 만들어졌고 왜 지웠는지·복원 방법). 🔴 **덤프 검증 후에만 다음 단계**

### B-2. 코드 선제거 → DB DROP (🔴 이 순서)
1. 코드에서 참조 제거(있다면) → tsc·test·build 통과 확인
2. DB DROP — 🔴 **의존 역순**(참조하는 것부터), FK/뷰 먼저
3. 🔴 **마이그레이션 파일로 남긴다**(`supabase/migrations/`) — 임의 SQL 실행이 아니라 기록되는 변경으로

### B-3. 양방향 재검증
- 코드 → DB: 고아 참조 0
- DB → 코드: 남은 테이블 중 코드가 안 쓰는 것 목록(참고용)
- 🔴 **tsc clean · 전체 테스트 통과 · build 성공 · 화면 변경 0**(diff로 증명)
- 🔴 **`us_*`·`kr_stock_snapshot` 행수 불변 확인**(before/after 수치로)

### B-4. 문서 정정 — 🔴 전수
- 🔴 **`MODEL_UNIVERSE_63` §3-1 전면 철회** — *"선행 작업이 하나뿐"*은 **US에 애초에 해당사항 없음**. STEP1041의 "8~11위로 축소"(부분 철회)도 **전면 철회로 갱신**
- 🔴 `ROADMAP_V2` **F-4** 배당 재료 *"60/1,977행"* → **「US 재료 0건」**
- 🔴 `ROADMAP_V2` **F-4-3** 순위 재산출 — 배당이 부도위험과 같은 **0% 그룹**으로 이동. 정렬 규칙 그대로 적용
- 🔴 `ROADMAP_V2` **F-5** 판정 대기 갱신 — *"재료부족 3건의 처리 경로"*를 **둘로 분리**: 성장(7.7%, **있는데 부족**) vs 배당·부도위험(**아예 없음**). H-4 경로가 달라진다
- 🔴 `SYSTEM_MAP.md`·`docs/INDEX.md`·`STATE.md` 반영 · `roadmap_v2.html` 동기화
- 🔴 **`probe_1039`(KR 전수 색출)에 「데이터 스키마 축을 안 봤다」를 정정으로 추가** — 이번 누락의 발생 경로

### B-5. 산출물
`docs/probe_1047_kr_pilot_removal.md` — Phase A 증명(두 방법·차집합) · B 실행 기록 · before/after 수치 · 🔴 3중 규칙 4종

---

## 2. 커밋 전 게이트
🔴 **tsc clean · 전체 테스트 통과 · build 성공 · `git diff --stat`으로 화면 코드 무변경 증명.** 게이트 7 · 게이트 9 · `CHANGELOG` 1047 · `STEP_LEDGER` 등재. 커밋·푸시.

## 3. DoD
- [ ] ⓪-1a 로드맵 여섯 층 대조표
- [ ] 🔴 **A-1 경계** — `kr_stock_snapshot` 계열 무접촉 확인 · 12개 밖 계보 유무
- [ ] 🔑 **A-2 두 방법 · 🔴 따옴표·문자열 조합까지 · 회원탈퇴·RLS 명시 확인 · 차집합**
- [ ] A-3 뷰·FK·RLS·RPC·마이그레이션 전수
- [ ] 🔴 **A-4 판정 — 참조가 있으면 Phase B를 하지 않았다**
- [ ] B-1 덤프 + DDL + README, 🔴 **덤프 검증 후 진행**
- [ ] B-2 🔴 **코드 선제거 → DB DROP 순서** · 마이그레이션 파일로 기록
- [ ] B-3 양방향 재검증 · tsc·test·build · 🔴 **화면 변경 0 · `us_*`·`kr_stock_snapshot` 행수 불변**
- [ ] 🔴 **B-4 문서 정정 전수** — `MODEL_UNIVERSE_63` §3-1 전면 철회 · F-4 · F-4-3 순위 재산출 · F-5 분리 · `probe_1039` 정정
- [ ] 마지막에: 못 한 축 / 아직 안 함 / 철회·정정 / 미측정

🔴 **0행이어도 코드가 부르면 깨진다 — 리딩방 때 회원탈퇴가 그랬다.**
🔴 **참조 0을 증명 못 하면 멈춘다. 임의로 고쳐 진행하지 않는다.**
