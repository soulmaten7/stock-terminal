# STEP 1028 — 잃어버린 답을 되찾고, 다시 잃지 않게 색인한다 (조사 + 문서)

> 🔴 **오늘 밤 22:45 UTC W1 첫 시험은 STEP 1026이 읽는다.** `app/api/cron/revdcf/route.ts` **diff 0**.
> 🔴 이 STEP의 **W1이 성공하면 1026을 기다리지 않고 원인이 확정될 수 있다.**

---

## 0-A. 🔴 같은 조사를 여섯 번 했다

STEP 1027 발견:
> Vercel 런타임 로그 접근 문제를 **1017이 조사한 게 6번째**였다 — **892 → 907 → 911 → 913 → 933 → 1017**.
> 🔴 **STEP 911과 933은 이미 작동하는 답을 얻었는데 1017이 그걸 찾지 못했다.**

🔴 **답이 없어서 막힌 게 아니라, 있는 답을 못 찾아서 막혔다.** 그리고 그 결과 1017이 "A(revdcf heartbeat 부재) 확정 불가"로 끝났고, 1018이 4단계 heartbeat를 새로 배선했고, 지금 오늘 밤까지 기다리고 있다.

🔴 **3중 규칙의 「검수 3 — 이전발언 대조」가 여섯 번 다 실패했다.** 규칙이 없어서가 아니다. **대조 대상이 `REVDCF_SPEC.md` 2,117줄 + `CHANGELOG.md` 4,000줄+ + `STEP_LEDGER.md`라 매번 전문을 읽을 수 없기 때문**이다. 규칙은 있고 실행 수단이 없었다.

---

## ⓪ 전제 점검

### ⓪-1. 직전 STEP 산출물 — 원문 인용 (검산 포함)

| 출처 | 인용 | 검산 |
|---|---|---|
| STEP 1027 | `REVDCF_SPEC.md`(**2,117줄**)·`VALUATION_SPEC.md`(**404줄**) 전문 읽고 20개 슬롯 3자 대조 → **4개 불일치**(#2 shares · #14 rf · #15 ERP · #16 beta) | ✅ 4/20 = 20% |
| STEP 1027 🔑 | 4건 전부 **같은 패턴** — 스펙의 **원래 요약 표**(§5 B-1/B-2, **07-30 작성**)가 나중 STEP(839·849·904·999~1005)의 조달 변경을 반영 못 함. **스펙 자신의 나중 절에는 이미 올바른 정보가 있었다** | — |
| STEP 1027 🔑 | **카탈로그와 코드는 처음부터 정확했다.** 3자 충돌이 아니라 **스펙 요약 표가 자기 본문보다 뒤처진 것.** 🔑 **"스펙이 옳고 코드가 틀린" 사례 0건** | 🔑 구현 이탈 없음 |
| STEP 1027 | 수정: B-2 표 교정(rf/ERP → `ERPbymonth.xlsx`) · 베타 행 추가 · **§10-J 신설**(997~1026 통합) · 문서 위계 명시 | — |
| STEP 1027 🔴 | **Vercel 로그 조사 6회**(892→907→911→913→933→1017) · **911·933이 작동하는 답을 얻었다** | 🔴 W1 대상 |
| STEP 1027 | `FRED_API_KEY`는 `.env.local`에 **존재**하나 **`revdcf`가 전혀 안 쓴다**(무관한 매크로 표시 기능에서만 사용) | ✅ Cowork 추정과 일치 |
| STEP 1027 | `DATA_SOURCE_CATALOG.md`는 **수정 불필요** — 이미 정확했다 | — |
| 현재 | `lens_cuts` US 07-30 **19일째** · `us_sector_relative` 08-10 **6일째** · **1026 미실행**(오늘 밤 22:45 UTC 이후) | — |

### ⓪-2. 이 STEP이 건드리는 슬롯
🔴 **없다.** 관측 수단과 문서만 다룬다.

### ⓪-3. 걸리는 금지사항 (해당되는 것만)
- [x] 🔴 **프로덕션 코드 diff 0** — 산출은 문서 + (필요 시) 조회 스크립트
- [x] 🔴 **DB 쓰기 0** · **크론 수동 실행 금지**(특히 `email-brief` = 실제 메일 발송)
- [x] 🔴 **`app/api/cron/revdcf/route.ts` diff 0** — 오늘 밤 W1 첫 시험 보호
- [x] 🔴 **게이트 임계·`BUDGET_MS`·취득 경로 불변** · **7렌즈 라이브 화면 수정 금지**
- [x] 🔴 **패스워드·자격증명을 어떤 필드에도 입력하지 말 것.** Vercel 로그인이 필요하면 **장은태에게 요청**한다. 🔴 **토큰이 이미 환경에 있으면 존재 여부만 확인하고 VALUE를 출력하지 않는다**
- [x] `REVDCF_ENABLED` Production OFF 유지 · `data/us_symbols.json` · `.github/workflows/**` · `vercel.json` 수정 금지 · **KR 전면 동결**

### ⓪-3b. 🔴 결함의 소재 확정
- W1이 다루는 건 **관측 수단**(Vercel 로그 접근)이지 프로덕션 코드가 아니다
- 🔴 911·933이 쓴 방법이 **지금도 유효한지**를 먼저 확인한다. 무효면 "당시 유효, 현재 무효"로 적고 그 사실 자체를 색인에 남긴다

### ⓪-4. 🔴 반증 조건

**W1 — Vercel 로그**
| 관측 | 결론 | 다음 축 |
|---|---|---|
| 911·933 방법이 **지금도 작동** | 🔑 **`revdcf` 08-13 실행 로그를 볼 수 있다** — 1026을 기다리지 않고 원인 확정 | 로그로 `stage` 대체 확인 |
| 방법은 찾았으나 **지금은 무효**(권한·API 변경) | 6회 반복의 원인이 "방법 소실"이 아니라 "환경 변화" | 색인에 무효 사유까지 기록 |
| 911·933에 **작동하는 답이 실은 없었다** | 🔴 1027의 서술이 과장 — **정정하고 그대로 적는다** | 6회가 아니라 전부 실패였음을 기록 |
| 기록 자체를 **못 찾는다** | 🔴 색인 부재가 확인됨 — W3이 유일한 답 | 색인 신설 |

**W2 — 중복 조사 전수**
| 관측 | 결론 |
|---|---|
| Vercel 외 중복이 **더 있다** | 색인의 초기 항목이 늘어난다 |
| Vercel **1건뿐** | 🔴 1027이 §1-3(이전발언 대조)을 **전수로 했는지** 먼저 확인. 안 했으면 이 STEP이 한다 |

🔴 **모든 칸에 다음 행동이 정해져 있다.**

### ⓪-5. 3번 생각
1. **범위** — 곁가지가 아니다. 🔴 **6회 반복은 최소 5회의 낭비**이고, 막지 않으면 계속 난다. 게다가 W1이 성공하면 **지금 막힌 것이 풀린다.**
2. **표본** — 🔴 **전수.** W2는 이번 세션(1006~1027)이 다룬 주제 **전부**를 대상으로 한다. 🔴 **전수 비용: 문서 grep이라 분 단위.** 상한 불필요.
3. **시점** — 오늘 밤 크론과 무관(읽기 전용 + 문서). 🔴 **W1이 성공해도 `revdcf` 코드를 건드리지 않는다** — 1026의 관측이 오염된다.

---

## 1. 할 일

### W1. 🔑 잃어버린 답 되찾기 — Vercel 로그 접근 (최우선)
1027이 6회 목록(892·907·911·913·933·1017)을 찾았다. **그 여섯 STEP의 기록을 전부 연다.**
- 각 STEP이 **무엇을 시도했고 무슨 결론이었는지** 한 줄씩(`파일:줄번호`)
- 🔴 **911·933이 얻었다는 "작동하는 답"의 실체를 찾는다** — 어떤 방법인가(CLI? 토큰? 다른 엔드포인트? 대시보드 수동?)
- 🔴 **그 방법을 지금 그대로 시도한다.** 🔴 자격증명 입력 금지 — 이미 환경에 있는 것만 사용
- 성공하면: **`revdcf` 2026-08-13 22:45 UTC 실행 로그**를 읽어 함수 종료 사유(정상/예외/timeout)와 소요시간을 낸다. 🔴 **이게 1026이 오늘 밤 `stage`로 얻으려는 답과 같은 것**이다 — 먼저 나오면 교차검증이 된다
- 🔴 실패하면 사유를 명확히(권한 없음 / 보존기간 초과 / 방법 무효). **추정 금지**

### W2. 중복 조사 전수 색출
🔴 **먼저 1027이 §1-3(이전발언 대조)을 전수로 했는지 확인**한다(`docs/probe_1027_three_way_audit.md`). 안 했으면 이 STEP이 한다.

이번 세션(1006~1027)이 다룬 주제를 나열하고, 각각에 대해 **이전 STEP에 같은 조사가 있는지** `REVDCF_SPEC.md` · `VALUATION_SPEC.md` · `CHANGELOG.md` · `STEP_LEDGER.md` · `docs/probe_*.md`에서 검색한다.

주제 목록(최소):
야후 결측 원인 · 07-30 코호트 · 시총 신선도 · 커버리지 게이트 · 프루닝 · `BUDGET_MS`/예산 초과 · `us_sector_relative` 정지 · 서빙 신선도 · 유니버스 구성/SIC · SEC 주식수 태그 · 나스닥 스크리너 · 복수클래스 · 업종 베타

산출: **「우리 STEP / 이미 있던 STEP / 결론이 같았나 달랐나 / 낭비였나」** 표
🔴 **낭비가 아니었던 것도 적는다**(예: 892가 못 한 걸 1007이 해냈다).

### W3. 🔑 `docs/KNOWN_ANSWERS.md` 신설 — 짧게, 검색 가능하게
🔴 **길면 안 읽힌다. 한 항목 3줄 이내.**

```
## Q. Vercel 런타임 로그를 읽을 수 있는가
- 답: (W1 결과)
- 확정: STEP ___ (2026-__-__) · 근거: 파일:줄
- 재검토 조건: (무엇이 바뀌면 다시 물어야 하나)
```

초기 항목 = W1 + W2에서 나온 것 전부. 🔴 **결론이 "불가"인 것도 답이다** — 그게 없어서 여섯 번 물었다.
🔴 각 항목에 **재검토 조건**을 반드시 넣는다. 조건 없는 항목은 영구 확정처럼 읽혀 위험하다.
🔴 `docs/INDEX.md`에 이 문서를 등재한다.

### W4. 템플릿 강제 — 안 읽으면 소용없다
🔴 색인을 만들어도 **안 보면 똑같다.** `docs/step_orders/_TEMPLATE.md` ⓪에 칸을 추가한다:

```
### ⓪-1b. 🔴 기존 답 확인 (KNOWN_ANSWERS)
- 이 STEP의 주제가 `docs/KNOWN_ANSWERS.md`에 있는가: 있음(STEP ___) / 없음
- 있으면: 그 답을 그대로 쓰는가, 재검토 조건이 충족돼 다시 묻는가
🔴 이 칸이 비면 미완성이다.
🔴 전례: Vercel 런타임 로그 접근을 892→907→911→913→933→1017 여섯 번 조사했다.
   911·933이 답을 얻었는데 1017이 못 찾았다. 규칙은 있었고 검색 수단이 없었다.
```

### 1-5. 산출물
`docs/probe_1028_known_answers.md`
- 맨 앞에 **W1 결과 한 줄**(로그 접근 가능/불가 + 가능하면 `revdcf` 08-13 종료 사유)
- W1 여섯 STEP 대조표
- W2 중복 조사 전수표
- W3 색인 초기 항목 수
- 🔴 3중 규칙 3종: **못 한 축 / 철회·정정 / 미측정**

## 2. 커밋 전 게이트
- 게이트 7: git 미추적 파일 참조 검사 — 🔴 런타임 `fs.readFileSync('/tmp/...')`도 잡을 것
- 게이트 8: push 후 **Vercel 배포 성공 + verify 초록을 눈으로 확인**하기 전까지 미완료
- 게이트 9: 🔴 정본 먼저, 사본 나중
- `docs/CHANGELOG.md` 1028 항목 · `docs/STEP_LEDGER.md` 1028 등재 · `docs/INDEX.md`에 `KNOWN_ANSWERS.md` 등재

## 3. DoD
- [ ] W1 여섯 STEP 전부 열었고, 각각 시도·결론이 `파일:줄번호`와 함께 적혔다
- [ ] 🔴 **911·933의 "작동하는 답"의 실체**가 규명됐다(또는 "실은 없었다"로 정정)
- [ ] 🔴 방법을 **실제로 시도**했고 결과가 적혔다 · 🔴 **자격증명 입력 0**
- [ ] 성공 시 `revdcf` **2026-08-13 22:45 UTC 실행 로그**의 종료 사유·소요시간
- [ ] W2 중복 조사 **전수표**(낭비였던 것과 아닌 것 구분)
- [ ] W3 `KNOWN_ANSWERS.md` 신설 · 🔴 **모든 항목에 재검토 조건** · `INDEX.md` 등재
- [ ] W4 템플릿 ⓪-1b 추가
- [ ] 🔴 **DB 쓰기 0 · 프로덕션 코드 diff 0** · `app/api/cron/revdcf/route.ts` diff 0
- [ ] tsc 0 · test 전량 통과
- [ ] Vercel 배포 성공 + verify 초록 **눈으로** 확인
- [ ] 마지막에: 못 한 축 / 철회·정정한 것 / 미측정으로 남은 것

🔴 **W1이 성공해도 `BUDGET_MS`·게이트·코드를 고치지 않는다. 판정은 장은태가 한다.**
