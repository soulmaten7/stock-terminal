# STEP 1061 — 렌즈 전수감사 ⑤ 기술(RSI·MA·52주) (🔵 읽기 전용 · 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 수동 실행 0)

> 🔴 **판정은 장은태가 한다.** 이 STEP은 결함을 실측으로 뜯어 적는 데까지다.
> 🔴 **순서를 바꿨다 — 근거를 적는다.** 기존 순서 근거(*"등급이 셋만 `strong`이다… **가장 센 주장부터 본다**"*, `LENS_AUDIT_02_MOMENTUM` 머리)는 **`strong` 넷(①밸류·②모멘텀·③저변동·④퀄리티)이 끝나며 소진**됐다. 남은 셋(자산성장·F스코어·기술)에는 그 기준이 안 걸린다.
> **새 기준 = 「주장과 근거의 간극이 가장 큰 것부터」.** 기술이 그것이다 — ㉠우리 백테스트가 **RSI를 기각**했고(`LENS_STRENGTH_MAP:12` *"CAPM α t≈−2.01 **[기각]**"*) ㉡`lens_state_changes` US **587건(26%)로 2위**(`LENS_DISPOSITION` §1) — **가장 많이 노출되는 축에 가장 약한 근거**가 붙어 있다. ㉢`LENS_DISPOSITION` §3이 technical을 **Q5 주력으로 승격 지목**했다.
> 🔵 읽기 전용 — 외부 신규 취득 0(STEP1060의 야후 조회 예외는 **이 STEP에 승계되지 않는다**).

---

## ⓪ 전제 점검

### ⓪-1. 직전 산출물 — 원문 인용

| 출처 | 인용(그대로) |
|---|---|
| STEP1060 결과(`LENS_DEV_PLAYBOOK.md` #115 · `LENS_AUDIT_04_QUALITY.md` §4-1) | 🔴 **pull 후 원문으로 확인할 것.** 요지: 175종목 층화 표본에서 **야후가 SEC보다 뒤처진 확정 사례 0건**, 33건의 「회계연도 불일치」 중 **23건은 순수 라벨링 관행**(값은 정확히 일치·비12월 결산), **5건은 반대 방향**(SEC 쪽 `us_fundamentals`가 REIT/소형은행 몇에서 옛 연도에 얼어 있음 — AGNC·AMH·LCNB·FLG). 4개 렌즈 전부 **배열 위치 인덱싱**(`financials[length−N]`)이라 라벨 문제에 구조적 면역. |
| 같은 결과 | STEP1059 §6의 MSFT 1.59%p·KO 0.39%p 편차 = **그 감사의 손계산이 낡은 수동 수치를 쓴 것**이지 배선 결함이 아님 — **코드 무죄로 확정.** |
| `docs/LENS_STRENGTH_MAP.md:12` | *"$5+ 153개월: RSI 저−고 연 **−8.7%·CAPM α t=−2.01** **[기각]** · 200일선 위−아래 +12.9%·t1.6·FF3 α t1.83(모멘텀 흡수) **[약함·비독립]**"* |
| 같은 문서 `:33` | *"기술 \| [+,+,+] t2.7 \| ⚠️ 견고하나 **모멘텀 중복**(FF3 모멘텀 미통제)·참고용 유지"* |
| 같은 문서 `:37` | *"**5렌즈 전부 판정 완료(미검증 없음)**: 검증=모멘텀·저변동·F-Score·밸류(E/P) / 조건부=밸류(B/M)·**기술(200일선)** / **기각=RSI 평균회귀**. STEP 508~522."* |

🔴 **직전 STEP(1060)이 「못 한 것」으로 남긴 것 중 이 STEP이 건드리는 것**: 없음(1060은 재무 입력, 기술 렌즈는 **가격 계열**만 쓴다). **겸하지 않는다.**

### ⓪-1a. 🔴 로드맵 원문 대조 (`docs/ROADMAP_V2.md` — 여섯 층 전부)

| 층 | 걸리는 절 | 인용(원문 그대로) |
|---|---|---|
| **WHY** | 조건 3 (`:67`) | *"**왜 그런지 알 수 있어야 한다** — 답만 주면 우리가 **또 하나의 「믿어야 할 대상」**이 된다 — 그건 리딩방과 구조가 같다."* → 🔴 **기술적 분석은 리딩방이 가장 많이 쓰는 언어다. 이 렌즈가 WHY의 시험대다.** |
| **HOW** | H-7 판별 셋 (`:210`) | *"**판별 셋**: 사용자가 이게 무엇인지 **알 수 있는가**(공개) · 우리가 이 값을 **책임질 수 있는가**(검산) · **원본인 척하는가**(기만)"* · *"🔑 **원전은 두 층이다** — **현상의 원전**(효과가 실재한다고 밝힌 문헌)과 **계산 사양의 출처**(숫자를 어떻게 내는지의 관행). 둘을 구분해 밝힌다."* → 🔴 **RSI는 계산 사양의 출처(와일더 1978)는 있는데 현상의 원전이 없다(우리가 기각했다). 이 층 구분이 이 렌즈에서 가장 첨예하다.** |
| **WHAT** | W-2-1 | *"**원하는 정보 목록이 먼저 있고**, 그것을 사람이 묻는 형태로 묶은 것이 질문 모델이다"* → 🔴 **질문 여섯에 억지로 맞추지 않는다**(2026-08-17 장은태 지시). |
| **만드는 절차** | ⬜ 확정 대기 | 무관 |
| **관문·순위** | F-1 ①③ (`:333·335`) | ① *"**사람들이 원하는 정보인가** — 없으면 **만들 이유가 없다**"* → 🔴 **기술은 관문 ①이 가장 강한 렌즈다**(수요는 확실). ③ *"**정직한가** — 계산 방법을 밝힐 수 있는가"* → **관문 ①이 세고 ②가 약할 때 어떻게 하는가**가 이 렌즈의 문제다. |
| **완성의 정의** | C-1 #5·#6·#7 (`:495~497`) | #5 *"**경계 처리** — 계산 불가 조건·**최소 표본**·결측 표기"* · #6 *"**주장 정합** — 검증 범위·실패 모드·적용 조건이 공개 문서와 **무모순**"* · #7 *"**화면 일관성** — 카드·목록·변화 피드·이메일·브리핑에서 **같은 이름·판정·단위**"* → 🔴 **#6이 이 STEP의 중심축이다**(⓪-4). |

### ⓪-1b. 기존 답 확인 — 🔴 `ls docs/` 전수

2026-08-18 확인분(원격 `main` = `9ad5488`): 감사 넷(`VALUE_LENS_DEFECT_AUDIT`·`LENS_AUDIT_02_MOMENTUM`·`LENS_AUDIT_03_LOWVOL`·`LENS_AUDIT_04_QUALITY`) — **기술 감사는 없음.**
🔴 그러나 기술은 **STEP 508~522·559·783이 이미 손댄 렌즈**다(`meta.backtestRef:"STEP559"` · `cutoffs` 주석 `STEP 783`). **`ls docs/`로 `STEP_5*`·`probe_5*`·`STEP_78*` 전수를 훑어 기술 관련 문서를 먼저 찾아라.** 있으면 그만큼 범위를 줄인다. **중복 감사하지 않는다.**

### ⓪-2. 건드리는 슬롯
**해당 없음** — 읽기 전용. 슬롯 #3(주가 계열)을 **조회만** 한다.

### ⓪-3. 걸리는 금지사항
- [ ] **코드 diff 0 · DB 쓰기 0 · 화면 변경 0** — 🔴 **7렌즈 라이브 화면 수정은 매번 별도 승인**(899·901 위반 전례)
- [ ] 🔴 **크론 수동 실행 금지**
- [ ] 🔴 **외부 신규 취득 0** — STEP1060의 야후 조회 예외는 **승계되지 않는다.** 기존 DB·캐시만.
- [ ] **KR 전면 동결** — 기술은 KR에도 살아 있다. **US만. KR은 대조군 조회로만**
- [ ] `data/us_symbols.json`·`.github/workflows/**`·`vercel.json` 수정 금지
- [ ] `REVDCF_ENABLED` OFF 유지 · `Q1_ENABLED` 무접촉
- [ ] 🔴 **robots.txt 우회 금지** · **유료 논문 구매 금지 · API키 발급 금지 · 키 VALUE 타이핑 금지**
- [ ] 🇺🇸 US 단독

### ⓪-3b. 🔴 결함의 소재 확정 — **이번엔 여기가 가장 중요하다**

관측 지점 = `lib/lenses.ts:193~232`(technical 본체) · `lib/technical.ts`(`sma`·`rsi`·`rsiState`·`maTrendState`) · `lib/lensCopy.ts`의 `technical` 블록(`note`·`scope` 3칸) · `lib/lensTones.ts` · `lib/lensCompute.ts`(가격 계열 공급) · `lens_scores`·`lens_state_changes`(DB). **전부 프로덕션이다.**

🔴 **명령서 작성 중 `lib/lensCopy.ts`의 `technical` 블록을 직접 열었다. 미리 알려둔다 — 중복 조사하지 말 것.** 그 `note`·`scope`는 **이미 대단히 정직하다**:
- *"기술 지표는 7개 렌즈 중 **유일하게 학술 검증 팩터가 아니에요**(참고용)"*
- *"판정(위/아래)은 200일선 대비 기준이고, **RSI·52주 위치는 함께 보여주되 판정엔 안 써요**"*
- *"RSI(와일더 1978): 30/70 과열·침체는 **와일더 본인의 경험칙이지 통계 검정 결과가 아니에요.** 침체 매수(저RSI 롱)는 우리 재검에서 수익 우위가 없었고 … **매매신호로 통하지 않아요**"*
- *"고전적 검정(Brock 외 1992)도 데이터 스누핑을 보정하니 **유의성이 사라졌어요(Sullivan-Timmermann-White 1999)** — 그래서 '검증됐다'고 못 해요"*
- *"우리 재검에서도 200일선 추세는 … **사실상 (횡단면) 모멘텀과 겹치는 신호라 독립적이지 않아요**"*

🔴 **그러므로 「RSI가 기각됐는데 라이브다」·「200일선이 약한데 쓴다」·「30/70이 근거 없다」 셋은 결함이 아니라 이미 공개된 한계다.** ⓪-3b(이중 계상 금지)에 정확히 걸린다. **이걸 결함으로 적으면 이 STEP은 실패다.**
🔴 **감사가 찾을 것은 「문구가 약속한 것을 코드·화면이 실제로 지키는가」다**(C-1 #6 주장 정합).

### ⓪-4. 🔴 반증 조건

**전제 ①** — 위 공개 문구(⓪-3b)가 **코드와 무모순**이다.
- 🔴 **셋을 하나씩 코드로 검증한다**:
  - ㉠ *"RSI·52주 위치는 판정엔 안 써요"* → `tState`(`lenses.ts:207`)는 `last`↔`ma200` **단독**인가. `verdict`·`spectrum`·`outlook`·`value`·`headline` 전부 `tState`/`ma200`만 쓰는가.
  - ㉡ *"매매 신호가 아니라 '지금 상태' 표시일 뿐"* → 🔴 **그런데 `verdict`가 up→`"pos"`(긍정)·down→`"warn"`(경고) 톤을 쓴다**(`lenses.ts:224`). **밸류 렌즈는 정확히 이 문제를 인식하고 고쳤다** — `lenses.ts` 밸류 위 주석: *"⚠️ 라벨은 '저평가/고평가'(가치 판단·verdict) 대신 '낮음/보통/높음'(PER 수준 사실)로 — 절대 임계값의 verdict는 검증 밖. **중립 표시.**"* 🔴 **기술은 같은 처리를 받았는가.** 안 받았다면 **문구↔톤 불일치**이고 이것이 이 STEP의 가장 유력한 결함 후보다.
  - ㉢ *"30/70은 와일더의 경험칙"* → `cutoffs:{lo:30,hi:70}`(`lenses.ts:231`)의 그 사실이 **화면에 실제로 렌더되는가.** ③ 감사가 `calmHigh` 40%에서 물은 것과 **같은 질문**이다(STEP824가 저변동에서 해소했다 — 기술도 해소됐나).
- 틀렸다면(=무모순이면) → **결함 아님으로 적고 넘어간다.** 없는 결함을 만들지 않는다.

**전제 ②** — 가격 계열이 **배당 조정돼 있다**.
- 🔴 `d.closes`(raw 종가)를 `rsi`·`sma`·`pos52` 셋 다 쓴다. **③저변동 감사 신규①(*"`realizedVol`이 raw종가 사용, 결정·공개 이력 없음"*)과 같은 계열**이고, ②모멘텀 감사 결함 ③(*"배당조정 폴백이 잣대를 섞음"*)과도 같은 뿌리다.
- 틀렸다면(=raw면) → 🔴 **공개 문구에 배당 조정 얘기가 한 글자도 없다**(⓪-3b 인용 전문 확인). **결함으로 올린다** — 저변동 신규①과 **같은 결함이 세 번째 렌즈에서 반복**되는 것이다.
- 🔴 **양쪽 모두 다음 행동이 정해져 있다.**

**전제 ③** — `pos52`가 **52주 위치**다.
- 코드는 `closes.slice(-252)`(`lenses.ts:203`) — **252 영업일이 없으면 있는 만큼으로 계산하면서 계속 "52주 위치"라 부른다.**
- 틀렸다면(=최소 표본 가드가 있으면) → 결함 아님.
- 그때 → **C-1 #5(최소 표본)** 관점에서 `sma(closes,200)`·`rsi(closes)`의 최소 표본 처리도 같이 본다. 🔴 **신규 상장주가 몇 %인가**를 DB로 잰다.

### ⓪-5. 🔴 3번 생각
1. **범위** — 곁가지 아님. WHY 조건 3이 *"리딩방과 구조가 같다"*를 경계하는데, **기술적 분석이 바로 그 언어**다. 그리고 노출이 2위(587건)다.
2. **표본** — 🔴 **전수.** `lens_scores` US 전량 · `lens_state_changes` 기술 전량. 외부 신규 호출 0.
   🔴 **전수의 조건** — grep만으로 「전수」라 부르지 않는다. **양방향**: ㉠코드→문구(코드가 하는 일이 `note`·`scope`에 있나) ㉡**문구→코드**(⓪-4①의 셋 — *문구가 약속한 것이 코드에 있나*). 🔴 **㉡가 이 STEP의 본체다.**
3. **시점** — 읽기 전용이라 크론과 무관. 🔴 단 기술은 **가격 기반이라 매일 값이 바뀐다.** 조회 시각(UTC)과 `lens_scores.updated_at`을 **반드시 병기**한다.

---

## 1. 배경

**확정**(코드 실측 · 원격 `main` = `9ad5488`)

| 항목 | 값 | 좌표 |
|---|---|---|
| 등급 | `reference` / `ref` — 🟢 **7렌즈 중 유일하게 낮은 등급으로 이미 표시** | `lib/lenses.ts:194` |
| 백테스트 참조 | STEP559 | 같은 줄 |
| `percentile` | **null** — 분포 근거 없음(다른 렌즈와 비대칭) | 같은 줄 |
| 판정(`tState`) | `last > ma200 ? "up" : last < ma200 ? "down" : "flat"` — **200일선 단독** | `lib/lenses.ts:207` |
| `verdict` 톤 | up→`"pos"` · down→`"warn"` 🔴 | `lib/lenses.ts:224` |
| 표시값 | `rsi14` · `ma200vs` · `pos52w` 셋 | `lib/lenses.ts:217~220` |
| `pos52` 창 | `closes.slice(-252)` — **최소 표본 가드 미확인** | `lib/lenses.ts:203` |
| 컷 | `{lo:30, hi:70}` **하드코딩**(STEP783 주석) | `lib/lenses.ts:231` |
| 가격 계열 | `d.closes` — **배당 조정 여부 미확인** 🔴 | `lib/lenses.ts:196` |
| 우리 백테스트 | RSI **[기각]**(α t=−2.01·연 −8.7%) · 200일선 **[약함·비독립]**(t1.6·모멘텀 흡수) | `LENS_STRENGTH_MAP:12` |
| 노출 | `lens_state_changes` US **587건(26%)** — 모멘텀 711 다음 2위 | `LENS_DISPOSITION` §1 |

**확정 안 됨** 🔴
- 공개 문구 셋(⓪-3b)이 **코드와 무모순인가**(특히 「매매신호 아님」 ↔ **긍정/경고 톤**)
- `d.closes`의 **배당 조정 여부**
- `pos52`·`sma200`·`rsi`의 **최소 표본 처리**
- 30/70이 **화면에 렌더되는가**
- 앞선 결함 **15건**(①6+②5+③4)의 생존 여부

---

## 2. 할 일

### 2-1. 코드 완독 (감사 3축 ①)
`lib/lenses.ts:193~232` · `lib/technical.ts` 전부(`sma`·`rsi`·`rsiState`·`maTrendState`의 산식·기간·최소표본) · `lib/lensCompute.ts`의 가격 계열 공급 경로 · `lib/lensTones.ts`(`pos`/`warn`/`flat` 톤이 화면에서 무엇이 되는지).
확인할 것 — ㉠`rsi()`의 기간 기본값과 **평활 방식**(와일더 평활 vs 단순평균 — 🔴 **둘은 값이 다르다.** 원전은 와일더 평활) ㉡`sma(closes,200)`이 200개 미만일 때 `null`인가 짧은 창인가 ㉢`rsiState`·`maTrendState`의 경계 처리 ㉣결측·거래정지일 처리.

### 2-2. 🔴 주장 정합 검증 — 이 STEP의 본체 (⓪-4① · C-1 #6)
⓪-3b에 옮겨 적은 공개 문구 **한 줄씩** 코드와 대조한다. 각 줄에 **지킴 / 안 지킴 / 부분** 판정 + 좌표.
🔴 **특히 ㉡(「매매 신호가 아니라 '지금 상태' 표시」 ↔ `verdict` 긍정/경고 톤)** — 화면에서 실제로 어떤 문구·색이 나오는지 `lensCopy.ts`의 `readOf`/`outlookOf` 텍스트까지 따라가 확인한다. **밸류 렌즈가 받은 중립화 처리를 기술이 받았는가.**

### 2-3. 원전 층 구분 (H-7 「원전은 두 층」)
세 신호 각각에 대해 **계산 사양의 출처**와 **현상의 원전**을 나눠 적는다.

| 신호 | 계산 사양의 출처 | 현상의 원전 | 우리 판정 |
|---|---|---|---|
| RSI | 와일더(1978) — `note`에 이미 있음 | 🔴 **없음**(우리가 기각) | 이미 공개 |
| 200일선 | ? | Brock 외(1992) → STW(1999) 반박 · Moskowitz 외(2012)·Faber(2007)는 **사촌** — `note`에 이미 있음 | 이미 공개 |
| **52주 위치** | ? | 🔴 **`note`·`scope`에 한 글자도 없다** — George & Hwang(2004 JF)이 학술 원전 후보인데 **인용이 없다** | 🔴 **확인 대상** |

🔴 **셋 중 52주 위치만 원전 언급이 없다.** 이것이 결함인지(밝혀야 하는데 안 밝힘) 아니면 *"'지금 상태' 표시일 뿐"*으로 이미 덮이는지 판정 재료를 놓는다. **원문 확보는 무료 공개본만**(유료 구매 금지, 못 구하면 「원문 미확보」).

### 2-4. 가격 계열 검증 (⓪-4②)
`d.closes`가 배당 조정인지 raw인지 코드로 확정. 🔴 **③저변동 신규①·②모멘텀 결함 ③과 같은 뿌리인지** 판정. 같으면 **개별 결함 3건이 아니라 하나의 공통 결함**으로 묶어 적는다(H-7 원인별 분기의 반대 방향 — 뭉뚱그리기가 아니라 **같은 것을 같다고 적기**).

### 2-5. 경계 처리 (⓪-4③ · C-1 #5)
- `pos52`: 252일 미만 종목 수를 DB로 실측. **"52주 위치"라 부르면서 실제 창이 다른 종목이 몇 %인가.**
- `sma200`: 200일 미만 종목 처리와 그 비율.
- `rsi`: 최소 표본.
- 🔴 **신규 상장주 비율**을 실측해 함께 적는다(STEP1058이 확인했듯 유니버스는 매일 갱신된다).

### 2-6. DB 전수 실측 (감사 3축 ③)
- `lens_scores` US 전량 — `technical_value`(=`ma200vs`) 분포(min·p10·중앙·p90·max) · `technical_state` 분포(up/down/flat/na) · 결측 건수
- 🔴 **③ 감사 신규②(스테일 극단치) 재확인** — `updated_at` 균일한가
- `lens_state_changes` 기술 — 전체 건수·종목 수·`LENS_DISPOSITION` §1의 **587건 재현 여부**(③·④ 감사가 각각 재현 검증을 했다, 같은 방식)
- 🔴 **회전율** — `LENS_STRENGTH_MAP:12`가 *"회전율 66%(비용 최악)"*라 했다. **상태 변화 빈도를 실측해 그 숫자와 대조한다.**

### 2-7. 손계산 검산 (C-1 #3)
표본 **3종목**의 `rsi14`·`ma200vs`·`pos52w`를 손으로 재계산해 DB 값과 대조. 🔴 **RSI는 와일더 평활 여부에 따라 값이 갈리므로 두 방식 다 계산해 어느 쪽인지 확정한다.**
🔴 **STEP1059의 실패를 반복하지 않는다** — STEP1060이 밝혔듯 §6의 편차는 **손계산이 낡은 수동 수치를 쓴 것**이었다. **대조 대상의 날짜·출처를 먼저 고정하고 계산한다.**

### 2-8. 🔴 앞선 결함 **15건** 생존 확인
①밸류 6 + ②모멘텀 5 + ③저변동 4. 각 건에 **고쳐짐 / 그대로 / 상태 변경 / 확인 못 함**.
🔴 **④ 감사가 «15건 중 코드가 실제로 바뀐 것은 0건»으로 확정했다**(`LENS_AUDIT_04_QUALITY.md` §8). **다시 전수 조사하지 말고 그 결과를 인용한 뒤, 그 이후(08-18) 변경분만 `git log`로 확인**해 차이가 있으면 갱신한다. **중복 감사하지 않는다.**

### 2-9. 🔴 질문 귀속 — 맞추지 않는다
`lensCopy.ts`의 `technical.question`은 이미 **"지금 흐름이 위인가, 아래인가?"**다. 정본 W-2 여섯과 대조해 **여섯 중 하나 / 새 질문 후보 / 못 씀** 셋 중 하나로 분류한다. 🔴 **억지로 맞추지 않는다**(W-2-1 · 2026-08-17 장은태 지시). 결론 내지 말고 **재료를 놓는다.**

### 2-10. 산출물
`docs/LENS_AUDIT_05_TECHNICAL.md` — 앞선 넷과 **같은 형식**(요약표 → ⓪-4 반증조건 판명 → 원전 층 구분 → 🟢 잘 된 것 → 결함별 상세 → 앞선 결함 생존 → 못 한 것/철회·정정한 것/미측정).
🔴 **새 버전 파일을 만들지 않는다.** 기존 문서를 고칠 일이 생기면 **그 문장만** 고친다(취소선 보존 + 사유 + 좌표 — ④·1060이 한 방식 그대로).

---

## 3. 🔴 하지 말 것
- 🔴 **판정하지 않는다.** 기술을 남길지 뺄지, 어느 질문에 붙일지, 톤을 바꿀지 정하지 않는다.
- 🔴 **이미 공개된 한계를 결함으로 이중 계상하지 않는다**(⓪-3b). **RSI 기각·200일선 약함·30/70 경험칙 셋은 이미 공개돼 있다.** 이걸 결함으로 적으면 이 STEP은 실패다.
- 🔴 **코드·화면·DB 무접촉.** 결함을 찾아도 고치지 않는다. 7렌즈 라이브 화면은 **별도 승인** 사안.
- 🔴 **크론 수동 실행 금지 · 외부 신규 취득 0**(1060의 예외 승계 안 됨).
- 🔴 **유료 논문 구매 금지 · robots.txt 우회 금지.** 못 구하면 「원문 미확보」.
- 🔴 **KR 무접촉** — 대조군 조회로만.
- 🔴 **다른 렌즈(자산성장·F스코어)를 함께 감사하지 않는다.** 하나를 하나씩.
- 🔴 **08-16 사고·`us_market_cap` 조인 결함을 손대지 않는다**(장은태 판정 대기).
- 🔴 **손계산 대조 출처의 날짜를 먼저 고정한다**(STEP1059 실패 반복 금지).

## 4. 커밋 전 게이트
- 게이트 7: git 미추적 파일 참조 검사
- 게이트 8: push 후 **GitHub Actions verify 초록 + Vercel 배포 성공을 눈으로 확인**
- 게이트 9: 새 테이블·크론·관측 필드 없음 → 해당 없음
- `docs/CHANGELOG.md` 항목 · `docs/STEP_LEDGER.md` 등재
- 🔴 **브랜치 규약(STEP1056 §2-7)** — `main`에 직접 push. **브랜치·PR 만들지 않는다. 예외 없음.**

## 5. DoD
- [ ] ⓪-1a 로드맵 **여섯 층** 대조 · ⓪-1b `ls docs/` 전수(STEP 508~522·559·783 관련 문서 선확인)
- [ ] 🔴 ⓪-4① **주장 정합 판명** — 공개 문구 셋(㉠판정 미사용 ㉡매매신호 아님↔긍정/경고 톤 ㉢30/70 경험칙) 각각 지킴/안지킴/부분
- [ ] 🔴 ⓪-4② **배당 조정 여부 확정** + ③저변동 신규①·②모멘텀 결함③과 **같은 뿌리인지** 판정
- [ ] ⓪-4③ `pos52`·`sma200`·`rsi` **최소 표본 처리** + 해당 종목 비율 실측
- [ ] 2-1 코드 완독 — 🔴 **RSI 평활 방식**(와일더 vs 단순) 확정
- [ ] 2-3 원전 **두 층 구분표** — 🔴 **52주 위치의 원전 인용 부재** 판정
- [ ] 2-6 DB 전수 — 분포·state·`updated_at`·**587건 재현**·🔴 **회전율 66% 대조**
- [ ] 2-7 손계산 3종목 — 🔴 **대조 출처 날짜 고정 후** 계산(1059 실패 반복 금지)
- [ ] 🔴 2-8 앞선 결함 15건 — **④ 결과 인용 + 08-18 이후 `git log` 차이만**
- [ ] 2-9 질문 귀속 — 셋 중 하나로. 🔴 억지로 맞추지 않았다
- [ ] 2-10 `LENS_AUDIT_05_TECHNICAL.md` 생성
- [ ] 🔴 **양방향 대조**(⓪-5②) — 코드→문구 · **문구→코드** 둘 다 돌았다
- [ ] 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 수동 실행 0 · 외부 취득 0 · tsc 0 · test 전량 통과
- [ ] 조회 시각(UTC)과 `updated_at` 병기
- [ ] 마지막에: **못 한 축 / 철회·정정한 것 / 미측정으로 남은 것**

🔴 **판정은 장은태가 한다. 이 STEP은 결함을 놓는 것까지다.**
