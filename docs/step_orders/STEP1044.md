# STEP 1044 — 로드맵 전수 대조: 쌓인 어긋남을 모은다 (🔵 조사·대조 · 코드 diff 0 · 화면 무변경)

> 🔴 **장은태 지시(2026-08-15)**: *"모든 걸 만들 때는 항상 로드맵을 열어보게 진행해."* → `_TEMPLATE.md` **⓪-1a** 신설. **이 STEP이 그 규칙의 첫 적용이자, 지금까지 안 열고 만든 것들의 뒤처리다.**
> 🔴 **로드맵을 정답지로 삼지 않는다.** 어긋나면 **어느 쪽이 틀렸는지**를 가린다. 조용히 맞추는 것이 가장 나쁘다.
> 🔵 코드·DB 쓰기 0. DB 읽기·코드 읽기는 한다.

---

## 0-A. 왜 이 STEP인가

`ROADMAP_V2.md`가 2026-08-15 하루에 **F-4-3·F-4-4·W-2-3·W-2-4**를 연달아 받아 적으며 커졌다. 그런데 그 갱신들은 **각 STEP의 산출물을 요약해 붙인 것**이고, **로드맵 전체와 코드·DB의 정합은 아무도 확인하지 않았다.**

🔴 **이미 드러난 어긋남이 있다**(아래 §1-2 씨앗 4건). 하나는 **오늘 만든 F-4-3과 오늘 만든 `probe_1043`이 서로 어긋난다.**

🔑 `ROADMAP_V2` **C-3 ㉡**가 이 STEP의 이유를 이미 적어놨다 — *"같은 형태가 이날 네 번 나왔다 … **공통점: 「확인했다고 생각했지만 실제로는 안 봤다」.** 항목 9는 그 마지막 방어선이며, **문서에도 똑같이 적용된다.**"*

---

## ⓪ 전제 점검

### ⓪-1. 직전 산출물 — 원문 인용
| 출처 | 인용 |
|---|---|
| `probe_1043` 1-4 | 재무건전성 = **Piotroski F-Score 단독** — GP/A·DuPont은 *"애초에 이 칸의 후보가 아니었다"* |
| `ROADMAP_V2` F-4-3 | 순위 6위 = **"재무건전성(F-Score+GP/A)"** 🔴 위와 어긋남 |
| `ROADMAP_V2` F-4-4 | *"`us_sector_wide`가 `us_valuation`보다 **6일 뒤처져**"* |
| `app/api/cron/revdcf/route.ts:97`(STEP973) | *"`us_sector_wide`는 크론이 매일 만드는 표가 아니다(1회 적재, 08-08 이후 갱신 없음)"* · :116~119(STEP974) *"**새 as_of를 만들지 않는다**"* 🔴 위와 어긋남 |
| `lib/lenses.ts` | `meta: { key: … }` **6개**(momentum·technical·valuation·lowvol·quality·assetgrowth) 🔴 문서 곳곳의 **"7렌즈"**와 어긋남 |
| `ROADMAP_V2` C-1 항목1 | *"원전 대조표(최우선) — 5칸 표(항목·논문 정의·우리 구현·**차이**·차이의 영향). 차이 열이 곧 화면에 공개하는 것"* |
| `ROADMAP_V2` C-3 ㉡ | *"확인했다고 생각했지만 실제로는 안 봤다"* — 이 STEP의 근거 |

### ⓪-1a. 🔴 로드맵 원문 대조
🔴 **이 STEP은 로드맵 전체가 대상이다.** 여섯 층 전부 — WHY · HOW(H-1~H-7) · WHAT(W-1~W-4) · 관문·순위(F-1~F-4-4) · 완성의 정의(C-1~C-3) · 수익 모델(M-1~M-4). 층별로 §1에서 처리한다.

### ⓪-1b. 🔴 기존 답 확인 — 재조사 금지
`probe_1037`~`probe_1043` · `MODEL_ROSTER` · `MODEL_UNIVERSE_63` · `LENS_ROADMAP` · `LENS_COMPLETION_STANDARD` · `SECTOR_AXIS_APPLICABILITY` · `STATE.md` · `KNOWN_ANSWERS.md`
🔴 **새 조사를 하지 않는다. 대조만 한다.** 유일한 예외 = §1-3의 **원전 확인**(저장소 안 PDF 열람).
🔴 이 칸이 비면 미완성.

### ⓪-2. 슬롯 — 없다(대조 전용)

### ⓪-3. 걸리는 금지사항
- [x] 🔵 **코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 호출 0.** 읽기만
- [x] 🔴 **`Q1_ENABLED`·`REVDCF_ENABLED` 무접촉**
- [x] 🔴 **로드맵을 조용히 맞추지 않는다** — 사실 오류만 정정하고, 판정이 필요한 것은 **판정 대상으로 올린다**(⓪-3b)
- [x] 🔴 **창작 금지** · **선택지 나열 금지** · **KR 전면 동결**
- [x] `data/us_symbols.json`·`.github/workflows/**`·`vercel.json` 수정 금지

### ⓪-3b. 🔴 결함의 소재 확정 — 어긋남을 세 가지로 나눈다
| 유형 | 뜻 | 처리 |
|---|---|---|
| **ⓐ 사실 오류** | 숫자·상태·코드 위치처럼 **참·거짓이 정해지는 것** | 🟢 **이 STEP에서 정정한다.** 어느 쪽이 정본인지 좌표를 달아서 |
| **ⓑ 판정 필요** | 어느 쪽이 맞는지가 **결정의 문제** | 🔴 **정정하지 않는다.** 양쪽을 나란히 적고 판정 대상으로 올린다 |
| **ⓒ 미측정** | 대조할 재료가 없다 | 🔴 「미측정」으로 적고 무엇을 재야 하는지 쓴다 |

🔴 **ⓐ와 ⓑ를 섞으면 로드맵이 조용히 바뀐다.** 판정이 필요한 것을 "정정"으로 처리하는 것이 이 STEP의 가장 큰 실패 모드다.

### ⓪-4. 🔴 반증 조건

| 관측 | 결론 |
|---|---|
| 어긋남이 씨앗 4건뿐 | ✅ 로드맵이 건강하다 — 전수를 돌았다는 증거(방법 ②)를 함께 적는다 |
| 🔴 **어긋남이 층별로 고르게 나온다** | 🔴 요약을 받아 적는 방식 자체의 문제 — 그렇게 적고 ⓪-1a로 막히는지 평가 |
| 🔴 **로드맵이 맞고 코드·DB가 틀린 경우가 나온다** | 🔑 **더 중요한 발견이다.** 문서 정정이 아니라 **구현 결함**이므로 별도 목록 |
| 🔴 **오늘 만든 문서끼리 어긋난다** | 🔴 하루 안에 생긴 것 — 발생 경로를 적는다(어느 STEP이 무엇을 요약하며 바꿨나) |
| 방법 ①과 ②의 결과가 크게 다르다 | 🔴 한쪽 방법이 못 잡는 종류가 있다는 뜻 — 차집합을 그대로 보고 |
| 어긋남 0건인 층이 있다 | 🔴 **정말 0인지 대조 흔적을 적는다** — 「안 봤다」와 「봐서 없다」를 구분(C-3 ㉡) |

### ⓪-5. 3번 생각
1. **범위** — **어긋남 수집과 ⓐ 정정**까지다. 🔴 ⓑ 판정과 코드 수정은 이 STEP이 아니다
2. 🔴 **표본 — 로드맵 전수.** 459행 문서 하나를 읽는 일이라 비용이 낮다. **상한 없음.** 🔴 층을 건너뛰지 않는다
3. 🔴 **「전수의 조건」 적용** — **두 방향**으로 돈다: ① **로드맵 → 실물**(로드맵의 각 주장이 코드·DB·정본에서 참인가) ② **실물 → 로드맵**(코드·DB·정본에 있는데 로드맵에 없거나 다르게 적힌 것). 🔴 **①만 하면 「로드맵에 없는 것」을 영원히 못 잡는다**

---

## 1. 할 일 — 🔴 이 순서를 지킨다

### 1-1. 층별 주장 목록화 (로드맵 → 실물)
여섯 층을 순서대로 읽으며 **검증 가능한 주장**(숫자·상태·파일 위치·"이미 있다/없다")을 뽑는다.
- 🔴 **주장마다 검증 방법을 먼저 적는다**(DB 쿼리 / 코드 grep / 문서 좌표 / 원전) — 방법이 없으면 **ⓒ 미측정**
- 🔴 **가치 판단·방향 서술은 대상이 아니다**(예: *"보는 법을 모르는 것이 원인"*). 검증 가능한 것만
- 층별로 **주장 수와 검증한 수**를 적는다 — 0건인 층은 ⓪-4 마지막 항 적용

### 1-2. 🔑 씨앗 4건 우선 확인 (이미 드러난 것)
🔴 **재조사가 아니라 확인이다.**

| # | 어긋남 | 대조 대상 |
|:--:|---|---|
| **ⓐ** | `ROADMAP_V2` F-4-3 *"재무건전성(**F-Score+GP/A**)"* ↔ `probe_1043` *"**Piotroski 단독**, GP/A는 다른 질문"* | 어느 쪽이 정본인가. 🔴 **오늘 하루에 생긴 어긋남** |
| **ⓑ** | F-4-4 *"**6일 뒤처져**"* ↔ `cron/revdcf/route.ts:97`·:116~119 *"**새 as_of를 만들지 않는다**"*(설계) | 「뒤처짐」인가 「고정」인가. 🔴 **처방이 갈린다** — 최신화·신선도상한(무효) vs 조인 키 교체(유효) |
| **ⓒ** | 문서 다수의 **"7렌즈"** ↔ `lib/lenses.ts` `meta.key` **6개** | 🔴 **F-Score가 어디에 어떤 형태로 있는지 코드로 확정**한다. 7이 맞다면 어디에 있고, 6이 맞다면 어느 문서를 고치나 |
| **ⓓ** | `probe_1043`이 Piotroski를 **「일치」**로 판정 ↔ 원전의 **표본 조건(고 B/M)** 미기재 | §1-3에서 원전으로 확정 |

### 1-3. 🔴 원전 확인 — 이 STEP의 유일한 신규 조사
`MODEL_ROSTER` §3이 *"무료 정본 PDF 확보"*라 한 **Piotroski(2000)**를 저장소에서 **열어서** 확인한다.
- **표본 조건**이 원전에 어떻게 쓰여 있는가(고 B/M 한정인가, 일반 적용인가)
- 그 조건이 **우리 적용 범위(US 전 종목)**와 맞는가 → `ROADMAP_V2` **C-1 항목 1**의 「차이」 열에 들어갈 내용
- 🔴 **없거나 못 열면 「미확보」**라 적는다. 웹 재검색은 하지 않는다(⓪-1b)
- 🔴 **`LENS_ROADMAP.md:20`을 직접 열어** *"F-스코어 🟡 건전성 해석만(수익 신호 아님, t 0.70)"*의 전후 맥락을 확인한다 — 이 줄이 **뒷받침인지 반증인지** 가른다(축 C)

### 1-4. 실물 → 로드맵 (역방향)
코드·DB·정본 산출물에 있는데 **로드맵에 없거나 다르게 적힌 것**을 찾는다.
- `STATE.md`의 미결·판정대기 항목 중 로드맵에 안 올라온 것
- `probe_1037`~`1043`의 「판정 필요」 중 로드맵에 안 올라온 것
- 코드의 피처 플래그·파킹 상태 중 로드맵에 없는 것
- 🔴 **①(1-1)과 ②(1-4)의 차집합을 표로 낸다**

### 1-5. 어긋남 분류와 처리
1-1~1-4의 결과를 ⓐ/ⓑ/ⓒ로 나눈다(⓪-3b).
- **ⓐ 사실 오류** → 🟢 이 STEP에서 **정정**하고 좌표를 단다
- **ⓑ 판정 필요** → 🔴 양쪽을 나란히, **무엇을 정하는 것인지 한 문장**으로
- **ⓒ 미측정** → 무엇을 재야 하는지
- 🔴 **각 어긋남에 「발생 경로」를 한 줄** — 어느 STEP이 무엇을 요약하며 만들었나. 🔑 **경로가 반복되면 그것이 진짜 결함이다**

### 1-6. 산출물
`docs/probe_1044_roadmap_audit.md`
- 맨 앞 **어긋남 한 장**(ⓐ/ⓑ/ⓒ 수 + 층별 분포) + ⓪-4 매트릭스 결과
- 1-1 층별 주장·검증 수 · 1-2 씨앗 4건 · 🔑 **1-3 원전 확인** · 1-4 역방향·차집합 · 1-5 분류와 발생 경로
- 🔴 3중 규칙 4종: 못 한 축(시도 기록) / 아직 안 함 / 철회·정정 / 미측정

🔴 `ROADMAP_V2.md`의 **ⓐ만 정정**하고 각 정정에 좌표·근거를 단다. `roadmap_v2.html` 동기화. 🔴 **ⓑ는 로드맵에 「판정 대기」로 등재만** 하고 내용을 바꾸지 않는다.

---

## 2. 커밋 전 게이트
게이트 7 · 게이트 9 · `CHANGELOG` 1044 · `STEP_LEDGER` 등재. 🔵 배포 불필요, 커밋·푸시는 한다.

## 3. DoD
- [ ] ⓪-1b — 🔴 **새 조사를 하지 않았다**(예외 = 1-3 원전 열람, 어디를 왜 열었는지 기재)
- [ ] 1-1 **여섯 층 전부** 처리 · 층별 주장 수/검증 수 · 🔴 **건너뛴 층 없음**
- [ ] 1-2 씨앗 4건 전부 확인 · 🔴 **ⓒ(7렌즈 vs 6키)는 F-Score 소재를 코드로 확정**
- [ ] 🔑 **1-3 Piotroski 원전을 실제로 열었다**(못 열었으면 「미확보」) · `LENS_ROADMAP.md:20` 직접 열람
- [ ] 1-4 역방향 · 🔴 **차집합 표**
- [ ] 1-5 ⓐ/ⓑ/ⓒ 분류 · 🔴 **각 어긋남에 발생 경로 한 줄**
- [ ] 🔴 **ⓐ만 정정 · ⓑ는 등재만**(⓪-3b — 판정을 정정으로 처리하지 않았다)
- [ ] `roadmap_v2.html` 동기화
- [ ] 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 플래그 무접촉
- [ ] 마지막에: 못 한 축 / 아직 안 함 / 철회·정정 / 미측정

🔴 **로드맵은 정답지가 아니다. 어긋남을 찾는 것이 이 STEP의 산출물이다.**
🔴 **「안 봤다」와 「봐서 없다」를 구분해 적는다**(C-3 ㉡).
