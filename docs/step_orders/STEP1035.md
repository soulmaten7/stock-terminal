# STEP 1035 — 리딩방·유사투자자문 분리 보관 후 삭제 (🔴 되돌릴 수 없음 · 장은태 판정 완료)

> 🔴 **장은태 판정(2026-08-15)**: *"리딩방 검증은 다른 플랫폼이나 다르게 이용할 가치가 있어 보여서 미뤄둔 거였어. 그런데 이게 이렇게 섞이게 됐으니 확실히 넘어가자. **우리 플랫폼에서 사용 안 할 거야.** 단 이건 **다른 플랫폼으로 다르게 이용할 수 있을 것 같으니 파일과 내용을 따로 정리해서 폴더로 넣어두자.** 그리고 우리 플랫폼에서는 **리딩방 관련 내용을 삭제, 없애버려.**"*
>
> 🔴 **순서가 이 STEP의 전부다: 조사 → 분리 보관 → 검증 → 삭제.** 순서를 지키지 않으면 되돌릴 수 없는 손실이 난다.
> 🔴 **커밋을 두 번으로 나눈다.** ①보관 커밋 → ②삭제 커밋. 삭제가 잘못돼도 직전 커밋으로 복구된다.
> 🔴 **DB는 이번에 건드리지 않는다.** §1-5 참조.

---

## 0-A. 왜 지금인가

`CLAUDE.md:31`(2026-08-08 정본): *"한국시장이라는 기준으로 된 모든 내용은 싹 다 미뤄버려. (…) **이제부터 US만 만들 거야.**"*

🔴 그런데 리딩방은 **「미룸」 상태로 세 곳에 흩어져** 있었다 — `PARKED_FIELD_SURFACES.md`(코드 보존·스위치 OFF) · `AD_MONETIZATION_PLAYBOOK` §1(T5 강등) · `LOCALE_SOURCE_PLAYBOOK` §11(파킹 규약). **보존인지 폐기인지가 문서마다 달라 섞였고**, 2026-08-15에 Cowork이 `ROADMAP.md`의 낡은 WHAT(*"리딩방 검증 디렉토리"*)을 현재 정의처럼 인용하는 오류로 드러났다.

🔑 **오늘 판정으로 확정: 우리 플랫폼에서 제거 · 별도 폴더로 보관(다른 플랫폼용).**

---

## ⓪ 전제 점검

### ⓪-1. 직전 STEP 산출물
| 출처 | 인용 |
|---|---|
| STEP1034 | 카탈로그 97% 전제 **9곳** 정정 · 슬롯 불일치 1건 · 🔴 `DATA_SOURCE_CATALOG.md`가 `INDEX.md` 미등재였음(고침) |
| `PARKED_FIELD_SURFACES.md` §2 | 보존된 컴포넌트·API 표 — 🔴 **이 표가 이번 조사의 출발점**(그러나 전수라는 보장은 없다) |
| `PARKED_FIELD_SURFACES.md` §6 | 파킹 전용 크론 6개는 2026-07-27(STEP794 §5)에 **스케줄 중지됨** |

### ⓪-1b. 🔴 기존 답 확인
- 🔴 **`PARKED_FIELD_SURFACES.md` §2·§3 전문** — 무엇이 보존돼 있고 복원 절차가 무엇인지. **이게 보관 대상 목록의 초안이다**
- 🔴 `AD_MONETIZATION_PLAYBOOK` §1(T5)·§5(KR 합법성 원장)·§7(광고 실패 원장) — 리딩방 서술 위치
- 🔴 `docs/KNOWN_ANSWERS.md`에 관련 항목이 있는지
- 🔴 이 칸이 비면 미완성이다

### ⓪-2. 슬롯
🔴 **없다.** 리딩방은 모델 슬롯과 무관하다.

### ⓪-3. 걸리는 금지사항
- [x] 🔴 **§1-3 검증을 통과하기 전에는 아무것도 지우지 않는다**
- [x] 🔴 **DB 미접촉** — 테이블·뷰·행 전부 그대로(§1-5)
- [x] 🔴 **KR 시장 데이터·크론을 건드리지 않는다** — 리딩방(유사투자자문)과 KR 주식 데이터는 **다른 것**이다. `kr_stock_snapshot`·`kr-perf`·`kr-lens-scores`는 **동결 대상이지 삭제 대상이 아니다**
- [x] 🔴 **`CHANGELOG.md`·`CLAUDE.md:475` 배너 등 「이력 기록」은 지우지 않는다** — 과거 기록이지 현재형 잔재가 아니다
- [x] `data/us_symbols.json`·`.github/workflows/**`·`vercel.json` 수정 금지 · `REVDCF_ENABLED` OFF 유지

### ⓪-3b. 🔴 결함의 소재 확정 — 「잔재」와 「이력」
- **잔재** = 현재형으로 살아 있어 다음 세션이 현재 사실로 오독할 수 있는 것 → **제거 대상**
- **이력** = 과거를 기록한 것(`CHANGELOG`·날짜 배너·STEP 기록) → 🔴 **보존**
- 🔴 **이 구분을 못 하면 이력까지 지운다. 애매하면 지우지 말고 목록에 「판단 보류」로 남긴다.**

### ⓪-4. 🔴 반증 조건

| 관측 | 결론 |
|---|---|
| 보관 후 검증에서 **원본과 복사본이 전부 일치** | ✅ 삭제 진행 |
| 🔴 **복사본에 빠진 파일이 있다** | 🔴 **삭제 중단.** 보관을 먼저 완성한다 |
| 🔴 **삭제 대상이 다른 기능과 얽혀 빌드가 깨진다** | 🔴 **얽힌 지점을 기록하고 그 부분만 남긴다.** 억지로 지우지 않는다 |
| 🔴 **리딩방 컴포넌트가 실제로 화면에 렌더되고 있었다** | 🔴 **최우선 보고** — 파킹이 안 돼 있었다는 뜻 |
| DB에 크론이 여전히 갱신 중 | 기록만(이번엔 DB 미접촉) |

### ⓪-5. 3번 생각
1. 🔴 **되돌릴 수 없다.** 그래서 커밋을 둘로 나누고, 검증 게이트를 사이에 둔다. 1032에서 프루닝을 켤 때 쓴 것과 같은 논리다
2. 🔴 **표본** — **전수**. 비용: `grep -rn` 수 회 = 초 단위. 상한을 걸 이유가 없다. 🔴 **`PARKED_FIELD_SURFACES.md` §2 표를 그대로 믿지 말 것** — 그 표가 전수라는 보장이 없다. 독립적으로 grep한다
3. **경계** — 🔴 **유사투자자문(리딩방)과 KR 주식 데이터는 다른 것이다.** `fss_advisors`·`room_favorites`는 리딩방, `kr_stock_snapshot`은 시장 데이터. **후자는 건드리지 않는다**

---

## 1. 할 일 — 🔴 이 순서를 지킨다

### 1-1. 전수 조사 (먼저)
검색어를 넓게: `리딩방` · `room` · `Room` · `advisor` · `Advisor` · `유사투자자문` · `fss_advisors` · `room_favorites` · `advisor_directory` · `검증`(탭 이름)

| 층 | 기록할 것 |
|---|---|
| **코드** | 파일 경로 · 🔴 **렌더 진입점이 살아 있나**(import하는 페이지 grep) · 다른 기능과의 얽힘 |
| **i18n** | `messages/{ko,en}.json` 키 이름 · 실제 사용처 |
| **DB** | 테이블·뷰 이름 · 행수 · 크론 갱신 여부 (🔵 **조회만**) |
| **문서** | 파일·줄 · 🔴 **현재형인가 이력인가** |
| **마이그레이션** | `supabase/migrations/**`에 관련 SQL이 있나 (🔴 **지우지 않는다** — 이력) |

🔴 **각 항목을 「제거 대상 / 이력 보존 / 판단 보류」 셋으로 분류한다.**

### 1-2. 🔴 분리 보관 — `spinoff/advisor-directory/`
저장소 안에 새 폴더를 만들고 **복사**한다(이동 아님 — 이 단계에선 원본을 남긴다).

- 코드 파일은 **경로 구조를 보존해서** 복사(예: `spinoff/advisor-directory/components/toolbox/AdvisorDirectory.tsx`)
- i18n 키는 관련 키만 뽑아 `spinoff/advisor-directory/i18n-keys.json`
- 🔴 **`spinoff/advisor-directory/README.md`를 쓴다**:
  ① **무엇인가**(유사투자자문사 디렉토리·리딩방 즐겨찾기) ② **왜 여기 있나**(2026-08-15 장은태 판정 — 우리 플랫폼 미사용, 다른 플랫폼 활용 가능성) ③ **DB 의존**(테이블명·스키마·행수 — **DB는 삭제 안 했음을 명시**) ④ **외부 의존**(금감원 데이터 출처·크론) ⑤ **되살리려면**(복원 좌표 = 이 STEP의 삭제 커밋 해시) ⑥ **법적 주의**(유사투자자문 표시의무 — `AD_MONETIZATION_PLAYBOOK` §5 KR 참조)
- 🔴 **`spinoff/`는 빌드에서 제외한다** — `tsconfig.json` exclude 또는 `.eslintignore` 등. 🔴 **빌드 설정 변경은 최소로**, 무엇을 바꿨는지 문서에 적는다

### 1-3. 🔴 검증 게이트 — 여기를 통과해야 삭제한다
- [ ] 1-1 목록의 **제거 대상 전부**가 `spinoff/`에 복사됐는가 (파일 수·이름 대조)
- [ ] README 6항목이 다 채워졌는가
- [ ] 🔴 **`spinoff/` 복사본만으로 다른 사람이 이게 뭔지 이해할 수 있는가**
- [ ] tsc 통과 (보관만 한 상태)

🔴 **여기서 커밋한다 (커밋 ①: 보관).** 🔴 **하나라도 미충족이면 여기서 멈추고 보고한다.**

### 1-4. 원본 삭제 (검증 통과 후에만)
- 코드·i18n 키·렌더 진입점 제거
- 🔴 **얽힌 지점이 나오면 억지로 지우지 않는다** — 기록하고 그 부분은 남긴다
- 🔴 **크론 라우트를 지우면 `vercel.json`과 어긋날 수 있다** — `vercel.json`은 수정 금지이므로, 크론 라우트 삭제가 배포를 깨뜨리면 **삭제하지 말고 기록**한다
- tsc 0 · 테스트 전량 통과 확인
- 🔴 **커밋 ②: 삭제.** 커밋 메시지에 **복원 좌표(커밋 ① 해시)**를 적는다

### 1-5. 🔴 DB는 이번에 건드리지 않는다
`fss_advisors`(약 1,553행)·`room_favorites`·`advisor_directory` 뷰 전부 **그대로 둔다.**
**이유**: ① DB 삭제는 되돌릴 수 없다 ② 크론이 이미 중지돼 있어 방치 비용이 거의 0이다 ③ `spinoff/`가 이 데이터를 참조하므로 **지금 지우면 보관본이 무의미해진다.**
🔴 **DB 처리는 별도 판정 대상으로 등재한다.**

### 1-6. 문서 정리
- 🔴 **`docs/ROADMAP.md` 맨 위에 한 줄**: *"🔴 2026-07-18에 멈춘 문서 — 현행 정본은 `docs/ROADMAP_V2.md`"*. **본문은 손대지 않는다**
- `PARKED_FIELD_SURFACES.md`에서 **리딩방·유사투자자문 행만** 「2026-08-15 `spinoff/`로 분리·본체 삭제(STEP1035)」로 갱신. 🔴 **다른 파킹 항목은 건드리지 않는다**
- `AD_MONETIZATION_PLAYBOOK` §1의 T5 행 · §7 원장 — 상태 갱신
- `docs/INDEX.md`에 `spinoff/advisor-directory/README.md` 등재

### 1-7. 산출물
`docs/probe_1035_advisor_spinoff.md`
- 맨 앞 한 줄 요약 + ⓪-4 매트릭스 결과
- 1-1 전수 목록(층별 · **제거/이력/보류** 분류)
- 1-2 보관 내역(파일 수·경로)
- 🔴 **1-3 검증 결과** · 커밋 ① 해시
- 1-4 삭제 내역 · 🔴 **못 지운 것과 그 이유** · 커밋 ② 해시
- 1-5 DB 현황(조회만) — 별도 판정 대상 명시
- 🔴 3중 규칙 3종

## 2. 커밋 전 게이트
- 게이트 7 · 게이트 8(배포 성공 눈으로 확인 — 🔴 **삭제가 있으므로 특히 중요**) · 게이트 9
- `CHANGELOG` 1035 · `STEP_LEDGER` 등재 — 🔴 **"장은태 판정(2026-08-15): 우리 플랫폼 미사용 확정, spinoff 분리 후 삭제"** 명시

## 3. DoD
- [ ] ⓪-1b 기존 답 확인
- [ ] 1-1 전수 조사 · 🔴 **`PARKED_FIELD_SURFACES` 표를 그대로 믿지 않고 독립 grep**
- [ ] 🔴 **1-2 `spinoff/advisor-directory/` 보관 + README 6항목**
- [ ] 🔴 **1-3 검증 게이트 통과 · 커밋 ①(보관) 완료**
- [ ] 1-4 원본 삭제 · 커밋 ②(삭제) · 🔴 **못 지운 것 기록**
- [ ] 🔴 **1-5 DB 미접촉 확인** · 별도 판정 등재
- [ ] 1-6 문서 정리 · 🔴 **ROADMAP.md 배너 한 줄(본문 무변)**
- [ ] 🔴 **KR 주식 데이터·크론 무영향 확인**(리딩방과 다른 것)
- [ ] tsc 0 · 테스트 전량 통과 · 배포 성공 눈으로 확인
- [ ] 마지막에: 못 한 축 / 철회·정정 / 미측정

🔴 **검증 게이트(1-3) 전에 삭제 금지.**
🔴 **다음은 ROADMAP_V2의 WHAT 층이다.**
