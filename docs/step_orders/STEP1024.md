# STEP 1024 — 유니버스 분류를 SEC SIC 정본으로 재판별한다 (읽기 전용 · 배선 0)

> 🔴 **오늘 밤 22:45 UTC가 W1(4단계 heartbeat)의 진짜 첫 시험이다.** `app/api/cron/revdcf/route.ts` **diff 0**.
> 🔴 **분모를 줄이러 가는 게 아니다.** 유니버스에 무엇이 있는지 정확히 아는 것이 목적이고, "빼도 97%를 못 넘는다"도 정당한 결론이다.

---

## 0-A. 🔴 1021→1022→1023 세 STEP이 프로덕션에 없는 문제를 쫓았다

1023이 §1-1에서 확인한 것:
- 프로덕션 `revdcf`(`app/api/cron/revdcf/route.ts:278-281`)는 **`frames` 엔드포인트를 아예 쓴 적이 없다.** `companyfacts`(전체 이력, 창 없음)를 쓴다
- **다르고 더 적절한 태그 우선순위 체인**을 쓰고, **복수클래스 주식수 스킵 처리도 이미 명시적으로 갖고 있다**
- "322건이 낡은 값을 쓴다"는 1022의 발견은 **일회용 조사 프로브(1019~1022)끼리 두 상태를 비교한 인공물**이었고 `us_fundamentals`를 건드린 적이 없다

🔴 **즉 "6분기 조회창 결함"은 프로덕션에 존재하지 않았다.** 세 STEP이 조사 도구의 결함을 프로덕션 결함으로 오인해 진행했다.
🔴 **원인은 내(Cowork) 명령서 설계다.** 1021 §1-4에서 "조회창"을 말할 때 그게 프로덕션의 것인지 프로브의 것인지 확인시키지 않았고, 1022 §1-1의 "`파일:줄번호`로 찾아라"는 프로브 파일이어도 통과했다.
🔴 **`_TEMPLATE.md`에 ⓪-3b(결함의 소재 확정)를 신설**해 반영했다. **이 STEP부터 그 칸이 비면 미완성이다.**

🔑 **좋은 소식도 있다: 슬롯 #2(발행주식수)의 프로덕션 취득 경로는 건전하다.** 전체 이력 조회 + 적절한 태그 체인 + 복수클래스 스킵.

---

## ⓪ 전제 점검

### ⓪-1. 직전 STEP 산출물 — 원문 인용 (검산 포함)

| 출처 | 인용 | 검산 |
|---|---|---|
| STEP 1023 판정 | **⓪-4 네 번째 갈래** — "322건 낡은 주식수(7.07%)"는 **프로덕션과 연결이 0**. 일회용 조사 프로브 안의 인공물이었고 `us_fundamentals`를 건드린 적 없음 | — |
| STEP 1023 | 프로덕션 `revdcf`는 `frames`를 쓴 적 없음 · `companyfacts`(전체 이력) 사용 · 태그 우선순위 체인 다름 · **복수클래스 스킵 이미 있음** | — |
| STEP 1023 | §1-1에서 **지시대로 멈추고** §1-2~1-4를 건너뜀 | 🔑 관문이 작동했다 |
| STEP 1023 | `ANSWERABILITY_MAP.md` §F · 카탈로그 슬롯 #2 + **§0-A "분자 경로 소진" 절** 갱신 완료(md+xlsx) | — |
| STEP 1022 | 조회창 확장 회복 **18건(9.9%)**, 그중 17건 중앙값 **5.3년** 낡음 → 실질 +1건 | ✅ 18/182 |
| STEP 1022 | 유니버스 분모 **5,976**으로 확정(5,973은 오표기) | 🔴 **이후 전부 5,976 기준** |
| STEP 1021 | 5,976 중 **85.7% 보통주**, **14.3%(852건)**가 CEF/신탁·ADR·SPAC·로열티트러스트 | ✅ 5,976×0.143 ≈ 854 |
| STEP 1021 🔴 | **분류 방법에 결함** — "CEF/신탁"으로 분류한 **320건 중 `lens_scores`에 든 7건이 전부 REIT(정상 운영회사)**였다 | 🔴 **852건 분류 정확도 미검증** |
| STEP 1021 🔑 | `revdcf_results` 604건엔 **CEF·신탁·SPAC이 이미 0건** — 역DCF는 처음부터 올바르게 배제 중 | — |
| STEP 1021 | CEF/신탁 제외 시 커버리지 96.95% → **98.39%** — 🔴 **채택 안 함** | — |
| 현재 | US `lens_cuts` 07-30 **17일째** · `us_sector_relative` 08-10 **5일째** · `us_market_cap_nasdaq` 0행 · **W1 오늘 밤 첫 시험** |
| 🔴 미정리 | `docs/LENS_DISPOSITION_2026-08-08.md`가 **미추적 상태로 작업트리에 남아 있다**(08-08, Cowork 작성, 이 축과 무관) | 🔴 §1-6 참조 |

### ⓪-2. 이 STEP이 건드리는 슬롯

| 슬롯# | 검증 상태 | 이 STEP의 취급 |
|---|---|---|
| **#2 발행주식수** | 🔑 **1023이 프로덕션 경로가 건전함을 확인**(전체 이력 · 태그 체인 · 복수클래스 스킵) | 🔴 **다루지 않는다** |
| **#18 섹터분류** | ✅ 1011(도달률 81.4%) | SIC 코드 취득 경로로만 참조 |
| **유니버스 정의** | 🔴 **분류 정확도 미검증**(REIT 혼입 확인) | 🔑 **이 STEP의 대상** |

### ⓪-3. 걸리는 금지사항 (해당되는 것만)
- [x] 🔴 **`data/us_symbols.json` 수정 금지.** 유니버스를 바꾸지 않는다 — **정확히 세기만 한다**
- [x] 🔴 **배선 0 · DB 쓰기 0** — 분류 결과를 어느 테이블에도 쓰지 않는다
- [x] 🔴 **게이트 임계(97%/95%) 불변** · 🔴 **임계 조정 제안 금지** · `lib/lensPrecompute.ts` 수정 금지
- [x] 🔴 **`app/api/cron/revdcf/route.ts` diff 0** — 오늘 밤 W1 진짜 첫 시험 보호
- [x] 🔴 **`BUDGET_MS`·`maxDuration` 불변** · **7렌즈 라이브 화면 수정 금지** · D 조회 키 버그도 고치지 않는다
- [x] 🔴 **SEC 레이트리밋 준수** — 10 req/s · UA 필수 · `nextAt` 동기예약(994). 🔴 **1022가 받아둔 companyfacts 결과·기존 벌크를 먼저 재사용**한다
- [x] 🔴 **크론 수동 실행 금지**(특히 `email-brief` = 실제 메일 발송) · robots.txt 우회 금지
- [x] `REVDCF_ENABLED` Production OFF 유지 · `.github/workflows/**` · `vercel.json` 수정 금지 · API 키 발급·유료 가입 금지 · **KR 전면 동결**

### ⓪-3b. 🔴 결함의 소재 확정
- 이 STEP이 다루는 것은 **데이터(유니버스 목록)의 구성**이지 코드 결함이 아니다
- 🔴 **다만 분류 로직은 이 STEP이 새로 만드는 조사 도구다.** 1021의 분류가 틀렸듯 이번 분류도 틀릴 수 있다 — 🔴 **§1-3의 교차검증을 반드시 한다**
- 🔴 프로덕션 코드(`lib/**`·`app/**`)에는 손대지 않는다

### ⓪-4. 🔴 반증 조건

| 관측 | 결론 | 다음 축 |
|---|---|---|
| SIC 정본으로 재판별하니 **1021 분류와 크게 다르다** | 🔑 1021의 852건은 신뢰 못 한다 — 정정 | 새 분류로 재계산 |
| **거의 같다** | 1021 분류가 대체로 맞았다 | 분모 판정 재료 확정 |
| 🔴 **분류가 정확해도 제외 후 97%를 못 넘는다** | 분모 정리로는 해결 안 된다 | **커버리지 축 전면 재검토** |
| 🔴 **SIC를 못 얻는 종목이 많다** | 정본 자체가 불완전 | 판별 근거 재선정 |

🔴 **네 결과 모두에 다음 행동이 정해져 있다.**
🔴 **세 번째가 나오면 그것으로 이 축은 닫힌다.** "그래도 낮추면 되지 않나"로 가지 말 것.

### ⓪-5. 3번 생각
1. **범위** — 분자 경로가 전부 소진된 상태(1023 §0-A)에서 **남은 유일한 커버리지 경로**다. 곁가지 아님.
2. **표본** — 🔴 **전수 5,976.** 🔴 **전수 비용을 먼저 적는다**: SIC는 SEC `submissions`에 있고, 🔴 **기존 벌크·1022 결과에 이미 있으면 재조회 0회**. 새로 받아야 하면 5,976건 = 10 req/s 기준 **약 10분** — 상한을 걸 이유가 없다.
3. **시점** — 읽기 전용이라 오늘 밤 W1을 오염시키지 않는다. 🔴 22:45 UTC를 넘기면 §1-5로 값을 읽어 붙인다.

---

## 1. 할 일

### 1-1. SIC 코드 전수 확보
- 🔴 **먼저 기존 자산을 확인**한다: 1022가 받아둔 companyfacts · 996 벌크 · `us_sector_wide`의 `cross_sic` — SIC가 이미 있으면 **재조회하지 않는다**
- 없는 종목만 SEC `submissions`로 취득(🔴 10 req/s · UA · `nextAt`)
- 🔴 **못 얻은 종목 수와 사유를 집계**한다(조용히 버리지 말 것 — 833)

### 1-2. SIC 기반 재판별 — 전수
SIC를 정본으로 종류를 다시 매긴다. 🔴 **최소한 아래는 구분한다**:
- **6798 = REIT** ← 🔴 **정상 운영회사다. 제외 대상 아님**(1021이 여기서 틀렸다)
- **6726 = 투자사무소(CEF·펀드)** ← 제외 후보
- **6770 = 백지수표회사(SPAC)** ← 제외 후보
- 그 외 사업 SIC = 보통주
- 🔴 SIC가 없거나 애매하면 **`판별불가`**로 두고 억지로 분류하지 않는다

### 1-3. 🔴 교차검증 — 이번 분류도 틀릴 수 있다
🔴 **1021이 여기서 틀렸다. 같은 실수를 반복하지 않는다.**
- SIC 분류와 1021의 이름패턴 분류를 **교차표**로 놓는다. 불일치 건수와 방향
- 🔴 불일치 중 **최소 20건을 개별 확인**한다(🔴 전수 비용: 20건이면 즉시. 🔴 불일치가 20건 미만이면 전수)
- 🔴 **`lens_scores` 상위 1000에 든 종목 중 "제외 후보"로 분류된 것은 전수 개별 확인**한다 — 1021이 정확히 그 자리에서 REIT를 발견했다
- 🔴 `revdcf_results` 604건 중 제외 후보가 있으면 **크게 보고**(1021은 0건이라 했다 — 바뀌었으면 이상)

### 1-4. 분모 시나리오 — 가상값, 채택 금지
| 시나리오 | 분모 | 야후 커버리지 | SEC 결합 커버리지 |
|---|---|---|---|
| 현행 | 5,976 | 93.9% | 96.95% |
| ⓐ SIC 6726(CEF·펀드)만 제외 | ? | ? | ? |
| ⓑ ⓐ + 6770(SPAC) | ? | ? | ? |
| ⓒ ⓑ + 우선주·ETN·유닛·워런트 | ? | ? | ? |

🔴 **어느 것도 채택하지 말 것.** 그리고 반드시 함께 적는다:
> **분모를 줄이면 커버리지가 오르는 것은 산술적으로 당연하다. 이게 개선인지 눈속임인지는 "빠진 종목이 정말 모델 대상이 아닌가"로만 판정된다.**

🔴 **REIT는 어느 시나리오에서도 빼지 않는다.**

### 1-5. 오늘 밤 관측 — 22:45 UTC가 지났으면 읽기만
🔑 **W1의 진짜 첫 시험.**
- 🔴 **먼저 배포 시각이 22:45 UTC보다 앞섰는지 확인**(1020이 겪은 함정)
- `cron_heartbeats.job='revdcf'`의 **`stage`**(`loop_done`/`valuation_done`/`sector_relative_done`/`complete`) ← 🔴 **이게 답이다**
- `elapsedMsAtStage` · `finallyMs` 6구간 · `finallyTotalMs` · `loopMs` · `budgetExhausted` · `sectorRelativeError`(스택) · `sectorRelativeSaved`
- `us_sector_relative` 최신 `as_of`가 08-14로 생겼는지
- 🔴 heartbeat 행 자체가 없으면 **크게 보고**
- `us-perf`의 `nasdaqError`도 함께(🔴 성공했으면 예상 밖이므로 보고)
- 🔴 **크론 호출 금지** · **`BUDGET_MS` 판정 금지**

### 1-6. 🔴 미추적 파일 정리
`docs/LENS_DISPOSITION_2026-08-08.md`가 작업트리에 미추적으로 남아 있다(1023이 플래그).
- 🔴 **내용을 판단하지 말고** 그대로 커밋에 편입해 보존한다(1014의 `step_orders` · 1015의 `ANSWERABILITY_MAP.md`와 같은 처리)
- 🔴 **삭제하지 말 것**

### 1-7. 산출물
`docs/probe_1024_universe_sic.md`
- 맨 앞에 **⓪-4 네 갈래 중 어느 것인지** + **SIC 확보율** + **1021 분류와의 불일치 건수**
- 1-1 SIC 확보(재조회 횟수 명시 · 미확보 사유별)
- 1-2 SIC 기반 종류 분포 — 🔴 **REIT를 별도 행으로**
- 1-3 교차표 + 개별 확인 결과 + `lens_scores` 상위 1000 내 제외후보 전수
- 1-4 시나리오 가상 커버리지 + 🔴 **경고 문구**
- 1-5 W1 첫 시험 결과 또는 미도래·구코드 사유

🔴 카탈로그의 유니버스 구성 절을 **SIC 기반으로 갱신**한다(1021 서술은 취소선 보존, md+xlsx 동시).

## 2. 커밋 전 게이트
- 게이트 7: git 미추적 파일 참조 검사 — 🔴 런타임 `fs.readFileSync('/tmp/...')`도 잡을 것 · 🔴 **§1-6의 미추적 파일이 이번 커밋에 들어갔는지 확인**
- 게이트 8: push 후 **Vercel 배포 성공 + verify 초록을 눈으로 확인**하기 전까지 미완료
- `docs/CHANGELOG.md` 1024 항목 · `docs/STEP_LEDGER.md` 1024 등재

## 3. DoD
- [ ] ⓪-4 네 갈래 중 어느 것인지 맨 앞에 + SIC 확보율 + 1021과의 불일치 건수
- [ ] 🔴 **전수 비용을 먼저 적었다**(재조회 0회 가능 여부 · 필요 시 5,976 ≈ 10분)
- [ ] 1-2 분류에 🔴 **REIT(6798)가 별도 행**으로 있고 **제외 대상이 아님**이 명시됐다
- [ ] 1-3 교차검증 완료 — 🔴 **`lens_scores` 상위 1000 내 제외후보는 전수 개별 확인**
- [ ] `revdcf_results` 604 중 제외후보 건수(1021 기준 0건과 대조)
- [ ] 1-4 시나리오 + 🔴 **경고 문구 포함** · 어느 것도 채택 안 함 · **REIT는 어디서도 안 뺐다**
- [ ] 1-6 미추적 파일 편입(삭제 아님)
- [ ] 1-5 W1 `stage` 값 또는 미도래/구코드 사유 · 🔴 배포 시각을 크론 시각과 대조했다
- [ ] 카탈로그 유니버스 절 SIC 기반 갱신, md·xlsx 일치, 취소선 보존
- [ ] 🔴 **DB 행수 변화 0 · 프로덕션 코드 diff 0** · `data/us_symbols.json` diff 0 · `app/api/cron/revdcf/route.ts` diff 0
- [ ] tsc 0 · test 전량 통과
- [ ] Vercel 배포 성공 + verify 초록 **눈으로** 확인
- [ ] 마지막에: 못 한 축 / 철회·정정한 것 / 미측정으로 남은 것

🔴 **유니버스 재정의 · 게이트 임계 · 배선은 전부 장은태 판정이다. 이 STEP은 분모에 무엇이 있는지 정확히 세는 것까지다.**
