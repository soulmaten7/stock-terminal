# STEP 1062 — 렌즈 전수감사 ⑥ F-스코어(Piotroski) (🔵 읽기 전용 · 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 수동 실행 0)

> 🔴 **판정은 장은태가 한다.** 이 STEP은 결함을 실측으로 뜯어 적는 데까지다.
> 🔴 **자산성장(⑦)보다 F-스코어를 먼저 놓은 근거 둘** — ㉠새 순서 기준(*"주장과 근거의 간극이 가장 큰 것부터"*, STEP1061에서 채택): F-스코어는 **「검증」 등급**(`LENS_STRENGTH_MAP:37` *"검증=모멘텀·저변동·F-Score·밸류(E/P)"*)인데 원전 표본은 **고B/M 가치주 5분위**이고 우리는 전 종목에 쓴다. 자산성장은 등급이 이미 `weakSignal`이라 간극이 작다. ㉡🔴 **이 STEP이 판정 대기 하나를 직접 푼다** — 「나」 Piotroski 32.3% 수용 여부의 **재료가 여기서 나온다**(⓪-4①).
> 🔵 읽기 전용 — 외부 신규 취득은 **원문 확보 목적만**(⓪-3).

---

## ⓪ 전제 점검

### ⓪-1. 직전 산출물 — 원문 인용

| 출처 | 인용(그대로) |
|---|---|
| `docs/LENS_AUDIT_05_TECHNICAL.md` 결함① | *"`toneForKey`·`tonesFromStates`·`tonesFor`·`firstPosLens`는 **등급(`strong`/`ref` 등)을 전혀 보지 않는다** … `reference`(검증 안 됨) 등급의 200일선 신호가 그 종목의 대표 「강점」 배지가 된다"* 🔴 **F-스코어에도 같은 레이어가 걸린다 — 다만 F-스코어는 「검증」 등급이라 같은 사실이 결함이 아닐 수 있다. §2-5에서 구분한다.** |
| 같은 문서 결함② | *"`lib/lenses.ts:196` `const closes = d.closes;`(**momentum만** `d.adjCloses ?? d.closes`를 씀) … **③저변동 신규①과 뿌리가 같다** — 개별 결함 2건이 아니라 **하나의 공통 결함이 두 렌즈에서 반복**"* → 🔴 **F-스코어는 가격을 쓰지 않는다. 이 결함은 안 걸린다**(확인만 하고 넘어갈 것). |
| 같은 문서 결함③ | `pos52` 최소 표본 가드 부재 — 🔴 **F-스코어에는 `needThree`(3개 회계연도)·`gap`(연도 불연속) 가드가 있는 것으로 보인다**(`lib/fscore.ts:29~31`). **실제로 작동하는지가 §2-4.** |
| 같은 문서 요약 | *"앞선 결함 15건 생존 → **15/15 그대로**"* → 🔴 이제 **18건**(15 + ⑤신규 3). |
| `docs/ROADMAP_V2.md` W-2-4 재무건전성 행 | *"🔴 조건 ②(산출 가능 범위, STEP1056 추가) — **9신호 전부 성립하는 종목은 기준연도 보유 3,606건 중 32.3%뿐**(좌표 `probe_1054` §2-5). 병목은 ⑤Δ레버리지 64.2%·⑧Δ마진 51.6% … **부분 합산은 H-7 위반이라 32.3% 바깥은 F-Score를 낼 수 없다.**"* |
| `lib/lensCopy.ts` `fscore.scope.failure` | *"원전은 **가치주 집단 안의 도구**예요(전 종목용 일반 품질 점수가 아니에요) — 효과도 소형·중형의 소외된 가치주에 몰렸고 대형주에선 약했어요. **은행·보험은 유동비율 등 항목이 없어 산출 불가**예요. 점수가 높다고 오른다는 뜻은 아니고, 우리 '양호(≥7)'는 **자체 기준**이에요(피오트로스키의 '높음'은 8~9점)."* 🔴 **이미 공개된 한계 — 결함으로 이중 계상 금지(⓪-3b).** |

🔴 **직전 STEP(1061)이 「못 한 것」으로 남긴 것 중 이 STEP이 건드리는 것**: 없음(⑤의 미완은 전부 가격 계열·집계 레이어 라이브 관측이고, ⑥은 재무 기반이다). 단 **집계 레이어(결함①)는 §2-5에서 F-스코어 관점으로만 본다.**

### ⓪-1a. 🔴 로드맵 원문 대조 (`docs/ROADMAP_V2.md` — 여섯 층 전부)

| 층 | 걸리는 절 | 인용(원문 그대로) |
|---|---|---|
| **WHY** | 조건 1 (`:65`) | *"**정확해야 한다** … 🔴 계산이 먼저다 — 협상 대상 아님"* |
| **HOW** | H-7 (`:203~206`) | *"원전 있는 모델에 **원전에 없는 항목 추가** 🔴 금지"* · *"**결측을 채우고 실측인 척** 🔴 **절대 금지**"* → 🔴 **부분 합산이 정확히 이 금지에 걸린다. §2-1의 최우선 배제 대상.** |
| **WHAT** | W-2-4 재무건전성 | *"🟡 **Piotroski F-Score** 단독 … 최종 선택은 안 바뀜(경쟁 후보 없음)"* — 🔴 **모델은 확정. 이 STEP은 그 모델이 맞게 계산되는지만 본다.** |
| **만드는 절차** | ⬜ 확정 대기 | 무관 |
| **관문·순위** | F-1 ②③ (`:334·335`) | ② *"정확한가 — 계산이 맞는가, **검산할 수 있는가**"* · ③ *"정직한가 — **계산 방법을 밝힐 수 있는가**"* |
| **완성의 정의** | C-1 #5 (`:495`) | *"**경계 처리** — **계산 불가 조건**·최소 표본·**결측 표기**"* → 🔴 **이 렌즈에서 가장 중요한 항목이다.** `LENS_COMPLETION_STANDARD:137` STEP889: *"계산 불가 사유는 뭉뚱그리지 않고 **실제 원인별로 정확히 분기**한다."* |

### ⓪-1b. 기존 답 확인 — 🔴 `ls docs/` 전수
2026-08-18 확인분(원격 `main` = `0f3cc35`): 감사 다섯(①밸류·②모멘텀·③저변동·④퀄리티·⑤기술) — **⑥F스코어·⑦자산성장은 없음.**
🔴 그러나 F-스코어는 **여러 STEP이 이미 손댄 렌즈**다 — `lib/fscore.ts:4` 주석에 **STEP801**, `:14` 주석에 **STEP810 §5**. `LENS_DEV_PLAYBOOK` **#48**이 *"원문 Table1+§2.3 대조 완료"*라 적혀 있다(W-2-4 인용). **`ls docs/`로 `STEP_80*`·`STEP_81*`·`probe_*` 전수를 훑고 `LENS_DEV_PLAYBOOK` #48을 먼저 읽어라.** 있으면 그만큼 범위를 줄인다. **중복 감사하지 않는다.**

### ⓪-2. 건드리는 슬롯
**해당 없음** — 읽기 전용. 재무 슬롯을 **조회·대조만.**

### ⓪-3. 걸리는 금지사항
- [ ] **코드 diff 0 · DB 쓰기 0 · 화면 변경 0** — 🔴 7렌즈 라이브 화면 수정은 매번 별도 승인(899·901 위반 전례)
- [ ] 🔴 **크론 수동 실행 금지**
- [ ] 🔴 **재무 데이터 재취득 금지** — `us_fundamentals`(SEC, 2026-08-17 23:38 fetched)가 이미 DB에 있다. **외부 신규 취득은 원문(논문) 확보 목적만.**
- [ ] **KR 전면 동결** — US만. KR은 대조군 조회로만
- [ ] `data/us_symbols.json`·`.github/workflows/**`·`vercel.json` 수정 금지
- [ ] `REVDCF_ENABLED` OFF 유지 · `Q1_ENABLED` 무접촉
- [ ] 🔴 **유료 논문 구매 금지 · robots.txt 우회 금지 · API키 발급 금지 · 키 VALUE 타이핑 금지.** 못 구하면 **「원문 미확보」**라 적는다
- [ ] 🇺🇸 US 단독

### ⓪-3b. 🔴 결함의 소재 확정
관측 지점 = `lib/fscore.ts` 전부 · `lib/lenses.ts`의 fscore 렌즈 · `lib/lensCopy.ts`의 `fscore` 블록(`about`·`scope` 3칸·`na`) · `lib/lensCompute.ts`(재무 공급) · `lib/lensTones.ts` · `lens_scores`·`lens_cuts`·`lens_state_changes`(DB). **전부 프로덕션이다.**

🔴 **명령서 작성 중 `lensCopy.ts`의 `fscore` 블록을 직접 열었다. 중복 조사하지 말 것.** `scope.failure`가 **이미 네 가지 한계를 공개**한다(⓪-1 인용): ①가치주 집단 도구 ②대형주에서 약함 ③은행·보험 산출 불가 ④「양호≥7」은 자체 기준.
🔴 **그러므로 이 넷은 결함이 아니다.** ⑤ 감사가 기술에서 했던 것처럼, **감사가 찾을 것은 「문구가 약속한 것을 코드가 지키는가」다**(C-1 #6).

### ⓪-4. 🔴 반증 조건

**전제 ①** 🔴 **이 STEP의 최우선** — 「나」의 근거 *"9신호 전부 성립 32.3%뿐 → 32.3% 바깥은 F-Score를 낼 수 없다"*가 **라이브에서도 참이다.**

🔴 **근거는 내가 명령서 작성 중 직접 조회한 것이다**(Cowork Supabase MCP, DB 조회 2026-08-18):
```
lens_scores (market=US, updated_at 균일 2026-08-17 22:03:40 UTC)
  전체 999행 · fscore_value 718 (71.9%) · assetgrowth 986 (98.7%) · technical 983 · quality 857
```
**71.9% vs 32.3% — 2배 이상 차이난다.**

- 원인 후보 셋. **㉢을 먼저 배제한다:**
  - ㉠ **유니버스 차이** — `probe_1054`는 전체 3,606건, 라이브는 **시총 상위 1,000**. 대형주는 재무 완비율이 높다. **가장 유력.**
  - ㉡ **재료 출처 차이** — `probe_1054`는 SEC(`us_fundamentals`), 라이브 7렌즈는 **야후 `fundamentalsTimeSeries`**(STEP1059 §2 확정). 야후가 필드를 더 줄 수 있다.
  - ㉢ 🔴 **부분 합산** — 9신호 중 일부만으로 점수를 낸다면 **H-7 「결측을 채우고 실측인 척」 절대 금지**에 정면으로 걸린다. **최악의 결함이다. 반드시 코드로 먼저 배제하라.**
- 🔴 **어느 쪽이든 「나」 판정의 재료가 바뀐다.** ㉠㉡이면 *"32.3%뿐이라 못 쓴다"*는 전제가 **라이브에는 안 걸리는 것**이 되고, ㉢이면 **즉시 멈춰야 할 결함**이다.
- 🔴 **③저변동 감사가 「원전 없음」이 틀렸음을 발견한 것과 같은 패턴일 수 있다** — 문서의 숫자가 라이브를 설명하지 못한다.

**전제 ②** — 9신호의 정의가 원전(Piotroski 2000)과 같다.
- 🔴 **분모 주의** — `lib/fscore.ts:4` 주석: *"ROA·자산회전율 분모 = **기초(전기말) 총자산**(Piotroski 2000 원전·STEP 801)"*. **퀄리티(GP/A)에서는 STEP815가 801의 「기초」 변경을 되돌렸다**(원전이 당해 기말이므로). **F-스코어는 원전이 실제로 기초를 쓰는가** — 원문으로 확인. **둘이 서로 다른 게 맞을 수 있다. 한쪽 기준으로 다른 쪽을 재단하지 말 것.**
- 틀렸다면 → 원전 대조표에 차이로 적는다.

**전제 ③** — 계산 불가 사유가 **원인별로 분기**돼 있다(C-1 #5 · STEP889).
- 코드에 `needThree`("3개 회계연도 부족")·`gap`("연도 불연속")·`dataMissing`("재무 부족") 셋이 보인다(`fscore.ts:29~31`).
- 틀렸다면(=하나로 뭉쳐 나가면) → **결함.** ①밸류 감사·STEP889가 같은 것을 지적했다.
- 🔴 **DB에서 실제 분포를 재서 셋이 실제로 갈리는지 확인한다.**

**전제 ④** — `lensTones.ts` 집계 레이어(⑤ 결함①)가 F-스코어에도 걸린다.
- 걸리는 건 맞다(`fscore: ["strong","weak","mid"]`). 🔴 **그러나 F-스코어는 「검증」 등급이므로 「강점」으로 셈해지는 것이 부당하지 않다.** ⑤의 결함은 *"참고용 등급이 검증 등급과 동일 가중"*이었다.
- 🔴 **그러니 여기서 같은 것을 결함으로 다시 적으면 이중 계상이다.** 확인만 하고 *"F-스코어에는 해당 없음"*으로 적는다.

### ⓪-5. 🔴 3번 생각
1. **범위** — 곁가지 아님. 「나」 판정이 이 결과에 달려 있고, F-스코어는 W-2-4에서 **재무건전성 칸의 단독 모델**로 확정돼 있다.
2. **표본** — 🔴 **전수.** `lens_scores` US 999행 전량 · `us_fundamentals` 전량. 외부 재무 취득 0.
   🔴 **전수의 조건** — 최소 둘로 교차: ㉠정방향(라이브 결측 271건의 사유 분포) ㉡역방향(`us_fundamentals`에 3년 연속이 있는데 라이브는 결측인 종목 — **있으면 그게 결함이다**) ㉢전체 나열 후 필터.
3. **시점** — 🔴 `lens_scores.updated_at`과 `us_fundamentals.fetched_at`, 조회 시각(UTC) **셋 다 적는다.** 회계연도 기반이라 시점이 답을 바꾼다.

---

## 1. 배경

**확정**(실측 · 원격 `main` = `0f3cc35` · DB 2026-08-18)
- 라이브 커버리지 **718/999 = 71.9%** ↔ 문서의 **32.3%**(`probe_1054` §2-5) — **미해명**
- `lib/fscore.ts`는 `supported: boolean` + `reason` 구조로 **계산 불가를 명시 처리**하는 형태를 갖췄다(부분 합산 안 하는 것처럼 보임 — **확인 필요**)
- 분모 = 기초(전기말) 총자산, 근거 STEP801 — 🔴 퀄리티와 반대 방향
- 라벨 = 양호/보통/취약(STEP810 §5 통일)
- 공개 문구가 **한계 넷을 이미 밝힌다**(⓪-3b)
- `LENS_DEV_PLAYBOOK` #48이 **원문 Table1+§2.3 대조를 이미 완료**했다고 적혀 있다

**확정 안 됨** 🔴
- 71.9% vs 32.3%의 **원인**(㉠유니버스 ㉡출처 ㉢**부분 합산**)
- 9신호 각각이 원전과 같은가
- 계산 불가 사유가 **실제로 갈리는가**
- 앞선 결함 **18건**의 생존 여부

---

## 2. 할 일

### 2-1. 🔴 71.9% vs 32.3% 갈림 해소 — **최우선** (⓪-4①)
- **먼저 ㉢(부분 합산) 배제.** `lib/fscore.ts` 전체를 읽어 **9신호 중 하나라도 결측이면 `supported:false`가 되는지** 코드로 확정. 🔴 **되면 배제, 안 되면 즉시 최상위 결함으로 올리고 이 STEP의 나머지보다 먼저 적는다.**
- 그다음 ㉠㉡. 같은 유니버스(시총 상위 999)에서 `probe_1054`의 방식으로 SEC 기준 커버리지를 다시 재고, 야후 기준(라이브)과 대조.
- 🔴 **결론을 「나」 판정의 재료로 명시적으로 적는다** — *"32.3%는 어느 모집단의 숫자이고, 라이브가 실제로 답을 내는 비율은 얼마인가."*

### 2-2. 원전 확보 (감사 3축 ②)
Piotroski, J. (2000), *"Value Investing: The Use of Historical Financial Statement Information to Separate Winners from Losers"*, **Journal of Accounting Research 38(Supplement)**.
🔴 **④가 Novy-Marx를, ⑤가 George-Hwang을 무료 공개본으로 확보한 방식 그대로.** 저자 홈페이지·워킹페이퍼 우선. **유료 구매 금지 · robots 우회 금지.** 못 구하면 **「원문 미확보」**.
🔴 **단 `LENS_DEV_PLAYBOOK` #48이 이미 대조했다고 한다 — 먼저 읽고, 겹치는 부분은 인용으로 대체한다. 중복 대조 금지.**

### 2-3. 9신호 원전 대조표
9개 각각에 **원전 정의 / 우리 구현 / 차이 / 차이의 영향** 4칸(C-1 #1은 5칸 — 항목 포함). 🔴 특히 —
- ROA·자산회전율의 **분모**(기초 vs 기말) — ⓪-4②
- ⑤Δ레버리지·⑧Δ마진(문서가 지목한 병목 둘)의 정의
- 「양호 ≥7」이 **원전의 8~9와 다르다**는 것이 화면에 나오는가(`scope`엔 있다 — 카드에도 있나)

### 2-4. 코드 완독 + 경계 처리 (⓪-4③)
`lib/fscore.ts` 전부 · `lib/lenses.ts`의 fscore 렌즈 · `lib/lensCompute.ts`의 재무 공급.
확인할 것 — ㉠3개 회계연도 가드가 **실제로 작동**하는가 ㉡연도 불연속(`gap`) 판정이 **STEP803의 「연도 차 ≠ 1」 검증**과 같은 규칙인가 ㉢액면분할 조정(`splitNote`)이 어디에 쓰이는가 ㉣**은행·보험**에서 어느 신호가 먼저 죽는가.

### 2-5. 주장 정합 (C-1 #6)
⓪-3b의 공개 문구 넷을 **한 줄씩** 코드와 대조. **지킴 / 안 지킴 / 부분** + 좌표.
🔴 **집계 레이어(⑤ 결함①)는 「F-스코어에는 해당 없음」으로 적고 넘어간다**(⓪-4④ — 이중 계상 금지).

### 2-6. DB 전수 실측 (감사 3축 ③)
- `lens_scores` US 전량 — `fscore_value` 분포(0~9 각 점수별 건수) · `fscore_state` 분포 · **결측 271건**
- 🔴 **계산 불가 사유 분포** — `needThree` vs `gap` vs `dataMissing`이 **실제로 갈리는가**(⓪-4③ · STEP889)
- 🔴 **역방향**(⓪-5②㉡) — `us_fundamentals`에 3년 연속 재무가 있는데 라이브는 결측인 종목. **있으면 그게 결함이다.**
- `lens_cuts` fscore의 컷과 `as_of` · `lens_state_changes` fscore 건수(`LENS_DISPOSITION` §1은 **112건**) 재현 여부
- 🔴 `updated_at` 균일성(③ 감사 신규② 스테일 극단치 재확인)

### 2-7. 손계산 검산 3종목 (C-1 #3) — 🔴 **이번엔 할 수 있다**
⑤는 원시 종가가 DB에 없어 **구조적으로 못 했다.** F-스코어는 다르다 — `us_fundamentals`에 SEC의 `net_income`·`cash_from_ops`·`total_assets`·`total_liabilities`·`revenue`·`equity`가 **3개 연도로 이미 있다.**
표본 3종목의 9신호를 손으로 채점해 라이브 값과 대조한다.
🔴 **STEP1059의 실패를 반복하지 않는다** — 대조 대상의 **회계연도를 먼저 고정**하고 계산한다(STEP1060이 밝힌 원인).

### 2-8. 🔴 앞선 결함 **18건** 생존 확인
①밸류 6 + ②모멘텀 5 + ③저변동 4 + ⑤기술 3.
🔴 **⑤ 감사가 «15/15 그대로»로 확정했다**(08-18 `git log` 변경 0건 포함). **다시 전수 조사하지 말고 그 결과를 인용한 뒤, 그 이후 변경분만 `git log`로 확인**해 차이가 있으면 갱신한다. **⑤ 신규 3건은 새로 상태를 적는다.**

### 2-9. 질문 귀속 — 맞추지 않는다
`lensCopy.ts`의 `fscore.question`은 **"재무가 튼튼한가?"**다. 🔴 **W-2-4가 이미 「재무건전성」 칸의 단독 모델로 확정했다.** 새로 도출하지 말고 **코드 문구와 정본 배정이 무모순인지만 확인**한다(⑤가 한 방식). 🔴 **억지로 맞추지 않는다.**

### 2-10. 산출물
`docs/LENS_AUDIT_06_FSCORE.md` — 앞선 다섯과 **같은 형식**(요약표 → ⓪-4 반증조건 판명 → 원전 대조표 → 🟢 잘 된 것 → 결함별 상세 → 앞선 결함 생존 → 못 한 것/철회·정정한 것/미측정).
🔴 **새 버전 파일을 만들지 않는다.** 기존 문서를 고칠 일이 생기면 **그 문장만** 고친다(취소선 보존 + 사유 + 좌표).
🔴 **`ROADMAP_V2.md` W-2-4의 32.3% 서술**은 §2-1 결과에 따라 **정정이 필요할 수 있다.** 필요하면 **그 문장만** 고치고, 판정(수용 여부)은 **하지 않는다.**

---

## 3. 🔴 하지 말 것
- 🔴 **판정하지 않는다.** 「나」(32.3% 수용 여부)를 정하지 않는다. **재료를 놓는 데까지.**
- 🔴 **이미 공개된 한계 넷을 결함으로 이중 계상하지 않는다**(⓪-3b). ⑤ 감사가 기술에서 이 함정을 피한 방식 그대로.
- 🔴 **집계 레이어 결함(⑤ 결함①)을 F-스코어에서 다시 세지 않는다**(⓪-4④).
- 🔴 **코드·화면·DB 무접촉.** 결함을 찾아도 고치지 않는다.
- 🔴 **크론 수동 실행 금지 · 재무 재취득 금지**(외부는 논문 확보만).
- 🔴 **유료 논문 구매 금지 · robots 우회 금지.** 못 구하면 「원문 미확보」.
- 🔴 **퀄리티(GP/A)의 분모 기준으로 F-스코어를 재단하지 않는다** — 원전이 서로 다를 수 있다(⓪-4②).
- 🔴 **KR 무접촉.**
- 🔴 **자산성장(⑦)을 함께 감사하지 않는다.** 하나를 하나씩.
- 🔴 **손계산 대조의 회계연도를 먼저 고정한다**(STEP1059 실패 반복 금지).

## 4. 커밋 전 게이트
- 게이트 7: git 미추적 파일 참조 검사
- 게이트 8: push 후 **GitHub Actions verify 초록 + Vercel 배포 성공을 눈으로 확인**
- 게이트 9: 새 테이블·크론·관측 필드 없음 → 해당 없음
- `docs/CHANGELOG.md` 항목 · `docs/STEP_LEDGER.md` 등재
- 🔴 **브랜치 규약(STEP1056 §2-7)** — `main`에 직접 push. **브랜치·PR 만들지 않는다. 예외 없음.**

## 5. DoD
- [ ] ⓪-1a 로드맵 **여섯 층** 대조 · ⓪-1b `ls docs/` 전수 + **`LENS_DEV_PLAYBOOK` #48 선(先)확인**
- [ ] 🔴 ⓪-4① **최우선 — ㉢부분 합산 배제 여부**를 코드로 확정. 그다음 71.9% vs 32.3% 원인 확정
- [ ] 🔴 그 결과를 **「나」 판정의 재료로** 명시 — *"32.3%는 어느 모집단이고 라이브 실답률은 얼마인가"*
- [ ] ⓪-4② 원전 확보 여부 + **분모(기초 vs 기말)** 원문 확인(못 구하면 「원문 미확보」)
- [ ] ⓪-4③ 계산 불가 사유 **셋이 실제로 갈리는지** DB 실측
- [ ] ⓪-4④ 집계 레이어 — **「해당 없음」으로 적고 넘어갔다**
- [ ] 2-3 **9신호 원전 대조표** — 특히 ⑤Δ레버리지·⑧Δ마진·「양호≥7」
- [ ] 2-4 코드 완독 — 3년 가드·`gap` 규칙·분할 조정·은행보험 실패 지점
- [ ] 2-6 DB 전수 — 점수 분포·사유 분포·🔴 **역방향(SEC엔 있는데 라이브 결측)**·112건 재현·`updated_at`
- [ ] 🔴 2-7 **손계산 3종목**(⑤가 못 한 것 — 이번엔 재료가 있다) · **회계연도 먼저 고정**
- [ ] 🔴 2-8 앞선 결함 **18건** — ⑤ 결과 인용 + 이후 `git log` 차이만
- [ ] 2-10 `LENS_AUDIT_06_FSCORE.md` 생성 (+ 필요시 W-2-4 문장만 정정)
- [ ] 🔴 **양방향 대조**(⓪-5②) — 정방향·역방향 둘 다 돌았다
- [ ] 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 수동 실행 0 · 재무 재취득 0 · tsc 0 · test 전량 통과
- [ ] `updated_at`·`fetched_at`·조회 시각(UTC) 셋 다 병기
- [ ] 마지막에: **못 한 축 / 철회·정정한 것 / 미측정으로 남은 것**

🔴 **판정은 장은태가 한다. 이 STEP은 결함을 놓는 것까지다.**
