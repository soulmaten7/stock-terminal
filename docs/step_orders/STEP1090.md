# STEP 1090 — 🔴 **`CLAUDE.md` 「세션 루틴」 절 갱신** — 새 세션이 빠짐없이 읽게 만든다 (🟡 문서 전용 · 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · 크론 수동 실행 0)

> 🔴 **장은태 요청(2026-08-19)**: *"새 세션이 진행할 때 읽고서 계속 진행할 파일이 있으면 **내가 뭐라고 새 세션에 말을 해야 빠짐없이 읽고 제대로 진행**하는지 · 대화 후에 그 파일들에 업데이트가 있을 수도 있으니 **그 변수도**"*
> 🔴 **새 파일을 만들지 않는다.** `CLAUDE.md`에 **이미 「세션 루틴 → 세션 시작 시」 절이 있다**(:697). 그것을 완전하게 만든다.
> 🔴 **`docs/_archive/`에 같은 유형이 6개 죽어 있다**(`NEW_SESSION_HANDOFF`·`NEXT_SESSION_PLAYBOOK`·`NEXT_SESSION_START`·`SESSION_BOOT`·`SESSION_KICKOFF`·`session-context`) — `CLAUDE.md:631` *"⛔ 현재상태 기록용 새 파일 신설 금지"*가 그 결과다. **되풀이하지 않는다.**
>
> **왜 지금인가 — 3번 규칙(2026-08-19):**
> **축 A(원문)** — `CLAUDE.md:697~701`(세션 시작 4단계) · `:631`(새 파일 금지 + 읽는 순서) · `:667`(*"`STATE.md` = 세션 시작 최우선"*).
> **축 B(실무·실측)** — 그 4단계에 **오늘 생긴 것이 없다**: `BUILD_SEQUENCE §6-C` 판정 대장(76행) · `§6-B-1` 확정된 칸 넷 · `STATE.md ⓪` 인수인계 절. 표현도 낡았다(*"6개국 파이프라인"* ↔ US 단독 전환). `:686`에 **미완료 항목**(hooks가 옛 4문서 검증)이 남아 있다.
> **축 C(반대 증거)** — ㉠**한 줄이 낡을 수 있다** → 🔴 **아니다. 내용을 복제하지 않고 파일을 가리키기만 하면 안 낡는다.** _archive 6개가 죽은 이유가 **상태를 복제했기 때문**일 가능성이 높다(§1-0에서 확인) ㉡**Claude Code는 `CLAUDE.md`를 자동 로드하니 말이 필요 없다** → 🔴 **Cowork은 자동 로드하지 않는다.** 한 줄은 **Cowork용**이다 ㉢**절을 늘리면 비대해진다** → 🔴 **가리키기만 하고 내용을 옮기지 않는다.**

---

## ⓪ 전제 점검

### ⓪-1. 열어야 할 것
```
CLAUDE.md                      # :631 · :666~668(문서 위계 표) · :686 · :688~715(세션 루틴 전체)
docs/STATE.md                  # ⓪ 인수인계 절 — 새 세션이 첫 번째로 읽을 것
docs/BUILD_SEQUENCE.md         # §2(만드는 절차) · §6·§6-B·§6-B-1·§6-C(판정 대장) · §8(반복 실수)
docs/_archive/SESSION_BOOT.md · NEXT_SESSION_START.md · NEW_SESSION_HANDOFF.md   # 🔴 왜 죽었는지 확인(§1-0)
.claude/hooks/ (있으면)         # :686 미완료 항목 상태 확인 — 🔴 읽기만, 고치지 않는다
```

### ⓪-2. 🔴 이 STEP이 하지 않는 것
- **새 파일 신설 0**(신규는 이 명령서 하나).
- **`CLAUDE.md`의 절대규칙·금지사항을 삭제하거나 약화시키지 않는다.**
- **상태(현재 위치·다음 할 일)를 `CLAUDE.md`에 복제하지 않는다** — 가리키기만.
- **판정하지 않는다.**

---

## §1-0. 🔴 먼저 — `_archive`의 6개는 왜 죽었나
`docs/_archive/`의 `SESSION_BOOT.md`·`NEXT_SESSION_START.md`·`NEW_SESSION_HANDOFF.md` 셋(또는 여섯 전부)을 열어 **무엇을 담고 있었는지** 본다.
- **상태를 복제했으면** → 🔴 그것이 죽은 원인이다. 이번 갱신은 **가리키기만** 한다는 원칙을 §1에 못 박는다.
- **다른 이유였으면** → 🔴 **그 이유를 기록**하고, 이번 설계가 같은 함정을 피하는지 확인한다.
🔴 **확인 결과를 보고한다.** 안 열었으면 "안 열었다".

## §1. 「세션 시작 시」 절 갱신 (🔴 가리키기만, 내용 복제 0)

현재 4단계를 **읽는 순서 그대로 유지**하되, 빠진 것을 넣는다:

| 순서 | 무엇 | 왜 |
|:--:|---|---|
| 1 | **`docs/STATE.md`** — 🔴 **맨 앞 「⓪ 직전 세션 요약」부터** | 직전 세션이 어디까지 갔고 무엇이 남았는지 |
| 2 | 🆕 **`docs/BUILD_SEQUENCE.md` §6-C 판정 대장** | 🔴 **지금 정해야 할 것 전수(76행)** — 다음 세션의 실제 출발점 |
| 3 | 🆕 **같은 문서 §2(만드는 절차 6단계) · §6-B(판정 B) · §6-B-1(확정된 칸)** | **무엇을 어떻게 만드는가**의 정본 |
| 4 | **`docs/SYSTEM_MAP.md`** | 아키텍처·크론·테이블 — 🔴 *"6개국 파이프라인"* 표현을 **US 단독**으로 정정 |
| 5 | 작업별 PLAYBOOK · 이력 `CHANGELOG.md` | 기존 그대로 |
| 6 | 사용자에게 제안 → 확인 후 명령어 작성 | 기존 그대로 |

🔴 **각 항목은 「파일:절」만 적는다.** 그 안의 내용(행수·상태·다음 할 일)을 `CLAUDE.md`에 옮기지 않는다 — 옮기는 순간 낡는다.

### §1-b. 🔴 지켜야 할 규칙을 이 절에서 가리킨다 (요약 복제 아님, 좌표만)
- **3번 규칙**(축 A 원문 / 축 B 실무 / 축 C 반대 증거 · 검증 3 · 검수 3 · **못 거친 항목은 「못 했다」 명시**)
- **근거 좌표 의무** — 단정할 때 `파일:절`, 못 달면 *"내 추측"*이라 명시
- **낱말의 뜻을 Cowork이 정하지 않는다**
- **기존 파일 업데이트 · 새 파일 금지**
- **답변마다 `[3중 점검] ⓪`에 이번에 연 파일·줄번호**
🔴 각각이 `CLAUDE.md`·`/preferences.md` 어디에 있는지 **좌표만** 적는다.

## §2. 🔴 「새 세션에 할 말」 한 줄 — `CLAUDE.md` 안에 박는다

「세션 시작 시」 절 **맨 위**에 장은태가 복사해 쓸 문장을 **코드블록으로** 둔다. 요건:
- **한두 줄.** 길면 안 쓰게 된다.
- **파일을 가리키기만 한다** — 상태·행수·날짜를 넣지 않는다(넣으면 낡는다).
- **Cowork용이다** — Claude Code는 `CLAUDE.md`를 자동 로드하지만 **Cowork은 안 한다**.
- 🔴 **문구는 실행 측이 위 요건에 맞게 만든다.** 예시(그대로 쓰지 말고 더 나으면 고칠 것):
  > `CLAUDE.md`와 `docs/STATE.md ⓪`를 먼저 읽고, `CLAUDE.md`「세션 시작 시」 순서대로 진행해.
- 🔴 **바로 아래에 한 줄 주석**: *"이 문장은 파일을 가리키기만 하므로 **문서가 바뀌어도 고칠 필요가 없다**. 바뀌는 것은 `STATE.md ⓪`와 `§6-C`이지 이 문장이 아니다."*

## §3. 「세션 종료 시」 절 갱신 — 오늘 배운 것 반영

현재 4단계(`STATE` 덮어쓰기 · `CHANGELOG` 블록 · `SYSTEM_MAP` · git push)에 **추가**:
- 🆕 **지침 문서 역방향 대조** — 🔴 *"grep(정방향)만으로는 「빠진 서술」을 못 찾는다. 문서에서 출발해 「내 담당 중 뭐가 바뀌었나」를 묻는다"*(`BUILD_SEQUENCE §8` #6, STEP1089).
- 🆕 **마감 체크 대상 명시** — `STATE.md`·`CHANGELOG.md`·`STEP_LEDGER.md`·`SYSTEM_MAP.md`(코드/스키마 바뀌면)·`LENS_DEV_PLAYBOOK.md`(렌즈 STEP이면)·`INDEX.md`(새 절/자산 생기면)·표시본.
- 🔴 **「다 됐다」로 적지 않는다 — 못 한 것을 나열한다**(3번 규칙 마지막 조항).

## §4. `:686` 미완료 항목 처리
*"⚠️ Claude Code 할 일: `.claude/hooks`가 옛 4문서(NEXT_SESSION_START 등)를 검증하면 STATE·CHANGELOG 검증으로 교체"*
→ 🔴 **`.claude/hooks/`를 열어 실제 상태를 확인**한다(읽기만).
- 이미 교체됐으면 → ✅ **해소 표기**.
- 아직이면 → 🔴 **고치지 않는다.** `BUILD_SEQUENCE §6-C` 대장에 **판정 대기 1행** 등재(hooks 수정은 `.claude/` 접촉이라 별도 승인).

## §5. 검증 3
① **가리키기만 했는가** — `CLAUDE.md`에 넣은 것 중 **상태(행수·날짜·다음 할 일)를 복제한 것이 0건**인지. 있으면 **삭제**하고 좌표로 바꾼다. 삭제 개수 보고.
② **좌표 정확** — §1·§1-b가 적은 모든 `파일:절`을 **실제로 열어** 그 자리에 그 내용이 있는지 확인. 틀린 좌표 0건.
③ **새 세션 모의** — 🔴 **§2의 한 줄만 가지고 시작한다고 가정**하고, 그 경로로 **오늘 남긴 것(§6-C 76행·최근변화 6/9·검산 인프라·미해결 위험)에 전부 도달 가능한지** 추적한다. 도달 못 하는 것이 있으면 **경로를 보강**한다.

## §6. 검수 3
① **새 파일 0 · 약화 0** — 신규는 이 명령서 하나 · `CLAUDE.md` 절대규칙·금지사항 삭제/약화 0 · `docs/_archive/` 무접촉.
② **비대화 안 했는가** — 「세션 시작 시」 절이 **화면 한 눈에 들어오는 분량**인지. 길어졌으면 **내용을 좌표로 접는다**.
③ **정직한가** — §1-0(_archive 6개 사인)·§4(hooks 상태)를 **실제로 열고 판단**했는지 · 못 한 것 전부 나열.

## §7. 커밋 + 보고
- 🔴 **`main` 직접 push.** 브랜치·PR·worktree 금지. **`CLAUDE.md` + `docs/` 외 무접촉**(`.claude/`는 **읽기만**).
- 커밋 메시지: `docs: STEP1090 CLAUDE.md 세션 루틴 갱신 — 새 세션 부팅 경로 완비`

```
[STEP1090 결과]
①-0 _archive 6개: 몇 개 열었나 / 죽은 원인(상태 복제였나 다른 이유였나)
① 「세션 시작 시」 갱신: 6단계 · 새로 넣은 것 N개 · 낡은 표현 정정 M개("6개국" 등)
①-b 규칙 좌표: N개 (전부 실제 좌표 확인?)
② 🔴 새 세션에 할 말 한 줄 — **최종 문구를 그대로 적을 것** + 주석 넣었나
③ 「세션 종료 시」 갱신: 추가한 항목 N개
④ :686 hooks: 이미 교체됨 ✅ / 아직 → 대장 등재(통번호)
⑤ 검증 3 — 1)상태 복제 삭제 N건 2)틀린 좌표 N건 3)새 세션 모의(도달 못 한 것 N건 → 보강)
⑥ 검수 3 — 1)새파일0·약화0 2)비대화(절 길이) 3)정직
🔴 못 한 것: (전부 나열)
커밋: <해시>
```
