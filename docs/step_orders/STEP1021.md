# STEP 1021 — 커버리지 분모에 무엇이 들어 있는가 (읽기 전용 · 배선 0)

> 🔴 **오늘 밤 22:45 UTC가 W1(4단계 heartbeat)의 진짜 첫 시험이다.** 1018·1019 배포가 어젯밤 크론보다 늦었다(01:06/01:26 UTC). `revdcf` 코드를 건드리지 않는다.

---

## 0-A. 🔴 1019의 숫자 오류 — 두 진술이 섞였다

1020이 정정한 대로, 아래 두 문장은 **둘 다 참인데 서로 다른 것**이다:
- **"못 채운 종목은 182건"** — 결합 방식으로도 시총이 안 나오는 종목 수 (182/5,973 = 3.05% → 커버리지 96.95%)
- **"게이트까지 3건 부족"** — 97%에 닿으려면 182건 중 3건만 더 살리면 된다

🔴 **1019는 전자를 후자로 잘못 썼고, 나(Cowork)는 그 숫자를 1020 명령서에 그대로 인용했다.** 인용 자체는 정확했지만 **검산할 수 있었는데 안 했다**(0.05% × 5,973 ≈ 3 vs 3.05% × 5,973 ≈ 182).

🔴 그리고 **§0-A 경고가 이제 더 중요해졌다.** "3건만 살리면 게이트 통과"는 **산술적으로 여전히 참**이다. 그래서 더더욱, **182건 중 아무 3건이나 골라 채우는 방향으로 가면 안 된다.**

---

## ⓪ 전제 점검

### ⓪-1. 직전 STEP 산출물 — 원문 인용 (🔴 검산 가능한 것은 검산했다)

| 출처 | 인용 | 검산 |
|---|---|---|
| STEP 1020 판정 | **1-1(안정성)은 재현 불가.** `us_market_cap`이 심볼당 1행이라 과거 fresh 집합을 복원할 수 없고, 결합 커버리지 자체가 어제 처음 계산된 신규 지표라 **시계열이 애초에 존재하지 않는다** | — |
| STEP 1020 🔑 | **W1 관측 자체가 아직 시작되지 않았다** — STEP1018·1019의 Vercel 배포가 어젯밤 22:45 UTC 크론보다 늦었다(각각 **01:06 / 01:26 UTC**). 그 크론은 **여전히 구 코드로 돌았다.** 지금 heartbeat 부재는 **1007 구코드의 반복**이지 새 계측의 결과가 아니다. **진짜 첫 시험은 오늘 밤(08-14 22:45 UTC)** | — |
| STEP 1020 🔴 | 못 채운 종목은 **182건**. 1019의 계산 오류(0.05%p를 절대 건수로 환산) 정정 | ✅ 182/5,973 = 3.048% → 96.95% **일치** |
| STEP 1020 🔑 | **182건의 65.9%가 클로즈드엔드펀드·신탁** | ✅ 182×0.659 ≈ **120건** |
| STEP 1020 | **EL·HRL·BRC 같은 대형 우량주가 SEC 표준 주식수 태그를 XBRL 전체 이력에서 한 번도 안 썼다**(직접 조회로 확인) | — |
| STEP 1020 | CEF는 **투자회사법 서식(N-CSR 등) 체계상 이 태그 자체가 없는 구조적 문제** | — |
| STEP 1020 | 복수클래스 737건 중 **60건은 이미 96.95%에 포함**돼 있어 질문이 "살리면 오른다"가 아니라 **"이미 채워진 값의 신뢰도 문제"**로 바뀐다. 🔴 **엄격 기준을 적용하면 오히려 95.96%로 낮아진다** | ✅ 낮아지는 방향 |
| STEP 1020 최종 | **96.95%는 원 추정보다 구조적으로 더 낮다** | — |
| 현재 | `us_sector_relative` 08-10 **5일째 정지** · US `lens_cuts` 07-30 **16일째** · `us_market_cap_nasdaq` 0행 |

### ⓪-2. 이 STEP이 건드리는 슬롯

| 슬롯# | 검증 상태 | 이 STEP의 취급 |
|---|---|---|
| **#1 시가총액** | ✅ 1011 · 야후 93.9% · SEC 결합 96.95%(엄격 시 95.96%) · **모든 경로가 97% 미만** | 🔴 **분모를 본다. 분자를 늘리려 하지 않는다** |
| **#2 발행주식수** | 🔴 SEC 태그 결측 23.5% · **CEF·신탁은 구조적으로 태그 없음** | 종류별 태그 보유율 |
| 유니버스 정의 | 🔴 **한 번도 검증된 적 없음** | 🔑 **이 STEP의 대상** |

### ⓪-3. 걸리는 금지사항 (해당되는 것만)
- [x] 🔴 **`data/us_symbols.json` 수정 금지.** 유니버스를 바꾸지 않는다 — **무엇이 들어 있는지 세기만 한다**
- [x] 🔴 **배선 0** — `us_market_cap`·`freshSet`·`capOf`·`us_valuation` 어디에도 쓰지 않는다
- [x] 🔴 **게이트 임계(97%/95%) 불변** · `lib/lensPrecompute.ts` 수정 금지 · 🔴 **임계 조정 제안도 이 STEP에서 하지 않는다**
- [x] 🔴 **`app/api/cron/revdcf/route.ts` diff 0** — 오늘 밤 W1 진짜 첫 시험 보호
- [x] 🔴 **`BUDGET_MS`·`maxDuration` 불변**
- [x] 🔴 **robots.txt 우회 금지** — `api.nasdaq.com` 접근 금지 상태 유지. 🔴 **로컬 스냅샷 파일(`data/sources/nasdaq/nasdaq_screener_20260808.json`)은 읽어도 된다**(이미 저장소에 있는 자산)
- [x] 🔴 **SEC 레이트리밋 준수** — 10 req/s · UA 명시 · `nextAt` 동기예약(994). 🔴 가능하면 기존 벌크 재사용
- [x] 🔴 **DB 쓰기 0** · **크론 수동 실행 금지**(특히 `email-brief` = 실제 메일 발송)
- [x] 🔴 **7렌즈 라이브 화면 수정 금지** · D 조회 키 버그도 고치지 않는다
- [x] `REVDCF_ENABLED` Production OFF 유지 · `.github/workflows/**` · `vercel.json` 수정 금지
- [x] API 키 발급·유료 가입 금지 · **KR 전면 동결**

### ⓪-4. 🔴 반증 조건

| 관측 | 결론 | 다음 축 |
|---|---|---|
| 유니버스 5,973에 **모델 대상이 아닌 종목이 상당수** 섞여 있다 | 🔑 **커버리지 분모가 틀렸다.** 97%를 못 넘는 게 아니라 **잘못된 것을 재고 있었다** | 유니버스 정의 판정(장은태) |
| 섞인 게 **거의 없다**(≥95%가 보통주) | 분모는 옳다. 커버리지 부족은 진짜다 | 게이트 전제 재검토 |
| 섞여 있지만 **빼도 97%를 못 넘는다** | 분모 정리로는 해결 안 된다 | 다른 축 |
| 판별 자체가 **불가능**하다(종류 정보 없음) | 유니버스에 종류 메타데이터가 없다는 것이 결함 | 메타데이터 취득 판정 |

🔴 **네 결과 모두에 다음 행동이 정해져 있다.**
🔴 첫 번째가 나와도 **이 STEP에서 유니버스를 줄이지 않는다.** 컷·유니버스 선정·역DCF 대상이 전부 바뀌는 일이다.

### ⓪-5. 3번 생각
1. **범위** — 곁가지가 아니다. 🔴 **모든 취득 경로가 97%를 못 넘는 것이 확정된 상태**(야후 93.9% · SEC 결합 96.95% · 엄격 95.96% · 나스닥 접근불가 · quoteSummary 사망)에서, 아직 안 본 것은 **분모**뿐이다.
2. **표본** — 🔴 **전수 5,973.** 종류별 비율을 표본으로 추정하지 않는다. 이 프로젝트에서 소표본 판정이 다섯 번 틀렸다.
3. **시점** — 읽기 전용이라 오늘 밤 W1 관측을 오염시키지 않는다. 🔴 작업이 22:45 UTC를 넘기면 §1-5로 값을 읽어 붙인다.

---

## 1. 할 일

### 1-1. 🔑 유니버스 구성 전수 — 5,973 종목이 무엇인가
`data/us_symbols.json`(수정 금지, 읽기만)과 로컬 나스닥 스냅샷(`data/sources/nasdaq/nasdaq_screener_20260808.json`)을 대조해 **종류별로 전수 분류**한다.

분류 축(가능한 것부터, 못 하면 `판별불가`):
- **보통주** / **클로즈드엔드펀드(CEF)** / **신탁·로열티트러스트** / **우선주** / **ETN** / **유닛·워런트·권리** / **ADR·외국주** / **SPAC** / **판별불가**

판별 근거로 쓸 수 있는 것:
- 나스닥 스냅샷의 필드(`Market Category` · **`Financial Status`** · 회사명 문자열 · ETF 플래그) — 🔴 카탈로그가 "나스닥 Financial Status(현재 미사용)"를 즉시이득 목록에 올려둔 바로 그 필드다
- `us_cik_map`의 CIK 유무 · 같은 CIK 다중 심볼
- SEC `submissions`의 `sic` 코드(6726 = 투자사무소 등) — 🔴 이미 받아둔 게 있으면 재사용, 없으면 취득 여부를 먼저 판단
- 회사명 패턴(`Fund` · `Trust` · `Income` · `Municipal` · 로마숫자 등) — 🔴 **패턴만으로 확정하지 말 것.** 다른 근거와 교차될 때만 확정, 아니면 `추정`으로 표시

🔴 **각 종목의 판별 근거를 함께 기록**한다. 근거 없이 분류하지 않는다.

### 1-2. 종류별 × 커버리지 교차표
분류된 종류마다:
- 종목 수 · 전체 대비 비율
- **야후 시총 보유율**(`us_market_cap` 최신 `as_of` 기준)
- **SEC 주식수 태그 보유율**
- **182건 미충족 코호트에 속한 건수** ← 🔴 이게 핵심 열
- **`lens_scores` 유니버스(상위 1000)에 든 건수**
- **`revdcf_results` 604건에 든 건수**

🔴 마지막 두 열이 중요하다 — **모델이 실제로 계산 대상으로 삼고 있는지**를 보여준다.
🔴 CEF·신탁이 역DCF나 7렌즈 대상에 들어가 있으면 **그 자체가 별도 발견**이다. 크게 표시할 것.

### 1-3. 🔴 분모를 바꿨을 때의 가상 커버리지
아래 각 시나리오에서 커버리지가 얼마가 되는지 계산한다. 🔴 **전부 가상값으로 명시하고, 어느 것도 채택하지 말 것.**

| 시나리오 | 분모 | 야후 커버리지 | SEC 결합 커버리지 |
|---|---|---|---|
| 현행 | 5,973 전체 | 93.9% | 96.95% |
| ⓐ CEF·신탁 제외 | ? | ? | ? |
| ⓑ ⓐ + 우선주·ETN·유닛·워런트 제외 | ? | ? | ? |
| ⓒ 보통주만 | ? | ? | ? |

🔴 **97%를 넘는 시나리오가 있어도 "그러니 이렇게 하자"고 쓰지 말 것.** 숫자만 놓는다.
🔴 🔴 **그리고 반드시 함께 적는다: 분모를 줄이면 커버리지가 오르는 것은 산술적으로 당연하다. 이게 개선인지 눈속임인지는 "빠진 종목이 정말 모델 대상이 아닌가"로만 판정된다.**

### 1-4. EL·HRL·BRC 계열 — 대형주가 태그를 안 쓰는 건
1020이 찾은 것: 대형 우량주가 SEC 표준 주식수 태그를 **XBRL 전체 이력에서 한 번도 안 썼다**.
- 182건 중 **보통주로 분류되는 건이 몇 건**이고 그중 이 계열이 몇 건인지
- 🔴 이들이 **다른 태그**를 쓰는지 확인한다(`dei:EntityCommonStockSharesOutstanding` 외 us-gaap 계열 등). 🔴 **최대 10종목만 개별 조회**(SEC 부담 최소화). 나머지는 벌크에서
- 🔴 대체 태그가 있으면 그 태그로 몇 건이 회복되는지 — **가상값으로 표시**

### 1-5. 오늘 밤 관측 — 22:45 UTC가 지났으면 읽기만
🔑 **W1의 진짜 첫 시험이다.**
- `cron_heartbeats.job='revdcf'`의 **`stage` 값**(`loop_done` / `valuation_done` / `sector_relative_done` / `complete`) ← 🔴 **이게 답이다**
- `elapsedMsAtStage` · `finallyMs` 6구간 · `finallyTotalMs` · `loopMs` · `budgetExhausted` · `sectorRelativeError`(스택) · `sectorRelativeSaved`
- `us_sector_relative` 최신 `as_of`가 08-14로 생겼는지
- 🔴 **배포 시각이 22:45 UTC보다 앞섰는지 먼저 확인**한다(1020이 겪은 함정). 늦었으면 **"이번에도 구 코드"**라고 적는다
- 🔴 heartbeat가 **또 하나도 안 남았으면** 계측이 도달조차 못 한다는 뜻 — **크게 보고**
- `us-perf`의 `nasdaqError`도 함께(1018에서 교체 안 했으므로 계속 실패 예상 — 🔴 **바뀌었으면 보고**)
- 🔴 **크론 호출 금지** · **`BUDGET_MS` 판정 금지**

### 1-6. 산출물
`docs/probe_1021_universe_composition.md`
- 맨 앞에 **⓪-4 네 갈래 중 어느 것인지** + **보통주 비율 %** + **182건 중 모델 대상 아닌 것의 비율**
- 1-1 종류별 전수 분류(판별 근거 열 포함, `추정`/`판별불가` 구분)
- 1-2 종류별 × 커버리지 교차표 — 🔴 **`lens_scores`·`revdcf_results` 편입 여부 열 필수**
- 1-3 시나리오별 가상 커버리지 + 🔴 **"분모를 줄이면 오르는 건 당연하다" 경고 문구**
- 1-4 대형주 태그 미사용 건
- 1-5 W1 첫 시험 결과 또는 미도래·구코드 사유

🔴 카탈로그(`DATA_SOURCE_CATALOG.md` + `.xlsx`)에 **유니버스 종류 구성을 새 절로 추가**한다. 🔴 기존 서술 정정이 필요하면 취소선 보존.

## 2. 커밋 전 게이트
- 게이트 7: git 미추적 파일 참조 검사 — 🔴 런타임 `fs.readFileSync('/tmp/...')`도 잡을 것. 🔴 **`data/sources/nasdaq/*.json`이 git 추적 중인지 확인**하고, 미추적이면 스크립트가 그걸 참조한다는 사실을 기록(985~990 사고의 원인)
- 게이트 8: push 후 **Vercel 배포 성공 + verify 초록을 눈으로 확인**하기 전까지 미완료
- `docs/CHANGELOG.md` 1021 항목 · `docs/STEP_LEDGER.md` 1021 등재

## 3. DoD
- [ ] ⓪-4 네 갈래 중 어느 것인지 맨 앞에 + 보통주 비율 + 182건 중 비대상 비율
- [ ] 1-1 **전수 5,973** 분류, 각 종목에 **판별 근거**, `추정`/`판별불가` 구분
- [ ] 1-2 교차표에 🔴 **`lens_scores`·`revdcf_results` 편입 건수 열**이 있다
- [ ] CEF·신탁이 역DCF·7렌즈 대상에 들어가 있으면 **크게 표시**됐다
- [ ] 1-3 시나리오 4개 가상 커버리지 + 🔴 **경고 문구 포함** · 어느 것도 채택하지 않았다
- [ ] 1-4 개별 SEC 조회 **10종목 이하**
- [ ] 1-5 W1 `stage` 값 또는 미도래/구코드 사유 · 🔴 **배포 시각을 크론 시각과 대조**했다
- [ ] 카탈로그에 유니버스 구성 절 추가, md·xlsx 일치
- [ ] 🔴 **DB 행수 변화 0 · 프로덕션 코드 diff 0** · `data/us_symbols.json` **diff 0** · `app/api/cron/revdcf/route.ts` **diff 0**
- [ ] tsc 0 · test 전량 통과
- [ ] Vercel 배포 성공 + verify 초록 **눈으로** 확인
- [ ] 마지막에: 못 한 축 / 철회·정정한 것 / 미측정으로 남은 것

🔴 **유니버스 정의 변경 · 게이트 임계 조정 · 배선은 전부 장은태 판정이다. 이 STEP은 분모에 무엇이 들어 있는지 세는 것까지다.**
