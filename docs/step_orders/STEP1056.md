# STEP 1056 — 정본↔표시본 갈림 대조 + 정본 반영 (🔵 문서 전용 · 코드 diff 0 · DB 쓰기 0 · 화면 변경 0)

> 🔴 **선행 조건**: `PR #1`(STEP1055, `worktree-step1055` → `main`)이 **머지된 뒤에** 시작한다. PR #1이 `docs/ROADMAP_V2.md`를 이미 수정하므로 머지 전에 같은 파일을 고치면 충돌한다.
> 🔴 **이 STEP은 「옮겨 적기」가 아니라 「대조 후 판정 재료 만들기」다.** 표시본이 정본보다 낫다고 가정하지 않는다 — 표시본에서 결함이 하나 이미 발견됐다(§1).
> 🔴 **판정은 장은태가 한다.** 갈림마다 어느 쪽이 맞는지 이 STEP이 정하지 않는다.

---

## ⓪ 전제 점검

### ⓪-1. 직전 STEP 산출물 — 원문 인용

| 출처 | 인용(그대로) |
|---|---|
| `docs/probe_1054_tag_supply.md` §2-4·2-5 | `Assets` **73.8%** · `Liabilities` **65.9%**(재구성 **73.6%**) · Altman Z+Z″ **65.1%** · Piotroski 9신호 전부 성립 **32.3%**(분모 3,606) · 자산성장 **99.7%** · 배당 **50.7%** |
| STEP1055 결과 | 불변 검증 **604건 × 8필드 = 불일치 0건** · `us_fundamentals` nullable 컬럼 7개 추가 · `ROADMAP_V2.md` F-5 ⑬-b·⑪ 정정 |
| 🔴 STEP1055가 「미측정」으로 남긴 것 | Piotroski ⑤⑧ 병목 배열 확장 여부 · Altman 섹터 근사 SIC 교체 여부 · 총부채 직접/재구성 경로 선택 — **이 STEP은 셋 다 건드리지 않는다** |

### ⓪-1a. 🔴 로드맵 원문 대조 (`docs/ROADMAP_V2.md`)

| 층 | 걸리는 절 | 인용(원문 그대로) |
|---|---|---|
| **WHY** | 조건 2 (`:67`) | *"**단순해야 한다** — 「어려워서 못 본다」가 원인인데 **어렵게 설명하면** 원인을 재생산한다"* → 조건 2는 **설명**에 관한 것. 이 STEP은 질문 문장을 다루므로 여기 걸린다 |
| **HOW** | H-7 | *"**원전 없는 항목의 정의를 우리가 정함** ✅ 허용 — 규칙 5-1의 5단계를 **전부** 지키면"* → 층 구분(모델/지표/부품)이 여기 해당 |
| **WHAT** | W-1 ② · W-2 · W-2-3 · W-2-4 | W-1 ② *"그 질문에 답하는 모델 — **한 번에 하나씩, 완성 후 다음**"* · W-2 질문 **여섯** 정본 · W-2-4 각 칸 최종 모델 |
| **관문·순위** | F-5 | 판정 대기 한 장 — ⑧⑨⑪이 이 STEP의 갈림과 겹친다 |
| **완성의 정의** | C-1 | 화면을 안 만들므로 심사 대상 아님 |
| **수익 모델** | 없음 | 무관 |

🔴 **로드맵과 어긋난 것이 이 STEP의 산출물이다** — §1이 그 목록이다.

### ⓪-1b. 기존 답 확인
- `docs/KNOWN_ANSWERS.md`에 「질문 개수」·「층 구분」 항목: **없음**
- 🔴 **이름 패턴으로 목록을 만들지 않는다** — `ls docs/` 전수 나열로 `probe_105*` 확인

### ⓪-2. 건드리는 슬롯
**해당 없음** — 문서 전용.

### ⓪-3. 걸리는 금지사항
- [ ] **코드 diff 0 · DB 쓰기 0 · 화면 변경 0** · `REVDCF_ENABLED`·`Q1_ENABLED` 무접촉
- [ ] **크론 수동 실행 금지**
- [ ] **KR 전면 동결**
- [ ] 🔴 **판정하지 않는다** — 갈림을 나열하고 근거를 붙이는 데까지

### ⓪-3b. 결함의 소재 확정
문제가 관측된 지점 = **`Downloads/roadmap_v2_2.html`(표시본)**과 **`docs/ROADMAP_V2.md`(정본)**의 대조. 🔴 **프로덕션 코드·DB 결함이 아니다.** 화면·계산에 영향 0. 이 STEP은 문서 정합성만 다룬다.

### ⓪-4. 🔴 반증 조건
- **전제**: 갈림은 §1에 적힌 다섯뿐이다.
- **틀렸다면** → §2-1 전수 대조에서 §1에 없는 갈림이 추가로 나온다.
- **그때 다음 축** → 추가분을 §1과 같은 형식(정본 / 표시본 / 근거 / 성격)으로 적는다. **조용히 한쪽에 맞추지 않는다.**
- 🔴 **양쪽 모두 다음 행동이 정해져 있다.**

### ⓪-5. 🔴 3번 생각
1. **범위** — 곁가지 아님. 정본과 표시본이 갈린 채로 판정을 더 얹으면 어느 쪽에 얹었는지 추적 불가가 된다(게이트 9 「정본 먼저, 사본 나중」의 취지).
2. **표본** — 🔴 **전수 대조다.** 표시본 WHAT 절 전체 vs 정본 W-1~W-3 전체. 두 문서 각 1개라 비용은 읽기 시간뿐. 상한 없음.
   🔴 **전수의 조건** — 표시본에서 정본 항목을 찾는 방식(패턴 검색)만 쓰지 않는다. **양방향**으로 돈다: ㉠정본 항목 → 표시본에 있나 ㉡표시본 항목 → 정본에 있나. §1의 결함은 ㉠ 방향에서만 잡혔다.
3. **시점** — 크론과 무관(문서 전용). 단 🔴 **PR #1 머지 이후**여야 한다.

---

## 1. 배경 — 이미 발견된 갈림 다섯

**확정**

🔴 **㉠ 성장 질문이 표시본에서 사라졌다 — 결함이다(판정 아님).**
정본 W-2에 *"사업 자체가 커지고 있을까?"*(성장)가 있는데 표시본 질문 목록에 없었다. **STEP1044/1045가 한 번 잃었다가 복원한 바로 그 항목**(`probe_1045` — *"Q3이 재정의 구조에서 통째로 빠짐"*)인데 **표시본에서 두 번째로 잃었다.**
→ 2026-08-17 표시본에 **복원 완료**(질문 7번으로 삽입, 복원 사유 명기). 표시본 질문 수가 일곱 → **여덟**이 됐다.
🔑 **원인이 같다** — 두 번 다 「도출 결과 목록」을 만들 때 성장이 호출되지 않았다. **세 번째를 막으려면 목록을 만들 때마다 정본 W-2와 양방향 대조**해야 한다(⓪-5②).

**확정 안 됨 🔴 (판정 대상 — 이 STEP이 정하지 않는다)**

| # | 정본(`ROADMAP_V2.md`) | 표시본(`roadmap_v2_2.html`) | 성격 |
|:--:|---|---|---|
| ㉡ | 질문 **여섯**(W-2, STEP1053 확정) | 질문 **여덟** | 개수 자체 |
| ㉢ | *"지금 사면 비싸게 사는 걸까"* = **배수+시장내재기대 병합** | *"지금 이 가격, 비싼가"*(배수) / *"이 가격은 무엇을 기대하고 있나"*(역DCF) **분리** | 병합 vs 분리 |
| ㉣ | 없음 | *"내 가정으로 계산하면 얼마인가?"* **신규** | 신설 여부 — W-2-4 「적정주가는 모델이 아니라 출력」 정리와 연결. 표시본은 이름 문제로 봤다(*"「적정주가」라 부르면 주어가 우리, 「당신의 가정으로 계산하기」라 부르면 사용자"*) |
| ㉤ | 층 구분 **없음**(0건) | **모델 일곱 / 지표 일곱 / 부품** | 🔴 근거가 `probe_1041`(베타=부품) **하나뿐**. 나머지 열셋은 2026-08-16 대화가 근거이고 **원전 대조 미실시** |

## 2. 할 일

### 2-1. 🔴 전수 양방향 대조
정본 W-1~W-3 전체와 표시본 WHAT 절 전체를 **양방향**으로 돈다(⓪-5②). §1의 다섯 외 갈림이 나오면 같은 형식으로 추가한다. 🔴 **한쪽에 맞춰 조용히 고치지 않는다.**

### 2-2. 결함만 정본에 반영 — 판정 없이 확정된 것
아래는 실측·원문으로 이미 확정돼 판정이 필요 없다. 정본에 **취소선으로 원문 보존 + 정정 사유 + 좌표**를 달아 고친다.

| 위치 | 현재(옛말) | 고칠 내용 |
|---|---|---|
| `:308` W-2-4 부도위험 | *"🔴 단 **지금 만들 수 없다**(`Assets`·`Liabilities` 컬럼 부재)"* | 실측 `Assets` 73.8% · `Liabilities` 65.9%(재구성 73.6%) · **Z+Z″ 65.1%** → **만들 수 있다.** 단 섹터 근사는 GICS 유사 라벨(SIC 아님) — 좌표 `probe_1054` §2-5 |
| `:312` 구조 잔여 3건 중 **자산성장** | *"처분 미정"*(재료 언급 없음) | 실측 **99.7%** — 재료가 병목이 아님이 확인됨. **처분 판정의 재료가 생겼다**(판정 자체는 안 한다) |
| W-2-4 재무건전성 | *"부분 일치·조건부"* = 표본 조건(고 B/M) 하나 | 조건이 **둘**로 — 표본 조건 + **산출 가능 범위 32.3%**(병목 ⑤Δ레버리지 64.2%·⑧Δ마진 51.6%, 부분 합산은 H-7 위반) |

### 2-3. 표시본에만 있는 확정 결과를 정본에 옮긴다
- **탈락 셋**(PEG·피터린치 / 애널리스트 목표주가·컨센서스 / 퀀트 종합 랭킹)과 각 탈락 사유
- **그 앞에서 걸러진 여덟**(Magic Formula·Residual Income·EVA·EPV·Net-Net·청산가치·Beneish·CFROI·추정치 개정·Rule of 40) — 🔴 탈락 셋과 성격이 다름을 함께 적는다(원리적 불가 vs 수요 미확인)
- **판정 해소 둘**(DCF 기본값 · 모멘텀 일관성) — 🔴 **둘 다 「판정으로 없어진 게 아니라 물음이 틀렸던 것」**이므로 무엇이 어떻게 틀렸는지까지 옮긴다
- **극단치 원칙**(자르는 건 분모 ≤ 0과 원전 표본 밖 둘뿐 — winsorize는 관문 ② 위반)

### 2-4. 판정 대기로 등재
§1의 ㉡㉢㉣㉤를 `ROADMAP_V2.md` **F-5 판정 대기 한 장**에 항목으로 추가한다. 🔴 각 항목에 **어느 쪽이 왜 그렇게 됐는지 근거**를 붙이되 **어느 쪽이 맞는지는 쓰지 않는다.**

### 2-5. 층 구분(㉤)의 근거 상태를 명시
정본에 올릴 때 🔴 *"층 구분은 2026-08-16 세션 도출이며 원전 대조 미실시. 근거가 있는 것은 베타의 「부품」 판정(`probe_1041`) 하나뿐"*을 **같은 자리에** 적는다. H-7 규칙 5-1의 5단계를 다 지켰는지 여부도 함께 판정 대상으로 남긴다.

### 2-6. 표시본 재생성 규칙
정본 반영이 끝나면 표시본(`Downloads/roadmap_v2_2.html`)을 정본에 맞춘다. 🔴 **순서는 정본 먼저, 표시본 나중**(게이트 9). 🔴 **새 파일을 만들지 않고 기존 파일을 덮어쓴다**(2026-08-16 장은태 지시 — 중복 파일 누적 방지).

### 2-7. 🔴 브랜치 규약 신설 — `docs/COMMIT_GATES.md`

> 🔴 **「머지 규약」은 만들지 않는다.** 그런 규칙은 저장소에 존재한 적이 없고(`COMMIT_GATES.md`·`CLAUDE.md` 전수 검색 0건), 실행 측이 *"장은태가 승인한다"*고 주장한 것은 **근거 없는 자기 제약**이었다. 아래는 그 대신 **브랜치를 만들지 않는 규칙**이다 — 브랜치가 없으면 PR이 없고, PR이 없으면 「누가 머지하나」라는 질문 자체가 생기지 않는다.

> **장은태 위임(2026-08-17)**: *"너가 베스트라고 생각하는 방식으로 하자."* → **직접 방식으로 통일.** 아래를 `COMMIT_GATES.md`에 게이트 8 바로 아래 신설한다.

```
### 브랜치 규약 (2026-08-17 신설 · 장은태 위임 판정)

🔴 **STEP은 `main`에 직접 push한다. 브랜치·PR을 만들지 않는다.**

**근거 넷 (전부 실측·기록)**
① **DB는 격리되지 않는다.** Supabase 개발 브랜치 0개(2026-08-17 조회) — 마이그레이션은 코드 브랜치와
   무관하게 프로덕션에 바로 간다. 코드만 브랜치로 빼면 **스키마는 새것·코드는 옛것**인 어긋남이 생긴다.
   🔴 **실증**: STEP1055가 worktree+draft PR을 쓴 결과 `us_fundamentals` 컬럼 7개는 프로덕션에 생겼는데
   채운 행이 **0/5,820**이었고, PR 미머지 상태로 밤을 넘길 뻔했다.
② **협업 충돌이 없다.** 작업자는 장은태와 Claude Code뿐이고 동시에 같은 파일을 고치는 상황이 아니다 —
   브랜치의 주된 이득(병렬 작업 격리)이 이 저장소에는 적용되지 않는다.
③ **STEP990의 교훈은 「브랜치를 쓰라」가 아니었다.** 원인은 **CI 결과를 38시간·25 STEP 동안 아무도
   안 본 것**이다(`STATE.md` ①). 처방은 게이트 7·8이었고 브랜치가 아니었다.
④ **기존 방식이 이것이다.** `CHANGELOG.md:1333` — *"`f8693cc`(STEP969)를 main·revdcf-preview에 push,
   두 브랜치 HEAD 일치 확인."* STEP1055의 브랜치 방식은 **명령서에 없던 것**을 실행 측이 스스로 고른
   것이다(STEP1053·1054·1055 전수 검색: worktree/PR/브랜치/머지 **0건**).

**대신 지키는 것 — 게이트 8이 유일한 안전장치다**
- push 후 **GitHub Actions verify 초록 + Vercel 배포 성공을 눈으로 확인**하기 전까지 STEP은 미완료.
- 🔴 CI가 빨간불이면 **즉시 고치거나 revert한다.** 다음 STEP으로 넘어가지 않는다.
- 정규 크론은 하루 1회(`revdcf` 22:45 UTC)라 push→CI 확인까지 수 분은 무해하다. 🔴 **크론 시각 직전
  push는 피한다.**

**🔴 예외 없음 — 그래서 「머지 규약」도 없다**
- 브랜치를 만들지 않으므로 PR이 없고, PR이 없으므로 **「누가 머지하나」라는 질문이 생기지 않는다.**
  🔴 예외를 하나라도 남기면 그 경우에 이 질문이 되살아난다. 그래서 예외를 두지 않는다.
- **되돌리기 수단은 브랜치가 아니라 revert다.** 이 저장소는 이미 그렇게 복구해 왔다 —
  STEP990은 브랜치가 아니라 `tsconfig.json` 수정으로 복구했다(`STATE.md` ①).
  명령서 ⓪-4(반증 조건·되돌림 기본값)가 그 역할을 담당한다.
- 🔴 **실행 측이 스스로 브랜치·PR을 고르지 않는다.** 이 규약이 유일한 답이다.
```

🔴 **이 조항을 넣는 이유** — 정하지 않으면 STEP마다 실행 측이 다르게 골라 매번 오늘 같은 멈춤이 생긴다.

## 3. 🔴 하지 말 것
- 🔴 **갈림을 판정하지 않는다**(㉡㉢㉣㉤). 나열·근거까지다.
- 🔴 **코드·DB·화면 무접촉** · 플래그 무접촉 · 크론 수동 실행 금지 · KR 무접촉
- 🔴 **정본을 표시본으로 덮어쓰지 않는다.** 표시본에 결함이 이미 하나 있었다(§1 ㉠).
- 🔴 **STEP1055가 미측정으로 남긴 셋**(Piotroski 병목 배열 · Altman SIC · 총부채 경로)을 건드리지 않는다.
- 🔴 **모델을 만들지 않는다.** 이 STEP은 확정을 기록하는 STEP이다(W-1 ② *"한 번에 하나씩"* — 지금 완성된 모델은 0개다).

## 4. 커밋 전 게이트
- 게이트 7: git 미추적 파일 참조 검사
- 게이트 8: push 후 **GitHub Actions verify 초록 + Vercel 배포 성공을 눈으로 확인**
- 게이트 9: 새 테이블·크론·관측 필드 없음 → 해당 없음. 단 🔴 **정본 먼저·표시본 나중** 순서는 §2-6에서 지킨다
- `docs/CHANGELOG.md` 항목 · `docs/STEP_LEDGER.md` 등재

## 5. DoD
- [ ] ⓪-1a 로드맵 여섯 층 대조 · ⓪-1b `ls` 전수 목록
- [ ] 🔴 2-1 **양방향** 전수 대조 — §1의 다섯 외 갈림 유무를 명시(없으면 "없음"이라 적는다)
- [ ] 2-2 확정된 것 셋 정본 반영(취소선 보존 + 사유 + 좌표)
- [ ] 2-3 탈락 셋 · 걸러진 여덟 · 판정 해소 둘 · 극단치 원칙 정본 반영
- [ ] 2-4 ㉡㉢㉣㉤를 F-5에 판정 대기로 등재 — 🔴 **어느 쪽이 맞는지 안 씀**
- [ ] 2-5 층 구분의 근거 상태(원전 대조 미실시) 명시
- [ ] 2-6 표시본을 정본에 맞춤 — 🔴 **기존 파일 덮어쓰기, 새 파일 금지**
- [ ] 2-7 `COMMIT_GATES.md`에 브랜치 규약 신설 — 🔴 **이 STEP 자신이 그 규약대로 `main`에 직접 push한다**(브랜치·PR 만들지 않음)
- [ ] 코드 diff 0 · DB 쓰기 0 · 화면 변경 0 · tsc 0 · test 전량 통과
- [ ] 마지막에: **못 한 축 / 철회·정정한 것 / 미측정으로 남은 것**

🔴 **판정은 장은태가 한다. 명령서는 갈림을 놓는 것까지다.**
