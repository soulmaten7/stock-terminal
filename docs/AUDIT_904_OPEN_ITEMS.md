<!-- 2026-08-05 · STEP 904 · 감사 전용 문서(판정 근거 모음) — 정본은 docs/REVDCF_SPEC.md §10 -->
# `REVDCF_SPEC.md` §10 미결 목록 — 전수 감사 (STEP 904)

> Cowork 사전 실측(33건)을 **전부 재확인**했고, 감사 과정에서 사전 실측에 없던 **2건(#36·#67)을 추가로 발견**했다 — 도합 **35건 감사**. 코드·DB·문서를 직접 열어 확인했다(플레이북 #10·#82). 검색 축은 하지 않음(내부 감사라 외부 출처 개입 자리 없음 — 개별 항목이 외부 사실을 요구하면 그때 명시).
>
> **판정 4갈래**: ✅ 해소(표시 누락) · 🔴 미해소(진짜 남음) · ⛔ 무효(전제 소멸) · ↗ 이관(다른 문서가 정본). 넷 중 하나로 안 들어가면 ❓ 판단 불가(이번엔 0건).

## 결과 요약

| 판정 | 건수 | 번호 |
|---|---|---|
| ✅ 해소 | 11 | 6·9·10·11·23·24·26·28·31·33·60 |
| ⛔ 무효 | 4 | 19·22·27·54 |
| ↗ 이관 | 2 | 38·39 |
| 🔴 미해소 | 18 | 17·29·32·36·37·40·41·42·43·44·45·46·48·62·67·70·71·74 |
| ❓ 판단 불가 | 0 | — |
| **합계** | **35** | (Cowork 사전실측 33 + 감사 중 발견 2) |

🔴 **Cowork 사전실측이 33건으로 셌으나 실제로는 최소 35건이 무표시 상태였다** — #36(registry.ts 코드 동기화)·#67(retryBudgetHit 로그 미확인)이 누락돼 있었다. 둘 다 §2에서 상세.

## 재확인 대상 3건 (Cowork 사전 실측 — 직접 재확인)

| # | Cowork 사전 판단 | 재확인 결과 |
|---|---|---|
| **26** | "이미 됐다" | ✅ **일치** — `app/api/cron/revdcf/route.ts:61`이 `data.sec.gov/api/xbrl/companyfacts/CIK….json`을 호출. `frames` 히트는 `lib/revdcf/registry.ts:83`의 설명 주석 1건뿐(횡단면 카운트 용도로 명시돼 있어 무관). |
| **33** | "이미 됐다" | ✅ **일치** — `lib/revdcf/compute.ts:30-37`의 `assembleWacc`가 relevered beta·cost of equity·after-tax cost of debt·가중치를 전부 구성요소로 조립한다. 업종 완성 WACC 값을 직접 가져오는 코드 없음. |
| **22** | "이제 무효" | ✅ **일치** — 825가 "은행이라 단정 안 함"을 원칙화(`docs/LENS_DEV_PLAYBOOK.md` #55). 게다가 이 필요 자체가 이미 `NOT_APPLICABLE_SECTOR`(유동/비유동 미분류라는 **데이터 신호**로 판정 — 업종명 단정 아님)로 충족돼 있다. 이중으로 무효: ①원칙 충돌 ②대체 구현 존재. |

## 35건 전수 판정표

### ✅ 해소 (11건 — 어디서 됐는지)

| # | 항목 요지 | 어디서 됐는지 |
|---|---|---|
| 6 | EBIT 폴백(Pretax+Interest 재구성) | `lib/revdcf/drivers.ts:109` `srcRecon` 분기(`ebitSource` 플래그로 추적) |
| 9 | D 표현 설계("27년"·민감도·결측 사유) | 853~903에 걸쳐 광범위 구현(배지 §7·민감도 3점 밴드 855·스킵 12종 문구 896·드라이버 설명 855·적자 처리 856·검증부재 고지 903). "27년"은 실제 코드·화면 어디에도 있었던 적 없음(초기 예시 표현) — grep 0건 확인 |
| 10 | 베타 산출 방식(개별회귀 vs 업종) | **업종 베타로 확정** — `app/api/cron/revdcf/route.ts:43` `damodaran_beta` 테이블을 업종 키로 조회(개별 회귀 코드 없음) |
| 11 | 판별 정확도 한계 문구 | `RevDcfMethod.betaCaveat`("베타가 개별 종목 수익률 차이를 잘 설명하지 못한다는 연구가 있습니다(Fama-French 1992)") |
| 23 | driver4·5 산출방법·커버리지 미측정 | 산출방법 = 844(수준형 확정) · 커버리지 = 880("산출가능 90.3%") |
| 24 | C 잔여(N_max·초과현금·주식수) | N_max=100은 859에서 25년 지평으로 대체(원전 T8 정합) · 주식수 = 희석→기본→발행→dei 폴백 체인(`drivers.ts`) · 비영업자산/초과현금 = `nonOperatingAssets` 조립(`route.ts`) |
| 26 | 수집 경로 frames→companyfacts | 위 재확인 3건 참조 |
| 28 | "5년 이력 미만" 화면 문구 | `skip.insufficientHistory`("상장·분할 후 5년이 지나지 않았습니다") — "데이터 없음"이 아니라 정확히 "이력 부족" 사유로 서술 |
| 31 | B분류 값 DB 배선(세율·ERP·무위험·스프레드·업종매핑) | `damodaran_global_inputs`·`damodaran_country_tax`·`damodaran_credit_spread`·`damodaran_beta`·`damodaran_industry` 5개 테이블 전부 `route.ts`에서 조회 |
| 33 | driver6 구속조건(업종 WACC 차용 불가) | 위 재확인 3건 참조 |
| 60 | 현금세율 커버리지 재측정 | 항목 자체가 **"기록만"**으로 끝나는 완결형 서술(추가 조치를 정의하지 않음) — 이미 완결 상태 |

### ⛔ 무효 (4건 — 왜 무효인지)

| # | 항목 요지 | 왜 무효인가 |
|---|---|---|
| 19 | 상위 1%(5사)를 컷으로 쓸지 3%로 쓸지 | **855가 "상위 x%" 표시 자체를 폐기**하고 rank+3분류(기대 낮음/중간/높음)로 대체 — "몇 %를 컷으로 쓸지"라는 질문의 전제(퍼센트 표시)가 사라짐 |
| 22 | 적용 밖 종목 문구(은행·리츠·보험·자산운용 4종 명시) | 위 재확인 3건 참조 — 원칙 충돌 + 대체 구현 존재 |
| 27 | 기존 커버리지 수치를 "하한"으로 재라벨 | **866이 "frames=하한"이라는 전제 자체를 뒤집었다** — frames는 과소가 아니라 **다른 모집단**(OTC·외국·ADR 포함)이었음이 밝혀짐(§10 #35 참조). "하한으로 재라벨"할 대상 자체가 없어짐 |
| 54 | 3안(A/C/D)의 도미노 재검증 필요 | **880이 marginal(원전 방식 그대로)을 채택**해 3안(A capex-only·C Δ매출하한·D 원문대체) 자체가 프로덕션에서 안 쓰이게 됐다 — 안 쓰는 방식을 재검증할 이유가 없어짐(#47과 같은 논리로 소멸) |

### ↗ 이관 (2건 — 어디로)

| # | 항목 요지 | 정본 위치 |
|---|---|---|
| 38 | Russell 3기준이 몇 개를 자르는지 미측정 | `docs/STATE.md` "10) 🅿️ 866~867 잔여 미측정" 목록(이미 그곳이 정본 — "Russell 3기준 측정 여부") |
| 39 | OTC 티어 라벨 신뢰도 미규명 | 위와 동일 목록("OTC 티어 라벨 신뢰도") |

### 🔴 미해소 (18건 — 무엇이 남았는지 + 보류 여부)

| # | 항목 요지 | 무엇이 남았는지 | 보류? |
|---|---|---|---|
| 17 | driver3 병기용 세율 표시 | 19.416%/20.198% 병기 문구 어디에도 없음 | 아니오 |
| 29 | 운전자본 음수 46.6% 경고 | 경고 문구 어디에도 없음(grep 0건) | 아니오 |
| 32 | 문서 ERP 4.17/4.45→4.46% 교체 | **부분 반영** — 상세표(§10 인접·A층 데이터표)엔 정정 반영됐으나 §9 요약표(REVDCF_SPEC.md:576)는 여전히 stale "4.17%/4.45%" | 아니오 |
| **36** | (독자발견) registry.ts 코드 동기화 | `lib/revdcf/registry.ts`의 `OUR_ADDITIONS`에서 `universe`·`liquidity` 둘 다 `status: "재개방"` — 867의 실제 결정("거래소상장 확정"·"폐기 확정")과 불일치. 코드 직접 확인(`registry.ts:259, 273`) | 아니오 |
| 37 | "판정 불가" 라벨 체계(Morningstar 참고 여부) | 채택 여부 미결 — 현재는 사유 12종+"skipped" 단일 라벨만 | 아니오 |
| 40 | overCapExplained "25년" ↔ `maxYears:25` 미배선 | 값은 우연히 일치하나 구조적으로 안 묶여 있음(851과 같은 함정 재발 가능) | 아니오 |
| 41 | 유니버스 공개 문안(867 §7) 미반영 | `messages/*.json`에 `universe`/`liquidity` 키 0건(grep 확인) | 아니오 |
| 42 | 매출성장 추정기 끝점 2개만 사용 | `drivers.ts`의 `salesGrowth` 공식 불변(중간 3년 미사용) | 아니오 |
| 43 | 원전 저/기준/고 성장 시나리오 범위 | WACC 밴드로 대체됐으나(855) 원 질문(성장률 범위)은 미답 | 아니오 |
| 44 | T5/T8 조정규칙(page 92) | page 92 미확보 — 외부자원 의존 | **원리적 불가** |
| 45 | T8 vs 책 값 불일치(15%/10%) | 위와 동일 사유 | **원리적 불가** |
| 46 | driver4 유동부채 전액차감 크기 미측정 | 프로브 미실행 | 아니오 |
| 48 | 원전 세 층 값 불일치(T8 정본 채택 근거) | page 92 확보시 재검토 — 위 #44/45와 동일 의존 | **원리적 불가** |
| 62 | row.tax.w 정량 배선(58%/16.2%) | 실시간 커버리지 계산 API 없음 — 새 인프라 필요 | **인프라 확충 후** |
| **67** | (독자발견) retryBudgetHit A안 판단 | 관측 장치(894)는 붙었으나 **실제 크론 로그를 아직 한 번도 확인한 적 없음**. `vercel logs` CLI는 "지금부터 5분"만 스트리밍 가능해 과거(야간 22:45 UTC) 실행 로그 조회 불가(904에서 실측 확인) | 아니오(도구 제약) |
| 70 | `REVDCF_ENABLED` Preview 스코프 유지/해제 | 결정 안 됨 | **보류(DoD9/노출)** |
| 71 | Preview 500 원인 정밀 규명 | SSO 벽으로 Claude Code 재현 불가, Cowork 브라우저도 스택트레이스 미확보 | **보류(DoD9/노출)** |
| 74 | `years` 배지 문구 비대칭(카드 vs 보드) | 결함/의도 판정 안 됨 — **DoD7 재개방 전제 조건**으로 이미 명시돼 있음 | **보류(DoD7)** |

## §4용 분류 (지금 가능 몇 건인지)

| 분류 | 건수 | 번호 |
|---|---|---|
| 보류(DoD 7·9 영역) | 3 | 70·71·74 |
| 인프라 확충 후에만 가능 | 1 | 62 |
| 원리적 불가(외부자원 미확보) | 3 | 44·45·48 |
| **지금 가능** | **11** | **17·29·32·36·37·40·41·42·43·46·67** |

🔴 **"지금 가능" 11건이 이 감사의 핵심 산출물이다** — 다음 코드/문서 작업 STEP이 실제로 존재한다면 이 11건이 그 목록이다(이 STEP은 구현하지 않는다). 순서·우선순위는 정하지 않는다(판정 아님).

**참고**: 지금 가능 11건 중 성격이 섞여 있다 — 순수 문서정정(32) · 코드 문자열 동기화(36) · 표시 문구 신설(17·29·41) · 배선 리팩터(40) · 결정 필요(37·43) · 측정 프로브(42·46) · 관측 대기(67, 도구 제약).
