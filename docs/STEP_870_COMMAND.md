# STEP 870 — 🔴 **방향 재정렬**: 작업 순서를 DoD 순번 → **원전 대조표 차이 9행**으로 되돌린다

**실행**: `cd ~/stock-terminal && claude --dangerously-skip-permissions --model sonnet`

**전제 상태**: STEP 869 커밋 `f87ab9a` 이후 HEAD · 트리 클린

**착수 전 필독**: 🔴 `CLAUDE.md` **🚫 창작 금지**·⓪-3 · `docs/LENS_COMPLETION_STANDARD.md` **607행**(원전 대조표 요약) · `lib/revdcf/registry.ts` `INPUTS`

---

## 0. 성격 — 🔴 문서·지침 전용. 코드 0줄

🔴 `lib/**`·`app/**`·`components/**`·`messages/**`·`scripts/**` **diff 0**이어야 한다.
🔴 **모델 계산을 바꾸지 않는다.** 이번 STEP은 **"다음에 뭘 할지"를 문서에 바로 적는 것**이 전부다.
🔴 플래그 `REVDCF_ENABLED` **OFF 유지**.
🔴 **차이 9행 중 어느 것도 이번에 판정하지 않는다.** 판정은 장은태.

---

## §0. 왜 이 STEP이 필요한가 (배경 · 반드시 읽을 것)

**장은태 지적(2026-08-02)**: *"우리 역DCF에 대해 얘기하지 않았나? 이걸 하자고 했어 아니면 원전원문 그대로 하기로 했어?"*

**합의된 것은 "원전 그대로"다** — `CLAUDE.md` 🚫 창작 금지:
> **원전에 없는 산출물·지표를 추가하지 않는다.** 추가하고 싶으면 **원전 구현이 완성된 뒤** 별건으로 논의한다.

**그런데 최근 STEP들이 그 작업이 아니었다.**

| STEP | 무엇 | 원전 구현인가 |
|---|---|---|
| 866~867 | 유니버스 확장·경계 확정 | 🔴 **아니다 — 원전에 유니버스 개념 자체가 없다**(튜토리얼 02·08 개봉 확인 · `liquidity`/`volume`/`universe`/`screening` 0건) |
| 868 | `/api/revdcf` 게이팅 | 🔴 아니다(보안 사고 · 불가피했음) |
| 869 | 문구·죽은 키 정리 | 🔴 아니다(청소 · 불가피했음) |

🔴 **원인은 새 세션의 오독이 아니다. `docs/STATE.md`가 그렇게 시키고 있었다.**

현재 `STATE.md` "▶ 다음" 1번 = **"DoD 4 → 5 → 6 → 8"**.
→ 새 세션은 STATE를 정확히 따랐을 뿐이다. **표류의 출처는 이전 세션 Cowork이 STATE를 쓴 방식이다.**

**무엇이 잘못됐나**: **DoD를 진행 관리 도구로 썼다.**
- DoD 9항목은 **"완성인지 판정하는 기준"**이지 **"무엇을 만들 차례인지"**가 아니다.
- 둘을 섞으니 **원전에 없는 층(DoD 4 컷·분포)이 다음 할 일로 올라오고**, 정작 원전과의 거리인 **차이 9행이 목록 밖으로 밀렸다**.
- 🔴 **차이 9행은 `STATE.md`에 한 줄도 없다.** `LENS_COMPLETION_STANDARD.md` 607행에 묻혀 있다. 새 세션은 STATE부터 읽으므로 **구조적으로 안 보인다.**

🔑 **이번 세션에 세 번 나온 "문서 ↔ 실제 불일치"(FALR · 터미널 · SIC 컷)와 같은 자리다.** 이번엔 **"목표가 문서에 안 적혀 있는" 버전**이다.

---

## §1 — 🔴 먼저 원본을 열고 실측한다 (⓪-3 · 명령서를 그대로 믿지 말 것)

**아래를 직접 열어 이 명령서의 전제가 맞는지 확인하고, 틀리면 그 사실을 보고에 적는다.**

1. `docs/LENS_COMPLETION_STANDARD.md` **607행 전후** — 대조표 요약 행. **차이 9행이 정확히 무엇인지 이름을 그대로 옮겨 적을 것.**
2. `docs/STATE.md` "▶ 다음" 섹션 — **차이 9행 언급이 정말 0건인지 grep으로 확인.**
3. `lib/revdcf/registry.ts` `INPUTS` — **`divergence`가 채워진 항목 수**를 세고, 607행의 "차이 9행"과 **개수·이름이 일치하는지 대조.**
   🔴 **불일치가 있으면 그것 자체가 발견이다.** 어느 쪽이 정본인지 판단하지 말고 **둘 다 보고**한다.
4. `data/sources/expectations-investing/T2` 관련 — 🔴 **T2는 스프레드시트가 없다**(튜토리얼 #1·#2는 404). driver 1의 원전 절차는 `data/sources/text/EI_tutorial_02_sales.html` **본문**에서 확인해야 한다. **열어서 원전이 매출성장률을 어떻게 잡으라고 하는지 원문 그대로 인용**할 것.

---

## §2 — `docs/STATE.md` "▶ 다음" 섹션 교체

**기존 1~5번을 아래로 대체한다.** (🅿️ 보류 줄은 그대로 유지)

```markdown
## ▶ 다음 — 🔴 장은태 지시 후에만

0. 🔑 **지금 하는 일 = 원전 그대로 구현**(CLAUDE.md 🚫 창작 금지).
   진행 기준은 **DoD 순번이 아니라 원전 대조표의 차이 9행**이다.
   🔴 **DoD 9항목은 "완성 판정 기준"이지 작업 순서가 아니다.** 섞지 말 것.
   대조표 정본 = `docs/LENS_COMPLETION_STANDARD.md` §1(행 수 22 = 동일 8 + 값만차이 1 + **차이 9** + 우리 추가물 4)

1. 🔴 **차이 9행을 하나씩 판정한다** — driver1 · driver3 · driver4 · driver5 · driver6
   · 인플레 · 모집단 · 데이터출처 · 검증사례
   각 행마다 세 가지를 실측해 장은태에게 올린다:
   ① 원전 절차로 바꾸면 **결과(GAP 분포·verdict)가 얼마나 달라지나**
   ② **커버리지를 얼마나 잃나**
   ③ 그래도 원전을 택할 것인가 → 🔴 **판정 = 장은태**
   🔴 **한 행씩. 다음 행 제안 금지.**

2. 🔴 **첫 행 = driver 1(매출 성장률)** — 가장 큰 차이다.
   원전 = 가이던스·Value Line·컨센서스(**앞을 본다**)
   우리 = SEC 매출 5년 CAGR(**뒤를 본다**)
   🔑 이건 조달처 차이가 아니라 **모델이 비교하는 대상 자체가 달라지는 자리**다.
   역DCF는 "시장이 건 기대"를 푸는 모델인데, 기대 자리에 과거 실적을 넣고 있다.
   🔴 트레이드오프 실측 필요: 무료 8-K 가이던스 존재율 **63.3%·N≈1년**(847 실측)
   → 원전 쪽으로 가면 커버리지를 크게 잃는다. **충실도 vs 커버리지 = 장은태 결정.**

3. 🔴 **DoD 4·5·6·8은 차이 9행이 정리된 뒤에 판정한다.**
   특히 **DoD 4(컷·분포)는 원전에 없는 우리 추가물 층**이라, 원전 구현보다 먼저 올 수 없다.

4. 🔴 **DoD 3 판정**(도메인 상한을 ✅로 볼지) = 장은태 결정 대기(독립 항목)

5. 🅿️ **인프라 미확정**(모델과 무관 · 장은태만 가능한 것 포함):
   브라우저 육안 2단계 미실행 · Vercel 플랜(`vercel.json` 크론 9개 vs STATE "Hobby" 모순)
   · `/api/revdcf` 외부 호출 이력(Vercel 로그 403) · `toms-projects` 스코프 접근 권한
   · FTSE Russell 1차 문서 원본 미저장(rule ⓪ 갭)
   · `lib/revdcf/registry.ts` `OUR_ADDITIONS.status` 코드 동기화

6. 🅿️ **866~867 잔여 미측정**(원전 구현과 무관 · 재개는 장은태 지시 후):
   Russell 3기준 측정 여부 · OTC 티어 라벨 신뢰도 · ICC 정본 선택 · skipped 전수 규명
```

🔴 **STATE는 1~2p 상한이다.** 위 교체로 길어지면 `## 🅿️ 배경` 이하를 줄여 총량을 맞춘다(내용 삭제가 아니라 압축).

---

## §3 — `docs/LENS_COMPLETION_STANDARD.md`에 **진행 표** 신설

607행의 요약 문장 **바로 아래**에 다음 표를 추가한다. **비어 있는 칸을 채우지 말 것 — 아직 실측 안 했다.**

```markdown
### 🔴 차이 9행 — 원전 구현 진행표 (2026-08-02 신설 · STEP 870)

> 🔑 **이 표가 "원전 그대로"의 잔여 작업 목록이다.** DoD 순번이 아니라 이 표를 따른다.
> 각 행은 ①결과 변화 ②커버리지 손실 ③채택 여부 순으로 실측·판정한다. **판정 = 장은태.**

| # | 차이 행 | 원전 절차 | 우리 절차 | ①결과 변화 | ②커버리지 손실 | ③판정 |
|---|---|---|---|---|---|---|
| 1 | **driver 1 매출성장** | 가이던스·Value Line·컨센서스(앞) | SEC 5년 CAGR(뒤) | 미측정 | 미측정 | 🔴 대기 |
| 2 | driver 3 세율 | (T6 판독본 인용) | US 한계세율 25.63% | 미측정 | 미측정 | 🔴 대기 |
| 3 | driver 4 운전자본 | (T4 판독본 인용) | 수준형 5년 평균 | 미측정 | 미측정 | 🔴 대기 |
| 4 | driver 5 고정자본 | (T5 판독본 인용) | 수준형 5년 평균 | 미측정 | 미측정 | 🔴 대기 |
| 5 | driver 6 자본비용 | T7 회사별 조립 | 다모다란 구성요소 조립 | 미측정 | 미측정 | 🔴 대기 |
| 6 | 인플레(터미널 i) | 도미노 1.6% | (우리 값) | 미측정 | — | 🔴 대기 |
| 7 | 모집단 | 단일 종목 | 거래소 상장 N | — | — | 🔴 대기 |
| 8 | 데이터 출처 | 수기 입력 | SEC API + 다모다란 | — | — | 🔴 대기 |
| 9 | 검증 사례 | 도미노 1건 | 도미노 재현 + 분포 3관찰 | — | — | 🔴 대기 |

🔴 **§1에서 실제로 확인한 이름·순서로 위 표를 채울 것.** 이 명령서의 이름이 틀렸으면 **실측 쪽을 따르고 그 사실을 보고**한다.
🔴 **6~9행은 "되돌릴 수 있는 성격인지"부터 판단해야 한다**(모집단·데이터출처는 되돌리면 플랫폼이 성립하지 않을 수 있다). 🔴 **이번 STEP에서 판단하지 말 것.**
```

---

## §4 — `CLAUDE.md` 두 줄 수정

### 4-1. `[3중 점검]` 블록 강제 조항 완화 (장은태 2026-08-02 지시)

현재 66행 근처:
> 🔴 **답변에 `[3중 점검]` 블록을 붙여 ①②③에서 무엇을 봤는지 한 줄씩 남긴다. 안 붙이면 규칙을 어긴 것이다.**

**아래로 교체**:
```markdown
🔴 **점검 자체는 그대로 한다. 다만 `[3중 점검]` 블록 출력은 생략 가능**(2026-08-02 장은태).
🔴 **단 아래 셋은 블록이 아니라 본문에 반드시 명시한다 — 이건 형식이 아니라 내용이다**:
   ① **못 한 축**("검색 안 했다"·"실측 못 했다") ② **철회·정정** ③ **미측정 목록**
🔴 **블록이 없다는 이유로 "규칙 위반"이라 판단하지 말 것.**
```

### 4-2. 🔴 **DoD를 작업 순서로 쓰지 말 것** (신규 · 🚫 창작 금지 블록 안)

`🚫 창작 금지` 규칙 5항목 뒤에 추가:
```markdown
6. 🔴 **DoD 9항목은 "완성 판정 기준"이지 "작업 순서"가 아니다.** 진행 순서는 **원전 대조표의 차이 행**을 따른다.
   **위반 사례(2026-08-02)**: `STATE.md` "다음"에 *"DoD 4 → 5 → 6 → 8"*을 적어두어, 원전에 없는 층(컷·분포)이
   다음 할 일로 올라오고 **차이 9행이 목록에서 사라졌다.** 새 세션은 STATE를 정확히 따랐을 뿐이고,
   **표류의 출처는 STATE를 그렇게 쓴 Cowork이었다.**
   🔑 **문서에 안 적힌 목표는 다음 세션에 존재하지 않는다.**
```

---

## §5 — `docs/LENS_DEV_PLAYBOOK.md` 로그 1행

문제해결 로그에 신규 행:
- **문제**: 866~869 네 STEP이 원전 구현이 아닌 곳(유니버스·보안·청소)에 쓰였고, 아무도 방향 이탈을 못 알아챘다
- **원인**: `STATE.md` "다음"이 **DoD 순번**을 가리켰다. DoD는 완성 판정 기준인데 진행 관리에 썼다. **차이 9행은 `LENS_COMPLETION_STANDARD` 607행에만 있어 STATE부터 읽는 세션에겐 구조적으로 안 보였다**
- **해결**: STATE "다음"을 차이 9행 기준으로 교체 + 진행표 신설 + CLAUDE.md에 "DoD ≠ 작업 순서" 명문화
- **교훈**: 🔴 **"완성 기준"과 "작업 순서"는 다른 문서 역할이다. 섞으면 원전에 없는 층이 우선순위로 올라온다.** 조건: 원전 대조가 있는 모델 전반에 적용. 🔑 **문서에 안 적힌 목표는 다음 세션에 존재하지 않는다** — FALR·터미널·SIC 컷이 "문서와 실제가 다른" 사고였다면, 이건 **"목표가 문서에 없는"** 사고다

---

## 검증

1. 🔴 `git diff --stat HEAD -- lib/ app/ components/ messages/ scripts/ data/` → **출력 없음**
2. `npx tsc --noEmit` 0 · `npm run test`
3. §1의 실측 결과 — **차이 9행의 실제 이름** · STATE grep 결과 · `registry.ts` `divergence` 개수 대조 · **EI_tutorial_02 원문 인용**
4. §1-3에서 **불일치가 나왔으면 그 내용**(없으면 "없음")
5. `docs/STATE.md` 교체 완료 + **1~2p 상한 지켰는지**
6. `docs/LENS_COMPLETION_STANDARD.md` 진행표 신설 (🔴 **미측정 칸을 채우지 않았는지 확인**)
7. `CLAUDE.md` 2건 수정
8. `docs/LENS_DEV_PLAYBOOK.md` 로그 1행
9. `docs/CHANGELOG.md`·`docs/STATE.md` 헤더 날짜 오늘
10. 커밋(main):
    ```bash
    git add docs/ CLAUDE.md
    git commit -m "STEP 870: realign work order from DoD sequence to primary-source divergence rows, relax the checkblock mandate"
    git push && git push origin main:revdcf-preview
    ```

## 완료 보고 → Cowork에게

- §1 **실측 결과**(차이 9행 실제 이름 · STATE grep · registry 대조 · EI_tutorial_02 원문)
- 🔴 **불일치 발견 여부**
- §2~§5 반영 결과
- 🔴 **못 한 것 · 미측정**
- 🔴 **여기서 멈춘다. driver 1 착수는 장은태 지시 후.** 다음 항목 제안 금지.
