<!-- 2026-07-06 -->
# Trillion 마스터 로드맵 — 무엇을 / 어떤 순서로

> **이 문서 = "무엇을 어떤 순서로"의 단일 기준.** 매 세션 `SESSION_BOOT.md` 다음으로 이걸 본다. Phase는 위에서 아래로 순서대로. 현재 위치는 §6.
> **합의: 2026-06-27 (장은태 ↔ Cowork 전략 대화).** 변경은 대화로 합의 후 이 문서부터 갱신.

---

## 1. 골든서클 — 왜 만드는가

- **WHY** — 개인 투자자는 ① 정보가 흩어져 있어 뭐가 진짜인지 모르고 ② 검증 안 된 리딩방·상품에 속아 돈을 잃는다. 정확한 사실을 한눈에 모아주면 **스스로 안 속고 판단할 수 있다.** → *"투자상품에 속지 않게 돕는 곳."*
- **HOW** — ① 중립 허브(거래·보증·추천 X → 팔 게 없으니 편향 없음) ② 사실만(금감원 등록·공시·수익률·계산결과 / 주관평가 X) ③ 한눈에(한·미 종목·뉴스·공시·거시·리딩방 한 동선) ④ 신뢰가 중심축(광고도 '광고' 라벨로 사실과 분리).
- **WHAT** — onetrillion.app : 한·미 금융정보 허브 + 리딩방 검증 디렉토리 → (최종) Trillion AI 전망.

## 2. 변하지 않는 원칙 (LOCK)

1. **거래 X · 보증 X · 추천 X** — 정보·검증·연결만.
2. **사실만** 제공. 별점·후기 같은 주관 평가는 안 만든다(조작·명예훼손 리스크).
3. **신뢰 = 중심축·해자.**
4. **수익 ≠ retention 분리** — 광고 = *생존* 수익(트래픽에 과금, 재방문 불필요) / AI 구독 = *프리미엄·retention* 수익. 둘을 분리하면 "AI까지 못 버티면?" 리스크가 사라진다.
5. **한 번에 한 시장 / 한 언어씩 완성.** 한국 먼저. 펼치지 말고 끝까지.
6. **언어(표시) ≠ 시장기준(분석 데이터).** UI가 한국어여도 미국탭이면 분석·데이터는 100% 미국 기준. 각 탭은 그 시장의 완전한 소스만 새로 연결(한국 기준 끼워넣기 금지).
7. **AI 출력 = "투명한 계산."** "오른다" 단정 X → *"검증 기법 N개로 계산한 결과 + 방법론 공개 (보장 아님)"*. 매수추천 X → **유사투자자문업 신고 후** 유료 구독.

## 2-1. 언어권(言語圈) 거미줄 확장 전략 (2026-07-06 · 장은태 확정)

> 원칙 #5·#6의 구체 실행 순서. **"언어권 하나를 끝까지 완성 → 다음 언어권."** 각 언어권 안에서 그 언어 사용자가 사고/보는 국가탭을 거미줄처럼 늘린다.

- **① 한국어권 (지금·최우선)** — 한국 사용자가 **살 수 있거나 보는** 국가 주식탭을 늘린다(그래서 국가탭 선정 기준 = 한국 서학개미 접근성).
  - 국가탭: US·KR·JP·CN(완료) → **베트남 → 영국** → (이후 인도·대만·EU…).
  - 그다음 **디테일 폴리시 + SEO(한국어) + 한국어 광고 세팅** 완료 → **한국어판 = MVP 완성**으로 확정(여기까지 먼저 박아둔다).
- **② 영어권(미국) (한국어 완성 후)** — **미국인 시선**에서 다시 본다: 미국 사용자가 보는 국가탭이 따로 있으면 추가 → **미국 기준 수익화(어필리에이트 등) 디테일 리서치·연결 + 영어 SEO** 완성.
- **③ 그 외 언어권** — 같은 방식으로 언어권마다 국가탭을 늘린다 → **거미줄처럼 언어권별 완성도 누적.**
- **왜 언어권 단위인가**: 매매처(brokers)·SEO·광고·수익화가 전부 **언어=사용자 지역** 기준(Playbook §4-3). 국가탭=데이터, 언어권=사용자. 한 언어권을 끝까지 완성해야 그 시장에서 돈·트래픽이 돈다.
- **비변경 원칙**: 한국어판을 레퍼런스로 → 영어판은 그 템플릿에 데이터·소스만 교체(Playbook 재사용). "한 번에 한 언어씩"(원칙 #5) 유지.

## 3. 수익 모델 (3층)

- **생존 — 국내 광고**: sponsored 슬롯('광고' 라벨 필수, 사실과 분리, 미신고를 인증처럼 X). 광고주 인테이크 = 업체 클레임·인증 시스템(STEP 430~440).
- **생존 — 해외 어필리에이트**: 현지 영업 불가 → 증권사 가입 수수료 등 referral.
- **프리미엄·해자 — Trillion AI 전망 구독**: 종목 상세 페이지에 안착. 무료=요약 / 유료=전체 분석.

### 광고·게재 정책 — 리딩방·검증 (2026-06-28 갱신·확정)

**용어 (정확)**: "금감원 등록"이 아니라 **"유사투자자문 신고"**(자본시장법 제101조 = **신고제**, 인가/등록 아님). 이들은 **유사투자자문업자 = 금융투자업이 아님**(증권사=매매·중개 / 자산운용사=집합투자 / 투자자문사=투자자문 라이선스와 다른, 신고만 한 하위 등급). 디스클레이머: **"'신고'는 인가·안전보증·인증이 아님."**

**3층 뱃지 (서로 독립된 축)**:
1. **규제 사실** — "유사투자자문 신고"(금감원 리스트면 **자동·무료·전부**) / 없으면 "미신고 ⚠️"
2. **우리 확인** — "운영자 인증"(클레임 + 국세청 진위확인 통과, **무료**)
3. **광고** — "광고" 라벨 + 상위노출(**신고+인증한 곳만**, **유료**)
   → **원칙: 사실(신고 뱃지)은 절대 안 판다(무료·자동). 파는 건 노출(순위)뿐.** (X 블루체크 반면교사 — 검증 신호에 가격표 X.)

**게재 = 신고된 곳만** (디렉토리 데이터 = 금감원 신고 리스트 `fss_advisors`). 신고된 곳 → 클레임(인증) 후 **무료 링크 1개 + 소개 관리 + (결제 시) 상위노출**.

**미신고 = 게재 개념 자체가 없음** (우리 데이터에 없음 → 디렉토리에 안 뜸 → 링크 자리 없음). 검색 시 "신고 목록에 없음 ⚠️" 안내 + 사용자 제보로만 다룸. **본인인증 추가해도 동일**(본인인증 ≠ 사업 합법성). "미신고(위법) ≠ 미인증(클레임 안 함)".

**"신고 = 입장권" 플라이휠**: 미신고 운영자가 우리한테 들어오려면 → **먼저 금감원에 신고** → 리스트 등재 → 우리가 `fss_advisors` **주기 갱신(예: 월 1회)** → 디렉토리 등장 → 클레임. 우리가 합법 판단 안 하고 **금감원 신고를 게이트로 빌려씀**. 결과: 플랫폼이 업계를 합법화 방향으로 미는 힘 + 우리는 법적 판단 부담 X.

**광고 = 순위 부스트일 뿐** (배너·마케팅 카피 X — 같은 사실 리스팅을 위로만). "광고" 라벨 필수, **사실 랭킹 순서는 안 건드림**(별도 핀/인피드, `AD_EVERY`개마다 1개).

**광고 매체 가드레일 3개** (신고됨 ≠ 안전이므로 매체 책임 관리):
1. **콘텐츠 가이드라인** — 우리 화면 텍스트(소개·링크 라벨)에 "수익률·원금 보장" 류 표현 금지(게재 전 체크)
2. **신고 누적 임계치 제한** — 신고 N건 이상 쌓인 곳은 결제해도 부스트 X (도착지=그들 채널은 통제 밖 → 임계치로 관리 + 신뢰 무기)
3. **"광고" 라벨 + "신고는 안전보증 아님" 디스클레이머** 상시

**과금**: 1개 링크 무료 + 추가 링크·상위노출 유료. 결제(PG)는 **Phase 2 후반** — 지금은 UI 껍데기 + "준비 중" 스텁.

**인프라**: 클레임·인증 파이프라인(검색→국세청 진위확인→서류→관리자 승인→운영자 인증→마이페이지 관리→디렉토리 노출) **이미 구현**(STEP 430~440, 커밋 `a2c0b5f`). 옛 자가등록(STEP 301·363)은 클레임으로 대체.

⚠️ **광고비 수취(결제) 전 법률 자문 필수** — ① 종목 전망이 유사투자자문 신고 대상인지 ② 광고 매체 책임 범위 ③ 미신고 관련.

## 4. 핵심 제품 결정 (메모)

- **종목 상세 페이지 = Trillion AI의 자리.** 네이버·토스는 거기 차트·시세를 넣지만 우리는 AI 분석을 넣는다. 지금은 안 만들고 **비워둔 자리**(나중 AI의 그릇). 종목 상세에서만 '머무는 곳', 나머지는 허브.
- **콜드스타트(리딩방 관심·신고 0)는 지금 안 건드린다.** 유저 생기면 자연히 채워짐. 금감원 등록 1,804건의 '사실 가치'는 지금도 유효.
- **결제 연동은 Phase 2 후반.** 광고주 0인데 결제부터 만들면 과투자 → 수동 영업(문의폼→수동 게재→인보이스) 먼저. 결제 키·연동은 **사용자가 대시보드에서** (Cowork은 화면·흐름만, 보안상 키 미취급). 한국=Toss/PortOne(`TOSS_*` env 흔적 있음).
- **i18n 번역·현지콘텐츠는 후순위.** 한국 완성 후 한 언어씩.
- **AI는 "공장" 구조.** 기법은 국불문 동일 → 한 시장에서 완성·검증(트랙레코드) 후, 시장별 데이터만 갈아끼워 확장. 시장마다 무료 데이터 양이 달라 돌릴 수 있는 기법 수만 차이(=소스 연결 완성도 문제, 기법 문제 아님).

## 5. 실행 순서 (Phase — 위에서 아래로)

### Phase 0 — 한국 '완성' 정의 ✅ 완료 (2026-06-27)
- **목표**: ① 모바일 폴리시 마무리 ② **한국 완성 감사** — 한국탭 기능·리스트·UI를 있다/예약/안 만든다로 도장.
- **결과**: `docs/KR_COMPLETENESS_AUDIT.md` 확정. **한국 = 허브 기준 완성, 블로킹 빈틈 없음.** (토론 제외 STEP 424 · 카카오 버튼 제거 STEP 425 · 모바일 시트 통일 STEP 422·423.)
- **결론**: Phase 1 큰 빌드 없이 **Phase 2(광고 슬롯)로 진행 가능.**

### Phase 1 — 한국 기능 완성
- **목표**: 감사에서 나온 '없다(만들어야 함)' 항목을 채워 한국탭을 기능적으로 끝낸다. (종목 상세는 'AI 자리'로 비워두는 게 정답이면 스킵.)
- **완료 기준**: 감사 체크리스트 전 항목 ✔ 또는 '안 만듦' 확정.

### Phase 2 — 한국 수익화 토대 (광고)
- **목표**: 광고 슬롯 UI 정의(리스트 상단 sponsored·사이드·리딩방 상위 등, '광고' 라벨) + 자가등록을 광고주 인테이크로 확장(sponsored 등급). **수동 영업**으로 시작.
- **나중**: 셀프서브 광고주 페이지 + 결제 자동화(수요 증명 후).

### Phase 3 — 첫 retention 훅 + 앱 출시
- **목표**: 관심 종목·리딩방 변화 **푸시 알림**(retention의 첫 계단). 이걸 **앱스토어/플레이스토어 출시와 한 세트**로(앱의 존재 이유 = 알림. 개발자 결제는 준비됨).
- **묶음**: **이메일 OTP 가입 + 카카오 로그인 재추가**도 여기서 — 알림용 SMTP가 어차피 필요하니 로그인 전환과 한 번에(현 로그인 = 구글 단일). 한국 유저는 카카오·인증번호 선호.
- **이유**: 알림 없이 웹앱 래퍼만 올리면 애플 4.2 반려 위험 + 유저 이득 없음.

### Phase 4 — 해외 확장 (수익화 포함)
- **목표**: 미국 어필리에이트 수익화(증권사 referral) → 주요국 1~2개(미국 다음 **일본**). **한 나라/한 언어씩 끝까지.**
- **후순위**: 중국(규제·데이터 접근=만리방화벽).

### Phase 5 — Trillion AI (최종 · 해자)
- **목표**: 퀀트식 검증 기법으로 종목 분석 → **종목 상세 페이지에 안착**, 무료 요약/유료 전체. **한국에서 먼저 완성·검증 → 트랙레코드 → 언어권 확장**(틀은 이미 잡혀 있음).
- **전제**: 유사투자자문업 신고(자본시장법) + 법률 자문. 출력은 '투명한 계산'(원칙 §2-7).

## 6. 현재 위치

- **2026-06-27 — Phase 0 ✅ 완료.** 한국 완성 감사 확정(`docs/KR_COMPLETENESS_AUDIT.md`) — 한국 = 허브 기준 완성.
- **2026-06-30 (현재) — 최신 코드 STEP 472, HEAD `b741ead`, 배포 ✓ `onetrillion.app` 라이브**(STEP 422~472 전부 push·배포, origin/main 최신). **Phase 2 한국 수익화 토대 = 무료 티어(광고 문의·채널 게재·관리자/운영자 동선) + 🔗링크 허브 풀충전(KR 138·전 10개 카테고리, MCP 직접·git 아님) + 모바일 패스 완료.** 남은 것 = **결제 PG·본인인증(Phase 2 후반).** ▶ 다음 = ① US 링크 풀충전(현 67→KR 수준) ② Phase 2 결제 PG+빌링+본인인증 ③ Trillion AI(Phase 5).

### 게재(노출) 가격 모델 (2026-06-28 확정)
- 신고(사실)·인증(소유확인)은 **무료**. **게재(노출)만 유료.**
- **무료: 인증 업체당 채널 1개 게재** — 공급·커버리지 최대화 → 이용자 정보 풍부(플라이휠).
- **유료: 추가 채널 1개당 ₩5만/월** — 멀티채널 운영자 = 매출 업사이드.
- 단위 = **채널(연결 링크)**, 업체 아님. 금감원 신고는 업체 단위(1신고=1업체)지만 운영자는 보통 여러 채널(무료방→유료방 퍼널) 운영 → 게재는 채널 단위.
- 리스트 렌더 = **채널 1개 = 독립 행**(같은 등록업체명, 채널명만 다름). 교차연결·"이 업체의 다른 채널" 묶음 **안 함**(독립이 추가 결제 동기).
- 유료여도 '광고/스폰서' 라벨 + 신고·디스클레이머 유지 — **노출을 사는 것이지 안전 보증 아님**(§3 원칙 그대로).
- ⚠️ 실제 결제(PG·본인인증)는 **Phase 2**. 그 전엔 수동(인증→게재 신청→입금 확인→게재) 가능. **광고비 실수취 전 법률자문 1회 필수.**

### 결제·빌링 레일 (Phase 2~5 — provider 무관 추상화) (2026-06-30 갱신·확정)

**핵심: 하나의 PG가 아니라, 우리 DB의 provider-무관 빌링 추상화.** 상품마다 결제 수단이 갈리지만(아래 두 갈래), 앱 로직(게재 활성·비공개, 구독 가드)은 provider를 몰라도 됨.

- **두 갈래(영구 구조)**:
  - **① 도메스틱 — 리딩방 게재**: 본질적으로 한국(금감원) 상품 → **한국 PG(토스페이먼츠 빌링 추천 / 포트원 대안)**. KRW·로컬 결제수단·**세금계산서**·빌링키 정기결제. (Paddle 같은 MoR은 한국 도메스틱 B2B엔 부적합 — 세금계산서 X·KRW X.)
  - **② 글로벌 — AI 전망 구독**: 여러 나라·통화·세금 → **Merchant of Record(Paddle / Lemon Squeezy)**. MoR이 200여 개국 VAT/GST/판매세를 **대신 징수·납부**(각국 세금 등록 회피). 수수료 ~5%+50¢지만 세무 부담 0. (Stripe=글로벌 되나 세금은 본인 책임 → 초보엔 MoR.)
- **provider-무관 빌링 테이블(설계)**: `subscriptions(id · target_type[room_channel/ai_plan] · target_id · user_id · provider[toss/portone/paddle/lemonsqueezy] · external_id[빌링키 or 구독id] · status[active/past_due/canceled] · amount · currency · current_period_end[=만료일] · created_at)` + `billing_events`(결제 이력·webhook 로그). → 한국 PG(지금)·MoR(나중)을 **같은 모델 뒤에** 꽂음 → 재작업 0.
- **라이프사이클**: 빌링키/구독 등록 → 정기결제(한국 PG=우리 cron이 빌링키로 매달 결제 호출 / MoR=걔네가 자동 청구) → **webhook(성공/실패)** → 성공=`current_period_end` 연장 → 리딩방은 `business_links.expires_at` 동기화(게재 유지) / 실패=`past_due`→유예→`canceled`→**자동 비공개**.
- **표시 쪽은 이미 결제-레디**: `business_links.expires_at` + 디렉토리 만료 필터(STEP 459) → "미결제→자동 비공개" 절반은 구현됨. 결제 성공 시 `expires_at`만 연장하면 됨.
- ⚠️ **보안**: PG/MoR 시크릿 키는 사용자가 `.env`에 주입, Cowork은 설계·코드만(키 직접 취급 금지).
- ⚠️ **법무·세무**: 수취 전 법률자문 + (국내)통신판매업 신고·정기결제 약관·환불정책·세금계산서 / (해외)MoR이 세금 처리.
- **단계**: 지금=무료 티어(결제 X) / **Phase 2a**=한국 PG 연동(리딩방 추가 채널 유료) + `subscriptions`/`billing_events` 테이블 + 정기결제 cron + webhook / **Phase 5**=글로벌 MoR(AI 구독) 같은 추상화에 추가.
