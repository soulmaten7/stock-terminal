<!-- 2026-08-15 · Cowork 작성 · 🔴 판정 아님 — 기존 문서 통합 + 공백 표시 · ③판정 = 장은태 -->

# 제작 순서 정본 — 우리는 무엇을, 어떤 순서로 만드는가

> 🔴 **이 문서는 새 결정을 만들지 않는다.** 흩어져 있던 순서 규칙을 한곳에 모으고, **비어 있는 칸을 비었다고 표시**한 것이 전부다.
> 🔴 **미해결 판정은 §6에 모아 뒀다. 그것들은 장은태만 채울 수 있다.**

> ✅ **사실관계 검증 완료(2026-08-15, Claude Code)** — DB 실측치(§5 전부: `lens_scores` 1,039·`revdcf_results` 604·`us_valuation` 5,820/per 2,067/pbr 3,279/psr 3,554/ev 1,451·`lens_cuts` US as_of 08-14) · 카탈로그 "92소스" · `ROADMAP.md` 낡음(07-18 정지) · §1·§4·§6 인용 원문 전부 원본 대조 — 전부 일치 확인. **오류 2건 발견·정정**: ① "SPEC 2,117줄"이 STEP1027 시점 스냅샷이라 낡음(현재 2,176줄로 갱신) ② "소표본 오판 6회"는 오기(`_TEMPLATE.md:64` 원문은 5회 — 6회는 같은 파일 다른 줄의 Vercel 로그 중복조사 횟수와 혼동). 문서 내 `MODEL_BUILD_ORDER.md`·`MODEL_UNIVERSE_63.md`·`USER_QUESTIONS.md` 인용은 실제 파일명(날짜 접미사 포함)과 달라 전부 정정. §6·§8-3(Cowork의 철회한 추천)은 이 저장소 밖 대화 내용이라 검증 대상에서 제외.

> 🔴 **STEP1065(2026-08-18) 정본↔표시본 대조**: 저장소에 `docs/build_sequence.html`(위 검증·정정 반영된 최신본, 25,482B)이 이미 커밋돼 있다(`0efb835`, 이 문서와 같은 커밋). 저장소 밖 `~/Downloads/build_sequence.html`(24,609B, 2026-08-15 12:57 저장)은 **그 직전의 미정정 버전**이다(SPEC "2,117줄"·"MODEL_BUILD_ORDER.md"[날짜접미사 없음]·"소표본 오판 6회" 등 위에서 정정된 오류가 그대로 남아 있음, 텍스트 diff로 확인). 자기지시적 정본 주장(STEP1056이 로드맵 표시본에서 찾은 유형)은 **둘 다 없음**. 🔴 **표시본 갱신 필요 — `docs/build_sequence.html`(저장소, 이미 정정됨)을 `~/Downloads/build_sequence.html`에 덮어쓰면 해소된다.** 이 STEP은 저장소 밖 파일을 고치지 않는다(⓪-3).

---

## §1. 우리가 만드는 것 — 🔴 정의가 셋 병존한다

| 정의 | 시점 | 문서 | 제품 정의 | 층(2026-08-19 STEP1073 판정A) |
|---|---|---|---|---|
| 정의1(기법나열) | 2026-07-03 | `LENS_ROADMAP.md` | *"검증된 프로 기법들이 이 종목을 각각 **어떻게 읽는지** + 각 기법이 **얼마나 믿을 만한지**"* — 기법 나열 + 신뢰도 표기, 판단은 사용자 | 🔴 **폐기(2026-08-19)** |
| 정의2(정직함) | 2026-08-07 | `MODEL_BUILD_ORDER_2026-08-07.md` §3-3 | *"차별화는 모델이 아니라 **정직함**에서 나온다"* — 같은 배수를 주되 **어느 이익·어느 회계연도·왜 결측인지**를 밝힌다 | 🟢 **신뢰층(2026-08-19)** |
| 정의3(질문구조) | 2026-08-08 | `USER_QUESTIONS_2026-08-08.md` | *"사람들이 실제로 묻는 **질문(Q0~Q5)**에 답한다"* — AAII Value 77%·Dividends 73%·Growth 69%·매도1위 60% | 🟢 **최상위(2026-08-19)** |

~~🔴 **셋은 서로 모순되지 않지만 우선순위가 없다.** 그래서 "축 구성이냐 표시 방법이냐 렌즈 거취냐"가 매번 처음부터 논쟁된다. **§6-A가 이 판정이다.**~~
🟢 **판정 A 확정(2026-08-19 장은태)** — **정의3(질문구조, `USER_QUESTIONS_2026-08-08.md`)이 최상위 · 정의2(정직함, `MODEL_BUILD_ORDER_2026-08-07.md`)는 그 위에 얹히는 신뢰층 · 정의1(기법나열, `LENS_ROADMAP.md` 2026-07-03)은 폐기.** (STEP1073, 좌표 `:227` 원문 선택지 ① 그대로)

### 변하지 않는 것 (LOCK — 재론 대상 아님)
- **예측하지 않는다** · **추천하지 않는다** · **값을 매기지 않는다**
- **원전 그대로 구현한다** — 새 모델·기법을 만들지 않는다(창작 금지, 2026-08-02 최상위)
- **못 잰 것은 못 쟀다고 쓴다**

---

## §2. 만드는 절차 — 한 번에 하나씩

> 🔴 **장은태 규칙(2026-08-15)**: *"한번에 몰아서 만든 게 7렌즈이고, 그렇게 하니 제대로 완성된 게 아니어서, 다시 검색한 후 **1개씩 만들고 완성 후 다음 거**를 만든다."*

| 단계 | 하는 일 | 정본 문서 | 실패하면 |
|:--:|---|---|---|
| **①** | **무엇을 만들지 고른다** — 관문 7개(§3-A) 통과 + 실사용 순위(§3-B) | `MODEL_ROSTER.md` · `MODEL_UNIVERSE_63_2026-08-07.md` · `MODEL_BUILD_ORDER_2026-08-07.md` | 관문 탈락이면 **목록에서 뺀다**(§3-C) |
| **②** | **공식을 원전에서 확보한다** — 저자·연도·문헌. 🔴 **저장소를 먼저 뒤진다** | 모델별 SPEC (예: `REVDCF_SPEC.md`) | 원전이 없으면 **정의 공개표**로 대체(규칙 5-1). 🔴 **추측해서 채우지 않는다** |
| **③** | **재료를 카탈로그에서 대입한다** | `DATA_SOURCE_CATALOG.md` | 없으면 ④로 |
| **④** | **없는 재료는 조합해서 만든다** — 🔴 **조립만. 창작 금지** | 같음 | 조립도 안 되면 **"못 답한다"고 명시**(`ANSWERABILITY_MAP.md`) |
| **⑤** | **만든다** | 코드 | — |
| **⑥** | **완성 기준 9항목으로 검증한다** | `LENS_COMPLETION_STANDARD.md` | 🔴 **통과 못 하면 "완성"이 아니다 — 다음으로 넘어가지 않는다** |

🔴 **④의 경계**: 원전이 *"이 값이 필요하다"*고 요구한 값을 재료에서 만드는 것 = **조립**(정당). 원전에 없는 새 지표를 만드는 것 = **창작**(금지). 전례 — SEC 12태그로 역DCF 입력 조립 = 조립 / 함의 성장률 역산 층 구조 제안 = 창작, 2026-08-02 철회.

---

## §3. 무엇을 먼저 만드는가

🔴 **2026-08-19 STEP1072 — 이 절(§3-A·§3-B·§3-C) 전체는 폐기된 관문 7개 기반이다.** 관문 7개는 F-2(2026-08-15)에서 폐기되고 관문 3개+재료% 기준으로 교체됐다(§6-부록 부-2·부-3, STEP1065 확인). 🔴 **표 내용은 고치거나 지우지 않는다** — 새 관문 3개로 다시 매기는 것은 판정이라 이 STEP 범위 밖이다(§2-4 미등재 — 이미 §6-부록에 파급이 기록돼 있어 중복 아님). 아래는 **2026-08-07 시점의 낡은 스냅샷으로 읽는다.**

### §3-A. 관문 7개 — 전부 통과해야 후보 (출처: 장은태 발언, `MODEL_BUILD_ORDER_2026-08-07.md` §0)

| # | 관문 |
|---|---|
| **G1** | 방향으로 번역되는가 (장기는 이렇고 단기는 이렇다) |
| **G2** | 🔴 **정확성을 증명할 수 있는가** — 가정 최소 · 정의 공개 · 검산 가능 |
| **G3** | 커버리지 — 종목 대부분에 답이 나오는가 |
| **G4** | 무료 데이터로 되는가 |
| **G5** | 법적으로 가능한가 |
| **G6** | 우리가 값을 매기지 않는가 |
| **G7** | 매일 자동으로 돌아가는가 |

🔴 **G2는 "원전이 있는가"가 아니라 "증명 가능한가"다.** 원전 대조는 증명 수단 중 하나지 목적이 아니다. **가정이 적을수록 증명이 쉽다.**

### §3-B. 통과한 것 — 실사용 순위 (`MODEL_BUILD_ORDER_2026-08-07.md` §1)

| 순위 | 모델 | 가정 수 | 우리 상태 |
|:--:|---|:--:|---|
| 1 | **P/E** — CFA "가장 널리 쓰이는 배수" | **0** | 밸류 렌즈로 존재 · 🔴 결함 실측됨 |
| 2 | **P/B** — 은행 리포트 56% | **0** | 밸류 렌즈로 존재 |
| 3 | **EV/EBITDA** — CFA "EV 배수 압도적 1위" | **0** | `us_valuation` 1,451건 |
| 4 | **추세·이동평균** — 펀드매니저 87% | **0** | 기술 렌즈로 존재 (등급 `reference`) |
| 5 | **배당** (수익률·성장·지속성) | **0** | 🔴 **없음** — Q2 대응축 |
| 6 | PSR · P/CF 등 보조 배수 | **0** | psr 3,554건 |
| 7 | PEG | 0~1 | 없음 |
| 8 | 자산가치 (장부가·순유동자산) | **0** | 없음 |
| 9 | 재무건전성 점수 (F-Score형) | 0 | F스코어 렌즈로 존재 |
| 10 | 그레이엄 넘버 / 안전마진 | 1 | 없음 |
| 11 | 팩터 위치 | 0 | 🔴 종목 렌즈가 아니라 포트폴리오 기법 — 성격 다름 |
| **12** | 🔴 **역DCF** | **3** (자본비용·기간·영구성장) | **SPEC 2,176줄(2026-08-15 실측 — 원문 "2,117줄"은 STEP1027 시점 수치, 이후 1030~1032가 §10-J/K에 계속 덧붙여 늘어남) · 엔진 가동 · 604종목** — 🔴 **G3 미달** |

🔴 **1~8위가 전부 가정 0인데, 우리가 가장 많이 투자한 것은 가정 3개짜리 12위다.** §6-B가 이 판정이다.

### §3-C. 만들면 안 되는 것 — 관문 탈락 (사용률이 높아도)

| 사용률 | 모델 | 탈락 관문 | 사유 |
|:--:|---|---|---|
| 4위 | **DCF (정방향)** | G6 | 우리가 적정주가를 매기는 행위 |
| 5위 | 애널리스트 목표주가 | G2·G4·G6 | 남의 예측 재판매 + 유료 |
| 9위 | SOTP | G3·G7 | 사업부 분해가 수작업 |
| 12위 | 잔여이익 / EVA | G6 | 값을 매김 + 자본비용 가정 |
| 13위 | 퀀트 종합 랭킹 | G2·G6 | proprietary 명시 · 순위 = 사실상 추천 |
| 19위 | Altman Z-Score | G5·G6 | 부도 **예측**물 |
| — | Magic Formula | G6 | 순위 = 종목 추천 |
| — | Beneish M-Score | G5 | 분식 예측 + 명예훼손 위험 |
| — | Owner Earnings · EPV | G2·G6 | 정의가 서술적이라 구현마다 갈림 |

---

## §4. 완성의 정의 — 9항목 (`LENS_COMPLETION_STANDARD.md`)

원전 대조표 · 입력 검증 · 값 검증 · 컷·분포 · 경계 처리 · 주장 정합 · 화면 일관성 · 테스트 · 라이브 실측.

🔴 **아홉을 다 통과해야 "완성"이다.** 원전이 없는 항목은 1번을 **정의 공개표**로 대체한다(규칙 5-1).
🔴 **현재 9/9 통과한 모델은 0개다.**

---

## §5. 지금 어디인가 (2026-08-15 실측 · 🔴 2026-08-19 STEP1072가 옆에 오늘 값을 붙임 — DB 실측, 추측 아님)

| 축 | 2026-08-15 | 2026-08-19(STEP1072 실측) |
|---|---|---|
| **역DCF 엔진** | 매일 크론 가동 · 604종목 · `REVDCF_ENABLED` **Production OFF**(화면 노출 0) | `revdcf_results` as_of=2026-08-18 **604행**(동일, DB 실측) · `REVDCF_ENABLED` 🔴 **직접 확인 불가**(Vercel SSO가 익명 접근 차단, `curl` 302 재확인) — 간접 근거로 OFF 추정: F-5 ⑤ "REVDCF_ENABLED 승인" 항목이 여전히 판정 대기·이후 CHANGELOG에 전환 기록 없음 |
| **Q1 재료** | `us_valuation` 5,820행 — per 2,067 · pbr 3,279 · psr 3,554 · ev_ebitda 1,451 | `us_valuation` as_of=2026-08-18 **5,820행**(동일) · per **2,130** · pbr **3,361** · psr **3,642** · ev_ebitda **1,512**(전부 소폭 증가 — STEP1058이 기록한 08-17 밤 자연복구[PER 1,516→2,133]와 정합, DB 실측) |
| **Q1 화면** | 🔴 **미착수** — `Q1_ENABLED` OFF | 🔴 **직접 확인 불가**(역DCF와 동일 사유) — F-5 ④ "Q1_ENABLED 승인" 항목 여전히 판정 대기 |
| **7렌즈** | 라이브 가동 · `lens_scores` US 1,039행 · 원전 있는 것 3/7 | `lens_scores` market='US' **998행**(DB 실측 — 1,039에서 감소, 원인 미조사·이 STEP 범위 밖) · 원전 있는 것 3/7(불변) |
| **취득 게이트** | ✅ 재정의 완료(1031) — `lens_cuts` 19일 정지 해소 | 재확인 안 함(코드 diff 0 없음 — 변경 근거 없어 옛 값 유지) |
| **프루닝** | ✅ 재활성 + 100행 상한(1032) — 첫 실행 = 2026-08-15 21:30 UTC | 재확인 안 함(위와 동일 사유) |
| **카탈로그** | 92소스 · 조달 지도 완성 | 재확인 안 함(위와 동일 사유) |
| **모델 로스터** | ✅ 63개 우주 + 5계층 전수(08-07 4건 + 1033) | 재확인 안 함(위와 동일 사유) |

🔴 **재확인 안 한 넷(취득게이트·프루닝·카탈로그·모델로스터)은 이 STEP이 "새 조사 0" 제약 아래 §2-5가 명시한 항목(`REVDCF_ENABLED`·`Q1_ENABLED`·역DCF 종목수·7렌즈 행수·배수 커버리지)만 조회했기 때문이다 — 값을 추측해 채우지 않고 빈칸으로 남긴다.**

🔴 **`ROADMAP.md`가 낡았다** — "현재 위치"가 2026-07-18에 멈춰 있고, Phase 1~5가 **한국 중심 + 6개국 탭 전제**로 쓰여 있다. 지금은 **US 단독 전면 전환**이다. §6-D 판정 대상.

---

## §6. 🔴 미해결 판정 — 장은태만 채울 수 있는 칸

| # | 판정 | 왜 막혀 있나 | 걸려 있는 것 |
|:--:|---|---|---|
| 🟢 **A** | **제품 정의 우선순위** — §1의 셋 중 무엇이 위인가 | ~~셋 다 살아 있고 순위가 없다~~ 🟢 **판정 A 확정(2026-08-19 장은태)** — **정의3(질문구조)이 최상위 · 정의2(정직함)는 신뢰층 · 정의1(기법나열)은 폐기.** (STEP1073, 2026-08-19) | 그 아래 모든 화면 결정 |
| 🟢 **B** | 🔴 **「새 모델 1개를 고른다」 vs 「이미 있는 것 중 1위를 제대로 만든다」** | ~~`MODEL_BUILD_ORDER` §3-2가 "07-30의 전제 자체가 성립하지 않았을 수 있다"고 제기 → 2026-08-07 이후 미판정~~ 🟢 **판정 B 확정(2026-08-19 장은태)** — **「확정된 모델을 1개씩 완성한다」.** 새 모델을 고르지 않는다. 「1위」라는 낱말은 채택되지 않았다(근거 §6-B 아래). (STEP1073, 2026-08-19) | 다음에 착수할 대상 전부 |
| **C** | 역DCF의 위치 — 12위·G3 미달인데 최대 투자분 | B의 하위 문제 | `REVDCF_ENABLED` 켤지 |
| **D** | `ROADMAP.md` 재작성 여부 | KR 중심·6개국 탭 전제가 US 단독과 충돌 | 사업 순서 전체 |
| **E** | 7렌즈 거취 | `LENS_DISPOSITION` §4 네 안 — A·B 판정에 종속 | 화면 표면 6곳 |
| **F** | Q1 착수 전 4건 | ⓛ(disagree 표시) · ⓜ(축 구성) · ⓞ(배율 표시) · 선결질문 61(KR 공유코드 열람) | Q1 카드 |

🔴 **A와 B가 먼저다.** C~F는 전부 그 아래에 달려 있다.

### §6-B. 🟢 판정 B — 물음 자체를 다시 적는다(2026-08-19 STEP1073)

**옛 문구(취소선, 지우지 않음):**
> ~~「새 모델 1개를 고른다」 vs 「이미 있는 것 중 1위를 제대로 만든다」~~

**새 문구:**
> 🟢 **판정 B 확정(2026-08-19 장은태)** — **「확정된 모델을 1개씩 완성한다」.**
> 새 모델을 고르지 않는다. **이미 확정된 것 중 1개**를 골라 **완성**하고, 완성한 뒤에 다음 것으로 간다.
> 🔴 **「1위」라는 낱말은 뺀다** — 근거 3건: ①`ROADMAP_V2.md:304` 배수는 *"동점 4개(순위 아님)"* ②`ROADMAP_V2.md:429` 사용률 1위(적정주가)는 관문 ②에서 **탈락·만들지 않는다** ③`BUILD_SEQUENCE.md` §8 #2가 *"사용률 1위가 밀려났다"*를 **반복 실수로 등재**.
> 🔑 **이 답은 새 규칙이 아니다** — §2(장은태 규칙 2026-08-15) *"1개씩 만들고 완성 후 다음 거를 만든다"*를 여기(§6-B)에 못 박은 것이다.

「확정된 모델」 목록은 §6-B 아래(§4-3 산출)에 별도 표로 둔다.

### §6-B-1. 🔴 「확정된 모델」 목록 정본(2026-08-19 STEP1073 §4 산출 — 새 파일 아님)

> B가 "**확정된** 모델을 1개씩"이므로 무엇이 확정됐는지의 목록이 정본에 있어야 한다. 아래 앞 셋(F스코어·모멘텀·기술)은 §4-0(7렌즈 감사=`CLAUDE.md:81` 규칙3 재사용 자격 심사 결과, `LENS_DISPOSITION` §8-1·§8-2·§8-3·§8-4 + `model_list_30_9.html` "마" 절에서 그대로 옮김, 새 판단 0). 나머지는 §4-2 전수 추출 결과(아래 부-12 참조) — **전부 이미 F-5 ⑮~⑲·`LENS_DISPOSITION §7-4 C#10`에 등재된 것과 같은 항목**이라 신규 등재 없이 여기 다시 인용만 한다.

| 모델/칸 이름 | 확정 근거(날짜+좌표) | 확정의 범위 | 9항목 검증(통과 개수) | 다음에 필요한 것 |
|---|---|:--:|:--:|---|
| **재무건전성**(F스코어 단독) | `CLAUDE.md:81` 규칙3 재사용 심사 통과, `LENS_DISPOSITION §8-1·§8-3` | **구조 확정**(WHAT 칸 배정) — 완성 승인 아님 | 🔴 **충돌 있음**(`LENS_DISPOSITION §7-4 C#9`) — `LENS_COMPLETION_STANDARD`는 9/9 ✅라 적었으나 감사는 항목3(값검증)이 **구조적 불가**라 확인. 판정 대기 | 라이브 71.9%(718/999) vs SEC단독 32.3% 전제 재정리(`LENS_DISPOSITION §7-4` A-8, 기등재) + C#9 판정 |
| **최근변화**(모멘텀) | 위와 동일 규칙3, `LENS_DISPOSITION §8-1·§8-3` | **구조 확정** | 🟡 부분 — 고칠 것 2건 남음(#7 `avg(r1,r3)` 창작+이중계산 `lenses.ts:176` · #9 `adjUsed` 미노출 `lensCompute.ts:129~130`) | `LENS_DISPOSITION §7-4` A-2 판정(어떻게 처리하나) |
| **최근변화**(기술, 모멘텀과 같은 칸) | 위와 동일 | **구조 확정** | 🔴 **충돌 있음**(위 F스코어와 같은 뿌리) — 손계산 **구조적 불가**(원시 종가 미저장+외부 재취득 금지). 고칠 것 1건(#15 `pos52` 최소표본 가드 없음 `lenses.ts:201~204`) | `LENS_DISPOSITION §7-4` A-5 판정 + C#9 판정 |
| 「지금 사면 비싸게 사는 걸까」(배수+역DCF 병합) | `model_list_30_9.html` 가-1 "✅ 병합 확정(2026-08-17 장은태)" — 🔴 **git 이력 없음**(STEP1069가 Downloads에서 편입한 시점이 최초 커밋, 편입 이전 이력 없음) | ~~🔴 **미확정** — `ROADMAP_V2.md` F-5 ⑯에 이 확정이 반영된 적 없음(STEP1069 발견)~~ 🟢 **2026-08-19 STEP1074 정정 — 확정으로 승격**: `ROADMAP_V2.md:235` W-2 첫 질문이 *"(배수+시장내재기대 병합)"*으로 **이미 병합 형태**다(2026-08-16 STEP1053 확정). 역DCF=시장내재기대(`ROADMAP_V2.md:434`). `model_list_30_9.html` 가-1 자신도 *"정본 일치"*라 적었고 `BUILD_SEQUENCE` 부-7이 08-18에 확인했다. 🔴 08-17 딱지는 새 확정이 아니라 **재확인**이다 — 남은 것은 F-5 ⑯의 "판정 대기" 문구가 낡았다는 것뿐(§4에서 갱신). 확정의 범위=**구조 확정**(질문 층 정본 등재) — 완성 승인 아님. 9항목 검증=🔴 Q1 트랙, `Q1_ENABLED` OFF·승인 미완(`ROADMAP_V2.md:470`) — 이 STEP은 Q1 무접촉 | 해당없음(모델 자체가 아니라 배치 판정) | F-5 ⑯ 문구 갱신(판정 아님, §4) |
| DCF 정방향(가-2, 보류 확정) | `model_list_30_9.html` 가-2 "🟡 보류 확정(2026-08-17)" — 🔴 **git 이력 없음**(위와 동일 사유) | ~~🔴 **미확정** — F-5 ⑰에 반영된 적 없음~~ ⚠️ **2026-08-19 STEP1074 정정 — "미확정"이 아니라 "정합"**: `BUILD_SEQUENCE` 부-7이 이미 확인 — *"html 내부에서도 「보류」로 낮춰져 있어 F-5의 「판정 대기」와 사실상 정합(모순 아님)"*. 즉 양쪽 다 미판정이라 **일치**한다(어긋남 아님) | 해당없음 | F-5 ⑰ 판정 대기 유지(어긋남 없음) |
| DDM 소속(가-3, 배당칸 병합) | `model_list_30_9.html` 가-3 "병합 확정(2026-08-17)" — 🔴 **git 이력 없음** | ~~🔴 **미확정** — F-5 ⑲에 반영된 적 없음~~ ⚠️ **2026-08-19 STEP1074 정정 — 표현 차이, 방향 일치**: `ROADMAP_V2.md:307` 배당 행에 *"DDM은 적정주가류로 판정(다른 칸)"*이 **이미 있다** — html의 "배당 칸에서 빼 병합"과 같은 뜻. 🔴 남은 것은 F-5 ⑲ 문구 갱신이지 판정이 아니다(§4) | 해당없음 | F-5 ⑲ 문구 갱신(판정 아님, §4) |
| 층 구분(다, 폐기 확정) | `model_list_30_9.html` 다 "✅ 폐기 확정(2026-08-17)" — 🔴 **git 이력 없음** | 🔴 **미확정** — F-5 ⑱에 반영된 적 없음(모델이 아니라 분류체계 문제라 이 표엔 참고로만 남김) 🟢 **2026-08-19 STEP1074 재확인 — 여기만 STEP1073이 맞았다.** 부-7 원문 그대로 "md 미반영"이 진짜 어긋남 — **판정 대기 유지**(F-5 ⑱) | 해당없음 | F-5 ⑱ 판정 대기 유지(진짜 미반영) |

🔴 **2026-08-19 STEP1074 — 다섯이 균일하지 않다.** 가-1=정본 확정 · 가-2=양쪽 다 미판정(정합) · 가-3=방향 일치·문구만 다름 · 다=진짜 미반영. **한 덩어리로 다루지 않는다.** (`BUILD_SEQUENCE` 부-7이 STEP1069에서 이미 한 줄씩 갈라 대조해둔 것을 STEP1073이 `git log` 한 계층만 보고 놓쳤다 — §8 #1·#4 재발 사례로 등재.)

🔴 **2026-08-19 STEP1074 — 위 표는 실질 4행이었으나(F스코어·모멘텀·기술·배수+역DCF, 나머지 셋은 정정 전 "미확정" 표시) 개수를 명시한 결론 문장이 없었다.** 아래에 못 박는다:
🟢 **확정된 칸은 넷이다**:
> ① **「지금 사면 비싸게 사는 걸까」**(배수+역DCF 병합) — 정본 W-2, STEP1053 2026-08-16 확정 · Q1 트랙(`Q1_ENABLED` OFF)
> ② **재무건전성**(F스코어) — `CLAUDE.md:81` 규칙3 재사용 심사 통과 · 당장 고칠 것 없음
> ③④ **최근변화**(모멘텀 + 기술, 한 칸) — 규칙3 통과 · 고칠 것 3건(#7·#9·#15)
> 🔴 **넷 다 「구조 확정」이고 「완성 승인」은 0개다** — `BUILD_SEQUENCE.md:106` *"현재 9/9 통과한 모델은 0개다"* 유효.

🔴 **무엇을 먼저 완성할지는 이 STEP이 정하지 않는다. 판정 = 장은태.**

🔴 **이 표는 "무엇을 먼저 만들지"를 정하지 않는다.** 목록만 만든다. **순서·우선순위 판정 = 장은태(다음 STEP).**

---

## §6-부록. 🔴 STEP1065 확인 결과(2026-08-18) — A·B 생존 여부·종속표·관문 파급

> 🔴 **이 부록은 A·B에 답을 내지 않는다.** "아직 살아 있는지"만 문서로 확정한다. 판정 = 장은태. 인용 기준 = 원격 `main` = `3eb786b`(STEP1064까지).

### 부록-1. 판정 A — 🔴 **살아 있음**

세 정의 원문을 전문으로 읽었다: `LENS_ROADMAP.md`(07-03, 전체 50행) · `MODEL_BUILD_ORDER_2026-08-07.md`(전체 108행) · `USER_QUESTIONS_2026-08-08.md`(구조 확인). 역방향 확인(*"해소됐다면 어디에 반영됐어야 하는가"*):

- ㉠ **로드맵 WHY 조건3**(*"우리는 답이 아니라 「보는 방법」을 준다"*, `ROADMAP_V2.md:67`)이 07-03(*"검증된 프로 기법들이 이 종목을 각각 어떻게 읽는지"*, `LENS_ROADMAP.md:4`)을 **흡수했을 가능성은 있으나, 명시적 선언 좌표를 못 찾았다.** 두 문장이 "설명 가능성"이라는 주제에서 겹칠 뿐, *"그래서 07-03 정의가 채택/폐기됐다"*는 문장은 어디에도 없다.
- ㉡ **W-2-3 Layer C(방법론 공개, `ROADMAP_V2.md:293`)**가 08-07(*"차별화는 모델이 아니라 정직함에서 나온다"*, `MODEL_BUILD_ORDER_2026-08-07.md:76`)의 핵심 개념을 **거의 그대로 재현한다** — Layer C는 *"모든 항목에 공통, 8번째 정보 아님"*이라고 명시해, 08-07의 "정직함"이 08-08 질문구조(정의3) **위에 얹히는 신뢰층**으로 배치됐다. 🔴 **이것은 "정의2가 정의3 아래 배치됐다"는 구조 정리이지, "정의3이 정의1·2를 이겼다"는 우선순위 선언이 아니다.** ⓪-3b의 구분이 정확히 여기 걸린다.
- ㉢ **W-2 질문 여섯**(`ROADMAP_V2.md:234~240`, STEP1053 확정)은 08-08의 구조 그대로다. 그리고 **실무상 STEP1040~1064의 거의 전부가 이 질문 구조를 프레임으로 쓰고 있다**(예: STEP1064 §7-5의 "렌즈↔질문 귀속"). 🔴 **그러나 "실무가 그렇게 흘러갔다"와 "그렇게 하기로 판정했다"는 다른 사실이다.** `STATE.md:219`(2026-08-16, 최신 현황 정본)가 **지금도 A를 미판정 칸으로 나열**하고 있다 — 만약 A가 해소됐다면 STATE.md가 그 사실을 반영했어야 하는데, 반영돼 있지 않다.

~~**결론: 부분 해소도 아니고 살아 있음.** 정의2(정직함)가 정의3(질문) 위의 신뢰층으로 배치된 것은 사실(㉡)이나, 이는 셋의 "층위 정리"이지 "무엇이 제품 정의인가"의 답이 아니다. 정의1(기법나열)이 아직 어디에 흡수·폐기됐는지 좌표가 없고, `STATE.md`가 명시적으로 미판정이라 적어뒀다.~~
🟢 **2026-08-19 STEP1073 — 판정됨.** 장은태가 선택지 ①을 채택. 부록-1의 「살아 있음」 진단(승인 기록이 없다) 자체는 **그 시점 사실로 정확했다** — 이제 그 승인 기록이 생겼다.

### 부록-2. 판정 B — 🔴 **살아 있음**

`MODEL_BUILD_ORDER_2026-08-07.md` §3-2 원문: *"2026-07-30의 전제 — 「새 모델 1개를 골라야 한다」 — 자체가 성립하지 않았을 수 있다 … 🔴 이 판단은 장은태 몫이다."*(:74)

`ROADMAP_V2.md` F-4-3(:468): *"F-4-1②(「새 모델 찾기가 아니라 1위를 제대로 만들기」)가 **사실로 확인됐다.**"*

🔴 **"확인"과 "판정"을 구분**: F-4-3의 헤더 자신이 *"최종 승인 = 장은태"*(:451)라 명시한다. "사실로 확인됐다"는 **실측 결과**(1·3·4·5위가 이미 코드로 존재한다는 사실 확인)이지, **B의 두 선택지 중 하나를 공식 채택했다는 승인 기록이 아니다.** `STATE.md:219`도 B를 *"2026-08-07 이후 미판정, 가장 먼저"*로 그대로 옮겨 적었다.

~~**결론: 살아 있음.** F-4-1②·F-4-3이 B의 "실용적 답 후보"(1위를 제대로 만들기 쪽이 사실관계상 유력)를 강하게 시사하지만, 승인 기록은 없다.~~
🟢 **2026-08-19 STEP1073 — 판정됨.** 장은태가 「확정된 모델을 1개씩 완성한다」로 물음 자체를 다시 적었다(§6-B). 🔴 **「확인」과 「판정」을 구분한 것이 옳았음이 이번에 증명됐다** — F-4-3이 "사실로 확인됐다"고 쓴 문장(1위를 제대로 만들기)을 그대로 판정으로 삼았다면 **「1위」라는 채택되지 않은 낱말째로 확정될 뻔했다.**

### 부록-3. 🔴 종속표 — C~F + WHAT 「남은 것 셋」 + 1064 판정 대기 8건

🔴 **2026-08-19 STEP1073 — A·B가 확정됨(§6-A·§6-B). "종속/독립" 열은 그 시점 진단으로 보존하고, 새 상태 열을 오른쪽에 붙인다. C·D·E·F를 실제로 착수하지 않는다 — 라벨만 바꾼다.**

| 항목 | 좌표 | A·B 종속/독립 | 근거 한 줄 | 새 상태(STEP1073) |
|---|---|:--:|---|---|
| C. 역DCF 위치(`REVDCF_ENABLED`) | §6-C | **종속** | §6-C 자신이 "B의 하위 문제"라 명시 | 🟡 **착수 가능** — B가 "새 모델을 고르지 않는다"로 답함 → 역DCF는 이미 확정된 모델(가-1 병합)의 일부이지 새 모델이 아니다. 단 `REVDCF_ENABLED` Production OFF는 유지(별도 승인 필요) |
| D. `ROADMAP.md` 재작성 **여부** | §6-D | **독립** | US 단독 전환 자체는 이미 확정된 사실(`CLAUDE.md` 2026-08-08)이라 "재작성이 필요한가"는 A·B 답과 무관하게 이미 "예" | (불변 — 이미 독립) |
| D. 재작성 **내용**(무엇을 우선순위로 쓸지) | §6-D | **종속** | 내용은 A(제품 정의)가 정해져야 채울 수 있음 | 🟡 **착수 가능** — A가 최상위 정의(정의3)를 줌 → 재작성 기준선 확보 |
| E. 7렌즈 거취 | §6-E, `LENS_DISPOSITION §4` | **종속** | §6-E 자신이 "A·B 판정에 종속"이라 명시. `LENS_DISPOSITION` §4의 안들이 Q5(최근변화) 신설 여부에 걸리고, Q5는 정의3(질문구조)의 산물 | 🟢 **답 있음** — 🔴 "거취" 프레임 자체가 틀렸다. 감사 일곱의 목적은 처분이 아니라 `CLAUDE.md:81` 규칙3(재사용 자격 심사)이었고 `STEP1066.md` 머리가 스스로 "규칙3의 종착"이라 명시. 답은 §4-0. 남는 것은 `LENS_DISPOSITION §4`의 배치(어느 칸에 두나)뿐 |
| F. Q1 착수 전 4건 | §6-F | **종속** | Q1의 우선순위 자체가 B(1위를 제대로 vs 새 모델)의 답에 따라 달라짐 | 🔴 **§4 결과 대기** — B가 "확정된 모델 1개 완성"으로 답함 → Q1이 그 1개인지는 §4가 정한다 |
| WHAT "나" F-스코어 32.3% 서술 반영(`ROADMAP_V2.md:308` 인접) | `LENS_DISPOSITION §7-4` 판정대기#8 | **독립** | `probe_1054`가 실제 무엇을 쟀는지 재확인은 A·B와 무관하게 지금 할 수 있음 | (불변 — 이미 독립) |
| WHAT "라" 저변동·자산성장 처분(F-5⑪) | `ROADMAP_V2.md:498` | **종속**(E 경유) | 처분은 7렌즈 거취(E)의 하위이고 E는 A·B 종속 — 단 **재료 확보(원전대조·손계산가능성)는 이미 끝남**(③⑦감사), 종속인 건 "판정" 자체뿐 | 🔴 E 프레임 자체가 바뀌어(위) 이 행도 재검토 필요 — **이 STEP은 재검토하지 않는다**(범위 밖, §4가 다루지 않은 별건) |
| WHAT 신규 섹터 공백(Q0 미분류 SIC 등) | `ROADMAP_V2.md` Q0 관련 | **독립** | Q0(업종 전제층)은 별도 트랙으로 이미 마감 확정(2026-08-08)된 작업이라 A·B와 무관하게 진행 가능 | (불변 — 이미 독립) |
| 1064 판정대기#1 밸류 6개 결함 처리 | `LENS_DISPOSITION §7-4` A-1 | **종속**(E 경유) | 밸류 렌즈가 남는다는 전제(E의 답) 없이는 "어떻게 고칠지"가 의미 없음 | 🔴 **이 STEP이 판단하지 않음** — E 프레임 변경의 파급은 §4 범위 밖(밸류는 §4-0에서 "Q1 별개 경로"로만 확인됨) |
| 1064 판정대기#2 모멘텀 4개 결함 처리 | 같은 문서 A-2 | **종속**(E 경유) | 위와 동일 | 🟡 §4-0이 모멘텀을 "✅최근변화, 고쳐야 2건"으로 이미 확인 — 결함 처리 자체는 별건 판정 |
| 1064 판정대기#3 집계 레이어 등급 반영 | 같은 문서 A-3 | **독립** | "참고용 렌즈를 검증 렌즈와 동일 가중으로 셈하지 않는다"는 원칙은 A·B 어느 답에서도 유효(정직성 원칙 자체, LOCK 항목과 정합) | (불변 — 이미 독립) |
| 1064 판정대기#4 배당조정 전환 | 같은 문서 A-4 | **종속**(E 경유) | 저변동·기술 렌즈 존치를 전제 | 🔴 저변동은 §4-0에서 "❌관문①에서 걸림"으로 확인 — 이 행의 전제(저변동 존치)가 무너짐, 재검토는 별건 |
| 1064 판정대기#5 `pos52` 가드 추가 | 같은 문서 A-5 | **종속**(E 경유) | 기술 렌즈 존치를 전제 | 🟡 §4-0이 기술을 "✅최근변화, 고쳐야 1건(`pos52` 가드)"으로 이미 확인 — 이 항목과 동일 사안 |
| 1064 판정대기#6 `lens_scores` 스테일 정리 정책 | 같은 문서 A-6 | **독립** | 어떤 렌즈가 남든 전체 파이프라인 안정성 문제라 무관 | (불변 — 이미 독립) |
| 1064 판정대기#7 검산 인프라 영구화 | 같은 문서 A-7 | **독립** | 4개 렌즈 공통 구조 문제이고, 남을 렌즈가 무엇이든 같은 인프라가 필요 | (불변 — 이미 독립) |

🔑 **독립 = 6건**(D-여부·나·신규섹터공백·1064#3·#6·#7), **종속 = 8건**(C·D-내용·E·F·라·1064#1·#2·#4·#5 — 대부분 E 경유). 🔴 **"독립이 하나도 없다"는 아니다** — A·B가 안 정해진 지금도 갈 수 있는 항목이 6개 있다.

### 부록-4. 관문 7개 폐기의 파급 (⓪-4③)

**선후 확정(`git log` 커밋 시각, KST)**:
1. **2026-08-15 13:03** — `BUILD_SEQUENCE.md` 신설(커밋 `0efb835`) — §3-A/B/C가 **관문 7개(G1~G7)** 기반
2. **2026-08-15 18:25** — `ROADMAP_V2.md` F-2가 **관문 7개 폐기**(커밋 `e59b78f`, 장은태 판정 인용: *"분류 방식 자체가 문제, 싹 다 갈아엎어야 한다"*)
3. **2026-08-15 22:41** — `ROADMAP_V2.md` F-4-3이 **관문3 + 재료% 기반 새 순위** 도출(커밋 `c7ee042`) — 역DCF **3위**(재료 10.4%)
4. **2026-08-16 00:37** — `ROADMAP_V2.md` W-2-4가 **Altman Z-Score를 부도위험 질문의 확정 모델**로 채택(커밋 `6b5622a`)

🔴 **같은 날(08-15) 안에서 5시간 22분 만에 §3-A/B/C의 기반(관문7개)이 폐기됐고, 9시간 38분 만에 그 자리를 관문3+재료% 기준이 대체했다.** `BUILD_SEQUENCE.md`는 신설 이후 **커밋이 한 번도 없다**(`git log --oneline -- docs/BUILD_SEQUENCE.md` = 1건) — 이 파급을 반영한 적이 없다.

**절별 유효/무효**:

| 절 | 관문 기반? | 상태 |
|---|:--:|---|
| §1(제품 정의 셋) | 아니오 | 무관 — 유효 |
| §2(만드는 절차 6단계) | 아니오 | 무관 — 유효 |
| §3-A(관문 7개 표) | **예** | 🔴 **폐기된 관문을 그대로 나열 — 낡음**(F-2로 무효) |
| §3-B(가정수 기준 12위 순위, 역DCF=12위) | **예**(관문7개 통과분만 후보로 삼음) | 🔴 **낡음** — F-4-3이 **다른 기준**(관문3+재료%)으로 재도출, 같은 모델(역DCF)에 **다른 순위**(3위)를 냄. 두 표는 **기준 자체가 다른 별개 산출물**이라 "어느 게 맞다"의 문제가 아니라 "낡은 기준표가 갱신 안 됨"의 문제 |
| §3-C(관문 탈락표, Altman·역DCF 포함) | **예** | 🔴 **낡음** — Altman은 W-2-4(08-16 00:37)가 이후 다른 결론(채택)을 냄. **다만 이것도 "같은 기준의 뒤집힘"이 아니라 "관문7개 틀 자체가 없어진 뒤 다른 틀에서 다시 판단된 것"** |
| §4(완성 9항목) | 아니오 | 무관 — 유효(단, `LENS_COMPLETION_STANDARD.md`의 구표와 STEP1064가 이미 격차를 지적) |
| §5(2026-08-15 실측 스냅샷) | 아니오 | 시점 스냅샷 — 08-18 기준으로는 갱신 필요(예: `REVDCF_ENABLED` 상태 등 최신 확인 필요, 이 STEP은 재확인 안 함) |
| §6(미해결 판정 A~F) | 아니오 | 무관 — 유효(이 부록이 갱신) |
| §7(문서 위계) | 아니오 | 무관 — 유효 |
| §8(반복 실수) | 아니오 | 무관 — 유효 |

**충돌 실물 둘(인용)**:
- **Altman**: `BUILD_SEQUENCE.md:90` *"19위 | Altman Z-Score | G5·G6 | 부도 예측물"* ↔ `ROADMAP_V2.md:308` *"부도위험 | ✅ Altman Z-Score … Z+Z″ 65.1%로 만들 수 있다"*
- **역DCF**: `BUILD_SEQUENCE.md:77` *"12 | 역DCF | 3 | … G3 미달"* ↔ `ROADMAP_V2.md:461` *"3 | 시장내재기대(역DCF) | 10.4%(604/5,820) | ✅ 이미 완성"*

🔴 **어느 쪽이 맞는지 판정하지 않는다.** 시간 순서상 `ROADMAP_V2.md`의 F-4-3·W-2-4가 더 나중이고 더 최신 기준(관문3+재료%)을 쓴다는 사실만 확정한다 — 이것이 "`ROADMAP_V2.md`가 옳다"는 뜻은 아니다(그 자체도 §6-A·B 미판정 위에 서 있다).

### 부록-5. 살아 있는 판정(A·B)에 재료 — 🔴 이미 있는 재료를 나열, 추천·순위 없음

**판정 A**:
| 칸 | 내용 |
|---|---|
| 무엇을 정해야 하나 | 제품 정의 셋(07-03 기법나열 / 08-07 정직한 배수 / 08-08 질문답변) 중 우선순위 — 또는 "셋이 층으로 공존"이 최종 구조라는 선언 |
| 선택지 | ①정의3(질문)이 최상위, 정의2(정직함)는 신뢰층, 정의1(기법나열)은 폐기 ②정의1·2·3을 모두 층으로 공식 확정(현 실무를 그대로 승인) ③다른 배치 |
| 각 선택의 결과 | ①은 7렌즈 중 질문에 안 붙는 것(저변동·자산성장)의 위치가 불명확해짐 / ②는 현 상태 추인, 추가 작업 없음 / ③은 재설계 필요 |
| 확보된 재료 | `ROADMAP_V2.md` W-2-3(Layer A·B·C 구조), W-2 질문 여섯, `LENS_ROADMAP.md`·`MODEL_BUILD_ORDER_2026-08-07.md` 원문 |
| 재료가 더 필요한가 | 불필요 — 세 정의 원문·현재 실무 구조 전부 확보됨 |

**판정 B**:
| 칸 | 내용 |
|---|---|
| 무엇을 정해야 하나 | ~~새 모델 1개를 고를지, 이미 있는 것(배수 1~4위) 중 1위를 제대로 만들지~~ 🟢 **판정됨(§6-B, 2026-08-19)** |
| 선택지 | ①~~"1위를 제대로"~~ → 🟢 **「확정된 모델을 1개씩 완성」으로 교체 확정**(F-4-1②·F-4-3이 사실관계로 강하게 시사) — Q1(배수 4축)의 `Q1_ENABLED` 승인에 집중 ②"새 모델" — 역DCF 등 확보된 다른 트랙에 집중 ③혼합(Q1 승인 + 병행 개발) |
| 각 선택의 결과 | ①은 이미 코드가 있어 승인만 남음(가장 빠름) / ②는 역DCF SPEC 완성도가 높으나 사용률·검증 재료가 얇음(§3-B 12위 vs F-4-3 3위, 기준 자체가 갈림 — 부록-4) / ③은 리소스 분산 |
| 확보된 재료 | `MODEL_BUILD_ORDER_2026-08-07.md` §3-2·§3-3, `ROADMAP_V2.md` F-4-1②·F-4-3·F-4-4(Q1 켜기 최소 승인 세트, 조건부 4칸 하나만 남음) |
| 재료가 더 필요한가 | 불필요 — Q1 승인에 필요한 마지막 1건(조건부 4칸 처리)까지 이미 실측됨(F-4-4) |

---

## §7. 문서 위계 — 무엇이 무엇의 정본인가

| 층 | 문서 | 답하는 질문 |
|---|---|---|
| 목적 | `USER_QUESTIONS_2026-08-08.md` | 사람들이 무엇을 묻는가 |
| 후보 | `MODEL_UNIVERSE_63_2026-08-07.md` · `MODEL_ROSTER.md` | 세상에 어떤 모델이 있는가 |
| 순서 | `MODEL_BUILD_ORDER_2026-08-07.md` · **이 문서** | 무엇을 먼저 만드는가 |
| 답지 | 모델별 SPEC (`REVDCF_SPEC.md` 등) | 무엇을 넣는가 |
| 조달 | `DATA_SOURCE_CATALOG.md` | 어디서 가져오는가 |
| 출력 | `ANSWERABILITY_MAP.md` | 무엇을 답할 수 있는가 |
| 완성 | `LENS_COMPLETION_STANDARD.md` | 끝났다고 말할 수 있는가 |
| 현황 | `STATE.md` | 지금 어디인가 |
| 🔴 **표시본**(신설, 2026-08-18 STEP1069) | `docs/roadmap_v2.html`·`docs/build_sequence.html`·`docs/data_source_catalog.html`·`docs/model_list_30_9.html`·`docs/tombstone.html` | 위 정본들을 사람이 읽기 좋게 보여주는 **비정본 렌더링**. 이 행이 없던 것이 STEP1069가 다룬 어긋남의 구조적 원인이었다(§7-부록) |

🔴 **`~/Downloads/` 사본의 지위(사실 기록, 판정 아님)** — `roadmap_v2_2.html`·`build_sequence.html`·`data_source_catalog.html`·`model_list_30_9.html`·`tombstone.html`은 **사용자 로컬 파일이며 git 밖**에 있다. 위 표의 `docs/` 표시본과 **같은 이름·유사 내용**이나 별개 파일이다 — 저장소는 이 파일들을 소유하지 않고 참조만 한다(2026-08-18 STEP1069 §2-1 전수 매핑으로 처음 확정, 상세는 §7-부록). `tombstone.html`은 **은퇴 문서**다(내용 갱신 대상 아님, "정본을 둘로 두지 않는다"는 원칙을 기록해 둔 것).

🔴 **표시본을 갱신할 때는 「머리 요약」과 「본문 표」를 둘 다 고친다.**(2026-08-19 STEP1075 신설)
이력: STEP1073이 `build_sequence.html`의 §1·§6 표만 고치고 머리 요약을 두어, **같은 문서가 앞에선 "A·B 미판정", 뒤에선 "🟢 확정"이라 말하는 상태**가 이틀 남았다. 문서는 위에서부터 읽으므로 **첫 화면이 틀리면 문서 전체가 틀린 것과 같다.**

---

## §7-부록. 🔴 STEP1069 확인 결과(2026-08-18) — 표시본 정합 전수 기록

> `docs/roadmap_v2.html`(91,937B, 마지막 커밋 STEP1053) ↔ `~/Downloads/roadmap_v2_2.html`(65,755B, 08-17 22:23) · `docs/build_sequence.html`(25,482B, STEP1033) ↔ `~/Downloads/build_sequence.html`(24,609B, STEP1033) — 26KB·873B 차이의 정체를 밝히고, git 밖에 있던 `model_list_30_9.html`·`data_source_catalog.html`·`tombstone.html`을 저장소로 들여놓는다. **판정 없음** — 충돌은 양쪽 병기.

### 부-1. `ls` 전수 (⓪-1b)

| 위치 | 파일 | bytes | mtime | 최대 STEP 인용 |
|---|---|--:|---|---|
| `docs/` | `build_sequence.html` | 25,482 | (checkout 시각, 최종 커밋 STEP1033 `0efb835`) | 1033 |
| `docs/` | `roadmap_v2.html` | 91,937 | (checkout 시각, 최종 커밋 STEP1053 `257932b`) | 1053 |
| `~/Downloads/` | `build_sequence.html` | 24,609 | 08-15 12:57 | 1033 |
| `~/Downloads/` | `data_source_catalog.html` | 108,889 | 08-13 20:35 | 1002 |
| `~/Downloads/` | `model_list_30_9.html` | 57,628 | 08-17 22:23 | 1055 |
| `~/Downloads/` | `roadmap_v2_2.html` | 65,755 | 08-17 22:23 | 1056 |
| `~/Downloads/` | `tombstone.html` | 2,409 | 08-16 22:03 | (없음) |

🔴 **㉠ 번호 붙은 사본** — `~/Downloads/model_list_30_9_1.html` **없음**(전수 확인, `ls ~/Downloads/*model_list*` 결과 1건뿐). `~/Desktop`·`~/Documents`·iCloud는 뒤지지 않았다(범위 밖).
🔴 **전제③ 확인** — `model_list_30_9.html`은 `git log --all --diff-filter=A -- '*model_list*'` **0건**(재확인) — 다른 이름으로도 들어온 적 없음(`docs/probe_1040_what_structure.md` 등 내용 검색으로도 이 파일 특유의 "질문 여섯·보류2·탈락3" 골격과 일치하는 기존 문서 없음).

### 부-2. 전수 매핑 (§2-1) — 양방향

| 정본(md) | 표시본 A(`docs/`) | 표시본 B(`Downloads/`) | 관계 |
|---|---|---|---|
| `ROADMAP_V2.md`(최종 STEP1065) | `roadmap_v2.html`(STEP1053) | `roadmap_v2_2.html`(자체 인용 STEP1056) | A는 md보다 2 STEP 뒤짐(1056·1065 누락) · B는 WHAT 섹션만 md에 없는 신규 구조 |
| `BUILD_SEQUENCE.md`(최종 STEP1065) | `build_sequence.html`(STEP1033) | `build_sequence.html`(STEP1033, 08-15 12:57) | **A=B 내용 소스 동일 시점**이나 A가 이미 정정 반영판(STEP1065가 이미 발견), B는 그 직전 초안 |
| `DATA_SOURCE_CATALOG.md`(최종 STEP1051) | 🔴 없음(이 STEP에서 `docs/data_source_catalog.html`로 신규 등재) | `data_source_catalog.html`(자체 인용 STEP1002) | md가 B보다 최소 49 STEP 앞섬(§0-A·유니버스SIC재판별·STEP1068 스코프불일치 발견 등 전부 B엔 없음) |
| 없음(WHAT 작업 파일, md 정본 없음) | 🔴 없음(이 STEP에서 `docs/model_list_30_9.html`로 신규 등재) | `model_list_30_9.html`(자체 인용 STEP1055, 08-17 22:23) | git 이력 0건 확인(전제③) |
| 없음(은퇴 문서) | 🔴 없음(이 STEP에서 `docs/tombstone.html`로 신규 등재) | `tombstone.html`(08-16 22:03) | 은퇴 문서 — 갱신 대상 아님 |

### 부-3. 절 단위 4분류 (§2-2) — `roadmap_v2`·`build_sequence`

**`build_sequence.html`**: heading 구조 완전 동일(A=B, 절 신설·삭제 0건). 전체가 **④(내용 다름)** 유형이나 문장 단위 diff(python3 difflib, 태그 제거 후 line diff) 결과 **8개 지점 전부 A(docs)가 이미 정정을 반영한 상태이고 B(Downloads)가 정정 이전 원문**(STEP1065가 이미 발견한 것과 정확히 일치 — 재확인만, 새 조사 아님). A를 바꿀 필요 없음.

**`roadmap_v2.html`**: heading 비교(정규식 `<h[1-4]>` 전수 추출) 결과:
| 분류 | 개수 | 목록 |
|---|--:|---|
| ① 양쪽 동일(WHY·HOW 전체) | 21개 헤딩 | H2 WHY·HOW 및 그 하위 H3 전부 |
| ② A(docs)만 있음 | 다수 | W-1~W-3(W-2-1~W-2-4)·F-1~F-5·C-1~C-3·M-1~M-4 — B는 이 구조를 아예 버리고 WHAT만 재구성했음 |
| ③ B(Downloads)만 있음 | 6개 헤딩 | "사용자가 보는 질문—여덟"·"도구—모델 일곱"·"도구—지표 일곱"·"재료를 여는 법—태그 다섯 줄"·"탈락—안 쓰기로 한 것"·"정리된 것 다섯—판정 대기 없음" (`WHAT` 아래) |
| ④ 이름은 같으나 내용 다름 | 1건(W-2-4 재무건전성·부도위험·자산성장 행 + F-5 표) | STEP1056이 md에 반영한 정정 3건 + F-5 신규 판정대기 5건(⑮~⑲) — A(docs)엔 없었음 |

### 부-4. ④ 문장 단위 diff (§2-3)

| 차이 | 종류 | 판별 근거 | 처리 |
|---|---|---|---|
| 재무건전성 "부분일치·조건부"→"조건 둘" | ㉢판정상태 | `ROADMAP_V2.md:308` 정본에 STEP1056 정정 이미 존재, A(docs)엔 미반영 | A에 취소선+정정+좌표로 반영(완료, 본문 W-2-4) |
| 부도위험 "만들 수 없음"→"Z+Z″ 65.1%로 만들 수 있다" | ㉠숫자+㉢판정상태 | 동일(`ROADMAP_V2.md:311`) | A에 반영(완료) |
| 자산성장 "처분 미정"→"재료는 병목 아님(99.7%)" | ㉠숫자 | 동일(`ROADMAP_V2.md:314`) | A에 반영(완료) |
| F-5 ⑮~⑲ 5행 부재 | ㉡STEP번호(정본 STEP1056 vs 표시본A STEP1046 최종) | 정본에 이미 있고 A만 없음 — B는 이미 갖고 있었음(자체 인용 STEP1056) | A에 추가(완료) |
| WHAT 절 구조 자체(질문6vs8·모델/지표 재구성) | ④ 구조적 재작성(문장 단위 아님) | 🔴 **md 정본은 이 구조를 갖고 있지 않다** — B의 재구성은 md에 반영된 적 없는 **표시본 단독 실험**. 축A로 "맞다/틀리다"를 가릴 수 없음(md에 대응 항목 자체가 없어서) | 판정하지 않음 — B 원문 그대로 A에 "표시본 전용, md 대응 없음"으로 이관(③ 규칙, 내용 보존) |

🔴 **26KB 차이의 정체**: 위 표대로 **통째로 없는 절(WHAT 하위 6개, 약 20KB 추정)**이 대부분이고, 나머지는 F-1~C-3~M-4(약 5,800B 상당, ②만 A에 존재)다. "저장소가 더 크다"가 아니라 **A는 기술 상세(F/C/M 층) 보유·B는 그 층을 비우고 WHAT만 재구성** — 서로 다른 것을 채우고 다른 것을 비웠다.

### 부-5. 자기지시적 주장 확인 (STEP1056 전례 재확인)

`~/Downloads/roadmap_v2_2.html`의 WHAT 절 말미(라인 522 부근)에 다음 문장이 **이미** 존재한다:

> *"🔴 2026-08-17 STEP1056 정정 — 여기 '이 절이 정본이다(2026-08-16 장은태 판정)'이라 적혀 있었으나, 정본은 `docs/ROADMAP_V2.md` 하나뿐이다. 이 페이지는 그 정본을 읽기 좋게 보여주는 표시본이다."*

🔑 **이번엔 자기지시적 결함이 없다** — STEP1056이 이미 고쳤고, 그 정정 문구까지 페이지 안에 남아 있다(취소선 방식 그대로). 새로 발견된 자기지시적 주장 **0건**.

### 부-6. `data_source_catalog.html` ↔ `DATA_SOURCE_CATALOG.md` 정합 목록 (§2-5b, 고치지 않음)

| 어긋남 | 상태 |
|---|---|
| `§0-A. 시가총액 커버리지 — 분자 경로 소진`(STEP1023) | html에 없음(html은 STEP1002 시점 스냅샷) |
| `유니버스 종류 구성`(STEP1021→1024 SIC 재판별) | html에 없음 |
| STEP1034 재대조 기록(19슬롯 일치·1건 불일치 확인) | html에 없음 |
| 🔴 STEP1068 발견 — "20슬롯은 역DCF 전용이라 총자산 슬롯 자체 없음"(스코프 불일치) | html·md 둘 다 **명시 문구 없음**(md도 이 STEP1068 표현 그대로는 안 담고 있음 — STEP1068 산출물은 `LENS_DISPOSITION_2026-08-08.md` §10에 있음, 카탈로그 자신에 역기재된 적 없음) |
| `roe.xls`·`margin.xls`(마진·수익성 신규 발견) | **양쪽 다 있음**(html·md 모두 보유 — 08-12 이전 발견이라 html에도 반영돼 있었음) |

🔑 **정리**: html은 STEP1002 스냅샷이고 md는 STEP1051까지 갱신됨 — 최소 49 STEP분 격차. 위 목록 외에도 STEP1021~1051 사이 md에만 있는 세부 항목이 더 있을 수 있으나(108KB↔120KB 완전 대조는 안 함), **격차의 존재 자체는 확인**했고 방향(md가 항상 앞섬)은 일관됨.

### 부-7. `model_list_30_9.html` ↔ `ROADMAP_V2.md` W-2 정합 목록 (§2-7, 고치지 않음)

| 항목 | `ROADMAP_V2.md` W-2/F-5 | `model_list_30_9.html` | 어긋남 성격 |
|---|---|---|---|
| 질문 개수 | 정본 W-2 = **여섯**, F-5 ⑮ = "판정 대기"(어느 쪽이 맞는지 미정) | "**여섯**은 새로 정한 게 아니라 되돌린 것" — "2026-08-17 장은태" 딱지로 **이미 확정**된 것처럼 서술 | 🔴 ㉢판정상태 어긋남 — **정본(F-5)엔 이 확정이 반영돼 있지 않다.** §2-3 규칙("정본에 판정 기록이 없으면 미판정 쪽이 맞다")대로면 **여전히 미판정**이 맞다. model_list_30_9.html의 "확정" 딱지가 md로 역반영된 적이 없다 |
| 배수+역DCF 병합 | F-5 ⑯ = 판정 대기 | "가-1. ✅ 병합 확정 2026-08-17 장은태" | 위와 동일 성격 — html엔 확정, md엔 미판정으로 남음 |
| DCF 정방향 신설 | F-5 ⑰ = 판정 대기 | "가-2. DCF 정방향" — 보류 2로 분류(모델—보류2 표, 확정 아님) | html 내부에서도 "보류"로 낮춰져 있어 F-5의 "판정 대기"와 사실상 정합(모순 아님) |
| 층 구분(모델/지표/부품) | F-5 ⑱ = 판정 대기, 근거 상태 명시 요구 | "다. ✅ 폐기 확정 2026-08-17 장은태" — 폐기 이유까지 상세 서술 | 🔴 위와 동일 어긋남 성격 — md 미반영 |
| DDM 소속 | F-5 ⑲ = 판정 대기(배당칸 vs 적정주가류) | 질문④ 각주 "DDM 제거 — 가-3"으로 **적정주가류 정리에 병합됐다고 서술** | 위와 동일 — md 미반영 |

🔑 **패턴 하나로 수렴**: `model_list_30_9.html`(08-17 22:23, git 밖)이 F-5 ⑮~⑲ 네 항목 전부에 대해 **"2026-08-17 장은태"** 딱지를 달고 이미 확정된 것처럼 서술하고 있으나, 이 확정은 **`ROADMAP_V2.md` F-5 표에 한 번도 반영되지 않았다.** 🔴 **이 STEP은 어느 서술이 맞는지 정하지 않는다** — `model_list_30_9.html`의 딱지가 실제 확정이라면 F-5를 갱신해야 하고, 아니라면 그 딱지 자체가 표시본의 성급한 서술이다. **둘 다 다음 판정 대상으로 남긴다.**

### 부-8. 검증 3 · 검수 3 (§2-8)

**검증 3**
1. **축A** — md 정본·`git log`·인용 STEP 번호로 판별한 항목: `roadmap_v2.html` 3건 취소선 정정 + F-5 5행 추가 + 정본 포인터 1행(총 9개소, 전부 md 근거 확인) · `build_sequence.html`은 STEP1065 기존 발견 재확인(신규 판별 0, A가 이미 정답)
2. **축B** — STEP1056 방법(취소선+정정+좌표) 그대로 재사용 확인(위 부-4 전부 이 형식) · STEP1056의 표시본 재생성 원칙("정본 먼저, 표시본 나중" · "기존 파일 덮어쓰기, 새 파일 금지") 준수 — `docs/roadmap_v2.html`을 덮어썼고 새 html 파일을 만들지 않음
3. **축C** — ㉠Downloads에만 있는 절: **6개**(WHAT 하위 재구성 전체, 부-3 참조) · ㉡Downloads가 더 큰 STEP 인용: **`roadmap_v2_2.html` 1건**(자체 STEP1056 인용, docs는 STEP1053 정지) · ㉢Downloads 숫자가 DB 실측과 더 맞는 곳: **0건**(모든 숫자 갈림은 md 정본 쪽이 더 최신 실측을 담고 있었음 — 재무건전성 71.9%/32.3% 같은 최신 정정은 오히려 md·LENS_DISPOSITION에도 없고 model_list_30_9.html 자체 갱신[이 STEP의 §2-5]으로 처음 반영됨)

**검수 3**
1. **내용 손실 없나** — `roadmap_v2.html` 절 수: 편집 전 약 46개 헤딩 → 편집 후 53개 헤딩(+7 = W-2-5 1개 + WHAT재구성 6개). ③에서 가져온 항목 = 6개 헤딩 전부(부-3) **전량 이관 확인**(요약 없이 원문 그대로 삽입, python splice로 바이트 단위 보존)
2. **판정을 만들지 않았나** — 미판정으로 남긴 충돌: F-5 ⑮~⑲ 재확인(5) + 부-7의 4개 항목(질문개수·병합·층구분·DDM, 위 표) = **총 9건 미판정 유지**. 이 STEP이 새로 판정한 것 **0건**
3. **숫자 판별이 추측 아닌가** — ㉠(숫자 차이) 중 DB 실측으로 확정한 건: model_list_30_9.html의 5개 재료 갱신(배수커버리지원인·Piotroski71.9%·자산성장99.7%·수익성수준축·7렌즈판정) 전부 **기존 STEP(1058·1064·1066·1067·1068)의 이미 검증된 실측치를 인용**한 것이지 이 STEP이 새로 잰 것은 아님(재확인만, 그대로 명시) · 미확인으로 남긴 것 = data_source_catalog.html의 나머지 세부 어긋남(108KB↔120KB 완전 문장 대조는 안 함, 부-6에 명시)

🔴 **못 거친 축**: `data_source_catalog.html`↔`DATA_SOURCE_CATALOG.md`의 **문장 단위 전수 diff**(108KB vs 120KB, §2-3 수준의 정밀도)는 하지 않았다 — 헤딩 비교 + 알려진 최근 절 유무 확인까지만 했다(부-6). 완전 대조는 다음 STEP 대상으로 남긴다.

🔴 **판정은 장은태가 한다. 이 부록은 자리를 바로잡고 합친 기록이지 어느 쪽이 맞는지의 결론이 아니다.**

### 부-9. STEP1070(2026-08-18) — worktree 해소 + 메인 폴더 동기화

> `docs/roadmap_v2.html`·`build_sequence.html`·`model_list_30_9.html`·`data_source_catalog.html`·`tombstone.html`(부-1~부-8)이 저장소 안에서 "정본 두 곳" 문제였다면, 이번엔 **작업 폴더 자체가 두 곳**이었다 — 메인 폴더(`/Users/maegbug/stock-terminal/`)가 STEP1054(`08699ce`)에 사흘째 정지, 실제 작업은 전부 `.claude/worktrees/step1055`(브랜치 `worktree-step1055`)에서 실행되고 있었다.

**§2-0 손실 점검 넷 — 원문 출력**

```
㉠ git log --oneline main..worktree-step1055  (로컬 main 기준)
→ 17줄(8d04143~877ed6c, STEP1055~1069 전부) — 문면상 "손실 위험"으로 보임
🔴 재확인: 로컬 main은 STEP1054(08699ce)에서 fast-forward된 적이 없었을 뿐(이 세션 내내
   `git push origin worktree-step1055:main`으로 origin/main만 갱신·로컬 main은 무접촉).
   git log --oneline origin/main..worktree-step1055 → 0줄. git rev-parse worktree-step1055 origin/main
   → 둘 다 8d0414308c5e5bde92116365f8d9231ffd1ce5fc(완전 동일). 손실 위험 없음(§2-1에서 로컬 main도 해소).

㉡ (worktree 안) git status --porcelain → 0줄(클린)

㉢ (메인 폴더) git status --porcelain 전량:
?? docs/probe_1055_invariance.json
?? docs/step_orders/STEP1055.md ~ STEP1070.md (16개)
→ diff <(git show origin/main:<path>) <path> 전수: STEP1055~1069.md(15개)·probe_1055_invariance.json
   전부 origin/main과 SAME. STEP1070.md는 origin/main에 파일 자체가 없음(이 STEP 자신이 아직 미push
   — 당연·보존 대상). 🔴 main에 없는 untracked 파일 0건(전부 origin/main에 이미 존재하거나
   이 STEP 자신의 산출물).

㉣ gh pr view 1 --json number,title,state,headRefName,baseRefName,mergedAt,url
→ {"number":1,"state":"MERGED","headRefName":"worktree-step1055","baseRefName":"main",
   "mergedAt":"2026-08-17T08:31:51Z", ...} — open 아님, 이미 병합됨. 멈출 이유 없음.
```

🔑 **넷 다 손실 위험 없음으로 판명 — 제거 조작 진행.**

**§2-1 메인 폴더 동기화**: 위 "SAME" 확인된 untracked 16개 중 15개(STEP1070.md 제외) 삭제 → `git fetch origin` → `git pull --ff-only origin main`. 🔴 **1회 실패**(`.git/index.lock` 존재 — `ps aux`로 실행 중인 git 프로세스 0건 확인, 0바이트·직전 실패 흔적으로 판단해 lock 파일만 제거, `reset --hard`·`clean` 미사용) → 재시도 성공, fast-forward `08699ce..8d04143`(50 files changed). `docs/model_list_30_9.html` 등 5개 html **존재 확인**.

**§2-2 worktree 처분**: `git worktree list`로 양쪽이 이미 동일 커밋(`8d04143`)임을 확인 후 `git worktree remove .claude/worktrees/step1055`(force 없이, 성공) → `git branch -d worktree-step1055`(`-d` 안전삭제 — git이 "Deleted branch worktree-step1055 (was 8d04143)"로 **fully-merged 자체 확인 후 삭제**, `-D` 불필요/미사용). 🔴 **원격 브랜치는 애초에 없었다** — `git branch -r`·`gh api repos/.../branches` 확인 결과 `worktree-step1055`라는 이름의 원격 브랜치가 **한 번도 존재한 적 없음**(이 세션 내내 `worktree-step1055:main` 형태로 로컬 브랜치를 origin/main에 직접 push했을 뿐, 원격에 동명 브랜치를 만든 적이 없다). 원격 브랜치 원장 = `main`·`revdcf-preview` 둘뿐(둘 다 무접촉). PR #1 무접촉.

**§2-3 worktree 발생 출처**: `CLAUDE.md`·`docs/step_orders/STEP1055.md` 전문에 "worktree" 문자열 **0건**(grep 확인). `.claude/settings.local.json`에도 없음. 🔴 **출처 확정**: 이 세션(백그라운드 job)의 **하네스 자체가 첫 파일 편집 전 자동으로 워크트리 격리를 강제**한다(공유 체크아웃에 대한 Edit/Write 호출이 하네스 가드에 의해 차단되고, 해제 방법으로 `.claude/settings.json`의 `worktree.bgIsolation:"none"`을 직접 안내함 — 이번 STEP 실행 중 실제로 이 가드에 걸려 확인). **저장소 안(추측 아님, 확정)**: 이 정책은 저장소 파일이 아니라 **세션 하네스 정책**이라 `CLAUDE.md`·STEP 명령서 어디를 찾아도 안 나온다 — "찾았는데 없다"가 정확한 서술이지 "출처 미확인"이 아니다.
🔴 **이 STEP이 §2-4를 실행하기 위해 그 가드를 껐다** — `.claude/settings.json` 신설(`{"worktree":{"bgIsolation":"none"}}`), Bash로 작성(Edit/Write 도구 자체가 가드에 막혀 있었음). 이 설정 자체가 §2-4 규약의 **집행 장치**다(문서가 "만들지 말라"고 적어도 하네스가 자동으로 만들면 규약이 무력화되므로).

**§2-6 검증 3 · 검수 3**

검증 3 — ① 조작 전후 `git log --oneline -1`: 전 `08699ce`(메인 폴더) → 후 `8d04143` ② `ls docs/model_list_30_9.html docs/data_source_catalog.html docs/tombstone.html docs/roadmap_v2.html docs/build_sequence.html` 5개 전부 존재 확인 ③ `git worktree list` → `/Users/maegbug/stock-terminal 8d04143 [main]` 한 줄만(워크트리 0개).

검수 3 — ① §2-0 네 점검 원문 출력을 위 코드블록에 그대로 실음 ② `-D`·`--force`·`reset --hard`·`git clean` **전부 미사용 확인**(사용한 명령: `git worktree remove`[force 없이]·`git branch -d`·`rm -f .git/index.lock`[git 상태 아님, 프로세스 확인 후 제거]) ③ 원격 브랜치·PR #1 무접촉 확인(위 §2-2 서술 그대로, 애초에 원격 브랜치가 없었다는 사실도 확정).

🔴 **못 거친 축**: 없음 — §2-0·2-1·2-2·2-3·2-6 전부 수행. 굳이 밝히면 `.git/worktrees/step1055/` 생성 시각(2026-08-17 07:22, STEP 문서 자체 인용)을 이 STEP이 **재검증하지 않고 그대로 인용**했다(worktree가 이미 제거돼 그 디렉토리 자체로는 재확인 불가).

🔴 **철회·정정한 것**: 전제①의 "main..worktree-step1055 0줄" 판정 기준을 **로컬 main → origin/main**으로 정정(문면 그대로 로컬 main과 비교하면 17줄이 나와 손실처럼 보이나, 그 원인이 "worktree의 미반영 작업"이 아니라 "로컬 main 자체가 안 움직였던 것"임을 밝힘 — 위 §2-0 ㉠ 코드블록에 그대로 기록).

🔴 **판정은 장은태가 한다. worktree 정리는 이 STEP이 했다. 원격 브랜치(애초에 없었음)·PR #1(이미 병합됨) 처분은 해당 사항 없음.**

### 부-10. STEP1071(2026-08-19) — 초안 26개 업데이트·중복 삭제·제자리 고정

> 방향 전환(2026-08-18 장은태): *"지워도 되나"*가 아니라 *"살릴 것을 어떻게 업데이트해 넣나, 그다음 중복을 어떻게 없애나"*. `~/Downloads/_to_delete/2026-08-16_roadmap_drafts/`의 26개 초안(A. roadmap 계보 9개 · B. model_list 계보 14개 · C. 제목 다른 2개 · D. 현재본 사본 1개)을 전량 텍스트 대조.

**§2-0 현재본 집합(10개)**: `docs/model_list_30_9.html`·`docs/roadmap_v2.html`·`docs/build_sequence.html`·`docs/data_source_catalog.html`·`docs/tombstone.html`·**`docs/ROADMAP_V2.md`**·**`docs/BUILD_SEQUENCE.md`**·**`docs/LENS_DISPOSITION_2026-08-08.md`**·`~/Downloads/model_list_30_9.html`·`~/Downloads/roadmap_v2_2.html` — md 정본 셋 포함.

**§2-1 26개 추출**: HTML 태그를 블록 경계(`<br>·</p>·</li>·</div>·</tr>·</td>·</h*>`)에서 줄바꿈으로 치환해 문장 단위 보존. 크기·mtime 대조 결과 **A·B 두 계보 모두 크기순=시간순 확인**(mtime이 계보 내부에서 단조 증가 — 단 A·B 두 계보가 시간상 인터리브돼 있음: 사용자가 두 문서를 번갈아 저장, `dup_11`[A, 08-17 20:22]이 `dup_5`[B, 08-17 20:26]보다 먼저 — 병렬 편집의 정황 증거, 순서 자체는 각 계보 내에서 미확정 아님).

**§2-2 인접 diff(⓪-4① 검증)**: A계보 8쌍 중 6쌍에서 사라진 문단 1건 이상(0·1·0·0·3·3·4·0건) · B계보 13쌍 전부 1건 이상(24~72건). 🔴 **전제① 반증 — 축소 없이 26개 전부 다룸.**

**§2-3~2-4 고유 문장(축 C㉡)**: 거르는 기준(길이 25자+ · 숫자/좌표/판정어 중 1개 이상 · 현재본 10개 집합 어디에도 없음, `dup_12` 제외[md5 일치]) 고정 후 실행 — **원시 1,680건 → 파일별 중복 포함 302건 → 파일 간 중복 제거한 distinct 169건**(`docs/probe_1071_to_delete_diff.json` — 원자료).

**§2-5 제목 다른 둘**: `model_list_30_4.html`("공통 모델 22 — 채택·탈락 판정")·`model_list_30_5.html`("쓰는 모델 19·탈락 11") 전문 확인 — **별개 문서가 아니라 B계보의 한 분기**(30_3과 30_6 사이, 22-모델 개별 판정 표 형식으로 잠깐 갈라졌다가 6-질문 프레임으로 합류). 🔴 **"탈락 11 → 3"의 8개 행방 확정**: 30_4.html 말미의 "앞서 추려낸 8개"(Magic Formula·Residual Income/EVA·EPV·Net-Net/청산가치·Beneish M-Score·CFROI·추정치개정/어닝서프라이즈·Rule of 40) = 22-모델 목록 이전에 걸러진 것 + 22개 중 탈락 3(PEG·애널리스트목표주가·퀀트종합랭킹) = 8+3=11. **현재 `docs/model_list_30_9.html`의 "그 앞에서 이미 걸러진 여덟" 절이 그 8개와 좌표 일치 — 추적됨.** 탈락 3도 "탈락 — 안 쓰기로 한 것" 절과 문장 수준으로 일치(아래 §2-7). **잃은 것 없음.**

**§2-6 반영(원문 좌표 그대로, 2건)**:
1. `docs/model_list_30_9.html` 가-2(DCF 정방향) ①행에 초안 표본 크기 병기 — `dup_11.html`(A계보): *"조사한 8곳 전부 '우리가 계산한 값' 형태이고 Simply Wall St조차 입력값 조정이 막혀 있다"* — 현재본의 "실무 1/3" 표기와 표본 크기가 다름(3곳 vs 8곳), **어느 쪽이 최종 조사 범위인지 미판정**으로 양쪽 병기
2. `docs/model_list_30_9.html` 가-3(DDM 병합) 단락 뒤에 신규 발견 추가 — `model_list_30_4.html`·`_30_6.html`(B계보): *"시나리오 밸류에이션(원 17번) — DCF에 포함됩니다"* 🔵흡수 판정 — 현재 `ROADMAP_V2.md`·`docs/model_list_30_9.html` 어디에도 이 흡수 판정 자체가 없었음(반영 전 확인)

🔴 **검토했으나 반영 안 함(사유 버킷 4개, 나머지 167건)**:
| 버킷 | 건수(추정) | 사유 | 대표 예 |
|---|--:|---|---|
| **① 이미 본문에 실질 존재**(정규화 실패로 인한 오탐 — 표현만 다름) | 약 90건 | B계보 초반의 모델 1줄 정의(P/E·P/B·P/S·Piotroski·Altman·그레이엄 등)는 현재본에 같은 뜻 다른 문장으로 이미 있음. `grep`으로 개념 매칭 확인(예: "조합이 원리적으로 불가능"·"남의 판단이고 검산이 불가능"·"가중합은 정보를 더하지 않고 줄인다" 전부 현재본에 원문 그대로 존재) | 탈락 3(PEG·애널리스트목표주가·퀀트종합랭킹) 사유 문장 |
| **② 이후 STEP이 정정한 낡은 사실** | 약 5건 | 반영하면 이미 고쳐진 사실을 재도입하게 됨 | `model_list_30_5.html`: *"US 재료 0건 — dividends 60행은 전부 KR이었음"*(STEP1054가 배당 지급기준 34.2%·50.7% 실측으로 대체) · `model_list_30_4.html`: *"Assets·Liabilities·RetainedEarnings 태그 미수집"*(STEP1054가 73.8%/65.9%/72.2%로 해소) |
| **③ 절차·메타 서술**(모델 판정 콘텐츠 아님) | 약 40건 | 초안 제목 변형("주식 플랫폼 채택 모델 30" 등)·"회색 숫자는 원래 번호"류 표기 안내·자기지시적 정본 주장(이미 STEP1056이 정정) — 최종 산출물에 들어갈 성격이 아님 | 헤딩·표 범례 텍스트 |
| **④ 이미 F-5/부록에 판정 대기로 등재된 항목의 반복 인용** | 약 32건 | ROADMAP_V2.md F-5 ⑮~⑲(STEP1056)가 이미 좌표를 갖고 있음 — 중복 등재 안 함 | 질문개수6vs8·배수+역DCF분리·DCF정방향신설·층구분·DDM소속 각 초안판 서술 |

🔴 **검토했으나 보류(1건)**: `roadmap_v2_2_1__2.html` 등의 "배수↔역DCF 커버리지 차(1,452~3,558 vs 604·158skip)" — 현재본(`model_list_30_9.html:341`)에 **더 정밀한 수치(배수2,592·역DCF107)**로 이미 존재 확인, 초안 수치는 그 이전 단계의 거친 값으로 판단해 반영 안 함(사유: 상위호환 존재).

**§2-7 반영 검증 삼자 대조**: **찾은 169 = 반영 2 + 사유 있는 미반영 167(①90+②5+③40+④32)**. 🔴 **사유 없는 미반영 0건.**

### 부-10-부록. §2-10 — `scripts/_probe_B_flows.ts` 처리(STEP1070 이월)

전문 확인(34줄, STEP811 KRX 무료 수급/공매도 API 접근성 프로브 — mtime 7/28). `docs/STEP_811_COMMAND.md`(추적됨)가 같은 조사 브리핑을 담고 있으나 **결과가 CHANGELOG·STEP_LEDGER 어디에도 기록돼 있지 않다**(grep 0건) — STEP811이 실행됐는지, 실행됐다면 무엇을 찾았는지 이 저장소 어디에도 없다. 코드 안의 특정 엔드포인트 후보(`/sto/inv_bydd_trd`·`MDCSTAT02203`·`MDCSTAT30001` 등)는 **다른 어떤 파일에도 중복이 없음**(grep 확인) — 실행하지 않고는(외부 취득 금지라 이 STEP에서 실행 안 함) 이게 "이미 아는 결과"인지 "미보존 시도"인지 못 가른다. 🔴 **판단 못 함 → 지우지 않는다.** KR 전면 동결 정책상 이 조사 자체가 지금은 진행 대상이 아니라는 점도 함께 기록(삭제 사유는 아님, 우선순위 사실만).

### 부-10-검증. 검증 3 · 검수 3

검증 3 — ① 축A: 26개 원문 텍스트 대조(block-aware 추출, `probe_1071_to_delete_diff.json`) ② 축B: STEP1056·1069 방법(절대조·문장단위diff·양방향) 재사용 확인, *"기존 파일 덮어쓰기"* 준수(반영 2건 전부 기존 파일의 기존 절 안에 삽입, 새 파일·새 md/html 0개) ③ 축C: ㉠사라진 문단 = 계보 21쌍 중 15쌍에서 1건+ ㉡고유 문장 = distinct 169건(반영 2·미반영 167·보류 1) ㉢제목 다른 둘 = 별개 문서 아님, B계보 분기로 확정(§2-5).

검수 3 — ① §2-4 원시(1,680)/잔여(169) 둘 다 기록, 기준(길이25+·숫자·좌표·판정어·현재본부재) 먼저 고정 후 실행 ② §2-7 삼자대조(169=2+167), 사유 없는 미반영 0건 ③ 삭제는 게이트 8(CI+Vercel) 통과 후에만 — 아래 §2-8 기록에서 확인.

🔴 **못 거친 축**: 없음(⓪-0~2-11 전부 수행). `_probe_B_flows.ts`의 실제 KRX API 응답(실행하면 알 수 있는 것)은 이 STEP 범위 밖(외부 취득 0 준수)으로 미확인.

🔴 **철회·정정한 것**: 없음(이 STEP 자신은 §2·§7·§9 등 기존 서술을 정정하지 않음 — 반영 2건은 "정정"이 아니라 "누락 보충").

### 부-10-삭제. §2-8 삭제 실행 기록(게이트8 통과 후, 2026-08-19)

**조건 넷 재확인**: ㉠ §2-7 삼자대조 통과(사유 없는 미반영 0건, 커밋 `cf33314`) ㉡ 반영분 `main` push + CI 그린 확인(`gh run list` → `cf33314` success · Vercel Production success, deployment id 5966105541) ㉢ 삭제 대상 전체 목록(26개 경로·크기) — 아래 코드블록 ㉣ 대상은 `_to_delete/` 폴더 전체뿐, `docs/` 및 `~/Downloads/` 현재본 5개 무접촉.

```
삭제 전 ~/Downloads/_to_delete/2026-08-16_roadmap_drafts/ (26개, total 2000)
dup_1.html 55750 · dup_2.html 58693 · dup_3.html 62229 · dup_4.html 64152 · dup_5.html 31460
dup_6.html 36318 · dup_7.html 46505 · dup_8.html 48195 · dup_9.html 50631 · dup_10.html 54003
dup_11.html 64421 · dup_12.html 57628
model_list_30.html 15855 · _30_1 15917 · _30_2 15285 · _30_3 17147 · _30_4 18527 · _30_5 18160
_30_6 18233 · _30_7 21900 · _30_8 25343 · _30_9 26851
roadmap_v2.html 15394 · roadmap_v2_1.html 27536 · roadmap_v2_2_1.html 53113 · roadmap_v2_2_1__2.html 54749

삭제 후 ~/Downloads/_to_delete/: 디렉토리 자체 없음(rm -rf 성공, exit 0)
삭제 전후 ~/Downloads/ 전체 ls -la 대조: _to_delete/ 한 줄만 사라짐.
현재본 5개(build_sequence.html·data_source_catalog.html·model_list_30_9.html·roadmap_v2_2.html·tombstone.html)
및 그 외 개인 파일(사진·영수증 등) 전부 삭제 전후 동일 — 무접촉 확인.
```

🔴 **판정은 장은태가 한다. 이 STEP은 살릴 것을 넣고 중복을 지우는 것까지다. `~/Downloads/` 현재본 5개 존치 여부·`_probe_B_flows.ts` 처분은 판정 대기.**

### 부-11. STEP1072(2026-08-19) — 미반영 정정 전수 수집 후 반영

> "왜 지금인가"(3번 규칙): 축A — B 없이는 만드는 일을 시작할 수 없다(§2 "1개씩 만들고 완성 후 다음" + "9/9 통과 모델 0개"). 축B — STEP1055~1071 17개 중 「만드는」은 1055 하나뿐, 나머지는 감사·정리. 축C — 여러 STEP이 "정정 재료로만 남기고 고치지 않았다"로 끝났다. 문서가 어긋난 채 판정하면 잘못 판정한다.

**§2-1 전수 수집**: 정방향 패턴검색(`정정 재료|정정하지 않|정정 안 함|미반영|반영된 적 없`, docs/*.md 전량) — CHANGELOG.md 19·BUILD_SEQUENCE.md 11·LENS_DISPOSITION 10·STEP_LEDGER.md 9(전제①의 08-18 스냅샷 16/10/8에서 하루 새 증가 — 1069·1071 자신의 서술이 늘어난 탓)·REVDCF_SPEC 3·STEP_8xx_COMMAND류 다수(전부 과거 아카이브, 현재상태 문서 아님이라 교정 대상 제외) · LENS_COMPLETION_STANDARD 2·LENS_DEV_PLAYBOOK 2. 🔴 **CHANGELOG·STEP_LEDGER·구 STEP_COMMAND류는 이력 문서라 교정 대상에서 제외**(CLAUDE.md "현재상태=STATE·이력=CHANGELOG·STEP_LEDGER" 원칙 — 이력을 소급 수정하면 오히려 기록이 왜곡된다). 역방향(STEP1055~1071 각 산출물의 "못 한 것/철회·정정한 것/미측정" 절 재열람) — 씨앗 10건과 대조해 **새로 걸린 것 0건**(씨앗 자체가 이미 이 두 축을 거쳐 나온 목록이었음, `docs/step_orders/STEP1072.md` ⓪-4①이 이미 정방향 패턴 결과를 실측치로 제시).

**§2-2 사실/판정 이분**(판별기준: 어느 쪽이 맞는지 정해야 하면 판정, 이미 실측으로 나왔으면 사실):
| 씨앗# | 분류 | 근거 한 줄 |
|:--:|:--:|---|
| 1(SWS 4개→6/6) | **사실** | GitHub 원문 직접 확인(STEP1067), 재확인 불필요 — 단 우리 질문축 재분류(3/6)는 STEP1068의 독립 관찰이라 함께 반영 |
| 2(MODEL_ROSTER 비공개→공개) | **사실** | 같은 GitHub 원문이 반증 |
| 3(32.3%) | **판정**(기등재) | `probe_1054` 원경로 재확인 전이라 취소선 성급(STEP1068 자신이 명시적으로 금지) — `LENS_DISPOSITION §7-4` A-8에 이미 등재, 중복 없음 |
| 4(probe_1041 DuPont) | 해당없음 | probe는 시점 스냅샷 문서 관행상 소급 교정 대상 아님(이력), `BUILD_SEQUENCE` §7-부록이 이미 낡음으로 기록 |
| 5(9/9 vs 구조적불가) | **판정**(신규) | "완성 승인"을 취소할지 각주만 붙일지가 곧 판정 — `LENS_DISPOSITION §7-4 C` #9 신규 등재 |
| 6(F-5 확정딱지) | **판정**(기록 보강) | 어느 서술이 맞는지 판정 사항 — F-5 ⑮~⑲ 공통 주석 추가 + `LENS_DISPOSITION §7-4 C` #10 |
| 7(관문7개 낡음) | **사실**(표시만) | F-2가 실제로 폐기했음이 이미 실측 확인됨(STEP1065) — §3 머리에 낡음 표시, 표 내용 무접촉 |
| 8(§5 스냅샷) | **사실** | DB 실측으로 갱신 가능, 판정 요소 없음 |
| 9(판정A "폐기") | **미확인** | 이 세션 어느 커밋된 문서에도 "「폐기」가 원문에 없는 낱말"이라는 주장 자체를 찾지 못함(grep 0건) — 근거 문서를 특정 못 해 반영·등재 둘 다 보류, 미확인으로만 기록 |
| 10(Downloads 5개·probe_B_flows) | **판정**(신규) | STEP1071이 이미 판정 대기로 명시 — `LENS_DISPOSITION §7-4 C` #11·#12 신규 등재 |

🔴 **애매했던 것**: 1의 "6/6 vs 3/6" — 두 숫자 다 사실이지만 **어느 분류축을 문서 전면에 쓸지**는 서술 선택이라 판정에 가깝다. 이 STEP은 **둘 다 병기**하는 방식으로 사실 정정에 포함시켰다(택일하지 않았으므로 판정을 만들지 않음).

**§2-3 사실 정정 반영**(취소선+정정+좌표, 정본 먼저 표시본 나중): `MODEL_ROSTER.md:84`(비공개→GitHub 공개 확인) · `LENS_DISPOSITION_2026-08-08.md` §2·§4·§7-5의 "4개가 수익성" 3개소(→6/6+질문축 3/6 병기) · `docs/roadmap_v2.html`은 이번 정정 내용이 없어 표시본 동기화 불필요(grep 0건 확인) · `docs/model_list_30_9.html`도 동일(grep 0건).

**§2-4 판정 등재**: `LENS_DISPOSITION_2026-08-08.md` §7-4에 신규 "C. STEP1072 신규 등재 — 4건"(#9~12, 다섯 칸 형식) · `ROADMAP_V2.md` F-5 ⑮~⑲ 뒤에 공통 주석 1행(model_list_30_9.html "확정" 딱지 미반영 지적, `LENS_DISPOSITION §7-4 C` #10 참조) + `docs/roadmap_v2.html` 표시본에 동일 주석 병기. 기등재(씨앗#3)는 새로 안 만들고 "기등재"로만 표시(위 §2-2 표).

**§2-5 스냅샷 갱신**(DB 실제 조회, `docs/BUILD_SEQUENCE.md` §5): `revdcf_results` as_of=2026-08-18 **604행**(불변) · `us_valuation` as_of=2026-08-18 **5,820행**(불변)·per **2,130**·pbr **3,361**·psr **3,642**·ev_ebitda **1,512**(전부 소폭 증가, STEP1058의 08-17 밤 자연복구 기록과 정합) · `lens_scores` market='US' **998행**(1,039에서 감소 — 원인 미조사, 이 STEP 범위 밖으로 명시만) · `REVDCF_ENABLED`·`Q1_ENABLED`는 **Vercel SSO가 익명 접근을 막아 직접 확인 불가**(프로덕션 URL `curl` 302 재확인, `gh api .../deployments/.../statuses`로 실제 프로덕션 URL 특정 후 재시도해도 동일) — 간접 근거(F-5 ④⑤가 여전히 판정 대기·전환 CHANGELOG 기록 없음)로 OFF 추정만, **추측을 실측으로 기재하지 않음**.

**§2-6 관문 7개 파급 표시**: `BUILD_SEQUENCE.md` §3 머리에 "이 절 전체는 폐기된 관문 7개 기반" 명시 + F-2 좌표. 표 내용(§3-A/B/C) 무접촉 확인.

### 부-11-검증. 검증 3 · 검수 3

검증 3 — ① 축A: 씨앗 10건 전부 **근거 STEP의 산출물 원문**을 직접 열어 확인(CHANGELOG 서술만 본 것 0건 — 예: 씨앗1·2는 `LENS_DISPOSITION §9-0`·`§9-2` 원문, 씨앗5는 `LENS_COMPLETION_STANDARD.md:117·119` 원문과 `§7-3` 원문 대조) ② 축B: 취소선+정정+좌표 형식 전부 준수, 정본(md) 먼저 고치고 표시본(html)은 변경 필요 항목 자체가 없어 grep으로 확인만 ③ 축C: ㉠판정 오분류로 등재만 한 것 = 씨앗3·5·6·10 (4건, 사실로 잘못 반영했으면 없는 판정을 발명했을 자리) ㉡중복 정정 = 0건(§2-4에서 기등재 표시로 회피) ㉢근거 뒤집힘 발견 = 씨앗1(STEP1067→STEP1068 분류축 정정, 병기로 해소).

검수 3 — ① 수집 = 반영(사실 2: 씨앗1·2) + 등재(판정 4: 씨앗5·6·9·10, 씨앗9는 미확인으로 별도) + 기등재(1: 씨앗3) + 해당없음(1: 씨앗4) + 사실·표시만(1: 씨앗7) + 사실·갱신(1: 씨앗8) = **10건 전부 처리 확인** ② 씨앗 밖 신규 발견 = **0건**(정방향·역방향 둘 다 씨앗 10건을 벗어나는 새 항목을 못 찾음 — 씨앗 자체가 이미 전수에 가까웠던 것으로 판단) ③ 정본만 고치고 표시본을 빠뜨린 문서 = 0건(표시본 동기화가 필요한 항목 자체가 없었음을 확인, F-5 ⑮~⑲ 주석은 md·html 둘 다 반영).

🔴 **못 거친 축**: 씨앗9("폐기"가 원문에 없는 낱말)의 근거 문서를 특정하지 못함 — grep으로 "폐기"·"원문에 없는" 조합을 여러 방식으로 시도했으나 이 세션의 어느 커밋된 텍스트도 해당 주장을 담고 있지 않았다. 대화 중 구두로만 오간 것일 가능성이 있으나 확인할 방법이 없어 **미확인으로 남긴다**(반영도 등재도 하지 않음 — 근거 없는 것을 판정 대기로 등재하면 그 자체가 근거 없는 항목 생성).

🔴 **철회·정정한 것**: 없음(이 STEP 자신의 서술을 정정하지 않음).

🔴 **판정은 장은태가 한다. 이 STEP은 사실을 맞추고, 판정할 것을 한자리에 모으는 것까지다.**

### 부-12. STEP1073(2026-08-19) — 판정 A·B 확정 반영 + 「확정된 모델」 목록 정본화

> `docs/step_orders/STEP1073.md` ⓪-1: 장은태가 `BUILD_SEQUENCE.md:227` 선택지 ①을 그대로 채택(판정A), 판정B는 선택지를 고른 게 아니라 **물음 자체를 "확정된 모델을 1개씩 완성하자"로 고쳐 적었다.** 이 STEP은 그 반영이다 — 새 판정 0.

**§1·§2 반영 위치(전부 취소선+정정+좌표, 지운 문장 0)**: `BUILD_SEQUENCE.md` §1(정의 셋 표에 층 라벨 열 추가) · §6 표(A·B 행) · §6-부록 부록-1(판정 A 결론) · §6-부록 부록-2(판정 B 결론) · §6-부록 부록-5(판정 B 다섯 칸 중 "무엇을 정해야 하나"·"선택지①") · `STATE.md`(§6 마지막 불릿 A·B 서술) · `ROADMAP_V2.md`(F-4-1② 결론부·F-4-3 인접 한 줄) · `MODEL_BUILD_ORDER_2026-08-07.md:74`(뒤에 한 줄).

**§3 C~F 상태 라벨**(부록-3 표에 "새 상태" 열 추가, 실착수 0): C(역DCF 위치)=🟡착수가능(B가 "새 모델 고르지 않음"이라 답해 역DCF는 이미 확정된 모델의 일부) · D-내용=🟡착수가능(A가 기준선 제공) · E(7렌즈 거취)=🟢답 있음(🔴 "거취" 프레임 자체가 틀렸다는 것이 이번 발견 — 실제 목적은 `CLAUDE.md:81` 규칙3 재사용 자격 심사였고 `STEP1066.md`가 스스로 "규칙3의 종착"이라 명시) · F(Q1 착수전 4건)=🔴§4결과대기. E경유 하위 항목들(1064 판정대기 #1·#2·#4·#5, WHAT "라")은 **이 STEP이 재검토하지 않음**(E 프레임 변경의 파급이지만 §4 범위 밖, 명시만).

**§4 「확정된 모델」 전수 추출**(`model_list_30_9.html`, grep "확정" 20건): 20건 중 모델/판정 확정과 무관한 것 12건(예: `expected_inflation`으로 확정[WACC 파라미터], "확정하지 못했습니다"[부정형], "코드 확정"[버그 원인 규명], CLAUDE.md 규칙3 인용, 가정형 서술 등 — 세부 목록은 이 부록 원자료에 갈음, 개별 나열 생략) · 모델/판정 확정 관련 8건이 실질 **5개 항목**으로 수렴(가-1 배수↔역DCF·가-2 DCF정방향·가-3 DDM·다 층구분·가-종합 질문여섯). `git log --oneline --all -- docs/model_list_30_9.html` = `cf33314`(STEP1071)·`8d04143`(STEP1069) **둘뿐** — STEP1069가 최초 커밋(Downloads에서 편입)이라 **편입 이전 저장소 이력 없음**(추측 금지, 있는 그대로 기록). 5개 항목 전부 **㉢ 표시본에만 있고 출처 추적 불가**로 분류 — 단 **전부 이미 등재돼 있음**: `ROADMAP_V2.md` F-5 ⑮(질문여섯)·⑯(배수↔역DCF)·⑰(DCF정방향)·⑱(층구분)·⑲(DDM), `LENS_DISPOSITION_2026-08-08.md §7-4 C#10`(STEP1072가 이미 신규 등재) — **중복 등재 0건, 신규 판정 대기 0건.**

**§4-0b**: `LENS_COMPLETION_STANDARD`의 "9/9 ✅"와 감사의 "항목3 구조적 불가" 충돌은 `LENS_DISPOSITION §7-4 C#9`(STEP1072가 이미 등재) — 좌표만 확인, 중복 등재 안 함.

### 부-12-검증. 검증 3 · 검수 3

검증 3 — ① 문구 일치: §1·§2에서 쓴 확정 문장을 `BUILD_SEQUENCE.md:227`(현 위치는 §1 표 아래 이동 없음, 좌표 유지)·장은태 원문과 대조 — "폐기"·"확정된"·"1개씩"·"완성" 전부 원문 낱말 그대로 사용 확인 ② 누락 없음: `grep -rn "미판정|판정 대기|장은태 몫|판정 = 장은태" docs/*.md`로 A·B 관련 잔존 재확인 — 아래 코드블록 ③ 역방향: 확정 문장에서 출발해 `STATE.md`·`ROADMAP_V2.md`·`MODEL_BUILD_ORDER`·`LENS_DISPOSITION` 넷 전부 확인 — `LENS_DISPOSITION`은 §4 프레임(§6-E) 병기만 하고 A·B 자체 서술은 없어 "왜 안 바꿔도 되는지"에 해당(A·B는 애초에 이 문서가 다루는 판정이 아니었음).

```
grep -rn "미판정|판정 대기|장은태 몫|판정 = 장은태" docs/*.md 중 A·B 관련 잔존:
- STATE.md:226 → 위에서 취소선+확정으로 처리 완료
- BUILD_SEQUENCE.md §6표 A·B행 → 처리 완료
- BUILD_SEQUENCE.md 부록-1·부록-2 결론 → 처리 완료
- ROADMAP_V2.md:438(F-4-1②)·:470(F-4-3) → 처리 완료
- MODEL_BUILD_ORDER_2026-08-07.md:74 → 처리 완료
잔존 0건(A·B 관련). C~F·§7-4의 "판정 대기" 문구는 그대로 남음(정상 — 이 STEP 대상 아님).
```

검수 3 — ① 창작 안 함: 이 STEP이 쓴 판정 문장은 전부 장은태 원문(⓪-1) 또는 저장소 기존 좌표(⓪-2·⓪-3) 인용 — Cowork 자체 판단 0 ② 새 파일 0개(`docs/step_orders/STEP1073.md` 1개만 신규, 명령서 자체는 허용 대상) ③ 판정 안 함: §4-2 ㉢ 5건 전부 정본에 올리지 않고 기존 등재(F-5·§7-4 C#10)만 인용 · C~F 실착수 0(라벨만) · `Q1_ENABLED`·`REVDCF_ENABLED` 무접촉(조회도 안 함, STEP1072와 달리 이번엔 §3-1이 아예 금지).

🔴 **못 거친 축**: 없음(§1~§7 전부 수행).

🔴 **판정은 장은태가 한다. 이 STEP은 이미 내려진 판정을 문서에 옮기고, 확정된 모델 목록을 정리한 것까지다. 무엇을 먼저 완성할지는 다음 STEP(장은태 판정) 몫이다.**

### 부-13. STEP1074(2026-08-19) — STEP1073 §4 오류 정정: 「확정된 모델」 판정을 기존 검수 기록과 맞춘다

> STEP1073이 §4(확정된 모델 전수 추출)에서 `git log --oneline --all -- docs/model_list_30_9.html`이 두 커밋뿐이라는 **한 계층만 보고** 다섯 항목(가-1~가-3·다·질문여섯)을 균일하게 "미확정"으로 처리했다. **같은 문서의 부-7(STEP1069, 08-18)이 이미 다섯을 한 줄씩 갈라 대조해뒀는데 열지 않았다.** `BUILD_SEQUENCE §8` #1(한 계층만 보고 후보소진)·#4(기존조사 안 찾고 재조사)에 해당하는 재발 사례.

**§1 §6-B-1 표 4행 정정**(취소선+정정+좌표, 행 삭제 0): 가-1(배수+역DCF)=🟢**확정으로 승격**(`ROADMAP_V2.md:235` W-2 첫 질문이 이미 병합 형태, STEP1053 2026-08-16 확정 · `model_list_30_9.html` 가-1 자신도 "정본 일치"라 적음) · 가-2(DCF정방향)=⚠️**"미확정"이 아니라 "정합"**(양쪽 다 미판정이라 어긋남 아님) · 가-3(DDM)=⚠️**표현 차이, 방향 일치**(`ROADMAP_V2.md:307`에 이미 "적정주가류로 판정(다른칸)") · 다(층구분)=✅**STEP1073이 맞음, 그대로 유지**(진짜 md 미반영). 표 머리에 "다섯이 균일하지 않다" 한 줄 추가.

**§2 확정된 칸 개수**: 명시적 결론 문장이 원래 없었음을 밝히고(허위 취소선 방지) 신규로 "확정된 칸은 넷이다"(①배수+역DCF ②F스코어 ③④모멘텀+기술) 명시. 완성 승인은 여전히 0개(`:106` 유효).

**§3 §8 재발 등재**: #1·#4 두 행에 각각 재발 사례 한 줄 + 재발방지 신설("정본에 반영됐나"를 물을 때 ㉠git이력 ㉡정본본문 ㉢기존대조기록[부록·probe] 셋을 다 본다).

**§4 F-5 갱신**: ⑮(질문개수)=🟢 정본 여섯 확정, 표시본 여덟은 스냅샷 · ⑯(배수+역DCF)=🟢 정본에 이미 병합 확정 · ⑲(DDM)=⚠️ 정본에 이미 방향 일치 · ⑰·⑱은 그대로(⑰=정합, ⑱=진짜 미반영). 공통 주석 취소선+정정("넷 전부 미반영"→"⑱ 하나만 남음").

**§5 `LENS_DISPOSITION §7-4 C#10` 범위 축소**(삭제 아님, 취소선+정정): "4항목 미반영" → "⑯·⑲는 정본에 이미 있었음, 남은 판정 대기는 ⑱ 하나".

**§6 표시본 동기화**: `model_list_30_9.html` 5곳 라벨 갱신(가-1·가-종합=🟢확정, 가-2=⚠️정합, 가-3=⚠️방향일치, 다=유지) · `roadmap_v2.html` F-5 ⑮·⑯·⑲ 행 + 공통주석 동일 반영 · `build_sequence.html`은 §6-B-1 자체가 STEP1073에서 이관되지 않은 영역이라(부-12 기록) 이 STEP의 §1~§5도 해당 사항 없음(추가 동기화 불필요, 기존 "부록 미이관" 안내 유지) · `~/Downloads/` 무접촉.

**검증 3**: ① 원문 대조 — §1 네 행 정정 전부 부-7 원문·`ROADMAP_V2.md:235`·`:307`과 글자 단위 확인, 부-7에 없는 판단 추가 0건 ② 누락 없음 — `grep -n "미확정" docs/BUILD_SEQUENCE.md` 전수 확인, 다(층구분) 1건만 의도적으로 남고 나머지 전부 정정됨 확인 ③ 역방향 — `ROADMAP_V2` F-5 ⑯·`LENS_DISPOSITION` C#10·`STATE.md`·표시본 3개(model_list_30_9.html·roadmap_v2.html 반영, build_sequence.html은 "해당 사항 없음" 사유 명시) 확인.

**검수 3**: ① 판정 안 함 — ⑰·⑱은 판정 대기 그대로, "무엇을 먼저 완성할지" 미결정, `Q1_ENABLED`·`REVDCF_ENABLED` 무접촉(조회도 안 함) ② 삭제 안 함 — STEP1073 서술 전부 취소선으로 보존, C#10도 삭제 아닌 범위 축소 ③ 새 파일 0개(`docs/step_orders/STEP1074.md` 1개만 신규).

🔴 **못 거친 축**: 없음(§1~§8 전부 수행). **열지 않은 파일**: 없음(⓪-2 목록 7개 전부 열람 확인).

🔴 **판정은 장은태가 한다. 이 STEP은 STEP1073의 오류를 기존 검수 기록에 맞춰 되돌린 것까지다.**

### 부-14. STEP1075(2026-08-19) — 표시본 머리 요약 동기화: 한 문서 안의 모순 해소

> STEP1073이 `docs/build_sequence.html`의 §1 표·§6 표(본문)만 고치고, 문서 맨 앞의 "머리 요약"(hero 카드·`<p class="note">` 한 줄)을 두었다. 결과: 같은 문서가 **앞에서는 "판정 A·B 미판정", 뒤에서는 "🟢 확정"**이라고 말하는 상태가 STEP1073(08-19)부터 이 STEP까지 남아 있었다. 문서는 위에서부터 읽으므로 첫 화면이 틀리면 전체가 틀린 것과 같다.

**§1 `docs/build_sequence.html` 머리 요약 4곳**(정본 문구 그대로 옮김, 새 문장 창작 0, 기존 `<s>` 취소선 표기 재사용): 「판정 A」카드=취소선+🟢확정(정의3 최상위·정의2 신뢰층·정의1 폐기) · 「판정 B」카드=취소선+🟢확정(「확정된 모델을 1개씩 완성한다」, 「1위」 미채택 근거 3건) · 「C~F」한 줄=취소선+새 상태(C=🟡착수가능·D내용=🟡착수가능·E=🟢답 있음·F=🔴§6-B-1 결과대기) · 🆕 「확정된 칸 넷」 카드 신설(`:173` 그대로 이관).

**§2 나머지 표시본 검사**(태그 제거 후 전문 파싱, 6개 패턴 `미판정`·`판정 대기`·`장은태 몫`·`1위를 제대로`·`셋 다 살아`·`A·B 아래` 전수 검색): `docs/roadmap_v2.html` — "1위를 제대로" 2건 발견, 둘 다 STEP1073이 이미 그 자리에서 바로 뒤에 정정 문단을 붙여둔 것으로 확인(추가 조치 불필요) · 그 외 "미판정"·"판정 대기" 다수는 전부 A·B가 아닌 다른 판정(F-5·M-4·구조잔여 3건)이라 무접촉(규칙대로 정상) → **검사함·해당 없음**. `docs/model_list_30_9.html` — "미판정"·"판정 대기" 다수 발견했으나 전부 STEP1073·1074가 이미 취소선+정정을 붙인 자리이거나(가-1~가-3 카드) A·B와 무관한 다른 주제(STEP1058 크론 수정·DCF 표본크기·SWS Past Performance) → **검사함·해당 없음**. 🔴 **두 파일 모두 build_sequence.html과 같은 "머리요약 vs 본문 불일치" 병은 없었다** — 애초에 두 파일엔 A·B를 다루는 별도의 "머리 요약" 섹션 자체가 없기 때문(A·B는 `BUILD_SEQUENCE.md` 고유 판정).

**§3 재발 방지**: `BUILD_SEQUENCE.md` §7(문서 위계) 표 바로 아래에 신설 — "표시본을 갱신할 때는 머리 요약과 본문 표를 둘 다 고친다."

### 부-14-검증. 검증 3 · 검수 3

검증 3 — ① 앞뒤 일치: `build_sequence.html` 전문 파싱 후 "판정 A"·"판정 B" 등장 위치 전수 나열 — 머리 요약(hero, 이번에 수정) 1곳 + §6 표 A·B행(STEP1073에서 이미 확정) 1곳, **둘 다 🟢 확정으로 일치**(미판정 잔존 0건) ② 정본 대조: §1에서 쓴 문구를 `BUILD_SEQUENCE.md` §6표·§6-B·`:173`과 대조 — 표시본에서 새로 지은 문장 0개(전부 정본 인용) ③ 역방향: §2의 두 파일 검사 결과를 위 표로 제시, 둘 다 "검사함·해당 없음"으로 명시(안 한 것과 구분).

검수 3 — ① 판정 안 함: C~F·F-5·§7-4의 "판정 대기"는 전부 그대로 둠(라벨만 있던 것 재확인, 새로 판정 안 함) · "어느 칸부터 완성할지" 정하지 않음 · `Q1_ENABLED`·`REVDCF_ENABLED` 무접촉(조회도 안 함) ② 삭제 안 함: 옛 문구 전부 `<s>` 취소선으로 보존, 표시본 기존 내용 손실 0(신설한 "확정된 칸 넷" 카드는 정본 인용이라 순증) ③ 새 파일·새 방식 안 함: `git status` 신규 = `docs/step_orders/STEP1075.md` 하나뿐, 취소선 표기는 이 문서가 이미 쓰던 `<s>` 태그 그대로 재사용(새 CSS·새 표기 창작 0).

🔴 **못 거친 축**: 없음(§1~§5 전부 수행). **열지 않은 파일**: 없음(⓪-2 목록 5개 전부 열람).

🔴 **판정은 장은태가 한다. 이 STEP은 이미 정본에 반영된 판정을 표시본 첫 화면까지 옮긴 것까지다.**

---

## §8. 🔴 반복된 실수 — 다시 하지 않기 위해 남긴다

| # | 실수 | 기록 |
|:--:|---|---|
| 1 | **한 계층만 보고 "후보 소진" 선언** | `LENS_ROADMAP`(07-03)이 팩터만 훑고 닫음 → 역DCF·배수·건전성 모델이 시야 밖. 1033이 12개 이상 재발견. 🔴 **재발(2026-08-19 STEP1073)** — 「확정된 모델」 목록을 만들며 `git log`(한 계층)만 보고 다섯 항목을 균일하게 「미확정」 처리했다. **같은 저장소 `BUILD_SEQUENCE.md` 부-7(STEP1069, 08-18)이 다섯을 이미 한 줄씩 갈라 대조해뒀는데 열지 않았다.** 결과: 정본에 08-16 확정돼 있던 가-1(배수+역DCF 병합)을 미확정으로 떨어뜨렸다. 정정 = STEP1074 |
| 2 | 🔴 **관문 없이 "원전 대조 가능성"만으로 모델 선정** | `MODEL_BUILD_ORDER` §5가 자기 실수로 기록 — *"사용률 1위가 「원전이라 부를 저작이 없다」는 이유로 밀려났다"* |
| 3 | 🔴 **같은 실수 반복(2026-08-15)** | Cowork이 §3-B를 안 읽고 *"원전이 책 한 권으로 명확하니 역DCF"*를 추천. **2번과 같은 병.** 철회함 |
| 4 | **기존 조사를 안 찾고 재조사** | 1033 명령서가 ⓪-1b를 규칙으로 걸어놓고 정작 08-07 조사 4건을 빠뜨림(INDEX 등재돼 있었음). 🔴 **재발(2026-08-19 STEP1073)** — 「확정된 모델」 목록을 만들며 같은 폴더(`BUILD_SEQUENCE.md`) 안의 부-7(STEP1069가 이미 낸 대조 기록)을 다시 찾아 열지 않고 처음부터 다시 판정했다. 정정 = STEP1074 |
| 5 | **소표본 오판 5회**(🔴 원문 "6회"는 오기 — `_TEMPLATE.md:64`가 실제로 명시하는 수는 다섯[985 XOM 1종목·998 5종목·999 20종목·1007 `USA` 수기분류·1015 초안 8/8]. "6번"은 같은 파일 다른 줄[:23]의 **Vercel 로그 중복조사 횟수**와 혼동한 것으로 보임 — 별개 사례) | 표본 상한을 걸 때 전수 비용을 안 적음 → 템플릿 ⓪-5에 규칙 신설 |

🔴 **공통 원인: 이미 있는 답을 안 찾고 새로 만든다.** 그래서 `KNOWN_ANSWERS.md`와 ⓪-1b가 생겼다.

🔴 **재발 방지 한 줄(2026-08-19 STEP1074 신설)** — 「정본에 반영됐나」를 물을 때는 ㉠git 이력 ㉡정본 본문 내용 ㉢기존 대조 기록(부록·probe) **셋을 다 본다.** 하나만 보고 판정하지 않는다.

---

**이 문서는 판정이 아니다. §6의 여섯 칸 = 장은태.**
