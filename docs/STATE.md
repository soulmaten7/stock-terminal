<!-- 2026-07-27 -->
# 🚀 Trillion(트릴리언) — STATE (현재 상태 단일 정본)

> **이 파일 = "지금 어디까지 왔나 + 다음 뭐 할까"의 유일한 정본. 매 세션 덮어쓴다(배너 쌓기 금지).**
> 새 세션 읽는 순서: **이 STATE → `docs/SYSTEM_MAP.md`(아키텍처) → 작업별 PLAYBOOK** → 이력 = `docs/CHANGELOG.md`.
> 규칙: **현재상태=여기에만 · 이력=CHANGELOG에만 · 아키텍처=SYSTEM_MAP에만.** (CLAUDE.md 문서 규칙)

## HEAD / 배포
- 코드 HEAD **`474cac0`**(STEP 798 · 레이아웃·문구 마감) · origin/main 동기화 · 트리 클린 · **배포 ✓ onetrillion.app** · CI 그린. ⚠️ **Vercel = Hobby: 크론 일 1회 한도(위반 시 배포 거부) · 하루 100 배포.** 크론 **9개**(§SYSTEM_MAP §4 — 793~798에서 파킹 전용 6개 중지).
- **베타 전 3중 검수 마감(793~798, HEAD `474cac0`)**: 보안 봉인(793) → 견고성(794) → UX 12건(795) → PC 공용 셸(796) → 정직한 결측 복구+GB 포이즈닝(797) → 레이아웃·문구 마감(798). 상세 = CHANGELOG 2026-07-27(3). **기술 블로커 0 — 베타 발송 대기.**

## 정체성 (권위 = `docs/BRAND_IDENTITY.md`)
- 사업자 **원트릴리언** · 대표 장은태 · 210-39-33812 · contact@onetrillion.app.
- 슬로건 **"종목을 보는 눈을, 누구에게나."** — 3기둥: 무기(TR-AI 렌즈)·직시·자립. 멍거 톤. **예측·추천 안 함 · 거래 X.** 미드나잇+민트 다크. 코드 식별자 `unjong-*` 유지.
- 🔥 **필드 = 5면**: **오늘(/) · 탐색(/explore) · 종목상세 · 관심 · 마이.** i18n = ko·en(전면 패리티).

## 지금 상태 (정직한 기준선)
- **🎯 활성 시장 = KR·US뿐(STEP799)**: JP·CN(HK 포함)·VN·GB는 검색·관심목록 등록·종목상세 진입·사이트맵에서 전부 차단(파킹 — 데이터·크론·라우트는 보존). 제어점 = `lib/activeMarkets.ts`의 `ACTIVE_MARKETS` 배열 하나(검색 인덱스·관심목록 스냅샷·watchlist 등록 게이트·종목상세 페이지·사이트맵이 전부 이걸 공유). 복원 절차 = `docs/PARKED_FIELD_SURFACES.md` §7.
- **오늘**: 내 관심 변화·간밤 미국·시장 변화(이름 전부 "상태가 바뀐 종목" 한 정본·STEP780) + **한 입 브리핑**(헤더 아래 LLM 리드 문단·결정론 입력+3중 가드·STEP778) + 관심 행 이름 영문 통일·PC 전용 hover 관심 별(STEP781) + 섹션 헤더 모바일 줄바꿈 회귀 수정(STEP785).
- **탐색**: 검색 6개국 + 렌즈 목록 3종(KR/US) + **랭킹 근거 둘째 줄**(강점 리스트=강점수+대표 렌즈 라벨·거래대금 리스트=조/달러 값·STEP779) + 섹션 헤더 모바일 줄바꿈 회귀 수정(STEP785).
- **종목상세**: 렌즈+게이트·6개국 + **렌즈 계산 3단 공개 → 초보 우선 재설계 완결**(STEP782~789): 카드 제목 = 초보자 질문형(`question` 필드, 학술명은 작은 부제로 격하) · 펼침 본문 = 판정→게이지→**서사 상시노출**(접힘 0개)→압축 근거줄→한계, PC(lg+) 2단 · 렌즈 목록 앞뒤로 파트 헤더 + 닫는 카드("7가지 방법을 종합하면": 카운트+강점·주의 나열+시간축 분포 기반 한 문장) · 헤더 모바일 붕괴(글자단위 줄바꿈)·PC 중복 표시 등 회귀 전부 수정 완료(STEP786·789). **US 종목명 ADR/중복토큰 정리 + 상단 안내 4→1 통합(STEP790)**: RELX류 3줄 ADR 보일러플레이트 절단(487건 실측·나쁜 절단 0)·"판단은 당신" 계열 중복 문구 4→상하단 각 1회로 정리. **파트 헤더 7개 목록 명시(STEP791)**: "7가지 방법으로 따로 보기" 옆에 실제 라벨 목록+`/about` 링크, 개수는 실제 렌더 카드 수 기준(ETF 4개·은행 6개 등 동적). **공시 카드 정리 6개국(STEP792)**: AI 요약 온디맨드(펼칠 때 1회만)·기본 5건+더보기·제목 공백 트림·동일날짜/제목 그룹핑("N건" 배지, 건수 보존).
- **관심 / 마이**: 마이페이지 3섹션+탈퇴 + **이메일 모닝 브리핑 opt-in 토글**(계정 탭·기본 OFF·STEP784).
- **🔒 베타 전 하드닝(793~798)**: ① **보안**: 무인증 LLM 8종 게이트(known-id + IP 레이트리밋 + 봇 차단·`lib/rateLimit.ts`)·CN/VN/GB 캐시 포이즈닝 봉인(키=`sha1(url)`·공용 `lib/summaryCacheKey.ts`)·US 경로순회 차단·크론 `Bearer undefined` 버그 봉인·수신거부 GET 부수효과 제거·`/api/dart` 화이트리스트. ② **견고성**: 렌즈 per-lens 격리(1개 throw해도 나머지 렌더)·공시요약 6종 upsert 에러 로깅·edgar 타임아웃·health 하트비트 감시(`cron_heartbeats`)·Sentry 폭주 억제(렌즈키별 1회). ③ **정직한 결측(797)**: 6개국 공시 API가 **ok(0건)/fetch_failed/unsupported 3상태**를 구분 반환 → 장애를 "사건 없음"이라 거짓 단언하던 §직시 위반 제거(실패=숨김·0건만 "없어요" 카드·브리핑 실패도 결측 1줄). ④ **PC 공용 셸(796)**: `components/layout/PageShell.tsx`로 5면 좌측선 144px 정합. ⑤ **UX·레이아웃 마감(795/798)**: 로그인 후 복귀(next 쿠키 왕복)·모바일 44px 타깃·해요체/`관심종목` 통일·죽은 키/CSS 정리·탐색 폭 회귀·미지 심볼 noindex.
- **이메일 모닝 브리핑(R4 1차)**: opt-in·매일 22:15 UTC(KST 07:15)·daily_brief 재사용(새 LLM 콜 0)+관심종목 오늘 전환+원클릭 수신거부. Resend 무료 티어. 실사용자(soulmaten7) ko/en 실수신+수신거부 왕복 라이브 검증 완료.
- **백그라운드(크론 9개 — 794 §5에서 파킹 전용 6개 중지)**: 가동 = `us-perf`·`kr-perf`·`kr-etp`·`kr-lens-scores`·`lens-scores`(US 렌즈)·`jp-disclosures`·`health`(하트비트 감시)·`daily-brief`(21:00)·`email-brief`(22:15). **중지(스케줄만·라우트/데이터 보존)** = `jp/cn/vn/gb-perf`·`fss-advisors`·`youtube-refresh`(파킹 표면 전용·`ToolboxClient` 렌더 0). `kr-etp`는 종목상세 KR ETF 헤더가 라이브로 읽어 **유지**. 복원 = `vercel.json` 재등록(`docs/PARKED_FIELD_SURFACES.md` §6).
- **미완/파킹**: JP/CN/VN/GB 렌즈 선계산 없음·JP 브릿지 프로브만·**대화형 LLM 미착수**(아래 결정 참고)·푸시 알림·앱스토어 미착수(아래 결정 참고).
- **⚠️ 잔여 확인**: STEP 785~792(모바일 헤더 회귀·렌즈 카드 재설계·종목명 정리·상단 안내 통합·파트 헤더 목록·공시 카드 정리)는 코드·API 응답 기준으로 검증했으나 **장은태 폰 실물 최종 확인 대기 중**(375px·PC 1280px·`/en`).

## 🔑 결정 (07-22~23)
- **대화형 LLM = 3단 공개 이후 선택지**(확정 로드맵 아님). 렌즈 계산 3단 공개(782/783, 787에서 상시노출로 완결)가 "왜 이 판정인가"에 결정론으로 이미 답함 — 사용자 질문↔LLM 응답의 대화형 레이어를 그 위에 얹을지는 **베타 반응 보고 재논의**. 지금 착수 안 함.
- **푸시 알림·앱스토어 전환 = Resend 열람률 검증 후 게이트**. 이메일 모닝 브리핑(784)이 재방문 훅 1차 실험 — Resend 대시보드 오픈레이트 누적을 보고서야 "이 훅이 통하는가"를 판단, 통하면 푸시·네이티브 앱(Phase 3)으로 확장. 지금은 이메일 단계에 머무름.

## ▶ 다음 (우선순위)
1. **🚀 클로즈드 베타 발송**(최우선 — 793~798로 베타 전 3중 검수 마감·기술 블로커 0). 발송 = 사용자, 문구는 Cowork 요청 시. 발송 직후 실사용 관찰 시작.
2. **베타 라이브 실측 게이트**(발송 직전 육안): PC 1280px 5면 좌측 144 정합·375px 회귀 0·로그인 후 복귀(관심·마이·종목상세)·F-Score 미지원 종목 카운트·공시 3상태(0건/실패/미매핑)가 화면에서 다르게 보이는지.
3. **이메일 브리핑 실사용 관찰 + Resend 열람률 누적** → 푸시/스토어 착수 여부 게이트(결정 §참고).

### 남은 관찰 항목 (베타 중 지켜볼 것 — 블로커 아님)
- **`/en` 관심목록 비미국 종목명**: KR/JP 등 비미국 종목이 `/en` 관심 화면에서 영문명으로 뜨는지(현재 `resolveWatchlistName` 경로 — 라이브 확인 필요).
- **사이트맵 hreflang**: `app/sitemap.ts`가 ko URL만 등재(대체 URL 미포함). 종목상세는 `generateMetadata` hreflang 있음 — 사이트맵에도 넣을지 SEO 관찰 후 판단.
- **존재하지 않는 심볼 404 전환**: 현재 미해석 심볼은 200+`noindex`(798 §9)로 색인만 방지. 완전한 404 전환은 후속(우선순위 낮음).

### 백로그 / 알려진 이슈
- (백로그) 🇯🇵 JP 계획 재정의(검색→종목상세 렌즈 + JP 렌즈 선계산)·JP/CN/VN/GB 렌즈 선계산 확장·프레시니스(US 패턴)·인기종목 한글 확충·이메일 인증 R4 토글 ON·운영모델(읽기전용 서브에이전트).
- 🐛 **VN 공시 요약**: 특정 기사 원문 추출 실패(502) — 데이터 소스 이슈(797로 이제 `fetch_failed`로 섹션 숨김, 거짓 "없음" 없음).
- 🐛 **JP 공시 요약**: 로컬에서 `no EDINET key` 500 — 라이브 재확인(로컬 env 누락 추정).

## 워크플로우 (역할 혼용 금지)
- **Cowork = 두뇌**: 대화·설계·문서/명령 작성 · DB는 Supabase MCP(`ccbwxcszdoyjxvckedfp`) 직접. **실행·빌드·git 안 함.**
- **Claude Code = 손**: STEP 실행·빌드·git. `cd ~/stock-terminal && claude --dangerously-skip-permissions --model sonnet`(Opus는 🔴 표시 시).
- 세션 종료 = STATE 덮어쓰기 + CHANGELOG 블록 + SYSTEM_MAP(아키텍처 변경 시) + git push.
