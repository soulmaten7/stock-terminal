<!-- 2026-08-03 -->
# 🚀 Trillion(트릴리언) — STATE (현재 상태 단일 정본)

> **이 파일 = "지금 어디까지 왔나 + 다음 뭐 할까"의 유일한 정본. 매 세션 덮어쓴다(배너 쌓기 금지).**
> 새 세션 읽는 순서: **이 STATE → `docs/REVDCF_SPEC.md`(모델 설계 정본) → `docs/SYSTEM_MAP.md`(아키텍처) → 작업별 PLAYBOOK** → 이력 = `docs/CHANGELOG.md`.
> 규칙: **현재상태=여기에만 · 이력=CHANGELOG에만 · 아키텍처=SYSTEM_MAP에만 · 모델 설계=REVDCF_SPEC에만.**

## HEAD / 배포
- HEAD = **이 커밋(STEP 881)** · 부모 `c260ee3`(STEP 880) · **push 완료 — origin/main + revdcf-preview 반영.**
- ✅ **§0 확인(880 사후, Cowork 독립 재확인)**: `route.ts:24~26` 유니버스 질의는 `verdict`·`skip_reason` 어떤 필터도 걸지 않는다 — `NO_MARGINAL_CAPEX` 50사도 유니버스에 남는다(880의 조치가 실제로 성립).
- 🔴 **STEP 881 = driver 6(자본비용) ③판정 확정(장은태 2026-08-03) — 현행 유지(업종 구성요소 조립).** T7 전 시트 셀 판독(Inputs 8셀 전부·WACC 수식·Tutorial 8 서술 전문) 완료. **5단계 분해**(도미노, T7 구조 고정): 도미노 WACC 차이(5.354%→7.19%·판정 years→over_cap)의 **절대다수가 방법이 아니라 시점**(2020 rf 0.65%→2026 rf 3.95%, 1단계에서 이미 전환) — 베타·부채비용·세율 방식은 그 위에서 ±0.3%p만 이동. 도미노 원전 WACC는 우리 515사 분포 **하위 8.3%**. 내부 점검 2건(합성스프레드 std_dev_equity=원문 정합·현금베타 재레버리지=명시적 서술 못 찾음, 확정 오류 아님) 완료. **근거 3개 + 대가(업종근사 편향 미측정) + 불리한 사실(도미노 스팟체크에서 합성스프레드가 실채권보다 높게 나옴) + 재검토조건**까지 판정표에 기록. 부수 발견: T7 원본 그대로 재현 시 GAP=**7**(기존 "8"은 T8의 살짝 다른 수치조합에서 나온 값).
- 🔴 **프로덕션 도메인 = `https://onetrillion.app`**(869부터 저장소에 커밋 · `docs/PROD_ACCESS_ANSWER2_2026-08-02.md`).
- 866~881(유니버스 조달 범위 확정·API 게이팅·문구정정·driver1 판정·driver4 판정+근거보정×2·driver5 확정(원전식 채택)·**driver6 확정(현행 유지)**·driver3 근거재정정×2) 전부 유효 — 상세 CHANGELOG (14)~(27). `app/`·`components/`·`messages/`·`data/` 로직 **diff 0**(881은 문서+`lib/revdcf/registry.ts` 문자열+`scripts/probe_881_wacc.ts`만).
- **tsc 0 · vitest 155/155**(무변화) · `revdcf_results` = 2026-08-01/02/03 각 **604**(무변경 — 크론 미실행, 아직 880의 level→marginal 전환 미반영) · `us_market_cap` = **5,887**(무변경).
- ⚠️ **Vercel = Hobby: 크론 일 1회 한도 · 하루 100 배포.**
- 🔴 **역DCF = 피처 플래그 `REVDCF_ENABLED` OFF.** 켜는 조건 = 프리뷰 육안 검증 → **장은태 명시 승인.** 프리뷰 dev = `localhost:3333`.

---

# 🔴 지금 하는 일 = **역DCF 하나를 원전 수준으로**

정본 설계 = **`docs/REVDCF_SPEC.md`** · 완성 기준 = `docs/LENS_COMPLETION_STANDARD.md` DoD 9항목 · 원본 = `data/sources/`

## 🟢 확립된 것

| | 상태 |
|---|---|
| **엔진(풀이)** | ✅ **원전 그대로** — 도미노 주당가치 **오차 0.0000** · MIFP **8=8** · 잔여가치 비중 N=5/8/25 **86.3/80.1/59.3% 소수점 일치** · T8 오라클 3케이스 일치 |
| **예측 지평** | ✅ 원전대로 **25년**(T8 `C31` LOOKUP) · over_cap=원전 `"25+"` · `explained_pct`=25년가치÷주가(C33) |
| **fade·터미널** | ✅ 계단 고정 · `NOPAT(1+i)/(WACC−i)` |
| **원전에 유니버스 없음** | ✅ **튜토리얼 전 8편** `universe`·`liquidity`·`volume`·`screening`·`market cap` **전부 0건**(08-02 재확인 · `Screen`/`portfolio`/`float` 히트는 전부 Squarespace CSS·이미지 파일명). 원전은 **단일 종목 분석서** |
| **모집단 상한(공식)** | ✅ SEC 공식 통계 확보 — CY2025 미국 소재 거래소 상장 **3,714**(비셸 3,692) · 직전 4분기 **3,600**(비셸 3,589). 원본 = `data/sources/sec/sec_reporting_issuers_20260630.xlsx` |
| **전수 계산(866)** | ✅ 컷 없이 **N=3,354** 실행 — 산출 364 / 판정불가 1,688 / 입력부족 1,302 |
| 🟢 **유니버스 = 거래소 상장(조달 범위) 확정** | ✅ **867 · 2026-08-02 장은태 승인** — 조달 범위 **N=2,857**(NYSE·NYSE American·NYSE Arca·Nasdaq·CBOE). **"컷"이 아니라 "조달 범위"**(판정 배제 규칙 아님). 근거 = 866C·866D 실측 비용(OTC 넣어도 산출 +8뿐·micro 산출률 하락) + FTSE Russell §5.6.1/§5.7.1(유일하게 이유를 서술하는 1차 출처) + 제품 구조(A-5). Russell 나머지 기준·FALR·OTC 티어는 미채택. Damodaran 반대 입장은 기각 아니라 병기(`REVDCF_SPEC` A-9 ⑤) |

🔑 **"풀이를 이해 못 한 것"이 아니다.** 재현이 소수점까지 되는데 결과가 안 나오면 **범위 문제**다(CLAUDE.md ⓪-3).

## 🔴 열려 있는 것

### 1) 유니버스 = **A층 종료** (866~867 완료 · 2026-08-02 장은태 승인 · 아래 실측 원장 보존)

🔴 **A-7의 "정식 종료(616/604)"와 다르다.** A-7은 **물려받은 시작 목록 1,000 안에서**의 종료였다(모집단 자체를 실측한 적 없음 — 08-02에 재개방된 이유). **이번 종료는 모집단을 실제로 잰 뒤의 종료**다: SEC 전수(company_tickers_exchange 10,432) → 사다리 3,354 → 거래소상장 **N=2,857**로 확정. 아래는 그 실측 과정 전체(866~866D)의 원장 — 삭제하지 않고 보존한다.

**모집단 사다리(866B 실측 · 거래소 병기)**

| 단계 | 계 | 거래소상장 | OTC | null |
|---|---|---|---|---|
| uniqueCik | 8,017 | 6,042 | 1,788 | 187 |
| 연차보고 有 | 6,529 | 5,493 | 1,015 | 21 |
| 외국(20-F/40-F) 제외 | 5,241 | 4,354 | 868 | 19 |
| 금융 SIC 6000~6999 제외 | 3,897 | 3,193 | 691 | 13 |
| 매출태그 有 = **final** | **3,354** | 2,857 | 486 | 11 |

- 🔴 **`droppedBy`는 배타적이지 않다** — `reit 200`·`spac 231`은 SIC 금융컷의 **부분집합**(추가 제외 0). 배타 합산은 `noAnnualForm 1,488 + foreign 1,288 + sicFinancial 1,344 + noRevenue 543`뿐.
- 🔴 **우리 규칙에 거래소 조항이 없다**(`REVDCF_SPEC` 제외 목록). 08-02 초안에 Cowork이 *"거래소 상장"* 을 근거 없이 삽입했다가 **866B에서 철회.** → **SEC 3,692는 우리 모집단의 상한이 아니다.**
- 🔴 **미결정**: 우리는 **거래소 기준**(SEC 통계)인가 **상장 여부 기준**(Damodaran)인가. **정한 적이 없다.**

**🔴 결정적 발견 — "OTC 산출 0"은 OTC의 성질이 아니다** (Cowork 검증 · 866B 산출물 직접 집계)

| 확인 | 결과 |
|---|---|
| final OTC 486의 시총 보유 | **0 / 486** |
| `damodaran_industry` OTCPK 2,152 중 `us_market_cap` 보유 | **8건 (0.4%)** — NYSE 94.8% · NasdaqGS 94.9% · NasdaqCM 90.4% |
| final OTC 486 중 `data/us_symbols.json`(6,766)에 있는 것 | **0개** (거래소상장은 2,838/2,857 = 99.3%) |
| Yahoo 공식 거래소 목록 | **OTC Markets Group 커버**(15분 지연 · ICE Data Services) |

→ **막힌 게 아니라 안 넣은 것이다.** 486 전원이 시총 결격이라 드라이버를 통과해도 `NO_MARKETCAP`에서 죽는다. **산출 0은 예정돼 있었다.**

**🟢 866C 실측 — "OTC를 포함하면"의 답**

| | 결과 |
|---|---|
| OTC 486 야후 조회 | 응답 477 · `marketCap` **446** · 가격만(주식수로 역산 가능) 30 · 무응답 9 |
| 시총 확보(453) 재계산 이동 | computed **8** · undecidable **125** · 여전히 부족 320 |
| 그중 원래 `NO_MARKETCAP`(135) 단독분 | computed 8 / undecidable 125 / 다른 사유로 실패 2 |
| 전수 재집계(N=3,354) | 산출 364→**372** · 산출률 10.9%→**11.1%** · GAP중앙 8→8년(불변) · ICC(860정의) 0.165→**0.177** · micro버킷 산출률 3.3%→**2.7%**(신규 OTC 대부분 micro인데 산출은 적어 희석) |

🔑 **OTC를 넣어도 (a) 산출은 8건만 늘고(364→372), 대신 (b) 판정불가가 125건 늘어 분포 구성이 바뀐다.** 시총이 없어서 "안 보였던" 종목들이 시총이 붙어도 대부분 `over_cap`/`value_destroying`/`below_one`로 떨어진다는 뜻 — OTC 소형주가 밸류에이션 극단(고평가·가치파괴)에 몰려 있다는 재료(판단 아님).

**🟢 866D 실측 — OTC는 한 덩어리가 아니다(티어 × 3분류 교차)**

| 티어 | n | 산출(a) | 산출률(a)÷N | 공시형결격 비율 | 시총 중앙값 |
|---|---|---|---|---|---|
| OTCQX(최상위) | 32 | 1 | 3.1% | 37.5% | $57.3M |
| OTCQB | 165 | 3 | 1.8% | 63.6% | $10.4M |
| PINK | 144 | 2 | 1.4% | 66.0% | $2.2M |
| OTCID(최하위·2025-07-01 신설) | 135 | 2 | 1.5% | 77.0% | $3.6M |
| UNKNOWN(무응답 등) | 10 | 0 | 0% | 70.0% | — |
| **[대조] EXCHANGE_LISTED** | 2,857 | 364 | **12.7%** | **23.5%** | $1,372.5M |

- **공시형결격 비율** = `(INSUFFICIENT_HISTORY+MISSING_TAG)÷n` — OTC Markets 공식(2024-10-14 블로그)이 주장하는 *"OTCID는 최소 정보기준만 요구, OTCQX/OTCQB의 qualitative standards 없음"* 이 우리 데이터에서 **대체로 보인다**(OTCQX 37.5% → OTCQB 63.6% → PINK 66.0% → OTCID 77.0%로 계단식 상승 · 숫자로만 확인, 해석·컷 제안 없음).
- **OTC 최상위 티어(OTCQX)조차 EXCHANGE_LISTED(23.5%)보다 공시형결격이 높고(37.5%)**, 산출률도 3.1% vs 12.7%로 4배 이상 낮다.
- **보고 구멍 2건 메움**(866C 산출 로직 자체는 정확했음 — 출력 누락만): ① `mcapSource` 분해 = `marketCap 446 + priceTimesShares 7 = 453`(`otcWithMcap`의 구성) ② `quoteReturnedButNoPrice` 1건(`WDSP` — 응답은 왔으나 marketCap도 price도 없어 기존 if/else-if에서 안 셈).

**외부 3주체 (거래소 조건 · 08-02 라이브 재확인)**

| 주체 | 규칙 | 밝힌 이유 | 규모 |
|---|---|---|---|
| New Constructs | 지수 사다리 + 3개월 평균거래량 순 · **거래소 조건 언급 0** | 🔴 **없음** | 2,748(2022-03-14) |
| Morningstar Quant | 유동성 하한만(현지통화 5,000/일·60일 중앙) | 모델 편향 완화 | ~40,000 (US 4,379) |
| **Damodaran** | **컷 없음** — 주가>0 전 상장사 · **거래소 안 물음** | **표본 편향 방지** | 48,156 |

🔴 **정정(866B)**: 우리 발췌본이 NC 제외 사유로 적었던 **"OTC"·"주식구조 복잡"은 원본에 없다.** 저장본 본문(8,112자)·라이브 페이지 양쪽 `OTC` **0건**. 유일한 제외 서술은 *"Companies with no revenue…"* 하나뿐. **`MULTI_CLASS_SHARES` 5사가 "NC와 같은 사유"라는 주장도 함께 철회.**

**결정 원칙**: 컷은 **전수 분포를 본 뒤에** 정한다. **"계산 못 함"과 "계산했는데 감춤"을 뒤섞지 않는다.**

### 2) DoD 현황 — **1·2 ✅ / 3·4·5·6·8 🔶 / 7·9 ❌**

| # | 항목 | 상태 |
|---|---|---|
| 1 | 원전 대조표 | ✅ (857·859 · 추가물 행에 `universe`·`liquidity` 반영) |
| 2 | 입력 검증 | ✅ (862 · D&A 결측 21.7%→**11.4%**) |
| 3 | 값 검증 | 🔶 **도메인 상한**(863·864 · 8곳 소진) · 재현가능 동시점 개별 대조 **0건** · 🔴 **✅ 상향은 장은태 판단** |
| 4 | 컷·분포 | 🔶 **유지**(근거는 채웠고 ✅ 상향은 장은태 판단) — 유니버스 확정(N=2,857)·전수 분포·OTC 포함 분포·동시 결격 3칸까지 전부 실측 완료(866~867). `docs/LENS_COMPLETION_STANDARD.md` "4) 컷·분포" 신설 |
| 5 | 경계 처리 | 🔶 (적자 78사 = 원전 미상정 → "적용 밖"이 865로 지지됨) |
| 6 | 주장 정합 | 🔶 초안만 |
| 7 | 화면 일관성 | ❌ 블록 — **모델 완성이 UI보다 먼저**(🚫 창작 금지 §4) |
| 8 | 테스트 | 🔶 |
| 9 | 라이브 실측 | ❌ 블록 — 플래그 OFF |

**전수 vs 604 (두 분모 병기 · 866C 열 = OTC 시총 조달 반영)**

| | 604 | 전수 3,354(866B) | 전수 3,354(866C·OTC 반영) |
|---|---|---|---|
| 산출 (a) | 177 | 364 | **372** |
| 산출률 (a)÷N | 29.3% | 10.9% | **11.1%** |
| 산출률 (a)÷((a)+(b)) | 34.4% | 17.7% | — |
| GAP 중앙 (p25/p75) | 11년 (6/17) | 8년 (4/15) | **8년**(4/14.25·불변) |
| 업종 ICC(860 정의·5사+) | 0.198 (기록 0.195) | 0.165 (31업종 296사) | **0.177** |
| micro 버킷 산출률(N기준) | — | 3.3%(n884) | **2.7%**(OTC 편입으로 분모↑·희석) |

시총 구간별 산출률(866B 기준) **(a)÷N** — mega 44.2%(n43) · large 28.1%(n537) · mid 17.4%(n671) · small 6.8%(n701) · micro 3.3%(n884) · **no-mcap n518(산출 0)**.
🔴 ICC는 **정의 차이**였다 — 0.195 vs 0.267은 불일치가 아니라 **최소 업종 크기 5 필터 + 업종 출처**의 차이(866B 규명).

### 3) 미측정 (🔴 "완료" 선언 금지 대상 · 유니버스 확정과 무관하게 남음)
- ~~OTC 486의 시총 조달 가능 여부~~ → ✅ **866C 해결**: 야후가 446/486(91.8%) 커버. 조달만 안 됐던 것 확인
- ~~`insufficientCause` 순서 의존 재분류~~ → ✅ **866C 해결**: 동시 결격 3칸 실측 — **우리 조달만 113 / 회사 공시만 889 / 둘 다 167**(1,302 전원 재평가). 866B의 `246/1,001/55`는 `supersededBy`로 정정 표시(파일은 안 지움)
- ~~SEC 전체 제출사 실값~~ → ✅ **해결(08-02)**: SEC 공식 통계로 대체. `frames` 4,998은 **하한이 아니라 다른 모집단**(OTC·외국·ADR 포함)이었음 — 정정 완료
- 🔴 **Russell 3기준(최저주가 $1.00·최저시총 $30M·float 5%) 미측정** — 우리 거래소상장 2,857(또는 전수 3,354)에서 몇 개를 자르는지 안 쟀다. **재기 전에는 채택 금지**(867 §금지 5)
- 🔴 **OTC 티어(OTCQX/QB/PINK/OTCID) 라벨 신뢰도 미규명** — 866D에서 **PINK 공시형결격 66.0% < OTCID 77.0%로 역전**(직관과 다름). 야후 `fullExchangeName` 라벨이 최신인지, PINK 내부에 하위구간이 섞여 있는지 확인 안 됨. 유니버스는 이미 거래소 상장으로 확정돼(867) OTC 자체가 조달 범위 밖이라 채택 여부와는 무관 — 순수 기록용
- 🔴 **ICC 정본 미선택** — 원전 관찰 대조가 604 기준(0.198)과 전수 기준(0.165)에서 다르게 나오는데 어느 쪽을 정본으로 볼지 미정. 0.195(기록값)↔0.198(재계산)의 0.003 차이 원인도 추정 없이 사실만 기록된 상태(866B)
- 🔴 **skipped 89사(15%) 전수 규명** — 회수 가능 vs 원리적 불가

## 🔴 세션 중 시정한 것 (2026-08-02(10))
- **Cowork 3중 규칙 위반 1건** — 866 산출물을 검증하며 **판정을 내리면서 ①의 B·C 축을 건너뛰었다.** *"외부 축이 필요 없다고 판단했다"* 고 적었는데, `CLAUDE.md:62-64`는 **"모든 판단 전 강제 · 못 거친 항목은 못 했다고 명시"** 이지 면제 조항이 아니다. **08-02(2) 위반과 같은 모양.** 장은태 지적 후 축을 채우자 **판정 1건이 뒤집혔다**(아래).
- 🔴 **Cowork이 정본에 근거 없는 조건을 삽입** — `REVDCF_SPEC`에 *"거래소 상장"* 을 써 넣고, 다음 턴에 **그 조건으로 866을 "결함"이라 판정**했다. 자기가 만든 기준으로 자기가 채점. **866은 규칙대로 돌았고 3,354는 유효하다.**
- **866B 정정 5건** 반영: 거래소 조건 철회 · NC 제외 사유 2건 철회 · `droppedBy` 비배타 경고 · 버킷 분모 통일 · ICC 정의 차이.
- **Claude Code 자체 발견 2건**: 866에서 `산출 515`(=(a)+(b))와 `(a) 177`을 혼동한 대조 로직 · 866B에서 cheerio `.text()`가 `<script>` 본문까지 먹어 23,903자로 나온 것(→8,112자 정정).
- **866C 완료**: OTC 486 야후 시총 조달 실측(446/486 확보) + 시총 반영 재계산(산출 364→372) + `insufficient` 1,302 전원 동시 결격 3칸 재분류(우리조달만 113 / 회사공시만 889 / 둘다 167 — 866B `246/1,001/55`를 `supersededBy`로 정정 표시). `us_market_cap`(5,886)·`revdcf_results`(604×3)·`data/us_symbols.json`·`lib/lensPrecompute.ts` 전부 실행 전후 무변경 확인.
- **866D 완료**: OTC 티어(OTCQX/OTCQB/PINK/OTCID) × 3분류 교차표 — 공시형결격 비율이 티어 순서대로 계단식(37.5%→77.0%, 대조군 EXCHANGE_LISTED 23.5%)이라 "티어=공시수준" 주장이 데이터에서 확인됨. Cowork 설계 누락(866C 명령서가 `byExchangeField`를 분포만 요구·교차 안 시킴)과 Claude Code 보고 구멍 2건(`mcapSource` 미출력·`quoteReturnedButNoPrice` if/else-if 누락 1건) 함께 메움. 신규 파일 없이 `scripts/probe_866c_otc_supply.ts`에 단계만 추가.
- **867 완료(문서 전용)**: 유니버스 조달 범위(거래소 상장 N=2,857) 확정을 `REVDCF_SPEC`·`LENS_COMPLETION_STANDARD`·STATE·CHANGELOG에 기재 — "컷"이 아니라 "조달 범위"로 표현 고정(FALR 전례 재발 방지). **투명 공개 3건**: ① `REVDCF_SPEC` A-9에 새 절을 "④"로 넣으라는 지시였으나 기존 미실행 "④ 다음(STEP 866 골자)" 스텁과 충돌 → 그 ④는 완료 처리(취소선)하고 신규 내용은 **"⑤"**로 삽입(이후 인용 전부 A-9 ⑤로 통일). ② `lib/revdcf/registry.ts`의 `OUR_ADDITIONS.status`는 여전히 문자열 `"재개방"`으로 남음 — 코드 동기화는 `lib/**` 수정이 금지된 이 STEP 범위 밖(후속 STEP 몫). ③ STEP 전제("FTSE Russell 1차 문서 확보 후")와 달리 `data/sources/`에 FTSE Russell 문서가 실제로 저장돼 있지 않음(검색 결과 0건) — CLAUDE.md 원칙 ⓪(원본 저장) 위반 상태, 이 STEP은 문서 전용이라 직접 조치 안 하고 장은태에 보고.
- **870 완료(문서 전용)**: 장은태 지적("원전원문 그대로 하기로 했어?")으로 866~869가 원전 구현이 아닌 곳에 쓰였음이 드러남 — 원인은 `STATE.md`가 DoD 순번을 작업 순서로 써서 원전 대조표의 "차이 9행"이 안 보였던 것. `LENS_COMPLETION_STANDARD.md`에 차이 9행 진행표 신설·`CLAUDE.md`에 "DoD≠작업순서" 명문화(6번)·`[3중 점검]` 블록 강제를 점검 자체는 유지하되 출력만 완화.
- **871 완료(측정 전용)**: 차이 9행의 1행(driver1·매출성장률) 실측. 착수 전 §0 정정 2건 발견·검증: ① 847의 "63.3%"는 60표본·8-K 언어감지 근사였지 커버리지가 아님(515 전수 재측정으로 재확인: 언어존재 58.8%·**금액추출 30.3%**). ② 원전이 지목한 5번째 소스(Yahoo Finance)를 우리가 이미 보유 중이었는데 `registry.ts`가 "FMP 미보유"만 적어 검토가 안 돼 있었음 → 실측 결과 **야후 애널리스트 컨센서스(0y) 커버리지 100%(515/515)**로 8-K보다 훨씬 높음. 부수 발견 2건: T8.xlsx는 튜토리얼 서술의 저(3%)/고(11%) 시나리오가 계산 스프레드시트엔 없고 **`Inputs!C6=0.07` 단일값만** 배선돼 있음(범위는 텍스트 서술뿐) · registry.ts `operatingMargin`(driver2) divergence 필드가 non-null인데 607행 "동일 8행"에 있는 등 registry.ts의 divergence non-null 개수(10)와 문서의 "차이 9행" 구성이 다름(모집단·데이터출처·검증사례는 registry.ts INPUTS에 아예 없음) — 정본 판단 없이 기록만. 성장률 교체 재실행 결과: GAP p50 11→9년·verdict 197/515(38.3%) 판정버킷 변경·부호반전 55사·저고시나리오 근사 513/515 가능. **③판정은 하지 않음.** 별건: 측정 중 배경 프로세스 하나가 야후 API 크럼 협상에서 무기한 대기(첫 시도, kill 후 8초 타임아웃 추가해 재실행 성공)·`revdcf_results` 무변경 확인 중 한 번 396행으로 보인 스냅샷은 프로덕션 크론 write-in-flight를 잡은 것으로 재조회 후 604×3 정상 확인(내 스크립트는 쓰기 0건).
- **872 완료(측정+원본저장+문서)**: driver1 ③판정의 빠진 근거 2개를 메움. ① CKL 2003(NBER w8282) 원본을 처음으로 `data/sources/academic/`에 저장하고 인용 2건(지속성·애널리스트 과대낙관)을 본문 문자열로 직접 검증. ② "둘째 안"(기준=과거CAGR 유지+범위=야후low/high) 자기정합성 측정 — `probe_871_rows.json` 재사용(재조회 없음), 현행 기준값이 그 범위 안에 드는 경우 **102/513=19.9%뿐**(부호반전 55사는 0/55). 🔴 **부수 발견(원본 대조로만 나온 것)**: `REVDCF_SPEC.md` §B-4의 기존 CKL 인용("우연 수준·2년25%·3년12.5%·4년6.3%")이 **논문의 "독립이라면 기대되는" 이론값(0.5ⁿ)을 실측치로 오인용**한 것이었다 — 원문 Table 3의 실제 결론은 **매출 성장 5년지속 6.3%(실측) vs 3.1%(기대)=약 2배로 오히려 지속성 존재**(이익 성장만 기대치와 같아 지속성 없음). 원본 미저장 상태에서 작성된 인용이었기에 이번에 원본을 열고서야 드러남 — 정정 완료. Cowork 권고(교체 안 함) 근거 ⓐ~ⓔ를 진행표 1행 각주로 부착. **③판정은 여전히 대기 — 승인은 장은태.**
- **873 완료(문서 전용)**: driver1 ③판정 확정(현행 유지·장은태 승인) — 진행표 각주를 확정 문안으로 교체, `REVDCF_SPEC.md`에 최종화 섹션 신설. `LENS_DEV_PLAYBOOK.md` #72 신규(하루 4건 인용오류 동일패턴 — NC제외사유·frames4998·847 63.3%·CKL2003 전부 "원본 열자 뒤집힘").
- **874 완료(측정 전용)**: 차이 3·4행(driver4·5) 동시 실측(T4·T5가 같은 증분+5년누적 구조라 함께 진행). §1 원본 재확인: T4 `Working Capital Analysis!I31`=0.501%·T5 `Cash Flow Method!I20`=11.6% 둘 다 정확 재현(별도 재구현 없이 원본 셀 자체가 산출). §2 driver4: A안(원전세부태그·필요현금매출×2%+AR+Inv+OtherCA−AP−Accrued) 커버리지 **65/515(12.6%)뿐**(AR62·Inv156·OtherCA254·AP78·**Accrued284 최대병목**) · B안(집계근사) 99.8%. §3 결과변화: A안 GAP p50 11→15(n20) · B안 11→10(n169) · driver5 marginal 11→10(n128, DB 실측과 515/515 정확 일치 확인 — level0.193/marginal0.272·음수0/101·\|>1\|71/133·계산불가50). 🔴 **정의 발견**: "years 유출"을 판정불가(null)까지 이탈로 셀지 여부에 따라 41(비교가능 기준) 또는 57(§0 인용치와 일치)로 갈림 — 둘 다 병기, 하나만 고르지 않음(873의 인용규율 교훈을 바로 적용). 🔴 **신규 발견 2건**: T4도 T5처럼 책(3.2%)≠스프레드시트(0.5%) 괴리(B32 각주) · T5는 계산11.6%/책10.0%(B52, page92 미보유)/T8입력15%의 **세 값** 공존. `registry.ts`의 "가설" 표현이 `REVDCF_SPEC.md` 동일 문구에서도 발견돼 "원전에 명시된 설계"로 정정(registry.ts 자체는 `lib/**` 금지라 미동기화 — 후속 STEP 몫). `docs/PRIMARY_SOURCE_MAP.md`(장은태/Cowork이 미리 준비해 둔 T3~T7 원본 직독본, untracked 상태였음) git 추가. **③판정은 하지 않음 — 장은태 몫.**
- **875 완료(측정+판정+문서)**: §1 도미노 앵커(874가 "원본 셀이 값을 낸다"만 확인한 것과 달리 **우리 공식을 도미노 입력으로 직접 돌림**) — driver4 A안(874 코드 그대로·AP+Accrued태그 2종)=**4.219%**로 원전 0.501% 재현 실패(원전 4항목 전부 쓰면 0.501% 정확 일치 — 태그가 도미노의 "Advertising fund liabilities"·"Other accrued liabilities" 2개를 못 잡는 게 원인, 공식 오류 아님) · B안은 `T4.xlsx` `Inputs`에 진짜 현금·이자부부채·집계잔액 데이터 자체가 없어 테스트 원천 불가 · driver5 marginal=**11.617%**로 원전 11.6% 정확 일치 · level은 T4·T5·T8 어디에도 PP&E 데이터 없어 테스트 불가. §2 driver4 ③판정 확정(현행 유지·장은태 승인, 근거=다모다란 실무권고+원전 불안정성 문헌+집계가 세부보다 낫다는 근거+원전식 전환해도 결과 안 크게 안 바뀜, 앵커결과는 이 결정을 안 뒤집음). §3 driver5는 "852 확정에 근거 없다"로 강등 — level은 원전·다모다란 둘 다 근거 없고(자본집약도≠재투자율), marginal은 원전과 검증됐으나 계산불가 9.7%·극단값 25.8%가 남아 바로 채택도 어려움 → 제3방식 미측정 상태로 재개방. `PRIMARY_SOURCE_MAP.md`에 §6(다모다란)·§7(앵커) 추가(기존 내용 보존) + 요약표 갱신. `LENS_DEV_PLAYBOOK.md` #74 신규(번호 #73 결번 — STEP 지시 그대로 #74 사용, 투명 공개).
- **876 완료(측정+문서·판정 불변)**: 875의 도미노 앵커가 874 A안(AP+Accrued 2종)의 결함(도미노 4.219% vs 원전 0.501%, 8배)을 드러냈으므로, 그 A안으로 잰 "커버리지 12.6%"를 driver4 판정 근거 3번에서 썼던 것이 무효임을 인정하고 재측정. §1: 무이자 유동부채성 태그를 미리 정하지 않고 515사 companyfacts에서 전수 스캔(36종 후보 확보) → 이자부·리스·총계·중단영업·이연수익 제외 → 빈도 상위 6종 채택 → 커버리지 **12.6%→29.5%**로 개선(여전히 B안 99.8%엔 크게 못 미침). 🔑 **도미노(DPZ)가 실제로 우리 515사 유니버스에 있다는 사실을 활용**(CIK 1286681) — 그 실제 2019 XBRL 대조: AP·`OtherAccruedLiabilitiesCurrent`는 T4 값과 정확 일치, "Accrued expenses"·"Advertising fund liabilities"는 DPZ의 어떤 현대 태그와도 불일치(`AccruedLiabilitiesCurrent` 자체가 DPZ에서 2012년 이후 미사용) — 태그 매핑을 아무리 개선해도 못 메우는, 원전 세부분류가 현대 XBRL에 보존 안 된 구조적 간극임을 확인. §2: 진행표 3행 각주의 근거 3번을 "무효" 처리하고 재측정 결과를 3′로 병기, **③칸(✅ 현행 유지)은 손대지 않음** — 근거 1·2·4가 판정을 지탱. §3: 플레이북 결번 #73(870의 진행표가 registry.ts와 어긋나 있던 문제 배경 기록)과 신규 #75(앵커 없이 잰 커버리지는 방법이 아니라 버그의 커버리지) 추가.
- **877 완료(문서 전용·판정 불변)**: `T7.xlsx` `Inputs`·`WACC` 시트 직접 개봉 — `Inputs!C10`(법인세율) = **0.165**, 즉 T6 현금세율·T8 `Inputs!C15`와 동일값. **원전은 WACC 세후부채비용에도 현금세율을 쓴다** — `Tutorial 8` B26의 "marginal tax rate" 서술은 실제 셀 값과 다르다(오늘 두 번째 서술≠셀 발견). 진행표 2행(driver3)에 처음으로 ①②(847 기존 실측 재인용)+③(✅ 현행 유지·847 기채택 확인) 채움 + 재정정 각주. `REVDCF_SPEC.md`의 "현금세율=이중계산이라 금지" 서술 2곳(driver3 확정 섹션·§B-3 원전차이 표) 정정 — T6는 unlevered라 이중계산 구조가 아니고, 실제 근거는 다모다란의 "영구구간=한계세율". driver4는 진행표 근거 3′을 3″로 교체(A안 원리적 불가로 재해석 — "더 줄일 수 있는 갭" 주장 철회). driver6(자본비용) 진행표에 처음으로 원전 대조 기록(베타=1·YTM=0.04546·도미노 WACC=0.05354 — 미판정). `PRIMARY_SOURCE_MAP.md` §1에 재정정 섹션 추가(기존 절 삭제 없이 취소선+정정). `LENS_DEV_PLAYBOOK` #76 신규 — 오늘 같은 유형의 오류가 네 번(T6·T7·T5·T4) 났고 전부 "서술을 읽고 셀 값을 안 본 것"이었다는 교훈.

---

## ▶ 다음 — 🔴 장은태 지시 후에만

0. 🔑 **지금 하는 일 = 원전 그대로 구현**(CLAUDE.md 🚫 창작 금지). 진행 기준은 **DoD 순번이 아니라 원전 대조표의 차이 9행**이다. 🔴 **DoD 9항목은 "완성 판정 기준"이지 작업 순서가 아니다.** 섞지 말 것. 대조표 정본 = `docs/LENS_COMPLETION_STANDARD.md` §1(행 수 22 = 동일 8 + 값만차이 1 + **차이 9** + 우리 추가물 4)

1. 🔴 **차이 9행을 하나씩 판정한다** — driver1·driver3·driver4·driver5·driver6·인플레·모집단·데이터출처·검증사례. 각 행마다 ① 원전 절차로 바꾸면 결과(GAP 분포·verdict)가 얼마나 달라지나 ② 커버리지를 얼마나 잃나 ③ 그래도 원전을 택할 것인가를 실측해 장은태에게 올린다 → **판정 = 장은태**. 🔴 **한 행씩(또는 같은 원전구조인 두 행씩·874처럼). 다음 행 제안 금지.**

2. ✅ **driver 1(매출 성장률) = 판정 완료(현행 유지·2026-08-02 승인)** — 근거·재검토조건 = `LENS_COMPLETION_STANDARD.md` 진행표 1행. 🔴 끝점2개 CAGR 추정기 자체는 미승인(§10 #42).

3. ✅ **driver 3(세율) = 판정 완료(현행 유지·847 기채택)** — 이번에 진행표 2행에 처음 반영. 근거는 다모다란의 "영구구간=한계세율"(847). 🔴 **877 재정정**: 원전은 NOPAT뿐 아니라 WACC 세후부채비용에도 현금세율(16.5%)을 쓴다(`T7 Inputs!C10=0.165` 직접 확인) — 구조는 같고 값만 다르다. **세율 16.5%↔25.63%의 순효과 미측정**(NOPAT·WACC 반대방향·§10 #50).

4. ✅ **driver 4(운전자본) = 판정 완료(현행 유지·2026-08-03 승인)** — 근거·재검토조건 = `LENS_COMPLETION_STANDARD.md` 진행표 3행(다모다란 실무근거+원전 불안정성 문헌+도미노 앵커로 A안 태그한계 확인). 🔴 남는 사실: 단기차입금이 운전자본에 섞임(크기 미측정·§10 #46). **877**: A안은 "커버리지가 낮은 게 아니라 원리적으로 불가능"(원전 세부분류가 현대 XBRL에 없음)으로 근거 재해석 — 잔여 갭은 줄일 수 있는 갭이 아님.

5. ✅ **driver 5(고정자본) = 판정 완료(원전식 marginal 채택·2026-08-03 장은태 승인·880)** — 852의 "확정"(level)이 875에서 근거부재로 무효화된 뒤, 6안(level·marginal·A·B·C×3·D) 전부 실측해 marginal만 원전 앵커를 통과함을 확인하고 채택. 근거·대가·재검토조건 = `LENS_COMPLETION_STANDARD.md` 진행표 4행. **대가(현행 유지 아님 — 모델이 실제로 바뀜)**: 커버리지 515→465/515(90.3%)·음수 0→101·극단값 133·판정 65사 변경. 🔴 재검토 조건: 인수>0·음수재투자율 있는 원전급 완전 사례 확보 시(§10 #54).

6. ✅ **driver 6(자본비용) = 판정 완료(현행 유지·2026-08-03 장은태 승인·881)** — T7 전 시트 재개봉+5단계 분해로 도미노 WACC 차이(5.35%→7.19%)의 절대다수가 방법이 아니라 시점(rf/ERP 2020→2026)임을 확인, 회사별 벤더베타·실채권YTM은 자동화 규모에서 조달 불가라 채택. 근거·대가·불리한사실·재검토조건 = `LENS_COMPLETION_STANDARD.md` 진행표 5행(§10 #51 해소).

7. 🔴 **DoD 4·5·6·8은 차이 9행이 정리된 뒤에 판정한다.** 특히 DoD 4(컷·분포)는 원전에 없는 우리 추가물 층이라 원전 구현보다 먼저 올 수 없다.

8. 🔴 **DoD 3 판정**(도메인 상한 ✅ 여부) = 장은태 결정 대기(독립 항목)

9. 🅿️ **인프라 미확정**(모델과 무관·장은태만 가능한 것 포함): 브라우저 육안 2단계 미실행 · Vercel 플랜(`vercel.json` 크론 9개 vs STATE "Hobby" 모순) · `/api/revdcf` 외부 호출 이력(Vercel 로그 403) · `toms-projects` 스코프 접근 권한 · FTSE Russell 1차 문서 원본 미저장(rule ⓪ 갭) · `lib/revdcf/registry.ts`의 `OUR_ADDITIONS.status`·driver4 "가설"표현 코드 동기화(874·875)

10. 🅿️ **866~867 잔여 미측정**(원전 구현과 무관·재개는 장은태 지시 후): Russell 3기준 측정 여부 · OTC 티어 라벨 신뢰도 · ICC 정본 선택 · skipped 전수 규명

- 🅿️ 보류: 항목 7·9(노출) · 베타 · 국가탭 확대 · 7렌즈 깊이 확장 — **모델 완성 전 재개 금지**

---

## 🅿️ 배경 (역DCF 밖 · 변경 없음) · 정체성 · 워크플로우
- **활성 시장 = KR·US뿐**(799·JP/CN(HK)/VN/GB 파킹 — `lib/activeMarkets.ts`). **7렌즈 = 유지·수정만**(812~830 완료·깊이확장은 역DCF 완성 뒤). 판정 = 분포 유도 컷(`lens_cuts` p30/p70·RSI 30/70·F-Score 3/7만 고정). `us_market_cap`(5,887)은 `data/us_symbols.json`(6,766) 기준. 미착수: JP/CN/VN/GB 렌즈 선계산·대화형 LLM·푸시/앱스토어.
- 사업자 **원트릴리언**(대표 장은태·210-39-33812) · **"종목을 보는 눈을, 누구에게나."**(무기·직시·자립·멍거 톤·예측·추천 안 함) · 5면·i18n ko·en 패리티.
- **Cowork=두뇌**(설계·문서·Supabase MCP·실행 안 함) / **Claude Code=손**(STEP 실행·빌드·git). 세션 종료 = STATE 덮어쓰기+CHANGELOG+push.
