<!-- 2026-08-02 -->
# 🚀 Trillion(트릴리언) — STATE (현재 상태 단일 정본)

> **이 파일 = "지금 어디까지 왔나 + 다음 뭐 할까"의 유일한 정본. 매 세션 덮어쓴다(배너 쌓기 금지).**
> 새 세션 읽는 순서: **이 STATE → `docs/REVDCF_SPEC.md`(모델 설계 정본) → `docs/SYSTEM_MAP.md`(아키텍처) → 작업별 PLAYBOOK** → 이력 = `docs/CHANGELOG.md`.
> 규칙: **현재상태=여기에만 · 이력=CHANGELOG에만 · 아키텍처=SYSTEM_MAP에만 · 모델 설계=REVDCF_SPEC에만.**

## HEAD / 배포
- 코드 HEAD **`9c5185b`**(STEP 865) · 그 위에 **문서 커밋만** 추가됨 · **tsc 0 · vitest 151/151** · 프로덕션 **404 유지**(플래그 OFF). ⚠️ **Vercel = Hobby: 크론 일 1회 한도 · 하루 100 배포.**
- 🔴 **역DCF = 피처 플래그 `REVDCF_ENABLED` OFF.** 켜는 조건 = 프리뷰 육안 검증 → **장은태 명시 승인.** 프리뷰 dev = `localhost:3333`.

---

# 🔴 지금 하는 일 = **역DCF 하나를 원전 수준으로**

정본 설계 = **`docs/REVDCF_SPEC.md`** · 완성 기준 = `docs/LENS_COMPLETION_STANDARD.md` DoD 9항목 · 원본 = `data/sources/`

## 🟢 확립된 것

| | 상태 |
|---|---|
| **엔진(풀이)** | ✅ **원전 그대로** — 도미노 주당가치 **오차 0.0000** · MIFP **8=8** · 잔여가치 비중 N=5/8/25 **86.3/80.1/59.3% 소수점 일치** · T8 오라클 3케이스 일치 |
| **예측 지평** | ✅ 원전대로 **25년**(T8 `C31` LOOKUP) · over_cap=원전 `"25+"` · `explained_pct`=25년가치÷주가(C33) |
| **fade·터미널** | ✅ 계단 고정 · `NOPAT(1+i)/(WACC−i)` |
| **분포 대조** | ✅ 원전 관찰 3개 재현(중앙 11년·업종 군집 ICC 0.195/F 2.99·잔여가치 중앙 79.6%) · 학술 **MICAP** 범위(총 10~15/개별 0~2~20+)와 정합 |

🔑 **"풀이를 이해 못 한 것"이 아니다.** 재현이 소수점까지 되는데 결과가 안 나오면 **범위 문제**다(CLAUDE.md ⓪-3).

## 🔴 열려 있는 것

### 1) 유니버스 = **A층 재개방** (2026-08-02 · 최우선)
- 🔴 **A-4 "유동성 FALR ≥ 0.75 확정" = 확정 취소.** 전 저장소 grep(08-02): 계산 경로 **0건**(engine·drivers·compute·배치·크론·API) · 유일 히트는 프로브 주석 *"구현 안 함, 오는지만"*. 문서만 확정이고 파이프라인은 컷 없이 604를 돌려왔다. **851 "표시 25년 컷" 사고의 2회차 재발.**
- 🔴 **원전에 유니버스 개념이 없다**(튜토리얼 02·08 개봉 · `liquidity`/`volume`/`universe`/`screening` 0건). 원전은 **단일 종목 분석서**. → 유니버스·유동성은 **"우리 추가물"** 행이다.
- 🔴 **외부 3주체가 전부 다르다**: NC=지수 사다리(**이유 서술 없음**·2,748) / Morningstar Quant=**유동성 하한만**(현지통화 5,000/일·"편향 완화"·**US 4,379**) / **Damodaran=컷 없음**(48,156·"표본 편향"·*"제외 말고 남기되 결측 아닌 기업만 값 보고"*). 발췌 = `data/sources/text/EXTERNAL_UNIVERSE_QUOTES.md`.
- 🔴 **A-7 "정식 종료 616/604" = 격하** — "물려받은 목록 1,000 안에서의 종료"일 뿐.
- **결정 원칙**: 컷은 **전수 분포를 본 뒤에** 정한다. "계산 못 함"과 "계산했는데 감춤"을 뒤섞지 않는다.

### 2) DoD 현황 — **1·2 ✅ / 3(도메인 상한)·4·5·6·8 🔶 / 7·9 ❌**
| # | 항목 | 상태 |
|---|---|---|
| 1 | 원전 대조표 | ✅ (857·859 · 차이 9행 · 🔴 추가물 행에 `universe`·`liquidity` 2행 보강 필요) |
| 2 | 입력 검증 | ✅ (862 · D&A 결측 21.7%→**11.4%** · 무차입 37/진짜결측 24 분리) |
| 3 | 값 검증 | 🔶 **도메인 상한**(863·864 · **8곳 소진**) · 재현가능 동시점 개별 대조 **0건** · 🔴 **✅ 상향은 장은태 판단** |
| 4 | 컷·분포 | 🔶 — 🔴 **1)이 바뀌면 근거 숫자가 전부 다시 나온다** |
| 5 | 경계 처리 | 🔶 (적자 78사 = 원전 미상정 → "적용 밖"이 865로 지지됨) |
| 6 | 주장 정합 | 🔶 초안만 |
| 7 | 화면 일관성 | ❌ 블록 — **모델 완성이 UI보다 먼저**(🚫 창작 금지 §4) |
| 8 | 테스트 | 🔶 |
| 9 | 라이브 실측 | ❌ 블록 — 플래그 OFF |

### 3) 미측정 (🔴 "완료" 선언 금지 대상)
- **SEC 전체 제출사 수의 실값** — 현재 **4,998(CY2024)은 `frames` 기반이라 하한**(frames 과소계상). companyfacts/벌크로 실값 확정 필요. 외부 비교값 = Morningstar Quant 미국 **4,379**
  - 🔴 이 항목은 본 세션 초안에서 *"미측정"*이라 잘못 적었다가 **3차 검수에서 자체 정정**(841 실측 기록이 §11 원장에 있었다 — 기록을 안 열고 쓴 ⓪-3 위반)
- **컷 없이 전수 계산 시 산출률·분포 변화**
- **skipped 89사(15%) 전수 규명** — 회수 가능 vs 원리적 불가
- ~~원본 3건 미저장~~ → ✅ **전부 확보(08-02)**: `external/morningstar_quant_methodology_2024-12-02.pdf`(813KB · 지정 URL 403 → 공식 대체 호스트) · `text/damodaran_data_update_1_2026.html` · `text/newconstructs_gap.html`(옛 404 → 현행 URL 확인)

## 🔴 세션 중 시정한 것 (2026-08-02(9) · 코드 0)
- **CLAUDE.md ⓪-3 신설 2건**: ① 항목별 **5단계 순서**(원본→갈리는 지점→DB→명령어→판정 후 멈춤) + *"우리 계산이 이상하다" vs "원전에 그 케이스가 없다"* 구분 ② **"검색은 결론보다 먼저다"** — 사후 검색 무효 · `[3중 점검]` **✅는 실제 한 것만**(거짓 ✅는 안 붙인 것보다 위험) · 출처가 이유를 안 밝히면 "이유 서술 없음" · **한 곳의 관행 ≠ 업계 표준**
- **위반 실례**: Cowork이 A-0만 보고 2연속 권고 + `제3자 ✅` 거짓 표기 → 장은태 지적 후 실검색하니 **권고가 뒤집힘**. 로그 = `LENS_DEV_PLAYBOOK` #43·#44
- 🔴 **3중 검수에서 추가 적발 — 문서 4곳이 구현과 반대**: 터미널을 `NOPAT÷WACC`(NC·CAP 방식)라 적고 있었으나 **실제는 `NOPAT(1+i)/(WACC−i)`**(원전 T8·`engine.ts:80`). CLAUDE.md 최상위·용어집·§6·§12 전부 정정. 함께 §12 `N=1…100`→**25**(859 반영 누락)·대조표 20행/추가물2 → **22행/추가물4**. 🔑 **문서끼리만 맞추면 못 잡는다 — 코드를 직접 열어서 나왔다.**

---

## ▶ 다음 — 🔴 **장은태 지시 후에만**
1. **STEP 866(미작성)**: SEC 전체 제출사 실측 → **컷 없이 전수 계산** → 3분류(**산출됨 / 판정 불가(라벨) / 입력 부족(사유별)**) → 604 대비 분포 변화. 🔴 화면·플래그 무변화 · 채택 제안 금지. 골자 = `REVDCF_SPEC` A-9 ④
2. 그다음 DoD 4 → 5 → 6 → 8 (**한 항목씩 · 다음 항목 제안 금지**)
3. 🔴 **DoD 3 판정**(도메인 상한을 ✅로 볼지) = 장은태 결정 대기
- 🅿️ 보류: 항목 7·9(노출) · 베타 · 국가탭 확대 · 7렌즈 깊이 확장 — **모델 완성 전 재개 금지**

---

## 🅿️ 배경 (역DCF 밖 · 변경 없음)
- **활성 시장 = KR·US뿐**(799). JP·CN(HK)·VN·GB 파킹(데이터·크론·라우트 보존 · `lib/activeMarkets.ts` · 복원 = `PARKED_FIELD_SURFACES.md` §7).
- **7렌즈 = 유지·수정만.** 3중검증(812~819)·주장 정합(820~827)·베타차단 보안(828)·운영 정직성(829)·베타 전 마감(830) 완료. 🔴 **깊이 확장은 역DCF 완성 뒤.**
- **판정 = 분포 유도 컷**(`lens_cuts` 시장별 p30/p70). RSI 30/70·F-Score 3/7만 고정.
- 미착수: JP/CN/VN/GB 렌즈 선계산 · 대화형 LLM · 푸시/앱스토어.

## 정체성 (권위 = `docs/BRAND_IDENTITY.md`)
- 사업자 **원트릴리언** · 대표 장은태 · 210-39-33812 · contact@onetrillion.app
- **"종목을 보는 눈을, 누구에게나."** — 3기둥: 무기·직시·자립. 멍거 톤. **예측·추천 안 함 · 거래 X.**
- 필드 = **5면**(오늘·탐색·종목상세·관심·마이) · i18n ko·en 전면 패리티.

## 워크플로우 (역할 혼용 금지)
- **Cowork = 두뇌**: 대화·설계·문서/명령 작성 · DB는 Supabase MCP(`ccbwxcszdoyjxvckedfp`) 직접. **실행·빌드·git 안 함.**
- **Claude Code = 손**: STEP 실행·빌드·git. `cd ~/stock-terminal && claude --dangerously-skip-permissions --model sonnet`(Opus는 🔴 표시 시).
- 세션 종료 = STATE 덮어쓰기 + CHANGELOG 블록 + SYSTEM_MAP(아키텍처 변경 시) + git push.
