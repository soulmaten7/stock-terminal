<!-- 2026-07-28 -->
# 🚀 Trillion(트릴리언) — STATE (현재 상태 단일 정본)

> **이 파일 = "지금 어디까지 왔나 + 다음 뭐 할까"의 유일한 정본. 매 세션 덮어쓴다(배너 쌓기 금지).**
> 새 세션 읽는 순서: **이 STATE → `docs/SYSTEM_MAP.md`(아키텍처) → 작업별 PLAYBOOK** → 이력 = `docs/CHANGELOG.md`.
> 규칙: **현재상태=여기에만 · 이력=CHANGELOG에만 · 아키텍처=SYSTEM_MAP에만.** (CLAUDE.md 문서 규칙)

## HEAD / 배포
- 코드 HEAD **`cdba316`**(STEP 806 · 상대 컷 문구 정합) · origin/main 동기화 · 트리 클린 · **배포 ✓ onetrillion.app** · CI 그린 · **tsc 0 · vitest 101/101**. ⚠️ **Vercel = Hobby: 크론 일 1회 한도(위반 시 배포 거부) · 하루 100 배포.** 크론 **8개**(§SYSTEM_MAP §4 — 806 §6에서 jp-disclosures 추가 중지).
- **베타 전 3중 검수 마감(793~798) → 렌즈 판정 엔진 정합(799~806)**: 표면 축소(799) → 기반 버그 4종(800) → 렌즈 기법 정의(801) → 컷 토대(802) → 입력 정확성(803) → 표시 정직성(804) → 분포 유도 컷 소비(805) → 상대 컷 문구 정합(806). 상세 = CHANGELOG 2026-07-28. **기술 블로커 0 — 베타 발송 대기.**

## 정체성 (권위 = `docs/BRAND_IDENTITY.md`)
- 사업자 **원트릴리언** · 대표 장은태 · 210-39-33812 · contact@onetrillion.app.
- 슬로건 **"종목을 보는 눈을, 누구에게나."** — 3기둥: 무기(TR-AI 렌즈)·직시·자립. 멍거 톤. **예측·추천 안 함 · 거래 X.** 미드나잇+민트 다크. 코드 식별자 `unjong-*` 유지.
- 🔥 **필드 = 5면**: **오늘(/) · 탐색(/explore) · 종목상세 · 관심 · 마이.** i18n = ko·en(전면 패리티).

## 지금 상태 (정직한 기준선)
- **🎯 활성 시장 = KR·US뿐(STEP799)**: JP·CN(HK 포함)·VN·GB는 검색·관심목록 등록·종목상세 진입·사이트맵·**`/api/lens`·`/api/brief`**(806 §6)에서 전부 차단(파킹 — 데이터·크론·라우트는 보존). 제어점 = `lib/activeMarkets.ts`의 `ACTIVE_MARKETS`·`isActiveSymbol` 하나. 복원 = `docs/PARKED_FIELD_SURFACES.md` §7.
- **🔬 렌즈 판정 = 분포 유도 컷(STEP 805~806)**: 7개 렌즈 중 5개(모멘텀·저변동·밸류·퀄리티·자산성장) verdict가 코드 상수가 아니라 **`lens_cuts` 테이블(시장별 값 분포 p30/p70)**을 읽어 판정. RSI 30/70·F-Score 3/7만 고정 표준값. **컷 없으면 판정 안 함**(state='pending'="기준 준비 중"·임의 상수 폴백 금지). 순환 의존은 **크론 2-pass**로 해소(pass1 값+직전컷 상태 → 분포서 컷 재유도 → pass2 저장값 상태 재매핑). 컷이 상대순위라 verdict 문구도 **상대 표현**("모멘텀 상위권"·"싼 편(시장 대비)")+절대 sanity 가드(12-1<0이면 "내렸지만 상위권"). 라이브(`/api/lens`)=선계산(`lens_scores`) 동일 컷. **실측: KR 저변동 "주의" 89.3%→30.0%**(하드코딩 20/40이 시장 레벨 차이를 종목 특성으로 오판하던 것 교정).
- **계산 정확성(STEP 801·803)**: Wilder RSI·12-1 모멘텀(조정종가)·GP/A·자산성장·F-Score 전부 학술 원전 정합(기초자산 분모·3년 요구·연속연도 검증). KR 입력 결함 7종 수정 = F-Score 거짓 "은행" 진술 제거·시총 야후 marketCap 우선·우선주 밸류 제외(90종목)·분할 정수배 판정 불가·저변동 최소 표본 120·krSnapshot 거래일 가드. **계산 유닛테스트 vitest 101/101**(fscore 15·저변동 5·렌즈 특성화 27·i18n 패리티 8·authRedirect 6·pending 경로 등).
- **표시 정직성(STEP 804)**: 결측을 0으로 날조 제거(`?? 0`→null·화면 "—")·데이터 나이 서빙(bas_dd·AsOfBadge 시장 로컬 타임존)·실패↔없음 구분(4곳)·토글 롤백(res.ok+연타 가드)·하이드레이션 가드(`!authLoading && !user`)·시장 URL 동기화.
- **로케일 쿠키(STEP 806 §5)**: `NEXT_LOCALE`(레거시·URL 방문마다 덮어써 en 잔류) → **`locale_choice`**(명시 선택만 심음)로 마이그레이션. proxy가 새 키만 읽고 레거시는 삭제. OAuth `post_login_locale` 왕복 불변.
- **오늘/탐색/종목상세/관심/마이**: 793~798 UX·레이아웃·정직 결측 마감 유지(PageShell 공용 셸·한 입 브리핑·렌즈 3단 공개·공시 카드 정리·이메일 브리핑 opt-in). 상세 = CHANGELOG 2026-07-27(3).
- **백그라운드(크론 8개)**: `us-perf`·`kr-perf`·`kr-etp`·`kr-lens-scores`·`lens-scores`(US 렌즈·2-pass)·`health`·`daily-brief`·`email-brief` 가동 + `jp-disclosures`는 806 §6에서 **추가 중지**(소비처 0). 중지분 라우트·데이터 보존(복원=`vercel.json` 재등록).
- **미완/파킹**: JP/CN/VN/GB 렌즈 선계산 없음(진입 시 `/api/lens` 400)·대화형 LLM 미착수·푸시/앱스토어 미착수.

## 🔑 결정 (07-22~28)
- **대화형 LLM = 3단 공개 이후 선택지**(확정 로드맵 아님). 베타 반응 보고 재논의. 지금 착수 안 함.
- **푸시 알림·앱스토어 = Resend 열람률 검증 후 게이트**. 이메일 모닝 브리핑(784)이 재방문 훅 1차 실험.
- **컷 없는 시장(JP/CN/VN/GB)은 pending이 아니라 게이트 차단**(806 §6) — 파킹 시장은 온디맨드 계산 자체를 막아 "기준 준비 중" 카드도 안 뜬다.

## ▶ 다음 (우선순위)
1. **🇺🇸 US 확장 검증**(최우선 — Cowork 재검수 후속): 미국인 시선 본감사 잔여 + 799~806의 판정 엔진 변화가 US 경로에서 정합한지(US `lens_cuts` 분포·상대 문구·pending 게이트·표시 정직성) 전수 실측. KR과 동급 DoD.
2. **🚀 클로즈드 베타 발송**(기술 블로커 0). 발송 = 사용자, 문구는 Cowork 요청 시. 발송 직후 실사용 관찰 시작.
3. **베타 라이브 실측 게이트**(발송 직전 육안): PC 1280px 5면 정합·375px 회귀 0·로그인 후 복귀·판정 컷 출처/PER 기준/상대 문구가 headline과 모순 없는지·pending 집계 제외.
4. **이메일 브리핑 실사용 관찰 + Resend 열람률 누적** → 푸시/스토어 착수 여부 게이트.

### 남은 관찰 항목 (베타 중 지켜볼 것 — 블로커 아님)
- **라이브=선계산 borderline 갈림**: 저장값이 직전 크론 산출이라 컷 경계 종목(예 SK하이닉스 PER 26.03 vs 컷 26.86)은 라이브(재계산)와 하루 어긋날 수 있음 — 로직 동일, 다음 크론서 수렴.
- **`/en` 관심목록 비미국 종목명**·**사이트맵 hreflang**·**존재하지 않는 심볼 404 전환**(현재 noindex).

### 백로그 / 알려진 이슈
- (백로그) 🇯🇵 JP 렌즈 선계산+진입 재개·프레시니스(US 패턴)·이메일 인증 R4 토글 ON·운영모델(읽기전용 서브에이전트).
- 🐛 VN/JP 공시 요약 소스 이슈(797로 `fetch_failed` 섹션 숨김·거짓 "없음" 없음).

## 워크플로우 (역할 혼용 금지)
- **Cowork = 두뇌**: 대화·설계·문서/명령 작성 · DB는 Supabase MCP(`ccbwxcszdoyjxvckedfp`) 직접. **실행·빌드·git 안 함.**
- **Claude Code = 손**: STEP 실행·빌드·git. `cd ~/stock-terminal && claude --dangerously-skip-permissions --model sonnet`(Opus는 🔴 표시 시).
- 세션 종료 = STATE 덮어쓰기 + CHANGELOG 블록 + SYSTEM_MAP(아키텍처 변경 시) + git push.
