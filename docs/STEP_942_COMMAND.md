<!-- 2026-08-08 · Cowork 작성 · Claude Code 실행용 -->

# STEP 942 — Q0 구현 ③ 마감: **`resolveSector` 3순위를 야후로 개편 ＋ 출처·주의 신호 산출**

> **범위**: `lib/sector.ts` 3순위 교체(A안) ＋ **교차 검증 「주의」 신호** 산출 ＋ 전수 실측.
> 🔴 **화면(⑤)·섹터 컷(④)은 이 STEP에 없다.**
> **정본**: `docs/USER_QUESTIONS_2026-08-08.md` §Q Q0 · 진행표 = `docs/STATE.md` ▶다음 0번

---

## 🔴 확정된 판정 (2026-08-08 장은태 · A안)

**3순위를 「Nasdaq ∩ SEC SIC 합의」에서 「야후 `assetProfile` 단독」으로 바꾼다.**

| 순 | 출처 | 예상 건수 | 근거 |
|:--:|---|:--:|---|
| **0** | SPDR 섹터 ETF holdings | ~498 | 진짜 GICS |
| **1** | Damodaran 티커 직접 | ~311 | 정답지 대비 **99.6%** |
| **2** | Damodaran 형제클래스 / 구두점 정규화 | ~5 | 같은 회사 |
| **3** | 🔑 **야후 `assetProfile`** | ~207 | **전 구간 95.8%** (497건 채점 · 단독 최고) |
| **4** | 미분류 | ~1 | 야후 취득 실패분만 |
| 🔻 | ~~Nasdaq ∩ SIC~~ | — | 🔴 **3순위에서 내려옴 · 「교차 검증 신호」로만 보존** |

**근거 3**
1. 🔑 **2-of-3(94.8%) < 야후 단독(95.8%)** — 정확도 73~75%짜리 둘을 섞으면 **끌어내린다**
2. 3-of-3(99.2%)가 잡는 건 **세 출처가 동의할 만큼 쉬운 242건**이라 계층 추가 실익이 작다
3. 야후 오류 21건은 **전부 S&P 500**이라 0순위가 먼저 잡는다

**🔴 원칙 변경도 함께 확정됐다 — 「비워서 모른다」에서 「붙이되 출처를 밝힌다」로.**
전 구간 95.8%짜리 출처가 실측된 이상 **비우는 것이 더 정직하지 않다** — 비우면 사용자는 아무것도 모른다. 대신 **규칙 5-2 ④에 따라 출처를 결과에 실어 보내고 화면에 표기**한다.

---

## ⓪-4 4×3 기록

### ③ 자체 데이터 확인 (Cowork)

| # | 열어본 것 | 결과 |
|:--:|---|---|
| 1 | `lib/sector.ts` | `resolveSector` 0~4순위 · 3순위가 `consensus`(나스닥∩SIC) |
| 2 | `docs/probe_941_third_source.json` | 조합별 정확도 · 야후 불일치 21건 전건 · `recoveredCount`는 **붙은 건수**(정확도 아님) |
| 3 | Supabase 직접 조회 | `us_sector_yahoo` 적재 확인 · `us_sector_nasdaq` 7,127 · `us_sector_gics` 503 |

### ① 3번 검색 ＋ ⓪-5-B

```
필요한 데이터 : 없음 — 941에서 취득한 데이터로 규칙만 바꾸는 STEP
link_hub 후보 : 해당 없음 (941에서 3곳 조회·기록 완료 — Stock Analysis 404 · iShares 차단 · 야후 채택)
실제 조회     : 미실시
직접 웹검색   : 미실시
```
🔴 **못 한 축으로 명시.**

### ② 3번 검증

1. **회귀** — 🔴 `industryGroup` 모드와 역DCF 경로 **완전 불변**
2. **0~2순위 불변** — 개편은 3순위만. 0·1·2순위 건수가 941 실측(498·311·5)과 **같아야 한다**
3. **채점 재현** — 0순위 제외 채점에서 3순위 정확도가 **941의 야후 단독 95.8%와 정합**해야 한다

### ④ 3번 검수 (자기 공격)

1. 🔴 **「주의」 신호를 판정으로 만들지 말 것.** 세 출처가 갈린다는 건 **사실**이고, 어느 쪽이 맞는지는 **우리가 모른다.** 신호는 *"출처들이 엇갈립니다"*까지이며 **방향을 주지 않는다**(요약층 §4 「엇갈림」 원칙과 동일).
2. 🔴 **야후 오류가 체계적이라고 보정 규칙을 만들지 말 것.** 포장재→Materials 같은 보정은 **우리 창작**이다(창작 금지). 그 구간은 0순위가 덮는다.
3. 🔴 **커버리지 100%가 목표가 아니다.** 야후 취득 실패분은 **미분류로 둔다.** 억지로 채우지 말 것.

---

## 실행 지시

### 1. `lib/sector.ts` — 3순위 교체

```
🔴 industryGroup 모드·시그니처는 절대 불변. 0·1·2순위 로직도 불변.

변경
 - 3순위: consensus(나스닥 ∩ SEC SIC)  →  us_sector_yahoo.sector 단독
 - SectorSource 타입: "consensus" 제거하고 "yahoo" 추가
 - 반환값 확장:
     { sector, source, crossCheck }
   crossCheck = {
     nasdaq: string|null,     // GICS로 옮긴 값 (Miscellaneous는 null)
     sic:    string|null,     // SIC 최빈 섹터 (최빈 비중 70% 미만이면 null)
     yahoo:  string|null,
     disagree: boolean        // 🔴 값이 있는 출처가 2개 이상이고 서로 다르면 true
   }
   🔴 crossCheck는 「사실」이다. 어느 쪽이 맞는지 판정하지 않는다.
   🔴 채택된 sector와 crossCheck는 독립이다 — crossCheck가 sector를 바꾸지 않는다.
```

### 2. 테스트 (`lib/sector.test.ts`)

1. 3순위가 야후를 쓴다 (나스닥·SIC만 있고 야후 없으면 → **미분류**)
2. 🔴 **0순위 우선 불변** — SPDR과 야후가 다르면 SPDR이 이긴다
3. `crossCheck.disagree` — 두 출처가 다르면 `true`, 하나만 있으면 `false`
4. 🔴 **`crossCheck`가 `sector`를 바꾸지 않는다**
5. 🔴 **회귀**: `industryGroup` 모드 기존 테스트 **전부 통과**
6. 🔴 **회귀**: 형제 매칭 테스트(940에서 오매칭 수정분) **전부 통과**

### 3. 실측 리포트 (`scripts/probe_942_final_resolve.ts` → `docs/probe_942_final_resolve.json`)

1. **출처별 건수** — 0~4순위 · 커버리지
   🔴 **0·1·2순위는 941 실측(498·311·5)과 같아야 한다.** 다르면 멈추고 보고
2. **0순위 제외 채점** — 순위별 정확도(SPDR 정답지 대비)
3. 🔑 **`disagree=true` 건수와 전건 목록** — 심볼 · 채택 섹터 · 각 출처가 뭐라 했는지
4. **섹터별 종목 수** — 🔴 **④단계(섹터 컷)의 직접 입력.** 하위 섹터가 몇 개까지 올라왔는지
5. **미분류 잔여** — 전건 목록과 사유

**Cowork 사전 추정 (🔴 맞추려 하지 말 것)**

| 항목 | 추정 |
|---|---|
| 커버리지 | **약 1,020 / 1,021** |
| 3순위(야후) | ~207 |
| `disagree=true` | 🔴 **모름** — 처음 재는 값 |
| 섹터별 최소 | 🔴 **모름** — 941의 하위 6개가 40~50이었으니 그보다 오를 것 |

🔴 **939·940·941에서 Cowork의 사전 추정이 세 번 다 빗나갔다**(490/488→494/492 · 형제 ~10→35→5 · 나스닥 단독 46.9%→73.7%). **실측이 이긴다. 숫자를 맞추려고 규칙을 바꾸지 말 것.**

### 4. 문서

- `docs/CHANGELOG.md` **(99) STEP 942** — 출처별 건수 · 채점 · `disagree` 건수 · 섹터별 최소값을 **수치 그대로**
- `docs/STATE.md` ▶다음 0번 — **③단계 ✅ 완료로 갱신 ＋ ④(섹터 컷)이 다음임을 명시** · 🔴 **④ 착수 전 §7 ⓖ(모집단 1,021 vs 604) 판정 필요**를 함께 적을 것
- `lib/revdcf/registry.ts` — 3순위 출처 변경 반영
- `docs/STEP_LEDGER.md` 등재

🔴 **`CLAUDE.md` · `docs/USER_QUESTIONS_2026-08-08.md` · `docs/LENS_COMPLETION_STANDARD.md`는 고치지 않는다** (Cowork이 942 판정분을 이미 반영했다).

---

## 🔴 금지 (전부 불변)

- `lib/sector.ts`의 **`industryGroup` 모드·시그니처 — 불변**
- **0·1·2순위 로직 — 불변** (940의 형제 오매칭 수정분 포함)
- 화면·UI 코드 — 손대지 않는다
- 테이블 신설·삭제 — **없음**(기존 3개 읽기만)
- `revdcf_results` · `us_market_cap` · `lens_scores` · `lens_cuts` — **쓰기 금지**
- `REVDCF_ENABLED` · `data/us_symbols.json` · `.github/workflows/**` · `vercel.json` — 불변
- 크론 수동 실행 — **금지**(특히 `email-brief`)
- `probe_*` 기존 파일 — 불변
- KR 관련(`ACTIVE_MARKETS` · KR 크론 3개 · `messages/ko.json` · `messages.test.ts` 패리티) — **끄지 않는다**
- 🔴 **API 키·비밀번호를 어떤 필드에도 입력하지 않는다**
- 🔴 **야후 오류 보정 규칙 신설 금지** (포장재→Materials 등 — 창작이다)

## 성공 기준

1. 3순위가 야후로 교체 · `crossCheck` 반환 · **0·1·2순위 건수가 941과 동일**
2. `npm test` 전체 통과 (신규 6건 ＋ 기존 회귀 전부) · `npx tsc --noEmit` 통과
3. 🔑 **실측 리포트 5항목** — 특히 **`disagree` 전건 목록**과 **섹터별 종목 수**
4. 역DCF 경로 diff 0 · 금지 경로 diff 0

## 🔴 막히면

**추측해서 진행하지 말고 멈추고 보고할 것.** 특히 ① 0·1·2순위 건수가 941과 다름 ② 3순위 정확도가 941의 95.8%와 크게 어긋남 ③ 커버리지가 95% 미만 — 이 셋은 **반드시 멈춘다.**
