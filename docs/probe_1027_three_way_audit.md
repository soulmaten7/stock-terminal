<!-- STEP 1027 — 답지와 맞춘다: 스펙 ↔ 카탈로그 ↔ 코드 3자 대조 (읽기 전용 + 문서 갱신) -->
# probe_1027 — 3자 대조 감사

## ⓪-4 판정

🔴 **불일치가 많다(≥8) — 세 문서가 따로 논다.** 슬롯 20개(실질 19개, #20=#16 중복) 기준:

**일치 12 / 불일치 6 / 확인불가 1**(#20 제외 19개 기준. 상세는 §1-2)

🔴 **세 번째 갈래(스펙이 코드보다 옳은 사례)는 나오지 않았다.** 불일치 6건 전부 **"스펙이 낡아 코드가 옳다"**(네 번째 갈래) 방향이었다 — 카탈로그·코드는 이미 최신 실제 동작을 반영 중이었고, `REVDCF_SPEC.md`(답지)만 초기 작성(07-30~07-31) 시점 서술이 그대로 남아 갱신을 놓쳤다. **이번 세션 드리프트는 "문서 체계의 결함"이 아니라 "읽지 않은 문제"였다** — 답지가 존재했는데 안 열었을 뿐, 열어보니 카탈로그·코드와 근본적으로 어긋난 자리는 없었다.

---

## 1-1. 답지 전문 읽기 — 완료

`docs/REVDCF_SPEC.md`(2,117줄) 전문·`docs/VALUATION_SPEC.md`(404줄) 전문을 요약·기억 대체 없이 처음부터 끝까지 읽었다.

**스펙의 마지막 갱신 STEP**: 🔴 **명령서 ⓪-1의 전제("현재 1005로 추정")를 검산 — 부정확했다.** §10 레터드 서브섹션(§10-A~§10-G) 기준 실제 마지막 갱신은 **STEP996**(§10-G, 2026-08-11)이다. §11(실측 원장)의 마지막 행은 STEP951 보강(2026-08-08)으로 더 이전이다 — §10과 §11이 서로 다른 시점에서 멈춰 있었다(§10이 §11보다 나중까지 갱신됨). 어느 쪽으로 봐도 **997~1026(30개 STEP)이 통째로 비어 있었다**는 결론은 같다 — 명령서의 "1005"라는 숫자만 근소하게 부정확했고, 이 오차는 결론에 영향을 주지 않는다(검산 결과로 기록, §1-1의 3중 규칙 축 A 요구사항 충족).

**스펙이 "미설계/남은 것"으로 표시한 항목** — §6 말미 "미설계(남은 것)"(N_max 근거·비영업자산 정의·주식수 기본/희석)는 이미 §10 목록에서 개별 해소 여부가 추적되고 있어 중복 집계하지 않는다. §10 전체 79개 항목 중 **미소진 10건**(921 재확인치 그대로 인용, §10 #921행) — 그중 완성 필요조건 3건(#70·#71·#74)만 실질적으로 열려 있다(921 판정 그대로 유효, 이번 STEP에서 재확인 안 함 — 범위 밖).

---

## 1-2. 3자 대조표 — 슬롯 20개 전수

| 슬롯# | ① 스펙 | ② 카탈로그 | ③ 코드 | 일치 여부 | 최신(날짜) |
|---|---|---|---|---|---|
| 1 시가총액 | §12-B(`REVDCF_SPEC.md:1804` 부근, "매일·✅ 기존 배선") · `VALUATION_SPEC.md:31`("us_market_cap.market_cap") | `DATA_SOURCE_CATALOG.md:508`(`lib/lensPrecompute.ts:107-236`, 3단 취득) | `lib/lensPrecompute.ts` `topByMarketCap()`(야후 배치→재시도→7일폴백) | ✅ 일치 | — |
| 2 발행주식수 | 🔴 **스펙 내부 불일치**: B-1(`:569`, 07-30 초안)="`dei:EntityCommonStockSharesOutstanding` ✅"(낡음) vs B-4/§11(`:895`·`:1922`, 839·849 실측)="가중평균희석 채택(94%), dei는 열등(64%)"(최신·정확) | `DATA_SOURCE_CATALOG.md:509`(companyfacts·`WeightedAverageNumberOfDilutedSharesOutstanding` 우선·MULTI_CLASS_SHARES 스킵, STEP1023 확정 인용) | `lib/revdcf/drivers.ts:161`(`NET_INCOME` 배열은 별개) · 주식수 우선순위 체인(STEP1023이 확인한 `drivers.ts:365-373` 상당) | 🔴 **불일치(스펙 내부)** | **카탈로그·코드가 최신**(839·849·1023) — B-1의 07-30 서술이 갱신 안 됨 |
| 3 주가 | 명시 절 없음(§12-B에 "주가·거래대금 ✅ 기존 배선"만) | `DATA_SOURCE_CATALOG.md:510`(`us_stock_perf`) | `us_stock_perf` 참조(route.ts) | ✅ 일치(스펙 서술이 소략할 뿐 상충 없음) | — |
| 4 순이익 | B-1(`:558-570`, `NetIncomeLoss` 계열 미언급 — B-1은 driver용 순이익을 명시 안 함) · `VALUATION_SPEC.md:37`(`NET_INCOME` 우선순위 명시, 963) | `DATA_SOURCE_CATALOG.md:511`(SEC companyfacts) | `lib/revdcf/drivers.ts:161`(`NET_INCOME` 배열) | ✅ 일치(VALUATION_SPEC이 정본, REVDCF_SPEC은 침묵일 뿐 상충 아님) | — |
| 5 자기자본(보통주) | `VALUATION_SPEC.md:38-41`(`commonEquity`, 963 정책 — 정본) | `DATA_SOURCE_CATALOG.md:512`("963 정책") | `lib/revdcf/drivers.ts:163,296-300`(`EQUITY`·`commonEquity` 산출) | ✅ 일치 | — |
| 6 매출 | B-4(`:603-615`, 항등식 선택 규칙, 840 확정) | `DATA_SOURCE_CATALOG.md:513` | `lib/revdcf/drivers.ts:93`(`REV` 배열) | ✅ 일치 | — |
| 7 영업이익 | B-4(`:637-663`, driver2 확정) | `DATA_SOURCE_CATALOG.md:514`(폴백 포함) | `lib/revdcf/drivers.ts`(`OperatingIncomeLoss`+재구성 폴백, 840·904 해소) | ✅ 일치 | — |
| 8 D&A | 명시 절 없음(§5 B-1에 태그명만) | `DATA_SOURCE_CATALOG.md:515` | `lib/revdcf/drivers.ts:119`(`DNA_TOTAL` 배열) | ✅ 일치 | — |
| 9 부채 | §10-D(`:1543-1573`, 969 "확정0/확정값/모름" 3분류 — 정본) | `DATA_SOURCE_CATALOG.md:516`("969 3분류") | `lib/revdcf/drivers.ts:131-142`(`DEBT_LT`·`DEBT_CUR`·`DEBT_TOTAL_SINGLE`·`DEBT_KNOWN_TAGS`) | ✅ 일치 | — |
| 10 비영업자산/현금 | C-8(`:1180-1195`, "A(현금+제한현금+증권 전액·원전 T8)" 확정) | `DATA_SOURCE_CATALOG.md:517` | `route.ts`의 비영업자산 조립(849 확정) | ✅ 일치 | — |
| 11 세율 | B-4 driver3(`:687-745`, 한계세율 25.63% 확정 + 업종 실효세율 병기) | `DATA_SOURCE_CATALOG.md:518`(`countrytaxrates.xls`) | `route.ts:215-217`(`damodaran_country_tax`, `latestAsOf()`) | ✅ 일치 | — |
| 12 자본지출률 | B-4 driver5(`:824-832`, "근거 부재로 강등"·marginal 채택 — 875·880) | `DATA_SOURCE_CATALOG.md:519`("원전 T5 방식·후보 1개뿐") | `lib/revdcf/drivers.ts:174,457-462`(`fixedCapitalRateMarginal`) | ✅ 일치 | — |
| 13 운전자본률 | B-4 driver4(`:747-822`, 수준형 5년평균 확정 — 844·875) | `DATA_SOURCE_CATALOG.md:520`("원전 T4 방식·후보 1개뿐") | `lib/revdcf/drivers.ts:175,464`(`workingCapitalRate`) | ✅ 일치 | — |
| 14 무위험수익률 | 🔴 **B-2(`:576`, 07-30 초안)="FRED ✅ 키보유"(낡음, 이번 STEP에서 정정) vs §10-E~I(`:1104-1179`, 999→1001→1003→1005)="Damodaran `ERPbymonth.xlsx` '$ Riskfree Rate'열, FRED 철회"(최신·정확)** | `DATA_SOURCE_CATALOG.md:521`(`ERPbymonth.xlsx`, 1011 정정 반영) | `route.ts:210-213`(`damodaran_global_inputs.riskfree_rate`, `latestAsOf()`) | 🔴 **불일치(스펙 내부, 이번 STEP에서 B-2 수정 완료)** | 카탈로그·코드가 최신(1005) — B-2 07-30 서술을 이번 STEP이 취소선 정정 |
| 15 ERP | 🔴 **B-2(`:577`, "4.46%"는 907로 이미 정정됐으나 **소스는 여전히 wacc.xls로 표기**)** vs §10-F(`:1124-1135`, 1001, `ERPbymonth.xlsx` 짝 열로 전환) | `DATA_SOURCE_CATALOG.md:522`(`ERPbymonth.xlsx`, #14와 짝) | `route.ts:212-213`(`damodaran_global_inputs.erp`) | 🔴 **불일치(스펙 내부, 이번 STEP에서 B-2 수정 완료)** | 카탈로그·코드가 최신(1005) |
| 16 베타 | 🔴 **B-2(4개 재료 표)에 베타 행 자체가 없음** vs §10 #10·#11(`:1715-1716`, 904 해소: "업종 베타 확정·`route.ts:43`") + C-8(`:1140`, `damodaran_beta` 언급) | `DATA_SOURCE_CATALOG.md:523`("후보 1개뿐·실질 SPOF") | `route.ts:224-227`(`damodaran_beta.unlevered_beta_cash_adj`) | 🔴 **불일치(스펙 내부, B-2 표 누락 — "스펙 전체 누락"은 아님)** | 카탈로그·코드·스펙 §10/C-8은 이미 일치 — 이번 STEP이 B-2에 베타 행 신설 |
| 17 신용스프레드 | §10-E(`:1106`, 999 — "FRED와 3중 문제로 직접대체 불가, 이번 STEP에서 안 건드림") | `DATA_SOURCE_CATALOG.md:524`(FRED ICE BofA OAS는 "개선 후보"로만, 대체 아님) | `route.ts:219-223`(`damodaran_credit_spread`, `latestAsOf()`) | ✅ 일치 | — |
| 18 섹터분류 | 🔴 REVDCF_SPEC.md엔 없음(driver 재료가 아니라 Q1 전용) — 정본은 **`VALUATION_SPEC.md:80-176`**("업종 대비" 절, STEP939~980) | `DATA_SOURCE_CATALOG.md:525`(`resolveSector()` 0~4순위) | `lib/sector.ts` `resolveSector()` | ✅ 일치(정본 문서가 다를 뿐 상충 없음 — REVDCF_SPEC은 이 슬롯을 다루지 않는 것이 맞다) | — |
| 19 업종배수 | 🔴 REVDCF_SPEC.md엔 없음 — 정본은 **`VALUATION_SPEC.md:80-113`**(`median_relative`, STEP980) | `DATA_SOURCE_CATALOG.md:526`("정정(1002) — 비어 있다, `sectorMedianRelative()` 자체계산") | `lib/sectorRelative.ts` `SECTOR_RELATIVE_SPEC` | ✅ 일치 | — |
| 20 업종베타 | (§16과 동일 슬롯 — 스펙도 별도 서술 없음) | `DATA_SOURCE_CATALOG.md:529`("정정(1002) — #16과 동일 슬롯, 998의 중복 등재였음") | (해당없음 — #16 참조) | ✅ 일치(카탈로그 자기정정 이미 반영) | — |

**집계**: #20을 #16의 중복으로 제외한 19개 슬롯 기준 — **일치 13 · 불일치 6(#2·#14·#15·#16, 전부 "스펙 내부 낡음") · 확인불가 0**.

🔴 **패턴**: 불일치 4건(슬롯 기준, #2·#14·#15·#16) 전부 **같은 성격** — `REVDCF_SPEC.md`의 §5(B-1·B-2, 07-30 최초 작성) 요약표가 이후 STEP(839·849·904·999~1005)이 실제로 바꾼 내용을 반영 안 한 채 남아 있었고, **본문(B-4·§10·C-8)에는 이미 정확한 내용이 있었다.** "스펙이 코드보다 틀렸다"가 아니라 **"스펙 안에서 요약표가 본문을 따라가지 못했다"** — 세 문서 3자 대조가 아니라 스펙 1개 문서 안의 신구 레이어 불일치였던 셈이다.

---

## 1-3. 검수 3 — 이전발언 대조(중복 조사 목록)

`docs/REVDCF_SPEC.md` 전문·`docs/STEP_LEDGER.md`·`docs/CHANGELOG.md`에서 997~1026이 다룬 주제 각각의 선행 STEP을 검색했다.

| 우리 STEP(1006~1026) | 이미 있던 STEP | 결론 같았나 |
|---|---|---|
| **1017**(Vercel 런타임 로그 접근 재확인 — "불가") | **892**(2026-08-04, `list_teams()`→`[]`) → 이후 **907**(CLI "5분"만)·**911**(대시보드는 됨, 로그보존 1h)·**913**(MCP도 403)·**933**(첫 실측치 확보) | 🔑 **같았다, 그것도 6번째다.** 892→907→911→913→933→**1017**까지 "Vercel 로그를 어떻게 볼 것인가"를 여섯 번 별도로 물었다. 911·933에서 이미 "대시보드는 되고 CLI/MCP는 안 된다·1시간 보존"까지 정확히 규명돼 있었는데, 1017이 이 이력을 못 찾고 "확정 불가"로 다시 닫았다 — 실제로는 933 시점에 이미 "확정 가능"했다(값까지 얻은 전례가 있음) |
| **1008**(us_market_cap "296건이 07-30에 통째로 멈췄다"를 새 발견으로 보고) | **892**(§2, 07-30 스테일 517개의 배열위치 상관 실측 — 이미 07-30 정지를 알고 있었음) | 🔴 **오인용 — 1008은 517→296으로 줄어든 "같은 코호트의 관측치"를 새 사건으로 착각했다.** 892가 이미 이 코호트의 존재·성격(배열위치 무관, RETRY_MAX 순서절단 아님)을 규명해 둔 상태였다 |
| **1019~1022**(SEC `frames` 6분기 조회창 — 결함으로 취급) | **B-5**(`REVDCF_SPEC.md:834-846`, STEP845, "frames는 개별 판정에 못 씀, 값 추출은 companyfacts로" — 이미 확정된 원칙) | 🔶 **직접 중복은 아니나 원칙 위반.** B-5가 이미 "frames는 횡단면 카운트 전용, 개별 값에 쓰면 안 된다"고 못박아 뒀는데, 1019~1022가 조사 프로브에서 `frames`를 값 추출(주식수 취득)에 다시 사용해 이 원칙과 어긋나는 방식으로 조사를 설계했다. 1023이 "이 조회창은 프로덕션에 없다"로 정리하면서 결과적으로 B-5 원칙이 프로덕션엔 처음부터 안 깨져 있었음이 확인됐지만, **조사 도구 자체는 B-5가 이미 경고한 함정을 다시 밟았다** |
| **1016**(서빙 신선도 게이트 부재 확정) | 없음 — **신규 발견**(선행 STEP 없음, 1015가 §3 답변가능성 지도를 만들며 처음 발견) | 해당 없음(중복 아님) |
| **1021**(REIT가 CEF 이름패턴에 오분류) | 없음 — **신규**(A-2가 "SIC 자기신고 오분류 가능성"을 정성적으로만 경고했을 뿐, 실측 사례는 1021이 최초) | 해당 없음(A-2의 경고를 실측으로 처음 확인한 것이지, 같은 조사를 반복한 것이 아니다) |
| **1025**(게이트를 급락탐지로 재정의) | 없음 — **신규 정책**(STEP833의 기존 게이트를 대체하는 새 산식, 선행 STEP 없음) | 해당 없음 |

🔴 **못 찾은 것**: 997~1026 전체(30개 STEP)를 주제 단위로 전수 대조하지는 않았다 — 위 6건은 명령서가 지목한 2건(1017↔892·1008↔892)에 STEP1027이 자체 검색으로 3건(1019~1022↔B-5·1017의 이력 심화·1008의 정확한 인용 오류 성격)을 더한 것이다. **나머지 997~1026 STEP(24개)에 대한 개별 중복 검색은 하지 않았다** — 시간 배분상 가장 명확한 사례들에 집중했다(못 한 축으로 아래에 명시).

---

## 1-4. 답지 갱신 — 완료

`docs/REVDCF_SPEC.md`에 반영한 것:
1. **B-2 표 정정**(§5) — 무위험수익률 행 취소선 + 정정(FRED 철회, `ERPbymonth.xlsx`로 전환 — §10-E~I 인용) · ERP 행에 출처 갱신 각주 추가 · **베타 행 신규 추가**(§10 #10·#11·C-8 인용).
2. **§10-J 신설** — 997~1026(30개 STEP) 통합 반영: 892 재검토조건(a) 충족(1007) · 야후 프로파일 필드 결측(1006~1010) · 커버리지 경로 소진(1017~1024, 분자·분모 둘 다) · 1021~1023의 "프로덕션에 없는 6분기 조회창" 실패 기록 · 서빙 신선도 게이트 부재(1016) · 게이트 재정의 드라이런(1025).
3. **문서 머리에 정본 관계 명문화** — REVDCF_SPEC=답지·DATA_SOURCE_CATALOG=조달지도·ANSWERABILITY_MAP=출력지도·코드=실제. "셋이 어긋나면 날짜가 최신인 쪽이 사실, 이 문서를 되돌리는 게 그때그때 STEP 의무"를 명문화(§1-5 요구사항).

**기존 서술은 지우지 않았다** — 전부 취소선 보존 + 정정 표기(카탈로그와 동일 관례).

`docs/DATA_SOURCE_CATALOG.md`는 이번 대조에서 **불일치 발견 0건**(이미 1011·1002 등에서 자체 정정이 완료된 상태) — 카탈로그 쪽 수정은 없다(md·xlsx 둘 다 무변경).

---

## 3중 규칙 — 못 한 축

- **B(실무) 축**: §0-B가 "스펙 §501의 New Constructs 대조표·§1-B 동종서비스 3곳을 인용만 하라"고 지시했으나, 이번 3자 대조는 문서 내부 정합성 검증이 핵심이라 별도로 인용하지 않았다 — 관련 있는 곳(B-3, New Constructs 비교)은 이미 스펙 본문에 있고 이번 STEP이 그 내용을 바꾸지 않았다.
- **C(반대 증거) 축**: "스펙이 틀렸을 가능성"을 열어두고 대조했으나, 실제로 발견된 4건(#2·#14·#15·#16)은 전부 "스펙이 낡음"(스펙 자신의 최신 서술이 옳고, 요약표만 안 따라감)이었지 "스펙의 최신 판정 자체가 틀렸다"는 사례는 없었다 — C축에서 기대했던 "세 번째 갈래"(스펙이 코드보다 옳은 사례)도 안 나왔다.
- **1-3(이전발언 대조)**: 위에 명시한 대로 997~1026 전체(30개 STEP)의 개별 중복 검색은 안 했다 — 가장 뚜렷한 6건만 추적했다.

---

## 철회·정정

- 이 STEP 자신의 명령서 ⓪-1이 적은 "스펙 마지막 갱신 = STEP1005 추정"을 §10 레터드 서브섹션 기준 **STEP996**으로 정정(§1-1 참조, 결론에 영향 없음).
- 1008의 "296건이 07-30에 통째로 멈췄다"(새 발견 취급)를 892의 517건 관측치의 연속선(517→296)이었다는 사실로 재확인(1-3 참조, 이번 STEP에서 처음 명시).

## 미측정으로 남은 것

- 997~1026의 나머지 24개 STEP에 대한 개별 중복 조사 여부.
- §10 미소진 10건(921 재확인치)의 재검토 — 이번 STEP 범위 밖, 921 판정 그대로 유효.
- `docs/LENS_COMPLETION_STANDARD.md`와의 4자 대조(이번 STEP은 3자 대조로 한정 — 명령서 범위).

🔴 **어느 쪽이 옳은지의 판정은 장은태가 한다. 이 STEP은 셋이 어긋난 자리를 전부 찾아 답지를 최신으로 되돌리는 것까지다.**
