<!-- 2026-09-05 신설 · docs/ORDER_트릴리언이관_0905.md STEP2 -->
# 실수 기록 (mistakes)

> 재발 방지를 위한 누적 기록. 새 항목은 여기 추가한다(옛 `docs/LENS_DEV_PLAYBOOK.md`의 "문제해결 로그" 역할을 승계 — 단 그 문서는 읽기 참조로 유지, 새 항목은 여기로). 형식: **무엇이 틀렸나 / 왜 / 어떻게 알았나**. 짧게, 조건부로("이 조건에선…") — "무조건 맞다"로 일반화하지 않는다.

---

## 2026-09-05 배운 것

- **서비스롤(비밀) 키는 채팅·명령어에 직접 붙이지 않는다 — 파일 편집기로 넣는다.** `read -rs NEW_K...` 방식으로 셸에서 값을 받아 붙이려다 두 파일(`.env.local`·stock-shorts `config/.env`)에서 셸 히스토리 잔재가 실제 키 값과 꼬여 붙는 사고가 났다(`SUPABASE_SERVICE_ROLE_KEY=read -rs NEW_K...sb_secret_...` 형태로 오염). 복구는 가능했지만(양쪽 오염 패턴 대조로 원래 값 역추정 후 curl로 검증) 애초에 셸 인자·대화 로그에 비밀 값이 스치지 않게, 파일을 직접 열어 편집하는 방식으로 넣어야 한다. **적용**: 키·토큰 교체 요청이 오면 `read -rs`/echo 파이프 대신 Edit 도구로 파일을 직접 고친다.
- **"모델을 다 걷어냈다"는 인식만으로 크론을 끄지 않는다 — 전수 조사 없이 "다 됐다"고 판단하면 살아있는 소비처를 놓친다.** 모델 잔재 전면 제거(85356be) 직후 "이제 렌즈 크론도 끌 수 있겠다"고 생각했으나, 실제 전수 조사(`lens-cron-consumers-audit` 포크)에서 홈의 "내 관심종목·렌즈 변화" 섹션이 `lens_state_changes`를 그대로 쓰는 라이브 소비처로 남아있었다 — 화면 잔재 제거와 데이터 소비처 제거는 별개 질문이라, 화면을 치웠다고 곧장 크론을 끄면 안 됐다. **적용**: 크론·API·테이블을 정지하기 전에는 "이 산출물을 지금 읽는 코드가 남아있는가"를 grep으로 전수 확인하고, 라이브/파킹/죽음 상태를 소비처 단위로 분류한 뒤에만 정지한다.
- **명령서(ORDER) 표에 빈칸을 남기지 않는다 — 실행자가 채우게 되면 그 자체가 임의 결정이 된다.** 판단이 안 끝난 항목을 빈칸이나 "TBD"로 두고 넘기면, 실행 단계에서 누군가(사람이든 에이전트든) 자기 판단으로 채우게 되고 그게 사실상 미승인 결정이 된다. **적용**: 명령서 표는 셀을 전부 채우고 낸다 — 정말 미정이면 빈칸이 아니라 "확정 대기(사유)"라고 명시해, 실행자가 자기 판단으로 채우지 못하게 한다.

## 2026-09-07 배운 것

- **fire-and-forget 트리거에서 pg_net 기본 타임아웃(5초)이 실제 처리시간보다 짧으면, 성공도 timeout으로 로깅돼 진짜 실패와 구분이 안 된다.** `channel_growth_stories`/`channel_reports`의 AFTER INSERT(OR UPDATE) 트리거가 `net.http_post()`로 번역 API를 비동기 호출하는데, 이 함수의 `timeout_milliseconds` 기본값은 5000이다. 실제 처리(원문 조회 + OpenAI 번역 호출 + 저장)는 6~8초 걸려서, `net._http_response`에는 **성공한 호출도 실패한 호출도 똑같이 `status_code: null, error_msg: "Timeout of 5000 ms reached"`**로만 남았다 — 트리거 자체 동작(Vercel 함수는 서버에서 계속 실행돼 정상 완료)엔 영향이 없었지만, 로그만 보고는 "이번 호출이 진짜 실패했는지" 전혀 판단할 수 없는 상태였다. 어떻게 알았나: `channel_growth_stories` 8건에 무해한 UPDATE로 트리거를 재발화해 실측 — 8건 다 `net._http_response`는 timeout으로 찍혔는데 `channel_growth_story_translations`엔 8건 다 `status='ok'`로 정상 저장돼 있어 모순을 발견. **적용**: `net.http_post()`를 쓰는 모든 fire-and-forget 트리거는 `timeout_milliseconds`를 관측된 실제 처리시간보다 충분히 크게(이번엔 15000) 명시한다 — 기본값 5000을 그대로 두면 안 된다. 수정 후 재발화로 `status_code: 200`이 찍히는 것까지 확인해야 로그를 다시 신뢰할 수 있다.
- **"영상 아직 안 만든 리포트"용 합성 dedup 키(`report_{report_idx}`)가 나중에 영상이 만들어져 진짜 키(`YYYYMMDD_종목명`)로 다시 push돼도 회수되지 않아 중복 행이 남는다.** `/reports?country=KR`에서 실리콘투·에이피알·코스맥스가 각각 2건씩 보였다(하나는 title 있음, 하나는 없음) — `channel_reports`를 직접 대조한 결과 완전한 중복 INSERT가 아니라, 같은 리포트(동일 symbol·report_date·broker·target_price/current_price/upside)가 **서로 다른 `episode_folder` 값으로 두 번 push**된 것이었다: 첫 push(00:30, 영상 제작 전)는 `report_652165`류 합성 키로 title=null·reasons=[]만 담아 들어오고, 두 번째 push(03:00, 영상 제작 후)는 진짜 `20260907_실리콘투`류 키로 title·reasons까지 채워 들어온다. `episode_folder` UNIQUE 제약은 이 두 값을 서로 다른 리포트로 보므로 upsert가 안 걸리고 그대로 둘 다 남는다(채널 쪽 `scripts/trillion_push.py`의 `find_episode_folder()`가 실제 폴더를 찾으면 그 값을 재사용하도록 설계돼 있지만, 그 값이 처음 합성 키로 들어간 옛 행을 찾아 지우거나 병합하는 단계가 없다 — 원인은 stock-shorts 저장소에 있어 이번엔 이 저장소에서 데이터만 정리했다, 코드는 안 건드림). 실측: 3쌍 다 target_price/current_price/upside가 완전히 동일해 같은 리포트임을 확인 후 title=null·reasons=[] 쪽(옛 합성 키 행) 3건만 삭제, title 있는 쪽만 남김. `090430`(아모레퍼시픽)·`161890`(한국콜마)처럼 아직 영상이 안 나온 건은 합성 키 행이 유일한 표현이라 삭제 대상이 아니다(대조 없이 지우면 데이터 소실). **적용**: 이런 "중복처럼 보이는" 행을 볼 때는 삭제 전에 반드시 실제 값(가격·비율 등 정량 필드)을 대조해 정말 같은 리포트인지 확인한다 — episode_folder가 다르다고 곧장 별개 리포트로, title이 없다고 곧장 최신 아님으로 단정하지 않는다.
- **번역 검증의 퍼센트 하드 게이트가 "%p"(퍼센트포인트) 표기를 오탐으로 막았다.** 원문 "원화 강세가 영업이익률에 미치는 영향이 1%p 미만"을 영어로 옮기며 LLM이 "%" 기호 없이 "1 percentage point"로 풀어 쓰자, 원문 쪽은 `\d+%` 정규식에 "1%"가 걸려 개수 1인데 번역문 쪽은 "%" 자체가 없어 개수 0 — `percent_mismatch`로 저장이 막혔다(에이피알 리포트, `report_id=176`, 화면엔 원문 한글 제목이 그대로 노출됨). 재현: 프로덕션 엔드포인트를 직접 호출해 동일 에러(`percent_mismatch: reasons[2].detail`) 재현 확인. 정확히 같은 구조의 실패 3건(`report_id 171~173`)이 더 있었는데, 그건 위 중복 행(합성 키, reasons=[])을 번역하려다 생긴 `reasons_length_mismatch`라 원인이 다르다 — 삭제로 같이 정리됨. **적용**: `translate-channel-report/route.ts`·`translate-growth-story/route.ts`의 `extractPct()`가 "%p"·"percentage point(s)"·"pp"를 별도 토큰으로 인식해 plain "%"와 같은 개념으로 비교하도록 수정(plain % 쪽은 "%p" 형태를 정규식에서 제외해 이중 카운트 방지). 숫자 무결성 검증처럼 "정답이 하나의 표기 형태만 있다"고 가정한 정규식은, 그 숫자가 언어별로 다르게 풀어 쓰일 수 있는 표기(퍼센트포인트·서수·배수 단위 등)라면 오탐을 낸다는 게 일반화된 교훈(서수·배수 단위는 이미 별도 처리돼 있었는데 %p만 빠져 있었다).

## 확인 필요

- **`docs/LENS_DEV_PLAYBOOK.md` 항목 수 불일치** — `CLAUDE.md`(구)·`docs/COMMIT_GATES.md:4`는 "87항목"이라 인용하지만, 실측(`grep -c "^| " docs/LENS_DEV_PLAYBOOK.md` 및 항목 번호 육안 확인)하면 §2 "문제해결 로그" 표가 **#118까지** 있다. 어느 시점 서술이 갱신 안 된 채 남아있는지, 실제 총 항목수가 몇인지 확인 필요(2026-09-05 STEP1 조사 중 발견, 이번 STEP 범위 밖이라 정정 보류).
