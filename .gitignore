# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Claude Code 로컬 설정 (사용자별 허용 명령어 — push 불필요)
.claude/settings.local.json
data/ff/
.env*.local

# scratch
tmp/
scripts/_*.ts
data/_*.json
*handoff.zip
.next-verify/

# STEP 846: 다모다란 원본 xls는 Supabase Storage(sources 버킷)에 보관 — git 경량화(22MB 제외). 로컬 파일은 유지.
data/sources/damodaran/

# STEP 847: 대용량 companyfacts 원시 캐시(8.7MB·재실행으로 재생성) — 요약 산출물만 커밋
docs/probe_847_raw.json
